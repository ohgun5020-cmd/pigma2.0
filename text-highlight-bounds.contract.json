{
  "sourceFile": "ai-typo-audit.js",
  "uiFile": "ui.html",
  "bundleFile": "code.patched.js",
  "buildScript": "build-patched-main.ps1",
  "requiredFunctions": [
    "measureTextHighlightBounds",
    "measureAlignmentSensitiveTextHighlightSelectionFromLeftLayout",
    "reconcileAlignmentSensitiveTextHighlightRowsWithEstimate",
    "getEstimatedTextHighlightSoftLineIndexForSelection",
    "isTextHighlightLineBreakCharacter"
  ],
  "requiredSnippets": [
    "if (isAlignmentSensitivePartialSingleLineSelection && directBoundsAreSafe)",
    "bounds: mergeTextHighlightBoundsList(directBoundsList)",
    "const estimatedLineIndex = getEstimatedTextHighlightSoftLineIndexForSelection(",
    "const measuredLineIndex = findTextHighlightSoftLineIndexForRow(",
    "The cloned node has the real Figma wrap row; the estimator is only a fallback.",
    "let lineIndex = measuredLineIndex;",
    "if (lineIndex < 0 && estimatedLineIndex >= 0 && estimatedLineIndex < leftLineRows.length)",
    "const explicitStart = hasExplicitRange ? Math.max(0, Math.floor(Number(message.start) || 0)) : 0;",
    "const explicitEnd = hasExplicitRange ? Math.max(explicitStart, Math.floor(Number(message.end) || 0)) : explicitStart;",
    "const liveRange = tryResolveLiveSelectedTextHighlightRange(hasExplicitRange ? message : null);",
    "if (message && message.preferLiveSelection === true && !hasExplicitRange)",
    "liveRange.node.id === node.id &&",
    "y: selectedLocalBounds.y",
    "if (yMismatch > tolerance)",
    "const maximumEstimatedXCorrection = Math.max(tolerance * 2, size * 0.75)",
    "return character === \"\\n\" || character === \"\\r\" || character === \"\\u2028\" || character === \"\\u2029\";"
  ],
  "requiredUiSnippets": [
    "nodeId: currentSource.nodeId",
    "start: currentSource.start",
    "end: currentSource.end",
    "sourceText: currentSource.text"
  ],
  "forbiddenSnippets": [
    "alignedReferenceLocalBounds",
    "Array.isArray(alignedLineRows)",
    "liveRange.node.id !== node.id || liveRange.start !== start || liveRange.end !== end",
    "y: yMismatch > tolerance ? estimated.y : row.y"
  ],
  "requiredOrderedSnippets": [
    [
      "const directBoundsAreSafe =",
      "if (isAlignmentSensitivePartialSingleLineSelection && directBoundsAreSafe)",
      "const underlineGeometryMeasurement = await measureTextHighlightUnderlineGeometryBounds("
    ],
    [
      "const measuredLineIndex = findTextHighlightSoftLineIndexForRow(",
      "let lineIndex = measuredLineIndex;",
      "if (lineIndex < 0 && estimatedLineIndex >= 0 && estimatedLineIndex < leftLineRows.length)"
    ],
    [
      "const explicitStart = hasExplicitRange",
      "const explicitEnd = hasExplicitRange",
      "const liveRange = tryResolveLiveSelectedTextHighlightRange(hasExplicitRange ? message : null);",
      "if (message && message.preferLiveSelection === true && !hasExplicitRange)"
    ],
    [
      "if (sourceText && compactText(sourceText) && text !== sourceText)",
      "const matchingLiveRange = tryResolveLiveSelectedTextHighlightRange(message);",
      "liveRange.node.id === node.id &&",
      "liveRange.start === start &&",
      "liveRange.end === end"
    ],
    [
      "if (yMismatch > tolerance)",
      "const maximumEstimatedXCorrection = Math.max(tolerance * 2, size * 0.75)",
      "x: xMismatch > tolerance ? estimated.x : row.x",
      "y: row.y"
    ]
  ],
  "requiredBuildSnippets": [
    "text-highlight-bounds.contract.json",
    "verify-text-highlight-bounds.js",
    "Text highlight bounds verification failed."
  ]
}
