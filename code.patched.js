"use strict";(()=>{var so=Object.defineProperty,lo=Object.defineProperties;var uo=Object.getOwnPropertyDescriptors;var Ct=Object.getOwnPropertySymbols;var co=Object.prototype.hasOwnProperty,po=Object.prototype.propertyIsEnumerable;var kt=(e,t,r)=>t in e?so(e,t,{enumerable:!0,configurable:!0,writable:!0,value:r}):e[t]=r,b=(e,t)=>{for(var r in t||(t={}))co.call(t,r)&&kt(e,r,t[r]);if(Ct)for(var r of Ct(t))po.call(t,r)&&kt(e,r,t[r]);return e},B=(e,t)=>lo(e,uo(t));var A={psdVersion:"max-compatibility",textExportMode:"editable-text",imageExportMode:"bitmap-only",hiddenLayerMode:"ignore-hidden",exportPackageMode:"psd-only",fileNamePattern:"{frame-name}.psd"},$={psdVersion:"max-compatibility",textExportMode:"editable-text",imageExportMode:"smart-object-if-possible",hiddenLayerMode:"ignore-hidden",exportPackageMode:"psd-only",fileNamePattern:"{frame-name}.psd"},U={psdVersion:"max-compatibility",textExportMode:"rasterize-text",imageExportMode:"smart-object-if-possible",hiddenLayerMode:"ignore-hidden",exportPackageMode:"psd-only",fileNamePattern:"{frame-name}.psd"},H={psdVersion:"max-compatibility",textExportMode:"rasterize-text",imageExportMode:"bitmap-only",hiddenLayerMode:"ignore-hidden",exportPackageMode:"psd-only",fileNamePattern:"{frame-name}.psd"},Mt="ko",ne={locale:Mt,localeMode:"auto",exportSettings:A};function It(e){return e?B(b({},e),{imageExportMode:e.imageExportMode===U.imageExportMode?A.imageExportMode:e.imageExportMode}):{}}function Rt(e){if(!e)return{};let t=typeof e.fileNamePattern=="string"&&e.fileNamePattern.trim().length>0?e.fileNamePattern.trim():H.fileNamePattern,r=e.psdVersion==="max-compatibility"?e.psdVersion:H.psdVersion,o=e.textExportMode==="editable-text"||e.textExportMode==="rasterize-text"?e.textExportMode:H.textExportMode,n=e.imageExportMode==="smart-object-if-possible"||e.imageExportMode==="bitmap-only"?e.imageExportMode:H.imageExportMode;return r!==H.psdVersion||o!==H.textExportMode||n!==H.imageExportMode||t!==H.fileNamePattern?e:B(b({},e),{imageExportMode:A.imageExportMode})}function At(e){if(!e)return{};let t=typeof e.fileNamePattern=="string"&&e.fileNamePattern.trim().length>0?e.fileNamePattern.trim():$.fileNamePattern,r=e.psdVersion==="max-compatibility"?e.psdVersion:$.psdVersion,o=e.textExportMode==="editable-text"||e.textExportMode==="rasterize-text"?e.textExportMode:$.textExportMode,n=e.imageExportMode==="smart-object-if-possible"||e.imageExportMode==="bitmap-only"?e.imageExportMode:$.imageExportMode;return r!==$.psdVersion||o!==$.textExportMode||n!==$.imageExportMode||t!==$.fileNamePattern?e:B(b({},e),{imageExportMode:A.imageExportMode})}function Lt(e){if(!e)return{};let t=typeof e.fileNamePattern=="string"&&e.fileNamePattern.trim().length>0?e.fileNamePattern.trim():U.fileNamePattern,r=e.psdVersion==="max-compatibility"?e.psdVersion:U.psdVersion,o=e.textExportMode==="editable-text"||e.textExportMode==="rasterize-text"?e.textExportMode:U.textExportMode,n=e.imageExportMode==="smart-object-if-possible"||e.imageExportMode==="bitmap-only"?e.imageExportMode:U.imageExportMode;return r!==U.psdVersion||o!==U.textExportMode||n!==U.imageExportMode||t!==U.fileNamePattern?e:B(b({},e),{textExportMode:A.textExportMode})}function Ft(e){let t=e!=null?e:{};return{psdVersion:t.psdVersion==="max-compatibility"?t.psdVersion:A.psdVersion,textExportMode:t.textExportMode==="editable-text"||t.textExportMode==="rasterize-text"?t.textExportMode:A.textExportMode,imageExportMode:t.imageExportMode==="smart-object-if-possible"||t.imageExportMode==="bitmap-only"?t.imageExportMode:A.imageExportMode,hiddenLayerMode:t.hiddenLayerMode==="preserve-hidden"||t.hiddenLayerMode==="ignore-hidden"?t.hiddenLayerMode:A.hiddenLayerMode,exportPackageMode:t.exportPackageMode==="bundle-with-rasters"||t.exportPackageMode==="psd-with-png"?"psd-with-png":t.exportPackageMode==="psd-with-jpg"||t.exportPackageMode==="psd-only"?t.exportPackageMode:A.exportPackageMode,fileNamePattern:typeof t.fileNamePattern=="string"&&t.fileNamePattern.trim().length>0?t.fileNamePattern.trim():A.fileNamePattern}}var fo={"내보내기 파일이 여기에 표시됩니다.":"내보내기 파일이 여기에 표시됩니다.","프레임 또는 레이어를 선택하세요.":"프레임 또는 레이어를 선택하세요.","세션 시작":"세션 시작","선택 대기 중입니다.":"선택 대기 중입니다.","PSD 만들기 준비 중입니다.":"PSD 만들기 준비 중입니다.","PSD 만들기 작업이 진행 중입니다.":"PSD 만들기 작업이 진행 중입니다.","PSD 가져오기 준비 중입니다.":"PSD 가져오기 준비 중입니다.","PSD 파일들을 선택했습니다.":"PSD 파일들을 선택했습니다.","PSD 파일을 선택했습니다.":"PSD 파일을 선택했습니다.","PSD 가져오기에 실패했습니다.":"PSD 가져오기에 실패했습니다.","PSD 가져오기 실패":"PSD 가져오기 실패","PSD 가져오기 시작":"PSD 가져오기 시작","PSD 묶음 완료":"PSD 묶음 완료","PSD 조립 실패":"PSD 조립 실패","PSD 조립 실패":"PSD 조립 실패","PSD 조립 중입니다.":"PSD 조립 중입니다.","플러그인 패널 오류":"플러그인 패널 오류","단일 합성 이미지":"단일 합성 이미지","프레임, 그룹, 레이어를 먼저 선택하세요.":"프레임, 그룹, 레이어를 먼저 선택하세요.","완료":"완료","현재 선택으로 PSD 파일을 만듭니다.":"현재 선택으로 PSD 파일을 만듭니다.","위험 메모":"위험 메모","편집 가능한 텍스트 조립이 실패해 대체 PSD로 전환했습니다.":"편집 가능한 텍스트 조립이 실패해 대체 PSD로 전환했습니다.","대체 PSD 포함":"대체 PSD 포함","내보내기 완료":"내보내기 완료","내보내기 세트 완료":"내보내기 세트 완료","내보내기 실패":"내보내기 실패","레이어 구조 유지":"레이어 구조 유지","브라우저 UI에서 최종 다운로드 묶음을 준비하지 못했습니다.":"브라우저 UI에서 최종 다운로드 묶음을 준비하지 못했습니다.","브라우저 UI에서 PSD 조립에 실패했습니다.":"브라우저 UI에서 PSD 조립에 실패했습니다.","플러그인":"플러그인","텍스트 후보":"텍스트 후보","세션 기록이 여기에 표시됩니다.":"세션 기록이 여기에 표시됩니다.","편집 가능한 텍스트 조립이 실패해 대체 PSD로 전환했습니다.":"편집 가능한 텍스트 조립이 실패해 대체 PSD로 전환했습니다.","세트":"세트","호환성 메모가 여기에 표시됩니다.":"호환성 메모가 여기에 표시됩니다.","디버그 추적이 여기에 표시됩니다.":"디버그 추적이 여기에 표시됩니다.","시간 없음":"시간 없음","제외 후 추출":"제외 후 추출","다운로드 오류":"다운로드 오류","오류":"오류","여러 루트 크기":"여러 루트 크기","사전 점검 화면.":"사전 점검 화면.","사전 점검.":"사전 점검.","1초 미만":"1초 미만","최근 내보내기 없음":"최근 내보내기 없음","최근 파일이 여기에 표시됩니다.":"최근 파일이 여기에 표시됩니다.","경고 없음":"경고 없음","배경 추적 없음":"배경 추적 없음","브라우저 UI에서 최종 다운로드 묶음을 준비하지 못했습니다.":"브라우저 UI에서 최종 다운로드 묶음을 준비하지 못했습니다.","브라우저 UI에서 PSD 조립에 실패했습니다.":"브라우저 UI에서 PSD 조립에 실패했습니다.","묶음 준비 실패":"묶음 준비 실패","로그 없음":"로그 없음","추출 후 숨김 유지":"추출 후 숨김 유지","PNG+JPG 포함":"PNG+JPG 포함","여러 PSD를 선택하면 한 페이지 안에 아래로 자동 이어 붙여 가져옵니다.":"여러 PSD를 선택하면 한 페이지 안에 아래로 자동 이어 붙여 가져옵니다.","피그마로 가져오기":"피그마로 가져오기"},Da=Object.entries(fo).reduce((e,[t,r])=>{let o=e.get(r);return o?o.push(t):e.set(r,[t]),e},new Map),mo={"PSD 가져오기 완료":"PSD 가져오기 완료","PSD 가져오기에 실패했습니다.":"PSD 가져오기에 실패했습니다.","PSD 합성 이미지를 찾지 못했습니다.":"PSD 합성 이미지를 찾지 못했습니다.","PSD 합성 이미지를 찾지 못했습니다.":"PSD 합성 이미지를 찾지 못했습니다.","가져올 수 있는 PSD 파일이 없습니다.":"가져올 수 있는 PSD 파일이 없습니다.","가져올 수 있는 PSD 레이어를 찾지 못했습니다.":"가져올 수 있는 PSD 레이어를 찾지 못했습니다.","지원하지 않는 PSD 배치 방식입니다.":"지원하지 않는 PSD 배치 방식입니다.","복원 가능한 PSD 레이어가 없어 합성 이미지 한 장으로 가져옵니다.":"복원 가능한 PSD 레이어가 없어 합성 이미지 한 장으로 가져옵니다."},Bt={"Select one or more frames, groups, or layers to export.":{ko:"내보낼 프레임, 그룹 또는 레이어를 하나 이상 선택하세요.",ja:"書き出すフレーム、グループ、またはレイヤーを1つ以上選択してください。","zh-CN":"请选择一个或多个要导出的画板、组或图层。"},"The exporter keeps editable text where possible, and multiple selected roots are packaged into a ZIP archive.":{ko:"가능한 경우 편집 가능한 텍스트를 유지하며, 여러 루트를 선택하면 ZIP 묶음으로 준비됩니다.",ja:"可能な限り編集可能テキストを保持し、複数のルートを選択すると ZIP にまとめられます。","zh-CN":"会在可能时保留可编辑文本，选择多个根节点时会打包为 ZIP。"},"This selection cannot be exported by Figma.":{ko:"이 선택 항목은 Figma에서 내보낼 수 없습니다.",ja:"この選択項目は Figma で書き出せません。","zh-CN":"此选择内容无法通过 Figma 导出。"},"Try a visible frame, group, text layer, or image layer.":{ko:"보이는 프레임, 그룹, 텍스트 레이어 또는 이미지 레이어를 선택해보세요.",ja:"表示されているフレーム、グループ、テキストレイヤー、または画像レイヤーを試してください。","zh-CN":"请尝试选择可见的画板、组、文本图层或图像图层。"},"This selection does not have exportable bounds.":{ko:"이 선택 항목에는 내보낼 수 있는 경계가 없습니다.",ja:"この選択項目には書き出し可能な境界がありません。","zh-CN":"此选择内容没有可导出的边界。"},"No exportable content was found inside this selection.":{ko:"이 선택 안에서 내보낼 수 있는 콘텐츠를 찾지 못했습니다.",ja:"この選択内に書き出し可能なコンテンツが見つかりませんでした。","zh-CN":"在此选择中未找到可导出的内容。"},"Masks or empty containers can still block export. Hidden layers can be preserved or ignored from the export defaults.":{ko:"마스크나 빈 컨테이너도 내보내기를 막을 수 있습니다. 숨김 레이어는 내보내기 기본값에서 유지하거나 무시할 수 있습니다.",ja:"マスクや空のコンテナも書き出しを妨げることがあります。非表示レイヤーは書き出しデフォルトで保持または無視できます。","zh-CN":"蒙版或空容器也可能阻止导出。隐藏图层可在导出默认值中保留或忽略。"},"Editable text is preserved when fills and typography are compatible. Hidden layers can be ignored or included as hidden PSD layers.":{ko:"채우기와 타이포그래피가 호환되면 편집 가능한 텍스트를 유지합니다. 숨김 레이어는 무시하거나 PSD 안에서 숨김 상태로 포함할 수 있습니다.",ja:"塗りとタイポグラフィが互換なら編集可能テキストを保持します。非表示レイヤーは無視することも、PSDで非表示のまま含めることもできます。","zh-CN":"当填充和排版兼容时会保留可编辑文本。隐藏图层可以忽略，也可以作为隐藏的 PSD 图层包含。"},"Text export is currently set to rasterize text, so text layers will export as bitmap layers until you change the export defaults.":{ko:"현재 텍스트 내보내기 설정이 비트맵 고정으로 되어 있어, 내보내기 기본값을 바꾸기 전까지 텍스트 레이어는 비트맵 레이어로 내보내집니다.",ja:"現在のテキスト書き出し設定はラスタライズ固定のため、書き出しデフォルトを変更するまでテキストレイヤーはビットマップレイヤーとして書き出されます。","zh-CN":"当前文本导出设置为栅格化文本，因此在更改导出默认值之前，文本图层都会导出为位图图层。"},"Each selected root is exported as its own PSD file. When more than one file is ready, the download is packaged as a ZIP archive.":{ko:"선택한 각 루트는 개별 PSD 파일로 내보내집니다. 두 개 이상 준비되면 ZIP으로 묶어 다운로드합니다.",ja:"選択した各ルートは個別の PSD ファイルとして書き出されます。2つ以上になると ZIP にまとめてダウンロードします。","zh-CN":"每个选中的根节点都会导出为单独的 PSD 文件。准备好的文件超过一个时会打包为 ZIP 下载。"},"An unknown error happened while building the PSD.":{ko:"PSD를 구성하는 중 알 수 없는 오류가 발생했습니다.",ja:"PSD の生成中に不明なエラーが発生しました。","zh-CN":"构建 PSD 时发生未知错误。"},"Unable to calculate document bounds for the selected root.":{ko:"선택한 루트의 문서 경계를 계산할 수 없습니다.",ja:"選択したルートのドキュメント境界を計算できません。","zh-CN":"无法计算所选根节点的文档边界。"},"No exportable layers remain after ignoring hidden layers.":{ko:"숨김 레이어를 제외하고 나면 내보낼 수 있는 레이어가 남지 않습니다.",ja:"非表示レイヤーを除外すると、書き出し可能なレイヤーが残りません。","zh-CN":"忽略隐藏图层后，没有剩余可导出的图层。"},"There are no visible exportable layers inside this selection.":{ko:"이 선택 안에는 보이는 내보내기 가능 레이어가 없습니다.",ja:"この選択内には表示されている書き出し可能レイヤーがありません。","zh-CN":"此选择中没有可见的可导出图层。"},"The exporter could not produce any PSD layers from this selection.":{ko:"이 선택에서 PSD 레이어를 생성하지 못했습니다.",ja:"この選択から PSD レイヤーを生成できませんでした。","zh-CN":"无法从此选择生成任何 PSD 图层。"},"Missing composite raster bytes for bundle packaging.":{ko:"묶음 패키징에 필요한 합성 래스터 바이트가 없습니다.",ja:"バンドル作成に必要な合成ラスターのバイトがありません。","zh-CN":"缺少用于打包的合成栅格字节数据。"},"Document bounds are required to place PSD vector paths.":{ko:"PSD 벡터 경로를 배치하려면 문서 경계가 필요합니다.",ja:"PSD のベクターパスを配置するにはドキュメント境界が必要です。","zh-CN":"放置 PSD 矢量路径需要文档边界。"},"Expected an SVG root element.":{ko:"SVG 루트 요소가 필요합니다.",ja:"SVG のルート要素が必要です。","zh-CN":"需要 SVG 根元素。"},"Invalid SVG matrix() transform.":{ko:"유효하지 않은 SVG matrix() 변환입니다.",ja:"無効な SVG matrix() 変換です。","zh-CN":"无效的 SVG matrix() 变换。"},"Invalid SVG rect dimensions.":{ko:"유효하지 않은 SVG rect 크기입니다.",ja:"無効な SVG rect サイズです。","zh-CN":"无效的 SVG rect 尺寸。"},"Raster smart object blur export requires Layer Blur metadata.":{ko:"래스터 스마트 오브젝트 블러 내보내기에는 Layer Blur 메타데이터가 필요합니다.",ja:"ラスターのスマートオブジェクトぼかしを書き出すには Layer Blur のメタデータが必要です。","zh-CN":"导出栅格智能对象模糊需要 Layer Blur 元数据。"},"Shape vector export requires a solid fill.":{ko:"도형 벡터 내보내기에는 단색 채우기가 필요합니다.",ja:"シェイプのベクター書き出しには単色塗りが必要です。","zh-CN":"形状矢量导出需要纯色填充。"},"The SVG did not contain any shape paths.":{ko:"SVG 안에 도형 경로가 없습니다.",ja:"SVG にシェイプパスが含まれていませんでした。","zh-CN":"SVG 中不包含任何形状路径。"},"The SVG path started without a command.":{ko:"SVG 경로가 명령 없이 시작되었습니다.",ja:"SVG パスがコマンドなしで開始されました。","zh-CN":"SVG 路径在没有命令的情况下开始。"},"The SVG viewBox was empty.":{ko:"SVG viewBox가 비어 있습니다.",ja:"SVG の viewBox が空でした。","zh-CN":"SVG viewBox 为空。"},"The exported SVG did not have an <svg> root.":{ko:"내보낸 SVG에 <svg> 루트가 없습니다.",ja:"書き出された SVG に <svg> ルートがありませんでした。","zh-CN":"导出的 SVG 没有 <svg> 根节点。"},"Unexpected end of SVG path data.":{ko:"SVG 경로 데이터가 예상보다 일찍 끝났습니다.",ja:"SVG パスデータが途中で終了しました。","zh-CN":"SVG 路径数据意外结束。"}},xo=[{pattern:/^Export failed for "(.+)" \((\d+)\/(\d+)\): (.+)$/,replace:(e,t)=>({ko:'"'.concat(t[1],'" 내보내기에 실패했습니다. (').concat(t[2],"/").concat(t[3],"): ").concat(w(e,t[4])),ja:'"'.concat(t[1],'" の書き出しに失敗しました。(').concat(t[2],"/").concat(t[3],"): ").concat(w(e,t[4])),"zh-CN":'"'.concat(t[1],'" 导出失败。(').concat(t[2],"/").concat(t[3],"): ").concat(w(e,t[4]))})[e]},{pattern:/^PSD ready: (\d+) layers, (\d+) editable text layers$/,replace:(e,t)=>({ko:"PSD 준비됨: 레이어 ".concat(t[1],"개, 편집 가능 텍스트 ").concat(t[2],"개"),ja:"PSD の準備完了: レイヤー ".concat(t[1],"枚、編集可能テキスト ").concat(t[2],"件"),"zh-CN":"PSD 已准备好：".concat(t[1]," 个图层，").concat(t[2]," 个可编辑文本图层")})[e]},{pattern:/^(\d+) PSD files are ready\. The download will be packaged as a ZIP archive\.$/,replace:(e,t)=>({ko:"PSD ".concat(t[1],"개가 준비되었습니다. 다운로드는 ZIP 묶음으로 제공됩니다."),ja:"PSD が ".concat(t[1]," 個準備できました。ダウンロードは ZIP にまとめられます。"),"zh-CN":"".concat(t[1]," 个 PSD 已准备好。下载内容将打包为 ZIP。")})[e]},{pattern:/^(\d+) roots selected: (\d+) PSD layers, (\d+) editable text layers, (\d+) preserved groups\.$/,replace:(e,t)=>({ko:"".concat(t[1],"개 루트 선택됨: PSD 레이어 ").concat(t[2],"개, 편집 가능 텍스트 ").concat(t[3],"개, 유지된 그룹 ").concat(t[4],"개"),ja:"".concat(t[1]," 個のルートを選択: PSD レイヤー ").concat(t[2]," 枚、編集可能テキスト ").concat(t[3]," 件、保持されたグループ ").concat(t[4]," 件"),"zh-CN":"已选择 ".concat(t[1]," 个根节点：").concat(t[2]," 个 PSD 图层，").concat(t[3]," 个可编辑文本图层，").concat(t[4]," 个保留的组")})[e]},{pattern:/^"(.+)" is ready: (\d+) PSD layers, (\d+) editable text layers, (\d+) preserved groups\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'" 준비 완료: PSD 레이어 ').concat(t[2],"개, 편집 가능 텍스트 ").concat(t[3],"개, 유지된 그룹 ").concat(t[4],"개"),ja:'"'.concat(t[1],'" の準備完了: PSD レイヤー ').concat(t[2]," 枚、編集可能テキスト ").concat(t[3]," 件、保持されたグループ ").concat(t[4]," 件"),"zh-CN":"“".concat(t[1],"”已就绪：").concat(t[2]," 个 PSD 图层，").concat(t[3]," 个可编辑文本图层，").concat(t[4]," 个保留的组")})[e]},{pattern:/^"(.+)" is not ready for batch export\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"은(는) 일괄 내보내기 준비가 되지 않았습니다.'),ja:'"'.concat(t[1],'" は一括書き出しの準備ができていません。'),"zh-CN":"“".concat(t[1],"”尚未准备好进行批量导出。")})[e]},{pattern:/^"(.+)" could not separate its background cleanly, so it was flattened\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"의 배경을 깔끔하게 분리하지 못해 평면화되었습니다.'),ja:'"'.concat(t[1],'" の背景をきれいに分離できなかったため、フラット化されました。'),"zh-CN":"无法干净地分离“".concat(t[1],"”的背景，因此已被扁平化。")})[e]},{pattern:/^"(.+)" could not keep its SVG\/vector data, so it fell back to a bitmap layer\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"의 SVG/벡터 데이터를 유지하지 못해 비트맵 레이어로 대체되었습니다.'),ja:'"'.concat(t[1],'" の SVG/ベクターデータを保持できず、ビットマップレイヤーにフォールバックしました。'),"zh-CN":"“".concat(t[1],"”无法保留 SVG/矢量数据，因此回退为位图图层。")})[e]},{pattern:/^"(.+)" could not be reconstructed as a PSD mask\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"을(를) PSD 마스크로 복원하지 못했습니다.'),ja:'"'.concat(t[1],'" を PSD マスクとして再構築できませんでした。'),"zh-CN":"无法将“".concat(t[1],"”重建为 PSD 蒙版。")})[e]},{pattern:/^"(.+)" uses masking and will export as a flattened bitmap layer\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"은(는) 마스킹을 사용하므로 평면화된 비트맵 레이어로 내보내집니다.'),ja:'"'.concat(t[1],'" はマスクを使用しているため、フラット化されたビットマップレイヤーとして書き出されます。'),"zh-CN":"“".concat(t[1],"”使用了蒙版，因此会导出为扁平化的位图图层。")})[e]},{pattern:/^"(.+)" uses Layer Blur, so it will preserve the blur as a Photoshop Smart Filter and rasterize the text instead of keeping an editable text layer\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"은(는) Layer Blur를 사용하므로 블러는 Photoshop 스마트 필터로 유지되고, 텍스트는 편집 가능한 레이어 대신 래스터화됩니다.'),ja:'"'.concat(t[1],'" は Layer Blur を使用しているため、ぼかしは Photoshop のスマートフィルターとして保持され、テキストは編集可能レイヤーではなくラスタライズされます。'),"zh-CN":"“".concat(t[1],"”使用了 Layer Blur，因此模糊会保留为 Photoshop 智能滤镜，而文本会被栅格化，不再保留为可编辑文本图层。")})[e]},{pattern:/^"(.+)" uses both layer and image-fill blend modes, so only the layer blend stayed editable in the PSD\.$/,replace:(e,t)=>({ko:'"'.concat(t[1],'"은(는) 레이어와 이미지 채우기 블렌드 모드를 함께 사용하므로 PSD에서는 레이어 블렌드만 편집 가능한 상태로 유지되었습니다.'),ja:'"'.concat(t[1],'" はレイヤーと画像塗りの両方にブレンドモードを使っているため、PSD ではレイヤーブレンドのみが編集可能なまま保持されました。'),"zh-CN":"“".concat(t[1],"”同时使用了图层与图像填充混合模式，因此在 PSD 中只有图层混合保留为可编辑。")})[e]},{pattern:/^Unable to encode (.+) preview in the browser UI\.$/,replace:(e,t)=>({ko:"브라우저 UI에서 ".concat(t[1]," 미리보기를 인코딩할 수 없습니다."),ja:"ブラウザ UI で ".concat(t[1]," プレビューをエンコードできません。"),"zh-CN":"无法在浏览器 UI 中编码 ".concat(t[1]," 预览。")})[e]}];function Gt(e,t=Mt){if(!e)return t;let r=e.trim().toLowerCase();return r==="ko"||r.startsWith("ko-")?"ko":r==="ja"||r.startsWith("ja-")?"ja":r==="en"||r.startsWith("en-")?"en":r==="es"||r.startsWith("es-")?"es":r==="zh"||r==="zh-cn"||r==="zh-hans"||r.startsWith("zh-cn")||r.startsWith("zh-hans")?"zh-CN":t}function go(e){var t;return(t=mo[e])!=null?t:e}function w(e,t){var i;if(!t)return t;let r=go(t);if(e==="ko"||e==="en")return r;let o=(i=Bt[r])!=null?i:Bt[t];if(o){let a=o[e];if(a)return a}if(e==="es")return r;for(let a of xo){let s=a.pattern.exec(r);if(s)return a.replace(e,s)}let n=r.match(/^([^:]+): (.+)$/);if(n){let a=w(e,n[2]);if(a!==n[2])return"".concat(n[1],": ").concat(a)}return r}var Xt={disableShapePreviewCanvas:!1,forceBitmapVectorPreview:!1,disableEditableTextPreview:!1,disableLayerBlur:!1,disableProgressiveLayerBlur:!1,disableBackgroundBlur:!1,disableNoise:!1,disableTexture:!1},ho=new Set(["COMPONENT","COMPONENT_SET","FRAME","INSTANCE","SECTION","TEXT"]),yo=new Set(["GROUP","SECTION","FRAME","COMPONENT","COMPONENT_SET","INSTANCE"]),So=new Set(["GROUP","FRAME","SECTION","COMPONENT","INSTANCE"]),bo=["fontName","fontSize","lineHeight","letterSpacing","fills","textDecoration","textCase"],Kt=["fontName"],je=new Map,We=null,Ze=null,O=b({},Xt),Eo=80,Po=70,No=70,To=12e6,Do=6e3,vo=12e6,wo=4e3,Co=8e6,ko=4e6,Bo=512,Yt=2048,Mo=1e4,Io=16e6,Ro=8e3,Ao=12e6,Lo={monotone:{primaryAlphaScale:.64,secondaryAlphaScale:1,overlayOpacityScale:1},duotone:{primaryAlphaScale:.56,secondaryAlphaScale:.48,overlayOpacityScale:1},multitone:{primaryAlphaScale:1,secondaryAlphaScale:1,overlayOpacityScale:.6}},qt="pigma:ui-preferences:v2",Fo="pigma:ui-preferences:v1",Jt="pigma:ui-preferences-flags:v1",S={closePlugin:()=>{figma.closePlugin()},notify:(e,t)=>figma.notify(e,t),postToUi:()=>{}},P=ne,G={},be=null,F=null,selectionPreviewCache=null,Qt=0;function er(e){return S=b(b({},S),e),{handleUiMessage:Go,invalidateSelectionCache:le,postSelectionState:Ee}}async function pigmaEnsureSelectionAccess(){if(typeof figma.loadAllPagesAsync!="function")return;try{await figma.loadAllPagesAsync()}catch(e){console.warn("[pigma] selection page access preload failed:",e)}}async function Go(e){var t,r;if(e.type==="request-preferences"){await pigmaEnsureSelectionAccess(),await zo(e.detectedLocale),Ot(),r=buildStartupSelectionResolution(),N({type:"selection-state",state:typeof pigmaSelectionDebugState=="function"?pigmaSelectionDebugState(tr(r.state)):tr(r.state)});return}if(e.type==="update-preferences"){await pigmaEnsureSelectionAccess(),await _o(e.preferences),Ot(),Ee();return}if(e.type==="request-selection-sync"){await pigmaEnsureSelectionAccess(),Ee();return}if(e.type==="request-runtime-cache-refresh"){pigmaRefreshRuntimeCaches(),Ee();return}if(e.type==="request-export-cancel"){pigmaCancelActiveExport();return}if(e.type==="request-export"){pigmaRefreshRuntimeCaches(),await Qo(e.hiddenLayerMode,Tt(e.settings),e.includeCompositePng===!0,e.developerExportExperiments);return}if(e.type==="request-next-export-root"){await se();return}if(e.type==="request-import"){await en(e.payload);return}if(e.type==="request-import-batch"){await tn(e.batch);return}e.type==="close-plugin"&&((t=S.closePlugin)==null||t.call(S))}function N(e){S.postToUi(e)}function pigmaExportCancelMessage(){return w(P.locale,"PSD \uB9CC\uB4E4\uAE30\uB97C \uCDE8\uC18C\uD588\uC2B5\uB2C8\uB2E4.")}function pigmaCancelActiveExport(){var e;let t=be;if(!t)return;t.cancelRequested=!0,t.cancelNotified=!0,be=null,Qe(),le();let r=pigmaExportCancelMessage();N({type:"export-error",message:r,cancelled:!0}),(e=S.notify)==null||e.call(S,r)}function pigmaExportWasCancelled(e){return!!(e&&e.cancelRequested)}function pigmaCleanupCancelledExport(e,t=null){try{t&&qe(t)}catch(r){}Qe(),le();return!0}function ae(e,t){console.info("[pigma-export][plugin]",b({stage:e},t))}function Oo(e){let t=e!=null?e:{};return{disableShapePreviewCanvas:t.disableShapePreviewCanvas===!0,forceBitmapVectorPreview:t.forceBitmapVectorPreview===!0,disableEditableTextPreview:t.disableEditableTextPreview===!0,disableLayerBlur:t.disableLayerBlur===!0,disableProgressiveLayerBlur:t.disableProgressiveLayerBlur===!0,disableBackgroundBlur:t.disableBackgroundBlur===!0,disableNoise:t.disableNoise===!0,disableTexture:t.disableTexture===!0}}function Uo(e){return O=Oo(e),O}function Qe(){O=b({},Xt)}function Ot(){N({type:"preferences",preferences:P})}function pigmaSelectionDebugState(e){try{let t=figma.currentPage.selection||[],r=t[0],o=r?f(r):"",n=r?r.type:"",i=figma.currentPage&&figma.currentPage.name?figma.currentPage.name:"";return B(b({},e),{detail:"[debug 0429 selection api=".concat(t.length," page=").concat(i," first=").concat(o||"-"," type=").concat(n||"-","] ").concat(e.detail||""),warnings:e.warnings})}catch(t){return B(b({},e),{detail:"[debug 0429 selection read error] ".concat(e.detail||"")})}}function Ee(){let e=selectionResolutionForUi();N({type:"selection-state",state:pigmaSelectionDebugState(tr(e.state))})}function le(){Qt+=1,F=null,selectionPreviewCache=null}function pigmaRefreshRuntimeCaches(){le(),be=null,je.clear(),We=null,Ze=null}async function zo(e){let t=Ho(e);try{G=$o(await figma.clientStorage.getAsync(Jt))}catch(r){G={}}try{let r=await figma.clientStorage.getAsync(qt);if(r!=null)P=Pe(r,t,!1,G.imageExportModeExplicit!==!0,G.textExportModeExplicit!==!0);else{let o=await figma.clientStorage.getAsync(Fo);P=Pe(o,t,!0,G.imageExportModeExplicit!==!0,G.textExportModeExplicit!==!0)}await et()}catch(r){G={},P=Pe(null,t),await et()}}async function _o(e){var t,r;if(e.exportSettings){let o=Object.prototype.hasOwnProperty.call(e.exportSettings,"imageExportMode"),n=Object.prototype.hasOwnProperty.call(e.exportSettings,"textExportMode");(o||n)&&(G=b(b(b({},G),o?{imageExportModeExplicit:!0}:{}),n?{textExportModeExplicit:!0}:{}))}P=Pe({locale:(t=e.locale)!=null?t:P.locale,localeMode:(r=e.localeMode)!=null?r:P.localeMode,exportSettings:e.exportSettings?b(b({},P.exportSettings),e.exportSettings):P.exportSettings}),await et()}async function et(){try{await figma.clientStorage.setAsync(qt,P),await figma.clientStorage.setAsync(Jt,G)}catch(e){}}function Pe(e,t=null,r=!1,o=!1,n=!1){var y;let i=e,a=r?It(i==null?void 0:i.exportSettings):i==null?void 0:i.exportSettings,s=o?Rt(a):a,l=n?Lt(s):s,u=o?At(l):l,c=(i==null?void 0:i.locale)==="ko"||(i==null?void 0:i.locale)==="en"||(i==null?void 0:i.locale)==="es"||(i==null?void 0:i.locale)==="ja"||(i==null?void 0:i.locale)==="zh-CN",p=c?i.locale:null,g=(i==null?void 0:i.localeMode)==="auto"||(i==null?void 0:i.localeMode)==="manual"?i.localeMode:c?"manual":"auto";return{locale:g==="auto"?(y=t!=null?t:p)!=null?y:ne.locale:p!=null?p:ne.locale,localeMode:g,exportSettings:Tt(u)}}function $o(e){let t=e;return b(b({},typeof(t==null?void 0:t.imageExportModeExplicit)=="boolean"?{imageExportModeExplicit:t.imageExportModeExplicit}:{}),typeof(t==null?void 0:t.textExportModeExplicit)=="boolean"?{textExportModeExplicit:t.textExportModeExplicit}:{})}function Ho(e){return e?Gt(e,ne.locale):null}function tr(e){return B(b({},e),{summary:w(P.locale,e.summary),detail:w(P.locale,e.detail),warnings:e.warnings.map(t=>w(P.locale,t))})}function buildSelectionPreviewEmptyState(e){return{ok:!1,state:{ready:!1,selectionId:null,selectionCount:0,selectionName:"",selectionType:null,summary:"Select one or more frames, groups, or layers to export.",detail:e.textExportMode==="rasterize-text"?"Text export is currently set to rasterize text, so text layers will export as bitmap layers until you change the export defaults.":"The exporter keeps editable text where possible, and multiple selected roots are packaged into a ZIP archive.",documentWidth:null,documentHeight:null,exportNodeCount:0,editableTextCount:0,preservedGroupCount:0,warnings:[],analysisPending:!1}}}function isSelectionPreviewHeavy(e){let t=xe(e);if(t){let r=d(t.width),o=d(t.height);if(o>=Do||r*o>=vo)return!0}if(!V(e)||e.children.length===0)return!1;let n=[e],i=0;for(;n.length>0;){let a=n.pop();if(i+=1,i>400)return!0;if(!V(a))continue;let s=a.children;if(!s||s.length===0)continue;if(s.length>120)return!0;for(let l=0;l<s.length;l+=1){let u=s[l];he(u)&&n.push(u)}}return!1}function shouldUseQuickSelectionPreview(e){if(e.length===0)return!1;for(let t=0;t<e.length;t+=1)if(isSelectionPreviewHeavy(e[t]))return!0;return!1}function buildQuickSelectionResolution(){let e=figma.currentPage.selection,t=Tt(P.exportSettings),r=Jo(e,t);if(selectionPreviewCache&&selectionPreviewCache.cacheKey===r)return selectionPreviewCache.resolution;if(e.length===0){let y=buildSelectionPreviewEmptyState(t);return selectionPreviewCache={cacheKey:r,resolution:y},y}let o=[],n=[],i=!1;for(let y=0;y<e.length;y+=1){let m=e[y];if(!he(m)){let T={ok:!1,state:Ye(e,m,"This selection cannot be exported by Figma.","Try a visible frame, group, text layer, or image layer.")};return selectionPreviewCache={cacheKey:r,resolution:T},T}let C=xe(m);if(!C){let T={ok:!1,state:Ye(e,m,"This selection does not have exportable bounds.","Try a visible frame, group, text layer, or image layer.")};return selectionPreviewCache={cacheKey:r,resolution:T},T}o.push(m),n.push(C),!i&&isSelectionPreviewHeavy(m)&&(i=!0)}let a=o[0],s=o.length,l=n[0],u=i?"Large selection detected. Pigma is showing a lightweight preview first to keep Figma responsive. Full layer analysis runs when export starts.":s===1?"Editable text is preserved when fills and typography are compatible. Hidden layers can be ignored or included as hidden PSD layers.":"Each selected root is exported as its own PSD file. When more than one file is ready, the download is packaged as a ZIP archive.",c=i?["Large selection detected. Detailed layer counts will load when export starts."]:[],p={ready:!0,selectionId:s===1?a.id:null,selectionCount:s,selectionName:no(o),selectionType:io(o),summary:s===1?'"'.concat(f(a),'" is ready to export.'):"".concat(s," roots selected and ready to export."),detail:u,documentWidth:s===1?d(l.width):null,documentHeight:s===1?d(l.height):null,exportNodeCount:s,editableTextCount:0,preservedGroupCount:0,warnings:c,analysisPending:i},g={ok:!0,nodes:o,state:p};return selectionPreviewCache={cacheKey:r,resolution:g},g}function buildStartupSelectionResolution(){let e=figma.currentPage.selection,t=Tt(P.exportSettings);if(e.length===0)return buildSelectionPreviewEmptyState(t);let r=[],o=[],n=null;for(let i=0;i<e.length;i+=1){let a=e[i];if(!he(a)){n={ok:!1,state:Ye(e,a,"This selection cannot be exported by Figma.","Try a visible frame, group, text layer, or image layer.")};break}let s=xe(a);if(!s){n={ok:!1,state:Ye(e,a,"This selection does not have exportable bounds.","Try a visible frame, group, text layer, or image layer.")};break}r.push(a),o.push(s)}if(n)return n;let i=r[0],a=r.length,s=o[0];return{ok:!0,nodes:r,state:{ready:!0,selectionId:a===1?i.id:null,selectionCount:a,selectionName:no(r),selectionType:io(r),summary:a===1?'"'.concat(f(i),'" is ready to export.'):"".concat(a," roots selected and ready to export."),detail:"Startup preview keeps the selection summary lightweight. Full layer analysis runs when export starts or when the selection changes.",documentWidth:a===1?d(s.width):null,documentHeight:a===1?d(s.height):null,exportNodeCount:a,editableTextCount:0,preservedGroupCount:0,warnings:["Detailed layer counts are deferred until export starts or the selection changes."],analysisPending:!0}}}function selectionResolutionForUi(){let e=figma.currentPage.selection;return shouldUseQuickSelectionPreview(e)?buildQuickSelectionResolution():rr()}function rr(){let e=figma.currentPage.selection,t=Tt(P.exportSettings),r=Jo(e,t);if((F==null?void 0:F.cacheKey)===r)return F.resolution;if(e.length===0){let m={ok:!1,state:{ready:!1,selectionId:null,selectionCount:0,selectionName:"",selectionType:null,summary:"Select one or more frames, groups, or layers to export.",detail:t.textExportMode==="rasterize-text"?"Text export is currently set to rasterize text, so text layers will export as bitmap layers until you change the export defaults.":"The exporter keeps editable text where possible, and multiple selected roots are packaged into a ZIP archive.",documentWidth:null,documentHeight:null,exportNodeCount:0,editableTextCount:0,preservedGroupCount:0,warnings:[]}};return F={cacheKey:r,resolution:m},m}let o=[],n=new Set,i=0,a=0,s=0;for(let m of e){if(!he(m)){let R={ok:!1,state:Ye(e,m,"This selection cannot be exported by Figma.","Try a visible frame, group, text layer, or image layer.")};return F={cacheKey:r,resolution:R},R}let T=m,C=xe(T);if(!C){let R={ok:!1,state:Ye(e,T,"This selection does not have exportable bounds.","Try a visible frame, group, text layer, or image layer.")};return F={cacheKey:r,resolution:R},R}let E=Ie(T,t.hiddenLayerMode);if(E.exportNodeCount===0){let R={ok:!1,state:Ye(e,T,"No exportable content was found inside this selection.","Masks or empty containers can still block export. Hidden layers can be preserved or ignored from the export defaults.",Array.from(E.warnings),C)};return F={cacheKey:r,resolution:R},R}lt(C,E.containsHeavyEffects)&&E.warnings.add(ut(d(C.width),d(C.height),E.containsHeavyEffects)),o.push({node:T,documentBounds:C,analysis:E}),i+=E.exportNodeCount,a+=E.editableTextCount,s+=E.preservedGroupCount;for(let R of E.warnings)n.add(Nt(f(T),R,e.length>1))}let l=o[0],u=o.length,c=ua(a,t),p={exportNodeCount:i,editableTextCount:c,preservedGroupCount:s,containsHeavyEffects:o.some(m=>m.analysis.containsHeavyEffects),warnings:n},g={ready:!0,selectionId:u===1?l.node.id:null,selectionCount:u,selectionName:no(o.map(m=>m.node)),selectionType:io(o.map(m=>m.node)),summary:fa(f(l.node),p,u),detail:ca(o,u,t,a),documentWidth:u===1?d(l.documentBounds.width):null,documentHeight:u===1?d(l.documentBounds.height):null,exportNodeCount:i,editableTextCount:c,preservedGroupCount:s,warnings:Array.from(n)},y={ok:!0,nodes:o.map(m=>m.node),roots:o,state:g};return F={cacheKey:r,resolution:y},y}function or(e){return e?{height:wo,area:Co}:{height:Do,area:vo}}function Ne(e,t,r){let o=or(r);return t>=o.height||e*t>=o.area}function lt(e,t){return Ne(d(e.width),d(e.height),t)}function ut(e,t,r){let o=or(r);return"This export is treated as a long frame (".concat(e," x ").concat(t,"px). Pigma will switch oversized layers to a lower-memory raster path once the document exceeds ").concat(o.height,"px tall or ").concat(o.area.toLocaleString("en-US")," pixels.")}function Vo(){return"Long-frame mode rasterizes editable text layers to keep export memory stable."}function jo(e){return'"'.concat(e,'" switches to a raster export in long-frame mode to keep memory usage stable.')}function Te(e,t){return'"'.concat(e,'" was split into ').concat(t," raster tiles in long-frame mode to keep memory usage stable.")}function nr(e,t){let r=d(e.width),o=d(e.height),n=r*o,i=t?{height:Ro,area:Ao}:{height:Mo,area:Io};return o>=i.height||n>=i.area}function ir(e,t){return nr(e,t)}function Wo(e,t,r){return'"'.concat(e,'" switched to a safe layered bitmap export (').concat(t," x ").concat(r,"px) so very large documents keep a PSD layer stack without triggering the heavier editable reconstruction path.")}function Zo(e,t){return'"'.concat(e,'" retried with the safe layered bitmap export after layered reconstruction failed (').concat(t,").")}function Xo(e,t,r){return'"'.concat(e,'" switched to a safe flattened export (').concat(t," x ").concat(r,"px) so very large documents finish as a complete PSD instead of failing during heavy layer reconstruction.")}function Ko(e,t){return'"'.concat(e,'" retried as a safe flattened PSD after layered export failed (').concat(t,").")}function Yo(e,t){return ze(e)||Xr(e)||!!fe(e,t)}function De(e){return Ne(d(e.width),d(e.height),!1)&&d(e.height)>Yt}function qo(e,t){let r=Math.max(1,d(e)),o=Math.max(1,d(t)),n=Math.floor(ko/r),i=Math.max(1,n),a=Math.max(Bo,Math.min(Yt,i));return Math.min(o,a)}function Jo(e,t){let r=e.map(o=>o.id).join(",");return[Qt,figma.currentPage.id,t.hiddenLayerMode,t.textExportMode,r].join("|")}async function Qo(e,t,r,o){var l;let n=Uo(o);P=b(b({},P),{exportSettings:Tt(t)});let i=rr();if(!i.ok){Qe(),le();let u=tr(i.state);N({type:"export-error",message:u.summary}),(l=S.notify)==null||l.call(S,u.summary,{error:!0});return}let a=i.nodes.length,s=xa(i.nodes,t);be={nodes:i.nodes,roots:i.roots,hiddenLayerMode:e,settings:t,includeCompositePng:r,developerExportExperiments:n,rootCount:a,bundleFileName:s,nextIndex:0,singlePayloadSummary:null},ae("session-start",{rootCount:a,includeCompositePng:r,developerExportExperiments:n}),N({type:"export-bundle-started",fileName:s,rootCount:a}),await se()}async function en(e){var t,r;N({type:"import-started",fileName:e.fileName,rootName:e.rootName,mode:e.mode});try{let o=ar(e.placement,e.rootName),n=nn(e,o),i=0;if(e.mode==="flatten-image"||e.nodes.length===0){if(!e.compositePngBytes||e.compositePngBytes.length===0)throw new Error("PSD 합성 이미지를 찾지 못했습니다.");let a=Q({kind:"bitmap",name:e.rootName,x:0,y:0,width:e.documentWidth,height:e.documentHeight,opacity:1,visible:!0,blendMode:"normal",effects:null,strokeEffect:null,pngBytes:e.compositePngBytes});n.appendChild(a),i=1}else i=await lr(e.nodes,n,e.warnings);o!==figma.currentPage&&await figma.setCurrentPageAsync(o),o.selection=[n],figma.viewport.scrollAndZoomIntoView([n]),N({type:"import-finished",rootName:e.rootName,importedNodeCount:i,warningCount:e.warnings.length}),(t=S.notify)==null||t.call(S,w(P.locale,"PSD 가져오기 완료"))}catch(o){let n=w(P.locale,o instanceof Error?o.message:"PSD 가져오기에 실패했습니다.");N({type:"import-error",message:n}),(r=S.notify)==null||r.call(S,n,{error:!0})}}async function tn(e){var o,n;let t=e.items.filter(i=>{var a,s;return i&&i.documentWidth>0&&i.documentHeight>0&&(i.mode==="flatten-image"||i.nodes.length>0||((s=(a=i.compositePngBytes)==null?void 0:a.length)!=null?s:0)>0)}),r=t.every(i=>i.mode==="flatten-image")?"flatten-image":"keep-layers";N({type:"import-started",fileName:e.fileName,rootName:e.rootName,mode:r});try{if(t.length===0)throw new Error("가져올 수 있는 PSD 파일이 없습니다.");if(e.arrangement!=="stitch-vertical")throw new Error("지원하지 않는 PSD 배치 방식입니다.");let i=Math.max(0,x(e.gap)),a=Math.max(...t.map(m=>d(m.documentWidth)),1),s=Math.max(1,t.reduce((m,T,C)=>{let E=d(T.documentHeight);return m+E+(C>0?i:0)},0)),l=ar(e.placement,e.rootName),u=sr(e.placement,a,s),c=figma.createFrame();c.name=e.rootName.trim().length>0?e.rootName:"PSD stitch",c.resize(a,s),c.clipsContent=!1,c.fills=[],c.strokes=[],c.x=u.x,c.y=u.y,l.appendChild(c);let p=0,g=0;for(let m of t){let T=Math.max(1,d(m.documentWidth)),C=Math.max(1,d(m.documentHeight)),E=figma.createFrame();E.name=m.rootName.trim().length>0?m.rootName:"PSD section",E.resize(T,C),E.clipsContent=!1,E.fills=[],E.strokes=[],E.x=0,E.y=g,c.appendChild(E),p+=await rn(m,E),g+=C+i}l!==figma.currentPage&&await figma.setCurrentPageAsync(l),l.selection=[c],figma.viewport.scrollAndZoomIntoView([c]);let y=Array.from(new Set(t.flatMap(m=>m.warnings.filter(Boolean)))).length;N({type:"import-finished",rootName:e.rootName,importedNodeCount:p,warningCount:y}),(o=S.notify)==null||o.call(S,w(P.locale,"PSD 가져오기 완료"))}catch(i){let a=w(P.locale,i instanceof Error?i.message:"PSD 가져오기에 실패했습니다.");N({type:"import-error",message:a}),(n=S.notify)==null||n.call(S,a,{error:!0})}}async function rn(e,t){if(e.mode==="flatten-image"||e.nodes.length===0){if(!e.compositePngBytes||e.compositePngBytes.length===0)throw new Error("PSD 합성 이미지를 찾지 못했습니다.");let r=Q({kind:"bitmap",name:e.rootName,x:0,y:0,width:e.documentWidth,height:e.documentHeight,opacity:1,visible:!0,blendMode:"normal",effects:null,strokeEffect:null,pngBytes:e.compositePngBytes});return t.appendChild(r),1}return lr(e.nodes,t,e.warnings)}function ar(e,t){if(e==="current-page")return figma.currentPage;let r=figma.createPage();return r.parent||figma.root.appendChild(r),r.name=on(t),r}function on(e){let t=e.trim();return t.length>0?"".concat(t," import"):"PSD import"}function nn(e,t){let r=figma.createFrame();r.name=e.rootName,r.resize(Math.max(1,d(e.documentWidth)),Math.max(1,d(e.documentHeight))),r.clipsContent=!1,r.fills=[],r.strokes=[];let o=sr(e.placement,e.documentWidth,e.documentHeight);return r.x=o.x,r.y=o.y,t.appendChild(r),r}function sr(e,t,r){return e==="new-page"?{x:0,y:0}:{x:x(figma.viewport.center.x-t/2),y:x(figma.viewport.center.y-r/2)}}async function lr(e,t,r){let o=0;for(let n of e){let i=await ur(n,r);i&&(t.appendChild(i.node),cr(i.node,n,t),o+=i.count)}return o}async function ur(e,t){if(e.kind==="group"){if(e.width<=0||e.height<=0)return null;let r=figma.createFrame();r.resize(Math.max(1,d(e.width)),Math.max(1,d(e.height))),r.clipsContent=!1,r.fills=[],r.strokes=[],r.x=x(e.x),r.y=x(e.y),ve(r,e,!0);let o=1;for(let n of e.children){let i=await ur(n,t);i&&(r.appendChild(i.node),cr(i.node,n,r),o+=i.count)}return{node:r,count:o}}if(e.kind==="text"){let r=await sn(e,t);return r?{node:r,count:1}:null}return e.kind==="shape"?{node:an(e),count:1}:e.width<=0||e.height<=0||e.pngBytes.length===0?null:{node:Q(e),count:1}}function cr(e,t,r){var c,p;if(e.type!=="TEXT"||t.kind!=="text")return;let o=t.text.shapeType==="box"?(c=k(e))!=null?c:I(e):(p=I(e))!=null?p:k(e),n=k(r),i=o&&n?o.x-n.x:e.x,a=o&&n?o.y-n.y:e.y,s=x(t.x-i),l=x(t.y-a);if(s===0&&l===0)return;let u=e.relativeTransform;e.relativeTransform=[[u[0][0],u[0][1],u[0][2]+s],[u[1][0],u[1][1],u[1][2]+l]]}function Q(e){let t=figma.createRectangle();t.resize(Math.max(1,d(e.width)),Math.max(1,d(e.height))),t.x=x(e.x),t.y=x(e.y),t.strokes=[],t.cornerRadius=0;let r=figma.createImage(e.pngBytes);return t.fills=[{type:"IMAGE",imageHash:r.hash,scaleMode:"FILL"}],ve(t,e,!1),t}function an(e){let t=figma.createRectangle();return t.resize(Math.max(1,d(e.width)),Math.max(1,d(e.height))),t.x=x(e.x),t.y=x(e.y),t.fills=hn(e.fill),t.strokes=[],yn(t,e.shape,e.width,e.height),Sn(t,e.stroke),ve(t,e,!1),t}async function sn(e,t){if(e.width<=0||e.height<=0)return e.pngBytes.length>0?Q(e):null;let r=figma.createText();try{let o=await ln(e,t);if(o.length===0)return r.removed||r.remove(),e.pngBytes.length>0?Q(e):null;await Ce(o[0].fontName),r.fontName=o[0].fontName,r.characters=e.text.value,r.autoRename=!1;for(let n of o)cn(r,n);return dn(r,e),pn(r,e),ve(r,e,!1),r}catch(o){return r.removed||r.remove(),e.pngBytes.length>0?(t.push('"'.concat(e.name,'" editable text could not be reconstructed (').concat(o instanceof Error?o.message:"unknown error","), so a bitmap preview was imported instead.")),Q(e)):null}}async function ln(e,t){let r=await dr(),o=new Map,n=[],i=0;for(let a of e.text.styleRuns){let s=Math.max(0,Math.round(a.length));if(s<=0||i>=e.text.value.length)continue;let l=Math.min(e.text.value.length,i+s),u="".concat(a.style.photoshopFontName,"\0").concat(a.style.fontFamily,"\0").concat(a.style.fontStyle),c=o.get(u);if(!c){let p=un(a.style,r);c=p.fontName,o.set(u,c),p.exact||t.push('"'.concat(e.name,'" uses Photoshop font "').concat(a.style.photoshopFontName,'", so "').concat(c.family," ").concat(c.style,'" was used in Figma.'))}await Ce(c),n.push({start:i,end:l,fontName:c,style:a.style}),i=l}return n}function un(e,t){var a,s;let r=Z(e.fontFamily),o=Z(e.fontStyle),n=Z(e.photoshopFontName),i=null;for(let l of t){let u=l.fontName,c=Z(u.family),p=Z(u.style),g=Z("".concat(u.family).concat(u.style)),y=Z("".concat(u.family,"Regular")),m=0;c===r?m+=100:r.length>0&&(c.includes(r)||r.includes(c))&&(m+=60),p===o?m+=40:o.length>0&&(p.includes(o)||o.includes(p))&&(m+=20),n.length>0&&g===n?m+=120:n.length>0&&y===n&&(m+=100);let T=c===r&&p===o;(!i||m>i.score||m===i.score&&T&&!i.exact)&&(i={fontName:u,score:m,exact:T})}return i&&i.score>0?{fontName:i.fontName,exact:i.exact}:{fontName:(s=(a=t[0])==null?void 0:a.fontName)!=null?s:{family:"Inter",style:"Regular"},exact:!1}}function Z(e){return e.trim().toLowerCase().replace(/[^a-z0-9]/g,"")}async function dr(){return We||(We=figma.listAvailableFontsAsync()),await We}function cn(e,t){e.setRangeFontName(t.start,t.end,t.fontName),e.setRangeFontSize(t.start,t.end,Math.max(1,t.style.fontSize)),e.setRangeLineHeight(t.start,t.end,fn(t.style.lineHeightPx)),e.setRangeLetterSpacing(t.start,t.end,mn(t.style.tracking,t.style.fontSize)),e.setRangeTextCase(t.start,t.end,xn(t.style.fontCaps)),e.setRangeTextDecoration(t.start,t.end,t.style.strikethrough?"STRIKETHROUGH":t.style.underline?"UNDERLINE":"NONE"),e.setRangeFills(t.start,t.end,pr(t.style.fillColor))}function dn(e,t){var r,o,n,i,a,s,l,u;if(e.textAlignHorizontal=gn(t.text.justification),e.textAlignVertical="TOP",t.text.shapeType==="box"){let c=Math.max(1,d(((o=(r=t.text.boxBounds)==null?void 0:r[2])!=null?o:t.width)-((i=(n=t.text.boxBounds)==null?void 0:n[0])!=null?i:0))),p=Math.max(1,d(((s=(a=t.text.boxBounds)==null?void 0:a[3])!=null?s:t.height)-((u=(l=t.text.boxBounds)==null?void 0:l[1])!=null?u:0)));if(t.text.sizingMode==="auto-height"){e.textAutoResize="HEIGHT",e.resizeWithoutConstraints(c,Math.max(1,d(e.height)));return}e.textAutoResize="NONE",e.resizeWithoutConstraints(c,p);return}e.textAutoResize="WIDTH_AND_HEIGHT"}function pn(e,t){let[r,o,n,i,a,s]=t.text.transform,l=Math.hypot(r,o),u=Math.hypot(n,i),c=l>1e-4?r/l:1,p=l>1e-4?o/l:0,g=u>1e-4?n/u:0,y=u>1e-4?i/u:1;e.relativeTransform=[[c,g,x(Number.isFinite(a)?a:t.x)],[p,y,x(Number.isFinite(s)?s:t.y)]]}function fn(e){return e===null||!Number.isFinite(e)||e<=0?{unit:"AUTO"}:{unit:"PIXELS",value:e}}function mn(e,t){return{unit:"PIXELS",value:t>0?e/1e3*t:0}}function xn(e){switch(e){case 2:return"UPPER";case 1:return"SMALL_CAPS";default:return"ORIGINAL"}}function gn(e){switch(e){case"center":case"justify-center":return"CENTER";case"right":case"justify-right":return"RIGHT";case"justify-all":case"justify-left":return"JUSTIFIED";default:return"LEFT"}}function pr(e){return!e||e.a<=0?[]:[{type:"SOLID",color:{r:e.r/255,g:e.g/255,b:e.b/255},opacity:h(e.a/255,0,1)}]}function hn(e){return e?e.kind==="solid"?pr(e.color):[]:[]}function yn(e,t,r,o){let n=Math.min(r,o)/2;e.cornerRadius=0,t.kind==="rounded-rect"&&(e.topLeftRadius=h(t.topLeftRadius,0,n),e.topRightRadius=h(t.topRightRadius,0,n),e.bottomRightRadius=h(t.bottomRightRadius,0,n),e.bottomLeftRadius=h(t.bottomLeftRadius,0,n))}function Sn(e,t){if(!t){e.strokes=[];return}e.strokes=[{type:"SOLID",color:{r:t.color.r/255,g:t.color.g/255,b:t.color.b/255},opacity:h(t.color.a/255,0,1)}],e.strokeWeight=Math.max(0,t.width),e.strokeAlign=t.position==="inside"?"INSIDE":t.position==="outside"?"OUTSIDE":"CENTER"}function ve(e,t,r){e.name=t.name||(t.kind==="group"?"Group":"Layer"),e.visible=t.visible,"opacity"in e&&typeof e.opacity=="number"&&(e.opacity=h(t.opacity,0,1)),"blendMode"in e&&(e.blendMode=J(t.blendMode,r)),bn(e,t.effects),Pn(e,t.strokeEffect),function(o,n){/*PIGMA_TEXT_IMPORT_GUARD::SOURCE_ID_TAGGING*/if(!o||typeof o.setPluginData!="function"||!n||n.id==null)return;try{o.setPluginData("__pigmaImportSourceId",String(n.id)),o.setPluginData("__pigmaImportSourceKind",String(n.kind||""))}catch(i){}}(e,t)}function bn(e,t){if(!("effects"in e))return;let r=e;r.effects=En(t)}function En(e){if(!e)return[];let t=[];for(let r of e)switch(r.style){case"drop-shadow":t.push({type:"DROP_SHADOW",color:ye(r.color),offset:{x:r.offsetX,y:r.offsetY},radius:r.blur,spread:r.spread,visible:!0,blendMode:J(r.blendMode,!1),showShadowBehindNode:r.showBehindTransparentAreas});break;case"inner-shadow":t.push({type:"INNER_SHADOW",color:ye(r.color),offset:{x:r.offsetX,y:r.offsetY},radius:r.blur,spread:r.spread,visible:!0,blendMode:J(r.blendMode,!1)});break;case"outer-glow":t.push({type:"DROP_SHADOW",color:ye(r.color),offset:{x:0,y:0},radius:r.blur,spread:r.spread,visible:!0,blendMode:J(r.blendMode,!1),showShadowBehindNode:!0});break;case"inner-glow":t.push({type:"INNER_SHADOW",color:ye(r.color),offset:{x:0,y:0},radius:r.blur,spread:r.spread,visible:!0,blendMode:J(r.blendMode,!1)});break;case"layer-blur":t.push({type:"LAYER_BLUR",blurType:"NORMAL",radius:r.blurType==="PROGRESSIVE"?Math.max(r.startRadius,r.radius):r.radius,visible:!0});break;case"background-blur":t.push({type:"BACKGROUND_BLUR",blurType:"NORMAL",radius:r.radius,visible:!0});break;case"noise":case"texture":break}return t}function ye(e){return{r:h(e.r/255,0,1),g:h(e.g/255,0,1),b:h(e.b/255,0,1),a:h(e.a/255,0,1)}}function Pn(e,t){if(!t||!("strokes"in e)||!("strokeWeight"in e)||!("strokeAlign"in e))return;let r=e;r.strokes=[{type:"SOLID",color:{r:t.color.r/255,g:t.color.g/255,b:t.color.b/255},opacity:h(t.color.a/255,0,1),blendMode:J(t.blendMode,!1)}],r.strokeWeight=Math.max(0,t.width),r.strokeAlign=t.position==="inside"?"INSIDE":t.position==="outside"?"OUTSIDE":"CENTER"}function J(e,t){switch(e){case"pass through":return t?"PASS_THROUGH":"NORMAL";case"darken":return"DARKEN";case"multiply":return"MULTIPLY";case"linear burn":return"LINEAR_BURN";case"color burn":return"COLOR_BURN";case"lighten":return"LIGHTEN";case"screen":return"SCREEN";case"linear dodge":return"LINEAR_DODGE";case"color dodge":return"COLOR_DODGE";case"overlay":return"OVERLAY";case"soft light":return"SOFT_LIGHT";case"hard light":return"HARD_LIGHT";case"difference":return"DIFFERENCE";case"exclusion":return"EXCLUSION";case"hue":return"HUE";case"saturation":return"SATURATION";case"color":return"COLOR";case"luminosity":return"LUMINOSITY";default:return"NORMAL"}}async function se(){var n,i,a;let e=be;if(!e)return;if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e);return}O=e.developerExportExperiments;let t=e.nextIndex;if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e);return}if(t>=e.rootCount){N({type:"export-finished",fileName:e.bundleFileName,rootCount:e.rootCount}),e.rootCount===1&&e.singlePayloadSummary?(n=S.notify)==null||n.call(S,w(P.locale,"PSD ready: ".concat(e.singlePayloadSummary.exportNodeCount," layers, ").concat(e.singlePayloadSummary.editableTextCount," text candidates"))):(i=S.notify)==null||i.call(S,w(P.locale,"".concat(e.rootCount," PSD files are ready. The download will be packaged as a ZIP archive."))),be=null,Qe(),le();return}let r=(e.roots||e.nodes)[t],o="node"in r?r.node:r,s="node"in r?r:null,u=f(o);ae("build-marker",{patch:"root-preserved-20260319-1949",main:"code.patched.js"});try{let l=await Dn(o,e.hiddenLayerMode,e.settings,t+1,e.rootCount,e.includeCompositePng,s);if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e,l);return}e.rootCount===1&&(e.singlePayloadSummary={exportNodeCount:l.exportNodeCount,editableTextCount:l.editableTextCount});let c=Xe(l);e.nextIndex+=1,N({type:"export-root-ready",fileName:e.bundleFileName,rootIndex:t+1,rootCount:e.rootCount,payload:c}),qe(l),e.nextIndex>=e.rootCount&&await se()}catch(l){if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e);return}let c=await Nn(o,e.hiddenLayerMode,e.settings,t+1,e.rootCount,e.includeCompositePng,l,s);if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e,c);return}if(c){e.rootCount===1&&(e.singlePayloadSummary={exportNodeCount:c.exportNodeCount,editableTextCount:c.editableTextCount});let p=Xe(c);e.nextIndex+=1,N({type:"export-root-ready",fileName:e.bundleFileName,rootIndex:t+1,rootCount:e.rootCount,payload:p}),qe(c),e.nextIndex>=e.rootCount&&await se();return}let g=await Tn(o,e.hiddenLayerMode,e.settings,t+1,e.rootCount,e.includeCompositePng,l,s);if(pigmaExportWasCancelled(e)){pigmaCleanupCancelledExport(e,g);return}if(g){e.rootCount===1&&(e.singlePayloadSummary={exportNodeCount:g.exportNodeCount,editableTextCount:g.editableTextCount});let p=Xe(g);e.nextIndex+=1,N({type:"export-root-ready",fileName:e.bundleFileName,rootIndex:t+1,rootCount:e.rootCount,payload:p}),qe(g),e.nextIndex>=e.rootCount&&await se();return}be=null,Qe(),le();let y=w(P.locale,l instanceof Error?e.rootCount>1?'Export failed for "'.concat(u,'" (').concat(t+1,"/").concat(e.rootCount,"): ").concat(l.message):l.message:"An unknown error happened while building the PSD.");N({type:"export-error",message:y}),(a=S.notify)==null||a.call(S,y,{error:!0})}}async function Nn(e,t,r,o,n,i,a,s=null){let l=s?s.documentBounds:xe(e);if(!l)return null;let u=s?s.analysis:Ie(e,t);return u.exportNodeCount===0||!ir(l,u.containsHeavyEffects)?null:await fr(e,t,r,o,n,i,u,a instanceof Error&&a.message.trim().length>0?a.message.trim():null,s)}async function Tn(e,t,r,o,n,i,a,s=null){let l=s?s.documentBounds:xe(e);if(!l)return null;let u=s?s.analysis:Ie(e,t);if(u.exportNodeCount===0||!nr(l,u.containsHeavyEffects))return null;let c=f(e),p=d(l.width),g=d(l.height),y=new Set(u.warnings);y.add(Xo(c,p,g)),y.add(Ko(c,a instanceof Error&&a.message.trim().length>0?a.message.trim():"unknown error")),N({type:"export-started",rootName:c,rootIndex:o,rootCount:n,total:1}),N({type:"export-progress",rootName:c,rootIndex:o,rootCount:n,current:1,total:1,layerName:c});let m=await e.exportAsync({format:"PNG",useAbsoluteBounds:l.useAbsoluteBounds});return{fileName:He(e,r,o,n),rootName:e.name||"Untitled",documentWidth:p,documentHeight:g,compositePngBytes:i?m:null,nodes:[{kind:"bitmap",id:"".concat(e.id,":safe-root"),name:c,sourceType:"".concat(e.type,"_SAFE_ROOT"),opacity:1,visible:!0,blendMode:"normal",effects:null,strokeEffect:null,x:0,y:0,width:p,height:g,nodeTransform:null,pngBytes:m}],warnings:Array.from(y),exportNodeCount:1,editableTextCount:0,preservedGroupCount:0,hasEditableText:!1,backgroundDebug:[]}}async function Dn(e,t,r,o,n,i,a=null){let s=Date.now(),l=a?a.documentBounds:xe(e);if(!l)throw new Error("Unable to calculate document bounds for the selected root.");let u=a?a.analysis:Ie(e,t);if(u.exportNodeCount===0)throw new Error(t==="ignore-hidden"?"No exportable layers remain after ignoring hidden layers.":"There are no visible exportable layers inside this selection.");let c=lt(l,u.containsHeavyEffects);ae("payload-start",{rootName:f(e),rootIndex:o,rootCount:n,includeCompositePng:i,exportNodeCount:u.exportNodeCount,documentWidth:d(l.width),documentHeight:d(l.height),longFrameMode:c});let p={root:e,documentBounds:l,totalLeaves:u.exportNodeCount,currentLeaf:0,warnings:new Set(u.warnings),editableTextCount:0,preservedGroupCount:0,hiddenLayerMode:t,settings:r,backgroundDebug:[],rootName:f(e),rootIndex:o,rootCount:n,lastProgressPostedAt:0,longFrameMode:c};c&&p.warnings.add(ut(d(l.width),d(l.height),u.containsHeavyEffects)),N({type:"export-started",rootName:p.rootName,rootIndex:p.rootIndex,rootCount:p.rootCount,total:p.totalLeaves});let g=mt(e,t),y=[];for(let m of g){let T=await ct(m,p);T&&y.push(T)}if(!y.length)throw new Error("The exporter could not produce any PSD layers from this selection.");ae("payload-nodes-built",{rootName:p.rootName,nodeCount:y.length,warningCount:p.warnings.size,editableTextCount:p.editableTextCount,durationMs:Date.now()-s});let C=i?await e.exportAsync({format:"PNG",useAbsoluteBounds:l.useAbsoluteBounds}):null;return C&&ae("payload-composite-exported",{rootName:p.rootName,bytes:C.byteLength,durationMs:Date.now()-s}),{fileName:He(e,r,o,n),rootName:e.name||"Untitled",documentWidth:d(l.width),documentHeight:d(l.height),compositePngBytes:C,nodes:y,warnings:Array.from(p.warnings),exportNodeCount:p.totalLeaves,editableTextCount:p.editableTextCount,preservedGroupCount:p.preservedGroupCount,hasEditableText:p.editableTextCount>0,backgroundDebug:p.backgroundDebug}}async function fr(e,t,r,o,n,i,a,s=null,l=null){let u=l?l.documentBounds:xe(e);if(!u)throw new Error("Unable to calculate document bounds for the selected root.");let c=lt(u,a.containsHeavyEffects),p=f(e),g=d(u.width),y=d(u.height),m=new Set(a.warnings);m.add(Wo(p,g,y)),s&&m.add(Zo(p,s));let T={root:e,documentBounds:u,totalLeaves:a.exportNodeCount,currentLeaf:0,warnings:m,editableTextCount:0,preservedGroupCount:0,hiddenLayerMode:t,settings:r,backgroundDebug:[],rootName:p,rootIndex:o,rootCount:n,lastProgressPostedAt:0,longFrameMode:c};c&&T.warnings.add(ut(g,y,a.containsHeavyEffects)),ae("payload-safe-layered-start",{rootName:p,rootIndex:o,rootCount:n,includeCompositePng:i,exportNodeCount:a.exportNodeCount,documentWidth:g,documentHeight:y,longFrameMode:c}),N({type:"export-started",rootName:T.rootName,rootIndex:T.rootIndex,rootCount:T.rootCount,total:T.totalLeaves});let C=t==="ignore-hidden"&&!q(e)?[]:[e];ae("payload-safe-layered-entry",{rootName:p,entryCount:C.length,entryMode:"root-preserved"});let E=[];for(let R of C){let wt=await mr(R,T);wt&&E.push(wt)}if(!E.length)throw new Error("The exporter could not produce any PSD layers from this selection.");let k=i?await e.exportAsync({format:"PNG",useAbsoluteBounds:u.useAbsoluteBounds}):null;return{fileName:He(e,r,o,n),rootName:e.name||"Untitled",documentWidth:g,documentHeight:y,compositePngBytes:k,nodes:E,warnings:Array.from(T.warnings),exportNodeCount:T.currentLeaf,editableTextCount:T.editableTextCount,preservedGroupCount:T.preservedGroupCount,hasEditableText:T.editableTextCount>0,backgroundDebug:T.backgroundDebug}}async function mr(e,t){if(t.hiddenLayerMode==="ignore-hidden"&&!q(e))return null;if(Cr(e)){let r=await gr(e,t,Ut(e));if(r.length>0)return t.preservedGroupCount+=1,{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:null,strokeEffect:null,mask:containerMask(e,t.documentBounds,t.root),children:r}}let r=e===t.root&&V(e)&&e.children.length>0&&Ur(e),o=r||hi(e);if(o){let n=await kn(e,t,Ut(e));if(n)return t.preservedGroupCount+=1,n;let i=await gr(e,t);if(i.length>0)return t.warnings.add("\"".concat(f(e),"\" could not separate its background cleanly, so it preserved the child layers without a synthetic background.")),t.preservedGroupCount+=1,{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:null,strokeEffect:null,mask:containerMask(e,t.documentBounds,t.root),children:i};t.warnings.add("\"".concat(f(e),"\" could not separate its background cleanly, so it was flattened."))}return await xr(e,t)}function vn(e,t){if(!Cr(e))return!1;let r=ue(e,t);return r.length===0?!1:r.every(o=>o.kind==="node")}async function wn(e,t){let r=[],o=ue(e,t.hiddenLayerMode);for(let n of o){if(n.kind!=="node")return await xr(e,t);let i=await mr(n.node,t);i&&r.push(i)}return r.length===0?null:(t.preservedGroupCount+=1,{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:null,strokeEffect:null,mask:containerMask(e,t.documentBounds,t.root),children:r})}async function xr(e,t){var n,i;let r=e.type==="TEXT"&&(n=ge(e,!1))!=null?n:v(e);if(!r)return t.warnings.add('"'.concat(f(e),'" was skipped because it has no exportable bounds.')),null;let a=oa(e),s=a!=null&&a.warning?a.warning:null;s&&t.warnings.add(s);let l=await hr(e,t,null),u=l.effects,c=_(u),p=me(e),g=progressiveBlurShouldRasterize(u),y=Re(e)?Nr(e):null,m=(e.type==="LINE"||e.type==="VECTOR")&&!!p&&!(y!=null&&y.fill),h=a||u||p||c?g?{normalizePaintOpacity:(a==null?void 0:a.normalizePaintOpacity)===!0,normalizePaintBlendMode:(a==null?void 0:a.normalizePaintBlendMode)===!0}:{normalizePaintOpacity:(a==null?void 0:a.normalizePaintOpacity)===!0,normalizePaintBlendMode:(a==null?void 0:a.normalizePaintBlendMode)===!0,removeSupportedEffects:l.removeSupportedEffects||!!c,removeSupportedStroke:!!p&&!m}:void 0,T=g?((i=v(e))!=null?i:r):a||u||p||c?((i=tt(e))!=null?i:r):r;if(t.longFrameMode&&pe(e)&&De(r)){t.currentLeaf+=1,Y(t,f(e));let C=await pt(e,r,h,t.documentBounds);return t.warnings.add(Te(f(e),C.length)),Be(e.id,f(e),e.type,a?a.effectiveOpacity:j(e),e.visible,at(a?a.effectiveBlendMode:K(e)),C)}g&&t.warnings.add('"'.concat(f(e),'" kept its layer blur as a bitmap layer for closer Photoshop matching.'));let E=await Me(e,t,T,h);return E?{kind:"bitmap",id:e.id,name:f(e),sourceType:e.type,opacity:a?a.effectiveOpacity:j(e),visible:e.visible,blendMode:at(a?a.effectiveBlendMode:K(e)),effects:g?null:u,strokeEffect:g||m?null:p,x:E.x,y:E.y,width:E.width,height:E.height,nodeTransform:de(e,t.documentBounds,E.x,E.y),pngBytes:E.pngBytes}:null}function Xe(e){return B(b({},e),{warnings:e.warnings.map(t=>w(P.locale,t))})}async function ct(e,t,r=null){var i,a,s;if(t.hiddenLayerMode==="ignore-hidden"&&!q(e))return null;let o=e.type==="TEXT"?await Hn(e):null;if(o&&!o.ok)return t.warnings.add(o.reason),await Cn(e,t);let n=Fe(e);if(n==="group"){let l=e,u=await gr(l,t,(i=Ut(l))!=null?i:r);if(u.length>0)return t.preservedGroupCount+=1,{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:null,strokeEffect:null,mask:containerMask(l,t.documentBounds,t.root),children:u}}if(n==="split"){let l=await kn(e,t,(a=Ut(e))!=null?a:r);if(l)return t.preservedGroupCount+=1,l;let u=await gr(e,t,r);if(u.length>0)return t.warnings.add("\"".concat(f(e),"\" could not separate its background cleanly, so it preserved the child layers without a synthetic background.")),t.preservedGroupCount+=1,{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:null,strokeEffect:null,mask:containerMask(e,t.documentBounds,t.root),children:u};t.warnings.add("\"".concat(f(e),"\" could not separate its background cleanly, so it was flattened."))}if(progressiveBlurShouldRasterize(L(e,t.root)))return await qn(e,t,r);if(e.type==="TEXT"&&t.settings.textExportMode!=="rasterize-text"){let l=await Gn(e,t,r);if(l)return _(l.effects)||(t.editableTextCount+=1),l}if(Re(e)){let l=(s=ft(e))!=null?s:v(e),u=!t.longFrameMode?await pigmaExportMultiFillGroup(e,t,r,l):null;if(u)return t.preservedGroupCount+=1,u;if(t.longFrameMode&&!!l&&Ne(d(l.width),d(l.height),!1))t.warnings.add(jo(f(e)));else{let c=await Jn(e,t);if(c)return c;t.warnings.add("\"".concat(f(e),"\" could not keep its SVG/vector data, so it fell back to a bitmap layer."))}}if(V(e)&&e.children.length>0){let l=L(e);_(l)?t.warnings.add(Mr(e,"past")):ze(e)?t.warnings.add(Ir(e,"past")):$e(l)?t.warnings.add(Rr(e,"past")):Pt(l)?t.warnings.add(Ar(e,"past")):t.warnings.add(Br(e,"past"))}return await qn(e,t,r)}async function Cn(e,t){var a;let r=e.type==="TEXT"&&(a=ge(e,!1))!=null?a:v(e);if(!r)return t.warnings.add('"'.concat(f(e),'" was skipped because it has no exportable bounds.')),null;if(t.currentLeaf+=1,Y(t,f(e)),t.longFrameMode&&pe(e)&&De(r)){let s=await pt(e,r,void 0,t.documentBounds);return t.warnings.add(Te(f(e),s.length)),Be(e.id,f(e),e.type,1,e.visible,"normal",s)}let o=await e.exportAsync({format:"PNG",useAbsoluteBounds:r.useAbsoluteBounds}),n=x(r.x-t.documentBounds.x),i=x(r.y-t.documentBounds.y);return{kind:"bitmap",id:e.id,name:f(e),sourceType:e.type,opacity:1,visible:e.visible,blendMode:"normal",effects:null,strokeEffect:null,x:n,y:i,width:d(r.width),height:d(r.height),nodeTransform:de(e,t.documentBounds,n,i),pngBytes:o}}async function kn(e,t,r=null){let o=await gr(e,t,r),n=await pigmaExportMultiFillBackgroundGroup(e,t,r);if(n)return n.backgroundLayer&&(o.push(n.backgroundLayer)),{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:n.groupEffects,strokeEffect:n.groupStrokeEffect,mask:containerMask(e,t.documentBounds,t.root),children:o};let i=L(e,t.root),a=me(e),s=await Rn(e,t),l=Bn(s,i,a);return!l.backgroundLayer&&o.length===0?null:(l.backgroundLayer&&(o.push(l.backgroundLayer),(l.backgroundLayer.kind==="bitmap"||l.backgroundLayer.kind==="shape")&&ha(t.backgroundDebug,ga(e,l.backgroundLayer,o.length-1,o.length))),{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:l.groupEffects,strokeEffect:l.groupStrokeEffect,mask:containerMask(e,t.documentBounds,t.root),children:o})}function Bn(e,t,r){if(!e)return{backgroundLayer:null,groupEffects:t,groupStrokeEffect:r};let o=Hi(t),n=Vi(t);if(!o&&!r)return{backgroundLayer:e,groupEffects:n,groupStrokeEffect:null};if(e.kind==="group")return{backgroundLayer:e,groupEffects:null,groupStrokeEffect:null};let i=e.kind==="shape"&&e.stroke?null:r;return{backgroundLayer:B(b({},e),{effects:o,strokeEffect:i}),groupEffects:n,groupStrokeEffect:null}}async function gr(e,t,r=null){let o=[],n=ue(e,t.hiddenLayerMode);for(let i of n){let a=await Mn(i,t,r);a&&o.push(a)}return o}async function Mn(e,t,r=null){return e.kind==="node"?await ct(e.node,t,r):await In(e,t,r)}async function In(e,t,r=null){var a;let o=(a=Lr(e.maskNode,xt(e.maskNode)))!=null?a:r,n=[];for(let s of e.maskedNodes){let l=await ct(s,t,o);l&&n.push(l)}if(n.length===0)return null;let i=await Si(e.maskNode,t);return i?(t.preservedGroupCount+=1,{kind:"group",id:"".concat(e.maskNode.id,":mask-group"),name:f(e.maskNode),sourceType:"MASK_OBJECT",opacity:1,visible:e.maskNode.visible,blendMode:"pass through",effects:null,strokeEffect:null,mask:i,children:n}):(t.warnings.add('"'.concat(f(e.maskNode),'" could not be reconstructed as a PSD mask.')),null)}async function Rn(e,t){if(!Ln(e))return null;let r=An(e,t.documentBounds,t);if(r)return r;if(!Ur(e))return null;let o=Fn(e);if(!o)return null;if(!await preloadTreeFontsSafely(e))return null;let n=oa(e),i=n&&((n.normalizePaintOpacity===!0)||(n.normalizePaintBlendMode===!0))?n:null,a=!!L(e,t.root)||!!fe(e,t.root),s=me(e),l=e.clone();try{try{if(Ii(l),i&&to(l,i),bt(l,o),t.longFrameMode&&De(o)){let u=await Er(l,o,t.documentBounds);return t.warnings.add(Te("".concat(f(e)," background"),u.length)),Be("".concat(e.id,":background"),"Background","".concat(e.type,"_BACKGROUND"),i?i.effectiveOpacity:1,!0,at(i?i.effectiveBlendMode:"normal"),u)}let c=await Me(l,t,o,{normalizePaintOpacity:(i==null?void 0:i.normalizePaintOpacity)===!0,normalizePaintBlendMode:(i==null?void 0:i.normalizePaintBlendMode)===!0,removeSupportedEffects:a,removeSupportedStroke:!!s});return c?{kind:"bitmap",id:"".concat(e.id,":background"),name:"Background",sourceType:"".concat(e.type,"_BACKGROUND"),opacity:i?i.effectiveOpacity:1,visible:!0,blendMode:at(i?i.effectiveBlendMode:"normal"),effects:null,strokeEffect:null,x:c.x,y:c.y,width:c.width,height:c.height,nodeTransform:null,pngBytes:c.pngBytes}:null}catch(c){return null}}finally{l.removed||l.remove()}}function An(e,t,r){if(fe(e))return null;let o=Di(e),n=oa(e),i=n&&((n.normalizePaintOpacity===!0)||(n.normalizePaintBlendMode===!0))?n:null;if(!o)return null;let a=Fr(e,t);return!a||!o.fill&&!o.stroke?null:{kind:"shape",id:"".concat(e.id,":background-shape"),name:"Background",sourceType:"".concat(e.type,"_BACKGROUND_SHAPE"),opacity:i?i.effectiveOpacity:1,visible:!0,blendMode:at(i?i.effectiveBlendMode:"normal"),effects:null,strokeEffect:null,x:a.x,y:a.y,width:a.width,height:a.height,nodeTransform:null,shape:a,fill:o.fill,stroke:o.stroke}}function Ln(e){return X(e,"fills")||X(e,"strokes")?!0:te(e)}function Fn(e){let t=k(e);if(t&&t.width>0&&t.height>0)return{x:t.x,y:t.y,width:t.width,height:t.height,useAbsoluteBounds:!0};if("absoluteTransform"in e&&"width"in e&&"height"in e){let r=ce(e);if(r)return r}return v(e)}function tt(e){var r;let o=Zr(e)||re(e),n=o?v(e):null;if(e.type==="TEXT")return n!=null?n:v(e);if(Re(e))return n!=null?n:(r=ft(e))!=null?r:v(e);if(n)return n;let t=k(e);if(t&&t.width>0&&t.height>0)return{x:t.x,y:t.y,width:t.width,height:t.height,useAbsoluteBounds:!0};if("absoluteTransform"in e&&"width"in e&&"height"in e){let i=ce(e);if(i)return i}return v(e)}function Ut(e){if(!ht(e))return null;let t=k(e);if(t&&t.width>0&&t.height>0)return{x:t.x,y:t.y,width:t.width,height:t.height,useAbsoluteBounds:!0};if("absoluteTransform"in e&&"width"in e&&"height"in e){let r=ce(e);if(r)return r}return null}async function hr(e,t,r=null){let o=L(e,t.root);if(!_e(o))return{effects:o,removeSupportedEffects:!!o};if(t.longFrameMode){t.warnings.add($t(f(e)));let a=jt(o);return{effects:a,removeSupportedEffects:!!a}}let i=await Ri(e,t,r);if(!i){t.warnings.add($t(f(e)));let a=jt(o);return{effects:a,removeSupportedEffects:!!a}}return{effects:_i(o,i),removeSupportedEffects:!0}}async function Gn(e,t,r=null){var n;let o=e;try{try{o=(n=await $n(e))!=null?n:e}catch(y){t.warnings.add('"'.concat(f(e),'" could not prepare its editable text clone because Figma could not load one of its fonts, so export continued without the point-text auto-fix.')),o=e}let i=Tr(o,t.documentBounds);if(!i){let y=Pr(o);return y.supported||t.warnings.add(y.reason),null}let a=jr(o,i),s=await hr(e,t,r),l=s.effects,u=_(l),c=me(e);u&&t.warnings.add(eo(f(e)));let p=await zn(o,i,{riskScore:a,removeSupportedEffects:s.removeSupportedEffects||!!u,removeSupportedStroke:!!c});if(!p)return null;_n(i,p.bounds,t.documentBounds,p.usedVisualProbe);let g=await On(o,t,i,p.bounds,{removeSupportedEffects:s.removeSupportedEffects||!!u,removeSupportedStroke:!!c});return g?{kind:"text",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:ro(e),effects:l,strokeEffect:c,x:g.x,y:g.y,width:g.width,height:g.height,nodeTransform:de(o,t.documentBounds,g.x,g.y),pngBytes:g.pngBytes,text:i}:null}finally{o!==e&&!o.removed&&o.remove()}}async function On(e,t,r,o,n){t.currentLeaf+=1,Y(t,f(e));let i=yt(e)||(n==null?void 0:n.normalizePaintOpacity)===!0||(n==null?void 0:n.normalizePaintBlendMode)===!0||(n==null?void 0:n.removeAllEffects)===!0||(n==null?void 0:n.removeSupportedEffects)===!0||(n==null?void 0:n.removeSupportedStroke)===!0,a=Un(e,r.shapeType,o),s=r.shapeType==="point"||a?await Hr(e,o,n,i):i?await $r(e,o,t,n):await e.exportAsync({format:"PNG",useAbsoluteBounds:o.useAbsoluteBounds});return{x:x(o.x-t.documentBounds.x),y:x(o.y-t.documentBounds.y),width:d(o.width),height:d(o.height),pngBytes:s}}function Un(e,t,r){if(t==="point")return!0;let o=Ae(e,t);return o?Math.abs(o.x-r.x)>.5||Math.abs(o.y-r.y)>.5||Math.abs(o.width-r.width)>.5||Math.abs(o.height-r.height)>.5:!0}async function zn(e,t,r){var a,s;let o=Ae(e,t.shapeType);if(!o)return null;let n=((a=r==null?void 0:r.riskScore)!=null?a:0)>=No?Wr(t,(s=r==null?void 0:r.riskScore)!=null?s:0):null;if(t.shapeType!=="point"){let l=I(e);if(l&&l.width>0&&l.height>0){let u=Vt(o,{x:l.x,y:l.y,width:l.width,height:l.height,useAbsoluteBounds:!1});return{bounds:ie(u,Se(Ht(t),n)),usedVisualProbe:!1}}return{bounds:ie(o,Se(Ht(t),n)),usedVisualProbe:!1}}let i=await Li(e,o,r);if(i){let l=Se(it(e,t),n);return{bounds:ie(Vt(o,i),l),usedVisualProbe:!0}}return{bounds:ie(o,Se(it(e,t),n)),usedVisualProbe:!1}}function Se(e,t){return t?{left:Math.max(e.left,t.left),top:Math.max(e.top,t.top),right:Math.max(e.right,t.right),bottom:Math.max(e.bottom,t.bottom)}:e}function _n(e,t,r,o){let n=x(t.x-r.x),i=x(t.y-r.y),a=x(n+t.width),s=x(i+t.height);e.bounds={left:n,top:i,right:a,bottom:s},e.boundingBox={left:n,top:i,right:a,bottom:s},e.hasVisualBoundsProbe=o}async function $n(e){if(!Wn(e))return null;let t=Zn(e);if(!t)return null;await we(e);let r=e.clone();try{return figma.currentPage.appendChild(r),r.visible=!0,r.textAutoResize="NONE",r.resizeWithoutConstraints(d(t.width),d(t.height)),Xn(e,r,t.x,t.y),Kn(e,r),r}catch(o){throw r.removed||r.remove(),o}}async function we(e){if(!e.characters||e.characters.length===0)return;let t=new Map;for(let r of e.getStyledTextSegments(Kt)){let o=r.fontName,n=ke(o);t.has(n)||t.set(n,o)}await Promise.all(Array.from(t.values(),r=>Ce(r)))}async function dt(e){let t=[],r=[e];for(;r.length>0;){let o=r.pop();if(o){if(o.type==="TEXT"){t.push(o);continue}"children"in o&&r.push(...o.children)}}await Promise.all(t.map(o=>we(o)))}async function preloadTreeFontsSafely(e){try{return await dt(e),!0}catch(t){return!1}}async function outlineLongFrameFallbackTiles(e,t,r=null){let o="";try{o=await e.exportAsync({format:"SVG_STRING",useAbsoluteBounds:t.useAbsoluteBounds,svgOutlineText:!0,svgIdAttribute:!0,svgSimplifyStroke:!1})}catch(i){return null}if(!o||o.trim().length===0)return null;let n=figma.createNodeFromSvg(o);try{let i=("absoluteBoundingBox"in n?n.absoluteBoundingBox:null)||("absoluteRenderBounds"in n?n.absoluteRenderBounds:null);return i&&("x"in n&&(n.x=x(n.x+(t.x-i.x))),"y"in n&&(n.y=x(n.y+(t.y-i.y)))),await Er(n,t,r)}catch(i){return n.removed||n.remove(),null}}async function Hn(e){if(!e.characters||e.characters.length===0)return{ok:!0};let t=new Map;for(let o of e.getStyledTextSegments(Kt)){let n=o.fontName,i=ke(n);t.has(i)||t.set(i,n)}let r=await Vn();for(let[o,n]of t)if(!r.has(o))return{ok:!1,reason:'"'.concat(f(e),'" uses "').concat(yr(n),'", which is not available in this Figma session, so it fell back to a flattened bitmap layer.')};try{return await Promise.all(Array.from(t.values(),o=>Ce(o))),{ok:!0}}catch(o){return{ok:!1,reason:'"'.concat(f(e),'" uses a font that Figma could not prepare for text editing (').concat(jn(o),"), so it fell back to a flattened bitmap layer.")}}}async function Ce(e){let t=ke(e),r=je.get(t);if(r){await r;return}let o=figma.loadFontAsync(e).catch(n=>{throw je.delete(t),new Error('font "'.concat(yr(e),'": ').concat(Sr(n)))});je.set(t,o),await o}function ke(e){return"".concat(e.family,"\0").concat(e.style)}async function Vn(){return Ze||(Ze=dr().then(e=>new Set(e.map(t=>ke(t.fontName))))),await Ze}function yr(e){return"".concat(e.family," ").concat(e.style).trim()}function jn(e){let t=Sr(e);return t.length>0?t:"unknown font loading error"}function Sr(e){return e instanceof Error&&typeof e.message=="string"&&e.message.trim().length>0?e.message.trim():typeof e=="string"&&e.trim().length>0?e.trim():"unknown error"}function Wn(e){if(e.textAutoResize!=="WIDTH_AND_HEIGHT")return!1;let t=k(e),r=I(e);if(!t||!r)return!1;let o=br(t,r),n=Le(e),i=n.ok?n.baseStyle.fontSize:16,a=Math.max(2,d(i*.02));return o.left>=a||o.top>=a||o.right>=a||o.bottom>=a}function Zn(e){let t=k(e),r=I(e),o=t&&r?Yn(t,r):t!=null?t:r;if(!o)return null;let n=Le(e),i=n.ok?n.baseStyle.fontSize:16,a={left:d(Math.max(4,i*.04)),top:d(Math.max(4,i*.03)),right:d(Math.max(8,i*.1)),bottom:d(Math.max(10,i*.12))};return{x:o.x-a.left,y:o.y-a.top,width:o.width+a.left+a.right,height:o.height+a.top+a.bottom,useAbsoluteBounds:!1}}function Xn(e,t,r,o){if("relativeTransform"in t){t.relativeTransform=[[e.absoluteTransform[0][0],e.absoluteTransform[0][1],r],[e.absoluteTransform[1][0],e.absoluteTransform[1][1],o]];return}let n=t;n.x=r,n.y=o}function Kn(e,t){let r=I(e),o=I(t);if(!r||!o)return;let n=x(r.x-o.x),i=x(r.y-o.y);if(n===0&&i===0)return;if("relativeTransform"in t){let s=t.relativeTransform;t.relativeTransform=[[s[0][0],s[0][1],s[0][2]+n],[s[1][0],s[1][1],s[1][2]+i]];return}let a=t;a.x+=n,a.y+=i}function br(e,t){let r=e.x+e.width,o=e.y+e.height,n=t.x+t.width,i=t.y+t.height;return{left:Math.max(0,e.x-t.x),top:Math.max(0,e.y-t.y),right:Math.max(0,n-r),bottom:Math.max(0,i-o)}}function Yn(e,t){let r=Math.min(e.x,t.x),o=Math.min(e.y,t.y),n=Math.max(e.x+e.width,t.x+t.width),i=Math.max(e.y+e.height,t.y+t.height);return{x:r,y:o,width:n-r,height:i-o}}function Be(e,t,r,o,n,i,a){return{kind:"group",id:"".concat(e,":tiles"),name:t,sourceType:r,opacity:o,visible:n,blendMode:i,effects:null,strokeEffect:null,mask:null,children:a.map((s,l)=>({kind:"bitmap",id:"".concat(e,":tile:").concat(l+1),name:"".concat(t," Tile ").concat(l+1),sourceType:"".concat(r,"_TILE"),opacity:1,visible:!0,blendMode:"normal",effects:null,strokeEffect:null,x:s.x,y:s.y,width:s.width,height:s.height,nodeTransform:null,pngBytes:s.pngBytes}))}}async function pt(e,t,r,o=null){let n=(r==null?void 0:r.normalizePaintOpacity)===!0||(r==null?void 0:r.normalizePaintBlendMode)===!0||(r==null?void 0:r.removeAllEffects)===!0||(r==null?void 0:r.removeSupportedEffects)===!0||(r==null?void 0:r.removeSupportedStroke)===!0,i=await preloadTreeFontsSafely(e);if(!i){let a=await outlineLongFrameFallbackTiles(e,t,o);if(a)return a;return[{x:o?x(t.x-o.x):x(t.x),y:o?x(t.y-o.y):x(t.y),width:Math.max(1,d(t.width)),height:Math.max(1,d(t.height)),pngBytes:await e.exportAsync({format:"PNG",useAbsoluteBounds:t.useAbsoluteBounds})}]}let s=e.clone();try{return n&&Oe(s,r),await Er(s,t,o)}finally{s.removed||s.remove()}}async function Er(e,t,r=null){let o=figma.createFrame(),n=Math.max(1,d(t.width)),i=Math.max(1,d(t.height)),a=qo(n,i),s=[];try{o.clipsContent=!0,o.fills=[],o.strokes=[],o.name="__pigma-long-frame-tile__",o.resize(n,Math.min(a,i)),figma.currentPage.appendChild(o),o.appendChild(e);for(let l=0;l<i;l+=a){let u=Math.min(a,i-l),c={x:t.x,y:t.y+l,width:t.width,height:u,useAbsoluteBounds:!1},p=r?x(c.x-r.x):x(c.x),g=r?x(c.y-r.y):x(c.y);o.resize(n,Math.max(1,u)),o.x=x(c.x),o.y=x(c.y),_r(e,t,c),s.push({x:p,y:g,width:n,height:d(u),pngBytes:await o.exportAsync({format:"PNG",useAbsoluteBounds:!1})})}return s}finally{o.removed||o.remove(),e.removed||e.remove()}}async function qn(e,t,r=null){var g,y;let o=oa(e),n=e.type==="TEXT"?Gi(e,t.documentBounds):null,i=(g=n!=null?n:tt(e))!=null?g:v(e),a=o!=null&&o.warning?o.warning:null;if(a&&t.warnings.add(a),!t.longFrameMode){let T=await pigmaExportMultiFillGroup(e,t,r,i);if(T)return T}if(t.longFrameMode&&i&&pe(e)&&De(i)){t.currentLeaf+=1,Y(t,f(e));let T=await pt(e,i,{normalizePaintOpacity:(o==null?void 0:o.normalizePaintOpacity)===!0,normalizePaintBlendMode:(o==null?void 0:o.normalizePaintBlendMode)===!0});return t.warnings.add(Te(f(e),T.length)),Be(e.id,f(e),e.type,o?o.effectiveOpacity:j(e),e.visible,at(o?o.effectiveBlendMode:K(e)),T)}let s=await hr(e,t,r),l=s.effects,u=_(l),c=me(e),p=progressiveBlurShouldRasterize(l),d=Re(e)?Nr(e):null,m=(e.type==="LINE"||e.type==="VECTOR")&&!!c&&!(d!=null&&d.fill),h=o||l||c||u?p?{normalizePaintOpacity:(o==null?void 0:o.normalizePaintOpacity)===!0,normalizePaintBlendMode:(o==null?void 0:o.normalizePaintBlendMode)===!0}:{normalizePaintOpacity:(o==null?void 0:o.normalizePaintOpacity)===!0,normalizePaintBlendMode:(o==null?void 0:o.normalizePaintBlendMode)===!0,removeSupportedEffects:s.removeSupportedEffects||!!u,removeSupportedStroke:!!c&&!m}:void 0,Bounds=p?(n!=null?n:v(e)):n!=null?n:(o||l||c||u)&&(y=tt(e))!=null?y:void 0;p&&t.warnings.add('"'.concat(f(e),'" kept its layer blur as a bitmap layer for closer Photoshop matching.'));let E=n&&e.type==="TEXT"?await Qn(e,t,n,h):await Me(e,t,Bounds,h);return E?{kind:"bitmap",id:e.id,name:f(e),sourceType:e.type,opacity:o?o.effectiveOpacity:j(e),visible:e.visible,blendMode:at(o?o.effectiveBlendMode:K(e)),effects:p?null:l,strokeEffect:p||m?null:c,x:E.x,y:E.y,width:E.width,height:E.height,nodeTransform:de(e,t.documentBounds,E.x,E.y),pngBytes:E.pngBytes}:null}async function Jn(e,t){if(O.forceBitmapVectorPreview)return null;let r=ft(e);if(!r)return null;let o=L(e,t.root),n=_(o),i=me(e),a=Nr(e),s=oa(e),l="fills"in e&&Array.isArray(e.fills)?e.fills.some(W):!1,u=(e.type==="LINE"||e.type==="VECTOR")&&!!i&&!l;if(u)return null;let c=!!i&&!(a!=null&&a.fill)&&!I(e)?Math.max(3,Math.ceil(i.width/2)+2):0,p=c?{x:r.x-c,y:r.y-c,width:r.width+c*2,height:r.height+c*2,useAbsoluteBounds:!1}:r,g=await Me(e,t,o||i||n||s?p:void 0,o||i||n||s?{normalizePaintOpacity:(s==null?void 0:s.normalizePaintOpacity)===!0,normalizePaintBlendMode:(s==null?void 0:s.normalizePaintBlendMode)===!0,removeSupportedEffects:!0,removeSupportedStroke:!!i&&!!a.fill}:void 0);if(!g)return null;let y=x(r.x-t.documentBounds.x),m=x(r.y-t.documentBounds.y),T=d(r.width),C=d(r.height),E=x(g.x-y),R=x(g.y-m),N="";try{N=await e.exportAsync({format:"SVG_STRING",useAbsoluteBounds:r.useAbsoluteBounds,svgOutlineText:!0,svgIdAttribute:!0,svgSimplifyStroke:!1})}catch(F){return null}if(!N||N.trim().length===0)return null;return{kind:"vector",id:e.id,name:f(e),sourceType:e.type,opacity:s?s.effectiveOpacity:j(e),visible:e.visible,blendMode:at(s?s.effectiveBlendMode:K(e)),effects:o,strokeEffect:a.fill?i:null,x:y,y:m,width:T,height:C,nodeTransform:de(e,t.documentBounds,y,m),pngBytes:g.pngBytes,strategy:a.strategy,svgString:N,fill:a.fill,previewOffsetX:E,previewOffsetY:R}}async function Qn(e,t,r,o){t.currentLeaf+=1,Y(t,f(e));let n=yt(e)||(o==null?void 0:o.normalizePaintOpacity)===!0||(o==null?void 0:o.normalizePaintBlendMode)===!0||(o==null?void 0:o.removeAllEffects)===!0||(o==null?void 0:o.removeSupportedEffects)===!0||(o==null?void 0:o.removeSupportedStroke)===!0,i=await Hr(e,r,o,n);return{x:x(r.x-t.documentBounds.x),y:x(r.y-t.documentBounds.y),width:d(r.width),height:d(r.height),pngBytes:i}}async function Me(e,t,r,o){let n=r!=null?r:v(e);if(!n)return t.warnings.add('"'.concat(f(e),'" was skipped because it has no exportable bounds.')),null;t.currentLeaf+=1,Y(t,f(e));let a=yt(e)||(o==null?void 0:o.normalizePaintOpacity)===!0||(o==null?void 0:o.normalizePaintBlendMode)===!0||(o==null?void 0:o.removeAllEffects)===!0||(o==null?void 0:o.removeSupportedEffects)===!0||(o==null?void 0:o.removeSupportedStroke)===!0?await $r(e,n,t,o):await e.exportAsync({format:"PNG",useAbsoluteBounds:n.useAbsoluteBounds});return{x:x(n.x-t.documentBounds.x),y:x(n.y-t.documentBounds.y),width:d(n.width),height:d(n.height),pngBytes:a}}function Y(e,t){let r=Date.now();!(e.currentLeaf>=e.totalLeaves)&&r-e.lastProgressPostedAt<Eo||(e.lastProgressPostedAt=r,N({type:"export-progress",rootName:e.rootName,rootIndex:e.rootIndex,rootCount:e.rootCount,current:e.currentLeaf,total:e.totalLeaves,layerName:t}))}function Ie(e,t){let r={exportNodeCount:0,editableTextCount:0,preservedGroupCount:0,containsHeavyEffects:!1,warnings:new Set},o=mt(e,t);for(let n of o)rt(n,r,t,e);return r}function rt(e,t,r,o){if(r==="ignore-hidden"&&!q(e))return;Yo(e,o)&&(t.containsHeavyEffects=!0),te(e,o)&&t.warnings.add(yi(e));let n=Fe(e);if(n==="group"){let i=ue(e,r);if(i.length>0){t.preservedGroupCount+=1;for(let a of i)zt(a,t,r,o);return}}if(n==="split"){t.preservedGroupCount+=1,t.exportNodeCount+=1;let i=ue(e,r);for(let a of i)zt(a,t,r,o);return}if(t.exportNodeCount+=1,t.exportNodeCount+=Wi(L(e,o)),e.type==="TEXT"){let i=Pr(e);i.supported?fe(e,o)?t.warnings.add(eo(f(e))):t.editableTextCount+=1:t.warnings.add(i.reason)}else if(Re(e))Nr(e).strategy==="smart-object"&&t.warnings.add('"'.concat(f(e),'" is a complex vector, so it will export as a smart object instead of a native PSD shape layer.'));else if(V(e)&&e.children.length>0){let i=L(e);_(i)?t.warnings.add(Mr(e,"future")):ze(e)?t.warnings.add(Ir(e,"future")):$e(i)?t.warnings.add(Rr(e,"future")):Pt(i)?t.warnings.add(Ar(e,"future")):t.warnings.add(Br(e,"future"))}re(e)&&t.warnings.add('"'.concat(f(e),'" uses masking and will export as a flattened bitmap layer.'))}function zt(e,t,r,o){if(e.kind==="node"){rt(e.node,t,r,o);return}t.preservedGroupCount+=1,t.exportNodeCount+=1;for(let n of e.maskedNodes)rt(n,t,r,o)}function Pr(e){if(!e.characters||e.characters.length===0)return{supported:!1,reason:'"'.concat(f(e),'" is empty, so it falls back to a bitmap layer.')};let t=Le(e);if(!t.ok)return{supported:!1,reason:'"'.concat(f(e),'" falls back to bitmap: ').concat(t.reason)};let r=Dr(e,t.baseStyle);return r.ok?{supported:!0}:{supported:!1,reason:'"'.concat(f(e),'" falls back to bitmap: ').concat(r.reason)}}function Re(e){return e.type==="VECTOR"||e.type==="BOOLEAN_OPERATION"||e.type==="RECTANGLE"||e.type==="ELLIPSE"||e.type==="POLYGON"||e.type==="STAR"||e.type==="LINE"}function Nr(e){if(re(e)||te(e)||fe(e))return{strategy:"smart-object",fill:null};let t=Ti(e);return t?{strategy:"shape",fill:t}:{strategy:"smart-object",fill:null}}function ft(e){let t=me(e),r=Re(e)?Nr(e):null,o=Re(e)&&!!t&&!(r!=null&&r.fill),n=o?I(e):null;if(n&&n.width>0&&n.height>0)return{x:n.x,y:n.y,width:n.width,height:n.height,useAbsoluteBounds:!1};let i=ce(e);if(!i)return v(e);if(!o)return{x:i.x,y:i.y,width:i.width,height:i.height,useAbsoluteBounds:!0};let a=Math.max(3,Math.ceil(t.width/2)+2);return{x:i.x-a,y:i.y-a,width:i.width+a*2,height:i.height+a*2,useAbsoluteBounds:!1}}function ce(e){let t=ei(e.absoluteTransform,e.width,e.height);if(t.length===0)return null;let r=t.map(l=>l.x),o=t.map(l=>l.y),n=Math.min(...r),i=Math.min(...o),a=Math.max(...r),s=Math.max(...o);return!Number.isFinite(n)||!Number.isFinite(i)||!Number.isFinite(a)||!Number.isFinite(s)?null:{x:n,y:i,width:a-n,height:s-i,useAbsoluteBounds:!0}}function ei(e,t,r){return[{x:0,y:0},{x:t,y:0},{x:t,y:r},{x:0,y:r}].map(n=>({x:e[0][0]*n.x+e[0][1]*n.y+e[0][2],y:e[1][0]*n.x+e[1][1]*n.y+e[1][2]}))}function de(e,t,r,o){if(!("absoluteTransform"in e)||!("width"in e)||!("height"in e))return null;let n=oo(e.absoluteTransform,t);return{matrix:[n[0],n[1],n[2],n[3],D(n[4]-r),D(n[5]-o)],width:d(e.width),height:d(e.height)}}function Tr(e,t){let r=Le(e);if(!r.ok)return null;let o=Dr(e,r.baseStyle);if(!o.ok)return null;let n=Ae(e,o.shapeType);if(!n)return null;let i=oo(e.absoluteTransform,t),a=x(n.x-t.x),s=x(n.y-t.y),l=x(a+n.width),u=x(s+n.height);return{value:r.value,transform:i,bounds:{left:a,top:s,right:l,bottom:u},boundingBox:{left:a,top:s,right:l,bottom:u},hasVisualBoundsProbe:!1,sizingMode:o.sizingMode,shapeType:o.shapeType,pointBase:o.pointBase,boxBounds:o.boxBounds,textTruncation:o.textTruncation,maxLines:o.maxLines,justification:ia(e.textAlignHorizontal),baseStyle:r.baseStyle,styleRuns:r.styleRuns}}function Dr(e,t){if(e.textAutoResize==="TRUNCATE")return{ok:!1,reason:"truncate mode is not mapped safely to editable Photoshop text yet"};if(e.textTruncation==="ENDING")return{ok:!1,reason:"ellipsis truncation is not mapped safely to editable Photoshop text yet"};let r=d(e.width),o=d(e.height),n=ti(e);return ri(e,t)?{ok:!0,sizingMode:n,shapeType:"point",pointBase:[0,0],boxBounds:null,textTruncation:"disabled",maxLines:e.maxLines}:e.textAutoResize==="HEIGHT"?{ok:!0,sizingMode:n,shapeType:"box",pointBase:null,boxBounds:[0,0,r,o],textTruncation:"disabled",maxLines:e.maxLines}:{ok:!0,sizingMode:n,shapeType:"box",pointBase:null,boxBounds:[0,0,r,o],textTruncation:"disabled",maxLines:e.maxLines}}function ti(e){return e.textAutoResize==="WIDTH_AND_HEIGHT"?"auto-width":e.textAutoResize==="HEIGHT"?"auto-height":"fixed"}function ri(e,t){return e.textAutoResize==="WIDTH_AND_HEIGHT"?!0:ni(e)?!1:oi(e,t)===1}function oi(e,t){let r=ai(e.characters);if(r>1)return r;let o=ii(t),n=d(e.height);if(!_t(n,o))return 2;let i=I(e);return i&&!_t(i.height,o)?2:1}function _t(e,t){return e<=t*1.45}function ni(e){let t=Math.max(1,d(e.width)),r=I(e);return r?r.width>t*1.2:!1}function ii(e){return e.lineHeightPx!==null&&e.lineHeightPx>0?e.lineHeightPx:Math.max(e.fontSize,e.fontSize*1.2)}function ai(e){return Math.max(1,e.replace(/\r\n?/g,"\n").split("\n").length)}function vr(e){return e.replace(/\r\n?/g,"\n").replace(/\s+/g,"").length}function Ae(e,t){return t==="point"?ge(e,!1):v(e)}function Le(e){let t=e.getStyledTextSegments([...bo]);if(t.length===0)return{ok:!1,reason:"the text node has no style segments"};let r=[],o="";for(let n of t){let i=si(n);if(!i)return{ok:!1,reason:"it uses unsupported fills or text styling for editable export"};o+=li(e.characters.slice(n.start,n.end),n.textCase),r.push({length:n.end-n.start,style:i})}return{ok:!0,value:o,baseStyle:r[0].style,styleRuns:r}}function si(e){let t=di(e.fills);return t?{photoshopFontName:sa(e.fontName),fontFamily:e.fontName.family,fontStyle:e.fontName.style,fontWeight:pigmaPhotoshopFontWeight(e.fontName.style),fontSize:Math.max(1,e.fontSize),fillColor:t,lineHeightPx:aa(e.lineHeight,e.fontSize),tracking:ci(e.letterSpacing,e.fontSize),fontCaps:ui(e.textCase),underline:e.textDecoration==="UNDERLINE",strikethrough:e.textDecoration==="STRIKETHROUGH"}:null}function li(e,t){switch(t){case"UPPER":return e.toLocaleUpperCase();case"LOWER":return e.toLocaleLowerCase();case"TITLE":return e.replace(new RegExp("\\b(\\p{L})(\\p{L}*)","gu"),(r,o,n)=>o.toLocaleUpperCase()+n.toLocaleLowerCase());default:return e}}function ui(e){switch(e){case"UPPER":return 2;case"SMALL_CAPS":case"SMALL_CAPS_FORCED":return 1;default:return 0}}function ci(e,t){return t<=0?0:e.unit==="PIXELS"?Math.round(e.value/t*1e3):Math.round(e.value/100*500)}function di(e){let t=e.filter(o=>W(o));if(t.length!==1)return null;let r=t[0];return r.type!=="SOLID"?null:{r:z(r.color.r*255),g:z(r.color.g*255),b:z(r.color.b*255),a:z((r.opacity!==void 0?r.opacity:1)*255)}}function mt(e,t){return t==="ignore-hidden"&&!q(e)?[]:!V(e)||e.children.length===0?[e]:Cr(e)||hi(e)?[e]:pi(e,t)}function pi(e,t){return ot(e).filter(o=>!(!he(o)||t==="ignore-hidden"&&!q(o)))}function ue(e,t){let r=fi(e).filter(n=>!(!he(n)||t==="ignore-hidden"&&!q(n))),o=[];for(let n=0;n<r.length;n+=1){let i=r[n];if(!re(i)){o.push({kind:"node",node:i});continue}let a=[],s=n+1;for(;s<r.length;s+=1){let l=r[s];if(mi(l))break;a.push(l)}if(a.length===0){o.push({kind:"node",node:i});continue}o.push({kind:"mask-group",maskNode:i,maskedNodes:a.reverse()}),n=s-1}return o.reverse()}function ot(e){let t=e.children.slice();return wr(e)?t:t.reverse()}function fi(e){let t=e.children.slice();return wr(e)?t.reverse():t}function wr(e){return"itemReverseZIndex"in e&&e.itemReverseZIndex===!0}function mi(e){return re(e)||xi(e)||gi(e)}function xi(e){return V(e)?e.children.some(t=>he(t)&&re(t)):!1}function gi(e){return"clipsContent"in e&&e.clipsContent===!0}function Fe(e){return!V(e)||e.children.length===0?null:!yo.has(e.type)||re(e)||"clipsContent"in e&&e.clipsContent&&!ht(e)?"flatten":kr(e)?ze(e)||$i(L(e))?"flatten":So.has(e.type)&&Ur(e)?"split":"flatten":"group"}function Cr(e){return Fe(e)==="group"}function hi(e){return Fe(e)==="split"}function kr(e){return X(e,"fills")||X(e,"strokes")||Zr(e)}function Br(e,t){let r=f(e),o=t==="future"?"will flatten into one bitmap layer":"was flattened";if((e.type==="INSTANCE"||e.type==="COMPONENT")&&kr(e)){let n=e.type==="INSTANCE"?"instance":"component";return'"'.concat(r,'" ').concat(o," because Photoshop cannot preserve Figma ").concat(n," structure once the container also renders its own appearance.")}return'"'.concat(r,'" ').concat(o," because the container renders its own appearance.")}function Mr(e,t){let r=t==="future"?"will flatten into one bitmap layer":"was flattened";return'"'.concat(f(e),'" ').concat(r," because Layer Blur on a container with children must apply to the whole group composite, and that editable group Smart Filter path is not implemented yet.")}function Ir(e,t){let r=t==="future"?"will flatten into one bitmap layer":"was flattened";return'"'.concat(f(e),'" ').concat(r," because Background Blur on a container with children must sample the whole backdrop behind the group composite, and that editable reconstruction path is not implemented yet.")}function Rr(e,t){let r=t==="future"?"will flatten into one bitmap layer":"was flattened";return'"'.concat(f(e),'" ').concat(r," because Photoshop does not reliably reopen container shadow/glow exported as folder layer effects, so this container is rasterized for compatibility.")}function Ar(e,t){let r=t==="future"?"will flatten into one bitmap layer":"was flattened";return'"'.concat(f(e),'" ').concat(r," because top-level Noise or Texture on a container with children must apply to the whole group composite, and that editable reconstruction path is not implemented yet.")}function yi(e){return Xr(e)?'"'.concat(f(e),'" uses Glass, which cannot stay editable in the PSD yet, so it will be baked into raster pixels.'):'"'.concat(f(e),'" uses Figma effects that cannot stay editable in the PSD, so they will be baked into raster pixels.')}function $t(e){return'"'.concat(e,'" uses Background Blur, but Pigma could not isolate a clean editable backdrop snapshot inside the selected export root, so that blur was baked into raster pixels.')}async function Si(e,t){let r=await bi(e,t);return r?{kind:"bitmap",mode:xt(e),x:r.x,y:r.y,width:r.width,height:r.height,pngBytes:r.pngBytes}:null}async function bi(e,t){let r=xt(e),o=Lr(e,r);if(!o)return t.warnings.add('"'.concat(f(e),'" could not provide mask bounds for export.')),null;t.currentLeaf+=1,Y(t,"".concat(f(e)," (Mask)"));let n=await Ei(e,o,t,r);return{x:x(o.x-t.documentBounds.x),y:x(o.y-t.documentBounds.y),width:d(o.width),height:d(o.height),pngBytes:n}}async function Ei(e,t,r,o){await dt(e);let n=e.clone(),i=null;try{if(Pi(n,o),"absoluteTransform"in e&&"relativeTransform"in n){i=figma.createFrame(),i.resize(Math.max(1,d(t.width)),Math.max(1,d(t.height))),i.clipsContent=!0,i.fills=[],i.strokes=[],i.name="__pigma-mask-preview__",i.x=t.x,i.y=t.y,figma.currentPage.appendChild(i),i.appendChild(n),Vr(e,n,t);return await i.exportAsync({format:"PNG",useAbsoluteBounds:!1})}return bt(n,t),await n.exportAsync({format:"PNG",useAbsoluteBounds:t.useAbsoluteBounds})}finally{i&&!i.removed&&i.remove(),n.removed||n.remove()}}function Pi(e,t){e.visible=!0,"isMask"in e&&(e.isMask=!1),t==="vector"&&("opacity"in e&&typeof e.opacity=="number"&&(e.opacity=1),"blendMode"in e&&(e.blendMode="NORMAL"),Qr(e),to(e,{normalizePaintOpacity:!0,normalizePaintBlendMode:!0}))}function xt(e){if(!("maskType"in e))return"alpha";switch(e.maskType){case"VECTOR":return"vector";case"LUMINANCE":return"luminance";default:return"alpha"}}function maskPreviewPaddingRadius(e){let t=L(e),r=0;if(t)for(let o of t)o&&o.style==="layer-blur"&&(r=Math.max(r,o.blurType==="PROGRESSIVE"?Math.max(o.startRadius,o.radius):o.radius));return r<=.01?0:Math.ceil(h(r*2.5+8,8,256))}function padExportBounds(e,t){return!e||t<=0?e:{x:e.x-t,y:e.y-t,width:e.width+t*2,height:e.height+t*2,useAbsoluteBounds:!1}}function Lr(e,t){let r=maskPreviewPaddingRadius(e),o=null;if(t!=="vector")return padExportBounds(v(e),r);let n=k(e);if(n&&n.width>0&&n.height>0)o={x:n.x,y:n.y,width:n.width,height:n.height,useAbsoluteBounds:!0};else if("absoluteTransform"in e&&"width"in e&&"height"in e){let i=ce(e);i&&(o=i)}return padExportBounds(o!=null?o:v(e),r)}function gt(e,t){return Fr(e,t,!0)}function Fr(e,t,r=!1){if(r&&!ht(e))return null;let o=k(e);if(!o||o.width<=0||o.height<=0)return null;let n=Ni(e,o.width,o.height);return{kind:"rounded-rect",x:x(o.x-t.x),y:x(o.y-t.y),width:d(o.width),height:d(o.height),topLeftRadius:n.topLeftRadius,topRightRadius:n.topRightRadius,bottomRightRadius:n.bottomRightRadius,bottomLeftRadius:n.bottomLeftRadius}}function redundantRootMask(e,t,r){if(e!==r)return!1;let o=gt(e,t);return!!o&&o.x===0&&o.y===0&&o.width===d(t.width)&&o.height===d(t.height)&&o.topLeftRadius===0&&o.topRightRadius===0&&o.bottomRightRadius===0&&o.bottomLeftRadius===0}function containerMask(e,t,r){return redundantRootMask(e,t,r)?null:gt(e,t)}function ht(e){if(!("clipsContent"in e)||e.clipsContent!==!0||!("width"in e)||!("height"in e))return!1;let t=k(e);return!!t&&t.width>0&&t.height>0}function Ni(e,t,r){let o=Math.min(t,r)/2,n={topLeftRadius:0,topRightRadius:0,bottomRightRadius:0,bottomLeftRadius:0};return!("topLeftRadius"in e)||!("topRightRadius"in e)||!("bottomRightRadius"in e)||!("bottomLeftRadius"in e)?n:{topLeftRadius:h(e.topLeftRadius,0,o),topRightRadius:h(e.topRightRadius,0,o),bottomRightRadius:h(e.bottomRightRadius,0,o),bottomLeftRadius:h(e.bottomLeftRadius,0,o)}}function Ti(e){return te(e)||X(e,"strokes")||!("fills"in e)||!Array.isArray(e.fills)?null:vi(e.fills)}function Di(e){if(te(e))return null;let t="fills"in e&&Array.isArray(e.fills)?wi(e.fills):null,r="strokes"in e&&Array.isArray(e.strokes)?Gr(e.strokes):null,o=null;if(r){let n=Jr(e);if(n===null)return null;o={kind:"solid",color:Ge(r),width:n,position:Or(e)}}return!t&&!o?null:{fill:t,stroke:o}}function vi(e){let t=e.filter(o=>W(o));if(t.length!==1)return null;let r=t[0];return r.type==="SOLID"?{kind:"solid",color:Ge(r)}:Ci(r)?ki(r):null}function wi(e){let t=e.filter(o=>W(o));if(t.length!==1)return null;let r=t[0];return r.type!=="SOLID"?null:{kind:"solid",color:Ge(r)}}function Gr(e){let t=e.filter(o=>W(o));if(t.length!==1)return null;let r=t[0];return r.type!=="SOLID"?null:r}function Ci(e){return e.type==="GRADIENT_LINEAR"||e.type==="GRADIENT_RADIAL"||e.type==="GRADIENT_ANGULAR"||e.type==="GRADIENT_DIAMOND"}function ki(e){var n;if(!Array.isArray(e.gradientStops)||e.gradientStops.length<2)return null;let t=Mi(e.gradientTransform);if(!t)return null;let r=h((n=e.opacity)!=null?n:1,0,1),o=e.gradientStops.slice().sort((i,a)=>i.position-a.position);return{kind:"gradient",gradientType:Bi(e.type),transform:t,colorStops:o.map(i=>({position:h(i.position,0,1),color:ee({r:i.color.r,g:i.color.g,b:i.color.b,a:1})})),opacityStops:o.map(i=>({position:h(i.position,0,1),opacity:h(i.color.a*r,0,1)}))}}function Bi(e){switch(e){case"GRADIENT_LINEAR":return"linear";case"GRADIENT_RADIAL":return"radial";case"GRADIENT_ANGULAR":return"angular";case"GRADIENT_DIAMOND":return"diamond";default:return"linear"}}function Mi(e){if(!Array.isArray(e)||e.length!==2||!Array.isArray(e[0])||!Array.isArray(e[1])||e[0].length!==3||e[1].length!==3)return null;let t=[D(e[0][0]),D(e[1][0]),D(e[0][1]),D(e[1][1]),D(e[0][2]),D(e[1][2])];return t.every(r=>Number.isFinite(r))?t:null}function Ge(e){return ee({r:e.color.r,g:e.color.g,b:e.color.b,a:e.opacity!==void 0?e.opacity:1})}function ee(e){return{r:z(e.r*255),g:z(e.g*255),b:z(e.b*255),a:z(e.a*255)}}function Or(e){if(!("strokeAlign"in e))return"center";switch(e.strokeAlign){case"INSIDE":return"inside";case"OUTSIDE":return"outside";default:return"center"}}function X(e,t){let r=Oi(e,t);return r==null?!1:Array.isArray(r)?r.some(o=>W(o)):!0}function Ur(e){return"clone"in e&&typeof e.clone=="function"}function pe(e){return"clone"in e&&typeof e.clone=="function"}function zr(e,t){let r=[],o=e;for(;o&&o!==t;){let n=o.parent&&"children"in o.parent?o.parent:null;if(!n)return null;let i=o.id,a=n.children.findIndex(s=>s.id===i);if(a<0)return null;r.unshift(a),o=n}return o===t?r:null}function Ii(e){"opacity"in e&&(e.opacity=1),"blendMode"in e&&(e.blendMode="NORMAL");for(let t of e.children)nt(t)}function nt(e){if("opacity"in e&&typeof e.opacity=="number"){e.opacity=0;return}e.visible=!1}async function Ri(e,t,r=null){var s;if(!qr(e,t.root)||!pe(t.root))return null;let o=(s=r!=null?r:tt(e))!=null?s:v(e);if(!o||Sa(o.width,o.height,To))return null;let n=zr(e,t.root);if(!n||n.length===0)return null;if(!await preloadTreeFontsSafely(t.root))return null;let i=t.root.clone(),a=figma.createFrame();try{return a.resize(d(o.width),d(o.height)),a.clipsContent=!0,a.fills=[],a.strokes=[],a.x=o.x,a.y=o.y,a.name="__pigma-background-blur-crop__",figma.currentPage.appendChild(a),a.appendChild(i),Ai(t.root,i,n)?(_r(i,t.documentBounds,o),await a.exportAsync({format:"PNG",useAbsoluteBounds:!1})):null}catch(l){return null}finally{a.removed||a.remove(),i.removed||i.remove()}}function Ai(e,t,r){let o=e,n=t;for(let i=0;i<r.length;i+=1){if(!("children"in o)||!("children"in n))return!1;let a=o.children[r[i]],s=n.children[r[i]];if(!a||!s)return!1;let l=ot(o),u=ot(n),c=l.findIndex(p=>p.id===a.id);if(c<0)return!1;for(let p=0;p<c;p+=1)nt(u[p]);if(i===r.length-1)return nt(s),!0;o=a,n=s}return!1}function _r(e,t,r){let o=x(t.x-r.x),n=x(t.y-r.y);if("relativeTransform"in e){let i=e,a=i.relativeTransform;i.relativeTransform=[[a[0][0],a[0][1],o],[a[1][0],a[1][1],n]];return}if("x"in e&&"y"in e){let i=e;i.x=o,i.y=n}}async function $r(e,t,r,o){await dt(e);let n=e.clone(),i=null,a=t&&t.useAbsoluteBounds===!1;try{return Oe(n,o),a?(i=figma.createFrame(),i.resize(Math.max(1,d(t.width)),Math.max(1,d(t.height))),i.clipsContent=!0,i.fills=[],i.strokes=[],i.name="__pigma-render-preview__",i.x=t.x,i.y=t.y,figma.currentPage.appendChild(i),i.appendChild(n),Vr(e,n,t),await i.exportAsync({format:"PNG",useAbsoluteBounds:!1})):(bt(n,t),await n.exportAsync({format:"PNG",useAbsoluteBounds:t.useAbsoluteBounds}))}finally{i&&!i.removed&&i.remove(),n.removed||n.remove()}}async function Hr(e,t,r,o){await we(e);let n=e.clone(),i=figma.createFrame();try{return i.resize(d(t.width),d(t.height)),i.clipsContent=!0,i.fills=[],i.strokes=[],i.name="__pigma-text-preview-padding__",i.x=t.x,i.y=t.y,figma.currentPage.appendChild(i),i.appendChild(n),o&&Oe(n,r),Vr(e,n,t),await i.exportAsync({format:"PNG",useAbsoluteBounds:!1})}finally{i.removed||i.remove(),n.removed||n.remove()}}async function Li(e,t,r){await we(e);let o=e.clone(),n=figma.createFrame();try{n.resize(d(t.width),d(t.height)),n.clipsContent=!1,n.fills=[],n.strokes=[],n.name="__pigma-text-visual-probe__",n.x=t.x,n.y=t.y,figma.currentPage.appendChild(n),n.appendChild(o),Oe(o,r),Vr(e,o,t);let i=figma.flatten([o],n),a=v(i);return a||null}catch(i){return null}finally{n.removed||n.remove(),o.removed||o.remove()}}function yt(e){return!q(e)||Fi(e)}function Fi(e){return"opacity"in e&&typeof e.opacity=="number"&&!St(e.opacity,1)?!0:"blendMode"in e?e.blendMode!=="NORMAL"&&e.blendMode!=="PASS_THROUGH":!1}function Oe(e,t){e.visible=!0,"opacity"in e&&typeof e.opacity=="number"&&(e.opacity=1),"blendMode"in e&&(e.blendMode="NORMAL"),t!=null&&t.removeAllEffects&&Qr(e),t!=null&&t.removeSupportedEffects&&ta(e),t!=null&&t.removeSupportedStroke&&ra(e),(t!=null&&t.normalizePaintOpacity||t!=null&&t.normalizePaintBlendMode)&&to(e,t)}function q(e){let t=e;for(;t;){if("visible"in t&&t.visible===!1)return!1;t=t.parent}return!0}function St(e,t){return Math.abs(e-t)<1e-4}function bt(e,t){if(e.parent!==figma.currentPage&&figma.currentPage.appendChild(e),"relativeTransform"in e){let r=e,o=r.relativeTransform;r.relativeTransform=[[o[0][0],o[0][1],t.x],[o[1][0],o[1][1],t.y]];return}if("x"in e&&"y"in e){let r=e;r.x=t.x,r.y=t.y}}function Vr(e,t,r){if("relativeTransform"in t){t.relativeTransform=[[e.absoluteTransform[0][0],e.absoluteTransform[0][1],e.absoluteTransform[0][2]-r.x],[e.absoluteTransform[1][0],e.absoluteTransform[1][1],e.absoluteTransform[1][2]-r.y]];return}let o=t,n=e;o.x=n.x-r.x,o.y=n.y-r.y}function it(e,t){let r=Math.max(1,t.baseStyle.fontSize),o=k(e),n=I(e),i=o?o.x+o.width:null,a=o?o.y+o.height:null,s=n?n.x+n.width:null,l=n?n.y+n.height:null,u=o&&n?Math.max(0,o.x-n.x):0,c=o&&n?Math.max(0,o.y-n.y):0,p=i!==null&&s!==null?Math.max(0,s-i):0,g=a!==null&&l!==null?Math.max(0,l-a):0;return{left:d(h(r*.1+u,6,r*.45)),top:d(h(r*.08+c,6,r*.3)),right:d(h(r*.16+p,10,r*.55)),bottom:d(h(r*.24+g,12,r*.7))}}function Ht(e){let t=Math.max(1,e.baseStyle.fontSize),r=vr(e.value),o=r<=1?1.4:r<=3?1.1:1;return{left:d(t*.06*o),top:d(t*.05),right:d(t*.12*o),bottom:d(t*.11*o)}}function jr(e,t){let r=Math.max(1,t.baseStyle.fontSize),o=vr(t.value),i=Math.max(1,t.bounds.right-t.bounds.left)/r,a=t.value.replace(/\s+/g,""),s=0;r>=400?s+=35:r>=240?s+=22:r>=120&&(s+=10),o<=1?s+=25:o<=3&&(s+=14),i<=.3?s+=20:i<=.55&&(s+=10),t.shapeType==="point"&&(s+=8),/[fgjkpqty]/i.test(a)&&(s+=12),/[^\u0000-\u00ff]/.test(a)&&(s+=10);let l=k(e),u=I(e);if(l&&u){let c=br(l,u);(c.left>0||c.top>0||c.right>0||c.bottom>0)&&(s+=10)}return h(s,0,100)}function Gi(e,t){let r=Tr(e,t);if(!r)return null;let o=jr(e,r);if(o<Po)return null;let n=Ae(e,r.shapeType);if(!n)return null;let i=r.shapeType==="point"?it(e,r):{left:0,top:0,right:0,bottom:0},a=Wr(r,o);return ie(n,{left:Math.max(i.left,a.left),top:Math.max(i.top,a.top),right:Math.max(i.right,a.right),bottom:Math.max(i.bottom,a.bottom)})}function Wr(e,t){let r=Math.max(1,e.baseStyle.fontSize),o=h(t/100,0,1);return{left:d(r*(.05+o*.2)),top:d(r*(.04+o*.1)),right:d(r*(.08+o*.3)),bottom:d(r*(.12+o*.16))}}function ie(e,t){return{x:e.x-t.left,y:e.y-t.top,width:e.width+t.left+t.right,height:e.height+t.top+t.bottom,useAbsoluteBounds:!1}}function Vt(e,t){let r=Math.min(e.x,t.x),o=Math.min(e.y,t.y),n=Math.max(e.x+e.width,t.x+t.width),i=Math.max(e.y+e.height,t.y+t.height);return{x:r,y:o,width:n-r,height:i-o,useAbsoluteBounds:!1}}function Oi(e,t){return t==="fills"?"fills"in e?e.fills:null:"strokes"in e?e.strokes:null}function Ui(e){return"visible"in e&&e.visible===!1?!1:e.type==="LAYER_BLUR"?O.disableLayerBlur?!1:!(O.disableProgressiveLayerBlur&&e.blurType==="PROGRESSIVE"):e.type==="BACKGROUND_BLUR"?!O.disableBackgroundBlur:e.type==="NOISE"?!O.disableNoise:e.type==="TEXTURE"?!O.disableTexture:!0}function Ue(e){if(!("effects"in e))return null;let t=e.effects;return Array.isArray(t)?t.filter(r=>Ui(r)):null}function Zr(e){let t=Ue(e);return!!t&&t.length>0}function ze(e){let t=Ue(e);return t?t.some(r=>r.type==="BACKGROUND_BLUR"):!1}function Xr(e){let t=Ue(e);return t?t.some(r=>r.type==="GLASS"):!1}function L(e,t=null){let r=Ue(e);if(!r||r.length===0)return null;let o=[],n=r.filter(a=>a.type==="DROP_SHADOW"&&Ke(a)).length,i=r.filter(a=>a.type==="INNER_SHADOW"&&Ke(a)).length;for(let a of r){if(a.type==="LAYER_BLUR"){if(Kr(o))return null;let u=Ki(a);if(!u)return null;o.push(u);continue}if(a.type==="BACKGROUND_BLUR"){if(_e(o))return null;let u=Yi(a,e,t);if(!u)return null;o.push(u);continue}if(a.type==="NOISE"){let u=qi(a);if(!u)return null;o.push(u);continue}if(a.type==="TEXTURE"){let u=ea(a);if(!u)return null;o.push(u);continue}if(a.type!=="DROP_SHADOW"&&a.type!=="INNER_SHADOW")return null;if(Ke(a)&&(a.type==="DROP_SHADOW"&&n===1||a.type==="INNER_SHADOW"&&i===1)){let u=Xi(a);if(!u)return null;o.push(u);continue}let l=Zi(a);if(!l)return null;o.push(l)}return ji(o)&&o.length>0?o:null}function fe(e,t=null){return _(L(e,t))}function pigmaLayerBlurShouldRasterize(e){return!!_(e)}function progressiveBlurShouldRasterize(e){return pigmaLayerBlurShouldRasterize(e)}function _(e){let t=Kr(e);return t&&zi(t)>0?t:null}function Kr(e){var t;return e&&(t=e.find(r=>r.style==="layer-blur"))!=null?t:null}function zi(e){return e.blurType==="PROGRESSIVE"?Math.max(e.startRadius,e.radius):e.radius}function _e(e){var t;return e&&(t=e.find(r=>r.style==="background-blur"))!=null?t:null}function _i(e,t){return e?e.map(r=>r.style==="background-blur"?B(b({},r),{backdropPngBytes:t}):r):null}function jt(e){if(!e)return null;let t=e.filter(r=>r.style!=="background-blur");return t.length>0?t:null}function Et(e){return e?e.filter(t=>t.style==="noise"):[]}function Yr(e){return e?e.filter(t=>t.style==="texture"):[]}function Pt(e){return Et(e).length>0||Yr(e).length>0}function $i(e){return!!_(e)||!!_e(e)||Pt(e)}function $e(e){return!!(e!=null&&e.some(t=>t.style==="drop-shadow"||t.style==="inner-shadow"||t.style==="outer-glow"||t.style==="inner-glow"))}function Hi(e){if(!e)return null;let t=e.filter(r=>r.style==="drop-shadow"||r.style==="inner-shadow"||r.style==="outer-glow"||r.style==="inner-glow");return t.length>0?t:null}function Vi(e){if(!e)return null;let t=e.filter(r=>r.style!=="drop-shadow"&&r.style!=="inner-shadow"&&r.style!=="outer-glow"&&r.style!=="inner-glow");return t.length>0?t:null}function ji(e){let t=_(e),r=_e(e),o=Et(e),n=Yr(e);return!t&&!r&&o.length===0&&n.length===0?!0:r?e.length===1:!(t&&(o.length>0||n.length>0)||o.length>0&&n.length>0||n.some(i=>i.clipToShape!==!0)||(o.length>0||n.length>0)&&$e(e))}function Wi(e){return Et(e).length}function te(e,t=null){return Zr(e)&&L(e,t)===null}function Zi(e){return!e.color||!e.offset?null:{style:e.type==="DROP_SHADOW"?"drop-shadow":"inner-shadow",blendMode:oe(e.blendMode),color:ee(e.color),offsetX:x(e.offset.x),offsetY:x(e.offset.y),blur:M(Math.max(0,e.radius)),spread:M(typeof e.spread=="number"?e.spread:0),showBehindTransparentAreas:e.type==="DROP_SHADOW"&&e.showShadowBehindNode===!0}}function Xi(e){return e.color?{style:e.type==="DROP_SHADOW"?"outer-glow":"inner-glow",blendMode:oe(e.blendMode),color:ee(e.color),blur:M(Math.max(0,e.radius)),spread:M(typeof e.spread=="number"?e.spread:0)}:null}function Ki(e){return e.blurType==="PROGRESSIVE"?{style:"layer-blur",blurType:"PROGRESSIVE",radius:M(Math.max(0,e.radius)),startRadius:M(Math.max(0,e.startRadius)),startOffset:Zt(e.startOffset),endOffset:Zt(e.endOffset)}:e.blurType!=="NORMAL"?null:{style:"layer-blur",blurType:"NORMAL",radius:M(Math.max(0,e.radius))}}function Yi(e,t,r){return e.blurType!=="NORMAL"||!qr(t,r)?null:{style:"background-blur",radius:M(Math.max(0,e.radius)),backdropPngBytes:null}}function qi(e){let t={style:"noise",noiseType:e.noiseType.toLowerCase(),blendMode:oe(e.blendMode),primaryColor:ee(e.color),secondaryColor:"secondaryColor"in e?ee(e.secondaryColor):null,noiseSize:M(Math.max(0,e.noiseSize)),density:h(e.density,0,1),opacity:h("opacity"in e?e.opacity:e.color.a,0,1)};return Ji(t)}function Ji(e){let t=Lo[e.noiseType];return B(b({},e),{primaryColor:Wt(e.primaryColor,t.primaryAlphaScale),secondaryColor:e.secondaryColor?Wt(e.secondaryColor,t.secondaryAlphaScale):null,density:Qi(e.density),opacity:D(h(e.opacity*t.overlayOpacityScale,0,1))})}function Qi(e){return D(h(e-e*e*.12,0,1))}function Wt(e,t){return t===1?e:B(b({},e),{a:z(e.a*t)})}function ea(e){return{style:"texture",noiseSize:M(Math.max(0,e.noiseSize)),radius:M(Math.max(0,e.radius)),clipToShape:e.clipToShape===!0}}function qr(e,t){if(!t||e===t||!he(e)||Re(e)||V(e)&&e.children.length>0||!V(t)||!pe(t))return!1;let r=zr(e,t);return!!r&&r.length>0}function Ke(e){return Math.abs(e.offset.x)<.01&&Math.abs(e.offset.y)<.01}function me(e){if(!("strokes"in e)||!Array.isArray(e.strokes))return null;let t=Gr(e.strokes);if(!t)return null;let r=Jr(e);return r===null?null:{blendMode:oe(t.blendMode),color:Ge(t),width:r,position:Or(e)}}function Jr(e){if(!("strokeWeight"in e)||typeof e.strokeWeight!="number"||e.strokeWeight<=0)return null;if(!("strokeTopWeight"in e)||!("strokeBottomWeight"in e)||!("strokeLeftWeight"in e)||!("strokeRightWeight"in e))return M(e.strokeWeight);let t=[e.strokeTopWeight,e.strokeRightWeight,e.strokeBottomWeight,e.strokeLeftWeight];if(t.some(o=>typeof o!="number"||!Number.isFinite(o)||o<=0))return null;let r=t[0];return t.some(o=>Math.abs(o-r)>.01)||Math.abs(e.strokeWeight-r)>.01?null:M(r)}function ta(e){!("effects"in e)||!Array.isArray(e.effects)||(e.effects=e.effects.filter(t=>"visible"in t&&t.visible===!1?!0:t.type!=="DROP_SHADOW"&&t.type!=="INNER_SHADOW"&&t.type!=="LAYER_BLUR"&&t.type!=="BACKGROUND_BLUR"))}function Qr(e){!("effects"in e)||!Array.isArray(e.effects)||(e.effects=e.effects.filter(t=>"visible"in t&&t.visible===!1))}function eo(e){return'"'.concat(e,'" uses Layer Blur, so it will preserve the blur as a Photoshop Smart Filter and rasterize the text instead of keeping an editable text layer.')}function ra(e){if(!("strokes"in e)||!Array.isArray(e.strokes))return;let t=e;t.strokes=e.strokes.filter(r=>!W(r))}function W(e){return!("visible"in e)||e.visible!==!1}function re(e){return"isMask"in e&&e.isMask}function xe(e){let t=ho.has(e.type);return ge(e,t)}function v(e){let t=e.type==="TEXT";return ge(e,t)}function ge(e,t){let r=t?k(e):I(e),o=t?I(e):k(e),n=r!=null?r:o;return!n||n.width<=0||n.height<=0?null:{x:n.x,y:n.y,width:n.width,height:n.height,useAbsoluteBounds:t}}function k(e){return"absoluteBoundingBox"in e?e.absoluteBoundingBox:null}function I(e){return"absoluteRenderBounds"in e?e.absoluteRenderBounds:null}function he(e){return"exportAsync"in e}function V(e){return"children"in e}function j(e){let t=!("opacity"in e)||typeof e.opacity!="number"?1:h(e.opacity,0,1);return h(t*(e&&e.blendMode==="COLOR_BURN"?.875:1),0,1)}function oa(e){var u;if(!("fills"in e)||!Array.isArray(e.fills))return null;let t=e.fills.filter(c=>W(c));if(t.length!==1)return null;let r=t[0],o=r.type==="IMAGE"&&!!r.imageHash,n=o?h((u=r.opacity)!=null?u:1,0,1):1,i=o&&!St(n,1),a=!st(r.blendMode);if(!i&&!a)return null;let s="blendMode"in e?e.blendMode:void 0,l=!st(s),c=a,p=c?oe(r.blendMode):K(e),g=l&&c&&K(e)!==p;return{normalizePaintOpacity:i||c&&o,normalizePaintBlendMode:c,effectiveOpacity:o?h(j(e)*n*pigmaColorBurnOpacityScale(r),0,1):h(j(e)*pigmaColorBurnOpacityScale(r),0,1),effectiveBlendMode:p,warning:g?'"'.concat(f(e),'" collapses Figma layer/fill blend modes into the fill blend for PSD export.'):null}}function pigmaVisibleFillEntries(e){return!("fills"in e)||!Array.isArray(e.fills)?[]:e.fills.map((t,r)=>({paint:t,index:r})).filter(t=>W(t.paint))}
function pigmaPsdFillStackEntries(e){return e.slice().reverse()}
function pigmaColorBurnOpacityScale(e){return e&&e.blendMode==="COLOR_BURN"?.875:1}
function pigmaFillOpacity(e){var t;return h(((t=e.opacity)!=null?t:1)*pigmaColorBurnOpacityScale(e),0,1)}
function pigmaPaintNeedsDedicatedLayer(e){return!st(e.blendMode)||!St(pigmaFillOpacity(e),1)}
function pigmaMultiFillBlendInfo(e,t=null){let r=pigmaVisibleFillEntries(e);if(r.length<2||e.type==="TEXT")return null;let o=te(e,t),n=r.filter(i=>pigmaPaintNeedsDedicatedLayer(i.paint));if(!o&&n.length===0)return null;return{entries:r,mode:o?"baked-fx":"split",warning:o?"\"".concat(f(e),"\" uses multiple visible fills plus unsupported effects, so PSD export kept separate fill layers and added one baked effects layer."):"\"".concat(f(e),"\" uses multiple visible fills with blend or opacity overrides, so PSD export split them into separate child layers.")}}
function pigmaFillBlendMode(e){return at(!st(e.blendMode)?oe(e.blendMode):"normal")}
function pigmaFillChildName(e,t,r,o){return r?o==="background"?"Background Fill ".concat(t+1):"".concat(f(e)," Fill ").concat(t+1):o==="background"?"Background":f(e)}
function pigmaFxChildName(e,t){return t==="background"?"Background Effects":"Effects"}
function pigmaBaseFillBounds(e,t=null){let r=k(e);if(r&&r.width>0&&r.height>0)return{x:r.x,y:r.y,width:r.width,height:r.height,useAbsoluteBounds:!0};if("absoluteTransform"in e&&"width"in e&&"height"in e){let o=ce(e);if(o)return{x:o.x,y:o.y,width:o.width,height:o.height,useAbsoluteBounds:!0}}return t!=null?t:v(e)}
function pigmaIsolateFillOnClone(e,t){if(!("fills"in e)||!Array.isArray(e.fills))return;let r=e,o=e.fills;r.fills=o.map((n,i)=>i===t?B(b({},n),{blendMode:"NORMAL",opacity:1,visible:!0}):B(b({},n),{blendMode:"NORMAL",opacity:1,visible:!1}))}
function pigmaHideAllFillsOnClone(e){if(!("fills"in e)||!Array.isArray(e.fills))return;let t=e,r=e.fills;t.fills=r.map(o=>B(b({},o),{blendMode:"NORMAL",opacity:1,visible:!1}))}
async function pigmaExportFillBitmapChild(e,t,r,o,n,i,a,s="node"){let l=e.clone();try{pigmaIsolateFillOnClone(l,o.index),s==="background"&&"children"in l&&Ii(l),Oe(l,a),bt(l,r);let u=x(r.x-t.documentBounds.x),c=x(r.y-t.documentBounds.y),p=d(r.width),g=d(r.height);return{kind:"bitmap",id:"".concat(e.id,":").concat(s==="background"?"background-fill":"fill",":").concat(o.index+1),name:pigmaFillChildName(e,o.index,i,s),sourceType:"".concat(e.type,s==="background"?"_BACKGROUND_FILL":"_FILL"),opacity:pigmaFillOpacity(o.paint),visible:!0,blendMode:pigmaFillBlendMode(o.paint),effects:null,strokeEffect:null,x:u,y:c,width:p,height:g,nodeTransform:de(e,t.documentBounds,u,c),pngBytes:await l.exportAsync({format:"PNG",useAbsoluteBounds:r.useAbsoluteBounds})}}finally{l.removed||l.remove()}}
async function pigmaExportFxBitmapChild(e,t,r,o,n="node"){let i=e.clone();try{pigmaHideAllFillsOnClone(i),n==="background"&&"children"in i&&Ii(i),Oe(i,o),bt(i,r);let a=x(r.x-t.documentBounds.x),s=x(r.y-t.documentBounds.y),l=d(r.width),u=d(r.height);return{kind:"bitmap",id:"".concat(e.id,":").concat(n==="background"?"background-fx":"fx"),name:pigmaFxChildName(e,n),sourceType:"".concat(e.type,n==="background"?"_BACKGROUND_FX":"_FX"),opacity:1,visible:!0,blendMode:"normal",effects:null,strokeEffect:null,x:a,y:s,width:l,height:u,nodeTransform:de(e,t.documentBounds,a,s),pngBytes:await i.exportAsync({format:"PNG",useAbsoluteBounds:r.useAbsoluteBounds})}}finally{i.removed||i.remove()}}
async function pigmaExportMultiFillGroup(e,t,r,o=null){let n=pigmaMultiFillBlendInfo(e,t.root);if(!n||t.longFrameMode)return null;let i=o!=null?o:tt(e),a=n.mode==="baked-fx"?tt(e):i,s=n.mode==="baked-fx"?pigmaBaseFillBounds(e,i):i;if(!a||!s)return null;let l=L(e,t.root);if(n.mode!=="baked-fx"&&progressiveBlurShouldRasterize(l))return null;let u=me(e),c=null,p=null,g={removeSupportedStroke:!!u},y=[],m=pigmaPsdFillStackEntries(n.entries);t.currentLeaf+=1,Y(t,f(e)),await dt(e),n.mode==="baked-fx"?g.removeAllEffects=!0:(c=await hr(e,t,r),p=c.effects,g.removeSupportedEffects=c.removeSupportedEffects||!!_(p));for(let T=0;T<m.length;T+=1){let E=await pigmaExportFillBitmapChild(e,t,s,m[T],T,n.entries.length>1,g,"node");E&&y.push(E)}if(n.mode==="baked-fx"){let T=await pigmaExportFxBitmapChild(e,t,a,{removeSupportedStroke:!!u},"node");T&&y.push(T)}let d=n.entries.length+(n.mode==="baked-fx"?1:0);return y.length<d?null:(n.warning&&t.warnings.add(n.warning),{kind:"group",id:e.id,name:f(e),sourceType:e.type,opacity:j(e),visible:e.visible,blendMode:K(e),effects:n.mode==="baked-fx"?null:p,strokeEffect:u,mask:null,children:y})}
async function pigmaExportMultiFillBackgroundGroup(e,t,r){let o=pigmaMultiFillBlendInfo(e,t.root);if(!o||t.longFrameMode||!Ln(e)||!Ur(e))return null;let n=Fn(e),i=o.mode==="baked-fx"?tt(e):n;if(!n||!i)return null;let a=L(e,t.root);if(o.mode!=="baked-fx"&&progressiveBlurShouldRasterize(a))return null;let s=me(e),l=null,u=null,c={removeSupportedStroke:!!s},p=[],g=pigmaPsdFillStackEntries(o.entries);t.currentLeaf+=1,Y(t,"".concat(f(e)," Background")),await dt(e),o.mode==="baked-fx"?c.removeAllEffects=!0:(l=Hi(a),u=Vi(a),c.removeSupportedEffects=!!a);for(let y=0;y<g.length;y+=1){let m=await pigmaExportFillBitmapChild(e,t,n,g[y],y,o.entries.length>1,c,"background");m&&p.push(m)}if(o.mode==="baked-fx"){let y=await pigmaExportFxBitmapChild(e,t,i,{removeSupportedStroke:!!s},"background");y&&p.push(y)}let d=o.entries.length+(o.mode==="baked-fx"?1:0);return p.length<d?null:(o.warning&&t.warnings.add(o.warning),{backgroundLayer:{kind:"group",id:"".concat(e.id,":background-stack"),name:"Background",sourceType:"".concat(e.type,"_BACKGROUND_GROUP"),opacity:1,visible:!0,blendMode:"normal",effects:o.mode==="baked-fx"?null:l,strokeEffect:s,mask:null,children:p},groupEffects:o.mode==="baked-fx"?null:u,groupStrokeEffect:null})}
function to(e,t){if(!("fills"in e)||!Array.isArray(e.fills))return;let r=e,o=e.fills;r.fills=o.map(n=>na(n,t))}function na(e,t){if(!W(e))return e;let r=e;return t.normalizePaintOpacity&&e.opacity!==void 0&&!St(e.opacity,1)&&(r=B(b({},r),{opacity:1})),t.normalizePaintBlendMode&&e.blendMode!==void 0&&!st(e.blendMode)&&(r=B(b({},r),{blendMode:"NORMAL"})),r}function K(e){return"blendMode"in e?oe(e.blendMode):"normal"}function ro(e){return at(K(e))}function at(e){return e==="pass through"?"normal":e}function oe(e){switch(e){case"DARKEN":return"darken";case"MULTIPLY":return"multiply";case"LINEAR_BURN":return"linear burn";case"COLOR_BURN":return"linear burn";case"LIGHTEN":return"lighten";case"SCREEN":return"screen";case"LINEAR_DODGE":return"linear dodge";case"COLOR_DODGE":return"color dodge";case"OVERLAY":return"overlay";case"SOFT_LIGHT":return"soft light";case"HARD_LIGHT":return"hard light";case"DIFFERENCE":return"difference";case"EXCLUSION":return"exclusion";case"HUE":return"hue";case"SATURATION":return"saturation";case"COLOR":return"color";case"LUMINOSITY":return"luminosity";case"PASS_THROUGH":return"pass through";default:return"normal"}}function st(e){return e===void 0||e==="NORMAL"||e==="PASS_THROUGH"}function ia(e){switch(e){case"CENTER":return"center";case"RIGHT":return"right";case"JUSTIFIED":return"justify-left";default:return"left"}}function aa(e,t){return e.unit==="AUTO"?null:e.unit==="PIXELS"?e.value:t*e.value/100}function oo(e,t){return[D(e[0][0]),D(e[1][0]),D(e[0][1]),D(e[1][1]),D(e[0][2]-t.x),D(e[1][2]-t.y)]}var pigmaPhotoshopFontMap={"210ohneuleun|book":"TTOhneuleunR","210ohneuleun|normal":"TTOhneuleunR","210ohneuleun|regular":"TTOhneuleunR","210ohneuleun|roman":"TTOhneuleunR","210ohneuleunr|book":"TTOhneuleunR","210ohneuleunr|normal":"TTOhneuleunR","210ohneuleunr|regular":"TTOhneuleunR","210ohneuleunr|roman":"TTOhneuleunR","210omnigothic|010":"TTOmniGothicT","210omnigothic|020":"TTOmniGothicL","210omnigothic|030":"TTOmniGothicM","210omnigothic|040":"TTOmniGothicB","210omnigothic|050":"TTOmniGothicH","210omnigothic010|book":"TTOmniGothicT","210omnigothic010|normal":"TTOmniGothicT","210omnigothic010|regular":"TTOmniGothicT","210omnigothic010|roman":"TTOmniGothicT","210omnigothic020|book":"TTOmniGothicL","210omnigothic020|normal":"TTOmniGothicL","210omnigothic020|regular":"TTOmniGothicL","210omnigothic020|roman":"TTOmniGothicL","210omnigothic030|book":"TTOmniGothicM","210omnigothic030|normal":"TTOmniGothicM","210omnigothic030|regular":"TTOmniGothicM","210omnigothic030|roman":"TTOmniGothicM","210omnigothic040|book":"TTOmniGothicB","210omnigothic040|normal":"TTOmniGothicB","210omnigothic040|regular":"TTOmniGothicB","210omnigothic040|roman":"TTOmniGothicB","210omnigothic050|book":"TTOmniGothicH","210omnigothic050|normal":"TTOmniGothicH","210omnigothic050|regular":"TTOmniGothicH","210omnigothic050|roman":"TTOmniGothicH","210timeline|bold":"TTTimelineB","210timeline|book":"TTTimelineR","210timeline|light":"TTTimelineL","210timeline|normal":"TTTimelineR","210timeline|regular":"TTTimelineR","210timeline|roman":"TTTimelineR","210timelineb|book":"TTTimelineB","210timelineb|normal":"TTTimelineB","210timelineb|regular":"TTTimelineB","210timelineb|roman":"TTTimelineB","210timelinel|book":"TTTimelineL","210timelinel|normal":"TTTimelineL","210timelinel|regular":"TTTimelineL","210timelinel|roman":"TTTimelineL","210timeliner|book":"TTTimelineR","210timeliner|normal":"TTTimelineR","210timeliner|regular":"TTTimelineR","210timeliner|roman":"TTTimelineR","acmegothic|black":"AcmeGothic-Black","acmegothic|bold":"AcmeGothic-Bold","acmegothic|book":"AcmeGothic-Regular","acmegothic|demi":"AcmeGothic-Semibold","acmegothic|demibold":"AcmeGothic-Semibold","acmegothic|heavy":"AcmeGothic-Black","acmegothic|light":"AcmeGothic-Light","acmegothic|normal":"AcmeGothic-Regular","acmegothic|regular":"AcmeGothic-Regular","acmegothic|roman":"AcmeGothic-Regular","acmegothic|semibold":"AcmeGothic-Semibold","acmegothicblack|book":"AcmeGothic-Black","acmegothicblack|normal":"AcmeGothic-Black","acmegothicblack|regular":"AcmeGothic-Black","acmegothicblack|roman":"AcmeGothic-Black","acmegothiccomp|bold":"AcmeGothic-CompressedBold","acmegothiccomp|book":"AcmeGothic-CompressedRegular","acmegothiccomp|normal":"AcmeGothic-CompressedRegular","acmegothiccomp|regular":"AcmeGothic-CompressedRegular","acmegothiccomp|roman":"AcmeGothic-CompressedRegular","acmegothiccompblack|book":"AcmeGothic-CompressedBlack","acmegothiccompblack|normal":"AcmeGothic-CompressedBlack","acmegothiccompblack|regular":"AcmeGothic-CompressedBlack","acmegothiccompblack|roman":"AcmeGothic-CompressedBlack","acmegothiccomplight|book":"AcmeGothic-CompressedLight","acmegothiccomplight|normal":"AcmeGothic-CompressedLight","acmegothiccomplight|regular":"AcmeGothic-CompressedLight","acmegothiccomplight|roman":"AcmeGothic-CompressedLight","acmegothiccompressed|black":"AcmeGothic-CompressedBlack","acmegothiccompressed|bold":"AcmeGothic-CompressedBold","acmegothiccompressed|book":"AcmeGothic-CompressedRegular","acmegothiccompressed|demi":"AcmeGothic-CompressedSemibold","acmegothiccompressed|demibold":"AcmeGothic-CompressedSemibold","acmegothiccompressed|heavy":"AcmeGothic-CompressedBlack","acmegothiccompressed|light":"AcmeGothic-CompressedLight","acmegothiccompressed|normal":"AcmeGothic-CompressedRegular","acmegothiccompressed|regular":"AcmeGothic-CompressedRegular","acmegothiccompressed|roman":"AcmeGothic-CompressedRegular","acmegothiccompressed|semibold":"AcmeGothic-CompressedSemibold","acmegothiccompsemibold|book":"AcmeGothic-CompressedSemibold","acmegothiccompsemibold|normal":"AcmeGothic-CompressedSemibold","acmegothiccompsemibold|regular":"AcmeGothic-CompressedSemibold","acmegothiccompsemibold|roman":"AcmeGothic-CompressedSemibold","acmegothiccond|bold":"AcmeGothic-CondensedBold","acmegothiccond|book":"AcmeGothic-CondensedRegular","acmegothiccond|normal":"AcmeGothic-CondensedRegular","acmegothiccond|regular":"AcmeGothic-CondensedRegular","acmegothiccond|roman":"AcmeGothic-CondensedRegular","acmegothiccondblack|book":"AcmeGothic-CondensedBlack","acmegothiccondblack|normal":"AcmeGothic-CondensedBlack","acmegothiccondblack|regular":"AcmeGothic-CondensedBlack","acmegothiccondblack|roman":"AcmeGothic-CondensedBlack","acmegothiccondensed|black":"AcmeGothic-CondensedBlack","acmegothiccondensed|bold":"AcmeGothic-CondensedBold","acmegothiccondensed|book":"AcmeGothic-CondensedRegular","acmegothiccondensed|demi":"AcmeGothic-CondensedSemibold","acmegothiccondensed|demibold":"AcmeGothic-CondensedSemibold","acmegothiccondensed|heavy":"AcmeGothic-CondensedBlack","acmegothiccondensed|light":"AcmeGothic-CondensedLight","acmegothiccondensed|normal":"AcmeGothic-CondensedRegular","acmegothiccondensed|regular":"AcmeGothic-CondensedRegular","acmegothiccondensed|roman":"AcmeGothic-CondensedRegular","acmegothiccondensed|semibold":"AcmeGothic-CondensedSemibold","acmegothiccondlight|book":"AcmeGothic-CondensedLight","acmegothiccondlight|normal":"AcmeGothic-CondensedLight","acmegothiccondlight|regular":"AcmeGothic-CondensedLight","acmegothiccondlight|roman":"AcmeGothic-CondensedLight","acmegothiccondsemibold|book":"AcmeGothic-CondensedSemibold","acmegothiccondsemibold|normal":"AcmeGothic-CondensedSemibold","acmegothiccondsemibold|regular":"AcmeGothic-CondensedSemibold","acmegothiccondsemibold|roman":"AcmeGothic-CondensedSemibold","acmegothicextrawide|black":"AcmeGothic-ExtrawideBlack","acmegothicextrawide|bold":"AcmeGothic-ExtrawideBold","acmegothicextrawide|book":"AcmeGothic-ExtrawideRegular","acmegothicextrawide|demi":"AcmeGothic-ExtrawideSemibold","acmegothicextrawide|demibold":"AcmeGothic-ExtrawideSemibold","acmegothicextrawide|heavy":"AcmeGothic-ExtrawideBlack","acmegothicextrawide|light":"AcmeGothic-ExtrawideLight","acmegothicextrawide|normal":"AcmeGothic-ExtrawideRegular","acmegothicextrawide|regular":"AcmeGothic-ExtrawideRegular","acmegothicextrawide|roman":"AcmeGothic-ExtrawideRegular","acmegothicextrawide|semibold":"AcmeGothic-ExtrawideSemibold","acmegothicexwide|bold":"AcmeGothic-ExtrawideBold","acmegothicexwide|book":"AcmeGothic-ExtrawideRegular","acmegothicexwide|normal":"AcmeGothic-ExtrawideRegular","acmegothicexwide|regular":"AcmeGothic-ExtrawideRegular","acmegothicexwide|roman":"AcmeGothic-ExtrawideRegular","acmegothicexwideblack|book":"AcmeGothic-ExtrawideBlack","acmegothicexwideblack|normal":"AcmeGothic-ExtrawideBlack","acmegothicexwideblack|regular":"AcmeGothic-ExtrawideBlack","acmegothicexwideblack|roman":"AcmeGothic-ExtrawideBlack","acmegothicexwidelight|book":"AcmeGothic-ExtrawideLight","acmegothicexwidelight|normal":"AcmeGothic-ExtrawideLight","acmegothicexwidelight|regular":"AcmeGothic-ExtrawideLight","acmegothicexwidelight|roman":"AcmeGothic-ExtrawideLight","acmegothicexwidesemibold|book":"AcmeGothic-ExtrawideSemibold","acmegothicexwidesemibold|normal":"AcmeGothic-ExtrawideSemibold","acmegothicexwidesemibold|regular":"AcmeGothic-ExtrawideSemibold","acmegothicexwidesemibold|roman":"AcmeGothic-ExtrawideSemibold","acmegothiclight|book":"AcmeGothic-Light","acmegothiclight|normal":"AcmeGothic-Light","acmegothiclight|regular":"AcmeGothic-Light","acmegothiclight|roman":"AcmeGothic-Light","acmegothicsemibold|book":"AcmeGothic-Semibold","acmegothicsemibold|normal":"AcmeGothic-Semibold","acmegothicsemibold|regular":"AcmeGothic-Semibold","acmegothicsemibold|roman":"AcmeGothic-Semibold","acmegothicwide|black":"AcmeGothic-WideBlack","acmegothicwide|bold":"AcmeGothic-WideBold","acmegothicwide|book":"AcmeGothic-WideRegular","acmegothicwide|demi":"AcmeGothic-WideSemibold","acmegothicwide|demibold":"AcmeGothic-WideSemibold","acmegothicwide|heavy":"AcmeGothic-WideBlack","acmegothicwide|light":"AcmeGothic-WideLight","acmegothicwide|normal":"AcmeGothic-WideRegular","acmegothicwide|regular":"AcmeGothic-WideRegular","acmegothicwide|roman":"AcmeGothic-WideRegular","acmegothicwide|semibold":"AcmeGothic-WideSemibold","acmegothicwideblack|book":"AcmeGothic-WideBlack","acmegothicwideblack|normal":"AcmeGothic-WideBlack","acmegothicwideblack|regular":"AcmeGothic-WideBlack","acmegothicwideblack|roman":"AcmeGothic-WideBlack","acmegothicwidelight|book":"AcmeGothic-WideLight","acmegothicwidelight|normal":"AcmeGothic-WideLight","acmegothicwidelight|regular":"AcmeGothic-WideLight","acmegothicwidelight|roman":"AcmeGothic-WideLight","acmegothicwidesemibold|book":"AcmeGothic-WideSemibold","acmegothicwidesemibold|normal":"AcmeGothic-WideSemibold","acmegothicwidesemibold|regular":"AcmeGothic-WideSemibold","acmegothicwidesemibold|roman":"AcmeGothic-WideSemibold","acuminpro|black":"AcuminPro-Black","acuminpro|bold":"AcuminPro-Bold","acuminpro|bolditalic":"AcuminPro-BoldItalic","acuminpro|book":"AcuminPro-Regular","acuminpro|demi":"AcuminPro-Semibold","acuminpro|demibold":"AcuminPro-Semibold","acuminpro|heavy":"AcuminPro-Black","acuminpro|italic":"AcuminPro-Italic","acuminpro|medium":"AcuminPro-Medium","acuminpro|normal":"AcuminPro-Regular","acuminpro|regular":"AcuminPro-Regular","acuminpro|roman":"AcuminPro-Regular","acuminpro|semibold":"AcuminPro-Semibold","acuminpro|ultrablack":"AcuminPro-UltraBlack","acuminproblack|book":"AcuminPro-Black","acuminproblack|normal":"AcuminPro-Black","acuminproblack|regular":"AcuminPro-Black","acuminproblack|roman":"AcuminPro-Black","acuminprocondensed|bold":"AcuminProCond-Bold","acuminprocondensed|light":"AcuminProCond-Light","acuminprocondensedlight|book":"AcuminProCond-Light","acuminprocondensedlight|normal":"AcuminProCond-Light","acuminprocondensedlight|regular":"AcuminProCond-Light","acuminprocondensedlight|roman":"AcuminProCond-Light","acuminproextracondensed|bold":"AcuminProExtraCond-Bold","acuminpromedium|book":"AcuminPro-Medium","acuminpromedium|normal":"AcuminPro-Medium","acuminpromedium|regular":"AcuminPro-Medium","acuminpromedium|roman":"AcuminPro-Medium","acuminprosemibold|book":"AcuminPro-Semibold","acuminprosemibold|normal":"AcuminPro-Semibold","acuminprosemibold|regular":"AcuminPro-Semibold","acuminprosemibold|roman":"AcuminPro-Semibold","acuminprosemicondensed|book":"AcuminProSemiCond-Regular","acuminprosemicondensed|normal":"AcuminProSemiCond-Regular","acuminprosemicondensed|regular":"AcuminProSemiCond-Regular","acuminprosemicondensed|roman":"AcuminProSemiCond-Regular","acuminproultrablack|book":"AcuminPro-UltraBlack","acuminproultrablack|normal":"AcuminPro-UltraBlack","acuminproultrablack|regular":"AcuminPro-UltraBlack","acuminproultrablack|roman":"AcuminPro-UltraBlack","acuminprowide|bold":"AcuminProWide-Bold","acuminprowide|bolditalic":"AcuminProWide-BoldItalic","acuminprowide|book":"AcuminProWide-Regular","acuminprowide|demi":"AcuminProWide-Semibold","acuminprowide|demibold":"AcuminProWide-Semibold","acuminprowide|italic":"AcuminProWide-Italic","acuminprowide|normal":"AcuminProWide-Regular","acuminprowide|regular":"AcuminProWide-Regular","acuminprowide|roman":"AcuminProWide-Regular","acuminprowide|semibold":"AcuminProWide-Semibold","acuminprowide|ultrablackitalic":"AcuminProWide-UltraBlackItalic","acuminprowidesemibold|book":"AcuminProWide-Semibold","acuminprowidesemibold|normal":"AcuminProWide-Semibold","acuminprowidesemibold|regular":"AcuminProWide-Semibold","acuminprowidesemibold|roman":"AcuminProWide-Semibold","acuminprowideultrablack|italic":"AcuminProWide-UltraBlackItalic","adobegothicstd|b":"AdobeGothicStd-Bold","adobegothicstd|bold":"AdobeGothicStd-Bold","adobegothicstdb|bold":"AdobeGothicStd-Bold","adobethai|bold":"AdobeThai-Bold","adobethai|bolditalic":"AdobeThai-BoldItalic","adobethai|book":"AdobeThai-Regular","adobethai|italic":"AdobeThai-Italic","adobethai|normal":"AdobeThai-Regular","adobethai|regular":"AdobeThai-Regular","adobethai|roman":"AdobeThai-Regular","aileron|bold":"Aileron-Bold","aileron|book":"Aileron-Regular","aileron|normal":"Aileron-Regular","aileron|regular":"Aileron-Regular","aileron|roman":"Aileron-Regular","aileronbold|book":"Aileron-Bold","aileronbold|normal":"Aileron-Bold","aileronbold|regular":"Aileron-Bold","aileronbold|roman":"Aileron-Bold","akazan|book":"AkazanRg","akazan|normal":"AkazanRg","akazan|regular":"AkazanRg","akazan|roman":"AkazanRg","akazanrg|book":"AkazanRg","akazanrg|normal":"AkazanRg","akazanrg|regular":"AkazanRg","akazanrg|roman":"AkazanRg","albra|blacktrial":"AlbraTRIAL-Black","albra|boldtrial":"AlbraTRIAL-Bold","albra|lighttrial":"AlbraTRIAL-Light","albra|mediumtrial":"AlbraTRIAL-Medium","albra|regulartrial":"AlbraTRIAL-Regular","albra|semitrial":"AlbraTRIAL-Semi","albrabooktrial|blacktrial":"AlbraBookTRIAL-Black","albrabooktrial|boldtrial":"AlbraBookTRIAL-Bold","albrabooktrial|lighttrial":"AlbraBookTRIAL-Light","albrabooktrial|mediumtrial":"AlbraBookTRIAL-Medium","albrabooktrial|regulartrial":"AlbraBookTRIAL-Regular","albrabooktrial|semitrial":"AlbraBookTRIAL-Semi","albrabooktrialblacktrial|book":"AlbraBookTRIAL-Black","albrabooktrialblacktrial|normal":"AlbraBookTRIAL-Black","albrabooktrialblacktrial|regular":"AlbraBookTRIAL-Black","albrabooktrialblacktrial|roman":"AlbraBookTRIAL-Black","albrabooktrialboldtrial|book":"AlbraBookTRIAL-Bold","albrabooktrialboldtrial|normal":"AlbraBookTRIAL-Bold","albrabooktrialboldtrial|regular":"AlbraBookTRIAL-Bold","albrabooktrialboldtrial|roman":"AlbraBookTRIAL-Bold","albrabooktriallighttrial|book":"AlbraBookTRIAL-Light","albrabooktriallighttrial|normal":"AlbraBookTRIAL-Light","albrabooktriallighttrial|regular":"AlbraBookTRIAL-Light","albrabooktriallighttrial|roman":"AlbraBookTRIAL-Light","albrabooktrialmediumtrial|book":"AlbraBookTRIAL-Medium","albrabooktrialmediumtrial|normal":"AlbraBookTRIAL-Medium","albrabooktrialmediumtrial|regular":"AlbraBookTRIAL-Medium","albrabooktrialmediumtrial|roman":"AlbraBookTRIAL-Medium","albrabooktrialsemitrial|bold":"AlbraBookTRIAL-Semi","albrabooktrialtrial|book":"AlbraBookTRIAL-Regular","albrabooktrialtrial|normal":"AlbraBookTRIAL-Regular","albrabooktrialtrial|regular":"AlbraBookTRIAL-Regular","albrabooktrialtrial|roman":"AlbraBookTRIAL-Regular","albragrotesktrial|blackitalictrial":"AlbraGroteskTRIAL-BlackItalic","albragrotesktrial|blacktrial":"AlbraGroteskTRIAL-Black","albragrotesktrial|bold":"AlbraGroteskTRIAL-BoldItalic","albragrotesktrial|bolditalictrial":"AlbraGroteskTRIAL-BoldItalic","albragrotesktrial|boldtrial":"AlbraGroteskTRIAL-Bold","albragrotesktrial|italic":"AlbraGroteskTRIAL-RegularItalic","albragrotesktrial|lightitalictrial":"AlbraGroteskTRIAL-LightItalic","albragrotesktrial|lighttrial":"AlbraGroteskTRIAL-Light","albragrotesktrial|mediumitalictrial":"AlbraGroteskTRIAL-MediumItalic","albragrotesktrial|mediumtrial":"AlbraGroteskTRIAL-Medium","albragrotesktrial|regularitalictrial":"AlbraGroteskTRIAL-RegularItalic","albragrotesktrial|regulartrial":"AlbraGroteskTRIAL-Regular","albragrotesktrial|semiitalictrial":"AlbraGroteskTRIAL-SemiItalic","albragrotesktrial|semitrial":"AlbraGroteskTRIAL-Semi","albragrotesktrialblackittrial|book":"AlbraGroteskTRIAL-BlackItalic","albragrotesktrialblackittrial|normal":"AlbraGroteskTRIAL-BlackItalic","albragrotesktrialblackittrial|regular":"AlbraGroteskTRIAL-BlackItalic","albragrotesktrialblackittrial|roman":"AlbraGroteskTRIAL-BlackItalic","albragrotesktrialblacktrial|book":"AlbraGroteskTRIAL-Black","albragrotesktrialblacktrial|normal":"AlbraGroteskTRIAL-Black","albragrotesktrialblacktrial|regular":"AlbraGroteskTRIAL-Black","albragrotesktrialblacktrial|roman":"AlbraGroteskTRIAL-Black","albragrotesktrialboldtrial|book":"AlbraGroteskTRIAL-Bold","albragrotesktrialboldtrial|normal":"AlbraGroteskTRIAL-Bold","albragrotesktrialboldtrial|regular":"AlbraGroteskTRIAL-Bold","albragrotesktrialboldtrial|roman":"AlbraGroteskTRIAL-Bold","albragrotesktriallightittrial|book":"AlbraGroteskTRIAL-LightItalic","albragrotesktriallightittrial|normal":"AlbraGroteskTRIAL-LightItalic","albragrotesktriallightittrial|regular":"AlbraGroteskTRIAL-LightItalic","albragrotesktriallightittrial|roman":"AlbraGroteskTRIAL-LightItalic","albragrotesktriallighttrial|book":"AlbraGroteskTRIAL-Light","albragrotesktriallighttrial|normal":"AlbraGroteskTRIAL-Light","albragrotesktriallighttrial|regular":"AlbraGroteskTRIAL-Light","albragrotesktriallighttrial|roman":"AlbraGroteskTRIAL-Light","albragrotesktrialmediumittrial|book":"AlbraGroteskTRIAL-MediumItalic","albragrotesktrialmediumittrial|normal":"AlbraGroteskTRIAL-MediumItalic","albragrotesktrialmediumittrial|regular":"AlbraGroteskTRIAL-MediumItalic","albragrotesktrialmediumittrial|roman":"AlbraGroteskTRIAL-MediumItalic","albragrotesktrialmediumtrial|book":"AlbraGroteskTRIAL-Medium","albragrotesktrialmediumtrial|normal":"AlbraGroteskTRIAL-Medium","albragrotesktrialmediumtrial|regular":"AlbraGroteskTRIAL-Medium","albragrotesktrialmediumtrial|roman":"AlbraGroteskTRIAL-Medium","albragrotesktrialsemitrial|bold":"AlbraGroteskTRIAL-Semi","albragrotesktrialsmittrial|bold":"AlbraGroteskTRIAL-SemiItalic","albragrotesktrialtrial|book":"AlbraGroteskTRIAL-Regular","albragrotesktrialtrial|normal":"AlbraGroteskTRIAL-Regular","albragrotesktrialtrial|regular":"AlbraGroteskTRIAL-Regular","albragrotesktrialtrial|roman":"AlbraGroteskTRIAL-Regular","albrasanstrial|blackitalictrial":"AlbraSansTRIAL-BlackItalic","albrasanstrial|blacktrial":"AlbraSansTRIAL-Black","albrasanstrial|bolditalictrial":"AlbraSansTRIAL-BoldItalic","albrasanstrial|boldtrial":"AlbraSansTRIAL-Bold","albrasanstrial|lightitalictrial":"AlbraSansTRIAL-LightItalic","albrasanstrial|lighttrial":"AlbraSansTRIAL-Light","albrasanstrial|mediumitalictrial":"AlbraSansTRIAL-MediumItalic","albrasanstrial|mediumtrial":"AlbraSansTRIAL-Medium","albrasanstrial|regularitalictrial":"AlbraSansTRIAL-RegularItalic","albrasanstrial|regulartrial":"AlbraSansTRIAL-Regular","albrasanstrial|semiitalictrial":"AlbraSansTRIAL-SemiItalic","albrasanstrial|semitrial":"AlbraSansTRIAL-Semi","albrasanstrialblackittrial|book":"AlbraSansTRIAL-BlackItalic","albrasanstrialblackittrial|normal":"AlbraSansTRIAL-BlackItalic","albrasanstrialblackittrial|regular":"AlbraSansTRIAL-BlackItalic","albrasanstrialblackittrial|roman":"AlbraSansTRIAL-BlackItalic","albrasanstrialblacktrial|book":"AlbraSansTRIAL-Black","albrasanstrialblacktrial|normal":"AlbraSansTRIAL-Black","albrasanstrialblacktrial|regular":"AlbraSansTRIAL-Black","albrasanstrialblacktrial|roman":"AlbraSansTRIAL-Black","albrasanstriallightittrial|book":"AlbraSansTRIAL-LightItalic","albrasanstriallightittrial|normal":"AlbraSansTRIAL-LightItalic","albrasanstriallightittrial|regular":"AlbraSansTRIAL-LightItalic","albrasanstriallightittrial|roman":"AlbraSansTRIAL-LightItalic","albrasanstriallighttrial|book":"AlbraSansTRIAL-Light","albrasanstriallighttrial|normal":"AlbraSansTRIAL-Light","albrasanstriallighttrial|regular":"AlbraSansTRIAL-Light","albrasanstriallighttrial|roman":"AlbraSansTRIAL-Light","albrasanstrialmediumittrial|book":"AlbraSansTRIAL-MediumItalic","albrasanstrialmediumittrial|normal":"AlbraSansTRIAL-MediumItalic","albrasanstrialmediumittrial|regular":"AlbraSansTRIAL-MediumItalic","albrasanstrialmediumittrial|roman":"AlbraSansTRIAL-MediumItalic","albrasanstrialmediumtrial|book":"AlbraSansTRIAL-Medium","albrasanstrialmediumtrial|normal":"AlbraSansTRIAL-Medium","albrasanstrialmediumtrial|regular":"AlbraSansTRIAL-Medium","albrasanstrialmediumtrial|roman":"AlbraSansTRIAL-Medium","albrasanstrialsemitrial|book":"AlbraSansTRIAL-Semi","albrasanstrialsemitrial|normal":"AlbraSansTRIAL-Semi","albrasanstrialsemitrial|regular":"AlbraSansTRIAL-Semi","albrasanstrialsemitrial|roman":"AlbraSansTRIAL-Semi","albrasanstrialsmittrial|book":"AlbraSansTRIAL-SemiItalic","albrasanstrialsmittrial|normal":"AlbraSansTRIAL-SemiItalic","albrasanstrialsmittrial|regular":"AlbraSansTRIAL-SemiItalic","albrasanstrialsmittrial|roman":"AlbraSansTRIAL-SemiItalic","albrasanstrialtrial|bold":"AlbraSansTRIAL-Bold","albrasanstrialtrial|bolditalic":"AlbraSansTRIAL-BoldItalic","albrasanstrialtrial|book":"AlbraSansTRIAL-Regular","albrasanstrialtrial|italic":"AlbraSansTRIAL-RegularItalic","albrasanstrialtrial|normal":"AlbraSansTRIAL-Regular","albrasanstrialtrial|regular":"AlbraSansTRIAL-Regular","albrasanstrialtrial|roman":"AlbraSansTRIAL-Regular","albratext|bold":"AlbraTextBold","albratextbold|book":"AlbraTextBold","albratextbold|normal":"AlbraTextBold","albratextbold|regular":"AlbraTextBold","albratextbold|roman":"AlbraTextBold","albratexttrial|blackitalictrial":"AlbraTextTRIAL-BlackItalic","albratexttrial|blacktrial":"AlbraTextTRIAL-Black","albratexttrial|bolditalictrial":"AlbraTextTRIAL-BoldItalic","albratexttrial|boldtrial":"AlbraTextTRIAL-Bold","albratexttrial|lightitalictrial":"AlbraTextTRIAL-LightItalic","albratexttrial|lighttrial":"AlbraTextTRIAL-Light","albratexttrial|mediumitalictrial":"AlbraTextTRIAL-MediumItalic","albratexttrial|mediumtrial":"AlbraTextTRIAL-Medium","albratexttrial|regularitalictrial":"AlbraTextTRIAL-RegularItalic","albratexttrial|regulartrial":"AlbraTextTRIAL-Regular","albratexttrial|semiitalictrial":"AlbraTextTRIAL-SemiItalic","albratexttrial|semitrial":"AlbraTextTRIAL-Semi","albratexttrialblackittrial|book":"AlbraTextTRIAL-BlackItalic","albratexttrialblackittrial|normal":"AlbraTextTRIAL-BlackItalic","albratexttrialblackittrial|regular":"AlbraTextTRIAL-BlackItalic","albratexttrialblackittrial|roman":"AlbraTextTRIAL-BlackItalic","albratexttrialblacktrial|book":"AlbraTextTRIAL-Black","albratexttrialblacktrial|normal":"AlbraTextTRIAL-Black","albratexttrialblacktrial|regular":"AlbraTextTRIAL-Black","albratexttrialblacktrial|roman":"AlbraTextTRIAL-Black","albratexttrialboldtrial|book":"AlbraTextTRIAL-Bold","albratexttrialboldtrial|normal":"AlbraTextTRIAL-Bold","albratexttrialboldtrial|regular":"AlbraTextTRIAL-Bold","albratexttrialboldtrial|roman":"AlbraTextTRIAL-Bold","albratexttrialitalictrial|bold":"AlbraTextTRIAL-BoldItalic","albratexttriallightittrial|book":"AlbraTextTRIAL-LightItalic","albratexttriallightittrial|normal":"AlbraTextTRIAL-LightItalic","albratexttriallightittrial|regular":"AlbraTextTRIAL-LightItalic","albratexttriallightittrial|roman":"AlbraTextTRIAL-LightItalic","albratexttriallighttrial|book":"AlbraTextTRIAL-Light","albratexttriallighttrial|normal":"AlbraTextTRIAL-Light","albratexttriallighttrial|regular":"AlbraTextTRIAL-Light","albratexttriallighttrial|roman":"AlbraTextTRIAL-Light","albratexttrialmediumittrial|book":"AlbraTextTRIAL-MediumItalic","albratexttrialmediumittrial|normal":"AlbraTextTRIAL-MediumItalic","albratexttrialmediumittrial|regular":"AlbraTextTRIAL-MediumItalic","albratexttrialmediumittrial|roman":"AlbraTextTRIAL-MediumItalic","albratexttrialmediumtrial|book":"AlbraTextTRIAL-Medium","albratexttrialmediumtrial|normal":"AlbraTextTRIAL-Medium","albratexttrialmediumtrial|regular":"AlbraTextTRIAL-Medium","albratexttrialmediumtrial|roman":"AlbraTextTRIAL-Medium","albratexttrialsemitrial|bold":"AlbraTextTRIAL-Semi","albratexttrialsmittrial|book":"AlbraTextTRIAL-SemiItalic","albratexttrialsmittrial|normal":"AlbraTextTRIAL-SemiItalic","albratexttrialsmittrial|regular":"AlbraTextTRIAL-SemiItalic","albratexttrialsmittrial|roman":"AlbraTextTRIAL-SemiItalic","albratexttrialtrial|book":"AlbraTextTRIAL-Regular","albratexttrialtrial|italic":"AlbraTextTRIAL-RegularItalic","albratexttrialtrial|normal":"AlbraTextTRIAL-Regular","albratexttrialtrial|regular":"AlbraTextTRIAL-Regular","albratexttrialtrial|roman":"AlbraTextTRIAL-Regular","albratrial|blackitalictrial":"AlbraTRIAL-BlackItalic","albratrial|bold":"AlbraTRIAL-BoldItalic","albratrial|bolditalictrial":"AlbraTRIAL-BoldItalic","albratrial|italic":"AlbraTRIAL-RegularItalic","albratrial|lightitalictrial":"AlbraTRIAL-LightItalic","albratrial|mediumitalictrial":"AlbraTRIAL-MediumItalic","albratrial|regularitalictrial":"AlbraTRIAL-RegularItalic","albratrial|semiitalictrial":"AlbraTRIAL-SemiItalic","albratrialblackitalictrial|book":"AlbraTRIAL-BlackItalic","albratrialblackitalictrial|normal":"AlbraTRIAL-BlackItalic","albratrialblackitalictrial|regular":"AlbraTRIAL-BlackItalic","albratrialblackitalictrial|roman":"AlbraTRIAL-BlackItalic","albratrialblacktrial|book":"AlbraTRIAL-Black","albratrialblacktrial|normal":"AlbraTRIAL-Black","albratrialblacktrial|regular":"AlbraTRIAL-Black","albratrialblacktrial|roman":"AlbraTRIAL-Black","albratrialboldtrial|book":"AlbraTRIAL-Bold","albratrialboldtrial|normal":"AlbraTRIAL-Bold","albratrialboldtrial|regular":"AlbraTRIAL-Bold","albratrialboldtrial|roman":"AlbraTRIAL-Bold","albratriallightitalictrial|book":"AlbraTRIAL-LightItalic","albratriallightitalictrial|normal":"AlbraTRIAL-LightItalic","albratriallightitalictrial|regular":"AlbraTRIAL-LightItalic","albratriallightitalictrial|roman":"AlbraTRIAL-LightItalic","albratriallighttrial|book":"AlbraTRIAL-Light","albratriallighttrial|normal":"AlbraTRIAL-Light","albratriallighttrial|regular":"AlbraTRIAL-Light","albratriallighttrial|roman":"AlbraTRIAL-Light","albratrialmediumitalictrial|book":"AlbraTRIAL-MediumItalic","albratrialmediumitalictrial|normal":"AlbraTRIAL-MediumItalic","albratrialmediumitalictrial|regular":"AlbraTRIAL-MediumItalic","albratrialmediumitalictrial|roman":"AlbraTRIAL-MediumItalic","albratrialmediumtrial|book":"AlbraTRIAL-Medium","albratrialmediumtrial|normal":"AlbraTRIAL-Medium","albratrialmediumtrial|regular":"AlbraTRIAL-Medium","albratrialmediumtrial|roman":"AlbraTRIAL-Medium","albratrialsemiitalictrial|bold":"AlbraTRIAL-SemiItalic","albratrialsemitrial|bold":"AlbraTRIAL-Semi","albratrialtrial|book":"AlbraTRIAL-Regular","albratrialtrial|normal":"AlbraTRIAL-Regular","albratrialtrial|regular":"AlbraTRIAL-Regular","albratrialtrial|roman":"AlbraTRIAL-Regular","amir|book":"AmiR-HM","amir|normal":"AmiR-HM","amir|regular":"AmiR-HM","amir|roman":"AmiR-HM","arabella|italic":"arabella","architect|medium":"ArchitectMedium","architectmedium|book":"ArchitectMedium","architectmedium|normal":"ArchitectMedium","architectmedium|regular":"ArchitectMedium","architectmedium|roman":"ArchitectMedium","arial|black":"Arial-Black","arial|bold":"Arial-BoldMT","arial|bolditalic":"Arial-BoldItalicMT","arial|book":"ArialMT","arial|heavy":"Arial-Black","arial|italic":"Arial-ItalicMT","arial|narrow":"ArialNarrow","arial|narrowbold":"ArialNarrow-Bold","arial|narrowbolditalic":"ArialNarrow-BoldItalic","arial|narrowitalic":"ArialNarrow-Italic","arial|normal":"ArialMT","arial|regular":"ArialMT","arial|roman":"ArialMT","arialblack|book":"Arial-Black","arialblack|normal":"Arial-Black","arialblack|regular":"Arial-Black","arialblack|roman":"Arial-Black","arialnarrow|bold":"ArialNarrow-Bold","arialnarrow|bolditalic":"ArialNarrow-BoldItalic","arialnarrow|book":"ArialNarrow","arialnarrow|italic":"ArialNarrow-Italic","arialnarrow|normal":"ArialNarrow","arialnarrow|regular":"ArialNarrow","arialnarrow|roman":"ArialNarrow","arumsans|book":"ArumSansRegular","arumsans|normal":"ArumSansRegular","arumsans|regular":"ArumSansRegular","arumsans|roman":"ArumSansRegular","arumsansrg|book":"ArumSansRegular","arumsansrg|normal":"ArumSansRegular","arumsansrg|regular":"ArumSansRegular","arumsansrg|roman":"ArumSansRegular","bahnschrift|book":"Bahnschrift","bahnschrift|normal":"Bahnschrift","bahnschrift|regular":"Bahnschrift","bahnschrift|roman":"Bahnschrift","bainsley|bold":"Bainsley-Bold","bainsley|bolditalic":"Bainsley-BoldItalic","bainsley|book":"Bainsley","bainsley|italic":"Bainsley-Italic","bainsley|normal":"Bainsley","bainsley|regular":"Bainsley","bainsley|roman":"Bainsley","balham|book":"Balham","balham|normal":"Balham","balham|regular":"Balham","balham|roman":"Balham","balletharmony|balletharmony":"BalletHarmony","balletharmony|book":"BalletHarmony","balletharmony|normal":"BalletHarmony","balletharmony|regular":"BalletHarmony","balletharmony|roman":"BalletHarmony","batang|book":"Batang","batang|normal":"Batang","batang|regular":"Batang","batang|roman":"Batang","batangche|book":"BatangChe","batangche|normal":"BatangChe","batangche|regular":"BatangChe","batangche|roman":"BatangChe","bebasneue|book":"BebasNeue-Regular","bebasneue|normal":"BebasNeue-Regular","bebasneue|regular":"BebasNeue-Regular","bebasneue|roman":"BebasNeue-Regular","bitstreamverasans|bold":"BitstreamVeraSans-Bold","bitstreamverasans|boldoblique":"BitstreamVeraSans-BoldOblique","bitstreamverasans|book":"BitstreamVeraSans-Roman","bitstreamverasans|normal":"BitstreamVeraSans-Roman","bitstreamverasans|oblique":"BitstreamVeraSans-Oblique","bitstreamverasans|regular":"BitstreamVeraSans-Roman","bitstreamverasans|roman":"BitstreamVeraSans-Roman","bitstreamveraserif|bold":"BitstreamVeraSerif-Bold","bitstreamveraserif|book":"BitstreamVeraSerif-Roman","bitstreamveraserif|normal":"BitstreamVeraSerif-Roman","bitstreamveraserif|regular":"BitstreamVeraSerif-Roman","bitstreamveraserif|roman":"BitstreamVeraSerif-Roman","bitter|black":"Bitter-Black","bitter|blackitalic":"Bitter-BlackItalic","bitter|bold":"Bitter-Bold","bitter|bolditalic":"Bitter-BoldItalic","bitter|book":"Bitter-Regular","bitter|demi":"Bitter-SemiBold","bitter|demibold":"Bitter-SemiBold","bitter|extrabold":"Bitter-ExtraBold","bitter|extrabolditalic":"Bitter-ExtraBoldItalic","bitter|extralight":"Bitter-ExtraLight","bitter|extralightitalic":"Bitter-ExtraLightItalic","bitter|heavy":"Bitter-Black","bitter|italic":"Bitter-Italic","bitter|light":"Bitter-Light","bitter|lightitalic":"Bitter-LightItalic","bitter|medium":"Bitter-Medium","bitter|mediumitalic":"Bitter-MediumItalic","bitter|normal":"Bitter-Regular","bitter|regular":"Bitter-Regular","bitter|roman":"Bitter-Regular","bitter|semibold":"Bitter-SemiBold","bitter|semibolditalic":"Bitter-SemiBoldItalic","bitter|thin":"Bitter-Thin","bitter|thinitalic":"Bitter-ThinItalic","bitter|ultralight":"Bitter-ExtraLight","bitterblack|book":"Bitter-Black","bitterblack|italic":"Bitter-BlackItalic","bitterblack|normal":"Bitter-Black","bitterblack|regular":"Bitter-Black","bitterblack|roman":"Bitter-Black","bitterextrabold|book":"Bitter-ExtraBold","bitterextrabold|italic":"Bitter-ExtraBoldItalic","bitterextrabold|normal":"Bitter-ExtraBold","bitterextrabold|regular":"Bitter-ExtraBold","bitterextrabold|roman":"Bitter-ExtraBold","bitterextralight|book":"Bitter-ExtraLight","bitterextralight|italic":"Bitter-ExtraLightItalic","bitterextralight|normal":"Bitter-ExtraLight","bitterextralight|regular":"Bitter-ExtraLight","bitterextralight|roman":"Bitter-ExtraLight","bitterlight|book":"Bitter-Light","bitterlight|italic":"Bitter-LightItalic","bitterlight|normal":"Bitter-Light","bitterlight|regular":"Bitter-Light","bitterlight|roman":"Bitter-Light","bittermedium|book":"Bitter-Medium","bittermedium|italic":"Bitter-MediumItalic","bittermedium|normal":"Bitter-Medium","bittermedium|regular":"Bitter-Medium","bittermedium|roman":"Bitter-Medium","bittersemibold|book":"Bitter-SemiBold","bittersemibold|italic":"Bitter-SemiBoldItalic","bittersemibold|normal":"Bitter-SemiBold","bittersemibold|regular":"Bitter-SemiBold","bittersemibold|roman":"Bitter-SemiBold","bitterthin|book":"Bitter-Thin","bitterthin|italic":"Bitter-ThinItalic","bitterthin|normal":"Bitter-Thin","bitterthin|regular":"Bitter-Thin","bitterthin|roman":"Bitter-Thin","bodonimoda|black":"BodoniModaRoman-Black","bodonimoda|blackitalic":"BodoniModaItalic-Black","bodonimoda|bold":"BodoniModaRoman-Bold","bodonimoda|bolditalic":"BodoniModaItalic-Bold","bodonimoda|book":"BodoniModa-Regular","bodonimoda|demi":"BodoniModaRoman-SemiBold","bodonimoda|demibold":"BodoniModaRoman-SemiBold","bodonimoda|extrabold":"BodoniModaRoman-ExtraBold","bodonimoda|extrabolditalic":"BodoniModaItalic-ExtraBold","bodonimoda|heavy":"BodoniModaRoman-Black","bodonimoda|italic":"BodoniModa-Italic","bodonimoda|medium":"BodoniModaRoman-Medium","bodonimoda|mediumitalic":"BodoniModaItalic-Medium","bodonimoda|normal":"BodoniModa-Regular","bodonimoda|regular":"BodoniModa-Regular","bodonimoda|roman":"BodoniModa-Regular","bodonimoda|semibold":"BodoniModaRoman-SemiBold","bodonimoda|semibolditalic":"BodoniModaItalic-SemiBold","bodonimoda72pt|book":"BodoniModa72pt-Regular","bodonimoda72pt|demi":"BodoniModa72pt-SemiBold","bodonimoda72pt|demibold":"BodoniModa72pt-SemiBold","bodonimoda72pt|normal":"BodoniModa72pt-Regular","bodonimoda72pt|regular":"BodoniModa72pt-Regular","bodonimoda72pt|roman":"BodoniModa72pt-Regular","bodonimoda72pt|semibold":"BodoniModa72pt-SemiBold","bodonimoda72ptsemibold|book":"BodoniModa72pt-SemiBold","bodonimoda72ptsemibold|normal":"BodoniModa72pt-SemiBold","bodonimoda72ptsemibold|regular":"BodoniModa72pt-SemiBold","bodonimoda72ptsemibold|roman":"BodoniModa72pt-SemiBold","bodonimoda9pt|italic":"BodoniModa9pt-Italic","bolina|book":"Bolina","bolina|normal":"Bolina","bolina|regular":"Bolina","bolina|roman":"Bolina","bookantiqua|bold":"BookAntiqua-Bold","bookantiqua|bolditalic":"BookAntiqua-BoldItalic","bookantiqua|book":"BookAntiqua","bookantiqua|italic":"BookAntiqua-Italic","bookantiqua|normal":"BookAntiqua","bookantiqua|regular":"BookAntiqua","bookantiqua|roman":"BookAntiqua","bookmanoldstyle|bold":"BookmanOldStyle-Bold","bookmanoldstyle|bolditalic":"BookmanOldStyle-BoldItalic","bookmanoldstyle|book":"BookmanOldStyle","bookmanoldstyle|italic":"BookmanOldStyle-Italic","bookmanoldstyle|normal":"BookmanOldStyle","bookmanoldstyle|regular":"BookmanOldStyle","bookmanoldstyle|roman":"BookmanOldStyle","bookshelfsymbol7|book":"BookshelfSymbolSeven","bookshelfsymbol7|normal":"BookshelfSymbolSeven","bookshelfsymbol7|regular":"BookshelfSymbolSeven","bookshelfsymbol7|roman":"BookshelfSymbolSeven","bosin|book":"Bosin","bosin|normal":"Bosin","bosin|regular":"Bosin","bosin|roman":"Bosin","brygada1918|bold":"Brygada1918-Bold","brygada1918|bolditalic":"Brygada1918-BoldItalic","brygada1918|book":"Brygada1918-Regular","brygada1918|demi":"Brygada1918-SemiBold","brygada1918|demibold":"Brygada1918-SemiBold","brygada1918|italic":"Brygada1918-Italic","brygada1918|medium":"Brygada1918-Medium","brygada1918|mediumitalic":"Brygada1918-MediumItalic","brygada1918|normal":"Brygada1918-Regular","brygada1918|regular":"Brygada1918-Regular","brygada1918|roman":"Brygada1918-Regular","brygada1918|semibold":"Brygada1918-SemiBold","brygada1918|semibolditalic":"Brygada1918-SemiBoldItalic","brygada1918medium|book":"Brygada1918-Medium","brygada1918medium|italic":"Brygada1918-MediumItalic","brygada1918medium|normal":"Brygada1918-Medium","brygada1918medium|regular":"Brygada1918-Medium","brygada1918medium|roman":"Brygada1918-Medium","brygada1918semibold|book":"Brygada1918-SemiBold","brygada1918semibold|italic":"Brygada1918-SemiBoldItalic","brygada1918semibold|normal":"Brygada1918-SemiBold","brygada1918semibold|regular":"Brygada1918-SemiBold","brygada1918semibold|roman":"Brygada1918-SemiBold","cabin|bold":"Cabin-Bold","cabin|bolditalic":"Cabin-BoldItalic","cabin|book":"Cabin-Regular","cabin|demi":"Cabin-SemiBold","cabin|demibold":"Cabin-SemiBold","cabin|italic":"Cabin-Italic","cabin|medium":"Cabin-Medium","cabin|mediumitalic":"Cabin-MediumItalic","cabin|normal":"Cabin-Regular","cabin|regular":"Cabin-Regular","cabin|roman":"Cabin-Regular","cabin|semibold":"Cabin-SemiBold","cabin|semibolditalic":"Cabin-SemiBoldItalic","calibri|bold":"Calibri-Bold","calibri|bolditalic":"Calibri-BoldItalic","calibri|book":"Calibri","calibri|italic":"Calibri-Italic","calibri|light":"Calibri-Light","calibri|lightitalic":"Calibri-LightItalic","calibri|normal":"Calibri","calibri|regular":"Calibri","calibri|roman":"Calibri","calibrilight|book":"Calibri-Light","calibrilight|italic":"Calibri-LightItalic","calibrilight|normal":"Calibri-Light","calibrilight|regular":"Calibri-Light","calibrilight|roman":"Calibri-Light","cambria|bold":"Cambria-Bold","cambria|bolditalic":"Cambria-BoldItalic","cambria|book":"Cambria","cambria|italic":"Cambria-Italic","cambria|normal":"Cambria","cambria|regular":"Cambria","cambria|roman":"Cambria","cambriamath|book":"CambriaMath","cambriamath|normal":"CambriaMath","cambriamath|regular":"CambriaMath","cambriamath|roman":"CambriaMath","candara|bold":"Candara-Bold","candara|bolditalic":"Candara-BoldItalic","candara|book":"Candara","candara|italic":"Candara-Italic","candara|light":"Candara-Light","candara|lightitalic":"Candara-LightItalic","candara|normal":"Candara","candara|regular":"Candara","candara|roman":"Candara","candaralight|book":"Candara-Light","candaralight|italic":"Candara-LightItalic","candaralight|normal":"Candara-Light","candaralight|regular":"Candara-Light","candaralight|roman":"Candara-Light","century|book":"Century","century|normal":"Century","century|regular":"Century","century|roman":"Century","centurygothic|bold":"CenturyGothic-Bold","centurygothic|bolditalic":"CenturyGothic-BoldItalic","centurygothic|book":"CenturyGothic","centurygothic|italic":"CenturyGothic-Italic","centurygothic|normal":"CenturyGothic","centurygothic|regular":"CenturyGothic","centurygothic|roman":"CenturyGothic","clarendonurw|light":"ClarendonURW-Lig","clarendonurwextrawide|book":"ClarendonURWExtWid-Reg","clarendonurwextrawide|normal":"ClarendonURWExtWid-Reg","clarendonurwextrawide|regular":"ClarendonURWExtWid-Reg","clarendonurwextrawide|roman":"ClarendonURWExtWid-Reg","clarendonurwextwid|book":"ClarendonURWExtWid-Reg","clarendonurwextwid|normal":"ClarendonURWExtWid-Reg","clarendonurwextwid|regular":"ClarendonURWExtWid-Reg","clarendonurwextwid|roman":"ClarendonURWExtWid-Reg","clarendonurwlig|book":"ClarendonURW-Lig","clarendonurwlig|normal":"ClarendonURW-Lig","clarendonurwlig|regular":"ClarendonURW-Lig","clarendonurwlig|roman":"ClarendonURW-Lig","clarissa|book":"Clarissa","clarissa|normal":"Clarissa","clarissa|regular":"Clarissa","clarissa|roman":"Clarissa","comicsansms|bold":"ComicSansMS-Bold","comicsansms|bolditalic":"ComicSansMS-BoldItalic","comicsansms|book":"ComicSansMS","comicsansms|italic":"ComicSansMS-Italic","comicsansms|normal":"ComicSansMS","comicsansms|regular":"ComicSansMS","comicsansms|roman":"ComicSansMS","consolas|bold":"Consolas-Bold","consolas|bolditalic":"Consolas-BoldItalic","consolas|book":"Consolas","consolas|italic":"Consolas-Italic","consolas|normal":"Consolas","consolas|regular":"Consolas","consolas|roman":"Consolas","constantia|bold":"Constantia-Bold","constantia|bolditalic":"Constantia-BoldItalic","constantia|book":"Constantia","constantia|italic":"Constantia-Italic","constantia|normal":"Constantia","constantia|regular":"Constantia","constantia|roman":"Constantia","corbel|bold":"Corbel-Bold","corbel|bolditalic":"Corbel-BoldItalic","corbel|book":"Corbel","corbel|italic":"Corbel-Italic","corbel|light":"CorbelLight","corbel|lightitalic":"CorbelLight-Italic","corbel|normal":"Corbel","corbel|regular":"Corbel","corbel|roman":"Corbel","corbellight|book":"CorbelLight","corbellight|italic":"CorbelLight-Italic","corbellight|normal":"CorbelLight","corbellight|regular":"CorbelLight","corbellight|roman":"CorbelLight","cormorantgaramond|bolditalic":"CormorantGaramond-BoldItalic","cormorantgaramond|demi":"CormorantGaramond-SemiBold","cormorantgaramond|demibold":"CormorantGaramond-SemiBold","cormorantgaramond|semibold":"CormorantGaramond-SemiBold","cormorantgaramond|semibolditalic":"CormorantGaramond-SemiBoldItalic","cormorantgaramondsemibold|book":"CormorantGaramond-SemiBold","cormorantgaramondsemibold|italic":"CormorantGaramond-SemiBoldItalic","cormorantgaramondsemibold|normal":"CormorantGaramond-SemiBold","cormorantgaramondsemibold|regular":"CormorantGaramond-SemiBold","cormorantgaramondsemibold|roman":"CormorantGaramond-SemiBold","couriernew|bold":"CourierNewPS-BoldMT","couriernew|bolditalic":"CourierNewPS-BoldItalicMT","couriernew|book":"CourierNewPSMT","couriernew|italic":"CourierNewPS-ItalicMT","couriernew|normal":"CourierNewPSMT","couriernew|regular":"CourierNewPSMT","couriernew|roman":"CourierNewPSMT","damion|book":"Damion-Regular","damion|normal":"Damion-Regular","damion|regular":"Damion-Regular","damion|roman":"Damion-Regular","demosnextpro|book":"DemosNextPro-Regular","demosnextpro|light":"DemosNextPro-Light","demosnextpro|medium":"DemosNextPro-Medium","demosnextpro|normal":"DemosNextPro-Regular","demosnextpro|regular":"DemosNextPro-Regular","demosnextpro|roman":"DemosNextPro-Regular","demosnextprolight|book":"DemosNextPro-Light","demosnextprolight|normal":"DemosNextPro-Light","demosnextprolight|regular":"DemosNextPro-Light","demosnextprolight|roman":"DemosNextPro-Light","demosnextpromedium|book":"DemosNextPro-Medium","demosnextpromedium|normal":"DemosNextPro-Medium","demosnextpromedium|regular":"DemosNextPro-Medium","demosnextpromedium|roman":"DemosNextPro-Medium","divulge|book":"DivulgeRg","divulge|normal":"DivulgeRg","divulge|regular":"DivulgeRg","divulge|roman":"DivulgeRg","divulgerg|book":"DivulgeRg","divulgerg|normal":"DivulgeRg","divulgerg|regular":"DivulgeRg","divulgerg|roman":"DivulgeRg","dotum|book":"Dotum","dotum|normal":"Dotum","dotum|regular":"Dotum","dotum|roman":"Dotum","dotumche|book":"DotumChe","dotumche|normal":"DotumChe","dotumche|regular":"DotumChe","dotumche|roman":"DotumChe","dreamorphans|bold":"DreamOrphans-Bold","dreamorphans|bolditalic":"DreamOrphans-BoldItalic","dreamorphans|book":"DreamOrphans-Regular","dreamorphans|italic":"DreamOrphans-Italic","dreamorphans|normal":"DreamOrphans-Regular","dreamorphans|regular":"DreamOrphans-Regular","dreamorphans|roman":"DreamOrphans-Regular","dubai|bold":"Dubai-Bold","dubai|book":"Dubai-Regular","dubai|light":"Dubai-Light","dubai|medium":"Dubai-Medium","dubai|normal":"Dubai-Regular","dubai|regular":"Dubai-Regular","dubai|roman":"Dubai-Regular","dubailight|book":"Dubai-Light","dubailight|normal":"Dubai-Light","dubailight|regular":"Dubai-Light","dubailight|roman":"Dubai-Light","dubaimedium|book":"Dubai-Medium","dubaimedium|normal":"Dubai-Medium","dubaimedium|regular":"Dubai-Medium","dubaimedium|roman":"Dubai-Medium","dunggeunmo|book":"DungGeunMo","dunggeunmo|normal":"DungGeunMo","dunggeunmo|regular":"DungGeunMo","dunggeunmo|roman":"DungGeunMo","dunkelsansv07|expandedglatt":"DunkelSans-Expanded-Glattv0.7","dunkelsansv07expandedglatt|book":"DunkelSans-Expanded-Glattv0.7","dunkelsansv07expandedglatt|normal":"DunkelSans-Expanded-Glattv0.7","dunkelsansv07expandedglatt|regular":"DunkelSans-Expanded-Glattv0.7","dunkelsansv07expandedglatt|roman":"DunkelSans-Expanded-Glattv0.7","dustismoroman|bold":"DustismoRoman-Bold","dustismoroman|bolditalic":"DustismoRoman-BoldItalic","dustismoroman|book":"DustismoRoman","dustismoroman|italic":"DustismoRoman-Italic","dustismoroman|normal":"DustismoRoman","dustismoroman|regular":"DustismoRoman","dustismoroman|roman":"DustismoRoman","dxhotplacestd|book":"DXHotplaceStd-Regular","dxhotplacestd|normal":"DXHotplaceStd-Regular","dxhotplacestd|regular":"DXHotplaceStd-Regular","dxhotplacestd|roman":"DXHotplaceStd-Regular","dxtogetherstd|medium":"DXTogetherStd-M","dxtogetherstdmedium|book":"DXTogetherStd-M","dxtogetherstdmedium|normal":"DXTogetherStd-M","dxtogetherstdmedium|regular":"DXTogetherStd-M","dxtogetherstdmedium|roman":"DXTogetherStd-M","ebrima|bold":"Ebrima-Bold","ebrima|book":"Ebrima","ebrima|normal":"Ebrima","ebrima|regular":"Ebrima","ebrima|roman":"Ebrima","encorpadaclassic|bold":"EncorpadaClassic-Bold","encorpadaclassic|light":"EncorpadaClassic-Light","encorpadaclassiclight|book":"EncorpadaClassic-Light","encorpadaclassiclight|normal":"EncorpadaClassic-Light","encorpadaclassiclight|regular":"EncorpadaClassic-Light","encorpadaclassiclight|roman":"EncorpadaClassic-Light","expom|book":"ExpoM-HM","expom|normal":"ExpoM-HM","expom|regular":"ExpoM-HM","expom|roman":"ExpoM-HM","foglihten|book":"Foglihten","foglihten|normal":"Foglihten","foglihten|regular":"Foglihten","foglihten|roman":"Foglihten","foglihtenno06|book":"FoglihtenNo06","foglihtenno06|normal":"FoglihtenNo06","foglihtenno06|regular":"FoglihtenNo06","foglihtenno06|roman":"FoglihtenNo06","foglihtenpcs|petitecaps":"FoglihtenPCS-Petitecaps","fogtwono5|book":"FogtwoNo5","fogtwono5|normal":"FogtwoNo5","fogtwono5|regular":"FogtwoNo5","fogtwono5|roman":"FogtwoNo5","franklingothic|medium":"FranklinGothic-Medium","franklingothicmedium|book":"FranklinGothic-Medium","franklingothicmedium|italic":"FranklinGothic-MediumItalic","franklingothicmedium|normal":"FranklinGothic-Medium","franklingothicmedium|regular":"FranklinGothic-Medium","franklingothicmedium|roman":"FranklinGothic-Medium","gabriola|book":"Gabriola","gabriola|normal":"Gabriola","gabriola|regular":"Gabriola","gabriola|roman":"Gabriola","gadugi|bold":"Gadugi-Bold","gadugi|book":"Gadugi","gadugi|normal":"Gadugi","gadugi|regular":"Gadugi","gadugi|roman":"Gadugi","gantari|black":"Gantari-Black","gantari|blackitalic":"Gantari-BlackItalic","gantari|bold":"Gantari-Bold","gantari|bolditalic":"Gantari-BoldItalic","gantari|book":"Gantari-Regular","gantari|demi":"Gantari-SemiBold","gantari|demibold":"Gantari-SemiBold","gantari|extrabold":"Gantari-ExtraBold","gantari|extrabolditalic":"Gantari-ExtraBoldItalic","gantari|heavy":"Gantari-Black","gantari|italic":"Gantari-Italic","gantari|light":"Gantari-Light","gantari|lightitalic":"Gantari-LightItalic","gantari|normal":"Gantari-Regular","gantari|regular":"Gantari-Regular","gantari|roman":"Gantari-Regular","gantari|semibold":"Gantari-SemiBold","gantari|semibolditalic":"Gantari-SemiBoldItalic","gantari|thin":"Gantari-Thin","gantari|thinitalic":"Gantari-ThinItalic","gantariblack|book":"Gantari-Black","gantariblack|italic":"Gantari-BlackItalic","gantariblack|normal":"Gantari-Black","gantariblack|regular":"Gantari-Black","gantariblack|roman":"Gantari-Black","gantariextrabold|book":"Gantari-ExtraBold","gantariextrabold|italic":"Gantari-ExtraBoldItalic","gantariextrabold|normal":"Gantari-ExtraBold","gantariextrabold|regular":"Gantari-ExtraBold","gantariextrabold|roman":"Gantari-ExtraBold","gantarilight|book":"Gantari-Light","gantarilight|italic":"Gantari-LightItalic","gantarilight|normal":"Gantari-Light","gantarilight|regular":"Gantari-Light","gantarilight|roman":"Gantari-Light","gantarisemibold|book":"Gantari-SemiBold","gantarisemibold|italic":"Gantari-SemiBoldItalic","gantarisemibold|normal":"Gantari-SemiBold","gantarisemibold|regular":"Gantari-SemiBold","gantarisemibold|roman":"Gantari-SemiBold","gantarithin|book":"Gantari-Thin","gantarithin|italic":"Gantari-ThinItalic","gantarithin|normal":"Gantari-Thin","gantarithin|regular":"Gantari-Thin","gantarithin|roman":"Gantari-Thin","garamond|bold":"Garamond-Bold","garamond|book":"Garamond","garamond|italic":"Garamond-Italic","garamond|normal":"Garamond","garamond|regular":"Garamond","garamond|roman":"Garamond","georgia|bold":"Georgia-Bold","georgia|bolditalic":"Georgia-BoldItalic","georgia|book":"Georgia","georgia|italic":"Georgia-Italic","georgia|normal":"Georgia","georgia|regular":"Georgia","georgia|roman":"Georgia","gmarketsansttf|bold":"GmarketSansTTFBold","gmarketsansttf|light":"GmarketSansTTFLight","gmarketsansttf|medium":"GmarketSansTTFMedium","gmarketsansttfbold|book":"GmarketSansTTFBold","gmarketsansttfbold|normal":"GmarketSansTTFBold","gmarketsansttfbold|regular":"GmarketSansTTFBold","gmarketsansttfbold|roman":"GmarketSansTTFBold","gmarketsansttflight|book":"GmarketSansTTFLight","gmarketsansttflight|normal":"GmarketSansTTFLight","gmarketsansttflight|regular":"GmarketSansTTFLight","gmarketsansttflight|roman":"GmarketSansTTFLight","gmarketsansttfmedium|book":"GmarketSansTTFMedium","gmarketsansttfmedium|normal":"GmarketSansTTFMedium","gmarketsansttfmedium|regular":"GmarketSansTTFMedium","gmarketsansttfmedium|roman":"GmarketSansTTFMedium","gontserrat|bold":"Gontserrat-Bold","gontserrat|bolditalic":"Gontserrat-BoldItalic","gontserrat|book":"Gontserrat-Regular","gontserrat|italic":"Gontserrat-Italic","gontserrat|normal":"Gontserrat-Regular","gontserrat|regular":"Gontserrat-Regular","gontserrat|roman":"Gontserrat-Regular","gravity|bold":"Gravity-Bold","gravity|bolditalic":"Gravity-BoldItalic","gravity|book":"Gravity-Book","gravity|bookitalic":"Gravity-BookItalic","gravity|extralight":"Gravity-UltraLight","gravity|italic":"Gravity-Italic","gravity|light":"Gravity-Light","gravity|lightitalic":"Gravity-LightItalic","gravity|normal":"Gravity-Book","gravity|regular":"Gravity-Book","gravity|roman":"Gravity-Book","gravity|ultralight":"Gravity-UltraLight","gravity|ultralightitalic":"Gravity-UltraLightItalic","gravitybook|book":"Gravity-Book","gravitybook|normal":"Gravity-Book","gravitybook|regular":"Gravity-Book","gravitybook|roman":"Gravity-Book","gravityitalic|bold":"Gravity-BoldItalic","gravitylight|book":"Gravity-Light","gravitylight|italic":"Gravity-LightItalic","gravitylight|normal":"Gravity-Light","gravitylight|regular":"Gravity-Light","gravitylight|roman":"Gravity-Light","gravityultralight|book":"Gravity-UltraLight","gravityultralight|italic":"Gravity-UltraLightItalic","gravityultralight|normal":"Gravity-UltraLight","gravityultralight|regular":"Gravity-UltraLight","gravityultralight|roman":"Gravity-UltraLight","gulim|book":"Gulim","gulim|normal":"Gulim","gulim|regular":"Gulim","gulim|roman":"Gulim","gulimche|book":"GulimChe","gulimche|normal":"GulimChe","gulimche|regular":"GulimChe","gulimche|roman":"GulimChe","gungsuh|book":"Gungsuh","gungsuh|normal":"Gungsuh","gungsuh|regular":"Gungsuh","gungsuh|roman":"Gungsuh","gungsuhche|book":"GungsuhChe","gungsuhche|normal":"GungsuhChe","gungsuhche|regular":"GungsuhChe","gungsuhche|roman":"GungsuhChe","hakgyoansimbareonbatang|b":"HakgyoansimBareonbatangB","hakgyoansimbareonbatang|r":"HakgyoansimBareonbatangR","hakgyoansimbareonbatangb|book":"HakgyoansimBareonbatangB","hakgyoansimbareonbatangb|normal":"HakgyoansimBareonbatangB","hakgyoansimbareonbatangb|regular":"HakgyoansimBareonbatangB","hakgyoansimbareonbatangb|roman":"HakgyoansimBareonbatangB","hakgyoansimbareonbatangr|book":"HakgyoansimBareonbatangR","hakgyoansimbareonbatangr|normal":"HakgyoansimBareonbatangR","hakgyoansimbareonbatangr|regular":"HakgyoansimBareonbatangR","hakgyoansimbareonbatangr|roman":"HakgyoansimBareonbatangR","hakgyoansimsantteutbatang|l":"HakgyoansimSantteutbatangL","hakgyoansimsantteutbatang|m":"HakgyoansimSantteutbatangM","hakgyoansimsantteutbatangl|book":"HakgyoansimSantteutbatangL","hakgyoansimsantteutbatangl|normal":"HakgyoansimSantteutbatangL","hakgyoansimsantteutbatangl|regular":"HakgyoansimSantteutbatangL","hakgyoansimsantteutbatangl|roman":"HakgyoansimSantteutbatangL","hakgyoansimsantteutbatangm|book":"HakgyoansimSantteutbatangM","hakgyoansimsantteutbatangm|normal":"HakgyoansimSantteutbatangM","hakgyoansimsantteutbatangm|regular":"HakgyoansimSantteutbatangM","hakgyoansimsantteutbatangm|roman":"HakgyoansimSantteutbatangM","handletterink|book":"HandletterinkRegular","handletterink|normal":"HandletterinkRegular","handletterink|regular":"HandletterinkRegular","handletterink|roman":"HandletterinkRegular","headliner|book":"HeadlineR-HM","headliner|normal":"HeadlineR-HM","headliner|regular":"HeadlineR-HM","headliner|roman":"HeadlineR-HM","hedviglettersserif|book":"HedvigLettersSerif-Regular","hedviglettersserif|normal":"HedvigLettersSerif-Regular","hedviglettersserif|regular":"HedvigLettersSerif-Regular","hedviglettersserif|roman":"HedvigLettersSerif-Regular","hedviglettersserif18pt|book":"HedvigLettersSerif18pt-Regular","hedviglettersserif18pt|normal":"HedvigLettersSerif18pt-Regular","hedviglettersserif18pt|regular":"HedvigLettersSerif18pt-Regular","hedviglettersserif18pt|roman":"HedvigLettersSerif18pt-Regular","hit|book":"Hit-Regular","hit|normal":"Hit-Regular","hit|regular":"Hit-Regular","hit|roman":"Hit-Regular","hygothic|medium":"H2gtrM","hygothicextra|book":"H2gtrE","hygothicextra|normal":"H2gtrE","hygothicextra|regular":"H2gtrE","hygothicextra|roman":"H2gtrE","hygothicmedium|book":"H2gtrM","hygothicmedium|normal":"H2gtrM","hygothicmedium|regular":"H2gtrM","hygothicmedium|roman":"H2gtrM","hygraphic|medium":"H2gprM","hygraphicmedium|book":"H2gprM","hygraphicmedium|normal":"H2gprM","hygraphicmedium|regular":"H2gprM","hygraphicmedium|roman":"H2gprM","hygungso|bold":"H2gsrB","hygungsobold|book":"H2gsrB","hygungsobold|normal":"H2gsrB","hygungsobold|regular":"H2gsrB","hygungsobold|roman":"H2gsrB","hyheadline|medium":"H2hdrM","hyheadlinemedium|book":"H2hdrM","hyheadlinemedium|normal":"H2hdrM","hyheadlinemedium|regular":"H2hdrM","hyheadlinemedium|roman":"H2hdrM","hymyeongjoextra|book":"H2mjrE","hymyeongjoextra|normal":"H2mjrE","hymyeongjoextra|regular":"H2mjrE","hymyeongjoextra|roman":"H2mjrE","hypmokgak|bold":"H2mkpB","hypmokgakbold|book":"H2mkpB","hypmokgakbold|normal":"H2mkpB","hypmokgakbold|regular":"H2mkpB","hypmokgakbold|roman":"H2mkpB","hypost|light":"H2porL","hypost|medium":"H2porM","hypostlight|book":"H2porL","hypostlight|normal":"H2porL","hypostlight|regular":"H2porL","hypostlight|roman":"H2porL","hypostmedium|book":"H2porM","hypostmedium|normal":"H2porM","hypostmedium|regular":"H2porM","hypostmedium|roman":"H2porM","hyshortsamul|medium":"H2sa1M","hyshortsamulmedium|book":"H2sa1M","hyshortsamulmedium|normal":"H2sa1M","hyshortsamulmedium|regular":"H2sa1M","hyshortsamulmedium|roman":"H2sa1M","hysinmyeongjo|medium":"H2mjsM","hysinmyeongjomedium|book":"H2mjsM","hysinmyeongjomedium|normal":"H2mjsM","hysinmyeongjomedium|regular":"H2mjsM","hysinmyeongjomedium|roman":"H2mjsM","impact|book":"Impact","impact|normal":"Impact","impact|regular":"Impact","impact|roman":"Impact","inkfree|book":"InkFree","inkfree|normal":"InkFree","inkfree|regular":"InkFree","inkfree|roman":"InkFree","inter|bold":"Inter-Bold","inter|book":"Inter-Regular","inter|demi":"Inter-SemiBold","inter|demibold":"Inter-SemiBold","inter|medium":"Inter-Medium","inter|normal":"Inter-Regular","inter|regular":"Inter-Regular","inter|roman":"Inter-Regular","inter|semibold":"Inter-SemiBold","interdisplay|bold":"InterDisplay-Bold","interdisplay|book":"InterDisplay-Regular","interdisplay|normal":"InterDisplay-Regular","interdisplay|regular":"InterDisplay-Regular","interdisplay|roman":"InterDisplay-Regular","iperion|book":"Iperion","iperion|normal":"Iperion","iperion|regular":"Iperion","iperion|roman":"Iperion","itsadzoke|book":"itsadzoke","itsadzoke|normal":"itsadzoke","itsadzoke|regular":"itsadzoke","itsadzoke|roman":"itsadzoke","itsadzokes01|book":"itsadzokeS01","itsadzokes01|normal":"itsadzokeS01","itsadzokes01|regular":"itsadzokeS01","itsadzokes01|roman":"itsadzokeS01","itsadzokes02|book":"itsadzokeS02","itsadzokes02|normal":"itsadzokeS02","itsadzokes02|regular":"itsadzokeS02","itsadzokes02|roman":"itsadzokeS02","javanesetext|book":"JavaneseText","javanesetext|normal":"JavaneseText","javanesetext|regular":"JavaneseText","javanesetext|roman":"JavaneseText","jillsville|book":"Jillsville","jillsville|normal":"Jillsville","jillsville|regular":"Jillsville","jillsville|roman":"Jillsville","jsongmyung|book":"JSongMyung-Regular","jsongmyung|normal":"JSongMyung-Regular","jsongmyung|regular":"JSongMyung-Regular","jsongmyung|roman":"JSongMyung-Regular","jsongmyungregular|book":"JSongMyung-Regular","jsongmyungregular|normal":"JSongMyung-Regular","jsongmyungregular|regular":"JSongMyung-Regular","jsongmyungregular|roman":"JSongMyung-Regular","kalnia|bold":"Kalnia-Bold","kalnia|book":"Kalnia-Regular","kalnia|demi":"Kalnia-SemiBold","kalnia|demibold":"Kalnia-SemiBold","kalnia|extralight":"Kalnia-ExtraLight","kalnia|light":"Kalnia-Light","kalnia|medium":"Kalnia-Medium","kalnia|normal":"Kalnia-Regular","kalnia|regular":"Kalnia-Regular","kalnia|roman":"Kalnia-Regular","kalnia|semibold":"Kalnia-SemiBold","kalnia|thin":"Kalnia-Thin","kalnia|ultralight":"Kalnia-ExtraLight","kalniaexpanded|bold":"KalniaExpanded-Bold","kalniaexpanded|book":"KalniaExpanded-Regular","kalniaexpanded|demi":"KalniaExpanded-SemiBold","kalniaexpanded|demibold":"KalniaExpanded-SemiBold","kalniaexpanded|extralight":"KalniaExpanded-ExtraLight","kalniaexpanded|light":"KalniaExpanded-Light","kalniaexpanded|medium":"KalniaExpanded-Medium","kalniaexpanded|normal":"KalniaExpanded-Regular","kalniaexpanded|regular":"KalniaExpanded-Regular","kalniaexpanded|roman":"KalniaExpanded-Regular","kalniaexpanded|semibold":"KalniaExpanded-SemiBold","kalniaexpanded|thin":"KalniaExpanded-Thin","kalniaexpanded|ultralight":"KalniaExpanded-ExtraLight","kalniaexpandedextralight|book":"KalniaExpanded-ExtraLight","kalniaexpandedextralight|normal":"KalniaExpanded-ExtraLight","kalniaexpandedextralight|regular":"KalniaExpanded-ExtraLight","kalniaexpandedextralight|roman":"KalniaExpanded-ExtraLight","kalniaexpandedlight|book":"KalniaExpanded-Light","kalniaexpandedlight|normal":"KalniaExpanded-Light","kalniaexpandedlight|regular":"KalniaExpanded-Light","kalniaexpandedlight|roman":"KalniaExpanded-Light","kalniaexpandedmedium|book":"KalniaExpanded-Medium","kalniaexpandedmedium|normal":"KalniaExpanded-Medium","kalniaexpandedmedium|regular":"KalniaExpanded-Medium","kalniaexpandedmedium|roman":"KalniaExpanded-Medium","kalniaexpandedsemibold|book":"KalniaExpanded-SemiBold","kalniaexpandedsemibold|normal":"KalniaExpanded-SemiBold","kalniaexpandedsemibold|regular":"KalniaExpanded-SemiBold","kalniaexpandedsemibold|roman":"KalniaExpanded-SemiBold","kalniaexpandedthin|book":"KalniaExpanded-Thin","kalniaexpandedthin|normal":"KalniaExpanded-Thin","kalniaexpandedthin|regular":"KalniaExpanded-Thin","kalniaexpandedthin|roman":"KalniaExpanded-Thin","kalniaextralight|book":"Kalnia-ExtraLight","kalniaextralight|normal":"Kalnia-ExtraLight","kalniaextralight|regular":"Kalnia-ExtraLight","kalniaextralight|roman":"Kalnia-ExtraLight","kalnialight|book":"Kalnia-Light","kalnialight|normal":"Kalnia-Light","kalnialight|regular":"Kalnia-Light","kalnialight|roman":"Kalnia-Light","kalniamedium|book":"Kalnia-Medium","kalniamedium|normal":"Kalnia-Medium","kalniamedium|regular":"Kalnia-Medium","kalniamedium|roman":"Kalnia-Medium","kalniasemibold|book":"Kalnia-SemiBold","kalniasemibold|normal":"Kalnia-SemiBold","kalniasemibold|regular":"Kalnia-SemiBold","kalniasemibold|roman":"Kalnia-SemiBold","kalniasemiexpanded|bold":"KalniaSemiExpanded-Bold","kalniasemiexpanded|book":"KalniaSemiExpanded-Regular","kalniasemiexpanded|demi":"KalniaSemiExpanded-SemiBold","kalniasemiexpanded|demibold":"KalniaSemiExpanded-SemiBold","kalniasemiexpanded|extralight":"KalniaSemiExpanded-ExtraLight","kalniasemiexpanded|light":"KalniaSemiExpanded-Light","kalniasemiexpanded|medium":"KalniaSemiExpanded-Medium","kalniasemiexpanded|normal":"KalniaSemiExpanded-Regular","kalniasemiexpanded|regular":"KalniaSemiExpanded-Regular","kalniasemiexpanded|roman":"KalniaSemiExpanded-Regular","kalniasemiexpanded|semibold":"KalniaSemiExpanded-SemiBold","kalniasemiexpanded|thin":"KalniaSemiExpanded-Thin","kalniasemiexpanded|ultralight":"KalniaSemiExpanded-ExtraLight","kalniasemiexpandedextralight|book":"KalniaSemiExpanded-ExtraLight","kalniasemiexpandedextralight|normal":"KalniaSemiExpanded-ExtraLight","kalniasemiexpandedextralight|regular":"KalniaSemiExpanded-ExtraLight","kalniasemiexpandedextralight|roman":"KalniaSemiExpanded-ExtraLight","kalniasemiexpandedlight|book":"KalniaSemiExpanded-Light","kalniasemiexpandedlight|normal":"KalniaSemiExpanded-Light","kalniasemiexpandedlight|regular":"KalniaSemiExpanded-Light","kalniasemiexpandedlight|roman":"KalniaSemiExpanded-Light","kalniasemiexpandedmedium|book":"KalniaSemiExpanded-Medium","kalniasemiexpandedmedium|normal":"KalniaSemiExpanded-Medium","kalniasemiexpandedmedium|regular":"KalniaSemiExpanded-Medium","kalniasemiexpandedmedium|roman":"KalniaSemiExpanded-Medium","kalniasemiexpandedsemibold|book":"KalniaSemiExpanded-SemiBold","kalniasemiexpandedsemibold|normal":"KalniaSemiExpanded-SemiBold","kalniasemiexpandedsemibold|regular":"KalniaSemiExpanded-SemiBold","kalniasemiexpandedsemibold|roman":"KalniaSemiExpanded-SemiBold","kalniasemiexpandedthin|book":"KalniaSemiExpanded-Thin","kalniasemiexpandedthin|normal":"KalniaSemiExpanded-Thin","kalniasemiexpandedthin|regular":"KalniaSemiExpanded-Thin","kalniasemiexpandedthin|roman":"KalniaSemiExpanded-Thin","kalniathin|book":"Kalnia-Thin","kalniathin|normal":"Kalnia-Thin","kalniathin|regular":"Kalnia-Thin","kalniathin|roman":"Kalnia-Thin","lakeside|book":"Lakeside","lakeside|normal":"Lakeside","lakeside|regular":"Lakeside","lakeside|roman":"Lakeside","leelawadeeui|bold":"LeelawadeeUI-Bold","leelawadeeui|book":"LeelawadeeUI","leelawadeeui|normal":"LeelawadeeUI","leelawadeeui|regular":"LeelawadeeUI","leelawadeeui|roman":"LeelawadeeUI","leelawadeeui|semilight":"LeelawadeeUI-Semilight","leelawadeeuisemilight|book":"LeelawadeeUI-Semilight","leelawadeeuisemilight|normal":"LeelawadeeUI-Semilight","leelawadeeuisemilight|regular":"LeelawadeeUI-Semilight","leelawadeeuisemilight|roman":"LeelawadeeUI-Semilight","lerku|book":"Lerku","lerku|normal":"Lerku","lerku|regular":"Lerku","lerku|roman":"Lerku","lgeiheadline|bold":"LGEIHeadline-Bold","lgeiheadline|book":"LGEIHeadline-Regular","lgeiheadline|demi":"LGEIHeadline-Semibold","lgeiheadline|demibold":"LGEIHeadline-Semibold","lgeiheadline|light":"LGEIHeadline-Light","lgeiheadline|normal":"LGEIHeadline-Regular","lgeiheadline|regular":"LGEIHeadline-Regular","lgeiheadline|roman":"LGEIHeadline-Regular","lgeiheadline|semibold":"LGEIHeadline-Semibold","lgeiheadline|thin":"LGEIHeadline-Thin","lgeiheadlinelight|book":"LGEIHeadline-Light","lgeiheadlinelight|normal":"LGEIHeadline-Light","lgeiheadlinelight|regular":"LGEIHeadline-Light","lgeiheadlinelight|roman":"LGEIHeadline-Light","lgeiheadlineregular|book":"LGEIHeadline-Regular","lgeiheadlineregular|normal":"LGEIHeadline-Regular","lgeiheadlineregular|regular":"LGEIHeadline-Regular","lgeiheadlineregular|roman":"LGEIHeadline-Regular","lgeiheadlinesemibold|book":"LGEIHeadline-Semibold","lgeiheadlinesemibold|normal":"LGEIHeadline-Semibold","lgeiheadlinesemibold|regular":"LGEIHeadline-Semibold","lgeiheadlinesemibold|roman":"LGEIHeadline-Semibold","lgeiheadlinettf|bold":"LGEIHeadlineTTF-Bold","lgeiheadlinettf|book":"LGEIHeadlineTTF-Regular","lgeiheadlinettf|demi":"LGEIHeadlineTTF-Semibold","lgeiheadlinettf|demibold":"LGEIHeadlineTTF-Semibold","lgeiheadlinettf|light":"LGEIHeadlineTTF-Light","lgeiheadlinettf|normal":"LGEIHeadlineTTF-Regular","lgeiheadlinettf|regular":"LGEIHeadlineTTF-Regular","lgeiheadlinettf|roman":"LGEIHeadlineTTF-Regular","lgeiheadlinettf|semibold":"LGEIHeadlineTTF-Semibold","lgeiheadlinettf|thin":"LGEIHeadlineTTF-Thin","lgeiheadlinettflight|book":"LGEIHeadlineTTF-Light","lgeiheadlinettflight|normal":"LGEIHeadlineTTF-Light","lgeiheadlinettflight|regular":"LGEIHeadlineTTF-Light","lgeiheadlinettflight|roman":"LGEIHeadlineTTF-Light","lgeiheadlinettfregular|book":"LGEIHeadlineTTF-Regular","lgeiheadlinettfregular|normal":"LGEIHeadlineTTF-Regular","lgeiheadlinettfregular|regular":"LGEIHeadlineTTF-Regular","lgeiheadlinettfregular|roman":"LGEIHeadlineTTF-Regular","lgeiheadlinettfsemibold|book":"LGEIHeadlineTTF-Semibold","lgeiheadlinettfsemibold|normal":"LGEIHeadlineTTF-Semibold","lgeiheadlinettfsemibold|regular":"LGEIHeadlineTTF-Semibold","lgeiheadlinettfsemibold|roman":"LGEIHeadlineTTF-Semibold","lgeiheadlinettfthin|book":"LGEIHeadlineTTF-Thin","lgeiheadlinettfthin|normal":"LGEIHeadlineTTF-Thin","lgeiheadlinettfthin|regular":"LGEIHeadlineTTF-Thin","lgeiheadlinettfthin|roman":"LGEIHeadlineTTF-Thin","lgeitext|bold":"LGEIText-Bold","lgeitext|book":"LGEIText-Regular","lgeitext|demi":"LGEIText-SemiBold","lgeitext|demibold":"LGEIText-SemiBold","lgeitext|light":"LGEIText-Light","lgeitext|normal":"LGEIText-Regular","lgeitext|regular":"LGEIText-Regular","lgeitext|roman":"LGEIText-Regular","lgeitext|semibold":"LGEIText-SemiBold","lgeitextbold|book":"LGEIText-Bold","lgeitextbold|normal":"LGEIText-Bold","lgeitextbold|regular":"LGEIText-Bold","lgeitextbold|roman":"LGEIText-Bold","lgeitextlight|book":"LGEIText-Light","lgeitextlight|normal":"LGEIText-Light","lgeitextlight|regular":"LGEIText-Light","lgeitextlight|roman":"LGEIText-Light","lgeitextregular|book":"LGEIText-Regular","lgeitextregular|normal":"LGEIText-Regular","lgeitextregular|regular":"LGEIText-Regular","lgeitextregular|roman":"LGEIText-Regular","lgeitextsemibold|book":"LGEIText-SemiBold","lgeitextsemibold|normal":"LGEIText-SemiBold","lgeitextsemibold|regular":"LGEIText-SemiBold","lgeitextsemibold|roman":"LGEIText-SemiBold","lgeitextttf|bold":"LGEITextTTF-Bold","lgeitextttf|book":"LGEITextTTF-Regular","lgeitextttf|demi":"LGEITextTTF-SemiBold","lgeitextttf|demibold":"LGEITextTTF-SemiBold","lgeitextttf|light":"LGEITextTTF-Light","lgeitextttf|normal":"LGEITextTTF-Regular","lgeitextttf|regular":"LGEITextTTF-Regular","lgeitextttf|roman":"LGEITextTTF-Regular","lgeitextttf|semibold":"LGEITextTTF-SemiBold","lgeitextttfbold|book":"LGEITextTTF-Bold","lgeitextttfbold|normal":"LGEITextTTF-Bold","lgeitextttfbold|regular":"LGEITextTTF-Bold","lgeitextttfbold|roman":"LGEITextTTF-Bold","lgeitextttflight|book":"LGEITextTTF-Light","lgeitextttflight|normal":"LGEITextTTF-Light","lgeitextttflight|regular":"LGEITextTTF-Light","lgeitextttflight|roman":"LGEITextTTF-Light","lgeitextttfregular|book":"LGEITextTTF-Regular","lgeitextttfregular|normal":"LGEITextTTF-Regular","lgeitextttfregular|regular":"LGEITextTTF-Regular","lgeitextttfregular|roman":"LGEITextTTF-Regular","lgeitextttfsemibold|book":"LGEITextTTF-SemiBold","lgeitextttfsemibold|normal":"LGEITextTTF-SemiBold","lgeitextttfsemibold|regular":"LGEITextTTF-SemiBold","lgeitextttfsemibold|roman":"LGEITextTTF-SemiBold","lgsmart|bold":"LGSmart-Bold","lgsmart|bolditalic":"LGSmart-BoldItalic","lgsmart|book":"LGSmart","lgsmart|demi":"LGSmartLight-SemiBold","lgsmart|demibold":"LGSmartLight-SemiBold","lgsmart|italic":"LGSmart-Italic","lgsmart|light":"LGSmartLight-Light","lgsmart|normal":"LGSmart","lgsmart|regular":"LGSmart","lgsmart|regularitalic":"LGSmart-Italic","lgsmart|roman":"LGSmart","lgsmart|semibold":"LGSmartLight-SemiBold","lgsmarth|bold":"LGSmHaB","lgsmarth|book":"LGSmHaR","lgsmarth|demi":"LGSmHaSB","lgsmarth|demibold":"LGSmHaSB","lgsmarth|light":"LGSmHaL","lgsmarth|normal":"LGSmHaR","lgsmarth|regular":"LGSmHaR","lgsmarth|roman":"LGSmHaR","lgsmarth|semibold":"LGSmHaSB","lgsmarth20|b":"LGSmHaTB","lgsmarth20|l":"LGSmHaTL","lgsmarth20|r":"LGSmHaTR","lgsmarth20|sb":"LGSmHaTSB","lgsmarth20b|book":"LGSmHaTB","lgsmarth20b|normal":"LGSmHaTB","lgsmarth20b|regular":"LGSmHaTB","lgsmarth20b|roman":"LGSmHaTB","lgsmarth20l|book":"LGSmHaTL","lgsmarth20l|normal":"LGSmHaTL","lgsmarth20l|regular":"LGSmHaTL","lgsmarth20l|roman":"LGSmHaTL","lgsmarth20r|book":"LGSmHaTR","lgsmarth20r|normal":"LGSmHaTR","lgsmarth20r|regular":"LGSmHaTR","lgsmarth20r|roman":"LGSmHaTR","lgsmarth20sb|book":"LGSmHaTSB","lgsmarth20sb|normal":"LGSmHaTSB","lgsmarth20sb|regular":"LGSmHaTSB","lgsmarth20sb|roman":"LGSmHaTSB","lgsmarthbold|book":"LGSmHaB","lgsmarthbold|normal":"LGSmHaB","lgsmarthbold|regular":"LGSmHaB","lgsmarthbold|roman":"LGSmHaB","lgsmarthlight|book":"LGSmHaL","lgsmarthlight|normal":"LGSmHaL","lgsmarthlight|regular":"LGSmHaL","lgsmarthlight|roman":"LGSmHaL","lgsmarthregular|book":"LGSmHaR","lgsmarthregular|normal":"LGSmHaR","lgsmarthregular|regular":"LGSmHaR","lgsmarthregular|roman":"LGSmHaR","lgsmarthsemibold|book":"LGSmHaSB","lgsmarthsemibold|normal":"LGSmHaSB","lgsmarthsemibold|regular":"LGSmHaSB","lgsmarthsemibold|roman":"LGSmHaSB","lgsmartlight|bold":"LGSmartLight-SemiBold","lgsmartlight|book":"LGSmartLight-Light","lgsmartlight|demi":"LGSmartLight-SemiBold","lgsmartlight|demibold":"LGSmartLight-SemiBold","lgsmartlight|light":"LGSmartLight-Light","lgsmartlight|normal":"LGSmartLight-Light","lgsmartlight|regular":"LGSmartLight-Light","lgsmartlight|roman":"LGSmartLight-Light","lgsmartlight|semibold":"LGSmartLight-SemiBold","lgsmartui|bold":"LGSmartUI-Bold","lgsmartui|book":"LGSmartUI-Regular","lgsmartui|demi":"LGSmartUI-SemiBold","lgsmartui|demibold":"LGSmartUI-SemiBold","lgsmartui|light":"LGSmartUI-Light","lgsmartui|normal":"LGSmartUI-Regular","lgsmartui|regular":"LGSmartUI-Regular","lgsmartui|roman":"LGSmartUI-Regular","lgsmartui|semibold":"LGSmartUI-SemiBold","lgsmartuibold|book":"LGSmartUI-Bold","lgsmartuibold|normal":"LGSmartUI-Bold","lgsmartuibold|regular":"LGSmartUI-Bold","lgsmartuibold|roman":"LGSmartUI-Bold","lgsmartuilight|book":"LGSmartUI-Light","lgsmartuilight|normal":"LGSmartUI-Light","lgsmartuilight|regular":"LGSmartUI-Light","lgsmartuilight|roman":"LGSmartUI-Light","lgsmartuiregular|book":"LGSmartUI-Regular","lgsmartuiregular|normal":"LGSmartUI-Regular","lgsmartuiregular|regular":"LGSmartUI-Regular","lgsmartuiregular|roman":"LGSmartUI-Regular","lgsmartuisemibold|book":"LGSmartUI-SemiBold","lgsmartuisemibold|normal":"LGSmartUI-SemiBold","lgsmartuisemibold|regular":"LGSmartUI-SemiBold","lgsmartuisemibold|roman":"LGSmartUI-SemiBold","liberationsans|bold":"LiberationSans-Bold","liberationsans|bolditalic":"LiberationSans-BoldItalic","liberationsans|book":"LiberationSans-Regular","liberationsans|italic":"LiberationSans-Italic","liberationsans|normal":"LiberationSans-Regular","liberationsans|regular":"LiberationSans-Regular","liberationsans|roman":"LiberationSans-Regular","lineseedsansth|bold":"LINESeedSansTH-Bold","lineseedsansth|book":"LINESeedSansTH-Regular","lineseedsansth|extrabold":"LINESeedSansTH-ExtraBold","lineseedsansth|normal":"LINESeedSansTH-Regular","lineseedsansth|regular":"LINESeedSansTH-Regular","lineseedsansth|roman":"LINESeedSansTH-Regular","lineseedsansthextrabold|book":"LINESeedSansTH-ExtraBold","lineseedsansthextrabold|normal":"LINESeedSansTH-ExtraBold","lineseedsansthextrabold|regular":"LINESeedSansTH-ExtraBold","lineseedsansthextrabold|roman":"LINESeedSansTH-ExtraBold","lizadisplaypro|book":"LizaDisplayPro-Regular","lizadisplaypro|normal":"LizaDisplayPro-Regular","lizadisplaypro|regular":"LizaDisplayPro-Regular","lizadisplaypro|roman":"LizaDisplayPro-Regular","lthoop|black":"LTHoop-Black","lthoop|bold":"LTHoop-Bold","lthoop|book":"LTHoop-Regular","lthoop|demi":"LTHoop-SemiBold","lthoop|demibold":"LTHoop-SemiBold","lthoop|extrabold":"LTHoop-ExtraBold","lthoop|extralight":"LTHoop-ExtraLight","lthoop|heavy":"LTHoop-Black","lthoop|light":"LTHoop-Light","lthoop|medium":"LTHoop-Medium","lthoop|normal":"LTHoop-Regular","lthoop|regular":"LTHoop-Regular","lthoop|roman":"LTHoop-Regular","lthoop|semibold":"LTHoop-SemiBold","lthoop|thin":"LTHoop-Thin","lthoop|ultralight":"LTHoop-ExtraLight","lthoopblack|book":"LTHoop-Black","lthoopblack|normal":"LTHoop-Black","lthoopblack|regular":"LTHoop-Black","lthoopblack|roman":"LTHoop-Black","lthoopextbd|book":"LTHoop-ExtraBold","lthoopextbd|normal":"LTHoop-ExtraBold","lthoopextbd|regular":"LTHoop-ExtraBold","lthoopextbd|roman":"LTHoop-ExtraBold","lthoopextlt|book":"LTHoop-ExtraLight","lthoopextlt|normal":"LTHoop-ExtraLight","lthoopextlt|regular":"LTHoop-ExtraLight","lthoopextlt|roman":"LTHoop-ExtraLight","lthooplight|book":"LTHoop-Light","lthooplight|normal":"LTHoop-Light","lthooplight|regular":"LTHoop-Light","lthooplight|roman":"LTHoop-Light","lthoopmed|book":"LTHoop-Medium","lthoopmed|normal":"LTHoop-Medium","lthoopmed|regular":"LTHoop-Medium","lthoopmed|roman":"LTHoop-Medium","lthoopsembd|book":"LTHoop-SemiBold","lthoopsembd|normal":"LTHoop-SemiBold","lthoopsembd|regular":"LTHoop-SemiBold","lthoopsembd|roman":"LTHoop-SemiBold","lthoopthin|book":"LTHoop-Thin","lthoopthin|normal":"LTHoop-Thin","lthoopthin|regular":"LTHoop-Thin","lthoopthin|roman":"LTHoop-Thin","ltremark|black":"LTRemark-Black","ltremark|blackitalic":"LTRemark-BlackItalic","ltremark|bold":"LTRemark-Bold","ltremark|bolditalic":"LTRemark-BoldItalic","ltremark|book":"LTRemark-Regular","ltremark|heavy":"LTRemark-Black","ltremark|italic":"LTRemark-Italic","ltremark|normal":"LTRemark-Regular","ltremark|regular":"LTRemark-Regular","ltremark|roman":"LTRemark-Regular","ltremarkblack|book":"LTRemark-Black","ltremarkblack|normal":"LTRemark-Black","ltremarkblack|regular":"LTRemark-Black","ltremarkblack|roman":"LTRemark-Black","ltremarkblackita|italic":"LTRemark-BlackItalic","ltremarkita|bolditalic":"LTRemark-BoldItalic","ltremarkita|italic":"LTRemark-Italic","ltsuperior|bold":"LTSuperior-Bold","ltsuperior|book":"LTSuperior-Regular","ltsuperior|demi":"LTSuperior-Semibold","ltsuperior|demibold":"LTSuperior-Semibold","ltsuperior|medium":"LTSuperior-Medium","ltsuperior|normal":"LTSuperior-Regular","ltsuperior|regular":"LTSuperior-Regular","ltsuperior|roman":"LTSuperior-Regular","ltsuperior|semibold":"LTSuperior-Semibold","ltsuperiormed|book":"LTSuperior-Medium","ltsuperiormed|normal":"LTSuperior-Medium","ltsuperiormed|regular":"LTSuperior-Medium","ltsuperiormed|roman":"LTSuperior-Medium","ltsuperiormedsemibold|book":"LTSuperior-Semibold","ltsuperiormedsemibold|normal":"LTSuperior-Semibold","ltsuperiormedsemibold|regular":"LTSuperior-Semibold","ltsuperiormedsemibold|roman":"LTSuperior-Semibold","lucidaconsole|book":"LucidaConsole","lucidaconsole|normal":"LucidaConsole","lucidaconsole|regular":"LucidaConsole","lucidaconsole|roman":"LucidaConsole","lucidasansunicode|book":"LucidaSansUnicode","lucidasansunicode|normal":"LucidaSansUnicode","lucidasansunicode|regular":"LucidaSansUnicode","lucidasansunicode|roman":"LucidaSansUnicode","magicr|book":"MagicR-HM","magicr|normal":"MagicR-HM","magicr|regular":"MagicR-HM","magicr|roman":"MagicR-HM","magiera|script":"Magiera-Script","magierascript|book":"Magiera-Script","magierascript|normal":"Magiera-Script","magierascript|regular":"Magiera-Script","magierascript|roman":"Magiera-Script","malgungothic|bold":"MalgunGothicBold","malgungothic|book":"MalgunGothic","malgungothic|normal":"MalgunGothic","malgungothic|regular":"MalgunGothic","malgungothic|roman":"MalgunGothic","malgungothic|semilight":"MalgunGothic-Semilight","malgungothicsemilight|book":"MalgunGothic-Semilight","malgungothicsemilight|normal":"MalgunGothic-Semilight","malgungothicsemilight|regular":"MalgunGothic-Semilight","malgungothicsemilight|roman":"MalgunGothic-Semilight","marlett|book":"Marlett","marlett|normal":"Marlett","marlett|regular":"Marlett","marlett|roman":"Marlett","masterofbreak|book":"MasterOfBreak","masterofbreak|normal":"MasterOfBreak","masterofbreak|regular":"MasterOfBreak","masterofbreak|roman":"MasterOfBreak","microsoftjhenghei|bold":"MicrosoftJhengHeiBold","microsoftjhenghei|book":"MicrosoftJhengHeiRegular","microsoftjhenghei|light":"MicrosoftJhengHeiLight","microsoftjhenghei|normal":"MicrosoftJhengHeiRegular","microsoftjhenghei|regular":"MicrosoftJhengHeiRegular","microsoftjhenghei|roman":"MicrosoftJhengHeiRegular","microsoftjhengheilight|book":"MicrosoftJhengHeiLight","microsoftjhengheilight|normal":"MicrosoftJhengHeiLight","microsoftjhengheilight|regular":"MicrosoftJhengHeiLight","microsoftjhengheilight|roman":"MicrosoftJhengHeiLight","microsoftjhengheiui|bold":"MicrosoftJhengHeiUIBold","microsoftjhengheiui|book":"MicrosoftJhengHeiUIRegular","microsoftjhengheiui|light":"MicrosoftJhengHeiUILight","microsoftjhengheiui|normal":"MicrosoftJhengHeiUIRegular","microsoftjhengheiui|regular":"MicrosoftJhengHeiUIRegular","microsoftjhengheiui|roman":"MicrosoftJhengHeiUIRegular","microsoftjhengheiuilight|book":"MicrosoftJhengHeiUILight","microsoftjhengheiuilight|normal":"MicrosoftJhengHeiUILight","microsoftjhengheiuilight|regular":"MicrosoftJhengHeiUILight","microsoftjhengheiuilight|roman":"MicrosoftJhengHeiUILight","microsoftnewtailue|bold":"MicrosoftNewTaiLue-Bold","microsoftnewtailue|book":"MicrosoftNewTaiLue","microsoftnewtailue|normal":"MicrosoftNewTaiLue","microsoftnewtailue|regular":"MicrosoftNewTaiLue","microsoftnewtailue|roman":"MicrosoftNewTaiLue","microsoftphagspa|bold":"MicrosoftPhagsPa-Bold","microsoftphagspa|book":"MicrosoftPhagsPa","microsoftphagspa|normal":"MicrosoftPhagsPa","microsoftphagspa|regular":"MicrosoftPhagsPa","microsoftphagspa|roman":"MicrosoftPhagsPa","microsoftsansserif|book":"MicrosoftSansSerif","microsoftsansserif|normal":"MicrosoftSansSerif","microsoftsansserif|regular":"MicrosoftSansSerif","microsoftsansserif|roman":"MicrosoftSansSerif","microsofttaile|bold":"MicrosoftTaiLe-Bold","microsofttaile|book":"MicrosoftTaiLe","microsofttaile|normal":"MicrosoftTaiLe","microsofttaile|regular":"MicrosoftTaiLe","microsofttaile|roman":"MicrosoftTaiLe","microsoftyahei|bold":"MicrosoftYaHei-Bold","microsoftyahei|book":"MicrosoftYaHei","microsoftyahei|light":"MicrosoftYaHeiLight","microsoftyahei|normal":"MicrosoftYaHei","microsoftyahei|regular":"MicrosoftYaHei","microsoftyahei|roman":"MicrosoftYaHei","microsoftyaheilight|book":"MicrosoftYaHeiLight","microsoftyaheilight|normal":"MicrosoftYaHeiLight","microsoftyaheilight|regular":"MicrosoftYaHeiLight","microsoftyaheilight|roman":"MicrosoftYaHeiLight","microsoftyaheiui|bold":"MicrosoftYaHeiUI-Bold","microsoftyaheiui|book":"MicrosoftYaHeiUI","microsoftyaheiui|light":"MicrosoftYaHeiUILight","microsoftyaheiui|normal":"MicrosoftYaHeiUI","microsoftyaheiui|regular":"MicrosoftYaHeiUI","microsoftyaheiui|roman":"MicrosoftYaHeiUI","microsoftyaheiuilight|book":"MicrosoftYaHeiUILight","microsoftyaheiuilight|normal":"MicrosoftYaHeiUILight","microsoftyaheiuilight|regular":"MicrosoftYaHeiUILight","microsoftyaheiuilight|roman":"MicrosoftYaHeiUILight","microsoftyibaiti|book":"Microsoft-Yi-Baiti","microsoftyibaiti|normal":"Microsoft-Yi-Baiti","microsoftyibaiti|regular":"Microsoft-Yi-Baiti","microsoftyibaiti|roman":"Microsoft-Yi-Baiti","milibus|book":"MilibusRg-Regular","milibus|normal":"MilibusRg-Regular","milibus|regular":"MilibusRg-Regular","milibus|roman":"MilibusRg-Regular","milibusrg|book":"MilibusRg-Regular","milibusrg|normal":"MilibusRg-Regular","milibusrg|regular":"MilibusRg-Regular","milibusrg|roman":"MilibusRg-Regular","mingliuextb|book":"MingLiU-ExtB","mingliuextb|normal":"MingLiU-ExtB","mingliuextb|regular":"MingLiU-ExtB","mingliuextb|roman":"MingLiU-ExtB","mingliuhkscsextb|book":"Ming-Lt-HKSCS-ExtB","mingliuhkscsextb|normal":"Ming-Lt-HKSCS-ExtB","mingliuhkscsextb|regular":"Ming-Lt-HKSCS-ExtB","mingliuhkscsextb|roman":"Ming-Lt-HKSCS-ExtB","mingliumscsextb|book":"Ming-Lt-MSCS-ExtB","mingliumscsextb|normal":"Ming-Lt-MSCS-ExtB","mingliumscsextb|regular":"Ming-Lt-MSCS-ExtB","mingliumscsextb|roman":"Ming-Lt-MSCS-ExtB","moeumtr|book":"MoeumTR-HM","moeumtr|normal":"MoeumTR-HM","moeumtr|regular":"MoeumTR-HM","moeumtr|roman":"MoeumTR-HM","mongolianbaiti|book":"MongolianBaiti","mongolianbaiti|normal":"MongolianBaiti","mongolianbaiti|regular":"MongolianBaiti","mongolianbaiti|roman":"MongolianBaiti","monotypecorsiva|book":"MonotypeCorsiva","monotypecorsiva|normal":"MonotypeCorsiva","monotypecorsiva|regular":"MonotypeCorsiva","monotypecorsiva|roman":"MonotypeCorsiva","msgothic|book":"MS-Gothic","msgothic|normal":"MS-Gothic","msgothic|regular":"MS-Gothic","msgothic|roman":"MS-Gothic","msoutlook|book":"MSOutlook","msoutlook|normal":"MSOutlook","msoutlook|regular":"MSOutlook","msoutlook|roman":"MSOutlook","mspgothic|book":"MS-PGothic","mspgothic|normal":"MS-PGothic","mspgothic|regular":"MS-PGothic","mspgothic|roman":"MS-PGothic","msreferencesansserif|book":"MSReferenceSansSerif","msreferencesansserif|normal":"MSReferenceSansSerif","msreferencesansserif|regular":"MSReferenceSansSerif","msreferencesansserif|roman":"MSReferenceSansSerif","msreferencespecialty|book":"MSReferenceSpecialty","msreferencespecialty|normal":"MSReferenceSpecialty","msreferencespecialty|regular":"MSReferenceSpecialty","msreferencespecialty|roman":"MSReferenceSpecialty","msuigothic|book":"MS-UIGothic","msuigothic|normal":"MS-UIGothic","msuigothic|regular":"MS-UIGothic","msuigothic|roman":"MS-UIGothic","mtextra|book":"MT-Extra","mtextra|normal":"MT-Extra","mtextra|regular":"MT-Extra","mtextra|roman":"MT-Extra","munson|bold":"Munson-Bold","munson|bolditalic":"Munson-BoldItalic","munson|book":"Munson","munson|italic":"Munson-Italic","munson|normal":"Munson","munson|regular":"Munson","munson|roman":"Munson","mvboli|book":"MVBoli","mvboli|normal":"MVBoli","mvboli|regular":"MVBoli","mvboli|roman":"MVBoli","myanmartext|bold":"MyanmarText-Bold","myanmartext|book":"MyanmarText","myanmartext|normal":"MyanmarText","myanmartext|regular":"MyanmarText","myanmartext|roman":"MyanmarText","myriadpro|bold":"MyriadPro-Bold","myriadpro|book":"MyriadPro-Regular","myriadpro|normal":"MyriadPro-Regular","myriadpro|regular":"MyriadPro-Regular","myriadpro|roman":"MyriadPro-Regular","nirmalatext|bold":"NirmalaText-Bold","nirmalatext|book":"NirmalaText","nirmalatext|normal":"NirmalaText","nirmalatext|regular":"NirmalaText","nirmalatext|roman":"NirmalaText","nirmalatext|semilight":"NirmalaText-Semilight","nirmalatextsemilight|book":"NirmalaText-Semilight","nirmalatextsemilight|normal":"NirmalaText-Semilight","nirmalatextsemilight|regular":"NirmalaText-Semilight","nirmalatextsemilight|roman":"NirmalaText-Semilight","nirmalaui|bold":"NirmalaUI-Bold","nirmalaui|book":"NirmalaUI","nirmalaui|normal":"NirmalaUI","nirmalaui|regular":"NirmalaUI","nirmalaui|roman":"NirmalaUI","nirmalaui|semilight":"NirmalaUI-Semilight","nirmalauisemilight|book":"NirmalaUI-Semilight","nirmalauisemilight|normal":"NirmalaUI-Semilight","nirmalauisemilight|regular":"NirmalaUI-Semilight","nirmalauisemilight|roman":"NirmalaUI-Semilight","nordica|black":"NordicaBlack","nordica|book":"NordicaRegular","nordica|hairline":"NordicaHairline","nordica|heavy":"NordicaBlack","nordica|normal":"NordicaRegular","nordica|regular":"NordicaRegular","nordica|roman":"NordicaRegular","nordica|thin":"NordicaHairline","notosans|bold":"NotoSans-Bold","notosans|bolditalic":"NotoSans-BoldItalic","notosans|book":"NotoSans-Regular","notosans|condensed":"NotoSans-Condensed","notosans|condensedblack":"NotoSans-CondensedBlack","notosans|condensedbold":"NotoSans-CondensedBold","notosans|condensedbolditalic":"NotoSans-CondensedBoldItalic","notosans|condensedextrabold":"NotoSans-CondensedExtraBold","notosans|condensedextrabolditalic":"NotoSans-CondensedExtraBoldItalic","notosans|condensedsemibold":"NotoSans-CondensedSemiBold","notosans|condensedsemibolditalic":"NotoSans-CondensedSemiBoldItalic","notosans|demi":"NotoSans-SemiBold","notosans|demibold":"NotoSans-SemiBold","notosans|extrabold":"NotoSans-ExtraBold","notosans|extracondensedblack":"NotoSans-ExtraCondensedBlack","notosans|extracondensedbold":"NotoSans-ExtraCondensedBold","notosans|extracondensedbolditalic":"NotoSans-ExtraCondensedBoldItalic","notosans|extracondensedextrabold":"NotoSans-ExtraCondensedExtraBold","notosans|extracondensedextrabolditalic":"NotoSans-ExtraCondensedExtraBoldItalic","notosans|extracondensedsemibold":"NotoSans-ExtraCondensedSemiBold","notosans|extracondensedsemibolditalic":"NotoSans-ExtraCondensedSemiBoldItalic","notosans|italic":"NotoSans-Italic","notosans|medium":"NotoSans-Medium","notosans|normal":"NotoSans-Regular","notosans|regular":"NotoSans-Regular","notosans|roman":"NotoSans-Regular","notosans|semibold":"NotoSans-SemiBold","notosans|semicondensedblack":"NotoSans-SemiCondensedBlack","notosans|semicondensedblackitalic":"NotoSans-SemiCondensedBlackItalic","notosans|semicondensedbold":"NotoSans-SemiCondensedBold","notosans|semicondensedbolditalic":"NotoSans-SemiCondensedBoldItalic","notosans|semicondensedextrabold":"NotoSans-SemiCondensedExtraBold","notosans|semicondensedextrabolditalic":"NotoSans-SemiCondensedExtraBoldItalic","notosans|semicondensedsemibold":"NotoSans-SemiCondensedSemiBold","notosans|semicondensedsemibolditalic":"NotoSans-SemiCondensedSemiBoldItalic","notosans|thin":"NotoSans-Thin","notosanscjkkr|black":"NotoSansCJKkr-Black","notosanscjkkr|bold":"NotoSansCJKkr-Bold","notosanscjkkr|book":"NotoSansCJKkr-Regular","notosanscjkkr|demilight":"NotoSansCJKkr-DemiLight","notosanscjkkr|heavy":"NotoSansCJKkr-Black","notosanscjkkr|light":"NotoSansCJKkr-Light","notosanscjkkr|medium":"NotoSansCJKkr-Medium","notosanscjkkr|normal":"NotoSansCJKkr-Regular","notosanscjkkr|regular":"NotoSansCJKkr-Regular","notosanscjkkr|roman":"NotoSansCJKkr-Regular","notosanscjkkr|thin":"NotoSansCJKkr-Thin","notosanscjkkrblack|book":"NotoSansCJKkr-Black","notosanscjkkrblack|normal":"NotoSansCJKkr-Black","notosanscjkkrblack|regular":"NotoSansCJKkr-Black","notosanscjkkrblack|roman":"NotoSansCJKkr-Black","notosanscjkkrbold|book":"NotoSansCJKkr-Bold","notosanscjkkrbold|normal":"NotoSansCJKkr-Bold","notosanscjkkrbold|regular":"NotoSansCJKkr-Bold","notosanscjkkrbold|roman":"NotoSansCJKkr-Bold","notosanscjkkrdemilight|book":"NotoSansCJKkr-DemiLight","notosanscjkkrdemilight|normal":"NotoSansCJKkr-DemiLight","notosanscjkkrdemilight|regular":"NotoSansCJKkr-DemiLight","notosanscjkkrdemilight|roman":"NotoSansCJKkr-DemiLight","notosanscjkkrlight|book":"NotoSansCJKkr-Light","notosanscjkkrlight|normal":"NotoSansCJKkr-Light","notosanscjkkrlight|regular":"NotoSansCJKkr-Light","notosanscjkkrlight|roman":"NotoSansCJKkr-Light","notosanscjkkrmedium|book":"NotoSansCJKkr-Medium","notosanscjkkrmedium|normal":"NotoSansCJKkr-Medium","notosanscjkkrmedium|regular":"NotoSansCJKkr-Medium","notosanscjkkrmedium|roman":"NotoSansCJKkr-Medium","notosanscjkkrregular|book":"NotoSansCJKkr-Regular","notosanscjkkrregular|normal":"NotoSansCJKkr-Regular","notosanscjkkrregular|regular":"NotoSansCJKkr-Regular","notosanscjkkrregular|roman":"NotoSansCJKkr-Regular","notosanscjkkrthin|book":"NotoSansCJKkr-Thin","notosanscjkkrthin|normal":"NotoSansCJKkr-Thin","notosanscjkkrthin|regular":"NotoSansCJKkr-Thin","notosanscjkkrthin|roman":"NotoSansCJKkr-Thin","notosanscond|bold":"NotoSans-CondensedBold","notosanscond|bolditalic":"NotoSans-CondensedBoldItalic","notosanscond|book":"NotoSans-Condensed","notosanscond|normal":"NotoSans-Condensed","notosanscond|regular":"NotoSans-Condensed","notosanscond|roman":"NotoSans-Condensed","notosanscondblk|book":"NotoSans-CondensedBlack","notosanscondblk|normal":"NotoSans-CondensedBlack","notosanscondblk|regular":"NotoSans-CondensedBlack","notosanscondblk|roman":"NotoSans-CondensedBlack","notosanscondensed|black":"NotoSans-CondensedBlack","notosanscondensed|bold":"NotoSans-CondensedBold","notosanscondensed|demi":"NotoSans-CondensedSemiBold","notosanscondensed|demibold":"NotoSans-CondensedSemiBold","notosanscondensed|extrabold":"NotoSans-CondensedExtraBold","notosanscondensed|heavy":"NotoSans-CondensedBlack","notosanscondensed|semibold":"NotoSans-CondensedSemiBold","notosanscondextbd|book":"NotoSans-CondensedExtraBold","notosanscondextbd|italic":"NotoSans-CondensedExtraBoldItalic","notosanscondextbd|normal":"NotoSans-CondensedExtraBold","notosanscondextbd|regular":"NotoSans-CondensedExtraBold","notosanscondextbd|roman":"NotoSans-CondensedExtraBold","notosanscondsembd|book":"NotoSans-CondensedSemiBold","notosanscondsembd|italic":"NotoSans-CondensedSemiBoldItalic","notosanscondsembd|normal":"NotoSans-CondensedSemiBold","notosanscondsembd|regular":"NotoSans-CondensedSemiBold","notosanscondsembd|roman":"NotoSans-CondensedSemiBold","notosansextbd|book":"NotoSans-ExtraBold","notosansextbd|normal":"NotoSans-ExtraBold","notosansextbd|regular":"NotoSans-ExtraBold","notosansextbd|roman":"NotoSans-ExtraBold","notosansextcond|bold":"NotoSans-ExtraCondensedBold","notosansextcond|bolditalic":"NotoSans-ExtraCondensedBoldItalic","notosansextcondblk|book":"NotoSans-ExtraCondensedBlack","notosansextcondblk|normal":"NotoSans-ExtraCondensedBlack","notosansextcondblk|regular":"NotoSans-ExtraCondensedBlack","notosansextcondblk|roman":"NotoSans-ExtraCondensedBlack","notosansextcondextbd|book":"NotoSans-ExtraCondensedExtraBold","notosansextcondextbd|italic":"NotoSans-ExtraCondensedExtraBoldItalic","notosansextcondextbd|normal":"NotoSans-ExtraCondensedExtraBold","notosansextcondextbd|regular":"NotoSans-ExtraCondensedExtraBold","notosansextcondextbd|roman":"NotoSans-ExtraCondensedExtraBold","notosansextcondsembd|book":"NotoSans-ExtraCondensedSemiBold","notosansextcondsembd|italic":"NotoSans-ExtraCondensedSemiBoldItalic","notosansextcondsembd|normal":"NotoSans-ExtraCondensedSemiBold","notosansextcondsembd|regular":"NotoSans-ExtraCondensedSemiBold","notosansextcondsembd|roman":"NotoSans-ExtraCondensedSemiBold","notosansextracondensed|black":"NotoSans-ExtraCondensedBlack","notosansextracondensed|bold":"NotoSans-ExtraCondensedBold","notosansextracondensed|demi":"NotoSans-ExtraCondensedSemiBold","notosansextracondensed|demibold":"NotoSans-ExtraCondensedSemiBold","notosansextracondensed|extrabold":"NotoSans-ExtraCondensedExtraBold","notosansextracondensed|heavy":"NotoSans-ExtraCondensedBlack","notosansextracondensed|semibold":"NotoSans-ExtraCondensedSemiBold","notosansjp|book":"NotoSansJP-Regular","notosansjp|normal":"NotoSansJP-Regular","notosansjp|regular":"NotoSansJP-Regular","notosansjp|roman":"NotoSansJP-Regular","notosanskr|black":"NotoSansKR-Black","notosanskr|bold":"NotoSansKR-Bold","notosanskr|book":"NotoSansKR-Regular","notosanskr|demilight":"NotoSansKR-DemiLight","notosanskr|heavy":"NotoSansKR-Black","notosanskr|light":"NotoSansKR-Light","notosanskr|medium":"NotoSansKR-Medium","notosanskr|normal":"NotoSansKR-Regular","notosanskr|regular":"NotoSansKR-Regular","notosanskr|roman":"NotoSansKR-Regular","notosanskr|thin":"NotoSansKR-Thin","notosansmed|book":"NotoSans-Medium","notosansmed|normal":"NotoSans-Medium","notosansmed|regular":"NotoSans-Medium","notosansmed|roman":"NotoSans-Medium","notosanssc|book":"NotoSansSC-Regular","notosanssc|normal":"NotoSansSC-Regular","notosanssc|regular":"NotoSansSC-Regular","notosanssc|roman":"NotoSansSC-Regular","notosanssembd|book":"NotoSans-SemiBold","notosanssembd|normal":"NotoSans-SemiBold","notosanssembd|regular":"NotoSans-SemiBold","notosanssembd|roman":"NotoSans-SemiBold","notosanssemcond|bold":"NotoSans-SemiCondensedBold","notosanssemcond|bolditalic":"NotoSans-SemiCondensedBoldItalic","notosanssemcondblk|book":"NotoSans-SemiCondensedBlack","notosanssemcondblk|italic":"NotoSans-SemiCondensedBlackItalic","notosanssemcondblk|normal":"NotoSans-SemiCondensedBlack","notosanssemcondblk|regular":"NotoSans-SemiCondensedBlack","notosanssemcondblk|roman":"NotoSans-SemiCondensedBlack","notosanssemcondextbd|book":"NotoSans-SemiCondensedExtraBold","notosanssemcondextbd|italic":"NotoSans-SemiCondensedExtraBoldItalic","notosanssemcondextbd|normal":"NotoSans-SemiCondensedExtraBold","notosanssemcondextbd|regular":"NotoSans-SemiCondensedExtraBold","notosanssemcondextbd|roman":"NotoSans-SemiCondensedExtraBold","notosanssemcondsembd|book":"NotoSans-SemiCondensedSemiBold","notosanssemcondsembd|italic":"NotoSans-SemiCondensedSemiBoldItalic","notosanssemcondsembd|normal":"NotoSans-SemiCondensedSemiBold","notosanssemcondsembd|regular":"NotoSans-SemiCondensedSemiBold","notosanssemcondsembd|roman":"NotoSans-SemiCondensedSemiBold","notosanssemicondensed|black":"NotoSans-SemiCondensedBlack","notosanssemicondensed|bold":"NotoSans-SemiCondensedBold","notosanssemicondensed|demi":"NotoSans-SemiCondensedSemiBold","notosanssemicondensed|demibold":"NotoSans-SemiCondensedSemiBold","notosanssemicondensed|extrabold":"NotoSans-SemiCondensedExtraBold","notosanssemicondensed|heavy":"NotoSans-SemiCondensedBlack","notosanssemicondensed|semibold":"NotoSans-SemiCondensedSemiBold","notosanstc|book":"NotoSansTC-Regular","notosanstc|normal":"NotoSansTC-Regular","notosanstc|regular":"NotoSansTC-Regular","notosanstc|roman":"NotoSansTC-Regular","notosansthin|book":"NotoSans-Thin","notosansthin|normal":"NotoSans-Thin","notosansthin|regular":"NotoSans-Thin","notosansthin|roman":"NotoSans-Thin","notoserifcjkkr|book":"NotoSerifCJKkr-Regular","notoserifcjkkr|normal":"NotoSerifCJKkr-Regular","notoserifcjkkr|regular":"NotoSerifCJKkr-Regular","notoserifcjkkr|roman":"NotoSerifCJKkr-Regular","notoserifdisplay|book":"NotoSerifDisplay-Regular","notoserifdisplay|italic":"NotoSerifDisplay-Italic","notoserifdisplay|normal":"NotoSerifDisplay-Regular","notoserifdisplay|regular":"NotoSerifDisplay-Regular","notoserifdisplay|roman":"NotoSerifDisplay-Regular","notoserifkr|black":"NotoSerifKR-Heavy","notoserifkr|bold":"NotoSerifKR-Bold","notoserifkr|book":"NotoSerifKR-Regular","notoserifkr|demi":"NotoSerifKR-SemiBold","notoserifkr|demibold":"NotoSerifKR-SemiBold","notoserifkr|extralight":"NotoSerifKR-ExtraLight","notoserifkr|heavy":"NotoSerifKR-Heavy","notoserifkr|light":"NotoSerifKR-Light","notoserifkr|medium":"NotoSerifKR-Medium","notoserifkr|normal":"NotoSerifKR-Regular","notoserifkr|regular":"NotoSerifKR-Regular","notoserifkr|roman":"NotoSerifKR-Regular","notoserifkr|semibold":"NotoSerifKR-SemiBold","notoserifkr|ultralight":"NotoSerifKR-ExtraLight","nsimsun|book":"NSimSun","nsimsun|normal":"NSimSun","nsimsun|regular":"NSimSun","nsimsun|roman":"NSimSun","odstemplik|bold":"odstemplikBold","odstemplik|book":"odstemplik","odstemplik|normal":"odstemplik","odstemplik|regular":"odstemplik","odstemplik|roman":"odstemplik","ohitashi|demi":"OhitashiSb-Regular","ohitashi|demibold":"OhitashiSb-Regular","ohitashi|semibold":"OhitashiSb-Regular","ohitashisb|book":"OhitashiSb-Regular","ohitashisb|normal":"OhitashiSb-Regular","ohitashisb|regular":"OhitashiSb-Regular","ohitashisb|roman":"OhitashiSb-Regular","oliviadhorgent|book":"OliviaDhorgent","oliviadhorgent|normal":"OliviaDhorgent","oliviadhorgent|oliviadhorgent":"OliviaDhorgent","oliviadhorgent|regular":"OliviaDhorgent","oliviadhorgent|roman":"OliviaDhorgent","ooohbaby|book":"OoohBaby-Regular","ooohbaby|normal":"OoohBaby-Regular","ooohbaby|regular":"OoohBaby-Regular","ooohbaby|roman":"OoohBaby-Regular","oranienbaum|book":"Oranienbaum","oranienbaum|normal":"Oranienbaum","oranienbaum|regular":"Oranienbaum","oranienbaum|roman":"Oranienbaum","owned|book":"Owned-Regular","owned|normal":"Owned-Regular","owned|regular":"Owned-Regular","owned|roman":"Owned-Regular","oxeran|book":"Oxeran","oxeran|normal":"Oxeran","oxeran|regular":"Oxeran","oxeran|roman":"Oxeran","palatinolinotype|bold":"PalatinoLinotype-Bold","palatinolinotype|bolditalic":"PalatinoLinotype-BoldItalic","palatinolinotype|book":"PalatinoLinotype-Roman","palatinolinotype|italic":"PalatinoLinotype-Italic","palatinolinotype|normal":"PalatinoLinotype-Roman","palatinolinotype|regular":"PalatinoLinotype-Roman","palatinolinotype|roman":"PalatinoLinotype-Roman","paralucent|thin":"Paralucent-Thin","paralucentthin|book":"Paralucent-Thin","paralucentthin|normal":"Paralucent-Thin","paralucentthin|regular":"Paralucent-Thin","paralucentthin|roman":"Paralucent-Thin","playfairdisplay|black":"PlayfairDisplay-Black","playfairdisplay|blackitalic":"PlayfairDisplay-BlackItalic","playfairdisplay|bold":"PlayfairDisplay-Bold","playfairdisplay|bolditalic":"PlayfairDisplay-BoldItalic","playfairdisplay|book":"PlayfairDisplay-Regular","playfairdisplay|demi":"PlayfairDisplay-SemiBold","playfairdisplay|demibold":"PlayfairDisplay-SemiBold","playfairdisplay|extrabold":"PlayfairDisplay-ExtraBold","playfairdisplay|extrabolditalic":"PlayfairDisplay-ExtraBoldItalic","playfairdisplay|heavy":"PlayfairDisplay-Black","playfairdisplay|italic":"PlayfairDisplay-Italic","playfairdisplay|medium":"PlayfairDisplay-Medium","playfairdisplay|mediumitalic":"PlayfairDisplay-MediumItalic","playfairdisplay|normal":"PlayfairDisplay-Regular","playfairdisplay|regular":"PlayfairDisplay-Regular","playfairdisplay|roman":"PlayfairDisplay-Regular","playfairdisplay|semibold":"PlayfairDisplay-SemiBold","playfairdisplay|semibolditalic":"PlayfairDisplay-SemiBoldItalic","playfairdisplayblack|book":"PlayfairDisplay-Black","playfairdisplayblack|italic":"PlayfairDisplay-BlackItalic","playfairdisplayblack|normal":"PlayfairDisplay-Black","playfairdisplayblack|regular":"PlayfairDisplay-Black","playfairdisplayblack|roman":"PlayfairDisplay-Black","playfairdisplayextrabold|book":"PlayfairDisplay-ExtraBold","playfairdisplayextrabold|italic":"PlayfairDisplay-ExtraBoldItalic","playfairdisplayextrabold|normal":"PlayfairDisplay-ExtraBold","playfairdisplayextrabold|regular":"PlayfairDisplay-ExtraBold","playfairdisplayextrabold|roman":"PlayfairDisplay-ExtraBold","playfairdisplaymedium|book":"PlayfairDisplay-Medium","playfairdisplaymedium|italic":"PlayfairDisplay-MediumItalic","playfairdisplaymedium|normal":"PlayfairDisplay-Medium","playfairdisplaymedium|regular":"PlayfairDisplay-Medium","playfairdisplaymedium|roman":"PlayfairDisplay-Medium","playfairdisplaysemibold|book":"PlayfairDisplay-SemiBold","playfairdisplaysemibold|italic":"PlayfairDisplay-SemiBoldItalic","playfairdisplaysemibold|normal":"PlayfairDisplay-SemiBold","playfairdisplaysemibold|regular":"PlayfairDisplay-SemiBold","playfairdisplaysemibold|roman":"PlayfairDisplay-SemiBold","pmingliuextb|book":"PMingLiU-ExtB","pmingliuextb|normal":"PMingLiU-ExtB","pmingliuextb|regular":"PMingLiU-ExtB","pmingliuextb|roman":"PMingLiU-ExtB","polyflec|book":"PolyflecRg","polyflec|normal":"PolyflecRg","polyflec|regular":"PolyflecRg","polyflec|roman":"PolyflecRg","polyflecrg|book":"PolyflecRg","polyflecrg|normal":"PolyflecRg","polyflecrg|regular":"PolyflecRg","polyflecrg|roman":"PolyflecRg","pretendard|black":"Pretendard-Black","pretendard|bold":"Pretendard-Bold","pretendard|book":"Pretendard-Regular","pretendard|demi":"Pretendard-SemiBold","pretendard|demibold":"Pretendard-SemiBold","pretendard|extrabold":"Pretendard-ExtraBold","pretendard|extralight":"Pretendard-ExtraLight","pretendard|heavy":"Pretendard-Black","pretendard|light":"Pretendard-Light","pretendard|medium":"Pretendard-Medium","pretendard|normal":"Pretendard-Regular","pretendard|regular":"Pretendard-Regular","pretendard|roman":"Pretendard-Regular","pretendard|semibold":"Pretendard-SemiBold","pretendard|thin":"Pretendard-Thin","pretendard|ultralight":"Pretendard-ExtraLight","pretendardblack|book":"Pretendard-Black","pretendardblack|normal":"Pretendard-Black","pretendardblack|regular":"Pretendard-Black","pretendardblack|roman":"Pretendard-Black","pretendardextrabold|book":"Pretendard-ExtraBold","pretendardextrabold|normal":"Pretendard-ExtraBold","pretendardextrabold|regular":"Pretendard-ExtraBold","pretendardextrabold|roman":"Pretendard-ExtraBold","pretendardextralight|book":"Pretendard-ExtraLight","pretendardextralight|normal":"Pretendard-ExtraLight","pretendardextralight|regular":"Pretendard-ExtraLight","pretendardextralight|roman":"Pretendard-ExtraLight","pretendardlight|book":"Pretendard-Light","pretendardlight|normal":"Pretendard-Light","pretendardlight|regular":"Pretendard-Light","pretendardlight|roman":"Pretendard-Light","pretendardmedium|book":"Pretendard-Medium","pretendardmedium|normal":"Pretendard-Medium","pretendardmedium|regular":"Pretendard-Medium","pretendardmedium|roman":"Pretendard-Medium","pretendardsemibold|book":"Pretendard-SemiBold","pretendardsemibold|normal":"Pretendard-SemiBold","pretendardsemibold|regular":"Pretendard-SemiBold","pretendardsemibold|roman":"Pretendard-SemiBold","pretendardthin|book":"Pretendard-Thin","pretendardthin|normal":"Pretendard-Thin","pretendardthin|regular":"Pretendard-Thin","pretendardthin|roman":"Pretendard-Thin","prida01|black":"Prida01Black","prida01|bold":"Prida01Bold","prida01|book":"Prida01","prida01|heavy":"Prida01Black","prida01|light":"Prida01Light","prida01|normal":"Prida01","prida01|regular":"Prida01","prida01|roman":"Prida01","proximanova|black":"ProximaNova-Black","proximanova|blackitalic":"ProximaNova-BlackIt","proximanova|bolditalic":"ProximaNova-BoldIt","proximanova|book":"ProximaNova-Regular","proximanova|demi":"ProximaNova-Semibold","proximanova|demibold":"ProximaNova-Semibold","proximanova|extrabold":"ProximaNova-Extrabld","proximanova|extrabolditalic":"ProximaNova-ExtrabldIt","proximanova|heavy":"ProximaNova-Black","proximanova|italic":"ProximaNova-RegularIt","proximanova|light":"ProximaNova-Light","proximanova|lightitalic":"ProximaNova-LightIt","proximanova|medium":"ProximaNova-Medium","proximanova|mediumitalic":"ProximaNova-MediumIt","proximanova|normal":"ProximaNova-Regular","proximanova|regular":"ProximaNova-Regular","proximanova|roman":"ProximaNova-Regular","proximanova|semibold":"ProximaNova-Semibold","proximanova|semibolditalic":"ProximaNova-SemiboldIt","proximanova|thin":"ProximaNovaT-Thin","proximanova|thinitalic":"ProximaNova-ThinIt","proximanovablack|book":"ProximaNova-Black","proximanovablack|italic":"ProximaNova-BlackIt","proximanovablack|normal":"ProximaNova-Black","proximanovablack|regular":"ProximaNova-Black","proximanovablack|roman":"ProximaNova-Black","proximanovaextrabold|book":"ProximaNova-Extrabld","proximanovaextrabold|italic":"ProximaNova-ExtrabldIt","proximanovaextrabold|normal":"ProximaNova-Extrabld","proximanovaextrabold|regular":"ProximaNova-Extrabld","proximanovaextrabold|roman":"ProximaNova-Extrabld","proximanovalight|book":"ProximaNova-Light","proximanovalight|italic":"ProximaNova-LightIt","proximanovalight|normal":"ProximaNova-Light","proximanovalight|regular":"ProximaNova-Light","proximanovalight|roman":"ProximaNova-Light","proximanovamedium|book":"ProximaNova-Medium","proximanovamedium|italic":"ProximaNova-MediumIt","proximanovamedium|normal":"ProximaNova-Medium","proximanovamedium|regular":"ProximaNova-Medium","proximanovamedium|roman":"ProximaNova-Medium","proximanovasemibold|book":"ProximaNova-Semibold","proximanovasemibold|italic":"ProximaNova-SemiboldIt","proximanovasemibold|normal":"ProximaNova-Semibold","proximanovasemibold|regular":"ProximaNova-Semibold","proximanovasemibold|roman":"ProximaNova-Semibold","proximanovathin|book":"ProximaNovaT-Thin","proximanovathin|italic":"ProximaNova-ThinIt","proximanovathin|normal":"ProximaNovaT-Thin","proximanovathin|regular":"ProximaNovaT-Thin","proximanovathin|roman":"ProximaNovaT-Thin","pyunjir|book":"PyunjiR-HM","pyunjir|normal":"PyunjiR-HM","pyunjir|regular":"PyunjiR-HM","pyunjir|roman":"PyunjiR-HM","rabbidhighwaysignii|book":"RabbidHighwaySignII","rabbidhighwaysignii|normal":"RabbidHighwaySignII","rabbidhighwaysignii|obl":"RabbidHighwaySignIIObl","rabbidhighwaysignii|regular":"RabbidHighwaySignII","rabbidhighwaysignii|roman":"RabbidHighwaySignII","rachellascript|book":"RachellaScript-Regular","rachellascript|normal":"RachellaScript-Regular","rachellascript|regular":"RachellaScript-Regular","rachellascript|roman":"RachellaScript-Regular","raybentmango|book":"RaybentMango","raybentmango|normal":"RaybentMango","raybentmango|raybentmango":"RaybentMango","raybentmango|regular":"RaybentMango","raybentmango|roman":"RaybentMango","report|demi":"ReportSb-Regular","report|demibold":"ReportSb-Regular","report|semibold":"ReportSb-Regular","reportsb|book":"ReportSb-Regular","reportsb|normal":"ReportSb-Regular","reportsb|regular":"ReportSb-Regular","reportsb|roman":"ReportSb-Regular","resagokr|bold":"ResagokrBold","resagokr|book":"Resagokr","resagokr|light":"ResagokrLight","resagokr|normal":"Resagokr","resagokr|regular":"Resagokr","resagokr|roman":"Resagokr","rimouski|book":"Rimouski","rimouski|normal":"Rimouski","rimouski|regular":"Rimouski","rimouski|roman":"Rimouski","rixdongnimgothicpro|book":"RixDongnimGothicPR","rixdongnimgothicpro|normal":"RixDongnimGothicPR","rixdongnimgothicpro|regular":"RixDongnimGothicPR","rixdongnimgothicpro|roman":"RixDongnimGothicPR","rixdongnimgothicproregular|book":"RixDongnimGothicPR","rixdongnimgothicproregular|normal":"RixDongnimGothicPR","rixdongnimgothicproregular|regular":"RixDongnimGothicPR","rixdongnimgothicproregular|roman":"RixDongnimGothicPR","rixsingosepro|bold":"RixSinGoSEPB","rixsingosepro|light":"RixSinGoSEPL","rixsingosepro|medium":"RixSinGoSEPM","rixsingoseprobold|book":"RixSinGoSEPB","rixsingoseprobold|normal":"RixSinGoSEPB","rixsingoseprobold|regular":"RixSinGoSEPB","rixsingoseprobold|roman":"RixSinGoSEPB","rixsingoseprolight|book":"RixSinGoSEPL","rixsingoseprolight|normal":"RixSinGoSEPL","rixsingoseprolight|regular":"RixSinGoSEPL","rixsingoseprolight|roman":"RixSinGoSEPL","rixsingosepromedium|book":"RixSinGoSEPM","rixsingosepromedium|normal":"RixSinGoSEPM","rixsingosepromedium|regular":"RixSinGoSEPM","rixsingosepromedium|roman":"RixSinGoSEPM","roboto|black":"Roboto-Black","roboto|bold":"Roboto-Bold","roboto|book":"Roboto-Regular","roboto|heavy":"Roboto-Black","roboto|medium":"Roboto-Medium","roboto|normal":"Roboto-Regular","roboto|regular":"Roboto-Regular","roboto|roman":"Roboto-Regular","robotoserif|20ptitalic":"RobotoSerif-20ptItalic","robotoserif|20ptregular":"RobotoSerif-20ptRegular","robotoserif|black":"RobotoSerif-Black","robotoserif|blackitalic":"RobotoSerif-BlackItalic","robotoserif|bold":"RobotoSerif-Bold","robotoserif|bolditalic":"RobotoSerif-BoldItalic","robotoserif|book":"RobotoSerif-Regular","robotoserif|demi":"RobotoSerif-SemiBold","robotoserif|demibold":"RobotoSerif-SemiBold","robotoserif|extrabold":"RobotoSerif-ExtraBold","robotoserif|extrabolditalic":"RobotoSerif-ExtraBoldItalic","robotoserif|extralight":"RobotoSerif-ExtraLight","robotoserif|extralightitalic":"RobotoSerif-ExtraLightItalic","robotoserif|heavy":"RobotoSerif-Black","robotoserif|italic":"RobotoSerifNormalItalic-Regular","robotoserif|light":"RobotoSerif-Light","robotoserif|lightitalic":"RobotoSerifNormalItalic-Light","robotoserif|medium":"RobotoSerif-Medium","robotoserif|mediumitalic":"RobotoSerifNormalItalic-Medium","robotoserif|normal":"RobotoSerif-Regular","robotoserif|regular":"RobotoSerif-Regular","robotoserif|roman":"RobotoSerif-Regular","robotoserif|semibold":"RobotoSerif-SemiBold","robotoserif|semibolditalic":"RobotoSerifNormalItalic-SemiBold","robotoserif|thin":"RobotoSerif-Thin","robotoserif|thinitalic":"RobotoSerifNormalItalic-Thin","robotoserif|ultralight":"RobotoSerif-ExtraLight","robotoserif120pt|black":"RobotoSerif120pt-Black","robotoserif120pt|blackitalic":"RobotoSerif120pt-BlackItalic","robotoserif120pt|bold":"RobotoSerif120pt-Bold","robotoserif120pt|bolditalic":"RobotoSerif120pt-BoldItalic","robotoserif120pt|book":"RobotoSerif120pt-Regular","robotoserif120pt|demi":"RobotoSerif120pt-SemiBold","robotoserif120pt|demibold":"RobotoSerif120pt-SemiBold","robotoserif120pt|extrabold":"RobotoSerif120pt-ExtraBold","robotoserif120pt|extrabolditalic":"RobotoSerif120pt-ExtraBoldItalic","robotoserif120pt|extralight":"RobotoSerif120pt-ExtraLight","robotoserif120pt|extralightitalic":"RobotoSerif120pt-ExtraLightItalic","robotoserif120pt|heavy":"RobotoSerif120pt-Black","robotoserif120pt|italic":"RobotoSerif120pt-Italic","robotoserif120pt|light":"RobotoSerif120pt-Light","robotoserif120pt|lightitalic":"RobotoSerif120pt-LightItalic","robotoserif120pt|medium":"RobotoSerif120pt-Medium","robotoserif120pt|mediumitalic":"RobotoSerif120pt-MediumItalic","robotoserif120pt|normal":"RobotoSerif120pt-Regular","robotoserif120pt|regular":"RobotoSerif120pt-Regular","robotoserif120pt|roman":"RobotoSerif120pt-Regular","robotoserif120pt|semibold":"RobotoSerif120pt-SemiBold","robotoserif120pt|semibolditalic":"RobotoSerif120pt-SemiBoldItalic","robotoserif120pt|thin":"RobotoSerif120pt-Thin","robotoserif120pt|thinitalic":"RobotoSerif120pt-ThinItalic","robotoserif120pt|ultralight":"RobotoSerif120pt-ExtraLight","robotoserif120ptblack|book":"RobotoSerif120pt-Black","robotoserif120ptblack|italic":"RobotoSerif120pt-BlackItalic","robotoserif120ptblack|normal":"RobotoSerif120pt-Black","robotoserif120ptblack|regular":"RobotoSerif120pt-Black","robotoserif120ptblack|roman":"RobotoSerif120pt-Black","robotoserif120ptcondensed|black":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensed|blackitalic":"RobotoSerif120ptCondensed-BlackItalic","robotoserif120ptcondensed|bold":"RobotoSerif120ptCondensed-Bold","robotoserif120ptcondensed|bolditalic":"RobotoSerif120ptCondensed-BoldItalic","robotoserif120ptcondensed|book":"RobotoSerif120ptCondensed-Regular","robotoserif120ptcondensed|demi":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensed|demibold":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensed|extrabold":"RobotoSerif120ptCondensed-ExtraBold","robotoserif120ptcondensed|extrabolditalic":"RobotoSerif120ptCondensed-ExtraBoldItalic","robotoserif120ptcondensed|extralight":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensed|extralightitalic":"RobotoSerif120ptCondensed-ExtraLightItalic","robotoserif120ptcondensed|heavy":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensed|italic":"RobotoSerif120ptCondensed-Italic","robotoserif120ptcondensed|light":"RobotoSerif120ptCondensed-Light","robotoserif120ptcondensed|lightitalic":"RobotoSerif120ptCondensed-LightItalic","robotoserif120ptcondensed|medium":"RobotoSerif120ptCondensed-Medium","robotoserif120ptcondensed|mediumitalic":"RobotoSerif120ptCondensed-MediumItalic","robotoserif120ptcondensed|normal":"RobotoSerif120ptCondensed-Regular","robotoserif120ptcondensed|regular":"RobotoSerif120ptCondensed-Regular","robotoserif120ptcondensed|roman":"RobotoSerif120ptCondensed-Regular","robotoserif120ptcondensed|semibold":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensed|semibolditalic":"RobotoSerif120ptCondensed-SemiBoldItalic","robotoserif120ptcondensed|thin":"RobotoSerif120ptCondensed-Thin","robotoserif120ptcondensed|thinitalic":"RobotoSerif120ptCondensed-ThinItalic","robotoserif120ptcondensed|ultralight":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensedblack|book":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensedblack|italic":"RobotoSerif120ptCondensed-BlackItalic","robotoserif120ptcondensedblack|normal":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensedblack|regular":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensedblack|roman":"RobotoSerif120ptCondensed-Black","robotoserif120ptcondensedextrabold|book":"RobotoSerif120ptCondensed-ExtraBold","robotoserif120ptcondensedextrabold|italic":"RobotoSerif120ptCondensed-ExtraBoldItalic","robotoserif120ptcondensedextrabold|normal":"RobotoSerif120ptCondensed-ExtraBold","robotoserif120ptcondensedextrabold|regular":"RobotoSerif120ptCondensed-ExtraBold","robotoserif120ptcondensedextrabold|roman":"RobotoSerif120ptCondensed-ExtraBold","robotoserif120ptcondensedextralight|book":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensedextralight|italic":"RobotoSerif120ptCondensed-ExtraLightItalic","robotoserif120ptcondensedextralight|normal":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensedextralight|regular":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensedextralight|roman":"RobotoSerif120ptCondensed-ExtraLight","robotoserif120ptcondensedlight|book":"RobotoSerif120ptCondensed-Light","robotoserif120ptcondensedlight|italic":"RobotoSerif120ptCondensed-LightItalic","robotoserif120ptcondensedlight|normal":"RobotoSerif120ptCondensed-Light","robotoserif120ptcondensedlight|regular":"RobotoSerif120ptCondensed-Light","robotoserif120ptcondensedlight|roman":"RobotoSerif120ptCondensed-Light","robotoserif120ptcondensedmedium|book":"RobotoSerif120ptCondensed-Medium","robotoserif120ptcondensedmedium|italic":"RobotoSerif120ptCondensed-MediumItalic","robotoserif120ptcondensedmedium|normal":"RobotoSerif120ptCondensed-Medium","robotoserif120ptcondensedmedium|regular":"RobotoSerif120ptCondensed-Medium","robotoserif120ptcondensedmedium|roman":"RobotoSerif120ptCondensed-Medium","robotoserif120ptcondensedsemibold|book":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensedsemibold|italic":"RobotoSerif120ptCondensed-SemiBoldItalic","robotoserif120ptcondensedsemibold|normal":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensedsemibold|regular":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensedsemibold|roman":"RobotoSerif120ptCondensed-SemiBold","robotoserif120ptcondensedthin|book":"RobotoSerif120ptCondensed-Thin","robotoserif120ptcondensedthin|italic":"RobotoSerif120ptCondensed-ThinItalic","robotoserif120ptcondensedthin|normal":"RobotoSerif120ptCondensed-Thin","robotoserif120ptcondensedthin|regular":"RobotoSerif120ptCondensed-Thin","robotoserif120ptcondensedthin|roman":"RobotoSerif120ptCondensed-Thin","robotoserif120ptexpanded|black":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpanded|blackitalic":"RobotoSerif120ptExpanded-BlackItalic","robotoserif120ptexpanded|bold":"RobotoSerif120ptExpanded-Bold","robotoserif120ptexpanded|bolditalic":"RobotoSerif120ptExpanded-BoldItalic","robotoserif120ptexpanded|book":"RobotoSerif120ptExpanded-Regular","robotoserif120ptexpanded|demi":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpanded|demibold":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpanded|extrabold":"RobotoSerif120ptExpanded-ExtraBold","robotoserif120ptexpanded|extrabolditalic":"RobotoSerif120ptExpanded-ExtraBoldItalic","robotoserif120ptexpanded|extralight":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpanded|extralightitalic":"RobotoSerif120ptExpanded-ExtraLightItalic","robotoserif120ptexpanded|heavy":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpanded|italic":"RobotoSerif120ptExpanded-Italic","robotoserif120ptexpanded|light":"RobotoSerif120ptExpanded-Light","robotoserif120ptexpanded|lightitalic":"RobotoSerif120ptExpanded-LightItalic","robotoserif120ptexpanded|medium":"RobotoSerif120ptExpanded-Medium","robotoserif120ptexpanded|mediumitalic":"RobotoSerif120ptExpanded-MediumItalic","robotoserif120ptexpanded|normal":"RobotoSerif120ptExpanded-Regular","robotoserif120ptexpanded|regular":"RobotoSerif120ptExpanded-Regular","robotoserif120ptexpanded|roman":"RobotoSerif120ptExpanded-Regular","robotoserif120ptexpanded|semibold":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpanded|semibolditalic":"RobotoSerif120ptExpanded-SemiBoldItalic","robotoserif120ptexpanded|thin":"RobotoSerif120ptExpanded-Thin","robotoserif120ptexpanded|thinitalic":"RobotoSerif120ptExpanded-ThinItalic","robotoserif120ptexpanded|ultralight":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpandedblack|book":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpandedblack|italic":"RobotoSerif120ptExpanded-BlackItalic","robotoserif120ptexpandedblack|normal":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpandedblack|regular":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpandedblack|roman":"RobotoSerif120ptExpanded-Black","robotoserif120ptexpandedextrabold|book":"RobotoSerif120ptExpanded-ExtraBold","robotoserif120ptexpandedextrabold|italic":"RobotoSerif120ptExpanded-ExtraBoldItalic","robotoserif120ptexpandedextrabold|normal":"RobotoSerif120ptExpanded-ExtraBold","robotoserif120ptexpandedextrabold|regular":"RobotoSerif120ptExpanded-ExtraBold","robotoserif120ptexpandedextrabold|roman":"RobotoSerif120ptExpanded-ExtraBold","robotoserif120ptexpandedextralight|book":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpandedextralight|italic":"RobotoSerif120ptExpanded-ExtraLightItalic","robotoserif120ptexpandedextralight|normal":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpandedextralight|regular":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpandedextralight|roman":"RobotoSerif120ptExpanded-ExtraLight","robotoserif120ptexpandedlight|book":"RobotoSerif120ptExpanded-Light","robotoserif120ptexpandedlight|italic":"RobotoSerif120ptExpanded-LightItalic","robotoserif120ptexpandedlight|normal":"RobotoSerif120ptExpanded-Light","robotoserif120ptexpandedlight|regular":"RobotoSerif120ptExpanded-Light","robotoserif120ptexpandedlight|roman":"RobotoSerif120ptExpanded-Light","robotoserif120ptexpandedmedium|book":"RobotoSerif120ptExpanded-Medium","robotoserif120ptexpandedmedium|italic":"RobotoSerif120ptExpanded-MediumItalic","robotoserif120ptexpandedmedium|normal":"RobotoSerif120ptExpanded-Medium","robotoserif120ptexpandedmedium|regular":"RobotoSerif120ptExpanded-Medium","robotoserif120ptexpandedmedium|roman":"RobotoSerif120ptExpanded-Medium","robotoserif120ptexpandedsemibold|book":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpandedsemibold|italic":"RobotoSerif120ptExpanded-SemiBoldItalic","robotoserif120ptexpandedsemibold|normal":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpandedsemibold|regular":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpandedsemibold|roman":"RobotoSerif120ptExpanded-SemiBold","robotoserif120ptexpandedthin|book":"RobotoSerif120ptExpanded-Thin","robotoserif120ptexpandedthin|italic":"RobotoSerif120ptExpanded-ThinItalic","robotoserif120ptexpandedthin|normal":"RobotoSerif120ptExpanded-Thin","robotoserif120ptexpandedthin|regular":"RobotoSerif120ptExpanded-Thin","robotoserif120ptexpandedthin|roman":"RobotoSerif120ptExpanded-Thin","robotoserif120ptextrabold|book":"RobotoSerif120pt-ExtraBold","robotoserif120ptextrabold|italic":"RobotoSerif120pt-ExtraBoldItalic","robotoserif120ptextrabold|normal":"RobotoSerif120pt-ExtraBold","robotoserif120ptextrabold|regular":"RobotoSerif120pt-ExtraBold","robotoserif120ptextrabold|roman":"RobotoSerif120pt-ExtraBold","robotoserif120ptextracondensed|black":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensed|blackitalic":"RobotoSerif120ptExtraCondensed-BlackItalic","robotoserif120ptextracondensed|bold":"RobotoSerif120ptExtraCondensed-Bold","robotoserif120ptextracondensed|bolditalic":"RobotoSerif120ptExtraCondensed-BoldItalic","robotoserif120ptextracondensed|book":"RobotoSerif120ptExtraCondensed-Regular","robotoserif120ptextracondensed|demi":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensed|demibold":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensed|extrabold":"RobotoSerif120ptExtraCondensed-ExtraBold","robotoserif120ptextracondensed|extrabolditalic":"RobotoSerif120ptExtraCondensed-ExtraBoldItalic","robotoserif120ptextracondensed|extralight":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensed|extralightitalic":"RobotoSerif120ptExtraCondensed-ExtraLightItalic","robotoserif120ptextracondensed|heavy":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensed|italic":"RobotoSerif120ptExtraCondensed-Italic","robotoserif120ptextracondensed|light":"RobotoSerif120ptExtraCondensed-Light","robotoserif120ptextracondensed|lightitalic":"RobotoSerif120ptExtraCondensed-LightItalic","robotoserif120ptextracondensed|medium":"RobotoSerif120ptExtraCondensed-Medium","robotoserif120ptextracondensed|mediumitalic":"RobotoSerif120ptExtraCondensed-MediumItalic","robotoserif120ptextracondensed|normal":"RobotoSerif120ptExtraCondensed-Regular","robotoserif120ptextracondensed|regular":"RobotoSerif120ptExtraCondensed-Regular","robotoserif120ptextracondensed|roman":"RobotoSerif120ptExtraCondensed-Regular","robotoserif120ptextracondensed|semibold":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensed|semibolditalic":"RobotoSerif120ptExtraCondensed-SemiBoldItalic","robotoserif120ptextracondensed|thin":"RobotoSerif120ptExtraCondensed-Thin","robotoserif120ptextracondensed|thinitalic":"RobotoSerif120ptExtraCondensed-ThinItalic","robotoserif120ptextracondensed|ultralight":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensedblack|book":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensedblack|italic":"RobotoSerif120ptExtraCondensed-BlackItalic","robotoserif120ptextracondensedblack|normal":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensedblack|regular":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensedblack|roman":"RobotoSerif120ptExtraCondensed-Black","robotoserif120ptextracondensedextrabold|book":"RobotoSerif120ptExtraCondensed-ExtraBold","robotoserif120ptextracondensedextrabold|italic":"RobotoSerif120ptExtraCondensed-ExtraBoldItalic","robotoserif120ptextracondensedextrabold|normal":"RobotoSerif120ptExtraCondensed-ExtraBold","robotoserif120ptextracondensedextrabold|regular":"RobotoSerif120ptExtraCondensed-ExtraBold","robotoserif120ptextracondensedextrabold|roman":"RobotoSerif120ptExtraCondensed-ExtraBold","robotoserif120ptextracondensedextralight|book":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensedextralight|italic":"RobotoSerif120ptExtraCondensed-ExtraLightItalic","robotoserif120ptextracondensedextralight|normal":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensedextralight|regular":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensedextralight|roman":"RobotoSerif120ptExtraCondensed-ExtraLight","robotoserif120ptextracondensedlight|book":"RobotoSerif120ptExtraCondensed-Light","robotoserif120ptextracondensedlight|italic":"RobotoSerif120ptExtraCondensed-LightItalic","robotoserif120ptextracondensedlight|normal":"RobotoSerif120ptExtraCondensed-Light","robotoserif120ptextracondensedlight|regular":"RobotoSerif120ptExtraCondensed-Light","robotoserif120ptextracondensedlight|roman":"RobotoSerif120ptExtraCondensed-Light","robotoserif120ptextracondensedmedium|book":"RobotoSerif120ptExtraCondensed-Medium","robotoserif120ptextracondensedmedium|italic":"RobotoSerif120ptExtraCondensed-MediumItalic","robotoserif120ptextracondensedmedium|normal":"RobotoSerif120ptExtraCondensed-Medium","robotoserif120ptextracondensedmedium|regular":"RobotoSerif120ptExtraCondensed-Medium","robotoserif120ptextracondensedmedium|roman":"RobotoSerif120ptExtraCondensed-Medium","robotoserif120ptextracondensedsemibold|book":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensedsemibold|italic":"RobotoSerif120ptExtraCondensed-SemiBoldItalic","robotoserif120ptextracondensedsemibold|normal":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensedsemibold|regular":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensedsemibold|roman":"RobotoSerif120ptExtraCondensed-SemiBold","robotoserif120ptextracondensedthin|book":"RobotoSerif120ptExtraCondensed-Thin","robotoserif120ptextracondensedthin|italic":"RobotoSerif120ptExtraCondensed-ThinItalic","robotoserif120ptextracondensedthin|normal":"RobotoSerif120ptExtraCondensed-Thin","robotoserif120ptextracondensedthin|regular":"RobotoSerif120ptExtraCondensed-Thin","robotoserif120ptextracondensedthin|roman":"RobotoSerif120ptExtraCondensed-Thin","robotoserif120ptextraexpanded|black":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpanded|blackitalic":"RobotoSerif120ptExtraExpanded-BlackItalic","robotoserif120ptextraexpanded|bold":"RobotoSerif120ptExtraExpanded-Bold","robotoserif120ptextraexpanded|bolditalic":"RobotoSerif120ptExtraExpanded-BoldItalic","robotoserif120ptextraexpanded|book":"RobotoSerif120ptExtraExpanded-Regular","robotoserif120ptextraexpanded|demi":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpanded|demibold":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpanded|extrabold":"RobotoSerif120ptExtraExpanded-ExtraBold","robotoserif120ptextraexpanded|extrabolditalic":"RobotoSerif120ptExtraExpanded-ExtraBoldItalic","robotoserif120ptextraexpanded|extralight":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpanded|extralightitalic":"RobotoSerif120ptExtraExpanded-ExtraLightItalic","robotoserif120ptextraexpanded|heavy":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpanded|italic":"RobotoSerif120ptExtraExpanded-Italic","robotoserif120ptextraexpanded|light":"RobotoSerif120ptExtraExpanded-Light","robotoserif120ptextraexpanded|lightitalic":"RobotoSerif120ptExtraExpanded-LightItalic","robotoserif120ptextraexpanded|medium":"RobotoSerif120ptExtraExpanded-Medium","robotoserif120ptextraexpanded|mediumitalic":"RobotoSerif120ptExtraExpanded-MediumItalic","robotoserif120ptextraexpanded|normal":"RobotoSerif120ptExtraExpanded-Regular","robotoserif120ptextraexpanded|regular":"RobotoSerif120ptExtraExpanded-Regular","robotoserif120ptextraexpanded|roman":"RobotoSerif120ptExtraExpanded-Regular","robotoserif120ptextraexpanded|semibold":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpanded|semibolditalic":"RobotoSerif120ptExtraExpanded-SemiBoldItalic","robotoserif120ptextraexpanded|thin":"RobotoSerif120ptExtraExpanded-Thin","robotoserif120ptextraexpanded|thinitalic":"RobotoSerif120ptExtraExpanded-ThinItalic","robotoserif120ptextraexpanded|ultralight":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpandedblack|book":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpandedblack|italic":"RobotoSerif120ptExtraExpanded-BlackItalic","robotoserif120ptextraexpandedblack|normal":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpandedblack|regular":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpandedblack|roman":"RobotoSerif120ptExtraExpanded-Black","robotoserif120ptextraexpandedextrabold|book":"RobotoSerif120ptExtraExpanded-ExtraBold","robotoserif120ptextraexpandedextrabold|italic":"RobotoSerif120ptExtraExpanded-ExtraBoldItalic","robotoserif120ptextraexpandedextrabold|normal":"RobotoSerif120ptExtraExpanded-ExtraBold","robotoserif120ptextraexpandedextrabold|regular":"RobotoSerif120ptExtraExpanded-ExtraBold","robotoserif120ptextraexpandedextrabold|roman":"RobotoSerif120ptExtraExpanded-ExtraBold","robotoserif120ptextraexpandedextralight|book":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpandedextralight|italic":"RobotoSerif120ptExtraExpanded-ExtraLightItalic","robotoserif120ptextraexpandedextralight|normal":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpandedextralight|regular":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpandedextralight|roman":"RobotoSerif120ptExtraExpanded-ExtraLight","robotoserif120ptextraexpandedlight|book":"RobotoSerif120ptExtraExpanded-Light","robotoserif120ptextraexpandedlight|italic":"RobotoSerif120ptExtraExpanded-LightItalic","robotoserif120ptextraexpandedlight|normal":"RobotoSerif120ptExtraExpanded-Light","robotoserif120ptextraexpandedlight|regular":"RobotoSerif120ptExtraExpanded-Light","robotoserif120ptextraexpandedlight|roman":"RobotoSerif120ptExtraExpanded-Light","robotoserif120ptextraexpandedmedium|book":"RobotoSerif120ptExtraExpanded-Medium","robotoserif120ptextraexpandedmedium|italic":"RobotoSerif120ptExtraExpanded-MediumItalic","robotoserif120ptextraexpandedmedium|normal":"RobotoSerif120ptExtraExpanded-Medium","robotoserif120ptextraexpandedmedium|regular":"RobotoSerif120ptExtraExpanded-Medium","robotoserif120ptextraexpandedmedium|roman":"RobotoSerif120ptExtraExpanded-Medium","robotoserif120ptextraexpandedsemibold|book":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpandedsemibold|italic":"RobotoSerif120ptExtraExpanded-SemiBoldItalic","robotoserif120ptextraexpandedsemibold|normal":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpandedsemibold|regular":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpandedsemibold|roman":"RobotoSerif120ptExtraExpanded-SemiBold","robotoserif120ptextraexpandedthin|book":"RobotoSerif120ptExtraExpanded-Thin","robotoserif120ptextraexpandedthin|italic":"RobotoSerif120ptExtraExpanded-ThinItalic","robotoserif120ptextraexpandedthin|normal":"RobotoSerif120ptExtraExpanded-Thin","robotoserif120ptextraexpandedthin|regular":"RobotoSerif120ptExtraExpanded-Thin","robotoserif120ptextraexpandedthin|roman":"RobotoSerif120ptExtraExpanded-Thin","robotoserif120ptextralight|book":"RobotoSerif120pt-ExtraLight","robotoserif120ptextralight|italic":"RobotoSerif120pt-ExtraLightItalic","robotoserif120ptextralight|normal":"RobotoSerif120pt-ExtraLight","robotoserif120ptextralight|regular":"RobotoSerif120pt-ExtraLight","robotoserif120ptextralight|roman":"RobotoSerif120pt-ExtraLight","robotoserif120ptlight|book":"RobotoSerif120pt-Light","robotoserif120ptlight|italic":"RobotoSerif120pt-LightItalic","robotoserif120ptlight|normal":"RobotoSerif120pt-Light","robotoserif120ptlight|regular":"RobotoSerif120pt-Light","robotoserif120ptlight|roman":"RobotoSerif120pt-Light","robotoserif120ptmedium|book":"RobotoSerif120pt-Medium","robotoserif120ptmedium|italic":"RobotoSerif120pt-MediumItalic","robotoserif120ptmedium|normal":"RobotoSerif120pt-Medium","robotoserif120ptmedium|regular":"RobotoSerif120pt-Medium","robotoserif120ptmedium|roman":"RobotoSerif120pt-Medium","robotoserif120ptsemibold|book":"RobotoSerif120pt-SemiBold","robotoserif120ptsemibold|italic":"RobotoSerif120pt-SemiBoldItalic","robotoserif120ptsemibold|normal":"RobotoSerif120pt-SemiBold","robotoserif120ptsemibold|regular":"RobotoSerif120pt-SemiBold","robotoserif120ptsemibold|roman":"RobotoSerif120pt-SemiBold","robotoserif120ptsemicondensed|black":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensed|blackitalic":"RobotoSerif120ptSemiCondensed-BlackItalic","robotoserif120ptsemicondensed|bold":"RobotoSerif120ptSemiCondensed-Bold","robotoserif120ptsemicondensed|bolditalic":"RobotoSerif120ptSemiCondensed-BoldItalic","robotoserif120ptsemicondensed|book":"RobotoSerif120ptSemiCondensed-Regular","robotoserif120ptsemicondensed|demi":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensed|demibold":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensed|extrabold":"RobotoSerif120ptSemiCondensed-ExtraBold","robotoserif120ptsemicondensed|extrabolditalic":"RobotoSerif120ptSemiCondensed-ExtraBoldItalic","robotoserif120ptsemicondensed|extralight":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensed|extralightitalic":"RobotoSerif120ptSemiCondensed-ExtraLightItalic","robotoserif120ptsemicondensed|heavy":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensed|italic":"RobotoSerif120ptSemiCondensed-Italic","robotoserif120ptsemicondensed|light":"RobotoSerif120ptSemiCondensed-Light","robotoserif120ptsemicondensed|lightitalic":"RobotoSerif120ptSemiCondensed-LightItalic","robotoserif120ptsemicondensed|medium":"RobotoSerif120ptSemiCondensed-Medium","robotoserif120ptsemicondensed|mediumitalic":"RobotoSerif120ptSemiCondensed-MediumItalic","robotoserif120ptsemicondensed|normal":"RobotoSerif120ptSemiCondensed-Regular","robotoserif120ptsemicondensed|regular":"RobotoSerif120ptSemiCondensed-Regular","robotoserif120ptsemicondensed|roman":"RobotoSerif120ptSemiCondensed-Regular","robotoserif120ptsemicondensed|semibold":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensed|semibolditalic":"RobotoSerif120ptSemiCondensed-SemiBoldItalic","robotoserif120ptsemicondensed|thin":"RobotoSerif120ptSemiCondensed-Thin","robotoserif120ptsemicondensed|thinitalic":"RobotoSerif120ptSemiCondensed-ThinItalic","robotoserif120ptsemicondensed|ultralight":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensedblack|book":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensedblack|italic":"RobotoSerif120ptSemiCondensed-BlackItalic","robotoserif120ptsemicondensedblack|normal":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensedblack|regular":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensedblack|roman":"RobotoSerif120ptSemiCondensed-Black","robotoserif120ptsemicondensedextrabold|book":"RobotoSerif120ptSemiCondensed-ExtraBold","robotoserif120ptsemicondensedextrabold|italic":"RobotoSerif120ptSemiCondensed-ExtraBoldItalic","robotoserif120ptsemicondensedextrabold|normal":"RobotoSerif120ptSemiCondensed-ExtraBold","robotoserif120ptsemicondensedextrabold|regular":"RobotoSerif120ptSemiCondensed-ExtraBold","robotoserif120ptsemicondensedextrabold|roman":"RobotoSerif120ptSemiCondensed-ExtraBold","robotoserif120ptsemicondensedextralight|book":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensedextralight|italic":"RobotoSerif120ptSemiCondensed-ExtraLightItalic","robotoserif120ptsemicondensedextralight|normal":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensedextralight|regular":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensedextralight|roman":"RobotoSerif120ptSemiCondensed-ExtraLight","robotoserif120ptsemicondensedlight|book":"RobotoSerif120ptSemiCondensed-Light","robotoserif120ptsemicondensedlight|italic":"RobotoSerif120ptSemiCondensed-LightItalic","robotoserif120ptsemicondensedlight|normal":"RobotoSerif120ptSemiCondensed-Light","robotoserif120ptsemicondensedlight|regular":"RobotoSerif120ptSemiCondensed-Light","robotoserif120ptsemicondensedlight|roman":"RobotoSerif120ptSemiCondensed-Light","robotoserif120ptsemicondensedmedium|book":"RobotoSerif120ptSemiCondensed-Medium","robotoserif120ptsemicondensedmedium|italic":"RobotoSerif120ptSemiCondensed-MediumItalic","robotoserif120ptsemicondensedmedium|normal":"RobotoSerif120ptSemiCondensed-Medium","robotoserif120ptsemicondensedmedium|regular":"RobotoSerif120ptSemiCondensed-Medium","robotoserif120ptsemicondensedmedium|roman":"RobotoSerif120ptSemiCondensed-Medium","robotoserif120ptsemicondensedsemibold|book":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensedsemibold|italic":"RobotoSerif120ptSemiCondensed-SemiBoldItalic","robotoserif120ptsemicondensedsemibold|normal":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensedsemibold|regular":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensedsemibold|roman":"RobotoSerif120ptSemiCondensed-SemiBold","robotoserif120ptsemicondensedthin|book":"RobotoSerif120ptSemiCondensed-Thin","robotoserif120ptsemicondensedthin|italic":"RobotoSerif120ptSemiCondensed-ThinItalic","robotoserif120ptsemicondensedthin|normal":"RobotoSerif120ptSemiCondensed-Thin","robotoserif120ptsemicondensedthin|regular":"RobotoSerif120ptSemiCondensed-Thin","robotoserif120ptsemicondensedthin|roman":"RobotoSerif120ptSemiCondensed-Thin","robotoserif120ptsemiexpanded|black":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpanded|blackitalic":"RobotoSerif120ptSemiExpanded-BlackItalic","robotoserif120ptsemiexpanded|bold":"RobotoSerif120ptSemiExpanded-Bold","robotoserif120ptsemiexpanded|bolditalic":"RobotoSerif120ptSemiExpanded-BoldItalic","robotoserif120ptsemiexpanded|book":"RobotoSerif120ptSemiExpanded-Regular","robotoserif120ptsemiexpanded|demi":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpanded|demibold":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpanded|extrabold":"RobotoSerif120ptSemiExpanded-ExtraBold","robotoserif120ptsemiexpanded|extrabolditalic":"RobotoSerif120ptSemiExpanded-ExtraBoldItalic","robotoserif120ptsemiexpanded|extralight":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpanded|extralightitalic":"RobotoSerif120ptSemiExpanded-ExtraLightItalic","robotoserif120ptsemiexpanded|heavy":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpanded|italic":"RobotoSerif120ptSemiExpanded-Italic","robotoserif120ptsemiexpanded|light":"RobotoSerif120ptSemiExpanded-Light","robotoserif120ptsemiexpanded|lightitalic":"RobotoSerif120ptSemiExpanded-LightItalic","robotoserif120ptsemiexpanded|medium":"RobotoSerif120ptSemiExpanded-Medium","robotoserif120ptsemiexpanded|mediumitalic":"RobotoSerif120ptSemiExpanded-MediumItalic","robotoserif120ptsemiexpanded|normal":"RobotoSerif120ptSemiExpanded-Regular","robotoserif120ptsemiexpanded|regular":"RobotoSerif120ptSemiExpanded-Regular","robotoserif120ptsemiexpanded|roman":"RobotoSerif120ptSemiExpanded-Regular","robotoserif120ptsemiexpanded|semibold":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpanded|semibolditalic":"RobotoSerif120ptSemiExpanded-SemiBoldItalic","robotoserif120ptsemiexpanded|thin":"RobotoSerif120ptSemiExpanded-Thin","robotoserif120ptsemiexpanded|thinitalic":"RobotoSerif120ptSemiExpanded-ThinItalic","robotoserif120ptsemiexpanded|ultralight":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpandedblack|book":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpandedblack|italic":"RobotoSerif120ptSemiExpanded-BlackItalic","robotoserif120ptsemiexpandedblack|normal":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpandedblack|regular":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpandedblack|roman":"RobotoSerif120ptSemiExpanded-Black","robotoserif120ptsemiexpandedextrabold|book":"RobotoSerif120ptSemiExpanded-ExtraBold","robotoserif120ptsemiexpandedextrabold|italic":"RobotoSerif120ptSemiExpanded-ExtraBoldItalic","robotoserif120ptsemiexpandedextrabold|normal":"RobotoSerif120ptSemiExpanded-ExtraBold","robotoserif120ptsemiexpandedextrabold|regular":"RobotoSerif120ptSemiExpanded-ExtraBold","robotoserif120ptsemiexpandedextrabold|roman":"RobotoSerif120ptSemiExpanded-ExtraBold","robotoserif120ptsemiexpandedextralight|book":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpandedextralight|italic":"RobotoSerif120ptSemiExpanded-ExtraLightItalic","robotoserif120ptsemiexpandedextralight|normal":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpandedextralight|regular":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpandedextralight|roman":"RobotoSerif120ptSemiExpanded-ExtraLight","robotoserif120ptsemiexpandedlight|book":"RobotoSerif120ptSemiExpanded-Light","robotoserif120ptsemiexpandedlight|italic":"RobotoSerif120ptSemiExpanded-LightItalic","robotoserif120ptsemiexpandedlight|normal":"RobotoSerif120ptSemiExpanded-Light","robotoserif120ptsemiexpandedlight|regular":"RobotoSerif120ptSemiExpanded-Light","robotoserif120ptsemiexpandedlight|roman":"RobotoSerif120ptSemiExpanded-Light","robotoserif120ptsemiexpandedmedium|book":"RobotoSerif120ptSemiExpanded-Medium","robotoserif120ptsemiexpandedmedium|italic":"RobotoSerif120ptSemiExpanded-MediumItalic","robotoserif120ptsemiexpandedmedium|normal":"RobotoSerif120ptSemiExpanded-Medium","robotoserif120ptsemiexpandedmedium|regular":"RobotoSerif120ptSemiExpanded-Medium","robotoserif120ptsemiexpandedmedium|roman":"RobotoSerif120ptSemiExpanded-Medium","robotoserif120ptsemiexpandedsemibold|book":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpandedsemibold|italic":"RobotoSerif120ptSemiExpanded-SemiBoldItalic","robotoserif120ptsemiexpandedsemibold|normal":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpandedsemibold|regular":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpandedsemibold|roman":"RobotoSerif120ptSemiExpanded-SemiBold","robotoserif120ptsemiexpandedthin|book":"RobotoSerif120ptSemiExpanded-Thin","robotoserif120ptsemiexpandedthin|italic":"RobotoSerif120ptSemiExpanded-ThinItalic","robotoserif120ptsemiexpandedthin|normal":"RobotoSerif120ptSemiExpanded-Thin","robotoserif120ptsemiexpandedthin|regular":"RobotoSerif120ptSemiExpanded-Thin","robotoserif120ptsemiexpandedthin|roman":"RobotoSerif120ptSemiExpanded-Thin","robotoserif120ptthin|book":"RobotoSerif120pt-Thin","robotoserif120ptthin|italic":"RobotoSerif120pt-ThinItalic","robotoserif120ptthin|normal":"RobotoSerif120pt-Thin","robotoserif120ptthin|regular":"RobotoSerif120pt-Thin","robotoserif120ptthin|roman":"RobotoSerif120pt-Thin","robotoserif120ptultracondensed|black":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensed|blackitalic":"RobotoSerif120ptUltraCondensed-BlackItalic","robotoserif120ptultracondensed|bold":"RobotoSerif120ptUltraCondensed-Bold","robotoserif120ptultracondensed|bolditalic":"RobotoSerif120ptUltraCondensed-BoldItalic","robotoserif120ptultracondensed|book":"RobotoSerif120ptUltraCondensed-Regular","robotoserif120ptultracondensed|demi":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensed|demibold":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensed|extrabold":"RobotoSerif120ptUltraCondensed-ExtraBold","robotoserif120ptultracondensed|extrabolditalic":"RobotoSerif120ptUltraCondensed-ExtraBoldItalic","robotoserif120ptultracondensed|extralight":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensed|extralightitalic":"RobotoSerif120ptUltraCondensed-ExtraLightItalic","robotoserif120ptultracondensed|heavy":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensed|italic":"RobotoSerif120ptUltraCondensed-Italic","robotoserif120ptultracondensed|light":"RobotoSerif120ptUltraCondensed-Light","robotoserif120ptultracondensed|lightitalic":"RobotoSerif120ptUltraCondensed-LightItalic","robotoserif120ptultracondensed|medium":"RobotoSerif120ptUltraCondensed-Medium","robotoserif120ptultracondensed|mediumitalic":"RobotoSerif120ptUltraCondensed-MediumItalic","robotoserif120ptultracondensed|normal":"RobotoSerif120ptUltraCondensed-Regular","robotoserif120ptultracondensed|regular":"RobotoSerif120ptUltraCondensed-Regular","robotoserif120ptultracondensed|roman":"RobotoSerif120ptUltraCondensed-Regular","robotoserif120ptultracondensed|semibold":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensed|semibolditalic":"RobotoSerif120ptUltraCondensed-SemiBoldItalic","robotoserif120ptultracondensed|thin":"RobotoSerif120ptUltraCondensed-Thin","robotoserif120ptultracondensed|thinitalic":"RobotoSerif120ptUltraCondensed-ThinItalic","robotoserif120ptultracondensed|ultralight":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensedblack|book":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensedblack|italic":"RobotoSerif120ptUltraCondensed-BlackItalic","robotoserif120ptultracondensedblack|normal":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensedblack|regular":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensedblack|roman":"RobotoSerif120ptUltraCondensed-Black","robotoserif120ptultracondensedextrabold|book":"RobotoSerif120ptUltraCondensed-ExtraBold","robotoserif120ptultracondensedextrabold|italic":"RobotoSerif120ptUltraCondensed-ExtraBoldItalic","robotoserif120ptultracondensedextrabold|normal":"RobotoSerif120ptUltraCondensed-ExtraBold","robotoserif120ptultracondensedextrabold|regular":"RobotoSerif120ptUltraCondensed-ExtraBold","robotoserif120ptultracondensedextrabold|roman":"RobotoSerif120ptUltraCondensed-ExtraBold","robotoserif120ptultracondensedextralight|book":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensedextralight|italic":"RobotoSerif120ptUltraCondensed-ExtraLightItalic","robotoserif120ptultracondensedextralight|normal":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensedextralight|regular":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensedextralight|roman":"RobotoSerif120ptUltraCondensed-ExtraLight","robotoserif120ptultracondensedlight|book":"RobotoSerif120ptUltraCondensed-Light","robotoserif120ptultracondensedlight|italic":"RobotoSerif120ptUltraCondensed-LightItalic","robotoserif120ptultracondensedlight|normal":"RobotoSerif120ptUltraCondensed-Light","robotoserif120ptultracondensedlight|regular":"RobotoSerif120ptUltraCondensed-Light","robotoserif120ptultracondensedlight|roman":"RobotoSerif120ptUltraCondensed-Light","robotoserif120ptultracondensedmedium|book":"RobotoSerif120ptUltraCondensed-Medium","robotoserif120ptultracondensedmedium|italic":"RobotoSerif120ptUltraCondensed-MediumItalic","robotoserif120ptultracondensedmedium|normal":"RobotoSerif120ptUltraCondensed-Medium","robotoserif120ptultracondensedmedium|regular":"RobotoSerif120ptUltraCondensed-Medium","robotoserif120ptultracondensedmedium|roman":"RobotoSerif120ptUltraCondensed-Medium","robotoserif120ptultracondensedsemibold|book":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensedsemibold|italic":"RobotoSerif120ptUltraCondensed-SemiBoldItalic","robotoserif120ptultracondensedsemibold|normal":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensedsemibold|regular":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensedsemibold|roman":"RobotoSerif120ptUltraCondensed-SemiBold","robotoserif120ptultracondensedthin|book":"RobotoSerif120ptUltraCondensed-Thin","robotoserif120ptultracondensedthin|italic":"RobotoSerif120ptUltraCondensed-ThinItalic","robotoserif120ptultracondensedthin|normal":"RobotoSerif120ptUltraCondensed-Thin","robotoserif120ptultracondensedthin|regular":"RobotoSerif120ptUltraCondensed-Thin","robotoserif120ptultracondensedthin|roman":"RobotoSerif120ptUltraCondensed-Thin","robotoserif20pt|book":"RobotoSerif-20ptRegular","robotoserif20pt|italic":"RobotoSerif-20ptItalic","robotoserif20pt|normal":"RobotoSerif-20ptRegular","robotoserif20pt|regular":"RobotoSerif-20ptRegular","robotoserif20pt|roman":"RobotoSerif-20ptRegular","robotoserif28pt|black":"RobotoSerif28pt-Black","robotoserif28pt|blackitalic":"RobotoSerif28pt-BlackItalic","robotoserif28pt|bold":"RobotoSerif28pt-Bold","robotoserif28pt|bolditalic":"RobotoSerif28pt-BoldItalic","robotoserif28pt|book":"RobotoSerif28pt-Regular","robotoserif28pt|demi":"RobotoSerif28pt-SemiBold","robotoserif28pt|demibold":"RobotoSerif28pt-SemiBold","robotoserif28pt|extrabold":"RobotoSerif28pt-ExtraBold","robotoserif28pt|extrabolditalic":"RobotoSerif28pt-ExtraBoldItalic","robotoserif28pt|extralight":"RobotoSerif28pt-ExtraLight","robotoserif28pt|extralightitalic":"RobotoSerif28pt-ExtraLightItalic","robotoserif28pt|heavy":"RobotoSerif28pt-Black","robotoserif28pt|italic":"RobotoSerif28pt-Italic","robotoserif28pt|light":"RobotoSerif28pt-Light","robotoserif28pt|lightitalic":"RobotoSerif28pt-LightItalic","robotoserif28pt|medium":"RobotoSerif28pt-Medium","robotoserif28pt|mediumitalic":"RobotoSerif28pt-MediumItalic","robotoserif28pt|normal":"RobotoSerif28pt-Regular","robotoserif28pt|regular":"RobotoSerif28pt-Regular","robotoserif28pt|roman":"RobotoSerif28pt-Regular","robotoserif28pt|semibold":"RobotoSerif28pt-SemiBold","robotoserif28pt|semibolditalic":"RobotoSerif28pt-SemiBoldItalic","robotoserif28pt|thin":"RobotoSerif28pt-Thin","robotoserif28pt|thinitalic":"RobotoSerif28pt-ThinItalic","robotoserif28pt|ultralight":"RobotoSerif28pt-ExtraLight","robotoserif28ptblack|book":"RobotoSerif28pt-Black","robotoserif28ptblack|italic":"RobotoSerif28pt-BlackItalic","robotoserif28ptblack|normal":"RobotoSerif28pt-Black","robotoserif28ptblack|regular":"RobotoSerif28pt-Black","robotoserif28ptblack|roman":"RobotoSerif28pt-Black","robotoserif28ptcondensed|black":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensed|blackitalic":"RobotoSerif28ptCondensed-BlackItalic","robotoserif28ptcondensed|bold":"RobotoSerif28ptCondensed-Bold","robotoserif28ptcondensed|bolditalic":"RobotoSerif28ptCondensed-BoldItalic","robotoserif28ptcondensed|book":"RobotoSerif28ptCondensed-Regular","robotoserif28ptcondensed|demi":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensed|demibold":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensed|extrabold":"RobotoSerif28ptCondensed-ExtraBold","robotoserif28ptcondensed|extrabolditalic":"RobotoSerif28ptCondensed-ExtraBoldItalic","robotoserif28ptcondensed|extralight":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensed|extralightitalic":"RobotoSerif28ptCondensed-ExtraLightItalic","robotoserif28ptcondensed|heavy":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensed|italic":"RobotoSerif28ptCondensed-Italic","robotoserif28ptcondensed|light":"RobotoSerif28ptCondensed-Light","robotoserif28ptcondensed|lightitalic":"RobotoSerif28ptCondensed-LightItalic","robotoserif28ptcondensed|medium":"RobotoSerif28ptCondensed-Medium","robotoserif28ptcondensed|mediumitalic":"RobotoSerif28ptCondensed-MediumItalic","robotoserif28ptcondensed|normal":"RobotoSerif28ptCondensed-Regular","robotoserif28ptcondensed|regular":"RobotoSerif28ptCondensed-Regular","robotoserif28ptcondensed|roman":"RobotoSerif28ptCondensed-Regular","robotoserif28ptcondensed|semibold":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensed|semibolditalic":"RobotoSerif28ptCondensed-SemiBoldItalic","robotoserif28ptcondensed|thin":"RobotoSerif28ptCondensed-Thin","robotoserif28ptcondensed|thinitalic":"RobotoSerif28ptCondensed-ThinItalic","robotoserif28ptcondensed|ultralight":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensedblack|book":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensedblack|italic":"RobotoSerif28ptCondensed-BlackItalic","robotoserif28ptcondensedblack|normal":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensedblack|regular":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensedblack|roman":"RobotoSerif28ptCondensed-Black","robotoserif28ptcondensedextrabold|book":"RobotoSerif28ptCondensed-ExtraBold","robotoserif28ptcondensedextrabold|italic":"RobotoSerif28ptCondensed-ExtraBoldItalic","robotoserif28ptcondensedextrabold|normal":"RobotoSerif28ptCondensed-ExtraBold","robotoserif28ptcondensedextrabold|regular":"RobotoSerif28ptCondensed-ExtraBold","robotoserif28ptcondensedextrabold|roman":"RobotoSerif28ptCondensed-ExtraBold","robotoserif28ptcondensedextralight|book":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensedextralight|italic":"RobotoSerif28ptCondensed-ExtraLightItalic","robotoserif28ptcondensedextralight|normal":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensedextralight|regular":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensedextralight|roman":"RobotoSerif28ptCondensed-ExtraLight","robotoserif28ptcondensedlight|book":"RobotoSerif28ptCondensed-Light","robotoserif28ptcondensedlight|italic":"RobotoSerif28ptCondensed-LightItalic","robotoserif28ptcondensedlight|normal":"RobotoSerif28ptCondensed-Light","robotoserif28ptcondensedlight|regular":"RobotoSerif28ptCondensed-Light","robotoserif28ptcondensedlight|roman":"RobotoSerif28ptCondensed-Light","robotoserif28ptcondensedmedium|book":"RobotoSerif28ptCondensed-Medium","robotoserif28ptcondensedmedium|italic":"RobotoSerif28ptCondensed-MediumItalic","robotoserif28ptcondensedmedium|normal":"RobotoSerif28ptCondensed-Medium","robotoserif28ptcondensedmedium|regular":"RobotoSerif28ptCondensed-Medium","robotoserif28ptcondensedmedium|roman":"RobotoSerif28ptCondensed-Medium","robotoserif28ptcondensedsemibold|book":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensedsemibold|italic":"RobotoSerif28ptCondensed-SemiBoldItalic","robotoserif28ptcondensedsemibold|normal":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensedsemibold|regular":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensedsemibold|roman":"RobotoSerif28ptCondensed-SemiBold","robotoserif28ptcondensedthin|book":"RobotoSerif28ptCondensed-Thin","robotoserif28ptcondensedthin|italic":"RobotoSerif28ptCondensed-ThinItalic","robotoserif28ptcondensedthin|normal":"RobotoSerif28ptCondensed-Thin","robotoserif28ptcondensedthin|regular":"RobotoSerif28ptCondensed-Thin","robotoserif28ptcondensedthin|roman":"RobotoSerif28ptCondensed-Thin","robotoserif28ptexpanded|black":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpanded|blackitalic":"RobotoSerif28ptExpanded-BlackItalic","robotoserif28ptexpanded|bold":"RobotoSerif28ptExpanded-Bold","robotoserif28ptexpanded|bolditalic":"RobotoSerif28ptExpanded-BoldItalic","robotoserif28ptexpanded|book":"RobotoSerif28ptExpanded-Regular","robotoserif28ptexpanded|demi":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpanded|demibold":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpanded|extrabold":"RobotoSerif28ptExpanded-ExtraBold","robotoserif28ptexpanded|extrabolditalic":"RobotoSerif28ptExpanded-ExtraBoldItalic","robotoserif28ptexpanded|extralight":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpanded|extralightitalic":"RobotoSerif28ptExpanded-ExtraLightItalic","robotoserif28ptexpanded|heavy":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpanded|italic":"RobotoSerif28ptExpanded-Italic","robotoserif28ptexpanded|light":"RobotoSerif28ptExpanded-Light","robotoserif28ptexpanded|lightitalic":"RobotoSerif28ptExpanded-LightItalic","robotoserif28ptexpanded|medium":"RobotoSerif28ptExpanded-Medium","robotoserif28ptexpanded|mediumitalic":"RobotoSerif28ptExpanded-MediumItalic","robotoserif28ptexpanded|normal":"RobotoSerif28ptExpanded-Regular","robotoserif28ptexpanded|regular":"RobotoSerif28ptExpanded-Regular","robotoserif28ptexpanded|roman":"RobotoSerif28ptExpanded-Regular","robotoserif28ptexpanded|semibold":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpanded|semibolditalic":"RobotoSerif28ptExpanded-SemiBoldItalic","robotoserif28ptexpanded|thin":"RobotoSerif28ptExpanded-Thin","robotoserif28ptexpanded|thinitalic":"RobotoSerif28ptExpanded-ThinItalic","robotoserif28ptexpanded|ultralight":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpandedblack|book":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpandedblack|italic":"RobotoSerif28ptExpanded-BlackItalic","robotoserif28ptexpandedblack|normal":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpandedblack|regular":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpandedblack|roman":"RobotoSerif28ptExpanded-Black","robotoserif28ptexpandedextrabold|book":"RobotoSerif28ptExpanded-ExtraBold","robotoserif28ptexpandedextrabold|italic":"RobotoSerif28ptExpanded-ExtraBoldItalic","robotoserif28ptexpandedextrabold|normal":"RobotoSerif28ptExpanded-ExtraBold","robotoserif28ptexpandedextrabold|regular":"RobotoSerif28ptExpanded-ExtraBold","robotoserif28ptexpandedextrabold|roman":"RobotoSerif28ptExpanded-ExtraBold","robotoserif28ptexpandedextralight|book":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpandedextralight|italic":"RobotoSerif28ptExpanded-ExtraLightItalic","robotoserif28ptexpandedextralight|normal":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpandedextralight|regular":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpandedextralight|roman":"RobotoSerif28ptExpanded-ExtraLight","robotoserif28ptexpandedlight|book":"RobotoSerif28ptExpanded-Light","robotoserif28ptexpandedlight|italic":"RobotoSerif28ptExpanded-LightItalic","robotoserif28ptexpandedlight|normal":"RobotoSerif28ptExpanded-Light","robotoserif28ptexpandedlight|regular":"RobotoSerif28ptExpanded-Light","robotoserif28ptexpandedlight|roman":"RobotoSerif28ptExpanded-Light","robotoserif28ptexpandedmedium|book":"RobotoSerif28ptExpanded-Medium","robotoserif28ptexpandedmedium|italic":"RobotoSerif28ptExpanded-MediumItalic","robotoserif28ptexpandedmedium|normal":"RobotoSerif28ptExpanded-Medium","robotoserif28ptexpandedmedium|regular":"RobotoSerif28ptExpanded-Medium","robotoserif28ptexpandedmedium|roman":"RobotoSerif28ptExpanded-Medium","robotoserif28ptexpandedsemibold|book":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpandedsemibold|italic":"RobotoSerif28ptExpanded-SemiBoldItalic","robotoserif28ptexpandedsemibold|normal":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpandedsemibold|regular":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpandedsemibold|roman":"RobotoSerif28ptExpanded-SemiBold","robotoserif28ptexpandedthin|book":"RobotoSerif28ptExpanded-Thin","robotoserif28ptexpandedthin|italic":"RobotoSerif28ptExpanded-ThinItalic","robotoserif28ptexpandedthin|normal":"RobotoSerif28ptExpanded-Thin","robotoserif28ptexpandedthin|regular":"RobotoSerif28ptExpanded-Thin","robotoserif28ptexpandedthin|roman":"RobotoSerif28ptExpanded-Thin","robotoserif28ptextrabold|book":"RobotoSerif28pt-ExtraBold","robotoserif28ptextrabold|italic":"RobotoSerif28pt-ExtraBoldItalic","robotoserif28ptextrabold|normal":"RobotoSerif28pt-ExtraBold","robotoserif28ptextrabold|regular":"RobotoSerif28pt-ExtraBold","robotoserif28ptextrabold|roman":"RobotoSerif28pt-ExtraBold","robotoserif28ptextracondensed|black":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensed|blackitalic":"RobotoSerif28ptExtraCondensed-BlackItalic","robotoserif28ptextracondensed|bold":"RobotoSerif28ptExtraCondensed-Bold","robotoserif28ptextracondensed|bolditalic":"RobotoSerif28ptExtraCondensed-BoldItalic","robotoserif28ptextracondensed|book":"RobotoSerif28ptExtraCondensed-Regular","robotoserif28ptextracondensed|demi":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensed|demibold":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensed|extrabold":"RobotoSerif28ptExtraCondensed-ExtraBold","robotoserif28ptextracondensed|extrabolditalic":"RobotoSerif28ptExtraCondensed-ExtraBoldItalic","robotoserif28ptextracondensed|extralight":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensed|extralightitalic":"RobotoSerif28ptExtraCondensed-ExtraLightItalic","robotoserif28ptextracondensed|heavy":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensed|italic":"RobotoSerif28ptExtraCondensed-Italic","robotoserif28ptextracondensed|light":"RobotoSerif28ptExtraCondensed-Light","robotoserif28ptextracondensed|lightitalic":"RobotoSerif28ptExtraCondensed-LightItalic","robotoserif28ptextracondensed|medium":"RobotoSerif28ptExtraCondensed-Medium","robotoserif28ptextracondensed|mediumitalic":"RobotoSerif28ptExtraCondensed-MediumItalic","robotoserif28ptextracondensed|normal":"RobotoSerif28ptExtraCondensed-Regular","robotoserif28ptextracondensed|regular":"RobotoSerif28ptExtraCondensed-Regular","robotoserif28ptextracondensed|roman":"RobotoSerif28ptExtraCondensed-Regular","robotoserif28ptextracondensed|semibold":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensed|semibolditalic":"RobotoSerif28ptExtraCondensed-SemiBoldItalic","robotoserif28ptextracondensed|thin":"RobotoSerif28ptExtraCondensed-Thin","robotoserif28ptextracondensed|thinitalic":"RobotoSerif28ptExtraCondensed-ThinItalic","robotoserif28ptextracondensed|ultralight":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensedblack|book":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensedblack|italic":"RobotoSerif28ptExtraCondensed-BlackItalic","robotoserif28ptextracondensedblack|normal":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensedblack|regular":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensedblack|roman":"RobotoSerif28ptExtraCondensed-Black","robotoserif28ptextracondensedextrabold|book":"RobotoSerif28ptExtraCondensed-ExtraBold","robotoserif28ptextracondensedextrabold|italic":"RobotoSerif28ptExtraCondensed-ExtraBoldItalic","robotoserif28ptextracondensedextrabold|normal":"RobotoSerif28ptExtraCondensed-ExtraBold","robotoserif28ptextracondensedextrabold|regular":"RobotoSerif28ptExtraCondensed-ExtraBold","robotoserif28ptextracondensedextrabold|roman":"RobotoSerif28ptExtraCondensed-ExtraBold","robotoserif28ptextracondensedextralight|book":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensedextralight|italic":"RobotoSerif28ptExtraCondensed-ExtraLightItalic","robotoserif28ptextracondensedextralight|normal":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensedextralight|regular":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensedextralight|roman":"RobotoSerif28ptExtraCondensed-ExtraLight","robotoserif28ptextracondensedlight|book":"RobotoSerif28ptExtraCondensed-Light","robotoserif28ptextracondensedlight|italic":"RobotoSerif28ptExtraCondensed-LightItalic","robotoserif28ptextracondensedlight|normal":"RobotoSerif28ptExtraCondensed-Light","robotoserif28ptextracondensedlight|regular":"RobotoSerif28ptExtraCondensed-Light","robotoserif28ptextracondensedlight|roman":"RobotoSerif28ptExtraCondensed-Light","robotoserif28ptextracondensedmedium|book":"RobotoSerif28ptExtraCondensed-Medium","robotoserif28ptextracondensedmedium|italic":"RobotoSerif28ptExtraCondensed-MediumItalic","robotoserif28ptextracondensedmedium|normal":"RobotoSerif28ptExtraCondensed-Medium","robotoserif28ptextracondensedmedium|regular":"RobotoSerif28ptExtraCondensed-Medium","robotoserif28ptextracondensedmedium|roman":"RobotoSerif28ptExtraCondensed-Medium","robotoserif28ptextracondensedsemibold|book":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensedsemibold|italic":"RobotoSerif28ptExtraCondensed-SemiBoldItalic","robotoserif28ptextracondensedsemibold|normal":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensedsemibold|regular":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensedsemibold|roman":"RobotoSerif28ptExtraCondensed-SemiBold","robotoserif28ptextracondensedthin|book":"RobotoSerif28ptExtraCondensed-Thin","robotoserif28ptextracondensedthin|italic":"RobotoSerif28ptExtraCondensed-ThinItalic","robotoserif28ptextracondensedthin|normal":"RobotoSerif28ptExtraCondensed-Thin","robotoserif28ptextracondensedthin|regular":"RobotoSerif28ptExtraCondensed-Thin","robotoserif28ptextracondensedthin|roman":"RobotoSerif28ptExtraCondensed-Thin","robotoserif28ptextraexpanded|black":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpanded|blackitalic":"RobotoSerif28ptExtraExpanded-BlackItalic","robotoserif28ptextraexpanded|bold":"RobotoSerif28ptExtraExpanded-Bold","robotoserif28ptextraexpanded|bolditalic":"RobotoSerif28ptExtraExpanded-BoldItalic","robotoserif28ptextraexpanded|book":"RobotoSerif28ptExtraExpanded-Regular","robotoserif28ptextraexpanded|demi":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpanded|demibold":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpanded|extrabold":"RobotoSerif28ptExtraExpanded-ExtraBold","robotoserif28ptextraexpanded|extrabolditalic":"RobotoSerif28ptExtraExpanded-ExtraBoldItalic","robotoserif28ptextraexpanded|extralight":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpanded|extralightitalic":"RobotoSerif28ptExtraExpanded-ExtraLightItalic","robotoserif28ptextraexpanded|heavy":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpanded|italic":"RobotoSerif28ptExtraExpanded-Italic","robotoserif28ptextraexpanded|light":"RobotoSerif28ptExtraExpanded-Light","robotoserif28ptextraexpanded|lightitalic":"RobotoSerif28ptExtraExpanded-LightItalic","robotoserif28ptextraexpanded|medium":"RobotoSerif28ptExtraExpanded-Medium","robotoserif28ptextraexpanded|mediumitalic":"RobotoSerif28ptExtraExpanded-MediumItalic","robotoserif28ptextraexpanded|normal":"RobotoSerif28ptExtraExpanded-Regular","robotoserif28ptextraexpanded|regular":"RobotoSerif28ptExtraExpanded-Regular","robotoserif28ptextraexpanded|roman":"RobotoSerif28ptExtraExpanded-Regular","robotoserif28ptextraexpanded|semibold":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpanded|semibolditalic":"RobotoSerif28ptExtraExpanded-SemiBoldItalic","robotoserif28ptextraexpanded|thin":"RobotoSerif28ptExtraExpanded-Thin","robotoserif28ptextraexpanded|thinitalic":"RobotoSerif28ptExtraExpanded-ThinItalic","robotoserif28ptextraexpanded|ultralight":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpandedblack|book":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpandedblack|italic":"RobotoSerif28ptExtraExpanded-BlackItalic","robotoserif28ptextraexpandedblack|normal":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpandedblack|regular":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpandedblack|roman":"RobotoSerif28ptExtraExpanded-Black","robotoserif28ptextraexpandedextrabold|book":"RobotoSerif28ptExtraExpanded-ExtraBold","robotoserif28ptextraexpandedextrabold|italic":"RobotoSerif28ptExtraExpanded-ExtraBoldItalic","robotoserif28ptextraexpandedextrabold|normal":"RobotoSerif28ptExtraExpanded-ExtraBold","robotoserif28ptextraexpandedextrabold|regular":"RobotoSerif28ptExtraExpanded-ExtraBold","robotoserif28ptextraexpandedextrabold|roman":"RobotoSerif28ptExtraExpanded-ExtraBold","robotoserif28ptextraexpandedextralight|book":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpandedextralight|italic":"RobotoSerif28ptExtraExpanded-ExtraLightItalic","robotoserif28ptextraexpandedextralight|normal":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpandedextralight|regular":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpandedextralight|roman":"RobotoSerif28ptExtraExpanded-ExtraLight","robotoserif28ptextraexpandedlight|book":"RobotoSerif28ptExtraExpanded-Light","robotoserif28ptextraexpandedlight|italic":"RobotoSerif28ptExtraExpanded-LightItalic","robotoserif28ptextraexpandedlight|normal":"RobotoSerif28ptExtraExpanded-Light","robotoserif28ptextraexpandedlight|regular":"RobotoSerif28ptExtraExpanded-Light","robotoserif28ptextraexpandedlight|roman":"RobotoSerif28ptExtraExpanded-Light","robotoserif28ptextraexpandedmedium|book":"RobotoSerif28ptExtraExpanded-Medium","robotoserif28ptextraexpandedmedium|italic":"RobotoSerif28ptExtraExpanded-MediumItalic","robotoserif28ptextraexpandedmedium|normal":"RobotoSerif28ptExtraExpanded-Medium","robotoserif28ptextraexpandedmedium|regular":"RobotoSerif28ptExtraExpanded-Medium","robotoserif28ptextraexpandedmedium|roman":"RobotoSerif28ptExtraExpanded-Medium","robotoserif28ptextraexpandedsemibold|book":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpandedsemibold|italic":"RobotoSerif28ptExtraExpanded-SemiBoldItalic","robotoserif28ptextraexpandedsemibold|normal":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpandedsemibold|regular":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpandedsemibold|roman":"RobotoSerif28ptExtraExpanded-SemiBold","robotoserif28ptextraexpandedthin|book":"RobotoSerif28ptExtraExpanded-Thin","robotoserif28ptextraexpandedthin|italic":"RobotoSerif28ptExtraExpanded-ThinItalic","robotoserif28ptextraexpandedthin|normal":"RobotoSerif28ptExtraExpanded-Thin","robotoserif28ptextraexpandedthin|regular":"RobotoSerif28ptExtraExpanded-Thin","robotoserif28ptextraexpandedthin|roman":"RobotoSerif28ptExtraExpanded-Thin","robotoserif28ptextralight|book":"RobotoSerif28pt-ExtraLight","robotoserif28ptextralight|italic":"RobotoSerif28pt-ExtraLightItalic","robotoserif28ptextralight|normal":"RobotoSerif28pt-ExtraLight","robotoserif28ptextralight|regular":"RobotoSerif28pt-ExtraLight","robotoserif28ptextralight|roman":"RobotoSerif28pt-ExtraLight","robotoserif28ptlight|book":"RobotoSerif28pt-Light","robotoserif28ptlight|italic":"RobotoSerif28pt-LightItalic","robotoserif28ptlight|normal":"RobotoSerif28pt-Light","robotoserif28ptlight|regular":"RobotoSerif28pt-Light","robotoserif28ptlight|roman":"RobotoSerif28pt-Light","robotoserif28ptmedium|book":"RobotoSerif28pt-Medium","robotoserif28ptmedium|italic":"RobotoSerif28pt-MediumItalic","robotoserif28ptmedium|normal":"RobotoSerif28pt-Medium","robotoserif28ptmedium|regular":"RobotoSerif28pt-Medium","robotoserif28ptmedium|roman":"RobotoSerif28pt-Medium","robotoserif28ptsemibold|book":"RobotoSerif28pt-SemiBold","robotoserif28ptsemibold|italic":"RobotoSerif28pt-SemiBoldItalic","robotoserif28ptsemibold|normal":"RobotoSerif28pt-SemiBold","robotoserif28ptsemibold|regular":"RobotoSerif28pt-SemiBold","robotoserif28ptsemibold|roman":"RobotoSerif28pt-SemiBold","robotoserif28ptsemicondensed|black":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensed|blackitalic":"RobotoSerif28ptSemiCondensed-BlackItalic","robotoserif28ptsemicondensed|bold":"RobotoSerif28ptSemiCondensed-Bold","robotoserif28ptsemicondensed|bolditalic":"RobotoSerif28ptSemiCondensed-BoldItalic","robotoserif28ptsemicondensed|book":"RobotoSerif28ptSemiCondensed-Regular","robotoserif28ptsemicondensed|demi":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensed|demibold":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensed|extrabold":"RobotoSerif28ptSemiCondensed-ExtraBold","robotoserif28ptsemicondensed|extrabolditalic":"RobotoSerif28ptSemiCondensed-ExtraBoldItalic","robotoserif28ptsemicondensed|extralight":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensed|extralightitalic":"RobotoSerif28ptSemiCondensed-ExtraLightItalic","robotoserif28ptsemicondensed|heavy":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensed|italic":"RobotoSerif28ptSemiCondensed-Italic","robotoserif28ptsemicondensed|light":"RobotoSerif28ptSemiCondensed-Light","robotoserif28ptsemicondensed|lightitalic":"RobotoSerif28ptSemiCondensed-LightItalic","robotoserif28ptsemicondensed|medium":"RobotoSerif28ptSemiCondensed-Medium","robotoserif28ptsemicondensed|mediumitalic":"RobotoSerif28ptSemiCondensed-MediumItalic","robotoserif28ptsemicondensed|normal":"RobotoSerif28ptSemiCondensed-Regular","robotoserif28ptsemicondensed|regular":"RobotoSerif28ptSemiCondensed-Regular","robotoserif28ptsemicondensed|roman":"RobotoSerif28ptSemiCondensed-Regular","robotoserif28ptsemicondensed|semibold":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensed|semibolditalic":"RobotoSerif28ptSemiCondensed-SemiBoldItalic","robotoserif28ptsemicondensed|thin":"RobotoSerif28ptSemiCondensed-Thin","robotoserif28ptsemicondensed|thinitalic":"RobotoSerif28ptSemiCondensed-ThinItalic","robotoserif28ptsemicondensed|ultralight":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensedblack|book":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensedblack|italic":"RobotoSerif28ptSemiCondensed-BlackItalic","robotoserif28ptsemicondensedblack|normal":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensedblack|regular":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensedblack|roman":"RobotoSerif28ptSemiCondensed-Black","robotoserif28ptsemicondensedextrabold|book":"RobotoSerif28ptSemiCondensed-ExtraBold","robotoserif28ptsemicondensedextrabold|italic":"RobotoSerif28ptSemiCondensed-ExtraBoldItalic","robotoserif28ptsemicondensedextrabold|normal":"RobotoSerif28ptSemiCondensed-ExtraBold","robotoserif28ptsemicondensedextrabold|regular":"RobotoSerif28ptSemiCondensed-ExtraBold","robotoserif28ptsemicondensedextrabold|roman":"RobotoSerif28ptSemiCondensed-ExtraBold","robotoserif28ptsemicondensedextralight|book":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensedextralight|italic":"RobotoSerif28ptSemiCondensed-ExtraLightItalic","robotoserif28ptsemicondensedextralight|normal":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensedextralight|regular":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensedextralight|roman":"RobotoSerif28ptSemiCondensed-ExtraLight","robotoserif28ptsemicondensedlight|book":"RobotoSerif28ptSemiCondensed-Light","robotoserif28ptsemicondensedlight|italic":"RobotoSerif28ptSemiCondensed-LightItalic","robotoserif28ptsemicondensedlight|normal":"RobotoSerif28ptSemiCondensed-Light","robotoserif28ptsemicondensedlight|regular":"RobotoSerif28ptSemiCondensed-Light","robotoserif28ptsemicondensedlight|roman":"RobotoSerif28ptSemiCondensed-Light","robotoserif28ptsemicondensedmedium|book":"RobotoSerif28ptSemiCondensed-Medium","robotoserif28ptsemicondensedmedium|italic":"RobotoSerif28ptSemiCondensed-MediumItalic","robotoserif28ptsemicondensedmedium|normal":"RobotoSerif28ptSemiCondensed-Medium","robotoserif28ptsemicondensedmedium|regular":"RobotoSerif28ptSemiCondensed-Medium","robotoserif28ptsemicondensedmedium|roman":"RobotoSerif28ptSemiCondensed-Medium","robotoserif28ptsemicondensedsemibold|book":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensedsemibold|italic":"RobotoSerif28ptSemiCondensed-SemiBoldItalic","robotoserif28ptsemicondensedsemibold|normal":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensedsemibold|regular":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensedsemibold|roman":"RobotoSerif28ptSemiCondensed-SemiBold","robotoserif28ptsemicondensedthin|book":"RobotoSerif28ptSemiCondensed-Thin","robotoserif28ptsemicondensedthin|italic":"RobotoSerif28ptSemiCondensed-ThinItalic","robotoserif28ptsemicondensedthin|normal":"RobotoSerif28ptSemiCondensed-Thin","robotoserif28ptsemicondensedthin|regular":"RobotoSerif28ptSemiCondensed-Thin","robotoserif28ptsemicondensedthin|roman":"RobotoSerif28ptSemiCondensed-Thin","robotoserif28ptsemiexpanded|black":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpanded|blackitalic":"RobotoSerif28ptSemiExpanded-BlackItalic","robotoserif28ptsemiexpanded|bold":"RobotoSerif28ptSemiExpanded-Bold","robotoserif28ptsemiexpanded|bolditalic":"RobotoSerif28ptSemiExpanded-BoldItalic","robotoserif28ptsemiexpanded|book":"RobotoSerif28ptSemiExpanded-Regular","robotoserif28ptsemiexpanded|demi":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpanded|demibold":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpanded|extrabold":"RobotoSerif28ptSemiExpanded-ExtraBold","robotoserif28ptsemiexpanded|extrabolditalic":"RobotoSerif28ptSemiExpanded-ExtraBoldItalic","robotoserif28ptsemiexpanded|extralight":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpanded|extralightitalic":"RobotoSerif28ptSemiExpanded-ExtraLightItalic","robotoserif28ptsemiexpanded|heavy":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpanded|italic":"RobotoSerif28ptSemiExpanded-Italic","robotoserif28ptsemiexpanded|light":"RobotoSerif28ptSemiExpanded-Light","robotoserif28ptsemiexpanded|lightitalic":"RobotoSerif28ptSemiExpanded-LightItalic","robotoserif28ptsemiexpanded|medium":"RobotoSerif28ptSemiExpanded-Medium","robotoserif28ptsemiexpanded|mediumitalic":"RobotoSerif28ptSemiExpanded-MediumItalic","robotoserif28ptsemiexpanded|normal":"RobotoSerif28ptSemiExpanded-Regular","robotoserif28ptsemiexpanded|regular":"RobotoSerif28ptSemiExpanded-Regular","robotoserif28ptsemiexpanded|roman":"RobotoSerif28ptSemiExpanded-Regular","robotoserif28ptsemiexpanded|semibold":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpanded|semibolditalic":"RobotoSerif28ptSemiExpanded-SemiBoldItalic","robotoserif28ptsemiexpanded|thin":"RobotoSerif28ptSemiExpanded-Thin","robotoserif28ptsemiexpanded|thinitalic":"RobotoSerif28ptSemiExpanded-ThinItalic","robotoserif28ptsemiexpanded|ultralight":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpandedblack|book":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpandedblack|italic":"RobotoSerif28ptSemiExpanded-BlackItalic","robotoserif28ptsemiexpandedblack|normal":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpandedblack|regular":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpandedblack|roman":"RobotoSerif28ptSemiExpanded-Black","robotoserif28ptsemiexpandedextrabold|book":"RobotoSerif28ptSemiExpanded-ExtraBold","robotoserif28ptsemiexpandedextrabold|italic":"RobotoSerif28ptSemiExpanded-ExtraBoldItalic","robotoserif28ptsemiexpandedextrabold|normal":"RobotoSerif28ptSemiExpanded-ExtraBold","robotoserif28ptsemiexpandedextrabold|regular":"RobotoSerif28ptSemiExpanded-ExtraBold","robotoserif28ptsemiexpandedextrabold|roman":"RobotoSerif28ptSemiExpanded-ExtraBold","robotoserif28ptsemiexpandedextralight|book":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpandedextralight|italic":"RobotoSerif28ptSemiExpanded-ExtraLightItalic","robotoserif28ptsemiexpandedextralight|normal":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpandedextralight|regular":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpandedextralight|roman":"RobotoSerif28ptSemiExpanded-ExtraLight","robotoserif28ptsemiexpandedlight|book":"RobotoSerif28ptSemiExpanded-Light","robotoserif28ptsemiexpandedlight|italic":"RobotoSerif28ptSemiExpanded-LightItalic","robotoserif28ptsemiexpandedlight|normal":"RobotoSerif28ptSemiExpanded-Light","robotoserif28ptsemiexpandedlight|regular":"RobotoSerif28ptSemiExpanded-Light","robotoserif28ptsemiexpandedlight|roman":"RobotoSerif28ptSemiExpanded-Light","robotoserif28ptsemiexpandedmedium|book":"RobotoSerif28ptSemiExpanded-Medium","robotoserif28ptsemiexpandedmedium|italic":"RobotoSerif28ptSemiExpanded-MediumItalic","robotoserif28ptsemiexpandedmedium|normal":"RobotoSerif28ptSemiExpanded-Medium","robotoserif28ptsemiexpandedmedium|regular":"RobotoSerif28ptSemiExpanded-Medium","robotoserif28ptsemiexpandedmedium|roman":"RobotoSerif28ptSemiExpanded-Medium","robotoserif28ptsemiexpandedsemibold|book":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpandedsemibold|italic":"RobotoSerif28ptSemiExpanded-SemiBoldItalic","robotoserif28ptsemiexpandedsemibold|normal":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpandedsemibold|regular":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpandedsemibold|roman":"RobotoSerif28ptSemiExpanded-SemiBold","robotoserif28ptsemiexpandedthin|book":"RobotoSerif28ptSemiExpanded-Thin","robotoserif28ptsemiexpandedthin|italic":"RobotoSerif28ptSemiExpanded-ThinItalic","robotoserif28ptsemiexpandedthin|normal":"RobotoSerif28ptSemiExpanded-Thin","robotoserif28ptsemiexpandedthin|regular":"RobotoSerif28ptSemiExpanded-Thin","robotoserif28ptsemiexpandedthin|roman":"RobotoSerif28ptSemiExpanded-Thin","robotoserif28ptthin|book":"RobotoSerif28pt-Thin","robotoserif28ptthin|italic":"RobotoSerif28pt-ThinItalic","robotoserif28ptthin|normal":"RobotoSerif28pt-Thin","robotoserif28ptthin|regular":"RobotoSerif28pt-Thin","robotoserif28ptthin|roman":"RobotoSerif28pt-Thin","robotoserif28ptultracondensed|black":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensed|blackitalic":"RobotoSerif28ptUltraCondensed-BlackItalic","robotoserif28ptultracondensed|bold":"RobotoSerif28ptUltraCondensed-Bold","robotoserif28ptultracondensed|bolditalic":"RobotoSerif28ptUltraCondensed-BoldItalic","robotoserif28ptultracondensed|book":"RobotoSerif28ptUltraCondensed-Regular","robotoserif28ptultracondensed|demi":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensed|demibold":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensed|extrabold":"RobotoSerif28ptUltraCondensed-ExtraBold","robotoserif28ptultracondensed|extrabolditalic":"RobotoSerif28ptUltraCondensed-ExtraBoldItalic","robotoserif28ptultracondensed|extralight":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensed|extralightitalic":"RobotoSerif28ptUltraCondensed-ExtraLightItalic","robotoserif28ptultracondensed|heavy":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensed|italic":"RobotoSerif28ptUltraCondensed-Italic","robotoserif28ptultracondensed|light":"RobotoSerif28ptUltraCondensed-Light","robotoserif28ptultracondensed|lightitalic":"RobotoSerif28ptUltraCondensed-LightItalic","robotoserif28ptultracondensed|medium":"RobotoSerif28ptUltraCondensed-Medium","robotoserif28ptultracondensed|mediumitalic":"RobotoSerif28ptUltraCondensed-MediumItalic","robotoserif28ptultracondensed|normal":"RobotoSerif28ptUltraCondensed-Regular","robotoserif28ptultracondensed|regular":"RobotoSerif28ptUltraCondensed-Regular","robotoserif28ptultracondensed|roman":"RobotoSerif28ptUltraCondensed-Regular","robotoserif28ptultracondensed|semibold":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensed|semibolditalic":"RobotoSerif28ptUltraCondensed-SemiBoldItalic","robotoserif28ptultracondensed|thin":"RobotoSerif28ptUltraCondensed-Thin","robotoserif28ptultracondensed|thinitalic":"RobotoSerif28ptUltraCondensed-ThinItalic","robotoserif28ptultracondensed|ultralight":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensedblack|book":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensedblack|italic":"RobotoSerif28ptUltraCondensed-BlackItalic","robotoserif28ptultracondensedblack|normal":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensedblack|regular":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensedblack|roman":"RobotoSerif28ptUltraCondensed-Black","robotoserif28ptultracondensedextrabold|book":"RobotoSerif28ptUltraCondensed-ExtraBold","robotoserif28ptultracondensedextrabold|italic":"RobotoSerif28ptUltraCondensed-ExtraBoldItalic","robotoserif28ptultracondensedextrabold|normal":"RobotoSerif28ptUltraCondensed-ExtraBold","robotoserif28ptultracondensedextrabold|regular":"RobotoSerif28ptUltraCondensed-ExtraBold","robotoserif28ptultracondensedextrabold|roman":"RobotoSerif28ptUltraCondensed-ExtraBold","robotoserif28ptultracondensedextralight|book":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensedextralight|italic":"RobotoSerif28ptUltraCondensed-ExtraLightItalic","robotoserif28ptultracondensedextralight|normal":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensedextralight|regular":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensedextralight|roman":"RobotoSerif28ptUltraCondensed-ExtraLight","robotoserif28ptultracondensedlight|book":"RobotoSerif28ptUltraCondensed-Light","robotoserif28ptultracondensedlight|italic":"RobotoSerif28ptUltraCondensed-LightItalic","robotoserif28ptultracondensedlight|normal":"RobotoSerif28ptUltraCondensed-Light","robotoserif28ptultracondensedlight|regular":"RobotoSerif28ptUltraCondensed-Light","robotoserif28ptultracondensedlight|roman":"RobotoSerif28ptUltraCondensed-Light","robotoserif28ptultracondensedmedium|book":"RobotoSerif28ptUltraCondensed-Medium","robotoserif28ptultracondensedmedium|italic":"RobotoSerif28ptUltraCondensed-MediumItalic","robotoserif28ptultracondensedmedium|normal":"RobotoSerif28ptUltraCondensed-Medium","robotoserif28ptultracondensedmedium|regular":"RobotoSerif28ptUltraCondensed-Medium","robotoserif28ptultracondensedmedium|roman":"RobotoSerif28ptUltraCondensed-Medium","robotoserif28ptultracondensedsemibold|book":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensedsemibold|italic":"RobotoSerif28ptUltraCondensed-SemiBoldItalic","robotoserif28ptultracondensedsemibold|normal":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensedsemibold|regular":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensedsemibold|roman":"RobotoSerif28ptUltraCondensed-SemiBold","robotoserif28ptultracondensedthin|book":"RobotoSerif28ptUltraCondensed-Thin","robotoserif28ptultracondensedthin|italic":"RobotoSerif28ptUltraCondensed-ThinItalic","robotoserif28ptultracondensedthin|normal":"RobotoSerif28ptUltraCondensed-Thin","robotoserif28ptultracondensedthin|regular":"RobotoSerif28ptUltraCondensed-Thin","robotoserif28ptultracondensedthin|roman":"RobotoSerif28ptUltraCondensed-Thin","robotoserif36pt|black":"RobotoSerif36pt-Black","robotoserif36pt|blackitalic":"RobotoSerif36pt-BlackItalic","robotoserif36pt|bold":"RobotoSerif36pt-Bold","robotoserif36pt|bolditalic":"RobotoSerif36pt-BoldItalic","robotoserif36pt|book":"RobotoSerif36pt-Regular","robotoserif36pt|demi":"RobotoSerif36pt-SemiBold","robotoserif36pt|demibold":"RobotoSerif36pt-SemiBold","robotoserif36pt|extrabold":"RobotoSerif36pt-ExtraBold","robotoserif36pt|extrabolditalic":"RobotoSerif36pt-ExtraBoldItalic","robotoserif36pt|extralight":"RobotoSerif36pt-ExtraLight","robotoserif36pt|extralightitalic":"RobotoSerif36pt-ExtraLightItalic","robotoserif36pt|heavy":"RobotoSerif36pt-Black","robotoserif36pt|italic":"RobotoSerif36pt-Italic","robotoserif36pt|light":"RobotoSerif36pt-Light","robotoserif36pt|lightitalic":"RobotoSerif36pt-LightItalic","robotoserif36pt|medium":"RobotoSerif36pt-Medium","robotoserif36pt|mediumitalic":"RobotoSerif36pt-MediumItalic","robotoserif36pt|normal":"RobotoSerif36pt-Regular","robotoserif36pt|regular":"RobotoSerif36pt-Regular","robotoserif36pt|roman":"RobotoSerif36pt-Regular","robotoserif36pt|semibold":"RobotoSerif36pt-SemiBold","robotoserif36pt|semibolditalic":"RobotoSerif36pt-SemiBoldItalic","robotoserif36pt|thin":"RobotoSerif36pt-Thin","robotoserif36pt|thinitalic":"RobotoSerif36pt-ThinItalic","robotoserif36pt|ultralight":"RobotoSerif36pt-ExtraLight","robotoserif36ptblack|book":"RobotoSerif36pt-Black","robotoserif36ptblack|italic":"RobotoSerif36pt-BlackItalic","robotoserif36ptblack|normal":"RobotoSerif36pt-Black","robotoserif36ptblack|regular":"RobotoSerif36pt-Black","robotoserif36ptblack|roman":"RobotoSerif36pt-Black","robotoserif36ptcondensed|black":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensed|blackitalic":"RobotoSerif36ptCondensed-BlackItalic","robotoserif36ptcondensed|bold":"RobotoSerif36ptCondensed-Bold","robotoserif36ptcondensed|bolditalic":"RobotoSerif36ptCondensed-BoldItalic","robotoserif36ptcondensed|book":"RobotoSerif36ptCondensed-Regular","robotoserif36ptcondensed|demi":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensed|demibold":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensed|extrabold":"RobotoSerif36ptCondensed-ExtraBold","robotoserif36ptcondensed|extrabolditalic":"RobotoSerif36ptCondensed-ExtraBoldItalic","robotoserif36ptcondensed|extralight":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensed|extralightitalic":"RobotoSerif36ptCondensed-ExtraLightItalic","robotoserif36ptcondensed|heavy":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensed|italic":"RobotoSerif36ptCondensed-Italic","robotoserif36ptcondensed|light":"RobotoSerif36ptCondensed-Light","robotoserif36ptcondensed|lightitalic":"RobotoSerif36ptCondensed-LightItalic","robotoserif36ptcondensed|medium":"RobotoSerif36ptCondensed-Medium","robotoserif36ptcondensed|mediumitalic":"RobotoSerif36ptCondensed-MediumItalic","robotoserif36ptcondensed|normal":"RobotoSerif36ptCondensed-Regular","robotoserif36ptcondensed|regular":"RobotoSerif36ptCondensed-Regular","robotoserif36ptcondensed|roman":"RobotoSerif36ptCondensed-Regular","robotoserif36ptcondensed|semibold":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensed|semibolditalic":"RobotoSerif36ptCondensed-SemiBoldItalic","robotoserif36ptcondensed|thin":"RobotoSerif36ptCondensed-Thin","robotoserif36ptcondensed|thinitalic":"RobotoSerif36ptCondensed-ThinItalic","robotoserif36ptcondensed|ultralight":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensedblack|book":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensedblack|italic":"RobotoSerif36ptCondensed-BlackItalic","robotoserif36ptcondensedblack|normal":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensedblack|regular":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensedblack|roman":"RobotoSerif36ptCondensed-Black","robotoserif36ptcondensedextrabold|book":"RobotoSerif36ptCondensed-ExtraBold","robotoserif36ptcondensedextrabold|italic":"RobotoSerif36ptCondensed-ExtraBoldItalic","robotoserif36ptcondensedextrabold|normal":"RobotoSerif36ptCondensed-ExtraBold","robotoserif36ptcondensedextrabold|regular":"RobotoSerif36ptCondensed-ExtraBold","robotoserif36ptcondensedextrabold|roman":"RobotoSerif36ptCondensed-ExtraBold","robotoserif36ptcondensedextralight|book":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensedextralight|italic":"RobotoSerif36ptCondensed-ExtraLightItalic","robotoserif36ptcondensedextralight|normal":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensedextralight|regular":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensedextralight|roman":"RobotoSerif36ptCondensed-ExtraLight","robotoserif36ptcondensedlight|book":"RobotoSerif36ptCondensed-Light","robotoserif36ptcondensedlight|italic":"RobotoSerif36ptCondensed-LightItalic","robotoserif36ptcondensedlight|normal":"RobotoSerif36ptCondensed-Light","robotoserif36ptcondensedlight|regular":"RobotoSerif36ptCondensed-Light","robotoserif36ptcondensedlight|roman":"RobotoSerif36ptCondensed-Light","robotoserif36ptcondensedmedium|book":"RobotoSerif36ptCondensed-Medium","robotoserif36ptcondensedmedium|italic":"RobotoSerif36ptCondensed-MediumItalic","robotoserif36ptcondensedmedium|normal":"RobotoSerif36ptCondensed-Medium","robotoserif36ptcondensedmedium|regular":"RobotoSerif36ptCondensed-Medium","robotoserif36ptcondensedmedium|roman":"RobotoSerif36ptCondensed-Medium","robotoserif36ptcondensedsemibold|book":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensedsemibold|italic":"RobotoSerif36ptCondensed-SemiBoldItalic","robotoserif36ptcondensedsemibold|normal":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensedsemibold|regular":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensedsemibold|roman":"RobotoSerif36ptCondensed-SemiBold","robotoserif36ptcondensedthin|book":"RobotoSerif36ptCondensed-Thin","robotoserif36ptcondensedthin|italic":"RobotoSerif36ptCondensed-ThinItalic","robotoserif36ptcondensedthin|normal":"RobotoSerif36ptCondensed-Thin","robotoserif36ptcondensedthin|regular":"RobotoSerif36ptCondensed-Thin","robotoserif36ptcondensedthin|roman":"RobotoSerif36ptCondensed-Thin","robotoserif36ptexpanded|black":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpanded|blackitalic":"RobotoSerif36ptExpanded-BlackItalic","robotoserif36ptexpanded|bold":"RobotoSerif36ptExpanded-Bold","robotoserif36ptexpanded|bolditalic":"RobotoSerif36ptExpanded-BoldItalic","robotoserif36ptexpanded|book":"RobotoSerif36ptExpanded-Regular","robotoserif36ptexpanded|demi":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpanded|demibold":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpanded|extrabold":"RobotoSerif36ptExpanded-ExtraBold","robotoserif36ptexpanded|extrabolditalic":"RobotoSerif36ptExpanded-ExtraBoldItalic","robotoserif36ptexpanded|extralight":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpanded|extralightitalic":"RobotoSerif36ptExpanded-ExtraLightItalic","robotoserif36ptexpanded|heavy":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpanded|italic":"RobotoSerif36ptExpanded-Italic","robotoserif36ptexpanded|light":"RobotoSerif36ptExpanded-Light","robotoserif36ptexpanded|lightitalic":"RobotoSerif36ptExpanded-LightItalic","robotoserif36ptexpanded|medium":"RobotoSerif36ptExpanded-Medium","robotoserif36ptexpanded|mediumitalic":"RobotoSerif36ptExpanded-MediumItalic","robotoserif36ptexpanded|normal":"RobotoSerif36ptExpanded-Regular","robotoserif36ptexpanded|regular":"RobotoSerif36ptExpanded-Regular","robotoserif36ptexpanded|roman":"RobotoSerif36ptExpanded-Regular","robotoserif36ptexpanded|semibold":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpanded|semibolditalic":"RobotoSerif36ptExpanded-SemiBoldItalic","robotoserif36ptexpanded|thin":"RobotoSerif36ptExpanded-Thin","robotoserif36ptexpanded|thinitalic":"RobotoSerif36ptExpanded-ThinItalic","robotoserif36ptexpanded|ultralight":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpandedblack|book":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpandedblack|italic":"RobotoSerif36ptExpanded-BlackItalic","robotoserif36ptexpandedblack|normal":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpandedblack|regular":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpandedblack|roman":"RobotoSerif36ptExpanded-Black","robotoserif36ptexpandedextrabold|book":"RobotoSerif36ptExpanded-ExtraBold","robotoserif36ptexpandedextrabold|italic":"RobotoSerif36ptExpanded-ExtraBoldItalic","robotoserif36ptexpandedextrabold|normal":"RobotoSerif36ptExpanded-ExtraBold","robotoserif36ptexpandedextrabold|regular":"RobotoSerif36ptExpanded-ExtraBold","robotoserif36ptexpandedextrabold|roman":"RobotoSerif36ptExpanded-ExtraBold","robotoserif36ptexpandedextralight|book":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpandedextralight|italic":"RobotoSerif36ptExpanded-ExtraLightItalic","robotoserif36ptexpandedextralight|normal":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpandedextralight|regular":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpandedextralight|roman":"RobotoSerif36ptExpanded-ExtraLight","robotoserif36ptexpandedlight|book":"RobotoSerif36ptExpanded-Light","robotoserif36ptexpandedlight|italic":"RobotoSerif36ptExpanded-LightItalic","robotoserif36ptexpandedlight|normal":"RobotoSerif36ptExpanded-Light","robotoserif36ptexpandedlight|regular":"RobotoSerif36ptExpanded-Light","robotoserif36ptexpandedlight|roman":"RobotoSerif36ptExpanded-Light","robotoserif36ptexpandedmedium|book":"RobotoSerif36ptExpanded-Medium","robotoserif36ptexpandedmedium|italic":"RobotoSerif36ptExpanded-MediumItalic","robotoserif36ptexpandedmedium|normal":"RobotoSerif36ptExpanded-Medium","robotoserif36ptexpandedmedium|regular":"RobotoSerif36ptExpanded-Medium","robotoserif36ptexpandedmedium|roman":"RobotoSerif36ptExpanded-Medium","robotoserif36ptexpandedsemibold|book":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpandedsemibold|italic":"RobotoSerif36ptExpanded-SemiBoldItalic","robotoserif36ptexpandedsemibold|normal":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpandedsemibold|regular":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpandedsemibold|roman":"RobotoSerif36ptExpanded-SemiBold","robotoserif36ptexpandedthin|book":"RobotoSerif36ptExpanded-Thin","robotoserif36ptexpandedthin|italic":"RobotoSerif36ptExpanded-ThinItalic","robotoserif36ptexpandedthin|normal":"RobotoSerif36ptExpanded-Thin","robotoserif36ptexpandedthin|regular":"RobotoSerif36ptExpanded-Thin","robotoserif36ptexpandedthin|roman":"RobotoSerif36ptExpanded-Thin","robotoserif36ptextrabold|book":"RobotoSerif36pt-ExtraBold","robotoserif36ptextrabold|italic":"RobotoSerif36pt-ExtraBoldItalic","robotoserif36ptextrabold|normal":"RobotoSerif36pt-ExtraBold","robotoserif36ptextrabold|regular":"RobotoSerif36pt-ExtraBold","robotoserif36ptextrabold|roman":"RobotoSerif36pt-ExtraBold","robotoserif36ptextracondensed|black":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensed|blackitalic":"RobotoSerif36ptExtraCondensed-BlackItalic","robotoserif36ptextracondensed|bold":"RobotoSerif36ptExtraCondensed-Bold","robotoserif36ptextracondensed|bolditalic":"RobotoSerif36ptExtraCondensed-BoldItalic","robotoserif36ptextracondensed|book":"RobotoSerif36ptExtraCondensed-Regular","robotoserif36ptextracondensed|demi":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensed|demibold":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensed|extrabold":"RobotoSerif36ptExtraCondensed-ExtraBold","robotoserif36ptextracondensed|extrabolditalic":"RobotoSerif36ptExtraCondensed-ExtraBoldItalic","robotoserif36ptextracondensed|extralight":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensed|extralightitalic":"RobotoSerif36ptExtraCondensed-ExtraLightItalic","robotoserif36ptextracondensed|heavy":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensed|italic":"RobotoSerif36ptExtraCondensed-Italic","robotoserif36ptextracondensed|light":"RobotoSerif36ptExtraCondensed-Light","robotoserif36ptextracondensed|lightitalic":"RobotoSerif36ptExtraCondensed-LightItalic","robotoserif36ptextracondensed|medium":"RobotoSerif36ptExtraCondensed-Medium","robotoserif36ptextracondensed|mediumitalic":"RobotoSerif36ptExtraCondensed-MediumItalic","robotoserif36ptextracondensed|normal":"RobotoSerif36ptExtraCondensed-Regular","robotoserif36ptextracondensed|regular":"RobotoSerif36ptExtraCondensed-Regular","robotoserif36ptextracondensed|roman":"RobotoSerif36ptExtraCondensed-Regular","robotoserif36ptextracondensed|semibold":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensed|semibolditalic":"RobotoSerif36ptExtraCondensed-SemiBoldItalic","robotoserif36ptextracondensed|thin":"RobotoSerif36ptExtraCondensed-Thin","robotoserif36ptextracondensed|thinitalic":"RobotoSerif36ptExtraCondensed-ThinItalic","robotoserif36ptextracondensed|ultralight":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensedblack|book":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensedblack|italic":"RobotoSerif36ptExtraCondensed-BlackItalic","robotoserif36ptextracondensedblack|normal":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensedblack|regular":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensedblack|roman":"RobotoSerif36ptExtraCondensed-Black","robotoserif36ptextracondensedextrabold|book":"RobotoSerif36ptExtraCondensed-ExtraBold","robotoserif36ptextracondensedextrabold|italic":"RobotoSerif36ptExtraCondensed-ExtraBoldItalic","robotoserif36ptextracondensedextrabold|normal":"RobotoSerif36ptExtraCondensed-ExtraBold","robotoserif36ptextracondensedextrabold|regular":"RobotoSerif36ptExtraCondensed-ExtraBold","robotoserif36ptextracondensedextrabold|roman":"RobotoSerif36ptExtraCondensed-ExtraBold","robotoserif36ptextracondensedextralight|book":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensedextralight|italic":"RobotoSerif36ptExtraCondensed-ExtraLightItalic","robotoserif36ptextracondensedextralight|normal":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensedextralight|regular":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensedextralight|roman":"RobotoSerif36ptExtraCondensed-ExtraLight","robotoserif36ptextracondensedlight|book":"RobotoSerif36ptExtraCondensed-Light","robotoserif36ptextracondensedlight|italic":"RobotoSerif36ptExtraCondensed-LightItalic","robotoserif36ptextracondensedlight|normal":"RobotoSerif36ptExtraCondensed-Light","robotoserif36ptextracondensedlight|regular":"RobotoSerif36ptExtraCondensed-Light","robotoserif36ptextracondensedlight|roman":"RobotoSerif36ptExtraCondensed-Light","robotoserif36ptextracondensedmedium|book":"RobotoSerif36ptExtraCondensed-Medium","robotoserif36ptextracondensedmedium|italic":"RobotoSerif36ptExtraCondensed-MediumItalic","robotoserif36ptextracondensedmedium|normal":"RobotoSerif36ptExtraCondensed-Medium","robotoserif36ptextracondensedmedium|regular":"RobotoSerif36ptExtraCondensed-Medium","robotoserif36ptextracondensedmedium|roman":"RobotoSerif36ptExtraCondensed-Medium","robotoserif36ptextracondensedsemibold|book":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensedsemibold|italic":"RobotoSerif36ptExtraCondensed-SemiBoldItalic","robotoserif36ptextracondensedsemibold|normal":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensedsemibold|regular":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensedsemibold|roman":"RobotoSerif36ptExtraCondensed-SemiBold","robotoserif36ptextracondensedthin|book":"RobotoSerif36ptExtraCondensed-Thin","robotoserif36ptextracondensedthin|italic":"RobotoSerif36ptExtraCondensed-ThinItalic","robotoserif36ptextracondensedthin|normal":"RobotoSerif36ptExtraCondensed-Thin","robotoserif36ptextracondensedthin|regular":"RobotoSerif36ptExtraCondensed-Thin","robotoserif36ptextracondensedthin|roman":"RobotoSerif36ptExtraCondensed-Thin","robotoserif36ptextraexpanded|black":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpanded|blackitalic":"RobotoSerif36ptExtraExpanded-BlackItalic","robotoserif36ptextraexpanded|bold":"RobotoSerif36ptExtraExpanded-Bold","robotoserif36ptextraexpanded|bolditalic":"RobotoSerif36ptExtraExpanded-BoldItalic","robotoserif36ptextraexpanded|book":"RobotoSerif36ptExtraExpanded-Regular","robotoserif36ptextraexpanded|demi":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpanded|demibold":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpanded|extrabold":"RobotoSerif36ptExtraExpanded-ExtraBold","robotoserif36ptextraexpanded|extrabolditalic":"RobotoSerif36ptExtraExpanded-ExtraBoldItalic","robotoserif36ptextraexpanded|extralight":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpanded|extralightitalic":"RobotoSerif36ptExtraExpanded-ExtraLightItalic","robotoserif36ptextraexpanded|heavy":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpanded|italic":"RobotoSerif36ptExtraExpanded-Italic","robotoserif36ptextraexpanded|light":"RobotoSerif36ptExtraExpanded-Light","robotoserif36ptextraexpanded|lightitalic":"RobotoSerif36ptExtraExpanded-LightItalic","robotoserif36ptextraexpanded|medium":"RobotoSerif36ptExtraExpanded-Medium","robotoserif36ptextraexpanded|mediumitalic":"RobotoSerif36ptExtraExpanded-MediumItalic","robotoserif36ptextraexpanded|normal":"RobotoSerif36ptExtraExpanded-Regular","robotoserif36ptextraexpanded|regular":"RobotoSerif36ptExtraExpanded-Regular","robotoserif36ptextraexpanded|roman":"RobotoSerif36ptExtraExpanded-Regular","robotoserif36ptextraexpanded|semibold":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpanded|semibolditalic":"RobotoSerif36ptExtraExpanded-SemiBoldItalic","robotoserif36ptextraexpanded|thin":"RobotoSerif36ptExtraExpanded-Thin","robotoserif36ptextraexpanded|thinitalic":"RobotoSerif36ptExtraExpanded-ThinItalic","robotoserif36ptextraexpanded|ultralight":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpandedblack|book":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpandedblack|italic":"RobotoSerif36ptExtraExpanded-BlackItalic","robotoserif36ptextraexpandedblack|normal":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpandedblack|regular":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpandedblack|roman":"RobotoSerif36ptExtraExpanded-Black","robotoserif36ptextraexpandedextrabold|book":"RobotoSerif36ptExtraExpanded-ExtraBold","robotoserif36ptextraexpandedextrabold|italic":"RobotoSerif36ptExtraExpanded-ExtraBoldItalic","robotoserif36ptextraexpandedextrabold|normal":"RobotoSerif36ptExtraExpanded-ExtraBold","robotoserif36ptextraexpandedextrabold|regular":"RobotoSerif36ptExtraExpanded-ExtraBold","robotoserif36ptextraexpandedextrabold|roman":"RobotoSerif36ptExtraExpanded-ExtraBold","robotoserif36ptextraexpandedextralight|book":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpandedextralight|italic":"RobotoSerif36ptExtraExpanded-ExtraLightItalic","robotoserif36ptextraexpandedextralight|normal":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpandedextralight|regular":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpandedextralight|roman":"RobotoSerif36ptExtraExpanded-ExtraLight","robotoserif36ptextraexpandedlight|book":"RobotoSerif36ptExtraExpanded-Light","robotoserif36ptextraexpandedlight|italic":"RobotoSerif36ptExtraExpanded-LightItalic","robotoserif36ptextraexpandedlight|normal":"RobotoSerif36ptExtraExpanded-Light","robotoserif36ptextraexpandedlight|regular":"RobotoSerif36ptExtraExpanded-Light","robotoserif36ptextraexpandedlight|roman":"RobotoSerif36ptExtraExpanded-Light","robotoserif36ptextraexpandedmedium|book":"RobotoSerif36ptExtraExpanded-Medium","robotoserif36ptextraexpandedmedium|italic":"RobotoSerif36ptExtraExpanded-MediumItalic","robotoserif36ptextraexpandedmedium|normal":"RobotoSerif36ptExtraExpanded-Medium","robotoserif36ptextraexpandedmedium|regular":"RobotoSerif36ptExtraExpanded-Medium","robotoserif36ptextraexpandedmedium|roman":"RobotoSerif36ptExtraExpanded-Medium","robotoserif36ptextraexpandedsemibold|book":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpandedsemibold|italic":"RobotoSerif36ptExtraExpanded-SemiBoldItalic","robotoserif36ptextraexpandedsemibold|normal":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpandedsemibold|regular":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpandedsemibold|roman":"RobotoSerif36ptExtraExpanded-SemiBold","robotoserif36ptextraexpandedthin|book":"RobotoSerif36ptExtraExpanded-Thin","robotoserif36ptextraexpandedthin|italic":"RobotoSerif36ptExtraExpanded-ThinItalic","robotoserif36ptextraexpandedthin|normal":"RobotoSerif36ptExtraExpanded-Thin","robotoserif36ptextraexpandedthin|regular":"RobotoSerif36ptExtraExpanded-Thin","robotoserif36ptextraexpandedthin|roman":"RobotoSerif36ptExtraExpanded-Thin","robotoserif36ptextralight|book":"RobotoSerif36pt-ExtraLight","robotoserif36ptextralight|italic":"RobotoSerif36pt-ExtraLightItalic","robotoserif36ptextralight|normal":"RobotoSerif36pt-ExtraLight","robotoserif36ptextralight|regular":"RobotoSerif36pt-ExtraLight","robotoserif36ptextralight|roman":"RobotoSerif36pt-ExtraLight","robotoserif36ptlight|book":"RobotoSerif36pt-Light","robotoserif36ptlight|italic":"RobotoSerif36pt-LightItalic","robotoserif36ptlight|normal":"RobotoSerif36pt-Light","robotoserif36ptlight|regular":"RobotoSerif36pt-Light","robotoserif36ptlight|roman":"RobotoSerif36pt-Light","robotoserif36ptmedium|book":"RobotoSerif36pt-Medium","robotoserif36ptmedium|italic":"RobotoSerif36pt-MediumItalic","robotoserif36ptmedium|normal":"RobotoSerif36pt-Medium","robotoserif36ptmedium|regular":"RobotoSerif36pt-Medium","robotoserif36ptmedium|roman":"RobotoSerif36pt-Medium","robotoserif36ptsemibold|book":"RobotoSerif36pt-SemiBold","robotoserif36ptsemibold|italic":"RobotoSerif36pt-SemiBoldItalic","robotoserif36ptsemibold|normal":"RobotoSerif36pt-SemiBold","robotoserif36ptsemibold|regular":"RobotoSerif36pt-SemiBold","robotoserif36ptsemibold|roman":"RobotoSerif36pt-SemiBold","robotoserif36ptsemicondensed|black":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensed|blackitalic":"RobotoSerif36ptSemiCondensed-BlackItalic","robotoserif36ptsemicondensed|bold":"RobotoSerif36ptSemiCondensed-Bold","robotoserif36ptsemicondensed|bolditalic":"RobotoSerif36ptSemiCondensed-BoldItalic","robotoserif36ptsemicondensed|book":"RobotoSerif36ptSemiCondensed-Regular","robotoserif36ptsemicondensed|demi":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensed|demibold":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensed|extrabold":"RobotoSerif36ptSemiCondensed-ExtraBold","robotoserif36ptsemicondensed|extrabolditalic":"RobotoSerif36ptSemiCondensed-ExtraBoldItalic","robotoserif36ptsemicondensed|extralight":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensed|extralightitalic":"RobotoSerif36ptSemiCondensed-ExtraLightItalic","robotoserif36ptsemicondensed|heavy":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensed|italic":"RobotoSerif36ptSemiCondensed-Italic","robotoserif36ptsemicondensed|light":"RobotoSerif36ptSemiCondensed-Light","robotoserif36ptsemicondensed|lightitalic":"RobotoSerif36ptSemiCondensed-LightItalic","robotoserif36ptsemicondensed|medium":"RobotoSerif36ptSemiCondensed-Medium","robotoserif36ptsemicondensed|mediumitalic":"RobotoSerif36ptSemiCondensed-MediumItalic","robotoserif36ptsemicondensed|normal":"RobotoSerif36ptSemiCondensed-Regular","robotoserif36ptsemicondensed|regular":"RobotoSerif36ptSemiCondensed-Regular","robotoserif36ptsemicondensed|roman":"RobotoSerif36ptSemiCondensed-Regular","robotoserif36ptsemicondensed|semibold":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensed|semibolditalic":"RobotoSerif36ptSemiCondensed-SemiBoldItalic","robotoserif36ptsemicondensed|thin":"RobotoSerif36ptSemiCondensed-Thin","robotoserif36ptsemicondensed|thinitalic":"RobotoSerif36ptSemiCondensed-ThinItalic","robotoserif36ptsemicondensed|ultralight":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensedblack|book":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensedblack|italic":"RobotoSerif36ptSemiCondensed-BlackItalic","robotoserif36ptsemicondensedblack|normal":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensedblack|regular":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensedblack|roman":"RobotoSerif36ptSemiCondensed-Black","robotoserif36ptsemicondensedextrabold|book":"RobotoSerif36ptSemiCondensed-ExtraBold","robotoserif36ptsemicondensedextrabold|italic":"RobotoSerif36ptSemiCondensed-ExtraBoldItalic","robotoserif36ptsemicondensedextrabold|normal":"RobotoSerif36ptSemiCondensed-ExtraBold","robotoserif36ptsemicondensedextrabold|regular":"RobotoSerif36ptSemiCondensed-ExtraBold","robotoserif36ptsemicondensedextrabold|roman":"RobotoSerif36ptSemiCondensed-ExtraBold","robotoserif36ptsemicondensedextralight|book":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensedextralight|italic":"RobotoSerif36ptSemiCondensed-ExtraLightItalic","robotoserif36ptsemicondensedextralight|normal":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensedextralight|regular":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensedextralight|roman":"RobotoSerif36ptSemiCondensed-ExtraLight","robotoserif36ptsemicondensedlight|book":"RobotoSerif36ptSemiCondensed-Light","robotoserif36ptsemicondensedlight|italic":"RobotoSerif36ptSemiCondensed-LightItalic","robotoserif36ptsemicondensedlight|normal":"RobotoSerif36ptSemiCondensed-Light","robotoserif36ptsemicondensedlight|regular":"RobotoSerif36ptSemiCondensed-Light","robotoserif36ptsemicondensedlight|roman":"RobotoSerif36ptSemiCondensed-Light","robotoserif36ptsemicondensedmedium|book":"RobotoSerif36ptSemiCondensed-Medium","robotoserif36ptsemicondensedmedium|italic":"RobotoSerif36ptSemiCondensed-MediumItalic","robotoserif36ptsemicondensedmedium|normal":"RobotoSerif36ptSemiCondensed-Medium","robotoserif36ptsemicondensedmedium|regular":"RobotoSerif36ptSemiCondensed-Medium","robotoserif36ptsemicondensedmedium|roman":"RobotoSerif36ptSemiCondensed-Medium","robotoserif36ptsemicondensedsemibold|book":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensedsemibold|italic":"RobotoSerif36ptSemiCondensed-SemiBoldItalic","robotoserif36ptsemicondensedsemibold|normal":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensedsemibold|regular":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensedsemibold|roman":"RobotoSerif36ptSemiCondensed-SemiBold","robotoserif36ptsemicondensedthin|book":"RobotoSerif36ptSemiCondensed-Thin","robotoserif36ptsemicondensedthin|italic":"RobotoSerif36ptSemiCondensed-ThinItalic","robotoserif36ptsemicondensedthin|normal":"RobotoSerif36ptSemiCondensed-Thin","robotoserif36ptsemicondensedthin|regular":"RobotoSerif36ptSemiCondensed-Thin","robotoserif36ptsemicondensedthin|roman":"RobotoSerif36ptSemiCondensed-Thin","robotoserif36ptsemiexpanded|black":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpanded|blackitalic":"RobotoSerif36ptSemiExpanded-BlackItalic","robotoserif36ptsemiexpanded|bold":"RobotoSerif36ptSemiExpanded-Bold","robotoserif36ptsemiexpanded|bolditalic":"RobotoSerif36ptSemiExpanded-BoldItalic","robotoserif36ptsemiexpanded|book":"RobotoSerif36ptSemiExpanded-Regular","robotoserif36ptsemiexpanded|demi":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpanded|demibold":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpanded|extrabold":"RobotoSerif36ptSemiExpanded-ExtraBold","robotoserif36ptsemiexpanded|extrabolditalic":"RobotoSerif36ptSemiExpanded-ExtraBoldItalic","robotoserif36ptsemiexpanded|extralight":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpanded|extralightitalic":"RobotoSerif36ptSemiExpanded-ExtraLightItalic","robotoserif36ptsemiexpanded|heavy":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpanded|italic":"RobotoSerif36ptSemiExpanded-Italic","robotoserif36ptsemiexpanded|light":"RobotoSerif36ptSemiExpanded-Light","robotoserif36ptsemiexpanded|lightitalic":"RobotoSerif36ptSemiExpanded-LightItalic","robotoserif36ptsemiexpanded|medium":"RobotoSerif36ptSemiExpanded-Medium","robotoserif36ptsemiexpanded|mediumitalic":"RobotoSerif36ptSemiExpanded-MediumItalic","robotoserif36ptsemiexpanded|normal":"RobotoSerif36ptSemiExpanded-Regular","robotoserif36ptsemiexpanded|regular":"RobotoSerif36ptSemiExpanded-Regular","robotoserif36ptsemiexpanded|roman":"RobotoSerif36ptSemiExpanded-Regular","robotoserif36ptsemiexpanded|semibold":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpanded|semibolditalic":"RobotoSerif36ptSemiExpanded-SemiBoldItalic","robotoserif36ptsemiexpanded|thin":"RobotoSerif36ptSemiExpanded-Thin","robotoserif36ptsemiexpanded|thinitalic":"RobotoSerif36ptSemiExpanded-ThinItalic","robotoserif36ptsemiexpanded|ultralight":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpandedblack|book":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpandedblack|italic":"RobotoSerif36ptSemiExpanded-BlackItalic","robotoserif36ptsemiexpandedblack|normal":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpandedblack|regular":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpandedblack|roman":"RobotoSerif36ptSemiExpanded-Black","robotoserif36ptsemiexpandedextrabold|book":"RobotoSerif36ptSemiExpanded-ExtraBold","robotoserif36ptsemiexpandedextrabold|italic":"RobotoSerif36ptSemiExpanded-ExtraBoldItalic","robotoserif36ptsemiexpandedextrabold|normal":"RobotoSerif36ptSemiExpanded-ExtraBold","robotoserif36ptsemiexpandedextrabold|regular":"RobotoSerif36ptSemiExpanded-ExtraBold","robotoserif36ptsemiexpandedextrabold|roman":"RobotoSerif36ptSemiExpanded-ExtraBold","robotoserif36ptsemiexpandedextralight|book":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpandedextralight|italic":"RobotoSerif36ptSemiExpanded-ExtraLightItalic","robotoserif36ptsemiexpandedextralight|normal":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpandedextralight|regular":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpandedextralight|roman":"RobotoSerif36ptSemiExpanded-ExtraLight","robotoserif36ptsemiexpandedlight|book":"RobotoSerif36ptSemiExpanded-Light","robotoserif36ptsemiexpandedlight|italic":"RobotoSerif36ptSemiExpanded-LightItalic","robotoserif36ptsemiexpandedlight|normal":"RobotoSerif36ptSemiExpanded-Light","robotoserif36ptsemiexpandedlight|regular":"RobotoSerif36ptSemiExpanded-Light","robotoserif36ptsemiexpandedlight|roman":"RobotoSerif36ptSemiExpanded-Light","robotoserif36ptsemiexpandedmedium|book":"RobotoSerif36ptSemiExpanded-Medium","robotoserif36ptsemiexpandedmedium|italic":"RobotoSerif36ptSemiExpanded-MediumItalic","robotoserif36ptsemiexpandedmedium|normal":"RobotoSerif36ptSemiExpanded-Medium","robotoserif36ptsemiexpandedmedium|regular":"RobotoSerif36ptSemiExpanded-Medium","robotoserif36ptsemiexpandedmedium|roman":"RobotoSerif36ptSemiExpanded-Medium","robotoserif36ptsemiexpandedsemibold|book":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpandedsemibold|italic":"RobotoSerif36ptSemiExpanded-SemiBoldItalic","robotoserif36ptsemiexpandedsemibold|normal":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpandedsemibold|regular":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpandedsemibold|roman":"RobotoSerif36ptSemiExpanded-SemiBold","robotoserif36ptsemiexpandedthin|book":"RobotoSerif36ptSemiExpanded-Thin","robotoserif36ptsemiexpandedthin|italic":"RobotoSerif36ptSemiExpanded-ThinItalic","robotoserif36ptsemiexpandedthin|normal":"RobotoSerif36ptSemiExpanded-Thin","robotoserif36ptsemiexpandedthin|regular":"RobotoSerif36ptSemiExpanded-Thin","robotoserif36ptsemiexpandedthin|roman":"RobotoSerif36ptSemiExpanded-Thin","robotoserif36ptthin|book":"RobotoSerif36pt-Thin","robotoserif36ptthin|italic":"RobotoSerif36pt-ThinItalic","robotoserif36ptthin|normal":"RobotoSerif36pt-Thin","robotoserif36ptthin|regular":"RobotoSerif36pt-Thin","robotoserif36ptthin|roman":"RobotoSerif36pt-Thin","robotoserif36ptultracondensed|black":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensed|blackitalic":"RobotoSerif36ptUltraCondensed-BlackItalic","robotoserif36ptultracondensed|bold":"RobotoSerif36ptUltraCondensed-Bold","robotoserif36ptultracondensed|bolditalic":"RobotoSerif36ptUltraCondensed-BoldItalic","robotoserif36ptultracondensed|book":"RobotoSerif36ptUltraCondensed-Regular","robotoserif36ptultracondensed|demi":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensed|demibold":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensed|extrabold":"RobotoSerif36ptUltraCondensed-ExtraBold","robotoserif36ptultracondensed|extrabolditalic":"RobotoSerif36ptUltraCondensed-ExtraBoldItalic","robotoserif36ptultracondensed|extralight":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensed|extralightitalic":"RobotoSerif36ptUltraCondensed-ExtraLightItalic","robotoserif36ptultracondensed|heavy":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensed|italic":"RobotoSerif36ptUltraCondensed-Italic","robotoserif36ptultracondensed|light":"RobotoSerif36ptUltraCondensed-Light","robotoserif36ptultracondensed|lightitalic":"RobotoSerif36ptUltraCondensed-LightItalic","robotoserif36ptultracondensed|medium":"RobotoSerif36ptUltraCondensed-Medium","robotoserif36ptultracondensed|mediumitalic":"RobotoSerif36ptUltraCondensed-MediumItalic","robotoserif36ptultracondensed|normal":"RobotoSerif36ptUltraCondensed-Regular","robotoserif36ptultracondensed|regular":"RobotoSerif36ptUltraCondensed-Regular","robotoserif36ptultracondensed|roman":"RobotoSerif36ptUltraCondensed-Regular","robotoserif36ptultracondensed|semibold":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensed|semibolditalic":"RobotoSerif36ptUltraCondensed-SemiBoldItalic","robotoserif36ptultracondensed|thin":"RobotoSerif36ptUltraCondensed-Thin","robotoserif36ptultracondensed|thinitalic":"RobotoSerif36ptUltraCondensed-ThinItalic","robotoserif36ptultracondensed|ultralight":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensedblack|book":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensedblack|italic":"RobotoSerif36ptUltraCondensed-BlackItalic","robotoserif36ptultracondensedblack|normal":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensedblack|regular":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensedblack|roman":"RobotoSerif36ptUltraCondensed-Black","robotoserif36ptultracondensedextrabold|book":"RobotoSerif36ptUltraCondensed-ExtraBold","robotoserif36ptultracondensedextrabold|italic":"RobotoSerif36ptUltraCondensed-ExtraBoldItalic","robotoserif36ptultracondensedextrabold|normal":"RobotoSerif36ptUltraCondensed-ExtraBold","robotoserif36ptultracondensedextrabold|regular":"RobotoSerif36ptUltraCondensed-ExtraBold","robotoserif36ptultracondensedextrabold|roman":"RobotoSerif36ptUltraCondensed-ExtraBold","robotoserif36ptultracondensedextralight|book":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensedextralight|italic":"RobotoSerif36ptUltraCondensed-ExtraLightItalic","robotoserif36ptultracondensedextralight|normal":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensedextralight|regular":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensedextralight|roman":"RobotoSerif36ptUltraCondensed-ExtraLight","robotoserif36ptultracondensedlight|book":"RobotoSerif36ptUltraCondensed-Light","robotoserif36ptultracondensedlight|italic":"RobotoSerif36ptUltraCondensed-LightItalic","robotoserif36ptultracondensedlight|normal":"RobotoSerif36ptUltraCondensed-Light","robotoserif36ptultracondensedlight|regular":"RobotoSerif36ptUltraCondensed-Light","robotoserif36ptultracondensedlight|roman":"RobotoSerif36ptUltraCondensed-Light","robotoserif36ptultracondensedmedium|book":"RobotoSerif36ptUltraCondensed-Medium","robotoserif36ptultracondensedmedium|italic":"RobotoSerif36ptUltraCondensed-MediumItalic","robotoserif36ptultracondensedmedium|normal":"RobotoSerif36ptUltraCondensed-Medium","robotoserif36ptultracondensedmedium|regular":"RobotoSerif36ptUltraCondensed-Medium","robotoserif36ptultracondensedmedium|roman":"RobotoSerif36ptUltraCondensed-Medium","robotoserif36ptultracondensedsemibold|book":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensedsemibold|italic":"RobotoSerif36ptUltraCondensed-SemiBoldItalic","robotoserif36ptultracondensedsemibold|normal":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensedsemibold|regular":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensedsemibold|roman":"RobotoSerif36ptUltraCondensed-SemiBold","robotoserif36ptultracondensedthin|book":"RobotoSerif36ptUltraCondensed-Thin","robotoserif36ptultracondensedthin|italic":"RobotoSerif36ptUltraCondensed-ThinItalic","robotoserif36ptultracondensedthin|normal":"RobotoSerif36ptUltraCondensed-Thin","robotoserif36ptultracondensedthin|regular":"RobotoSerif36ptUltraCondensed-Thin","robotoserif36ptultracondensedthin|roman":"RobotoSerif36ptUltraCondensed-Thin","robotoserif72pt|black":"RobotoSerif72pt-Black","robotoserif72pt|blackitalic":"RobotoSerif72pt-BlackItalic","robotoserif72pt|bold":"RobotoSerif72pt-Bold","robotoserif72pt|bolditalic":"RobotoSerif72pt-BoldItalic","robotoserif72pt|book":"RobotoSerif72pt-Regular","robotoserif72pt|demi":"RobotoSerif72pt-SemiBold","robotoserif72pt|demibold":"RobotoSerif72pt-SemiBold","robotoserif72pt|extrabold":"RobotoSerif72pt-ExtraBold","robotoserif72pt|extrabolditalic":"RobotoSerif72pt-ExtraBoldItalic","robotoserif72pt|extralight":"RobotoSerif72pt-ExtraLight","robotoserif72pt|extralightitalic":"RobotoSerif72pt-ExtraLightItalic","robotoserif72pt|heavy":"RobotoSerif72pt-Black","robotoserif72pt|italic":"RobotoSerif72pt-Italic","robotoserif72pt|light":"RobotoSerif72pt-Light","robotoserif72pt|lightitalic":"RobotoSerif72pt-LightItalic","robotoserif72pt|medium":"RobotoSerif72pt-Medium","robotoserif72pt|mediumitalic":"RobotoSerif72pt-MediumItalic","robotoserif72pt|normal":"RobotoSerif72pt-Regular","robotoserif72pt|regular":"RobotoSerif72pt-Regular","robotoserif72pt|roman":"RobotoSerif72pt-Regular","robotoserif72pt|semibold":"RobotoSerif72pt-SemiBold","robotoserif72pt|semibolditalic":"RobotoSerif72pt-SemiBoldItalic","robotoserif72pt|thin":"RobotoSerif72pt-Thin","robotoserif72pt|thinitalic":"RobotoSerif72pt-ThinItalic","robotoserif72pt|ultralight":"RobotoSerif72pt-ExtraLight","robotoserif72ptblack|book":"RobotoSerif72pt-Black","robotoserif72ptblack|italic":"RobotoSerif72pt-BlackItalic","robotoserif72ptblack|normal":"RobotoSerif72pt-Black","robotoserif72ptblack|regular":"RobotoSerif72pt-Black","robotoserif72ptblack|roman":"RobotoSerif72pt-Black","robotoserif72ptcondensed|black":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensed|blackitalic":"RobotoSerif72ptCondensed-BlackItalic","robotoserif72ptcondensed|bold":"RobotoSerif72ptCondensed-Bold","robotoserif72ptcondensed|bolditalic":"RobotoSerif72ptCondensed-BoldItalic","robotoserif72ptcondensed|book":"RobotoSerif72ptCondensed-Regular","robotoserif72ptcondensed|demi":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensed|demibold":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensed|extrabold":"RobotoSerif72ptCondensed-ExtraBold","robotoserif72ptcondensed|extrabolditalic":"RobotoSerif72ptCondensed-ExtraBoldItalic","robotoserif72ptcondensed|extralight":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensed|extralightitalic":"RobotoSerif72ptCondensed-ExtraLightItalic","robotoserif72ptcondensed|heavy":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensed|italic":"RobotoSerif72ptCondensed-Italic","robotoserif72ptcondensed|light":"RobotoSerif72ptCondensed-Light","robotoserif72ptcondensed|lightitalic":"RobotoSerif72ptCondensed-LightItalic","robotoserif72ptcondensed|medium":"RobotoSerif72ptCondensed-Medium","robotoserif72ptcondensed|mediumitalic":"RobotoSerif72ptCondensed-MediumItalic","robotoserif72ptcondensed|normal":"RobotoSerif72ptCondensed-Regular","robotoserif72ptcondensed|regular":"RobotoSerif72ptCondensed-Regular","robotoserif72ptcondensed|roman":"RobotoSerif72ptCondensed-Regular","robotoserif72ptcondensed|semibold":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensed|semibolditalic":"RobotoSerif72ptCondensed-SemiBoldItalic","robotoserif72ptcondensed|thin":"RobotoSerif72ptCondensed-Thin","robotoserif72ptcondensed|thinitalic":"RobotoSerif72ptCondensed-ThinItalic","robotoserif72ptcondensed|ultralight":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensedblack|book":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensedblack|italic":"RobotoSerif72ptCondensed-BlackItalic","robotoserif72ptcondensedblack|normal":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensedblack|regular":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensedblack|roman":"RobotoSerif72ptCondensed-Black","robotoserif72ptcondensedextrabold|book":"RobotoSerif72ptCondensed-ExtraBold","robotoserif72ptcondensedextrabold|italic":"RobotoSerif72ptCondensed-ExtraBoldItalic","robotoserif72ptcondensedextrabold|normal":"RobotoSerif72ptCondensed-ExtraBold","robotoserif72ptcondensedextrabold|regular":"RobotoSerif72ptCondensed-ExtraBold","robotoserif72ptcondensedextrabold|roman":"RobotoSerif72ptCondensed-ExtraBold","robotoserif72ptcondensedextralight|book":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensedextralight|italic":"RobotoSerif72ptCondensed-ExtraLightItalic","robotoserif72ptcondensedextralight|normal":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensedextralight|regular":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensedextralight|roman":"RobotoSerif72ptCondensed-ExtraLight","robotoserif72ptcondensedlight|book":"RobotoSerif72ptCondensed-Light","robotoserif72ptcondensedlight|italic":"RobotoSerif72ptCondensed-LightItalic","robotoserif72ptcondensedlight|normal":"RobotoSerif72ptCondensed-Light","robotoserif72ptcondensedlight|regular":"RobotoSerif72ptCondensed-Light","robotoserif72ptcondensedlight|roman":"RobotoSerif72ptCondensed-Light","robotoserif72ptcondensedmedium|book":"RobotoSerif72ptCondensed-Medium","robotoserif72ptcondensedmedium|italic":"RobotoSerif72ptCondensed-MediumItalic","robotoserif72ptcondensedmedium|normal":"RobotoSerif72ptCondensed-Medium","robotoserif72ptcondensedmedium|regular":"RobotoSerif72ptCondensed-Medium","robotoserif72ptcondensedmedium|roman":"RobotoSerif72ptCondensed-Medium","robotoserif72ptcondensedsemibold|book":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensedsemibold|italic":"RobotoSerif72ptCondensed-SemiBoldItalic","robotoserif72ptcondensedsemibold|normal":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensedsemibold|regular":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensedsemibold|roman":"RobotoSerif72ptCondensed-SemiBold","robotoserif72ptcondensedthin|book":"RobotoSerif72ptCondensed-Thin","robotoserif72ptcondensedthin|italic":"RobotoSerif72ptCondensed-ThinItalic","robotoserif72ptcondensedthin|normal":"RobotoSerif72ptCondensed-Thin","robotoserif72ptcondensedthin|regular":"RobotoSerif72ptCondensed-Thin","robotoserif72ptcondensedthin|roman":"RobotoSerif72ptCondensed-Thin","robotoserif72ptexpanded|black":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpanded|blackitalic":"RobotoSerif72ptExpanded-BlackItalic","robotoserif72ptexpanded|bold":"RobotoSerif72ptExpanded-Bold","robotoserif72ptexpanded|bolditalic":"RobotoSerif72ptExpanded-BoldItalic","robotoserif72ptexpanded|book":"RobotoSerif72ptExpanded-Regular","robotoserif72ptexpanded|demi":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpanded|demibold":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpanded|extrabold":"RobotoSerif72ptExpanded-ExtraBold","robotoserif72ptexpanded|extrabolditalic":"RobotoSerif72ptExpanded-ExtraBoldItalic","robotoserif72ptexpanded|extralight":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpanded|extralightitalic":"RobotoSerif72ptExpanded-ExtraLightItalic","robotoserif72ptexpanded|heavy":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpanded|italic":"RobotoSerif72ptExpanded-Italic","robotoserif72ptexpanded|light":"RobotoSerif72ptExpanded-Light","robotoserif72ptexpanded|lightitalic":"RobotoSerif72ptExpanded-LightItalic","robotoserif72ptexpanded|medium":"RobotoSerif72ptExpanded-Medium","robotoserif72ptexpanded|mediumitalic":"RobotoSerif72ptExpanded-MediumItalic","robotoserif72ptexpanded|normal":"RobotoSerif72ptExpanded-Regular","robotoserif72ptexpanded|regular":"RobotoSerif72ptExpanded-Regular","robotoserif72ptexpanded|roman":"RobotoSerif72ptExpanded-Regular","robotoserif72ptexpanded|semibold":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpanded|semibolditalic":"RobotoSerif72ptExpanded-SemiBoldItalic","robotoserif72ptexpanded|thin":"RobotoSerif72ptExpanded-Thin","robotoserif72ptexpanded|thinitalic":"RobotoSerif72ptExpanded-ThinItalic","robotoserif72ptexpanded|ultralight":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpandedblack|book":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpandedblack|italic":"RobotoSerif72ptExpanded-BlackItalic","robotoserif72ptexpandedblack|normal":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpandedblack|regular":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpandedblack|roman":"RobotoSerif72ptExpanded-Black","robotoserif72ptexpandedextrabold|book":"RobotoSerif72ptExpanded-ExtraBold","robotoserif72ptexpandedextrabold|italic":"RobotoSerif72ptExpanded-ExtraBoldItalic","robotoserif72ptexpandedextrabold|normal":"RobotoSerif72ptExpanded-ExtraBold","robotoserif72ptexpandedextrabold|regular":"RobotoSerif72ptExpanded-ExtraBold","robotoserif72ptexpandedextrabold|roman":"RobotoSerif72ptExpanded-ExtraBold","robotoserif72ptexpandedextralight|book":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpandedextralight|italic":"RobotoSerif72ptExpanded-ExtraLightItalic","robotoserif72ptexpandedextralight|normal":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpandedextralight|regular":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpandedextralight|roman":"RobotoSerif72ptExpanded-ExtraLight","robotoserif72ptexpandedlight|book":"RobotoSerif72ptExpanded-Light","robotoserif72ptexpandedlight|italic":"RobotoSerif72ptExpanded-LightItalic","robotoserif72ptexpandedlight|normal":"RobotoSerif72ptExpanded-Light","robotoserif72ptexpandedlight|regular":"RobotoSerif72ptExpanded-Light","robotoserif72ptexpandedlight|roman":"RobotoSerif72ptExpanded-Light","robotoserif72ptexpandedmedium|book":"RobotoSerif72ptExpanded-Medium","robotoserif72ptexpandedmedium|italic":"RobotoSerif72ptExpanded-MediumItalic","robotoserif72ptexpandedmedium|normal":"RobotoSerif72ptExpanded-Medium","robotoserif72ptexpandedmedium|regular":"RobotoSerif72ptExpanded-Medium","robotoserif72ptexpandedmedium|roman":"RobotoSerif72ptExpanded-Medium","robotoserif72ptexpandedsemibold|book":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpandedsemibold|italic":"RobotoSerif72ptExpanded-SemiBoldItalic","robotoserif72ptexpandedsemibold|normal":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpandedsemibold|regular":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpandedsemibold|roman":"RobotoSerif72ptExpanded-SemiBold","robotoserif72ptexpandedthin|book":"RobotoSerif72ptExpanded-Thin","robotoserif72ptexpandedthin|italic":"RobotoSerif72ptExpanded-ThinItalic","robotoserif72ptexpandedthin|normal":"RobotoSerif72ptExpanded-Thin","robotoserif72ptexpandedthin|regular":"RobotoSerif72ptExpanded-Thin","robotoserif72ptexpandedthin|roman":"RobotoSerif72ptExpanded-Thin","robotoserif72ptextrabold|book":"RobotoSerif72pt-ExtraBold","robotoserif72ptextrabold|italic":"RobotoSerif72pt-ExtraBoldItalic","robotoserif72ptextrabold|normal":"RobotoSerif72pt-ExtraBold","robotoserif72ptextrabold|regular":"RobotoSerif72pt-ExtraBold","robotoserif72ptextrabold|roman":"RobotoSerif72pt-ExtraBold","robotoserif72ptextracondensed|black":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensed|blackitalic":"RobotoSerif72ptExtraCondensed-BlackItalic","robotoserif72ptextracondensed|bold":"RobotoSerif72ptExtraCondensed-Bold","robotoserif72ptextracondensed|bolditalic":"RobotoSerif72ptExtraCondensed-BoldItalic","robotoserif72ptextracondensed|book":"RobotoSerif72ptExtraCondensed-Regular","robotoserif72ptextracondensed|demi":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensed|demibold":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensed|extrabold":"RobotoSerif72ptExtraCondensed-ExtraBold","robotoserif72ptextracondensed|extrabolditalic":"RobotoSerif72ptExtraCondensed-ExtraBoldItalic","robotoserif72ptextracondensed|extralight":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensed|extralightitalic":"RobotoSerif72ptExtraCondensed-ExtraLightItalic","robotoserif72ptextracondensed|heavy":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensed|italic":"RobotoSerif72ptExtraCondensed-Italic","robotoserif72ptextracondensed|light":"RobotoSerif72ptExtraCondensed-Light","robotoserif72ptextracondensed|lightitalic":"RobotoSerif72ptExtraCondensed-LightItalic","robotoserif72ptextracondensed|medium":"RobotoSerif72ptExtraCondensed-Medium","robotoserif72ptextracondensed|mediumitalic":"RobotoSerif72ptExtraCondensed-MediumItalic","robotoserif72ptextracondensed|normal":"RobotoSerif72ptExtraCondensed-Regular","robotoserif72ptextracondensed|regular":"RobotoSerif72ptExtraCondensed-Regular","robotoserif72ptextracondensed|roman":"RobotoSerif72ptExtraCondensed-Regular","robotoserif72ptextracondensed|semibold":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensed|semibolditalic":"RobotoSerif72ptExtraCondensed-SemiBoldItalic","robotoserif72ptextracondensed|thin":"RobotoSerif72ptExtraCondensed-Thin","robotoserif72ptextracondensed|thinitalic":"RobotoSerif72ptExtraCondensed-ThinItalic","robotoserif72ptextracondensed|ultralight":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensedblack|book":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensedblack|italic":"RobotoSerif72ptExtraCondensed-BlackItalic","robotoserif72ptextracondensedblack|normal":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensedblack|regular":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensedblack|roman":"RobotoSerif72ptExtraCondensed-Black","robotoserif72ptextracondensedextrabold|book":"RobotoSerif72ptExtraCondensed-ExtraBold","robotoserif72ptextracondensedextrabold|italic":"RobotoSerif72ptExtraCondensed-ExtraBoldItalic","robotoserif72ptextracondensedextrabold|normal":"RobotoSerif72ptExtraCondensed-ExtraBold","robotoserif72ptextracondensedextrabold|regular":"RobotoSerif72ptExtraCondensed-ExtraBold","robotoserif72ptextracondensedextrabold|roman":"RobotoSerif72ptExtraCondensed-ExtraBold","robotoserif72ptextracondensedextralight|book":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensedextralight|italic":"RobotoSerif72ptExtraCondensed-ExtraLightItalic","robotoserif72ptextracondensedextralight|normal":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensedextralight|regular":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensedextralight|roman":"RobotoSerif72ptExtraCondensed-ExtraLight","robotoserif72ptextracondensedlight|book":"RobotoSerif72ptExtraCondensed-Light","robotoserif72ptextracondensedlight|italic":"RobotoSerif72ptExtraCondensed-LightItalic","robotoserif72ptextracondensedlight|normal":"RobotoSerif72ptExtraCondensed-Light","robotoserif72ptextracondensedlight|regular":"RobotoSerif72ptExtraCondensed-Light","robotoserif72ptextracondensedlight|roman":"RobotoSerif72ptExtraCondensed-Light","robotoserif72ptextracondensedmedium|book":"RobotoSerif72ptExtraCondensed-Medium","robotoserif72ptextracondensedmedium|italic":"RobotoSerif72ptExtraCondensed-MediumItalic","robotoserif72ptextracondensedmedium|normal":"RobotoSerif72ptExtraCondensed-Medium","robotoserif72ptextracondensedmedium|regular":"RobotoSerif72ptExtraCondensed-Medium","robotoserif72ptextracondensedmedium|roman":"RobotoSerif72ptExtraCondensed-Medium","robotoserif72ptextracondensedsemibold|book":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensedsemibold|italic":"RobotoSerif72ptExtraCondensed-SemiBoldItalic","robotoserif72ptextracondensedsemibold|normal":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensedsemibold|regular":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensedsemibold|roman":"RobotoSerif72ptExtraCondensed-SemiBold","robotoserif72ptextracondensedthin|book":"RobotoSerif72ptExtraCondensed-Thin","robotoserif72ptextracondensedthin|italic":"RobotoSerif72ptExtraCondensed-ThinItalic","robotoserif72ptextracondensedthin|normal":"RobotoSerif72ptExtraCondensed-Thin","robotoserif72ptextracondensedthin|regular":"RobotoSerif72ptExtraCondensed-Thin","robotoserif72ptextracondensedthin|roman":"RobotoSerif72ptExtraCondensed-Thin","robotoserif72ptextraexpanded|black":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpanded|blackitalic":"RobotoSerif72ptExtraExpanded-BlackItalic","robotoserif72ptextraexpanded|bold":"RobotoSerif72ptExtraExpanded-Bold","robotoserif72ptextraexpanded|bolditalic":"RobotoSerif72ptExtraExpanded-BoldItalic","robotoserif72ptextraexpanded|book":"RobotoSerif72ptExtraExpanded-Regular","robotoserif72ptextraexpanded|demi":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpanded|demibold":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpanded|extrabold":"RobotoSerif72ptExtraExpanded-ExtraBold","robotoserif72ptextraexpanded|extrabolditalic":"RobotoSerif72ptExtraExpanded-ExtraBoldItalic","robotoserif72ptextraexpanded|extralight":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpanded|extralightitalic":"RobotoSerif72ptExtraExpanded-ExtraLightItalic","robotoserif72ptextraexpanded|heavy":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpanded|italic":"RobotoSerif72ptExtraExpanded-Italic","robotoserif72ptextraexpanded|light":"RobotoSerif72ptExtraExpanded-Light","robotoserif72ptextraexpanded|lightitalic":"RobotoSerif72ptExtraExpanded-LightItalic","robotoserif72ptextraexpanded|medium":"RobotoSerif72ptExtraExpanded-Medium","robotoserif72ptextraexpanded|mediumitalic":"RobotoSerif72ptExtraExpanded-MediumItalic","robotoserif72ptextraexpanded|normal":"RobotoSerif72ptExtraExpanded-Regular","robotoserif72ptextraexpanded|regular":"RobotoSerif72ptExtraExpanded-Regular","robotoserif72ptextraexpanded|roman":"RobotoSerif72ptExtraExpanded-Regular","robotoserif72ptextraexpanded|semibold":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpanded|semibolditalic":"RobotoSerif72ptExtraExpanded-SemiBoldItalic","robotoserif72ptextraexpanded|thin":"RobotoSerif72ptExtraExpanded-Thin","robotoserif72ptextraexpanded|thinitalic":"RobotoSerif72ptExtraExpanded-ThinItalic","robotoserif72ptextraexpanded|ultralight":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpandedblack|book":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpandedblack|italic":"RobotoSerif72ptExtraExpanded-BlackItalic","robotoserif72ptextraexpandedblack|normal":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpandedblack|regular":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpandedblack|roman":"RobotoSerif72ptExtraExpanded-Black","robotoserif72ptextraexpandedextrabold|book":"RobotoSerif72ptExtraExpanded-ExtraBold","robotoserif72ptextraexpandedextrabold|italic":"RobotoSerif72ptExtraExpanded-ExtraBoldItalic","robotoserif72ptextraexpandedextrabold|normal":"RobotoSerif72ptExtraExpanded-ExtraBold","robotoserif72ptextraexpandedextrabold|regular":"RobotoSerif72ptExtraExpanded-ExtraBold","robotoserif72ptextraexpandedextrabold|roman":"RobotoSerif72ptExtraExpanded-ExtraBold","robotoserif72ptextraexpandedextralight|book":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpandedextralight|italic":"RobotoSerif72ptExtraExpanded-ExtraLightItalic","robotoserif72ptextraexpandedextralight|normal":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpandedextralight|regular":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpandedextralight|roman":"RobotoSerif72ptExtraExpanded-ExtraLight","robotoserif72ptextraexpandedlight|book":"RobotoSerif72ptExtraExpanded-Light","robotoserif72ptextraexpandedlight|italic":"RobotoSerif72ptExtraExpanded-LightItalic","robotoserif72ptextraexpandedlight|normal":"RobotoSerif72ptExtraExpanded-Light","robotoserif72ptextraexpandedlight|regular":"RobotoSerif72ptExtraExpanded-Light","robotoserif72ptextraexpandedlight|roman":"RobotoSerif72ptExtraExpanded-Light","robotoserif72ptextraexpandedmedium|book":"RobotoSerif72ptExtraExpanded-Medium","robotoserif72ptextraexpandedmedium|italic":"RobotoSerif72ptExtraExpanded-MediumItalic","robotoserif72ptextraexpandedmedium|normal":"RobotoSerif72ptExtraExpanded-Medium","robotoserif72ptextraexpandedmedium|regular":"RobotoSerif72ptExtraExpanded-Medium","robotoserif72ptextraexpandedmedium|roman":"RobotoSerif72ptExtraExpanded-Medium","robotoserif72ptextraexpandedsemibold|book":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpandedsemibold|italic":"RobotoSerif72ptExtraExpanded-SemiBoldItalic","robotoserif72ptextraexpandedsemibold|normal":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpandedsemibold|regular":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpandedsemibold|roman":"RobotoSerif72ptExtraExpanded-SemiBold","robotoserif72ptextraexpandedthin|book":"RobotoSerif72ptExtraExpanded-Thin","robotoserif72ptextraexpandedthin|italic":"RobotoSerif72ptExtraExpanded-ThinItalic","robotoserif72ptextraexpandedthin|normal":"RobotoSerif72ptExtraExpanded-Thin","robotoserif72ptextraexpandedthin|regular":"RobotoSerif72ptExtraExpanded-Thin","robotoserif72ptextraexpandedthin|roman":"RobotoSerif72ptExtraExpanded-Thin","robotoserif72ptextralight|book":"RobotoSerif72pt-ExtraLight","robotoserif72ptextralight|italic":"RobotoSerif72pt-ExtraLightItalic","robotoserif72ptextralight|normal":"RobotoSerif72pt-ExtraLight","robotoserif72ptextralight|regular":"RobotoSerif72pt-ExtraLight","robotoserif72ptextralight|roman":"RobotoSerif72pt-ExtraLight","robotoserif72ptlight|book":"RobotoSerif72pt-Light","robotoserif72ptlight|italic":"RobotoSerif72pt-LightItalic","robotoserif72ptlight|normal":"RobotoSerif72pt-Light","robotoserif72ptlight|regular":"RobotoSerif72pt-Light","robotoserif72ptlight|roman":"RobotoSerif72pt-Light","robotoserif72ptmedium|book":"RobotoSerif72pt-Medium","robotoserif72ptmedium|italic":"RobotoSerif72pt-MediumItalic","robotoserif72ptmedium|normal":"RobotoSerif72pt-Medium","robotoserif72ptmedium|regular":"RobotoSerif72pt-Medium","robotoserif72ptmedium|roman":"RobotoSerif72pt-Medium","robotoserif72ptsemibold|book":"RobotoSerif72pt-SemiBold","robotoserif72ptsemibold|italic":"RobotoSerif72pt-SemiBoldItalic","robotoserif72ptsemibold|normal":"RobotoSerif72pt-SemiBold","robotoserif72ptsemibold|regular":"RobotoSerif72pt-SemiBold","robotoserif72ptsemibold|roman":"RobotoSerif72pt-SemiBold","robotoserif72ptsemicondensed|black":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensed|blackitalic":"RobotoSerif72ptSemiCondensed-BlackItalic","robotoserif72ptsemicondensed|bold":"RobotoSerif72ptSemiCondensed-Bold","robotoserif72ptsemicondensed|bolditalic":"RobotoSerif72ptSemiCondensed-BoldItalic","robotoserif72ptsemicondensed|book":"RobotoSerif72ptSemiCondensed-Regular","robotoserif72ptsemicondensed|demi":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensed|demibold":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensed|extrabold":"RobotoSerif72ptSemiCondensed-ExtraBold","robotoserif72ptsemicondensed|extrabolditalic":"RobotoSerif72ptSemiCondensed-ExtraBoldItalic","robotoserif72ptsemicondensed|extralight":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensed|extralightitalic":"RobotoSerif72ptSemiCondensed-ExtraLightItalic","robotoserif72ptsemicondensed|heavy":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensed|italic":"RobotoSerif72ptSemiCondensed-Italic","robotoserif72ptsemicondensed|light":"RobotoSerif72ptSemiCondensed-Light","robotoserif72ptsemicondensed|lightitalic":"RobotoSerif72ptSemiCondensed-LightItalic","robotoserif72ptsemicondensed|medium":"RobotoSerif72ptSemiCondensed-Medium","robotoserif72ptsemicondensed|mediumitalic":"RobotoSerif72ptSemiCondensed-MediumItalic","robotoserif72ptsemicondensed|normal":"RobotoSerif72ptSemiCondensed-Regular","robotoserif72ptsemicondensed|regular":"RobotoSerif72ptSemiCondensed-Regular","robotoserif72ptsemicondensed|roman":"RobotoSerif72ptSemiCondensed-Regular","robotoserif72ptsemicondensed|semibold":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensed|semibolditalic":"RobotoSerif72ptSemiCondensed-SemiBoldItalic","robotoserif72ptsemicondensed|thin":"RobotoSerif72ptSemiCondensed-Thin","robotoserif72ptsemicondensed|thinitalic":"RobotoSerif72ptSemiCondensed-ThinItalic","robotoserif72ptsemicondensed|ultralight":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensedblack|book":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensedblack|italic":"RobotoSerif72ptSemiCondensed-BlackItalic","robotoserif72ptsemicondensedblack|normal":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensedblack|regular":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensedblack|roman":"RobotoSerif72ptSemiCondensed-Black","robotoserif72ptsemicondensedextrabold|book":"RobotoSerif72ptSemiCondensed-ExtraBold","robotoserif72ptsemicondensedextrabold|italic":"RobotoSerif72ptSemiCondensed-ExtraBoldItalic","robotoserif72ptsemicondensedextrabold|normal":"RobotoSerif72ptSemiCondensed-ExtraBold","robotoserif72ptsemicondensedextrabold|regular":"RobotoSerif72ptSemiCondensed-ExtraBold","robotoserif72ptsemicondensedextrabold|roman":"RobotoSerif72ptSemiCondensed-ExtraBold","robotoserif72ptsemicondensedextralight|book":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensedextralight|italic":"RobotoSerif72ptSemiCondensed-ExtraLightItalic","robotoserif72ptsemicondensedextralight|normal":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensedextralight|regular":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensedextralight|roman":"RobotoSerif72ptSemiCondensed-ExtraLight","robotoserif72ptsemicondensedlight|book":"RobotoSerif72ptSemiCondensed-Light","robotoserif72ptsemicondensedlight|italic":"RobotoSerif72ptSemiCondensed-LightItalic","robotoserif72ptsemicondensedlight|normal":"RobotoSerif72ptSemiCondensed-Light","robotoserif72ptsemicondensedlight|regular":"RobotoSerif72ptSemiCondensed-Light","robotoserif72ptsemicondensedlight|roman":"RobotoSerif72ptSemiCondensed-Light","robotoserif72ptsemicondensedmedium|book":"RobotoSerif72ptSemiCondensed-Medium","robotoserif72ptsemicondensedmedium|italic":"RobotoSerif72ptSemiCondensed-MediumItalic","robotoserif72ptsemicondensedmedium|normal":"RobotoSerif72ptSemiCondensed-Medium","robotoserif72ptsemicondensedmedium|regular":"RobotoSerif72ptSemiCondensed-Medium","robotoserif72ptsemicondensedmedium|roman":"RobotoSerif72ptSemiCondensed-Medium","robotoserif72ptsemicondensedsemibold|book":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensedsemibold|italic":"RobotoSerif72ptSemiCondensed-SemiBoldItalic","robotoserif72ptsemicondensedsemibold|normal":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensedsemibold|regular":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensedsemibold|roman":"RobotoSerif72ptSemiCondensed-SemiBold","robotoserif72ptsemicondensedthin|book":"RobotoSerif72ptSemiCondensed-Thin","robotoserif72ptsemicondensedthin|italic":"RobotoSerif72ptSemiCondensed-ThinItalic","robotoserif72ptsemicondensedthin|normal":"RobotoSerif72ptSemiCondensed-Thin","robotoserif72ptsemicondensedthin|regular":"RobotoSerif72ptSemiCondensed-Thin","robotoserif72ptsemicondensedthin|roman":"RobotoSerif72ptSemiCondensed-Thin","robotoserif72ptsemiexpanded|black":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpanded|blackitalic":"RobotoSerif72ptSemiExpanded-BlackItalic","robotoserif72ptsemiexpanded|bold":"RobotoSerif72ptSemiExpanded-Bold","robotoserif72ptsemiexpanded|bolditalic":"RobotoSerif72ptSemiExpanded-BoldItalic","robotoserif72ptsemiexpanded|book":"RobotoSerif72ptSemiExpanded-Regular","robotoserif72ptsemiexpanded|demi":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpanded|demibold":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpanded|extrabold":"RobotoSerif72ptSemiExpanded-ExtraBold","robotoserif72ptsemiexpanded|extrabolditalic":"RobotoSerif72ptSemiExpanded-ExtraBoldItalic","robotoserif72ptsemiexpanded|extralight":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpanded|extralightitalic":"RobotoSerif72ptSemiExpanded-ExtraLightItalic","robotoserif72ptsemiexpanded|heavy":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpanded|italic":"RobotoSerif72ptSemiExpanded-Italic","robotoserif72ptsemiexpanded|light":"RobotoSerif72ptSemiExpanded-Light","robotoserif72ptsemiexpanded|lightitalic":"RobotoSerif72ptSemiExpanded-LightItalic","robotoserif72ptsemiexpanded|medium":"RobotoSerif72ptSemiExpanded-Medium","robotoserif72ptsemiexpanded|mediumitalic":"RobotoSerif72ptSemiExpanded-MediumItalic","robotoserif72ptsemiexpanded|normal":"RobotoSerif72ptSemiExpanded-Regular","robotoserif72ptsemiexpanded|regular":"RobotoSerif72ptSemiExpanded-Regular","robotoserif72ptsemiexpanded|roman":"RobotoSerif72ptSemiExpanded-Regular","robotoserif72ptsemiexpanded|semibold":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpanded|semibolditalic":"RobotoSerif72ptSemiExpanded-SemiBoldItalic","robotoserif72ptsemiexpanded|thin":"RobotoSerif72ptSemiExpanded-Thin","robotoserif72ptsemiexpanded|thinitalic":"RobotoSerif72ptSemiExpanded-ThinItalic","robotoserif72ptsemiexpanded|ultralight":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpandedblack|book":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpandedblack|italic":"RobotoSerif72ptSemiExpanded-BlackItalic","robotoserif72ptsemiexpandedblack|normal":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpandedblack|regular":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpandedblack|roman":"RobotoSerif72ptSemiExpanded-Black","robotoserif72ptsemiexpandedextrabold|book":"RobotoSerif72ptSemiExpanded-ExtraBold","robotoserif72ptsemiexpandedextrabold|italic":"RobotoSerif72ptSemiExpanded-ExtraBoldItalic","robotoserif72ptsemiexpandedextrabold|normal":"RobotoSerif72ptSemiExpanded-ExtraBold","robotoserif72ptsemiexpandedextrabold|regular":"RobotoSerif72ptSemiExpanded-ExtraBold","robotoserif72ptsemiexpandedextrabold|roman":"RobotoSerif72ptSemiExpanded-ExtraBold","robotoserif72ptsemiexpandedextralight|book":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpandedextralight|italic":"RobotoSerif72ptSemiExpanded-ExtraLightItalic","robotoserif72ptsemiexpandedextralight|normal":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpandedextralight|regular":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpandedextralight|roman":"RobotoSerif72ptSemiExpanded-ExtraLight","robotoserif72ptsemiexpandedlight|book":"RobotoSerif72ptSemiExpanded-Light","robotoserif72ptsemiexpandedlight|italic":"RobotoSerif72ptSemiExpanded-LightItalic","robotoserif72ptsemiexpandedlight|normal":"RobotoSerif72ptSemiExpanded-Light","robotoserif72ptsemiexpandedlight|regular":"RobotoSerif72ptSemiExpanded-Light","robotoserif72ptsemiexpandedlight|roman":"RobotoSerif72ptSemiExpanded-Light","robotoserif72ptsemiexpandedmedium|book":"RobotoSerif72ptSemiExpanded-Medium","robotoserif72ptsemiexpandedmedium|italic":"RobotoSerif72ptSemiExpanded-MediumItalic","robotoserif72ptsemiexpandedmedium|normal":"RobotoSerif72ptSemiExpanded-Medium","robotoserif72ptsemiexpandedmedium|regular":"RobotoSerif72ptSemiExpanded-Medium","robotoserif72ptsemiexpandedmedium|roman":"RobotoSerif72ptSemiExpanded-Medium","robotoserif72ptsemiexpandedsemibold|book":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpandedsemibold|italic":"RobotoSerif72ptSemiExpanded-SemiBoldItalic","robotoserif72ptsemiexpandedsemibold|normal":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpandedsemibold|regular":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpandedsemibold|roman":"RobotoSerif72ptSemiExpanded-SemiBold","robotoserif72ptsemiexpandedthin|book":"RobotoSerif72ptSemiExpanded-Thin","robotoserif72ptsemiexpandedthin|italic":"RobotoSerif72ptSemiExpanded-ThinItalic","robotoserif72ptsemiexpandedthin|normal":"RobotoSerif72ptSemiExpanded-Thin","robotoserif72ptsemiexpandedthin|regular":"RobotoSerif72ptSemiExpanded-Thin","robotoserif72ptsemiexpandedthin|roman":"RobotoSerif72ptSemiExpanded-Thin","robotoserif72ptthin|book":"RobotoSerif72pt-Thin","robotoserif72ptthin|italic":"RobotoSerif72pt-ThinItalic","robotoserif72ptthin|normal":"RobotoSerif72pt-Thin","robotoserif72ptthin|regular":"RobotoSerif72pt-Thin","robotoserif72ptthin|roman":"RobotoSerif72pt-Thin","robotoserif72ptultracondensed|black":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensed|blackitalic":"RobotoSerif72ptUltraCondensed-BlackItalic","robotoserif72ptultracondensed|bold":"RobotoSerif72ptUltraCondensed-Bold","robotoserif72ptultracondensed|bolditalic":"RobotoSerif72ptUltraCondensed-BoldItalic","robotoserif72ptultracondensed|book":"RobotoSerif72ptUltraCondensed-Regular","robotoserif72ptultracondensed|demi":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensed|demibold":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensed|extrabold":"RobotoSerif72ptUltraCondensed-ExtraBold","robotoserif72ptultracondensed|extrabolditalic":"RobotoSerif72ptUltraCondensed-ExtraBoldItalic","robotoserif72ptultracondensed|extralight":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensed|extralightitalic":"RobotoSerif72ptUltraCondensed-ExtraLightItalic","robotoserif72ptultracondensed|heavy":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensed|italic":"RobotoSerif72ptUltraCondensed-Italic","robotoserif72ptultracondensed|light":"RobotoSerif72ptUltraCondensed-Light","robotoserif72ptultracondensed|lightitalic":"RobotoSerif72ptUltraCondensed-LightItalic","robotoserif72ptultracondensed|medium":"RobotoSerif72ptUltraCondensed-Medium","robotoserif72ptultracondensed|mediumitalic":"RobotoSerif72ptUltraCondensed-MediumItalic","robotoserif72ptultracondensed|normal":"RobotoSerif72ptUltraCondensed-Regular","robotoserif72ptultracondensed|regular":"RobotoSerif72ptUltraCondensed-Regular","robotoserif72ptultracondensed|roman":"RobotoSerif72ptUltraCondensed-Regular","robotoserif72ptultracondensed|semibold":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensed|semibolditalic":"RobotoSerif72ptUltraCondensed-SemiBoldItalic","robotoserif72ptultracondensed|thin":"RobotoSerif72ptUltraCondensed-Thin","robotoserif72ptultracondensed|thinitalic":"RobotoSerif72ptUltraCondensed-ThinItalic","robotoserif72ptultracondensed|ultralight":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensedblack|book":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensedblack|italic":"RobotoSerif72ptUltraCondensed-BlackItalic","robotoserif72ptultracondensedblack|normal":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensedblack|regular":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensedblack|roman":"RobotoSerif72ptUltraCondensed-Black","robotoserif72ptultracondensedextrabold|book":"RobotoSerif72ptUltraCondensed-ExtraBold","robotoserif72ptultracondensedextrabold|italic":"RobotoSerif72ptUltraCondensed-ExtraBoldItalic","robotoserif72ptultracondensedextrabold|normal":"RobotoSerif72ptUltraCondensed-ExtraBold","robotoserif72ptultracondensedextrabold|regular":"RobotoSerif72ptUltraCondensed-ExtraBold","robotoserif72ptultracondensedextrabold|roman":"RobotoSerif72ptUltraCondensed-ExtraBold","robotoserif72ptultracondensedextralight|book":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensedextralight|italic":"RobotoSerif72ptUltraCondensed-ExtraLightItalic","robotoserif72ptultracondensedextralight|normal":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensedextralight|regular":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensedextralight|roman":"RobotoSerif72ptUltraCondensed-ExtraLight","robotoserif72ptultracondensedlight|book":"RobotoSerif72ptUltraCondensed-Light","robotoserif72ptultracondensedlight|italic":"RobotoSerif72ptUltraCondensed-LightItalic","robotoserif72ptultracondensedlight|normal":"RobotoSerif72ptUltraCondensed-Light","robotoserif72ptultracondensedlight|regular":"RobotoSerif72ptUltraCondensed-Light","robotoserif72ptultracondensedlight|roman":"RobotoSerif72ptUltraCondensed-Light","robotoserif72ptultracondensedmedium|book":"RobotoSerif72ptUltraCondensed-Medium","robotoserif72ptultracondensedmedium|italic":"RobotoSerif72ptUltraCondensed-MediumItalic","robotoserif72ptultracondensedmedium|normal":"RobotoSerif72ptUltraCondensed-Medium","robotoserif72ptultracondensedmedium|regular":"RobotoSerif72ptUltraCondensed-Medium","robotoserif72ptultracondensedmedium|roman":"RobotoSerif72ptUltraCondensed-Medium","robotoserif72ptultracondensedsemibold|book":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensedsemibold|italic":"RobotoSerif72ptUltraCondensed-SemiBoldItalic","robotoserif72ptultracondensedsemibold|normal":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensedsemibold|regular":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensedsemibold|roman":"RobotoSerif72ptUltraCondensed-SemiBold","robotoserif72ptultracondensedthin|book":"RobotoSerif72ptUltraCondensed-Thin","robotoserif72ptultracondensedthin|italic":"RobotoSerif72ptUltraCondensed-ThinItalic","robotoserif72ptultracondensedthin|normal":"RobotoSerif72ptUltraCondensed-Thin","robotoserif72ptultracondensedthin|regular":"RobotoSerif72ptUltraCondensed-Thin","robotoserif72ptultracondensedthin|roman":"RobotoSerif72ptUltraCondensed-Thin","robotoserifblack|book":"RobotoSerif-Black","robotoserifblack|italic":"RobotoSerif-BlackItalic","robotoserifblack|normal":"RobotoSerif-Black","robotoserifblack|regular":"RobotoSerif-Black","robotoserifblack|roman":"RobotoSerif-Black","robotoserifcondensed|black":"RobotoSerifCondensed-Black","robotoserifcondensed|blackitalic":"RobotoSerifCondensed-BlackItalic","robotoserifcondensed|bold":"RobotoSerifCondensed-Bold","robotoserifcondensed|bolditalic":"RobotoSerifCondensed-BoldItalic","robotoserifcondensed|book":"RobotoSerifCondensed-Regular","robotoserifcondensed|demi":"RobotoSerifCondensed-SemiBold","robotoserifcondensed|demibold":"RobotoSerifCondensed-SemiBold","robotoserifcondensed|extrabold":"RobotoSerifCondensed-ExtraBold","robotoserifcondensed|extrabolditalic":"RobotoSerifCondensed-ExtraBoldItalic","robotoserifcondensed|extralight":"RobotoSerifCondensed-ExtraLight","robotoserifcondensed|extralightitalic":"RobotoSerifCondensed-ExtraLightItalic","robotoserifcondensed|heavy":"RobotoSerifCondensed-Black","robotoserifcondensed|italic":"RobotoSerifCondensed-Italic","robotoserifcondensed|light":"RobotoSerifCondensed-Light","robotoserifcondensed|lightitalic":"RobotoSerifCondensed-LightItalic","robotoserifcondensed|medium":"RobotoSerifCondensed-Medium","robotoserifcondensed|mediumitalic":"RobotoSerifCondensed-MediumItalic","robotoserifcondensed|normal":"RobotoSerifCondensed-Regular","robotoserifcondensed|regular":"RobotoSerifCondensed-Regular","robotoserifcondensed|roman":"RobotoSerifCondensed-Regular","robotoserifcondensed|semibold":"RobotoSerifCondensed-SemiBold","robotoserifcondensed|semibolditalic":"RobotoSerifCondensed-SemiBoldItalic","robotoserifcondensed|thin":"RobotoSerifCondensed-Thin","robotoserifcondensed|thinitalic":"RobotoSerifCondensed-ThinItalic","robotoserifcondensed|ultralight":"RobotoSerifCondensed-ExtraLight","robotoserifcondensedblack|book":"RobotoSerifCondensed-Black","robotoserifcondensedblack|italic":"RobotoSerifCondensed-BlackItalic","robotoserifcondensedblack|normal":"RobotoSerifCondensed-Black","robotoserifcondensedblack|regular":"RobotoSerifCondensed-Black","robotoserifcondensedblack|roman":"RobotoSerifCondensed-Black","robotoserifcondensedextrabold|book":"RobotoSerifCondensed-ExtraBold","robotoserifcondensedextrabold|italic":"RobotoSerifCondensed-ExtraBoldItalic","robotoserifcondensedextrabold|normal":"RobotoSerifCondensed-ExtraBold","robotoserifcondensedextrabold|regular":"RobotoSerifCondensed-ExtraBold","robotoserifcondensedextrabold|roman":"RobotoSerifCondensed-ExtraBold","robotoserifcondensedextralight|book":"RobotoSerifCondensed-ExtraLight","robotoserifcondensedextralight|italic":"RobotoSerifCondensed-ExtraLightItalic","robotoserifcondensedextralight|normal":"RobotoSerifCondensed-ExtraLight","robotoserifcondensedextralight|regular":"RobotoSerifCondensed-ExtraLight","robotoserifcondensedextralight|roman":"RobotoSerifCondensed-ExtraLight","robotoserifcondensedlight|book":"RobotoSerifCondensed-Light","robotoserifcondensedlight|italic":"RobotoSerifCondensed-LightItalic","robotoserifcondensedlight|normal":"RobotoSerifCondensed-Light","robotoserifcondensedlight|regular":"RobotoSerifCondensed-Light","robotoserifcondensedlight|roman":"RobotoSerifCondensed-Light","robotoserifcondensedmedium|book":"RobotoSerifCondensed-Medium","robotoserifcondensedmedium|italic":"RobotoSerifCondensed-MediumItalic","robotoserifcondensedmedium|normal":"RobotoSerifCondensed-Medium","robotoserifcondensedmedium|regular":"RobotoSerifCondensed-Medium","robotoserifcondensedmedium|roman":"RobotoSerifCondensed-Medium","robotoserifcondensedsemibold|book":"RobotoSerifCondensed-SemiBold","robotoserifcondensedsemibold|italic":"RobotoSerifCondensed-SemiBoldItalic","robotoserifcondensedsemibold|normal":"RobotoSerifCondensed-SemiBold","robotoserifcondensedsemibold|regular":"RobotoSerifCondensed-SemiBold","robotoserifcondensedsemibold|roman":"RobotoSerifCondensed-SemiBold","robotoserifcondensedthin|book":"RobotoSerifCondensed-Thin","robotoserifcondensedthin|italic":"RobotoSerifCondensed-ThinItalic","robotoserifcondensedthin|normal":"RobotoSerifCondensed-Thin","robotoserifcondensedthin|regular":"RobotoSerifCondensed-Thin","robotoserifcondensedthin|roman":"RobotoSerifCondensed-Thin","robotoserifexpanded|black":"RobotoSerifExpanded-Black","robotoserifexpanded|blackitalic":"RobotoSerifExpanded-BlackItalic","robotoserifexpanded|bold":"RobotoSerifExpanded-Bold","robotoserifexpanded|bolditalic":"RobotoSerifExpanded-BoldItalic","robotoserifexpanded|book":"RobotoSerifExpanded-Regular","robotoserifexpanded|demi":"RobotoSerifExpanded-SemiBold","robotoserifexpanded|demibold":"RobotoSerifExpanded-SemiBold","robotoserifexpanded|extrabold":"RobotoSerifExpanded-ExtraBold","robotoserifexpanded|extrabolditalic":"RobotoSerifExpanded-ExtraBoldItalic","robotoserifexpanded|extralight":"RobotoSerifExpanded-ExtraLight","robotoserifexpanded|extralightitalic":"RobotoSerifExpanded-ExtraLightItalic","robotoserifexpanded|heavy":"RobotoSerifExpanded-Black","robotoserifexpanded|italic":"RobotoSerifExpanded-Italic","robotoserifexpanded|light":"RobotoSerifExpanded-Light","robotoserifexpanded|lightitalic":"RobotoSerifExpanded-LightItalic","robotoserifexpanded|medium":"RobotoSerifExpanded-Medium","robotoserifexpanded|mediumitalic":"RobotoSerifExpanded-MediumItalic","robotoserifexpanded|normal":"RobotoSerifExpanded-Regular","robotoserifexpanded|regular":"RobotoSerifExpanded-Regular","robotoserifexpanded|roman":"RobotoSerifExpanded-Regular","robotoserifexpanded|semibold":"RobotoSerifExpanded-SemiBold","robotoserifexpanded|semibolditalic":"RobotoSerifExpanded-SemiBoldItalic","robotoserifexpanded|thin":"RobotoSerifExpanded-Thin","robotoserifexpanded|thinitalic":"RobotoSerifExpanded-ThinItalic","robotoserifexpanded|ultralight":"RobotoSerifExpanded-ExtraLight","robotoserifexpandedblack|book":"RobotoSerifExpanded-Black","robotoserifexpandedblack|italic":"RobotoSerifExpanded-BlackItalic","robotoserifexpandedblack|normal":"RobotoSerifExpanded-Black","robotoserifexpandedblack|regular":"RobotoSerifExpanded-Black","robotoserifexpandedblack|roman":"RobotoSerifExpanded-Black","robotoserifexpandedextrabold|book":"RobotoSerifExpanded-ExtraBold","robotoserifexpandedextrabold|italic":"RobotoSerifExpanded-ExtraBoldItalic","robotoserifexpandedextrabold|normal":"RobotoSerifExpanded-ExtraBold","robotoserifexpandedextrabold|regular":"RobotoSerifExpanded-ExtraBold","robotoserifexpandedextrabold|roman":"RobotoSerifExpanded-ExtraBold","robotoserifexpandedextralight|book":"RobotoSerifExpanded-ExtraLight","robotoserifexpandedextralight|italic":"RobotoSerifExpanded-ExtraLightItalic","robotoserifexpandedextralight|normal":"RobotoSerifExpanded-ExtraLight","robotoserifexpandedextralight|regular":"RobotoSerifExpanded-ExtraLight","robotoserifexpandedextralight|roman":"RobotoSerifExpanded-ExtraLight","robotoserifexpandedlight|book":"RobotoSerifExpanded-Light","robotoserifexpandedlight|italic":"RobotoSerifExpanded-LightItalic","robotoserifexpandedlight|normal":"RobotoSerifExpanded-Light","robotoserifexpandedlight|regular":"RobotoSerifExpanded-Light","robotoserifexpandedlight|roman":"RobotoSerifExpanded-Light","robotoserifexpandedmedium|book":"RobotoSerifExpanded-Medium","robotoserifexpandedmedium|italic":"RobotoSerifExpanded-MediumItalic","robotoserifexpandedmedium|normal":"RobotoSerifExpanded-Medium","robotoserifexpandedmedium|regular":"RobotoSerifExpanded-Medium","robotoserifexpandedmedium|roman":"RobotoSerifExpanded-Medium","robotoserifexpandedsemibold|book":"RobotoSerifExpanded-SemiBold","robotoserifexpandedsemibold|italic":"RobotoSerifExpanded-SemiBoldItalic","robotoserifexpandedsemibold|normal":"RobotoSerifExpanded-SemiBold","robotoserifexpandedsemibold|regular":"RobotoSerifExpanded-SemiBold","robotoserifexpandedsemibold|roman":"RobotoSerifExpanded-SemiBold","robotoserifexpandedthin|book":"RobotoSerifExpanded-Thin","robotoserifexpandedthin|italic":"RobotoSerifExpanded-ThinItalic","robotoserifexpandedthin|normal":"RobotoSerifExpanded-Thin","robotoserifexpandedthin|regular":"RobotoSerifExpanded-Thin","robotoserifexpandedthin|roman":"RobotoSerifExpanded-Thin","robotoserifextrabold|book":"RobotoSerif-ExtraBold","robotoserifextrabold|italic":"RobotoSerif-ExtraBoldItalic","robotoserifextrabold|normal":"RobotoSerif-ExtraBold","robotoserifextrabold|regular":"RobotoSerif-ExtraBold","robotoserifextrabold|roman":"RobotoSerif-ExtraBold","robotoserifextracondensed|black":"RobotoSerifExtraCondensed-Black","robotoserifextracondensed|blackitalic":"RobotoSerifExtraCondensed-BlackItalic","robotoserifextracondensed|bold":"RobotoSerifExtraCondensed-Bold","robotoserifextracondensed|bolditalic":"RobotoSerifExtraCondensed-BoldItalic","robotoserifextracondensed|book":"RobotoSerifExtraCondensed-Regular","robotoserifextracondensed|demi":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensed|demibold":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensed|extrabold":"RobotoSerifExtraCondensed-ExtraBold","robotoserifextracondensed|extrabolditalic":"RobotoSerifExtraCondensed-ExtraBoldItalic","robotoserifextracondensed|extralight":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensed|extralightitalic":"RobotoSerifExtraCondensed-ExtraLightItalic","robotoserifextracondensed|heavy":"RobotoSerifExtraCondensed-Black","robotoserifextracondensed|italic":"RobotoSerifExtraCondensed-Italic","robotoserifextracondensed|light":"RobotoSerifExtraCondensed-Light","robotoserifextracondensed|lightitalic":"RobotoSerifExtraCondensed-LightItalic","robotoserifextracondensed|medium":"RobotoSerifExtraCondensed-Medium","robotoserifextracondensed|mediumitalic":"RobotoSerifExtraCondensed-MediumItalic","robotoserifextracondensed|normal":"RobotoSerifExtraCondensed-Regular","robotoserifextracondensed|regular":"RobotoSerifExtraCondensed-Regular","robotoserifextracondensed|roman":"RobotoSerifExtraCondensed-Regular","robotoserifextracondensed|semibold":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensed|semibolditalic":"RobotoSerifExtraCondensed-SemiBoldItalic","robotoserifextracondensed|thin":"RobotoSerifExtraCondensed-Thin","robotoserifextracondensed|thinitalic":"RobotoSerifExtraCondensed-ThinItalic","robotoserifextracondensed|ultralight":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensedblack|book":"RobotoSerifExtraCondensed-Black","robotoserifextracondensedblack|italic":"RobotoSerifExtraCondensed-BlackItalic","robotoserifextracondensedblack|normal":"RobotoSerifExtraCondensed-Black","robotoserifextracondensedblack|regular":"RobotoSerifExtraCondensed-Black","robotoserifextracondensedblack|roman":"RobotoSerifExtraCondensed-Black","robotoserifextracondensedextrabold|book":"RobotoSerifExtraCondensed-ExtraBold","robotoserifextracondensedextrabold|italic":"RobotoSerifExtraCondensed-ExtraBoldItalic","robotoserifextracondensedextrabold|normal":"RobotoSerifExtraCondensed-ExtraBold","robotoserifextracondensedextrabold|regular":"RobotoSerifExtraCondensed-ExtraBold","robotoserifextracondensedextrabold|roman":"RobotoSerifExtraCondensed-ExtraBold","robotoserifextracondensedextralight|book":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensedextralight|italic":"RobotoSerifExtraCondensed-ExtraLightItalic","robotoserifextracondensedextralight|normal":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensedextralight|regular":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensedextralight|roman":"RobotoSerifExtraCondensed-ExtraLight","robotoserifextracondensedlight|book":"RobotoSerifExtraCondensed-Light","robotoserifextracondensedlight|italic":"RobotoSerifExtraCondensed-LightItalic","robotoserifextracondensedlight|normal":"RobotoSerifExtraCondensed-Light","robotoserifextracondensedlight|regular":"RobotoSerifExtraCondensed-Light","robotoserifextracondensedlight|roman":"RobotoSerifExtraCondensed-Light","robotoserifextracondensedmedium|book":"RobotoSerifExtraCondensed-Medium","robotoserifextracondensedmedium|italic":"RobotoSerifExtraCondensed-MediumItalic","robotoserifextracondensedmedium|normal":"RobotoSerifExtraCondensed-Medium","robotoserifextracondensedmedium|regular":"RobotoSerifExtraCondensed-Medium","robotoserifextracondensedmedium|roman":"RobotoSerifExtraCondensed-Medium","robotoserifextracondensedsemibold|book":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensedsemibold|italic":"RobotoSerifExtraCondensed-SemiBoldItalic","robotoserifextracondensedsemibold|normal":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensedsemibold|regular":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensedsemibold|roman":"RobotoSerifExtraCondensed-SemiBold","robotoserifextracondensedthin|book":"RobotoSerifExtraCondensed-Thin","robotoserifextracondensedthin|italic":"RobotoSerifExtraCondensed-ThinItalic","robotoserifextracondensedthin|normal":"RobotoSerifExtraCondensed-Thin","robotoserifextracondensedthin|regular":"RobotoSerifExtraCondensed-Thin","robotoserifextracondensedthin|roman":"RobotoSerifExtraCondensed-Thin","robotoserifextraexpanded|black":"RobotoSerifExtraExpanded-Black","robotoserifextraexpanded|blackitalic":"RobotoSerifExtraExpanded-BlackItalic","robotoserifextraexpanded|bold":"RobotoSerifExtraExpanded-Bold","robotoserifextraexpanded|bolditalic":"RobotoSerifExtraExpanded-BoldItalic","robotoserifextraexpanded|book":"RobotoSerifExtraExpanded-Regular","robotoserifextraexpanded|demi":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpanded|demibold":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpanded|extrabold":"RobotoSerifExtraExpanded-ExtraBold","robotoserifextraexpanded|extrabolditalic":"RobotoSerifExtraExpanded-ExtraBoldItalic","robotoserifextraexpanded|extralight":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpanded|extralightitalic":"RobotoSerifExtraExpanded-ExtraLightItalic","robotoserifextraexpanded|heavy":"RobotoSerifExtraExpanded-Black","robotoserifextraexpanded|italic":"RobotoSerifExtraExpanded-Italic","robotoserifextraexpanded|light":"RobotoSerifExtraExpanded-Light","robotoserifextraexpanded|lightitalic":"RobotoSerifExtraExpanded-LightItalic","robotoserifextraexpanded|medium":"RobotoSerifExtraExpanded-Medium","robotoserifextraexpanded|mediumitalic":"RobotoSerifExtraExpanded-MediumItalic","robotoserifextraexpanded|normal":"RobotoSerifExtraExpanded-Regular","robotoserifextraexpanded|regular":"RobotoSerifExtraExpanded-Regular","robotoserifextraexpanded|roman":"RobotoSerifExtraExpanded-Regular","robotoserifextraexpanded|semibold":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpanded|semibolditalic":"RobotoSerifExtraExpanded-SemiBoldItalic","robotoserifextraexpanded|thin":"RobotoSerifExtraExpanded-Thin","robotoserifextraexpanded|thinitalic":"RobotoSerifExtraExpanded-ThinItalic","robotoserifextraexpanded|ultralight":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpandedblack|book":"RobotoSerifExtraExpanded-Black","robotoserifextraexpandedblack|italic":"RobotoSerifExtraExpanded-BlackItalic","robotoserifextraexpandedblack|normal":"RobotoSerifExtraExpanded-Black","robotoserifextraexpandedblack|regular":"RobotoSerifExtraExpanded-Black","robotoserifextraexpandedblack|roman":"RobotoSerifExtraExpanded-Black","robotoserifextraexpandedextrabold|book":"RobotoSerifExtraExpanded-ExtraBold","robotoserifextraexpandedextrabold|italic":"RobotoSerifExtraExpanded-ExtraBoldItalic","robotoserifextraexpandedextrabold|normal":"RobotoSerifExtraExpanded-ExtraBold","robotoserifextraexpandedextrabold|regular":"RobotoSerifExtraExpanded-ExtraBold","robotoserifextraexpandedextrabold|roman":"RobotoSerifExtraExpanded-ExtraBold","robotoserifextraexpandedextralight|book":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpandedextralight|italic":"RobotoSerifExtraExpanded-ExtraLightItalic","robotoserifextraexpandedextralight|normal":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpandedextralight|regular":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpandedextralight|roman":"RobotoSerifExtraExpanded-ExtraLight","robotoserifextraexpandedlight|book":"RobotoSerifExtraExpanded-Light","robotoserifextraexpandedlight|italic":"RobotoSerifExtraExpanded-LightItalic","robotoserifextraexpandedlight|normal":"RobotoSerifExtraExpanded-Light","robotoserifextraexpandedlight|regular":"RobotoSerifExtraExpanded-Light","robotoserifextraexpandedlight|roman":"RobotoSerifExtraExpanded-Light","robotoserifextraexpandedmedium|book":"RobotoSerifExtraExpanded-Medium","robotoserifextraexpandedmedium|italic":"RobotoSerifExtraExpanded-MediumItalic","robotoserifextraexpandedmedium|normal":"RobotoSerifExtraExpanded-Medium","robotoserifextraexpandedmedium|regular":"RobotoSerifExtraExpanded-Medium","robotoserifextraexpandedmedium|roman":"RobotoSerifExtraExpanded-Medium","robotoserifextraexpandedsemibold|book":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpandedsemibold|italic":"RobotoSerifExtraExpanded-SemiBoldItalic","robotoserifextraexpandedsemibold|normal":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpandedsemibold|regular":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpandedsemibold|roman":"RobotoSerifExtraExpanded-SemiBold","robotoserifextraexpandedthin|book":"RobotoSerifExtraExpanded-Thin","robotoserifextraexpandedthin|italic":"RobotoSerifExtraExpanded-ThinItalic","robotoserifextraexpandedthin|normal":"RobotoSerifExtraExpanded-Thin","robotoserifextraexpandedthin|regular":"RobotoSerifExtraExpanded-Thin","robotoserifextraexpandedthin|roman":"RobotoSerifExtraExpanded-Thin","robotoserifextralight|book":"RobotoSerif-ExtraLight","robotoserifextralight|italic":"RobotoSerif-ExtraLightItalic","robotoserifextralight|normal":"RobotoSerif-ExtraLight","robotoserifextralight|regular":"RobotoSerif-ExtraLight","robotoserifextralight|roman":"RobotoSerif-ExtraLight","robotoseriflight|book":"RobotoSerif-Light","robotoseriflight|italic":"RobotoSerif-LightItalic","robotoseriflight|normal":"RobotoSerif-Light","robotoseriflight|regular":"RobotoSerif-Light","robotoseriflight|roman":"RobotoSerif-Light","robotoserifmedium|book":"RobotoSerif-Medium","robotoserifmedium|italic":"RobotoSerif-MediumItalic","robotoserifmedium|normal":"RobotoSerif-Medium","robotoserifmedium|regular":"RobotoSerif-Medium","robotoserifmedium|roman":"RobotoSerif-Medium","robotoserifsemibold|book":"RobotoSerif-SemiBold","robotoserifsemibold|italic":"RobotoSerif-SemiBoldItalic","robotoserifsemibold|normal":"RobotoSerif-SemiBold","robotoserifsemibold|regular":"RobotoSerif-SemiBold","robotoserifsemibold|roman":"RobotoSerif-SemiBold","robotoserifsemicondensed|black":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensed|blackitalic":"RobotoSerifSemiCondensed-BlackItalic","robotoserifsemicondensed|bold":"RobotoSerifSemiCondensed-Bold","robotoserifsemicondensed|bolditalic":"RobotoSerifSemiCondensed-BoldItalic","robotoserifsemicondensed|book":"RobotoSerifSemiCondensed-Regular","robotoserifsemicondensed|demi":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensed|demibold":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensed|extrabold":"RobotoSerifSemiCondensed-ExtraBold","robotoserifsemicondensed|extrabolditalic":"RobotoSerifSemiCondensed-ExtraBoldItalic","robotoserifsemicondensed|extralight":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensed|extralightitalic":"RobotoSerifSemiCondensed-ExtraLightItalic","robotoserifsemicondensed|heavy":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensed|italic":"RobotoSerifSemiCondensed-Italic","robotoserifsemicondensed|light":"RobotoSerifSemiCondensed-Light","robotoserifsemicondensed|lightitalic":"RobotoSerifSemiCondensed-LightItalic","robotoserifsemicondensed|medium":"RobotoSerifSemiCondensed-Medium","robotoserifsemicondensed|mediumitalic":"RobotoSerifSemiCondensed-MediumItalic","robotoserifsemicondensed|normal":"RobotoSerifSemiCondensed-Regular","robotoserifsemicondensed|regular":"RobotoSerifSemiCondensed-Regular","robotoserifsemicondensed|roman":"RobotoSerifSemiCondensed-Regular","robotoserifsemicondensed|semibold":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensed|semibolditalic":"RobotoSerifSemiCondensed-SemiBoldItalic","robotoserifsemicondensed|thin":"RobotoSerifSemiCondensed-Thin","robotoserifsemicondensed|thinitalic":"RobotoSerifSemiCondensed-ThinItalic","robotoserifsemicondensed|ultralight":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensedblack|book":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensedblack|italic":"RobotoSerifSemiCondensed-BlackItalic","robotoserifsemicondensedblack|normal":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensedblack|regular":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensedblack|roman":"RobotoSerifSemiCondensed-Black","robotoserifsemicondensedextrabold|book":"RobotoSerifSemiCondensed-ExtraBold","robotoserifsemicondensedextrabold|italic":"RobotoSerifSemiCondensed-ExtraBoldItalic","robotoserifsemicondensedextrabold|normal":"RobotoSerifSemiCondensed-ExtraBold","robotoserifsemicondensedextrabold|regular":"RobotoSerifSemiCondensed-ExtraBold","robotoserifsemicondensedextrabold|roman":"RobotoSerifSemiCondensed-ExtraBold","robotoserifsemicondensedextralight|book":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensedextralight|italic":"RobotoSerifSemiCondensed-ExtraLightItalic","robotoserifsemicondensedextralight|normal":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensedextralight|regular":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensedextralight|roman":"RobotoSerifSemiCondensed-ExtraLight","robotoserifsemicondensedlight|book":"RobotoSerifSemiCondensed-Light","robotoserifsemicondensedlight|italic":"RobotoSerifSemiCondensed-LightItalic","robotoserifsemicondensedlight|normal":"RobotoSerifSemiCondensed-Light","robotoserifsemicondensedlight|regular":"RobotoSerifSemiCondensed-Light","robotoserifsemicondensedlight|roman":"RobotoSerifSemiCondensed-Light","robotoserifsemicondensedmedium|book":"RobotoSerifSemiCondensed-Medium","robotoserifsemicondensedmedium|italic":"RobotoSerifSemiCondensed-MediumItalic","robotoserifsemicondensedmedium|normal":"RobotoSerifSemiCondensed-Medium","robotoserifsemicondensedmedium|regular":"RobotoSerifSemiCondensed-Medium","robotoserifsemicondensedmedium|roman":"RobotoSerifSemiCondensed-Medium","robotoserifsemicondensedsemibold|book":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensedsemibold|italic":"RobotoSerifSemiCondensed-SemiBoldItalic","robotoserifsemicondensedsemibold|normal":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensedsemibold|regular":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensedsemibold|roman":"RobotoSerifSemiCondensed-SemiBold","robotoserifsemicondensedthin|book":"RobotoSerifSemiCondensed-Thin","robotoserifsemicondensedthin|italic":"RobotoSerifSemiCondensed-ThinItalic","robotoserifsemicondensedthin|normal":"RobotoSerifSemiCondensed-Thin","robotoserifsemicondensedthin|regular":"RobotoSerifSemiCondensed-Thin","robotoserifsemicondensedthin|roman":"RobotoSerifSemiCondensed-Thin","robotoserifsemiexpanded|black":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpanded|blackitalic":"RobotoSerifSemiExpanded-BlackItalic","robotoserifsemiexpanded|bold":"RobotoSerifSemiExpanded-Bold","robotoserifsemiexpanded|bolditalic":"RobotoSerifSemiExpanded-BoldItalic","robotoserifsemiexpanded|book":"RobotoSerifSemiExpanded-Regular","robotoserifsemiexpanded|demi":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpanded|demibold":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpanded|extrabold":"RobotoSerifSemiExpanded-ExtraBold","robotoserifsemiexpanded|extrabolditalic":"RobotoSerifSemiExpanded-ExtraBoldItalic","robotoserifsemiexpanded|extralight":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpanded|extralightitalic":"RobotoSerifSemiExpanded-ExtraLightItalic","robotoserifsemiexpanded|heavy":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpanded|italic":"RobotoSerifSemiExpanded-Italic","robotoserifsemiexpanded|light":"RobotoSerifSemiExpanded-Light","robotoserifsemiexpanded|lightitalic":"RobotoSerifSemiExpanded-LightItalic","robotoserifsemiexpanded|medium":"RobotoSerifSemiExpanded-Medium","robotoserifsemiexpanded|mediumitalic":"RobotoSerifSemiExpanded-MediumItalic","robotoserifsemiexpanded|normal":"RobotoSerifSemiExpanded-Regular","robotoserifsemiexpanded|regular":"RobotoSerifSemiExpanded-Regular","robotoserifsemiexpanded|roman":"RobotoSerifSemiExpanded-Regular","robotoserifsemiexpanded|semibold":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpanded|semibolditalic":"RobotoSerifSemiExpanded-SemiBoldItalic","robotoserifsemiexpanded|thin":"RobotoSerifSemiExpanded-Thin","robotoserifsemiexpanded|thinitalic":"RobotoSerifSemiExpanded-ThinItalic","robotoserifsemiexpanded|ultralight":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpandedblack|book":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpandedblack|italic":"RobotoSerifSemiExpanded-BlackItalic","robotoserifsemiexpandedblack|normal":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpandedblack|regular":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpandedblack|roman":"RobotoSerifSemiExpanded-Black","robotoserifsemiexpandedextrabold|book":"RobotoSerifSemiExpanded-ExtraBold","robotoserifsemiexpandedextrabold|italic":"RobotoSerifSemiExpanded-ExtraBoldItalic","robotoserifsemiexpandedextrabold|normal":"RobotoSerifSemiExpanded-ExtraBold","robotoserifsemiexpandedextrabold|regular":"RobotoSerifSemiExpanded-ExtraBold","robotoserifsemiexpandedextrabold|roman":"RobotoSerifSemiExpanded-ExtraBold","robotoserifsemiexpandedextralight|book":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpandedextralight|italic":"RobotoSerifSemiExpanded-ExtraLightItalic","robotoserifsemiexpandedextralight|normal":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpandedextralight|regular":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpandedextralight|roman":"RobotoSerifSemiExpanded-ExtraLight","robotoserifsemiexpandedlight|book":"RobotoSerifSemiExpanded-Light","robotoserifsemiexpandedlight|italic":"RobotoSerifSemiExpanded-LightItalic","robotoserifsemiexpandedlight|normal":"RobotoSerifSemiExpanded-Light","robotoserifsemiexpandedlight|regular":"RobotoSerifSemiExpanded-Light","robotoserifsemiexpandedlight|roman":"RobotoSerifSemiExpanded-Light","robotoserifsemiexpandedmedium|book":"RobotoSerifSemiExpanded-Medium","robotoserifsemiexpandedmedium|italic":"RobotoSerifSemiExpanded-MediumItalic","robotoserifsemiexpandedmedium|normal":"RobotoSerifSemiExpanded-Medium","robotoserifsemiexpandedmedium|regular":"RobotoSerifSemiExpanded-Medium","robotoserifsemiexpandedmedium|roman":"RobotoSerifSemiExpanded-Medium","robotoserifsemiexpandedsemibold|book":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpandedsemibold|italic":"RobotoSerifSemiExpanded-SemiBoldItalic","robotoserifsemiexpandedsemibold|normal":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpandedsemibold|regular":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpandedsemibold|roman":"RobotoSerifSemiExpanded-SemiBold","robotoserifsemiexpandedthin|book":"RobotoSerifSemiExpanded-Thin","robotoserifsemiexpandedthin|italic":"RobotoSerifSemiExpanded-ThinItalic","robotoserifsemiexpandedthin|normal":"RobotoSerifSemiExpanded-Thin","robotoserifsemiexpandedthin|regular":"RobotoSerifSemiExpanded-Thin","robotoserifsemiexpandedthin|roman":"RobotoSerifSemiExpanded-Thin","robotoserifthin|book":"RobotoSerif-Thin","robotoserifthin|italic":"RobotoSerif-ThinItalic","robotoserifthin|normal":"RobotoSerif-Thin","robotoserifthin|regular":"RobotoSerif-Thin","robotoserifthin|roman":"RobotoSerif-Thin","robotoserifultracondensed|black":"RobotoSerifUltraCondensed-Black","robotoserifultracondensed|blackitalic":"RobotoSerifUltraCondensed-BlackItalic","robotoserifultracondensed|bold":"RobotoSerifUltraCondensed-Bold","robotoserifultracondensed|bolditalic":"RobotoSerifUltraCondensed-BoldItalic","robotoserifultracondensed|book":"RobotoSerifUltraCondensed-Regular","robotoserifultracondensed|demi":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensed|demibold":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensed|extrabold":"RobotoSerifUltraCondensed-ExtraBold","robotoserifultracondensed|extrabolditalic":"RobotoSerifUltraCondensed-ExtraBoldItalic","robotoserifultracondensed|extralight":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensed|extralightitalic":"RobotoSerifUltraCondensed-ExtraLightItalic","robotoserifultracondensed|heavy":"RobotoSerifUltraCondensed-Black","robotoserifultracondensed|italic":"RobotoSerifUltraCondensed-Italic","robotoserifultracondensed|light":"RobotoSerifUltraCondensed-Light","robotoserifultracondensed|lightitalic":"RobotoSerifUltraCondensed-LightItalic","robotoserifultracondensed|medium":"RobotoSerifUltraCondensed-Medium","robotoserifultracondensed|mediumitalic":"RobotoSerifUltraCondensed-MediumItalic","robotoserifultracondensed|normal":"RobotoSerifUltraCondensed-Regular","robotoserifultracondensed|regular":"RobotoSerifUltraCondensed-Regular","robotoserifultracondensed|roman":"RobotoSerifUltraCondensed-Regular","robotoserifultracondensed|semibold":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensed|semibolditalic":"RobotoSerifUltraCondensed-SemiBoldItalic","robotoserifultracondensed|thin":"RobotoSerifUltraCondensed-Thin","robotoserifultracondensed|thinitalic":"RobotoSerifUltraCondensed-ThinItalic","robotoserifultracondensed|ultralight":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensedblack|book":"RobotoSerifUltraCondensed-Black","robotoserifultracondensedblack|italic":"RobotoSerifUltraCondensed-BlackItalic","robotoserifultracondensedblack|normal":"RobotoSerifUltraCondensed-Black","robotoserifultracondensedblack|regular":"RobotoSerifUltraCondensed-Black","robotoserifultracondensedblack|roman":"RobotoSerifUltraCondensed-Black","robotoserifultracondensedextrabold|book":"RobotoSerifUltraCondensed-ExtraBold","robotoserifultracondensedextrabold|italic":"RobotoSerifUltraCondensed-ExtraBoldItalic","robotoserifultracondensedextrabold|normal":"RobotoSerifUltraCondensed-ExtraBold","robotoserifultracondensedextrabold|regular":"RobotoSerifUltraCondensed-ExtraBold","robotoserifultracondensedextrabold|roman":"RobotoSerifUltraCondensed-ExtraBold","robotoserifultracondensedextralight|book":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensedextralight|italic":"RobotoSerifUltraCondensed-ExtraLightItalic","robotoserifultracondensedextralight|normal":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensedextralight|regular":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensedextralight|roman":"RobotoSerifUltraCondensed-ExtraLight","robotoserifultracondensedlight|book":"RobotoSerifUltraCondensed-Light","robotoserifultracondensedlight|italic":"RobotoSerifUltraCondensed-LightItalic","robotoserifultracondensedlight|normal":"RobotoSerifUltraCondensed-Light","robotoserifultracondensedlight|regular":"RobotoSerifUltraCondensed-Light","robotoserifultracondensedlight|roman":"RobotoSerifUltraCondensed-Light","robotoserifultracondensedmedium|book":"RobotoSerifUltraCondensed-Medium","robotoserifultracondensedmedium|italic":"RobotoSerifUltraCondensed-MediumItalic","robotoserifultracondensedmedium|normal":"RobotoSerifUltraCondensed-Medium","robotoserifultracondensedmedium|regular":"RobotoSerifUltraCondensed-Medium","robotoserifultracondensedmedium|roman":"RobotoSerifUltraCondensed-Medium","robotoserifultracondensedsemibold|book":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensedsemibold|italic":"RobotoSerifUltraCondensed-SemiBoldItalic","robotoserifultracondensedsemibold|normal":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensedsemibold|regular":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensedsemibold|roman":"RobotoSerifUltraCondensed-SemiBold","robotoserifultracondensedthin|book":"RobotoSerifUltraCondensed-Thin","robotoserifultracondensedthin|italic":"RobotoSerifUltraCondensed-ThinItalic","robotoserifultracondensedthin|normal":"RobotoSerifUltraCondensed-Thin","robotoserifultracondensedthin|regular":"RobotoSerifUltraCondensed-Thin","robotoserifultracondensedthin|roman":"RobotoSerifUltraCondensed-Thin","ronaldsongothic|book":"RonaldsonGothic","ronaldsongothic|normal":"RonaldsonGothic","ronaldsongothic|regular":"RonaldsonGothic","ronaldsongothic|roman":"RonaldsonGothic","ronaldsongothiclicht|book":"RonaldsonGothicLicht","ronaldsongothiclicht|normal":"RonaldsonGothicLicht","ronaldsongothiclicht|regular":"RonaldsonGothicLicht","ronaldsongothiclicht|roman":"RonaldsonGothicLicht","rondal|bold":"Rondal-Bold","rondal|book":"Rondal-Regular","rondal|demi":"Rondal-Semibold","rondal|demibold":"Rondal-Semibold","rondal|normal":"Rondal-Regular","rondal|regular":"Rondal-Regular","rondal|roman":"Rondal-Regular","rondal|semibold":"Rondal-Semibold","rondalsemibold|book":"Rondal-Semibold","rondalsemibold|normal":"Rondal-Semibold","rondalsemibold|regular":"Rondal-Semibold","rondalsemibold|roman":"Rondal-Semibold","sandollpress|basic":"SDPress-bBasic","sandollpressbasic|book":"SDPress-bBasic","sandollpressbasic|normal":"SDPress-bBasic","sandollpressbasic|regular":"SDPress-bBasic","sandollpressbasic|roman":"SDPress-bBasic","sansmateo2|bold":"SansMateo2-Bold","sansmateo2|bolditalic":"SansMateo2-BoldItalic","sansmateo2|book":"SansMateo2","sansmateo2|demi":"SansMateo2-SemiBold","sansmateo2|demibold":"SansMateo2-SemiBold","sansmateo2|italic":"SansMateo2-Italic","sansmateo2|medium":"SansMateo2-Medium","sansmateo2|mediumitalic":"SansMateo2-MediumItalic","sansmateo2|normal":"SansMateo2","sansmateo2|regular":"SansMateo2","sansmateo2|roman":"SansMateo2","sansmateo2|semibold":"SansMateo2-SemiBold","sansmateo2|semibolditalic":"SansMateo2-SemiBoldItalic","sansmateo2medium|book":"SansMateo2-Medium","sansmateo2medium|italic":"SansMateo2-MediumItalic","sansmateo2medium|normal":"SansMateo2-Medium","sansmateo2medium|regular":"SansMateo2-Medium","sansmateo2medium|roman":"SansMateo2-Medium","sansmateo2semi|bold":"SansMateo2-SemiBold","sansmateo2semi|bolditalic":"SansMateo2-SemiBoldItalic","sansserifcollection|book":"SansSerifCollection","sansserifcollection|normal":"SansSerifCollection","sansserifcollection|regular":"SansSerifCollection","sansserifcollection|roman":"SansSerifCollection","sarabun|bold":"Sarabun-Bold","sarabun|bolditalic":"Sarabun-BoldItalic","sarabun|book":"Sarabun-Regular","sarabun|demi":"Sarabun-SemiBold","sarabun|demibold":"Sarabun-SemiBold","sarabun|extrabold":"Sarabun-ExtraBold","sarabun|extrabolditalic":"Sarabun-ExtraBoldItalic","sarabun|extralight":"Sarabun-ExtraLight","sarabun|extralightitalic":"Sarabun-ExtraLightItalic","sarabun|italic":"Sarabun-Italic","sarabun|light":"Sarabun-Light","sarabun|lightitalic":"Sarabun-LightItalic","sarabun|medium":"Sarabun-Medium","sarabun|mediumitalic":"Sarabun-MediumItalic","sarabun|normal":"Sarabun-Regular","sarabun|regular":"Sarabun-Regular","sarabun|roman":"Sarabun-Regular","sarabun|semibold":"Sarabun-SemiBold","sarabun|semibolditalic":"Sarabun-SemiBoldItalic","sarabun|thin":"Sarabun-Thin","sarabun|thinitalic":"Sarabun-ThinItalic","sarabun|ultralight":"Sarabun-ExtraLight","sarabunextrabold|book":"Sarabun-ExtraBold","sarabunextrabold|italic":"Sarabun-ExtraBoldItalic","sarabunextrabold|normal":"Sarabun-ExtraBold","sarabunextrabold|regular":"Sarabun-ExtraBold","sarabunextrabold|roman":"Sarabun-ExtraBold","sarabunextralight|book":"Sarabun-ExtraLight","sarabunextralight|italic":"Sarabun-ExtraLightItalic","sarabunextralight|normal":"Sarabun-ExtraLight","sarabunextralight|regular":"Sarabun-ExtraLight","sarabunextralight|roman":"Sarabun-ExtraLight","sarabunlight|book":"Sarabun-Light","sarabunlight|italic":"Sarabun-LightItalic","sarabunlight|normal":"Sarabun-Light","sarabunlight|regular":"Sarabun-Light","sarabunlight|roman":"Sarabun-Light","sarabunmedium|book":"Sarabun-Medium","sarabunmedium|italic":"Sarabun-MediumItalic","sarabunmedium|normal":"Sarabun-Medium","sarabunmedium|regular":"Sarabun-Medium","sarabunmedium|roman":"Sarabun-Medium","sarabunsemibold|book":"Sarabun-SemiBold","sarabunsemibold|italic":"Sarabun-SemiBoldItalic","sarabunsemibold|normal":"Sarabun-SemiBold","sarabunsemibold|regular":"Sarabun-SemiBold","sarabunsemibold|roman":"Sarabun-SemiBold","sarabunthin|book":"Sarabun-Thin","sarabunthin|italic":"Sarabun-ThinItalic","sarabunthin|normal":"Sarabun-Thin","sarabunthin|regular":"Sarabun-Thin","sarabunthin|roman":"Sarabun-Thin","segoefluenticons|book":"SegoeFluentIcons","segoefluenticons|normal":"SegoeFluentIcons","segoefluenticons|regular":"SegoeFluentIcons","segoefluenticons|roman":"SegoeFluentIcons","segoemdl2assets|book":"SegoeMDL2Assets","segoemdl2assets|normal":"SegoeMDL2Assets","segoemdl2assets|regular":"SegoeMDL2Assets","segoemdl2assets|roman":"SegoeMDL2Assets","segoeprint|bold":"SegoePrint-Bold","segoeprint|book":"SegoePrint","segoeprint|normal":"SegoePrint","segoeprint|regular":"SegoePrint","segoeprint|roman":"SegoePrint","segoescript|bold":"SegoeScript-Bold","segoescript|book":"SegoeScript","segoescript|normal":"SegoeScript","segoescript|regular":"SegoeScript","segoescript|roman":"SegoeScript","segoeui|black":"SegoeUIBlack","segoeui|blackitalic":"SegoeUIBlack-Italic","segoeui|bold":"SegoeUI-Bold","segoeui|bolditalic":"SegoeUI-BoldItalic","segoeui|book":"SegoeUI","segoeui|demi":"SegoeUI-Semibold","segoeui|demibold":"SegoeUI-Semibold","segoeui|heavy":"SegoeUIBlack","segoeui|italic":"SegoeUI-Italic","segoeui|light":"SegoeUI-Light","segoeui|lightitalic":"SegoeUI-LightItalic","segoeui|normal":"SegoeUI","segoeui|regular":"SegoeUI","segoeui|roman":"SegoeUI","segoeui|semibold":"SegoeUI-Semibold","segoeui|semibolditalic":"SegoeUI-SemiboldItalic","segoeui|semilight":"SegoeUI-Semilight","segoeui|semilightitalic":"SegoeUI-SemilightItalic","segoeuiblack|book":"SegoeUIBlack","segoeuiblack|italic":"SegoeUIBlack-Italic","segoeuiblack|normal":"SegoeUIBlack","segoeuiblack|regular":"SegoeUIBlack","segoeuiblack|roman":"SegoeUIBlack","segoeuiemoji|book":"SegoeUIEmoji","segoeuiemoji|normal":"SegoeUIEmoji","segoeuiemoji|regular":"SegoeUIEmoji","segoeuiemoji|roman":"SegoeUIEmoji","segoeuihistoric|book":"SegoeUIHistoric","segoeuihistoric|normal":"SegoeUIHistoric","segoeuihistoric|regular":"SegoeUIHistoric","segoeuihistoric|roman":"SegoeUIHistoric","segoeuilight|book":"SegoeUI-Light","segoeuilight|italic":"SegoeUI-LightItalic","segoeuilight|normal":"SegoeUI-Light","segoeuilight|regular":"SegoeUI-Light","segoeuilight|roman":"SegoeUI-Light","segoeuisemibold|book":"SegoeUI-Semibold","segoeuisemibold|italic":"SegoeUI-SemiboldItalic","segoeuisemibold|normal":"SegoeUI-Semibold","segoeuisemibold|regular":"SegoeUI-Semibold","segoeuisemibold|roman":"SegoeUI-Semibold","segoeuisemilight|book":"SegoeUI-Semilight","segoeuisemilight|italic":"SegoeUI-SemilightItalic","segoeuisemilight|normal":"SegoeUI-Semilight","segoeuisemilight|regular":"SegoeUI-Semilight","segoeuisemilight|roman":"SegoeUI-Semilight","segoeuisymbol|book":"SegoeUISymbol","segoeuisymbol|normal":"SegoeUISymbol","segoeuisymbol|regular":"SegoeUISymbol","segoeuisymbol|roman":"SegoeUISymbol","segoeuivariable|book":"SegoeUIVariable","segoeuivariable|normal":"SegoeUIVariable","segoeuivariable|regular":"SegoeUIVariable","segoeuivariable|roman":"SegoeUIVariable","silicone|book":"SiliconeRg-Regular","silicone|normal":"SiliconeRg-Regular","silicone|regular":"SiliconeRg-Regular","silicone|roman":"SiliconeRg-Regular","siliconerg|book":"SiliconeRg-Regular","siliconerg|normal":"SiliconeRg-Regular","siliconerg|regular":"SiliconeRg-Regular","siliconerg|roman":"SiliconeRg-Regular","simplysans|bold":"SimplySans-Bold","simplysans|bolditalic":"SimplySans-BoldItalic","simplysans|book":"SimplySans-Book","simplysans|bookitalic":"SimplySans-BookItalic","simplysans|normal":"SimplySans-Book","simplysans|regular":"SimplySans-Book","simplysans|roman":"SimplySans-Book","simsun|book":"SimSun","simsun|normal":"SimSun","simsun|regular":"SimSun","simsun|roman":"SimSun","simsunextb|book":"SimSun-ExtB","simsunextb|normal":"SimSun-ExtB","simsunextb|regular":"SimSun-ExtB","simsunextb|roman":"SimSun-ExtB","simsunextg|book":"SimSun-ExtG","simsunextg|normal":"SimSun-ExtG","simsunextg|regular":"SimSun-ExtG","simsunextg|roman":"SimSun-ExtG","sinzano|book":"Sinzano-Regular","sinzano|normal":"Sinzano-Regular","sinzano|regular":"Sinzano-Regular","sinzano|roman":"Sinzano-Regular","sitka|text":"SitkaText","sitka|textitalic":"SitkaTextItalic","sitkatext|book":"SitkaText","sitkatext|italic":"SitkaTextItalic","sitkatext|normal":"SitkaText","sitkatext|regular":"SitkaText","sitkatext|roman":"SitkaText","soap|book":"Soap-Regular","soap|normal":"Soap-Regular","soap|regular":"Soap-Regular","soap|roman":"Soap-Regular","spinwerad|bold":"spinweradBold","spinweradc|bold":"spinweradCBold","spiritsneutral|black":"SpiritsNeutral-Black","spiritsneutral|book":"SpiritsNeutral-Regular","spiritsneutral|heavy":"SpiritsNeutral-Black","spiritsneutral|light":"SpiritsNeutral-Light","spiritsneutral|medium":"SpiritsNeutral-Medium","spiritsneutral|normal":"SpiritsNeutral-Regular","spiritsneutral|regular":"SpiritsNeutral-Regular","spiritsneutral|roman":"SpiritsNeutral-Regular","spiritsneutralblack|book":"SpiritsNeutral-Black","spiritsneutralblack|normal":"SpiritsNeutral-Black","spiritsneutralblack|regular":"SpiritsNeutral-Black","spiritsneutralblack|roman":"SpiritsNeutral-Black","spiritsneutrallight|book":"SpiritsNeutral-Light","spiritsneutrallight|normal":"SpiritsNeutral-Light","spiritsneutrallight|regular":"SpiritsNeutral-Light","spiritsneutrallight|roman":"SpiritsNeutral-Light","spiritsneutralmedium|book":"SpiritsNeutral-Medium","spiritsneutralmedium|normal":"SpiritsNeutral-Medium","spiritsneutralmedium|regular":"SpiritsNeutral-Medium","spiritsneutralmedium|roman":"SpiritsNeutral-Medium","spiritssharp|black":"SpiritsSharp-Black","spiritssharp|book":"SpiritsSharp-Regular","spiritssharp|heavy":"SpiritsSharp-Black","spiritssharp|light":"SpiritsSharp-Light","spiritssharp|medium":"SpiritsSharp-Medium","spiritssharp|normal":"SpiritsSharp-Regular","spiritssharp|regular":"SpiritsSharp-Regular","spiritssharp|roman":"SpiritsSharp-Regular","spiritssharpblack|book":"SpiritsSharp-Black","spiritssharpblack|normal":"SpiritsSharp-Black","spiritssharpblack|regular":"SpiritsSharp-Black","spiritssharpblack|roman":"SpiritsSharp-Black","spiritssharplight|book":"SpiritsSharp-Light","spiritssharplight|normal":"SpiritsSharp-Light","spiritssharplight|regular":"SpiritsSharp-Light","spiritssharplight|roman":"SpiritsSharp-Light","spiritssharpmedium|book":"SpiritsSharp-Medium","spiritssharpmedium|normal":"SpiritsSharp-Medium","spiritssharpmedium|regular":"SpiritsSharp-Medium","spiritssharpmedium|roman":"SpiritsSharp-Medium","spiritssoft|black":"SpiritsSoft-Black","spiritssoft|book":"SpiritsSoft-Regular","spiritssoft|heavy":"SpiritsSoft-Black","spiritssoft|light":"SpiritsSoft-Light","spiritssoft|medium":"SpiritsSoft-Medium","spiritssoft|normal":"SpiritsSoft-Regular","spiritssoft|regular":"SpiritsSoft-Regular","spiritssoft|roman":"SpiritsSoft-Regular","spiritssoftblack|book":"SpiritsSoft-Black","spiritssoftblack|normal":"SpiritsSoft-Black","spiritssoftblack|regular":"SpiritsSoft-Black","spiritssoftblack|roman":"SpiritsSoft-Black","spiritssoftlight|book":"SpiritsSoft-Light","spiritssoftlight|normal":"SpiritsSoft-Light","spiritssoftlight|regular":"SpiritsSoft-Light","spiritssoftlight|roman":"SpiritsSoft-Light","spiritssoftmedium|book":"SpiritsSoft-Medium","spiritssoftmedium|normal":"SpiritsSoft-Medium","spiritssoftmedium|regular":"SpiritsSoft-Medium","spiritssoftmedium|roman":"SpiritsSoft-Medium","suit|black":"SUIT-Heavy","suit|bold":"SUIT-Bold","suit|book":"SUIT-Regular","suit|demi":"SUIT-SemiBold","suit|demibold":"SUIT-SemiBold","suit|extrabold":"SUIT-ExtraBold","suit|extralight":"SUIT-ExtraLight","suit|heavy":"SUIT-Heavy","suit|light":"SUIT-Light","suit|medium":"SUIT-Medium","suit|normal":"SUIT-Regular","suit|regular":"SUIT-Regular","suit|roman":"SUIT-Regular","suit|semibold":"SUIT-SemiBold","suit|thin":"SUIT-Thin","suit|ultralight":"SUIT-ExtraLight","suitextrabold|book":"SUIT-ExtraBold","suitextrabold|normal":"SUIT-ExtraBold","suitextrabold|regular":"SUIT-ExtraBold","suitextrabold|roman":"SUIT-ExtraBold","suitextralight|book":"SUIT-ExtraLight","suitextralight|normal":"SUIT-ExtraLight","suitextralight|regular":"SUIT-ExtraLight","suitextralight|roman":"SUIT-ExtraLight","suitheavy|book":"SUIT-Heavy","suitheavy|normal":"SUIT-Heavy","suitheavy|regular":"SUIT-Heavy","suitheavy|roman":"SUIT-Heavy","suitlight|book":"SUIT-Light","suitlight|normal":"SUIT-Light","suitlight|regular":"SUIT-Light","suitlight|roman":"SUIT-Light","suitmedium|book":"SUIT-Medium","suitmedium|normal":"SUIT-Medium","suitmedium|regular":"SUIT-Medium","suitmedium|roman":"SUIT-Medium","suitsemibold|book":"SUIT-SemiBold","suitsemibold|normal":"SUIT-SemiBold","suitsemibold|regular":"SUIT-SemiBold","suitsemibold|roman":"SUIT-SemiBold","suitthin|book":"SUIT-Thin","suitthin|normal":"SUIT-Thin","suitthin|regular":"SUIT-Thin","suitthin|roman":"SUIT-Thin","sylfaen|book":"Sylfaen","sylfaen|normal":"Sylfaen","sylfaen|regular":"Sylfaen","sylfaen|roman":"Sylfaen","symbol|book":"SymbolMT","symbol|normal":"SymbolMT","symbol|regular":"SymbolMT","symbol|roman":"SymbolMT","tahoma|bold":"Tahoma-Bold","tahoma|book":"Tahoma","tahoma|normal":"Tahoma","tahoma|regular":"Tahoma","tahoma|roman":"Tahoma","telidon|book":"TelidonRg-Regular","telidon|normal":"TelidonRg-Regular","telidon|regular":"TelidonRg-Regular","telidon|roman":"TelidonRg-Regular","telidonrg|book":"TelidonRg-Regular","telidonrg|normal":"TelidonRg-Regular","telidonrg|regular":"TelidonRg-Regular","telidonrg|roman":"TelidonRg-Regular","thonglorsoi4nr|bold":"ThonglorSoi4Nr-Bold","thonglorsoi4nr|book":"ThonglorSoi4Nr-Regular","thonglorsoi4nr|demi":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nr|demibold":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nr|extrabold":"ThonglorSoi4Nr-ExtraBold","thonglorsoi4nr|extralight":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nr|light":"ThonglorSoi4Nr-Light","thonglorsoi4nr|medium":"ThonglorSoi4Nr-Medium","thonglorsoi4nr|normal":"ThonglorSoi4Nr-Regular","thonglorsoi4nr|regular":"ThonglorSoi4Nr-Regular","thonglorsoi4nr|roman":"ThonglorSoi4Nr-Regular","thonglorsoi4nr|semibold":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nr|thin":"ThonglorSoi4Nr-Thin","thonglorsoi4nr|ultralight":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nrextrabold|book":"ThonglorSoi4Nr-ExtraBold","thonglorsoi4nrextrabold|normal":"ThonglorSoi4Nr-ExtraBold","thonglorsoi4nrextrabold|regular":"ThonglorSoi4Nr-ExtraBold","thonglorsoi4nrextrabold|roman":"ThonglorSoi4Nr-ExtraBold","thonglorsoi4nrextralight|book":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nrextralight|normal":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nrextralight|regular":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nrextralight|roman":"ThonglorSoi4Nr-ExtraLight","thonglorsoi4nrlight|book":"ThonglorSoi4Nr-Light","thonglorsoi4nrlight|normal":"ThonglorSoi4Nr-Light","thonglorsoi4nrlight|regular":"ThonglorSoi4Nr-Light","thonglorsoi4nrlight|roman":"ThonglorSoi4Nr-Light","thonglorsoi4nrmedium|book":"ThonglorSoi4Nr-Medium","thonglorsoi4nrmedium|normal":"ThonglorSoi4Nr-Medium","thonglorsoi4nrmedium|regular":"ThonglorSoi4Nr-Medium","thonglorsoi4nrmedium|roman":"ThonglorSoi4Nr-Medium","thonglorsoi4nrsemibold|book":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nrsemibold|normal":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nrsemibold|regular":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nrsemibold|roman":"ThonglorSoi4Nr-SemiBold","thonglorsoi4nrthin|book":"ThonglorSoi4Nr-Thin","thonglorsoi4nrthin|normal":"ThonglorSoi4Nr-Thin","thonglorsoi4nrthin|regular":"ThonglorSoi4Nr-Thin","thonglorsoi4nrthin|roman":"ThonglorSoi4Nr-Thin","timesnewroman|bold":"TimesNewRomanPS-BoldMT","timesnewroman|bolditalic":"TimesNewRomanPS-BoldItalicMT","timesnewroman|book":"TimesNewRomanPSMT","timesnewroman|italic":"TimesNewRomanPS-ItalicMT","timesnewroman|normal":"TimesNewRomanPSMT","timesnewroman|regular":"TimesNewRomanPSMT","timesnewroman|roman":"TimesNewRomanPSMT","trebuchetms|bold":"TrebuchetMS-Bold","trebuchetms|bolditalic":"Trebuchet-BoldItalic","trebuchetms|book":"TrebuchetMS","trebuchetms|italic":"TrebuchetMS-Italic","trebuchetms|normal":"TrebuchetMS","trebuchetms|regular":"TrebuchetMS","trebuchetms|roman":"TrebuchetMS","turpis|book":"Turpis","turpis|italic":"Turpis-Italic","turpis|normal":"Turpis","turpis|regular":"Turpis","turpis|roman":"Turpis","verdana|bold":"Verdana-Bold","verdana|bolditalic":"Verdana-BoldItalic","verdana|book":"Verdana","verdana|italic":"Verdana-Italic","verdana|normal":"Verdana","verdana|regular":"Verdana","verdana|roman":"Verdana","webdings|book":"Webdings","webdings|normal":"Webdings","webdings|regular":"Webdings","webdings|roman":"Webdings","wingdings|book":"Wingdings-Regular","wingdings|normal":"Wingdings-Regular","wingdings|regular":"Wingdings-Regular","wingdings|roman":"Wingdings-Regular","wingdings2|book":"Wingdings2","wingdings2|normal":"Wingdings2","wingdings2|regular":"Wingdings2","wingdings2|roman":"Wingdings2","wingdings3|book":"Wingdings3","wingdings3|normal":"Wingdings3","wingdings3|regular":"Wingdings3","wingdings3|roman":"Wingdings3","yesevaone|book":"YesevaOne","yesevaone|normal":"YesevaOne","yesevaone|regular":"YesevaOne","yesevaone|roman":"YesevaOne","yetr|book":"YetR-HM","yetr|normal":"YetR-HM","yetr|regular":"YetR-HM","yetr|roman":"YetR-HM","yiggivoounicode|book":"YiggivooUnicode","yiggivoounicode|italic":"YiggivooUnicode-Italic","yiggivoounicode|normal":"YiggivooUnicode","yiggivoounicode|regular":"YiggivooUnicode","yiggivoounicode|roman":"YiggivooUnicode","yiggivoounicode3d|book":"YiggivooUnicode3D","yiggivoounicode3d|italic":"YiggivooUnicode3D-Italic","yiggivoounicode3d|normal":"YiggivooUnicode3D","yiggivoounicode3d|regular":"YiggivooUnicode3D","yiggivoounicode3d|roman":"YiggivooUnicode3D","yugothic|bold":"YuGothic-Bold","yugothic|book":"YuGothic-Regular","yugothic|light":"YuGothic-Light","yugothic|medium":"YuGothic-Medium","yugothic|normal":"YuGothic-Regular","yugothic|regular":"YuGothic-Regular","yugothic|roman":"YuGothic-Regular","yugothiclight|book":"YuGothic-Light","yugothiclight|normal":"YuGothic-Light","yugothiclight|regular":"YuGothic-Light","yugothiclight|roman":"YuGothic-Light","yugothicmedium|book":"YuGothic-Medium","yugothicmedium|normal":"YuGothic-Medium","yugothicmedium|regular":"YuGothic-Medium","yugothicmedium|roman":"YuGothic-Medium","yugothicui|bold":"YuGothicUI-Bold","yugothicui|book":"YuGothicUI-Regular","yugothicui|demi":"YuGothicUI-Semibold","yugothicui|demibold":"YuGothicUI-Semibold","yugothicui|light":"YuGothicUI-Light","yugothicui|normal":"YuGothicUI-Regular","yugothicui|regular":"YuGothicUI-Regular","yugothicui|roman":"YuGothicUI-Regular","yugothicui|semibold":"YuGothicUI-Semibold","yugothicui|semilight":"YuGothicUI-Semilight","yugothicuilight|book":"YuGothicUI-Light","yugothicuilight|normal":"YuGothicUI-Light","yugothicuilight|regular":"YuGothicUI-Light","yugothicuilight|roman":"YuGothicUI-Light","yugothicuisemibold|book":"YuGothicUI-Semibold","yugothicuisemibold|normal":"YuGothicUI-Semibold","yugothicuisemibold|regular":"YuGothicUI-Semibold","yugothicuisemibold|roman":"YuGothicUI-Semibold","yugothicuisemilight|book":"YuGothicUI-Semilight","yugothicuisemilight|normal":"YuGothicUI-Semilight","yugothicuisemilight|regular":"YuGothicUI-Semilight","yugothicuisemilight|roman":"YuGothicUI-Semilight","zwadobef|book":"ZWAdobeF","zwadobef|normal":"ZWAdobeF","zwadobef|regular":"ZWAdobeF","zwadobef|roman":"ZWAdobeF"};function pigmaNormalizePhotoshopFontToken(e){let t=String(e||"").trim().toLowerCase();try{t=t.normalize("NFKD").replace(/[\u0300-\u036f]/g,"")}catch(r){}return t.replace(/[^a-z0-9]+/g,"")}function pigmaPhotoshopStyleName(e){let t=pigmaNormalizePhotoshopFontToken(e),r={regular:"Regular",roman:"Regular",normal:"Regular",book:"Regular",thin:"Thin",hairline:"Thin",extralight:"ExtraLight",ultralight:"ExtraLight",light:"Light",demilight:"DemiLight",semilight:"SemiLight",medium:"Medium",semibold:"SemiBold",demibold:"DemiBold",demi:"DemiBold",bold:"Bold",extrabold:"ExtraBold",ultrabold:"ExtraBold",black:"Black",heavy:"Heavy"}[t];if(r)return r;let o=String(e||"Regular").replace(/[^A-Za-z0-9]+/g,"");return o||"Regular"}function pigmaPhotoshopFontWeight(e){let t=pigmaNormalizePhotoshopFontToken(e);return/black|heavy/.test(t)?900:/extrabold|ultrabold/.test(t)?800:/bold/.test(t)?700:/semibold|demibold|demi/.test(t)?600:/medium/.test(t)?500:/light|demilight|semilight/.test(t)?300:/thin|hairline/.test(t)?100:400}function pigmaPhotoshopStyleTokens(e){let t=pigmaNormalizePhotoshopFontToken(e||"Regular"),r=pigmaNormalizePhotoshopFontToken(pigmaPhotoshopStyleName(e)),o=[t,r],n={regular:["regular","roman","normal","book"],roman:["regular","roman","normal","book"],normal:["regular","roman","normal","book"],book:["regular","roman","normal","book"],semibold:["semibold","demibold","demi"],demibold:["demibold","semibold","demi"],demi:["demibold","semibold","demi"],extralight:["extralight","ultralight"],ultralight:["ultralight","extralight"],black:["black","heavy"],heavy:["heavy","black"]}[t];return n&&o.push(...n),Array.from(new Set(o.filter(Boolean)))}function pigmaPhotoshopFamilyTokens(e,t){let r=pigmaNormalizePhotoshopFontToken(e),o=[r];for(let n of pigmaPhotoshopStyleTokens(t)){n&&r.endsWith(n)&&r.length>n.length+2&&o.push(r.slice(0,-n.length))}return Array.from(new Set(o.filter(Boolean)))}function pigmaMappedPhotoshopFontName(e,t){let r=pigmaPhotoshopFamilyTokens(e,t),o=pigmaPhotoshopStyleTokens(t||"Regular");for(let n of r)for(let i of o){let a=pigmaPhotoshopFontMap[n+"|"+i];if(a)return a}return null}function pigmaKnownPhotoshopFontName(e,t){let r=typeof e=="object"&&e?String(e.family||"").trim():String(e||"").trim(),o=typeof e=="object"&&e?String(e.style||"Regular").trim():String(t||"Regular").trim(),n=pigmaMappedPhotoshopFontName(r,o);if(n)return n;let i=r.toLowerCase(),a=pigmaNormalizePhotoshopFontToken(o),s=i==="lg ei headline ttf"?"LGEIHeadlineTTF":i==="lg ei headline"?"LGEIHeadline":"",u=pigmaPhotoshopStyleName(o);if(s){let c={regular:"Regular",bold:"Bold",semibold:"Semibold",light:"Light",thin:"Thin"}[a]||u;return"".concat(s,"-").concat(c)}let l={"inter":"Inter","inter display":"InterDisplay","noto sans kr":"NotoSansKR","noto serif kr":"NotoSerifKR","noto sans sc":"NotoSansSC","noto serif sc":"NotoSerifSC","noto sans tc":"NotoSansTC","noto serif tc":"NotoSerifTC","noto sans jp":"NotoSansJP","noto serif jp":"NotoSerifJP","noto sans hk":"NotoSansHK","noto serif hk":"NotoSerifHK","noto sans cjk kr":"NotoSansCJKkr","noto serif cjk kr":"NotoSerifCJKkr","noto sans cjk sc":"NotoSansCJKsc","noto serif cjk sc":"NotoSerifCJKsc","noto sans cjk tc":"NotoSansCJKtc","noto serif cjk tc":"NotoSerifCJKtc","noto sans cjk jp":"NotoSansCJKjp","noto serif cjk jp":"NotoSerifCJKjp","noto sans cjk hk":"NotoSansCJKhk","noto serif cjk hk":"NotoSerifCJKhk"}[i];return l?(l==="NotoSerifKR"&&a==="black"&&(u="Heavy"),"".concat(l,"-").concat(u)):null}function sa(e){let t=la(e);if(t)return t;let r=pigmaKnownPhotoshopFontName(e);if(r)return r;let o=e.family.replace(/[^A-Za-z0-9]/g,""),n=e.style.replace(/[^A-Za-z0-9]/g,"");return o==="Arial"?n==="Bold"?"Arial-BoldMT":n==="Italic"?"Arial-ItalicMT":n==="BoldItalic"?"Arial-BoldItalicMT":"ArialMT":o?!n||n==="Regular"?"".concat(o,"-Regular"):"".concat(o,"-").concat(n):"ArialMT"}function la(e){let t=e.family.trim().toLowerCase(),r=e.style.trim().toLowerCase();return t==="italianno"&&r==="regular"?"Italianno Regular":null}function Ye(e,t,r,o,n=[],i){let a=e.length,s=a>1,l=f(t);return{ready:!1,selectionId:s?null:t.id,selectionCount:a,selectionName:no(e),selectionType:io(e),summary:s?'"'.concat(l,'" is not ready for batch export.'):r,detail:s?"".concat(o,' Remove "').concat(l,'" from the selection or export it separately.'):o,documentWidth:s?null:i?d(i.width):null,documentHeight:s?null:i?d(i.height):null,exportNodeCount:0,editableTextCount:0,preservedGroupCount:0,warnings:n.map(u=>Nt(l,u,s))}}function no(e){return e.length===0?"":f(e[0])}function io(e){if(e.length===0)return null;let t=Array.from(new Set(e.map(r=>r.type)));return e.length===1?t[0]:t.length===1?"".concat(t[0]," x ").concat(e.length):"Mixed x ".concat(e.length)}function Nt(e,t,r){return r?"".concat(e,": ").concat(t):t}function ua(e,t){return t.textExportMode==="rasterize-text"?0:e}function ca(e,t,r,o){let n=da(e);return n?Nt(n.rootName,n.warning,t>1):o>0&&r.textExportMode==="rasterize-text"?"Text export is currently set to rasterize text, so text layers will export as bitmap layers until you change the export defaults.":t===1?"Editable text is preserved when fills and typography are compatible. Hidden layers can be ignored or included as hidden PSD layers.":"Each selected root is exported as its own PSD file. When more than one file is ready, the download is packaged as a ZIP archive."}function da(e){let t=null;for(let r of e)for(let o of r.analysis.warnings){let n=pa(o);if((!t||n<t.priority)&&(t={rootName:f(r.node),warning:o,priority:n},n===0))return t}return t}function pa(e){let t=e.toLowerCase();return t.includes("flatten into one bitmap layer")||t.includes("flattened bitmap layer")||t.includes("fell back to a bitmap layer")?0:t.includes("mask")?1:t.includes("smart object")?2:3}function fa(e,t,r=1){return r>1?"".concat(r," roots selected: ").concat(t.exportNodeCount," PSD layers, ").concat(t.editableTextCount," editable text layers, ").concat(t.preservedGroupCount," preserved groups."):'"'.concat(e,'" is ready: ').concat(t.exportNodeCount," PSD layers, ").concat(t.editableTextCount," editable text layers, ").concat(t.preservedGroupCount," preserved groups.")}function ma(e){return"pigma-export-".concat(e,"-files.zip")}function xa(e,t){return e.length===1?He(e[0],t,1,1):ma(e.length)}function f(e){return e.name&&e.name.length>0?e.name:e.type}function qe(e){for(let t of e.nodes)ao(t);e.nodes=[],e.compositePngBytes=null,e.backgroundDebug=[],e.warnings=[]}function ao(e){var t;if(e.kind==="group"){for(let r of e.children)ao(r);e.children=[],((t=e.mask)==null?void 0:t.kind)==="bitmap"&&(e.mask.pngBytes=new Uint8Array(0));return}if(e.kind!=="shape"&&(e.pngBytes=new Uint8Array(0)),e.kind==="vector"&&(e.svgString=""),e.effects)for(let r of e.effects)r.style==="background-blur"&&(r.backdropPngBytes=null)}function ga(e,t,r,o){return{stage:"plugin-export",sourceNodeId:e.id,sourceNodeName:f(e),sourceNodeType:e.type,backgroundNodeId:t.id,backgroundKind:t.kind,groupPath:ya(e),x:t.x,y:t.y,width:t.width,height:t.height,orderIndex:r,siblingCount:o,parentNodeId:e.id,parentNodeName:f(e),note:"컨테이너 fill에서 가상 배경을 만들고 내보내기 모델의 마지막 자식으로 추가했습니다."}}function ha(e,t){e.push(t),console.info("[background-trace][plugin-export]",t)}function ya(e){let t=[],r=e;for(;r&&r.type!=="PAGE"&&r.type!=="DOCUMENT";)"name"in r&&typeof r.name=="string"&&r.name.length>0&&t.unshift(r.name),r=r.parent;return t}function Je(e){let t=e.replace(/[<>:"/\\|?*]/g," "),r=Array.from(t,o=>o.charCodeAt(0)<32?" ":o).join("").trim();return r.length>0?r:"figma-export"}function Tt(e){return Ft(e)}function He(e,t,r,o){let n=Je(e.name||"figma-export"),i={"frame-name":n,"page-name":Je(figma.currentPage.name||"page"),index:o>1?String(r).padStart(2,"0"):String(r)},a=t.fileNamePattern.replace(/\{([a-z-]+)\}/gi,(l,u)=>{var p;let c=String(u).toLowerCase();return(p=i[c])!=null?p:l}),s=Je(a.replace(/\.psd$/i,"").trim()||n);return"".concat(s,".psd")}function Zt(e){return{x:D(h(e.x,0,1)),y:D(h(e.y,0,1))}}function z(e){return h(Math.round(e),0,255)}function d(e){return Math.max(1,Math.round(e))}function M(e){return Math.round(e*100)/100}function x(e){return Math.round(e)}function D(e){return Math.round(e*1e4)/1e4}function Sa(e,t,r){return e>0&&t>0&&e*t>r}function h(e,t,r){return Math.min(r,Math.max(t,e))}var ba=270,Ea=560,Pa=60;figma.showUI(__html__,{width:ba,height:Ea,themeColors:!0});var Dt=er({closePlugin:()=>{figma.closePlugin()},notify:(e,t)=>figma.notify(e,t),postToUi:e=>{figma.ui.postMessage(e)}}),Ve=null;function vt(e=!1){e&&Dt.invalidateSelectionCache(),Ve&&clearTimeout(Ve),Ve=setTimeout(()=>{Ve=null,Dt.postSelectionState()},Pa)}figma.on("selectionchange",()=>{typeof pigmaEnsureSelectionAccess=="function"?pigmaEnsureSelectionAccess().finally(()=>vt(!0)):vt(!0)});figma.on("currentpagechange",()=>{typeof pigmaEnsureSelectionAccess=="function"?pigmaEnsureSelectionAccess().finally(()=>vt(!0)):vt(!0)});figma.ui.onmessage=e=>{Dt.handleUiMessage(e)};(typeof pigmaEnsureSelectionAccess=="function"?pigmaEnsureSelectionAccess():Promise.resolve()).finally(()=>{vt(!0);setTimeout(()=>{vt(!0)},250)});})();

;(()=>{
  // PIGMA_TEXT_IMPORT_GUARD::SOURCE_OF_TRUTH
  // Keep PSD import text guard rules in this file so rebuilds do not depend on
  // ad-hoc edits inside the generated runtime bundle.
  // PIGMA_TEXT_IMPORT_GUARD::BROAD_TEXT_UPDATES_DISABLED_IN_BUNDLE
  // PIGMA_TEXT_IMPORT_GUARD::SOURCE_ID_MATCHING
  // PIGMA_TEXT_IMPORT_GUARD::SOURCE_ID_TAGGING
  const originalOnMessage = figma.ui.onmessage;
  const DEFAULT_BATCH_FRAME_GAP = 10;
  const IMPORT_SOURCE_ID_KEY = "__pigmaImportSourceId";
  const IMPORT_SYNTHETIC_ROOT_KEY = "__pigmaSyntheticImportRoot";
  const IMPORT_POSTPROCESS_DEBUG = true;
  const SVG_IMPORT_BATCH_ROOT_NAME = "SVG Import";
  let availableFontsPromise = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async message => {
    if (isSvgImportMessage(message)) {
      return handleSvgImportMessage(message);
    }

    if (!isImportMessage(message)) {
      return originalOnMessage(message);
    }

    await prepareImportPayload(message);
    await originalOnMessage(message);

    try {
      await Promise.resolve();
      if (message.type === "request-import") {
        fixSingleImport(message.payload, getSelectedImportRoot());
      } else {
        fixBatchImport(message.batch, getSelectedImportRoot());
      }
    } catch (error) {
      console.warn("[pigma-import-text-fix]", error);
    }
  };

  function isImportMessage(message) {
    return !!message && (message.type === "request-import" || message.type === "request-import-batch");
  }

  function isSvgImportMessage(message) {
    return !!message && (message.type === "request-svg-import" || message.type === "request-svg-import-batch");
  }

  async function handleSvgImportMessage(message) {
    const batch = normalizeSvgImportBatch(message);
    if (!batch || batch.items.length === 0) {
      figma.notify("가져올 SVG 파일이 없습니다.", { error: true });
      return;
    }

    const targetPage = await resolveSvgImportPage(batch);
    if (targetPage !== figma.currentPage) {
      await figma.setCurrentPageAsync(targetPage);
    }

    try {
      const importedNodes = [];
      for (const item of batch.items) {
        importedNodes.push(await importSvgPayload(item));
      }

      layoutImportedSvgNodes(importedNodes, batch.gap, figma.viewport.center);
      figma.currentPage.selection = importedNodes;
      figma.viewport.scrollAndZoomIntoView(importedNodes);
      figma.notify(
        importedNodes.length > 1
          ? `SVG ${importedNodes.length}개를 가져왔습니다.`
          : "SVG를 가져왔습니다."
      );
    } catch (error) {
      console.warn("[pigma-svg-import]", error);
      figma.notify(getSvgImportErrorMessage(error), { error: true });
    }
  }

  function normalizeSvgImportBatch(message) {
    if (message && message.type === "request-svg-import" && message.payload) {
      return {
        placement: normalizeSvgPlacement(message.payload.placement),
        gap: DEFAULT_BATCH_FRAME_GAP,
        rootName: normalizeSvgName(message.payload.rootName || SVG_IMPORT_BATCH_ROOT_NAME),
        items: [message.payload],
      };
    }

    if (!message || message.type !== "request-svg-import-batch" || !message.batch) {
      return null;
    }

    return {
      placement: normalizeSvgPlacement(message.batch.placement),
      gap: normalizeSvgGap(message.batch.gap),
      rootName: normalizeSvgName(message.batch.rootName || SVG_IMPORT_BATCH_ROOT_NAME),
      items: Array.isArray(message.batch.items) ? message.batch.items.filter(Boolean) : [],
    };
  }

  function normalizeSvgPlacement(value) {
    return value === "new-page" ? "new-page" : "current-page";
  }

  function normalizeSvgGap(value) {
    return Number.isFinite(value) ? Math.max(0, Math.round(value)) : DEFAULT_BATCH_FRAME_GAP;
  }

  function normalizeSvgName(value) {
    const normalized = typeof value === "string" ? value.trim() : "";
    return normalized.length > 0 ? normalized : SVG_IMPORT_BATCH_ROOT_NAME;
  }

  async function resolveSvgImportPage(batch) {
    if (!batch || batch.placement !== "new-page") {
      return figma.currentPage;
    }

    const page = figma.createPage();
    page.name = batch.rootName;
    return page;
  }

  async function importSvgPayload(payload) {
    const svgText = payload && typeof payload.svgText === "string" ? payload.svgText.trim() : "";
    const svgTextFallback =
      payload && typeof payload.svgTextFallback === "string" ? payload.svgTextFallback.trim() : "";
    const bitmapBackgroundTiles = getBitmapBackgroundTiles(payload);
    const hasBitmapBackground =
      getByteLength(payload && payload.backgroundPngBytes) > 0 || bitmapBackgroundTiles.length > 0;
    if (!svgText && !hasBitmapBackground) {
      const fileLabel = payload && typeof payload.fileName === "string" && payload.fileName.trim().length > 0
        ? payload.fileName.trim()
        : "SVG";
      throw new Error(`${fileLabel} 파일 내용이 비어 있습니다.`);
    }

    const importedNode = hasBitmapBackground
      ? createBitmapSvgImportNode(payload, bitmapBackgroundTiles)
      : figma.createNodeFromSvg(svgText);
    importedNode.name = normalizeSvgName(payload.rootName || payload.fileName || SVG_IMPORT_BATCH_ROOT_NAME);
    let textImportResult = null;
    try {
      textImportResult = await appendEditableTextRuns(importedNode, payload);
    } catch (error) {
      console.warn("[pigma-svg-import] failed to append editable text runs", error);
    }
    if (
      textImportResult &&
      textImportResult.requestedCount > 0 &&
      textImportResult.createdCount === 0 &&
      svgTextFallback &&
      svgTextFallback !== svgText
    ) {
      return replaceSvgImportWithFallback(importedNode, svgTextFallback);
    }
    return importedNode;
  }

  function createBitmapSvgImportNode(payload, bitmapBackgroundTiles) {
    const frame = figma.createFrame();
    const width = Math.max(1, roundNumber(payload && payload.documentWidth));
    const height = Math.max(1, roundNumber(payload && payload.documentHeight));
    frame.name = normalizeSvgName(payload && (payload.rootName || payload.fileName) || SVG_IMPORT_BATCH_ROOT_NAME);
    frame.resize(width, height);
    frame.clipsContent = true;
    frame.strokes = [];
    frame.fills = [];

    if (Array.isArray(bitmapBackgroundTiles) && bitmapBackgroundTiles.length > 0) {
      for (let index = 0; index < bitmapBackgroundTiles.length; index += 1) {
        const tile = bitmapBackgroundTiles[index];
        const background = figma.createRectangle();
        background.name = normalizeSvgName(tile.name || `Background Tile ${index + 1}`);
        background.resize(Math.max(1, roundNumber(tile.width)), Math.max(1, roundNumber(tile.height)));
        background.x = roundNumber(tile.x);
        background.y = roundNumber(tile.y);
        background.strokes = [];
        background.fills = [createBitmapFillFromBytes(tile.pngBytes)];
        frame.appendChild(background);
      }

      return frame;
    }

    const background = figma.createRectangle();
    background.name = "Background PNG";
    background.resize(width, height);
    background.x = 0;
    background.y = 0;
    background.strokes = [];
    background.fills = [createBitmapFillFromBytes(payload.backgroundPngBytes)];
    frame.appendChild(background);

    return frame;
  }

  function createBitmapFillFromBytes(bytes) {
    const normalizedBytes = normalizeImageBytes(bytes);
    const image = figma.createImage(normalizedBytes);
    return {
      type: "IMAGE",
      imageHash: image.hash,
      scaleMode: "FILL",
      visible: true,
      opacity: 1,
    };
  }

  function normalizeImageBytes(bytes) {
    if (bytes instanceof Uint8Array) {
      return bytes;
    }
    if (bytes instanceof ArrayBuffer) {
      return new Uint8Array(bytes);
    }
    if (ArrayBuffer.isView(bytes)) {
      return new Uint8Array(bytes.buffer, bytes.byteOffset, bytes.byteLength);
    }
    throw new Error("The bitmap import payload did not include valid image bytes.");
  }

  function getBitmapBackgroundTiles(payload) {
    if (!payload || !Array.isArray(payload.backgroundTiles)) {
      return [];
    }

    return payload.backgroundTiles
      .map(tile => {
        if (!tile || getByteLength(tile.pngBytes) <= 0) {
          return null;
        }

        return {
          name: typeof tile.name === "string" && tile.name.trim().length > 0 ? tile.name.trim() : "",
          x: roundNumber(tile.x),
          y: roundNumber(tile.y),
          width: Math.max(1, roundNumber(tile.width)),
          height: Math.max(1, roundNumber(tile.height)),
          pngBytes: tile.pngBytes,
        };
      })
      .filter(Boolean);
  }

  async function appendEditableTextRuns(importedNode, payload) {
    const textRuns = getEditableTextRunsFromPayload(payload);
    if (textRuns.length === 0 || !importedNode || typeof importedNode.appendChild !== "function") {
      return {
        requestedCount: textRuns.length,
        createdCount: 0,
      };
    }

    const overlay = createEditableTextOverlay(importedNode);
    if (!overlay) {
      return {
        requestedCount: textRuns.length,
        createdCount: 0,
      };
    }

    const availableFonts = await getAvailableFontsSafely();
    let createdCount = 0;
    let visibleCount = 0;
    for (const run of textRuns) {
      let textNode = null;
      try {
        textNode = await createEditableTextNode(run, availableFonts);
      } catch (error) {
        console.warn("[pigma-svg-import] failed to create one editable text node", error, run);
        continue;
      }
      if (!textNode) {
        continue;
      }

      overlay.appendChild(textNode);
      textNode.x = normalizeEditableTextCoordinate(run.x);
      textNode.y = resolveEditableTextTopCoordinate(run, textNode);
      createdCount += 1;
      if (isRenderableEditableTextNode(textNode)) {
        visibleCount += 1;
      }
    }

    if (visibleCount === 0) {
      overlay.remove();
      return {
        requestedCount: textRuns.length,
        createdCount: visibleCount,
      };
    }

    overlay.name = `Editable Text (${visibleCount}/${textRuns.length})`;
    setSceneNodeExpanded(overlay, true);
    return {
      requestedCount: textRuns.length,
      createdCount: visibleCount,
    };
  }

  function replaceSvgImportWithFallback(importedNode, svgTextFallback) {
    if (!importedNode || typeof importedNode.remove !== "function") {
      return figma.createNodeFromSvg(svgTextFallback);
    }

    const parent = importedNode.parent;
    const index =
      parent && Array.isArray(parent.children) ? parent.children.indexOf(importedNode) : -1;
    const fallbackNode = figma.createNodeFromSvg(svgTextFallback);
    fallbackNode.name = importedNode.name;
    fallbackNode.x = importedNode.x;
    fallbackNode.y = importedNode.y;

    importedNode.remove();

    if (parent && typeof parent.insertChild === "function") {
      try {
        parent.insertChild(index >= 0 ? index : parent.children.length, fallbackNode);
      } catch (error) {
        console.warn("[pigma-svg-import] failed to preserve fallback node order", error);
      }
    }

    return fallbackNode;
  }

  function getEditableTextRunsFromPayload(payload) {
    if (!payload || !Array.isArray(payload.editableTextRuns)) {
      return [];
    }

    return payload.editableTextRuns.filter(run => {
      return !!run && typeof run.characters === "string" && run.characters.trim().length > 0;
    });
  }

  function createEditableTextOverlay(importedNode) {
    if (!importedNode || typeof importedNode.appendChild !== "function") {
      return null;
    }

    const overlay = figma.createFrame();
    overlay.name = "Editable Text";
    overlay.resize(
      Math.max(1, roundNumber(importedNode.width)),
      Math.max(1, roundNumber(importedNode.height))
    );
    overlay.x = 0;
    overlay.y = 0;
    overlay.clipsContent = false;
    overlay.fills = [];
    overlay.strokes = [];
    importedNode.appendChild(overlay);
    return overlay;
  }

  async function createEditableTextNode(run, availableFonts) {
    const characters = normalizeEditableTextCharacters(run && run.characters);
    if (!characters) {
      return null;
    }

    const fontName = await loadEditableTextFont(run, availableFonts);
    if (!fontName) {
      return null;
    }

    const textNode = figma.createText();
    textNode.fontName = fontName;
    textNode.fontSize = normalizeEditableTextFontSize(run);
    textNode.characters = characters;
    applyEditableTextCase(textNode, run);
    applyEditableTextLineHeight(textNode, run);
    applyEditableTextLetterSpacing(textNode, run);
    fitEditableTextFontSizeToRun(textNode, run, characters);
    applyEditableTextLineHeight(textNode, run);
    applyEditableTextSizing(textNode, run, characters);
    applyEditableTextDecoration(textNode, run);
    textNode.name = buildEditableTextLayerName(characters);

    const fillPaint = createEditableTextPaint(run);
    if (fillPaint) {
      textNode.fills = [fillPaint];
    } else {
      textNode.fills = [createSolidPaintFromColor("000000", 1)];
    }

    const rotation = normalizeEditableTextRotation(run && run.rotation);
    if (rotation !== 0) {
      textNode.rotation = rotation;
    }

    return textNode;
  }

  function applyEditableTextCase(textNode, run) {
    if (!textNode || !("textCase" in textNode)) {
      return;
    }

    textNode.textCase = normalizeEditableTextCase(run);
  }

  function normalizeEditableTextCase(run) {
    const normalizedCase = String((run && run.textCase) || "").trim().toLowerCase();
    if (normalizedCase === "upper") {
      return "UPPER";
    }
    if (normalizedCase === "small-caps") {
      return "SMALL_CAPS";
    }
    return "ORIGINAL";
  }

  function applyEditableTextDecoration(textNode, run) {
    if (!textNode || !("textDecoration" in textNode)) {
      return;
    }

    textNode.textDecoration = normalizeEditableTextDecoration(run);
  }

  function normalizeEditableTextDecoration(run) {
    if (run && run.strikeThrough === true) {
      return "STRIKETHROUGH";
    }
    if (run && run.underline === true) {
      return "UNDERLINE";
    }
    return "NONE";
  }

  function applyEditableTextLetterSpacing(textNode, run) {
    if (!textNode || !("letterSpacing" in textNode)) {
      return 0;
    }

    const letterSpacing = Number(run && run.letterSpacing);
    if (!Number.isFinite(letterSpacing)) {
      return 0;
    }

    const normalizedLetterSpacing = Math.round(letterSpacing * 1000) / 1000;
    textNode.letterSpacing = {
      unit: "PIXELS",
      value: normalizedLetterSpacing,
    };
    return normalizedLetterSpacing;
  }

  function applyEditableTextLineHeight(textNode, run) {
    if (!textNode || !("lineHeight" in textNode)) {
      return;
    }

    const resolvedLineHeight = normalizeEditableTextLineHeight(run, textNode.fontSize);
    if (!Number.isFinite(resolvedLineHeight) || resolvedLineHeight <= 0) {
      return;
    }

    textNode.lineHeight = {
      unit: "PIXELS",
      value: resolvedLineHeight,
    };
  }

  function fitEditableTextFontSizeToRun(textNode, run, characters) {
    if (!textNode || typeof textNode.fontSize !== "number") {
      return;
    }

    const currentFontSize = Number(textNode.fontSize);
    const measuredWidth = Number(textNode.width) || 0;
    const measuredHeight = Number(textNode.height) || 0;
    const targetWidth = Number(run && run.width);
    const targetHeight = Number(run && run.height);
    if (!Number.isFinite(currentFontSize) || currentFontSize <= 0 || measuredWidth <= 0 || measuredHeight <= 0) {
      return;
    }

    const ratios = [];
    if (Number.isFinite(targetHeight) && targetHeight > 0.5) {
      ratios.push(clampEditableTextScale(targetHeight / measuredHeight, 0.82, 1.24));
    }

    const hasLineBreak = typeof characters === "string" && /[\r\n]/.test(characters);
    if (!hasLineBreak && Number.isFinite(targetWidth) && targetWidth > 0.5) {
      const widthRatio = targetWidth / measuredWidth;
      if (Math.abs(widthRatio - 1) >= 0.02) {
        ratios.push(clampEditableTextScale(widthRatio, 0.86, 1.16));
      }
    }

    if (ratios.length === 0) {
      return;
    }

    let selectedRatio = ratios[0];
    if (ratios.length > 1) {
      const smallestRatio = Math.min(...ratios);
      const averageRatio = ratios.reduce((sum, ratio) => sum + ratio, 0) / ratios.length;
      selectedRatio = smallestRatio < 1 ? smallestRatio : averageRatio;
    }

    if (!Number.isFinite(selectedRatio) || Math.abs(selectedRatio - 1) < 0.02) {
      return;
    }

    const adjustedFontSize = Math.max(1, roundNumber(currentFontSize * selectedRatio));
    if (Math.abs(adjustedFontSize - currentFontSize) < 0.1) {
      return;
    }

    textNode.fontSize = adjustedFontSize;
  }

  function applyEditableTextSizing(textNode, run, characters) {
    if (!textNode) {
      return;
    }

    const requestedFontSize = normalizeEditableTextFontSize(run);
    const measuredWidth = Number(textNode.width) || 0;
    const measuredHeight = Number(textNode.height) || 0;
    const fallbackWidth = requestedFontSize * Math.max(1, String(characters || "").length * 0.55);
    const targetWidth = Math.max(
      1,
      roundNumber(Number(run && run.width) || 0),
      roundNumber(measuredWidth),
      roundNumber(fallbackWidth)
    );
    const targetHeight = Math.max(
      1,
      roundNumber(Number(run && run.height) || 0),
      roundNumber(measuredHeight),
      roundNumber(requestedFontSize * 1.2)
    );

    try {
      if ("textAutoResize" in textNode) {
        textNode.textAutoResize = "NONE";
      }
      if (typeof textNode.resize === "function") {
        textNode.resize(targetWidth, targetHeight);
      }
    } catch (error) {
      console.warn("[pigma-svg-import] failed to apply explicit text sizing", error);
      if ("textAutoResize" in textNode) {
        textNode.textAutoResize = "WIDTH_AND_HEIGHT";
      }
    }
  }

  function isRenderableEditableTextNode(textNode) {
    if (!textNode) {
      return false;
    }

    return (
      Number.isFinite(textNode.width) &&
      Number.isFinite(textNode.height) &&
      Number(textNode.width) > 0.5 &&
      Number(textNode.height) > 0.5 &&
      typeof textNode.characters === "string" &&
      textNode.characters.trim().length > 0
    );
  }

  function normalizeEditableTextCharacters(value) {
    if (typeof value !== "string") {
      return "";
    }

    return value.replace(/\u0000/g, "").replace(/\r\n?/g, "\n");
  }

  function buildEditableTextLayerName(characters) {
    const normalized = String(characters).replace(/\s+/g, " ").trim();
    if (normalized.length === 0) {
      return "Editable Text";
    }

    return normalized.length > 48 ? normalized.slice(0, 48) : normalized;
  }

  function normalizeEditableTextCoordinate(value) {
    return Number.isFinite(value) ? Math.round(Number(value) * 1000) / 1000 : 0;
  }

  function resolveEditableTextTopCoordinate(run, textNode) {
    const requestedTop = normalizeEditableTextCoordinate(run && run.y);
    if (!textNode) {
      return requestedTop;
    }

    const requestedHeight = Number(run && run.height);
    const measuredHeight = Number(textNode.height);
    if (!Number.isFinite(requestedHeight) || requestedHeight <= 0 || !Number.isFinite(measuredHeight) || measuredHeight <= 0) {
      return requestedTop;
    }

    const topShare = resolveEditableTextTopShare(run);
    return normalizeEditableTextCoordinate(requestedTop + (requestedHeight - measuredHeight) * topShare);
  }

  function resolveEditableTextTopShare(run) {
    const ascentRatio = Number(run && run.ascentRatio);
    const descentRatio = Number(run && run.descentRatio);
    if (Number.isFinite(ascentRatio) && ascentRatio > 0 && Number.isFinite(descentRatio) && descentRatio >= 0) {
      const total = ascentRatio + descentRatio;
      if (total > 0.01) {
        return clampEditableTextScale(ascentRatio / total, 0.45, 0.92);
      }
    }

    return 0.8;
  }

  function normalizeEditableTextFontSize(run) {
    const primarySize = run && Number(run.fontSize);
    if (Number.isFinite(primarySize) && primarySize > 0.1) {
      return Math.max(1, Math.round(primarySize * 1000) / 1000);
    }

    const nominalSize = run && Number(run.nominalFontSize);
    if (Number.isFinite(nominalSize) && nominalSize > 0.1) {
      return Math.max(1, Math.round(nominalSize * 1000) / 1000);
    }

    const fallbackSize = run && Number(run.height);
    if (Number.isFinite(fallbackSize) && fallbackSize > 0.1) {
      return Math.max(1, Math.round(fallbackSize * 0.92 * 1000) / 1000);
    }

    return 12;
  }

  function normalizeEditableTextLineHeight(run, currentFontSize) {
    const explicitLineHeight = run && Number(run.lineHeight);
    if (Number.isFinite(explicitLineHeight) && explicitLineHeight > 0.1) {
      return Math.max(1, Math.round(explicitLineHeight * 1000) / 1000);
    }

    const lineHeightRatio = run && Number(run.lineHeightRatio);
    const fontSize = Number(currentFontSize);
    if (Number.isFinite(lineHeightRatio) && lineHeightRatio > 0.1 && Number.isFinite(fontSize) && fontSize > 0.1) {
      return Math.max(1, Math.round(fontSize * lineHeightRatio * 1000) / 1000);
    }

    return NaN;
  }

  function normalizeEditableTextRotation(value) {
    return Number.isFinite(value) ? Math.round(Number(value) * 1000) / 1000 : 0;
  }

  function clampEditableTextScale(value, min, max) {
    if (!Number.isFinite(value)) {
      return 1;
    }

    return Math.max(min, Math.min(max, Number(value)));
  }

  function createEditableTextPaint(run) {
    if (!run || typeof run.fillColor !== "string") {
      return null;
    }

    return createSolidPaintFromColor(run.fillColor, run.fillOpacity);
  }

  function createSolidPaintFromColor(value, opacityValue) {
    const normalized = typeof value === "string" ? value.trim() : "";
    if (!normalized) {
      return null;
    }

    const hexMatch = normalized.match(/^#?([0-9a-f]{3}|[0-9a-f]{6})$/i);
    let fullHex = "";
    if (hexMatch) {
      const compactHex = hexMatch[1];
      fullHex =
        compactHex.length === 3
          ? compactHex[0] + compactHex[0] + compactHex[1] + compactHex[1] + compactHex[2] + compactHex[2]
          : compactHex;
    } else {
      const rgbMatch = normalized.match(
        /^rgba?\(\s*([0-9.]+)\s*,\s*([0-9.]+)\s*,\s*([0-9.]+)(?:\s*,\s*[0-9.]+\s*)?\)$/i
      );
      if (!rgbMatch) {
        return null;
      }

      fullHex = rgbMatch
        .slice(1, 4)
        .map(channel => Math.max(0, Math.min(255, Math.round(Number(channel) || 0))).toString(16).padStart(2, "0"))
        .join("");
    }

    const red = Number.parseInt(fullHex.slice(0, 2), 16);
    const green = Number.parseInt(fullHex.slice(2, 4), 16);
    const blue = Number.parseInt(fullHex.slice(4, 6), 16);
    if (!Number.isFinite(red) || !Number.isFinite(green) || !Number.isFinite(blue)) {
      return null;
    }

    const paint = {
      type: "SOLID",
      color: {
        r: red / 255,
        g: green / 255,
        b: blue / 255
      }
    };
    if (Number.isFinite(opacityValue) && opacityValue >= 0 && opacityValue < 1) {
      paint.opacity = Math.max(0, Math.min(1, Number(opacityValue)));
    }
    return paint;
  }

  async function loadEditableTextFont(run, availableFonts) {
    const candidates = getEditableTextFontCandidates(run, availableFonts);
    for (const fontName of candidates) {
      try {
        await figma.loadFontAsync(fontName);
        return fontName;
      } catch (error) {
        console.warn("[pigma-svg-import] failed to load font", fontName, error);
      }
    }

    return null;
  }

  function getEditableTextFontCandidates(run, availableFonts) {
    const candidates = [];
    const requestedStyle = getRequestedEditableFontStyle(run);
    const requestedFamilies = getRequestedEditableFontFamilies(run);

    for (const requestedFamily of requestedFamilies) {
      const splitRequest = splitRequestedEditableFontFamily(requestedFamily);

      pushDirectFontNameCandidate(candidates, requestedFamily, requestedStyle);
      pushFontNameCandidate(candidates, findExactFontMatch({
        fontFamily: requestedFamily,
        fontStyle: requestedStyle
      }, availableFonts));

      if (splitRequest.family && splitRequest.family !== requestedFamily) {
        pushDirectFontNameCandidate(candidates, splitRequest.family, requestedStyle);
        pushFontNameCandidate(candidates, findExactFontMatch({
          fontFamily: splitRequest.family,
          fontStyle: requestedStyle
        }, availableFonts));
        if (splitRequest.style) {
          pushDirectFontNameCandidate(candidates, splitRequest.family, splitRequest.style);
          pushFontNameCandidate(candidates, findExactFontMatch({
            fontFamily: splitRequest.family,
            fontStyle: splitRequest.style
          }, availableFonts));
        }
      }

      pushFontNameCandidate(candidates, findFamilyFontMatch(requestedFamily, run, availableFonts));
      if (splitRequest.family && splitRequest.family !== requestedFamily) {
        pushFontNameCandidate(candidates, findFamilyFontMatch(splitRequest.family, run, availableFonts));
      }
    }

    pushKnownEditableFontFallbacks(candidates, run);
    pushFontNameCandidate(candidates, findFallbackFontName(availableFonts));
    return candidates;
  }

  function pushDirectFontNameCandidate(candidates, family, style) {
    const normalizedFamily = cleanupEditableFontFamily(family);
    if (!normalizedFamily) {
      return;
    }

    pushFontNameCandidate(candidates, {
      family: normalizedFamily,
      style: normalizeEditableFontStyle(style) || "Regular"
    });
  }

  function pushKnownEditableFontFallbacks(candidates, run) {
    const styleCandidates = buildEditableFontStyleCandidates(run);
    const families = [
      "LGEIText",
      "Malgun Gothic",
      "Apple SD Gothic Neo",
      "Noto Sans KR",
      "Noto Sans CJK KR",
      "Arial",
      "Inter",
      "Roboto"
    ];

    for (const family of families) {
      for (const style of styleCandidates) {
        pushDirectFontNameCandidate(candidates, family, style);
      }
    }
  }

  function getRequestedEditableFontFamily(run) {
    const preferred = cleanupEditableFontFamily(run && run.fontFamily);
    return preferred || "Arial";
  }

  function getRequestedEditableFontFamilies(run) {
    const families = [];
    pushRequestedEditableFontFamily(families, run && run.fontFamily);
    pushRequestedEditableFontFamily(families, run && run.fontFamilyRaw);
    if (families.length === 0) {
      families.push("Arial");
    }
    return families;
  }

  function pushRequestedEditableFontFamily(families, value) {
    const normalized = cleanupEditableFontFamily(value);
    if (!normalized || families.indexOf(normalized) !== -1) {
      return;
    }

    families.push(normalized);
  }

  function getRequestedEditableFontStyle(run) {
    const preferred = normalizeEditableFontStyle(run && run.fontStyle);
    return preferred || "Regular";
  }

  function cleanupEditableFontFamily(value) {
    if (typeof value !== "string") {
      return "";
    }

    return value
      .trim()
      .replace(/^["']+|["']+$/g, "")
      .replace(/(?:PS)?MT$/i, "")
      .replace(/\s+/g, " ")
      .trim();
  }

  function normalizeEditableFontStyle(value) {
    const normalized = typeof value === "string" ? value.replace(/[_-]+/g, " ").trim().toLowerCase() : "";
    if (!normalized) {
      return "";
    }

    if (
      (normalized.indexOf("bold") !== -1 && normalized.indexOf("italic") !== -1) ||
      (normalized.indexOf("bold") !== -1 && normalized.indexOf("oblique") !== -1)
    ) {
      return "Bold Italic";
    }
    if (
      (normalized.indexOf("semi") !== -1 || normalized.indexOf("demi") !== -1) &&
      normalized.indexOf("bold") !== -1
    ) {
      return "Semi Bold";
    }
    if (
      (normalized.indexOf("extra") !== -1 || normalized.indexOf("ultra") !== -1) &&
      normalized.indexOf("bold") !== -1
    ) {
      return "Extra Bold";
    }
    if (normalized.indexOf("bold") !== -1) {
      return "Bold";
    }
    if (normalized.indexOf("medium") !== -1) {
      return "Medium";
    }
    if (normalized.indexOf("light") !== -1) {
      return "Light";
    }
    if (normalized.indexOf("thin") !== -1) {
      return "Thin";
    }
    if (normalized.indexOf("black") !== -1 || normalized.indexOf("heavy") !== -1) {
      return "Black";
    }
    if (normalized.indexOf("italic") !== -1 || normalized.indexOf("oblique") !== -1) {
      return "Italic";
    }
    if (normalized.indexOf("book") !== -1) {
      return "Book";
    }
    if (normalized.indexOf("roman") !== -1 || normalized.indexOf("regular") !== -1) {
      return "Regular";
    }

    return "";
  }

  function splitRequestedEditableFontFamily(value) {
    const normalized = cleanupEditableFontFamily(value);
    if (!normalized) {
      return { family: "", style: "" };
    }

    const styleMatch = normalized.match(
      /(?:^|[-\s])(bold[\s-]*italic|italic[\s-]*bold|semi[\s-]*bold|demi[\s-]*bold|extra[\s-]*bold|ultra[\s-]*bold|bold|medium|light|thin|black|heavy|italic|oblique|regular|roman|book)$/i
    );
    const rawStyle = styleMatch ? styleMatch[1] : "";
    const family = cleanupEditableFontFamily(
      styleMatch ? normalized.slice(0, styleMatch.index).replace(/[-\s]+$/, "") : normalized
    );
    return {
      family: family || normalized,
      style: normalizeEditableFontStyle(rawStyle)
    };
  }

  function buildEditableFontStyleCandidates(run) {
    const candidates = [];
    const requestedStyle = getRequestedEditableFontStyle(run);
    const weight = Number(run && run.fontWeight);
    const normalizedWeight = Number.isFinite(weight) ? weight : 400;

    pushEditableFontStyleCandidate(candidates, requestedStyle);
    if (requestedStyle === "Bold Italic") {
      pushEditableFontStyleCandidate(candidates, "Italic");
      pushEditableFontStyleCandidate(candidates, "Bold");
    } else if (requestedStyle === "Italic") {
      pushEditableFontStyleCandidate(candidates, "Regular");
    }

    if (normalizedWeight >= 800) {
      pushEditableFontStyleCandidate(candidates, "Black");
      pushEditableFontStyleCandidate(candidates, "Extra Bold");
    }
    if (normalizedWeight >= 700) {
      pushEditableFontStyleCandidate(candidates, "Bold");
    } else if (normalizedWeight >= 600) {
      pushEditableFontStyleCandidate(candidates, "Semi Bold");
    } else if (normalizedWeight >= 500) {
      pushEditableFontStyleCandidate(candidates, "Medium");
    } else if (normalizedWeight <= 300) {
      pushEditableFontStyleCandidate(candidates, "Light");
    }

    pushEditableFontStyleCandidate(candidates, "Regular");
    pushEditableFontStyleCandidate(candidates, "Book");
    pushEditableFontStyleCandidate(candidates, "Roman");
    return candidates;
  }

  function pushEditableFontStyleCandidate(candidates, value) {
    const normalized = normalizeEditableFontStyle(value);
    if (!normalized) {
      return;
    }

    for (const existing of candidates) {
      if (normalizeFontToken(existing) === normalizeFontToken(normalized)) {
        return;
      }
    }

    candidates.push(normalized);
  }

  function getEditableFontFamilyTokens(value) {
    const tokens = [];
    const cleaned = cleanupEditableFontFamily(value);
    const split = splitRequestedEditableFontFamily(cleaned);
    pushEditableFontFamilyToken(tokens, cleaned);
    pushEditableFontFamilyToken(tokens, split.family);
    return tokens;
  }

  function pushEditableFontFamilyToken(tokens, value) {
    const normalized = normalizeFontToken(cleanupEditableFontFamily(value));
    if (!normalized || tokens.indexOf(normalized) !== -1) {
      return;
    }

    tokens.push(normalized);
  }

  function findFamilyFontMatch(family, run, availableFonts) {
    if (!Array.isArray(availableFonts) || availableFonts.length === 0) {
      return null;
    }

    const familyTokens = getEditableFontFamilyTokens(family);
    if (familyTokens.length === 0) {
      return null;
    }

    const exactMatches = [];
    const partialMatches = [];
    for (const entry of availableFonts) {
      if (!entry || !entry.fontName) {
        continue;
      }

      const entryFamily = normalizeFontToken(cleanupEditableFontFamily(entry.fontName.family));
      if (!entryFamily) {
        continue;
      }

      if (familyTokens.indexOf(entryFamily) !== -1) {
        exactMatches.push(entry);
        continue;
      }

      if (familyTokens.some(token => token.indexOf(entryFamily) !== -1 || entryFamily.indexOf(token) !== -1)) {
        partialMatches.push(entry);
      }
    }

    return selectFontEntryByStyle(exactMatches.length > 0 ? exactMatches : partialMatches, run);
  }

  function selectFontEntryByStyle(entries, run) {
    if (!Array.isArray(entries) || entries.length === 0) {
      return null;
    }

    const styleCandidates = buildEditableFontStyleCandidates(run);
    for (const styleName of styleCandidates) {
      const normalizedStyle = normalizeFontToken(styleName);
      for (const entry of entries) {
        if (!entry || !entry.fontName) {
          continue;
        }

        if (normalizeFontToken(entry.fontName.style) === normalizedStyle) {
          return entry.fontName;
        }
      }
    }

    for (const fallbackStyle of ["Regular", "Book", "Roman"]) {
      const normalizedFallbackStyle = normalizeFontToken(fallbackStyle);
      for (const entry of entries) {
        if (!entry || !entry.fontName) {
          continue;
        }

        if (normalizeFontToken(entry.fontName.style) === normalizedFallbackStyle) {
          return entry.fontName;
        }
      }
    }

    return entries[0] && entries[0].fontName ? entries[0].fontName : null;
  }

  function findFallbackFontName(availableFonts) {
    if (!Array.isArray(availableFonts) || availableFonts.length === 0) {
      return null;
    }

    let fallback = findFamilyFontMatch("Arial", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    fallback = findFamilyFontMatch("Inter", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    fallback = findFamilyFontMatch("Roboto", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    return selectFontEntryByStyle(availableFonts, { fontStyle: "Regular", fontWeight: 400 });
  }

  function pushFontNameCandidate(candidates, fontName) {
    if (!fontName || typeof fontName !== "object") {
      return;
    }

    const candidateKey = normalizeFontToken(fontName.family) + ":" + normalizeFontToken(fontName.style);
    for (const existing of candidates) {
      const existingKey = normalizeFontToken(existing.family) + ":" + normalizeFontToken(existing.style);
      if (existingKey === candidateKey) {
        return;
      }
    }

    candidates.push(fontName);
  }

  function layoutImportedSvgNodes(nodes, gap, center) {
    if (!Array.isArray(nodes) || nodes.length === 0) {
      return;
    }

    const resolvedGap = normalizeSvgGap(gap);
    const totalHeight =
      nodes.reduce((sum, node) => sum + Math.max(1, roundNumber(node.height)), 0) +
      Math.max(0, nodes.length - 1) * resolvedGap;
    const maxWidth = nodes.reduce((max, node) => Math.max(max, Math.max(1, roundNumber(node.width))), 1);
    const centerX = center && Number.isFinite(center.x) ? center.x : 0;
    const centerY = center && Number.isFinite(center.y) ? center.y : 0;
    const startX = roundNumber(centerX - maxWidth / 2);
    let cursorY = roundNumber(centerY - totalHeight / 2);

    for (const node of nodes) {
      const nodeWidth = Math.max(1, roundNumber(node.width));
      node.x = roundNumber(startX + (maxWidth - nodeWidth) / 2);
      node.y = cursorY;
      cursorY += Math.max(1, roundNumber(node.height)) + resolvedGap;
    }
  }

  function getSvgImportErrorMessage(error) {
    if (error instanceof Error && typeof error.message === "string" && error.message.trim().length > 0) {
      return `SVG 가져오기에 실패했습니다. ${error.message.trim()}`;
    }

    return "SVG 가져오기에 실패했습니다.";
  }

  async function prepareImportPayload(message) {
    if (message.type === "request-import") {
      prepareImportPayloadItem(message.payload);
      return;
    }

    if (!message.batch || !Array.isArray(message.batch.items)) {
      return;
    }

    for (const item of message.batch.items) {
      prepareImportPayloadItem(item);
    }
  }

  function prepareImportPayloadItem(payload) {
    normalizeImportRoot(payload);
  }

  function getNormalizedPayloadNodes(payload) {
    if (!payload || payload.mode === "flatten-image" || !Array.isArray(payload.nodes)) {
      return null;
    }

    if (payload.nodes.length !== 1) {
      return payload.nodes;
    }

    return getCollapsedRootChildren(payload, payload.nodes[0]) || payload.nodes;
  }

  function getSelectedImportRoot() {
    return figma.currentPage.selection.length > 0 ? figma.currentPage.selection[0] : null;
  }

  function downgradeTextNodes(payload, availableFonts) {
    if (!payload || payload.mode === "flatten-image" || !Array.isArray(payload.nodes)) {
      return;
    }

    const warnings = Array.isArray(payload.warnings) ? payload.warnings : [];
    if (!Array.isArray(payload.warnings)) {
      payload.warnings = warnings;
    }

    const forceRasterizeAllTextPayload = shouldForceRasterizeAllTextPayload(payload);
    const forceRasterizeMultiTextPayload = !forceRasterizeAllTextPayload && shouldRasterizeMultiTextPayload(payload);

    if (forceRasterizeAllTextPayload) {
      warnings.push("This PSD was imported with bitmap text layers because editable text reconstruction is disabled to prevent cross-layer text updates.");
    } else if (forceRasterizeMultiTextPayload) {
      warnings.push("This PSD was imported with bitmap text layers because multi-text editable reconstruction is disabled to avoid cross-layer text updates.");
    }

    payload.nodes = payload.nodes.map(node =>
      downgradeNode(node, availableFonts, warnings, forceRasterizeAllTextPayload || forceRasterizeMultiTextPayload)
    );
  }

  function shouldForceRasterizeAllTextPayload(payload) {
    const payloadNodes = getNormalizedPayloadNodes(payload);
    return countImportedTextNodes(payloadNodes) > 0;
  }

  // Legacy bitmap fallback helper kept for emergency debugging only.
  function shouldRasterizeMultiTextPayload(payload) {
    const payloadNodes = getNormalizedPayloadNodes(payload);
    return countImportedTextNodes(payloadNodes) > 1;
  }

  function normalizeImportRoot(payload) {
    const normalizedNodes = getNormalizedPayloadNodes(payload);
    if (normalizedNodes) {
      payload.nodes = normalizedNodes;
    }
  }

  function getCollapsedRootChildren(payload, node) {
    if (!shouldCollapseRootGroup(payload, node)) {
      return null;
    }

    const children = node.children.slice();
    const documentWidth = normalizeDimension(payload.documentWidth, 0);
    const documentHeight = normalizeDimension(payload.documentHeight, 0);
    const rootMatchesDocumentSize =
      documentWidth > 0 &&
      documentHeight > 0 &&
      dimensionsMatch(node.width, documentWidth) &&
      dimensionsMatch(node.height, documentHeight) &&
      isNearOrigin(node.x) &&
      isNearOrigin(node.y);

    if (rootMatchesDocumentSize) {
      return children;
    }

    const preferredCanvasBounds = findPreferredCanvasBounds(children);
    const preferredMatchesDocumentSize =
      !!preferredCanvasBounds &&
      documentWidth > 0 &&
      documentHeight > 0 &&
      dimensionsMatch(preferredCanvasBounds.width, documentWidth) &&
      dimensionsMatch(preferredCanvasBounds.height, documentHeight);

    if (!preferredCanvasBounds || !preferredMatchesDocumentSize) {
      return null;
    }

    const deltaX = -preferredCanvasBounds.x;
    const deltaY = -preferredCanvasBounds.y;
    return children.map(child => offsetImportedNode(child, deltaX, deltaY));
  }

  function shouldCollapseRootGroup(payload, node) {
    if (!node || node.kind !== "group" || !Array.isArray(node.children) || node.children.length === 0) {
      return false;
    }

    const payloadName = normalizeLayerName(payload.rootName);
    const nodeName = normalizeLayerName(node.name);
    if (payloadName.length === 0 || payloadName !== nodeName) {
      return false;
    }

    if (!node.visible || !isNeutralOpacity(node.opacity) || !isNeutralBlendMode(node.blendMode)) {
      return false;
    }

    if (node.effects !== null || node.strokeEffect !== null) {
      return false;
    }

    if (!isPositiveDimension(node.width) || !isPositiveDimension(node.height)) {
      return false;
    }

    return true;
  }

  function dimensionsMatch(value, expected) {
    return isPositiveDimension(value) && isPositiveDimension(expected) && Math.abs(Number(value) - Number(expected)) <= 1;
  }

  function isNearOrigin(value) {
    return Math.abs(Number(value) || 0) <= 1;
  }

  function normalizeLayerName(value) {
    return typeof value === "string" ? value.trim().toLowerCase() : "";
  }

  function isNeutralOpacity(value) {
    const opacity = Number.isFinite(value) ? Number(value) : 1;
    return Math.abs(opacity - 1) <= 0.001;
  }

  function isNeutralBlendMode(value) {
    return value === "normal" || value === "pass through";
  }

  function isPositiveDimension(value) {
    return Number.isFinite(value) && Number(value) > 0;
  }

  function normalizeDimension(preferredValue, fallbackValue) {
    if (isPositiveDimension(preferredValue)) {
      return Math.max(1, Math.round(Number(preferredValue)));
    }

    if (isPositiveDimension(fallbackValue)) {
      return Math.max(1, Math.round(Number(fallbackValue)));
    }

    return 1;
  }

  function offsetImportedNode(node, deltaX, deltaY) {
    if (!node) {
      return node;
    }

    const shiftedNode = Object.assign({}, node, {
      x: roundNumber((Number(node.x) || 0) + deltaX),
      y: roundNumber((Number(node.y) || 0) + deltaY)
    });

    if (node.kind === "text" && node.text) {
      shiftedNode.text = offsetTextMetadata(node.text, deltaX, deltaY);
    }

    return shiftedNode;
  }

  function offsetTextMetadata(text, deltaX, deltaY) {
    const transform = Array.isArray(text.transform) ? text.transform.slice(0, 6) : null;
    return Object.assign({}, text, {
      transform: transform && transform.length >= 6
        ? [
            transform[0],
            transform[1],
            transform[2],
            transform[3],
            roundNumber((Number(transform[4]) || 0) + deltaX),
            roundNumber((Number(transform[5]) || 0) + deltaY)
          ]
        : text.transform,
      bounds: offsetBounds(text.bounds, deltaX, deltaY),
      boundingBox: offsetBounds(text.boundingBox, deltaX, deltaY)
    });
  }

  function offsetBounds(bounds, deltaX, deltaY) {
    if (!bounds) {
      return bounds;
    }

    return {
      left: roundNumber((Number(bounds.left) || 0) + deltaX),
      top: roundNumber((Number(bounds.top) || 0) + deltaY),
      right: roundNumber((Number(bounds.right) || 0) + deltaX),
      bottom: roundNumber((Number(bounds.bottom) || 0) + deltaY)
    };
  }

  function findPreferredCanvasBounds(nodes) {
    return collectPayloadVisibleBounds(nodes, 0, 0);
  }

  function collectPayloadVisibleBounds(nodes, offsetX, offsetY) {
    if (!Array.isArray(nodes)) {
      return null;
    }

    let bounds = null;
    for (const node of nodes) {
      if (!node || node.visible === false) {
        continue;
      }

      const absoluteX = roundNumber((Number(node.x) || 0) + offsetX);
      const absoluteY = roundNumber((Number(node.y) || 0) + offsetY);

      if (node.kind === "group" && Array.isArray(node.children)) {
        bounds = mergeRectBounds(bounds, collectPayloadVisibleBounds(node.children, absoluteX, absoluteY));
        continue;
      }

      if (!isPositiveDimension(node.width) || !isPositiveDimension(node.height)) {
        continue;
      }

      const width = Math.max(1, Math.round(Number(node.width)));
      const height = Math.max(1, Math.round(Number(node.height)));
      bounds = mergeRectBounds(bounds, {
        x: absoluteX,
        y: absoluteY,
        width,
        height
      });
    }

    return bounds;
  }

  function isBackgroundLikeName(value) {
    if (typeof value !== "string") {
      return false;
    }

    return /\b(background|bg|backdrop|canvas)\b/i.test(value.trim());
  }

  function roundNumber(value) {
    return Number.isFinite(value) ? Math.round(Number(value)) : 0;
  }

  function downgradeNode(node, availableFonts, warnings, forceRasterizeText) {
    if (!node) {
      return node;
    }

    if (node.kind === "group" && Array.isArray(node.children)) {
      node.children = node.children.map(child => downgradeNode(child, availableFonts, warnings, forceRasterizeText));
      return node;
    }

    if (node.kind !== "text") {
      return node;
    }

    if (forceRasterizeText && getByteLength(node.pngBytes) === 0) {
      return node;
    }

    if (!forceRasterizeText && !shouldRasterizeTextNode(node, availableFonts)) {
      return node;
    }

    if (!forceRasterizeText) {
      warnings.push(buildRasterizeWarning(node));
    }
    return {
      kind: "bitmap",
      name: node.name,
      x: node.x,
      y: node.y,
      width: node.width,
      height: node.height,
      opacity: node.opacity,
      visible: node.visible,
      blendMode: node.blendMode,
      effects: node.effects,
      strokeEffect: node.strokeEffect,
      pngBytes: node.pngBytes
    };
  }

  // Legacy bitmap fallback helper kept for emergency debugging only.
  function shouldRasterizeTextNode(node, availableFonts) {
    if (!node || node.kind !== "text" || getByteLength(node.pngBytes) === 0 || !node.text) {
      return false;
    }

    if (isLargeSingleGlyphText(node)) {
      return true;
    }

    const styleRuns = Array.isArray(node.text.styleRuns) ? node.text.styleRuns : [];
    if (styleRuns.length === 0) {
      return false;
    }

    return styleRuns.some(run => !findExactFontMatch(run.style, availableFonts));
  }

  function buildRasterizeWarning(node) {
    const quotedName = typeof node.name === "string" && node.name.trim().length > 0 ? `"${node.name}"` : "This text layer";
    if (isLargeSingleGlyphText(node)) {
      return `${quotedName} was imported as a bitmap to preserve a large single-glyph Photoshop text layer.`;
    }
    return `${quotedName} was imported as a bitmap because an exact Figma font match was not available.`;
  }

  function isLargeSingleGlyphText(node) {
    if (!node || node.kind !== "text" || !node.text || typeof node.text.value !== "string") {
      return false;
    }

    const glyphCount = node.text.value.replace(/\s+/g, "").length;
    if (glyphCount !== 1) {
      return false;
    }

    const baseStyle = node.text.baseStyle || {};
    const fontSize = Number.isFinite(baseStyle.fontSize) ? Number(baseStyle.fontSize) : 0;
    const width = Number.isFinite(node.width) ? Number(node.width) : 0;
    const height = Number.isFinite(node.height) ? Number(node.height) : 0;
    return fontSize >= 72 || width >= 160 || height >= 160;
  }

  function findExactFontMatch(style, availableFonts) {
    if (!style || !Array.isArray(availableFonts)) {
      return null;
    }

    const targetFamily = normalizeFontToken(style.fontFamily);
    const targetStyle = normalizeFontToken(style.fontStyle);
    if (targetFamily.length === 0 || targetStyle.length === 0) {
      return null;
    }

    for (const entry of availableFonts) {
      if (!entry || !entry.fontName) {
        continue;
      }

      const family = normalizeFontToken(entry.fontName.family);
      const fontStyle = normalizeFontToken(entry.fontName.style);
      if (family === targetFamily && fontStyle === targetStyle) {
        return entry.fontName;
      }
    }

    return null;
  }

  async function getAvailableFonts() {
    if (!availableFontsPromise) {
      availableFontsPromise = figma.listAvailableFontsAsync().catch(error => {
        availableFontsPromise = null;
        throw error;
      });
    }

    return availableFontsPromise;
  }

  async function getAvailableFontsSafely() {
    try {
      return await getAvailableFonts();
    } catch (error) {
      console.warn("[pigma-import-text-fix] failed to read available fonts", error);
      return [];
    }
  }

  function normalizeFontToken(value) {
    return typeof value === "string" ? value.trim().toLowerCase().replace(/[^a-z0-9]/g, "") : "";
  }

  function fixSingleImport(payload, root) {
    if (!payload || !hasChildren(root)) {
      debugImportPostprocess("single-skip", "missing-root");
      return;
    }

    markSyntheticImportRoot(root);
    const importRoot = flattenImportedDuplicateRoot(root, payload) || root;
    debugImportPostprocess(
      "single-root",
      [
        `selected=${safeNodeName(root)}`,
        `removed=${!!root.removed}`,
        `result=${safeNodeName(importRoot)}`,
        `resultParent=${safeNodeName(importRoot && importRoot.parent)}`
      ].join(" | ")
    );

    const payloadNodes = getNormalizedPayloadNodes(payload);
    if (!payloadNodes || payload.mode === "flatten-image") {
      setExpandedRecursively(importRoot, true);
      selectImportedNodes([importRoot]);
      return;
    }

    if (shouldApplyScopedTextFixes(payloadNodes, importRoot)) {
      applyTextFixes(payloadNodes, importRoot);
    }

    materializeSingleImportArtboardFrames(importRoot, payload, payloadNodes);

    const separatedArtboards = separateSingleImportArtboardRoot(importRoot, payload);
    if (separatedArtboards.length > 0) {
      separatedArtboards.forEach(node => setExpandedRecursively(node, true));
      selectImportedNodes(separatedArtboards);
      debugImportPostprocess(
        "single-artboards",
        `released=${separatedArtboards.length} | source=${safeNodeName(importRoot)}`
      );
      return;
    }

    setExpandedRecursively(importRoot, true);
    selectImportedNodes([importRoot]);
  }

  function fixBatchImport(batch, root) {
    if (!batch || !hasChildren(root) || !Array.isArray(batch.items)) {
      debugImportPostprocess("batch-skip", "missing-root");
      return;
    }

    const items = batch.items.filter(isBatchItemImportable);
    const sections = separateStitchedBatchSections(batch, root, items);
    const count = Math.min(items.length, sections.length);
    const normalizedSections = [];

    for (let index = 0; index < count; index += 1) {
      const item = items[index];
      const section = sections[index];
      if (!item || !hasChildren(section)) {
        continue;
      }

      markSyntheticImportRoot(section);
      const importSection = flattenImportedDuplicateRoot(section, item) || section;
      normalizedSections.push(importSection);

      const payloadNodes = getNormalizedPayloadNodes(item);
      if (!payloadNodes || item.mode === "flatten-image") {
        setExpandedRecursively(importSection, true);
        continue;
      }

      if (shouldApplyScopedTextFixes(payloadNodes, importSection)) {
        applyTextFixes(payloadNodes, importSection);
      }
      setExpandedRecursively(importSection, true);
    }

    if (!root.removed) {
      setExpandedRecursively(root, true);
    }

    selectImportedNodes(normalizedSections);
    debugImportPostprocess("batch-root", `sections=${normalizedSections.length} | rootRemoved=${!!root.removed}`);
  }

  function separateStitchedBatchSections(batch, root, items) {
    if (!shouldSeparateStitchedBatchRoot(batch, root, items)) {
      return Array.from(root.children);
    }

    const parentNode = root && root.parent && hasChildren(root.parent) ? root.parent : null;
    if (!parentNode) {
      return Array.from(root.children);
    }

    const rootBounds = getAbsoluteBounds(root, true);
    const sections = Array.from(root.children);
    const insertionIndex = Math.max(0, Array.from(parentNode.children).indexOf(root));

    for (let index = 0; index < sections.length; index += 1) {
      reparentSceneNode(sections[index], parentNode, insertionIndex + index, 0, 0);
    }

    arrangeBatchSectionsVertically(sections, rootBounds, getBatchFrameGap(batch));

    try {
      root.remove();
    } catch (error) {
      console.warn("[pigma-import-text-fix] failed to remove stitched batch root", error);
    }

    selectImportedBatchSections(sections);
    return sections;
  }

  function shouldSeparateStitchedBatchRoot(batch, root, items) {
    if (!batch || !hasChildren(root)) {
      return false;
    }

    const arrangement = typeof batch.arrangement === "string" ? batch.arrangement : "stitch-vertical";
    if (arrangement !== "stitch-vertical") {
      return false;
    }

    const rootName = normalizeLayerName(root.name);
    if (!rootName.endsWith("stitched")) {
      return false;
    }

    const sections = Array.from(root.children).filter(node => {
      return node && isPositiveDimension(node.width) && isPositiveDimension(node.height);
    });

    if (sections.length < 2) {
      return false;
    }

    if (Array.isArray(items) && items.length > 1 && sections.length < items.length) {
      return false;
    }

    return true;
  }

  function getBatchFrameGap(batch) {
    const gap = Number(batch == null ? void 0 : batch.gap);
    if (Number.isFinite(gap) && gap > 0) {
      return Math.max(0, Math.round(gap));
    }

    return DEFAULT_BATCH_FRAME_GAP;
  }

  function arrangeBatchSectionsVertically(sections, rootBounds, gap) {
    if (!Array.isArray(sections) || sections.length === 0) {
      return;
    }

    const anchorBounds = rootBounds || getAbsoluteBounds(sections[0], true);
    if (!anchorBounds) {
      return;
    }

    const targetX = roundNumber(anchorBounds.x);
    let targetY = roundNumber(anchorBounds.y);
    const itemGap = Math.max(0, Math.round(gap));

    for (const section of sections) {
      const bounds = getAbsoluteBounds(section, true);
      if (!bounds) {
        continue;
      }

      translateSceneNode(section, targetX - bounds.x, targetY - bounds.y);
      targetY += Math.max(1, roundNumber(bounds.height)) + itemGap;
    }
  }

  function selectImportedBatchSections(sections) {
    if (!Array.isArray(sections) || sections.length === 0) {
      return;
    }

    const selectable = sections.filter(node => node && node.parent === figma.currentPage);
    if (selectable.length === 0) {
      return;
    }

    try {
      figma.currentPage.selection = selectable;
    } catch (error) {
      // Ignore selection assignment failures in restricted contexts.
    }
  }

  function flattenImportedDuplicateRoot(root, payload) {
    if (!hasChildren(root)) {
      return root;
    }

    if (isSyntheticImportRoot(root)) {
      const releasedNodes = releaseSyntheticImportRoot(root);
      debugImportPostprocess(
        "synthetic-root",
        `root=${safeNodeName(root)} | released=${releasedNodes.length} | remaining=${hasChildren(root) ? root.children.length : 0}`
      );
      if (releasedNodes.length > 0) {
        return preferPrimaryImportedNode(releasedNodes, payload);
      }
      return root.removed ? null : root;
    }

    const payloadWrapper = getPayloadDuplicateRootWrapper(payload);
    const wrapper = findDuplicateImportedWrapper(root);
    if (!wrapper || !hasChildren(wrapper) || !canFlattenImportedDuplicateRoot(root, wrapper, payloadWrapper)) {
      return root;
    }

    const rootBounds = getAbsoluteBounds(root, true);
    const preferredCanvasBounds = findPreferredSceneCanvasBounds(Array.from(wrapper.children), root);
    const deltaX = rootBounds && preferredCanvasBounds ? roundNumber(rootBounds.x - preferredCanvasBounds.x) : 0;
    const deltaY = rootBounds && preferredCanvasBounds ? roundNumber(rootBounds.y - preferredCanvasBounds.y) : 0;
    const wrapperIndex = Array.from(root.children).indexOf(wrapper);
    const children = Array.from(wrapper.children);

    for (let index = 0; index < children.length; index += 1) {
      reparentSceneNode(children[index], root, Math.max(0, wrapperIndex) + index, deltaX, deltaY);
    }

    wrapper.remove();
    return root;
  }

  function releaseSyntheticImportRoot(root) {
    if (!isSyntheticImportRoot(root) || !hasChildren(root)) {
      return [];
    }

    const parentNode = root.parent && hasChildren(root.parent) ? root.parent : null;
    if (!parentNode) {
      return [];
    }

    const children = Array.from(root.children);
    if (children.length === 0) {
      return [];
    }

    const rootIndex = Math.max(0, Array.from(parentNode.children).indexOf(root));
    for (let index = 0; index < children.length; index += 1) {
      reparentSceneNode(children[index], parentNode, rootIndex + index, 0, 0);
    }

    root.remove();
    return children.filter(node => node && !node.removed);
  }

  function preferPrimaryImportedNode(nodes, payload) {
    if (!Array.isArray(nodes) || nodes.length === 0) {
      return null;
    }

    const targetName = normalizeLayerName(payload && payload.rootName);
    if (targetName.length > 0) {
      const exactMatch = nodes.find(node => normalizeLayerName(node.name) === targetName);
      if (exactMatch) {
        return exactMatch;
      }
    }

    return nodes[0];
  }

  function debugImportPostprocess(label, detail) {
    if (!IMPORT_POSTPROCESS_DEBUG) {
      return;
    }

    try {
      figma.notify(`[pigma] ${label}: ${detail}`, { timeout: 2500 });
    } catch (error) {
      // Ignore notification failures in restricted contexts.
    }
  }

  function safeNodeName(node) {
    if (!node) {
      return "null";
    }

    if ("name" in node && typeof node.name === "string" && node.name.trim().length > 0) {
      return node.name;
    }

    return node.type || "node";
  }

  function selectImportedNodes(nodes) {
    if (!Array.isArray(nodes) || nodes.length === 0) {
      return;
    }

    const selectable = nodes.filter(node => node && !node.removed);
    if (selectable.length === 0) {
      return;
    }

    try {
      figma.currentPage.selection = selectable;
    } catch (error) {
      // Ignore selection assignment failures in restricted contexts.
    }
  }

  function getPayloadDuplicateRootWrapper(payload) {
    if (!payload || payload.mode === "flatten-image" || !Array.isArray(payload.nodes) || payload.nodes.length !== 1) {
      return null;
    }

    const node = payload.nodes[0];
    return shouldCollapseRootGroup(payload, node) ? node : null;
  }

  function canFlattenImportedDuplicateRoot(root, wrapper, payloadWrapper) {
    if (!hasChildren(root) || !hasChildren(wrapper) || !isNeutralSceneWrapper(wrapper)) {
      return false;
    }

    if (payloadWrapper && isSyntheticImportRoot(root)) {
      return true;
    }

    const rootBounds = getAbsoluteBounds(root, true);
    const wrapperBounds = getAbsoluteBounds(wrapper, true);
    if (!rootBounds || !wrapperBounds) {
      return false;
    }

    const sameSize =
      dimensionsMatch(wrapperBounds.width, rootBounds.width) &&
      dimensionsMatch(wrapperBounds.height, rootBounds.height);
    const sameOrigin =
      Math.abs(wrapperBounds.x - rootBounds.x) <= 2 &&
      Math.abs(wrapperBounds.y - rootBounds.y) <= 2;

    if (sameSize && sameOrigin) {
      return true;
    }

    const preferredCanvasBounds = findPreferredSceneCanvasBounds(Array.from(wrapper.children), root);
    return !!preferredCanvasBounds &&
      dimensionsMatch(preferredCanvasBounds.width, rootBounds.width) &&
      dimensionsMatch(preferredCanvasBounds.height, rootBounds.height);
  }

  function findDuplicateImportedWrapper(root) {
    if (!hasChildren(root)) {
      return null;
    }

    const rootName = normalizeLayerName(root.name);
    if (rootName.length === 0) {
      return null;
    }

    const candidates = Array.from(root.children).filter(node => {
      return hasChildren(node) && normalizeLayerName(node.name) === rootName;
    });

    if (candidates.length === 0) {
      return null;
    }

    candidates.sort((left, right) => {
      const leftArea = (Number(left.width) || 0) * (Number(left.height) || 0);
      const rightArea = (Number(right.width) || 0) * (Number(right.height) || 0);
      return rightArea - leftArea;
    });

    return candidates[0];
  }

  function separateSingleImportArtboardRoot(root, payload) {
    if (!shouldSeparateSingleImportArtboardRoot(root, payload)) {
      return [];
    }

    const parentNode = root && root.parent && hasChildren(root.parent) ? root.parent : null;
    if (!parentNode) {
      return [];
    }

    const children = Array.from(root.children);
    const rootIndex = Math.max(0, Array.from(parentNode.children).indexOf(root));
    for (let index = 0; index < children.length; index += 1) {
      reparentSceneNode(children[index], parentNode, rootIndex + index, 0, 0);
    }

    try {
      root.remove();
    } catch (error) {
      console.warn("[pigma-import-text-fix] failed to remove single PSD artboard wrapper", error);
    }

    return children.filter(node => node && !node.removed);
  }

  function materializeSingleImportArtboardFrames(root, payload, payloadNodes) {
    const targets = getSingleImportArtboardTargets(root);
    if (targets.length < 2) {
      return 0;
    }

    const specs = getPayloadArtboardSpecs(payloadNodes || getNormalizedPayloadNodes(payload));
    if (specs.length < 2) {
      return 0;
    }

    const specsByName = new Map();
    specs.forEach(spec => {
      if (!spec || !spec.normalizedName) {
        return;
      }

      if (!specsByName.has(spec.normalizedName)) {
        specsByName.set(spec.normalizedName, []);
      }
      specsByName.get(spec.normalizedName).push(spec);
    });

    const matchedTargets = targets
      .map(target => {
        const matches = specsByName.get(normalizeLayerName(target.node && target.node.name)) || [];
        const spec = matches.shift() || null;
        if (!spec) {
          return null;
        }

        return Object.assign({}, target, { spec });
      })
      .filter(Boolean);

    if (matchedTargets.length < 2 || matchedTargets.length !== targets.length) {
      return 0;
    }

    const origin = getPayloadArtboardOrigin(matchedTargets.map(target => target.spec));
    let materializedCount = 0;

    for (const target of matchedTargets) {
      if (!target.node || target.node.removed) {
        continue;
      }

      const frame = target.node.type === "FRAME"
        ? normalizeExistingImportArtboardFrame(target.node, target.spec, origin)
        : replaceImportArtboardGroupWithFrame(target.node, target.spec, origin, root);

      if (frame) {
        setExpandedRecursively(frame, true);
        materializedCount += 1;
      }
    }

    if (materializedCount > 0) {
      debugImportPostprocess("artboard-frames", `materialized=${materializedCount}`);
    }

    return materializedCount;
  }

  function normalizeExistingImportArtboardFrame(frame, spec, origin) {
    if (!frame || frame.removed || frame.type !== "FRAME") {
      return null;
    }

    resizeImportArtboardFrame(frame, spec);
    frame.x = roundNumber((Number(spec.x) || 0) - origin.x);
    frame.y = roundNumber((Number(spec.y) || 0) - origin.y);
    return frame;
  }

  function replaceImportArtboardGroupWithFrame(group, spec, origin, root) {
    if (!group || group.removed) {
      return null;
    }

    const frame = createImportArtboardFrame(group.name, spec, origin);
    const parentNode = group.parent && hasChildren(group.parent) ? group.parent : root;
    const insertionIndex = Math.max(0, Array.from(parentNode.children).indexOf(group));
    parentNode.insertChild(insertionIndex, frame);
    frame.x = roundNumber((Number(spec.x) || 0) - origin.x);
    frame.y = roundNumber((Number(spec.y) || 0) - origin.y);

    if (hasChildren(group)) {
      const children = Array.from(group.children);
      for (const child of children) {
        reparentSceneNode(child, frame, frame.children.length, 0, 0);
      }
    }

    try {
      group.remove();
    } catch (error) {
      console.warn("[pigma-import-text-fix] failed to remove PSD artboard group", error);
    }

    return frame;
  }

  function createImportArtboardFrame(name, spec, origin) {
    const frame = figma.createFrame();
    frame.name = typeof name === "string" && name.trim().length > 0 ? name.trim() : "PSD Artboard";
    resizeImportArtboardFrame(frame, spec);
    frame.x = roundNumber((Number(spec.x) || 0) - origin.x);
    frame.y = roundNumber((Number(spec.y) || 0) - origin.y);
    return frame;
  }

  function resizeImportArtboardFrame(frame, spec) {
    const width = Math.max(1, roundNumber(spec && spec.width));
    const height = Math.max(1, roundNumber(spec && spec.height));
    frame.resize(width, height);
    frame.clipsContent = true;
    frame.fills = [];
    frame.strokes = [];
  }

  function getSingleImportArtboardTargets(root) {
    if (!hasChildren(root)) {
      return [];
    }

    return Array.from(root.children)
      .map(node => {
        const size = parseLayerNameSize(node && node.name);
        return size ? { node, size } : null;
      })
      .filter(Boolean);
  }

  function getPayloadArtboardSpecs(nodes) {
    if (!Array.isArray(nodes)) {
      return [];
    }

    return nodes
      .map(node => {
        if (!node || node.kind !== "group") {
          return null;
        }

        const sizeFromName = parseLayerNameSize(node.name);
        if (!sizeFromName) {
          return null;
        }

        const width = dimensionsMatch(node.width, sizeFromName.width) ? Number(node.width) : sizeFromName.width;
        const height = dimensionsMatch(node.height, sizeFromName.height) ? Number(node.height) : sizeFromName.height;
        return {
          name: node.name,
          normalizedName: normalizeLayerName(node.name),
          x: Number.isFinite(node.x) ? Number(node.x) : 0,
          y: Number.isFinite(node.y) ? Number(node.y) : 0,
          width,
          height
        };
      })
      .filter(Boolean);
  }

  function getPayloadArtboardOrigin(specs) {
    if (!Array.isArray(specs) || specs.length === 0) {
      return { x: 0, y: 0 };
    }

    let minX = Number.POSITIVE_INFINITY;
    let minY = Number.POSITIVE_INFINITY;
    specs.forEach(spec => {
      if (!spec) {
        return;
      }

      minX = Math.min(minX, Number.isFinite(spec.x) ? spec.x : 0);
      minY = Math.min(minY, Number.isFinite(spec.y) ? spec.y : 0);
    });

    return {
      x: Number.isFinite(minX) ? minX : 0,
      y: Number.isFinite(minY) ? minY : 0
    };
  }

  function shouldSeparateSingleImportArtboardRoot(root, payload) {
    if (!hasChildren(root) || !payload || payload.mode === "flatten-image") {
      return false;
    }

    if (!isNeutralSceneWrapper(root, { allowClipsContent: true, allowFills: true })) {
      return false;
    }

    const payloadName = normalizeLayerName(payload.rootName);
    const rootName = normalizeLayerName(root.name);
    if (payloadName.length > 0 && rootName.length > 0 && payloadName !== rootName) {
      return false;
    }

    const children = Array.from(root.children).filter(node => node && !node.removed);
    if (children.length < 2) {
      return false;
    }

    const artboardLikeChildren = children.filter(isSceneArtboardLikeChild);
    if (artboardLikeChildren.length < 2 || artboardLikeChildren.length !== children.length) {
      return false;
    }

    return true;
  }

  function isSceneArtboardLikeChild(node) {
    if (!node || node.visible === false || !isPositiveDimension(node.width) || !isPositiveDimension(node.height)) {
      return false;
    }

    const sizeFromName = parseLayerNameSize(node.name);
    if (!sizeFromName) {
      return false;
    }

    return dimensionsMatch(node.width, sizeFromName.width) && dimensionsMatch(node.height, sizeFromName.height);
  }

  function parseLayerNameSize(value) {
    const source = typeof value === "string" ? value : "";
    const match = /(?:^|[^0-9])(\d{2,5})\s*[x×]\s*(\d{2,5})(?:[^0-9]|$)/i.exec(source);
    if (!match) {
      return null;
    }

    const width = Number(match[1]);
    const height = Number(match[2]);
    if (!isPositiveDimension(width) || !isPositiveDimension(height)) {
      return null;
    }

    return {
      width,
      height
    };
  }

  function isNeutralSceneWrapper(node, options) {
    const allowClipsContent = !!(options && options.allowClipsContent);
    const allowFills = !!(options && options.allowFills);
    if (!node || node.visible === false || (!allowClipsContent && node.clipsContent === true)) {
      return false;
    }

    if ("opacity" in node && !isNeutralOpacity(node.opacity)) {
      return false;
    }

    if ("blendMode" in node && !isNeutralBlendMode(normalizeSceneBlendMode(node.blendMode))) {
      return false;
    }

    if ("effects" in node && Array.isArray(node.effects) && node.effects.some(isVisibleSceneStyle)) {
      return false;
    }

    if (!allowFills && "fills" in node && Array.isArray(node.fills) && node.fills.some(isVisibleSceneStyle)) {
      return false;
    }

    if ("strokes" in node && Array.isArray(node.strokes) && node.strokes.some(isVisibleSceneStyle)) {
      return false;
    }

    return true;
  }

  function normalizeSceneBlendMode(value) {
    return typeof value === "string" ? value.trim().toLowerCase().replace(/_/g, " ") : "";
  }

  function isVisibleSceneStyle(style) {
    if (!style || style.visible === false) {
      return false;
    }

    if ("opacity" in style && Number.isFinite(style.opacity) && Number(style.opacity) <= 0.001) {
      return false;
    }

    return true;
  }

  function markSyntheticImportRoot(node) {
    if (!node || typeof node.setPluginData !== "function") {
      return;
    }

    try {
      node.setPluginData(IMPORT_SYNTHETIC_ROOT_KEY, "1");
    } catch (error) {
      // Ignore plugin data failures in restricted contexts.
    }
  }

  function isSyntheticImportRoot(node) {
    if (!node || typeof node.getPluginData !== "function") {
      return false;
    }

    try {
      return node.getPluginData(IMPORT_SYNTHETIC_ROOT_KEY) === "1";
    } catch (error) {
      return false;
    }
  }

  function reparentSceneNode(node, parentNode, index, deltaX, deltaY) {
    if (!node || !parentNode || !hasChildren(parentNode)) {
      return;
    }

    const beforeBounds = getAbsoluteBounds(node, true);
    parentNode.insertChild(index, node);

    const afterBounds = getAbsoluteBounds(node, true);
    const baseX = beforeBounds ? beforeBounds.x : afterBounds ? afterBounds.x : 0;
    const baseY = beforeBounds ? beforeBounds.y : afterBounds ? afterBounds.y : 0;
    const desiredX = roundNumber(baseX + deltaX);
    const desiredY = roundNumber(baseY + deltaY);

    if (!afterBounds) {
      return;
    }

    translateSceneNode(node, desiredX - afterBounds.x, desiredY - afterBounds.y);
  }

  function translateSceneNode(node, deltaX, deltaY) {
    if (!node || (deltaX === 0 && deltaY === 0)) {
      return;
    }

    if (Array.isArray(node.relativeTransform) && node.relativeTransform.length === 2) {
      const transform = node.relativeTransform;
      node.relativeTransform = [
        [transform[0][0], transform[0][1], roundNumber(transform[0][2] + deltaX)],
        [transform[1][0], transform[1][1], roundNumber(transform[1][2] + deltaY)]
      ];
      return;
    }

    if ("x" in node && "y" in node) {
      node.x = roundNumber((Number(node.x) || 0) + deltaX);
      node.y = roundNumber((Number(node.y) || 0) + deltaY);
    }
  }

  function findPreferredSceneCanvasBounds(nodes, root) {
    return collectSceneVisibleBounds(nodes);
  }

  function collectSceneVisibleBounds(nodes) {
    if (!Array.isArray(nodes)) {
      return null;
    }

    let bounds = null;
    for (const node of nodes) {
      if (!node || node.visible === false) {
        continue;
      }

      if (hasChildren(node)) {
        bounds = mergeRectBounds(bounds, collectSceneVisibleBounds(Array.from(node.children)));
      }

      const nodeBounds = getAbsoluteBounds(node, true);
      if (!nodeBounds || !isPositiveDimension(nodeBounds.width) || !isPositiveDimension(nodeBounds.height)) {
        continue;
      }

      bounds = mergeRectBounds(bounds, {
        x: roundNumber(nodeBounds.x),
        y: roundNumber(nodeBounds.y),
        width: Math.max(1, Math.round(Number(nodeBounds.width))),
        height: Math.max(1, Math.round(Number(nodeBounds.height)))
      });
    }

    return bounds;
  }

  function mergeRectBounds(currentBounds, nextBounds) {
    if (!nextBounds) {
      return currentBounds;
    }

    if (!currentBounds) {
      return {
        x: roundNumber(nextBounds.x),
        y: roundNumber(nextBounds.y),
        width: Math.max(1, Math.round(Number(nextBounds.width))),
        height: Math.max(1, Math.round(Number(nextBounds.height)))
      };
    }

    const left = Math.min(currentBounds.x, roundNumber(nextBounds.x));
    const top = Math.min(currentBounds.y, roundNumber(nextBounds.y));
    const right = Math.max(currentBounds.x + currentBounds.width, roundNumber(nextBounds.x) + Math.max(1, Math.round(Number(nextBounds.width))));
    const bottom = Math.max(currentBounds.y + currentBounds.height, roundNumber(nextBounds.y) + Math.max(1, Math.round(Number(nextBounds.height))));

    return {
      x: left,
      y: top,
      width: Math.max(1, right - left),
      height: Math.max(1, bottom - top)
    };
  }

  // PIGMA_TEXT_IMPORT_GUARD::NO_BROAD_TEXT_UPDATES
  function shouldApplyScopedTextFixes(payloadNodes, parentNode) {
    if (!Array.isArray(payloadNodes) || !hasChildren(parentNode)) {
      return false;
    }

    return countImportedTextNodes(payloadNodes) > 0;
  }

  function countImportedTextNodes(nodes) {
    if (!Array.isArray(nodes) || nodes.length === 0) {
      return 0;
    }

    let count = 0;
    for (const node of nodes) {
      if (!isSceneNodeImported(node)) {
        continue;
      }

      if (node.kind === "text") {
        count += 1;
        continue;
      }

      if (node.kind === "group" && Array.isArray(node.children)) {
        count += countImportedTextNodes(node.children);
      }
    }

    return count;
  }

  // PIGMA_TEXT_IMPORT_GUARD::TEXT_ALIGNMENT_SCOPE
  function applyTextFixes(payloadNodes, parentNode) {
    if (!Array.isArray(payloadNodes) || !hasChildren(parentNode)) {
      return;
    }

    const importedTextNodesBySourceId = collectImportedTextNodesBySourceId(parentNode);
    const orderedFallbackMatches =
      importedTextNodesBySourceId.size === 0 ? createOrderedTextFallbackMatches(payloadNodes, parentNode) : new Map();

    if (importedTextNodesBySourceId.size === 0 && orderedFallbackMatches.size === 0) {
      console.warn("[pigma-import-text-fix] skipped text alignment because imported source IDs were not tagged");
      return;
    }

    if (importedTextNodesBySourceId.size === 0 && orderedFallbackMatches.size > 0) {
      console.warn("[pigma-import-text-fix] aligned text using ordered fallback because imported source IDs were not tagged");
    }

    applyTextFixesBySourceId(payloadNodes, importedTextNodesBySourceId, orderedFallbackMatches);
  }

  function createOrderedTextFallbackMatches(payloadNodes, parentNode) {
    const payloadTextNodes = collectPayloadTextNodes(payloadNodes);
    const importedTextNodes = collectImportedTextNodes(parentNode);

    if (payloadTextNodes.length === 0 || importedTextNodes.length === 0) {
      return new Map();
    }

    if (payloadTextNodes.length !== importedTextNodes.length) {
      console.warn(
        `[pigma-import-text-fix] skipped ordered text fallback because payload text count (${payloadTextNodes.length}) did not match imported text count (${importedTextNodes.length})`
      );
      return new Map();
    }

    const orderedFallbackMatches = new Map();
    for (let index = 0; index < payloadTextNodes.length; index += 1) {
      orderedFallbackMatches.set(payloadTextNodes[index], importedTextNodes[index]);
    }

    return orderedFallbackMatches;
  }

  function collectPayloadTextNodes(nodes, collected = []) {
    if (!Array.isArray(nodes)) {
      return collected;
    }

    for (const node of nodes) {
      if (!isSceneNodeImported(node)) {
        continue;
      }

      if (node.kind === "text") {
        collected.push(node);
        continue;
      }

      if (node.kind === "group" && Array.isArray(node.children)) {
        collectPayloadTextNodes(node.children, collected);
      }
    }

    return collected;
  }

  function collectImportedTextNodes(parentNode) {
    const importedTextNodes = [];
    collectImportedTextNodesInto(parentNode, importedTextNodes);
    return importedTextNodes;
  }

  function collectImportedTextNodesInto(node, importedTextNodes) {
    if (!node) {
      return;
    }

    if (node.type === "TEXT") {
      importedTextNodes.push(node);
    }

    if (!hasChildren(node)) {
      return;
    }

    for (const child of node.children) {
      collectImportedTextNodesInto(child, importedTextNodes);
    }
  }

  function applyTextFixesBySourceId(payloadNodes, importedTextNodesBySourceId, orderedFallbackMatches) {
    if (!Array.isArray(payloadNodes)) {
      return;
    }

    for (const payloadNode of payloadNodes) {
      if (!isSceneNodeImported(payloadNode)) {
        continue;
      }

      if (payloadNode.kind === "text") {
        const sceneNode = findImportedTextNodeForPayload(payloadNode, importedTextNodesBySourceId, orderedFallbackMatches);
        const sceneParent = sceneNode && hasChildren(sceneNode.parent) ? sceneNode.parent : null;
        if (sceneNode && sceneParent) {
          alignImportedTextNode(sceneNode, payloadNode, sceneParent);
        }
        continue;
      }

      if (payloadNode.kind === "group" && Array.isArray(payloadNode.children)) {
        applyTextFixesBySourceId(payloadNode.children, importedTextNodesBySourceId, orderedFallbackMatches);
      }
    }
  }

  function collectImportedTextNodesBySourceId(parentNode) {
    const importedTextNodesBySourceId = new Map();
    collectImportedTextNodesBySourceIdInto(parentNode, importedTextNodesBySourceId);
    return importedTextNodesBySourceId;
  }

  function collectImportedTextNodesBySourceIdInto(node, importedTextNodesBySourceId) {
    if (!node) {
      return;
    }

    if (node.type === "TEXT") {
      const sourceId = getImportedSourceId(node);
      if (sourceId) {
        importedTextNodesBySourceId.set(sourceId, node);
      }
    }

    if (!hasChildren(node)) {
      return;
    }

    for (const child of node.children) {
      collectImportedTextNodesBySourceIdInto(child, importedTextNodesBySourceId);
    }
  }

  function findImportedTextNodeForPayload(payloadNode, importedTextNodesBySourceId, orderedFallbackMatches) {
    if (!payloadNode || payloadNode.kind !== "text" || !(importedTextNodesBySourceId instanceof Map)) {
      return null;
    }

    const sourceId = getPayloadSourceId(payloadNode);
    if (sourceId) {
      const matchedBySourceId = importedTextNodesBySourceId.get(sourceId);
      if (matchedBySourceId) {
        return matchedBySourceId;
      }
    }

    if (!(orderedFallbackMatches instanceof Map)) {
      return null;
    }

    return orderedFallbackMatches.get(payloadNode) || null;
  }

  function getPayloadSourceId(payloadNode) {
    if (!payloadNode || payloadNode.id == null) {
      return "";
    }

    return String(payloadNode.id);
  }

  function getImportedSourceId(node) {
    if (!node || typeof node.getPluginData !== "function") {
      return "";
    }

    try {
      return String(node.getPluginData(IMPORT_SOURCE_ID_KEY) || "");
    } catch (error) {
      return "";
    }
  }

  function alignImportedTextNode(node, payloadNode, parentNode) {
    const parentBounds = getAbsoluteBounds(parentNode, true);
    const nodeBounds = getAbsoluteBounds(node, true);
    const desiredPosition = getDesiredTextPosition(payloadNode);

    if (!parentBounds || !nodeBounds || !desiredPosition) {
      return;
    }

    const currentX = nodeBounds.x - parentBounds.x;
    const currentY = nodeBounds.y - parentBounds.y;
    const deltaX = roundNumber(desiredPosition.x - currentX);
    const deltaY = roundNumber(desiredPosition.y - currentY);

    if (deltaX === 0 && deltaY === 0) {
      return;
    }

    const transform = node.relativeTransform;
    node.relativeTransform = [
      [transform[0][0], transform[0][1], roundNumber(transform[0][2] + deltaX)],
      [transform[1][0], transform[1][1], roundNumber(transform[1][2] + deltaY)]
    ];
  }

  function getDesiredTextPosition(payloadNode) {
    if (!payloadNode || payloadNode.kind !== "text") {
      return null;
    }

    return (
      getLayerTopLeft(payloadNode) ||
      getTransformTopLeft(payloadNode.text && payloadNode.text.transform) ||
      getRectTopLeft(payloadNode.text && payloadNode.text.boundingBox) ||
      getRectTopLeft(payloadNode.text && payloadNode.text.bounds)
    );
  }

  function getLayerTopLeft(payloadNode) {
    if (!Number.isFinite(payloadNode == null ? void 0 : payloadNode.x) || !Number.isFinite(payloadNode == null ? void 0 : payloadNode.y)) {
      return null;
    }

    return {
      x: roundNumber(payloadNode.x),
      y: roundNumber(payloadNode.y)
    };
  }

  function getRectTopLeft(rect) {
    if (!rect || !Number.isFinite(rect.left) || !Number.isFinite(rect.top)) {
      return null;
    }

    return {
      x: roundNumber(rect.left),
      y: roundNumber(rect.top)
    };
  }

  function getTransformTopLeft(transform) {
    if (!Array.isArray(transform) || transform.length < 6) {
      return null;
    }

    if (!Number.isFinite(transform[4]) || !Number.isFinite(transform[5])) {
      return null;
    }

    return {
      x: roundNumber(transform[4]),
      y: roundNumber(transform[5])
    };
  }

  function getAbsoluteBounds(node, preferBoundingBox) {
    if (!node) {
      return null;
    }

    const primary = preferBoundingBox ? node.absoluteBoundingBox : node.absoluteRenderBounds;
    const fallback = preferBoundingBox ? node.absoluteRenderBounds : node.absoluteBoundingBox;
    const bounds = primary || fallback;

    if (!bounds || !Number.isFinite(bounds.x) || !Number.isFinite(bounds.y)) {
      return null;
    }

    return bounds;
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children);
  }

  function setExpandedRecursively(node, expanded) {
    if (!node) {
      return;
    }

    setSceneNodeExpanded(node, expanded);

    if (!hasChildren(node)) {
      return;
    }

    for (const child of node.children) {
      setExpandedRecursively(child, expanded);
    }
  }

  function setSceneNodeExpanded(node, expanded) {
    if (!node || !("expanded" in node)) {
      return;
    }

    try {
      node.expanded = !!expanded;
    } catch (error) {
      // Ignore nodes that expose the property as readonly in the current editor/runtime.
    }
  }

  function isSceneNodeImported(node) {
    if (!node) {
      return false;
    }

    if (node.kind === "group") {
      return node.width > 0 && node.height > 0;
    }

    if (node.kind === "text") {
      return (node.width > 0 && node.height > 0) || getByteLength(node.pngBytes) > 0;
    }

    if (node.kind === "shape") {
      return true;
    }

    return node.width > 0 && node.height > 0 && getByteLength(node.pngBytes) > 0;
  }

  function isBatchItemImportable(item) {
    return !!item && item.documentWidth > 0 && item.documentHeight > 0 && (item.mode === "flatten-image" || (Array.isArray(item.nodes) && item.nodes.length > 0) || getByteLength(item.compositePngBytes) > 0);
  }

  function getByteLength(value) {
    return value && typeof value.length === "number" ? value.length : 0;
  }

  function roundNumber(value) {
    return Number.isFinite(value) ? Math.round(value) : 0;
  }
})();

;(()=>{
  // PIGMA_EXPORT_BOUNDARY::SOURCE_OF_TRUTH
  // Keep export-only message guards in this file so PSD export changes can
  // evolve without touching PSD import post-processing.
  // PIGMA_EXPORT_BOUNDARY::MESSAGE_TYPES
  // PIGMA_EXPORT_BOUNDARY::NORMALIZE_SETTINGS
  // PIGMA_EXPORT_BOUNDARY::LONG_EDITABLE_SEGMENTS
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  const originalOnMessage = figma.ui.onmessage;
  const originalPostMessage = figma.ui.postMessage.bind(figma.ui);
  const DEFAULT_EXPORT_SETTINGS = Object.freeze({
    psdVersion: "max-compatibility",
    textExportMode: "editable-text",
    imageExportMode: "bitmap-only",
    hiddenLayerMode: "ignore-hidden",
    exportPackageMode: "psd-only",
    fileNamePattern: "{frame-name}.psd"
  });
  const LONG_EDITABLE_SEGMENT_HEIGHT = 4500;
  const LONG_EDITABLE_SEGMENT_AREA = 10000000;
  const LONG_EDITABLE_SEGMENT_MIN_HEIGHT = 3500;
  const LONG_EDITABLE_COMPLEX_NODE_COUNT = 80;
  const LONG_EDITABLE_COMPLEX_TEXT_COUNT = 25;
  const LONG_EDITABLE_COMPLEX_IMAGE_COUNT = 4;
  const LONG_EDITABLE_COMPLEX_EFFECT_COUNT = 8;
  const LONG_EDITABLE_ANALYSIS_LIMIT = 2500;
  const LONG_EDITABLE_WHITESPACE_CAPTURE_MIN = 8;
  const LONG_EDITABLE_WHITESPACE_CAPTURE_MAX = 160;
  const LONG_EDITABLE_WHITESPACE_EDGE_TOLERANCE = 4;
  const PIGMA_EXPORT_TEMP_NODE_NAMES = Object.freeze([
    "__pigma-mask-preview__",
    "__pigma-background-blur-crop__",
    "__pigma-vector-preview__",
    "__pigma-text-preview-padding__",
    "__pigma-text-visual-probe__",
    "__pigma-long-frame-tile__"
  ]);
  const PIGMA_EXPORT_TEMP_NODE_NAME_SET = new Set(PIGMA_EXPORT_TEMP_NODE_NAMES);
  let activeEditableSegmentSession = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  try {
    figma.ui.postMessage = message => {
      const result = originalPostMessage(message);
      if (message && (message.type === "export-finished" || message.type === "export-error")) {
        cleanupExportMemory("after-export");
      }
      return result;
    };
  } catch (error) {
  }

  figma.ui.onmessage = async message => {
    if (message && message.type === "psd-export-long-editable-cancelled") {
      figma.ui.postMessage({ type: "export-error", message: "PSD 만들기를 취소했습니다." });
      return;
    }

    if (!isExportMessage(message)) {
      return originalOnMessage(message);
    }

    const normalizedMessage = normalizeExportMessage(message);

    if (normalizedMessage.type === "request-export") {
      cleanupExportMemory("before-export");

      const segmentPlan = getLongEditableSegmentPlan(normalizedMessage);
      if (segmentPlan && normalizedMessage.longEditableSegmentConsent !== true) {
        figma.ui.postMessage({
          type: "psd-export-long-editable-confirm",
          request: normalizedMessage,
          plan: serializeLongEditableSegmentPlan(segmentPlan)
        });
        figma.notify("긴 PSD 내보내기 확인이 필요합니다. 플러그인 창의 안내를 확인해주세요.");
        return;
      }

      if (segmentPlan && normalizedMessage.longEditableSegmentConsent === true) {
        return runLongEditableSegmentExport(normalizedMessage, segmentPlan);
      }
    }

    return originalOnMessage(normalizedMessage);
  };

  function isExportMessage(message) {
    return !!message && (message.type === "request-export" || message.type === "request-next-export-root");
  }

  function normalizeExportMessage(message) {
    if (!message || typeof message !== "object") {
      return message;
    }

    if (message.type === "request-next-export-root") {
      return { type: "request-next-export-root" };
    }

    return Object.assign({}, message, {
      type: "request-export",
      hiddenLayerMode: normalizeHiddenLayerMode(message.hiddenLayerMode),
      includeCompositePng: message.includeCompositePng === true,
      settings: normalizeExportSettings(message.settings),
      developerExportExperiments: normalizeExportExperiments(message.developerExportExperiments)
    });
  }

  function normalizeExportSettings(settings) {
    const source = settings && typeof settings === "object" ? settings : {};

    return {
      psdVersion: DEFAULT_EXPORT_SETTINGS.psdVersion,
      textExportMode: normalizeTextExportMode(source.textExportMode),
      imageExportMode: normalizeImageExportMode(source.imageExportMode),
      hiddenLayerMode: normalizeHiddenLayerMode(source.hiddenLayerMode),
      exportPackageMode: normalizeExportPackageMode(source.exportPackageMode),
      fileNamePattern: normalizeFileNamePattern(source.fileNamePattern)
    };
  }

  function normalizeTextExportMode(value) {
    return value === "rasterize-text" ? value : DEFAULT_EXPORT_SETTINGS.textExportMode;
  }

  function normalizeImageExportMode(value) {
    return value === "smart-object-if-possible" ? value : DEFAULT_EXPORT_SETTINGS.imageExportMode;
  }

  function normalizeHiddenLayerMode(value) {
    return value === "preserve-hidden" ? value : DEFAULT_EXPORT_SETTINGS.hiddenLayerMode;
  }

  function normalizeExportPackageMode(value) {
    if (value === "bundle-with-rasters" || value === "psd-with-png") {
      return "psd-with-png";
    }

    if (value === "psd-with-jpg") {
      return value;
    }

    return DEFAULT_EXPORT_SETTINGS.exportPackageMode;
  }

  function normalizeFileNamePattern(value) {
    return typeof value === "string" && value.trim().length > 0 ? value.trim() : DEFAULT_EXPORT_SETTINGS.fileNamePattern;
  }

  function normalizeExportExperiments(value) {
    const source = value && typeof value === "object" ? value : {};

    return {
      disableShapePreviewCanvas: source.disableShapePreviewCanvas === true,
      forceBitmapVectorPreview: source.forceBitmapVectorPreview === true,
      disableEditableTextPreview: source.disableEditableTextPreview === true,
      disableLayerBlur: source.disableLayerBlur === true,
      disableProgressiveLayerBlur: source.disableProgressiveLayerBlur === true,
      disableBackgroundBlur: source.disableBackgroundBlur === true,
      disableNoise: source.disableNoise === true,
      disableTexture: source.disableTexture === true
    };
  }

  function getLongEditableSegmentPlan(message) {
    if (message.longEditableSegmentActive === true) {
      return null;
    }

    const selection = figma.currentPage.selection;
    if (!selection || selection.length !== 1) {
      return null;
    }

    const root = selection[0];
    if (!root || !("exportAsync" in root)) {
      return null;
    }

    const bounds = getNodeBounds(root);
    if (!bounds || bounds.width <= 0 || bounds.height <= LONG_EDITABLE_SEGMENT_HEIGHT) {
      return null;
    }

    const complexity = analyzeLongEditableComplexity(root);
    if (!isLongEditableRisk(bounds, complexity)) {
      return null;
    }

    const segmentCount = calculateLongEditableSegmentCount(bounds);
    if (segmentCount < 2) {
      return null;
    }

    return {
      root: root,
      rootName: getNodeName(root),
      bounds: bounds,
      complexity: complexity,
      segmentCount: segmentCount,
      segments: buildLongEditableSegments(root, bounds, segmentCount)
    };
  }

  function isLongEditableRisk(bounds, complexity) {
    if (!bounds || !complexity) {
      return false;
    }

    if (complexity.truncated) {
      return true;
    }

    if (complexity.nodeCount >= LONG_EDITABLE_COMPLEX_NODE_COUNT) {
      return true;
    }

    if (complexity.textCount >= LONG_EDITABLE_COMPLEX_TEXT_COUNT) {
      return true;
    }

    if (complexity.imagePaintCount >= LONG_EDITABLE_COMPLEX_IMAGE_COUNT) {
      return true;
    }

    if (complexity.effectCount >= LONG_EDITABLE_COMPLEX_EFFECT_COUNT) {
      return true;
    }

    return bounds.width * bounds.height >= LONG_EDITABLE_SEGMENT_AREA * 3 && complexity.nodeCount > 10;
  }

  function calculateLongEditableSegmentCount(bounds) {
    const width = Math.max(1, Math.round(bounds.width));
    const height = Math.max(1, Math.round(bounds.height));
    const byHeight = Math.ceil(height / LONG_EDITABLE_SEGMENT_HEIGHT);
    const byArea = Math.ceil(width * height / LONG_EDITABLE_SEGMENT_AREA);
    let count = Math.max(2, byHeight, byArea);
    while (count > 2 && height / (count - 1) <= LONG_EDITABLE_SEGMENT_HEIGHT) {
      if (height / count >= LONG_EDITABLE_SEGMENT_MIN_HEIGHT) {
        break;
      }
      count -= 1;
    }
    return count;
  }

  function buildLongEditableSegments(root, bounds, count) {
    const segments = [];
    const height = Math.max(1, Math.round(bounds.height));
    for (let index = 0; index < count; index += 1) {
      const start = Math.round(index * height / count);
      const end = Math.round((index + 1) * height / count);
      segments.push({
        index: index,
        offsetY: start,
        height: Math.max(1, end - start),
        rect: {
          x: bounds.x,
          y: bounds.y + start,
          width: bounds.width,
          height: Math.max(1, end - start)
        }
      });
    }
    return includeLongEditableAdjacentWhitespaceAroundSegments(segments, root, bounds);
  }

  function includeLongEditableAdjacentWhitespaceAroundSegments(segments, root, bounds) {
    const intervals = getLongEditableContentIntervals(root);
    if (!intervals.length) {
      return segments;
    }

    const maxAllowedHeight = LONG_EDITABLE_SEGMENT_HEIGHT + LONG_EDITABLE_WHITESPACE_CAPTURE_MAX;
    const adjusted = segments.map(segment => includeLongEditableWhitespaceAroundSegment(segment, intervals, bounds, maxAllowedHeight));
    return removeLongEditableSegmentOverlap(adjusted);
  }

  function includeLongEditableWhitespaceAroundSegment(segment, intervals, rootBounds, maxAllowedHeight) {
    const contentRange = getLongEditableSegmentContentRange(segment, intervals);
    if (!contentRange) {
      return segment;
    }

    let nextY = segment.rect.y;
    let nextEnd = segment.rect.y + segment.rect.height;

    if (Math.abs(contentRange.start - segment.rect.y) <= LONG_EDITABLE_WHITESPACE_EDGE_TOLERANCE) {
      const previousEnd = findPreviousLongEditableContentEnd(intervals, contentRange.start);
      if (previousEnd !== null) {
        const gap = contentRange.start - previousEnd;
        if (gap >= LONG_EDITABLE_WHITESPACE_CAPTURE_MIN && gap <= LONG_EDITABLE_WHITESPACE_CAPTURE_MAX) {
          nextY = previousEnd;
        }
      } else {
        const gap = contentRange.start - rootBounds.y;
        if (gap >= LONG_EDITABLE_WHITESPACE_CAPTURE_MIN && gap <= LONG_EDITABLE_WHITESPACE_CAPTURE_MAX) {
          nextY = rootBounds.y;
        }
      }
    }

    if (Math.abs(contentRange.end - (segment.rect.y + segment.rect.height)) <= LONG_EDITABLE_WHITESPACE_EDGE_TOLERANCE) {
      const nextStart = findNextLongEditableContentStart(intervals, contentRange.end);
      if (nextStart !== null) {
        const gap = nextStart - contentRange.end;
        if (gap >= LONG_EDITABLE_WHITESPACE_CAPTURE_MIN && gap <= LONG_EDITABLE_WHITESPACE_CAPTURE_MAX) {
          nextEnd = nextStart;
        }
      } else {
        const rootEnd = rootBounds.y + rootBounds.height;
        const gap = rootEnd - contentRange.end;
        if (gap >= LONG_EDITABLE_WHITESPACE_CAPTURE_MIN && gap <= LONG_EDITABLE_WHITESPACE_CAPTURE_MAX) {
          nextEnd = rootEnd;
        }
      }
    }

    if (nextEnd - nextY > maxAllowedHeight) {
      return segment;
    }

    return buildLongEditableSegmentFromAbsoluteRange(segment.index, rootBounds, nextY, nextEnd);
  }

  function buildLongEditableSegmentFromAbsoluteRange(index, rootBounds, y, end) {
    const offsetY = Math.max(0, Math.round(y - rootBounds.y));
    const height = Math.max(1, Math.round(end - y));
    return {
      index: index,
      offsetY: offsetY,
      height: height,
      rect: {
        x: rootBounds.x,
        y: rootBounds.y + offsetY,
        width: rootBounds.width,
        height: height
      }
    };
  }

  function getLongEditableSegmentContentRange(segment, intervals) {
    const segmentStart = segment.rect.y;
    const segmentEnd = segment.rect.y + segment.rect.height;
    let start = Infinity;
    let end = -Infinity;
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.end <= segmentStart + 0.5 || interval.start >= segmentEnd - 0.5) {
        continue;
      }
      start = Math.min(start, interval.start);
      end = Math.max(end, interval.end);
    }
    if (!Number.isFinite(start) || !Number.isFinite(end) || end <= start) {
      return null;
    }
    return { start: start, end: end };
  }

  function getLongEditableContentIntervals(root) {
    const intervals = [];
    const stack = [];
    if (root && "children" in root && Array.isArray(root.children)) {
      for (let index = root.children.length - 1; index >= 0; index -= 1) {
        stack.push(root.children[index]);
      }
    }

    while (stack.length > 0) {
      const node = stack.pop();
      if (!node || node.removed || node.visible === false) {
        continue;
      }

      const bounds = getNodeBounds(node);
      if (bounds && isLongEditableWhitespaceContentNode(node, bounds)) {
        intervals.push({
          start: bounds.y,
          end: bounds.y + bounds.height
        });
      }

      if ("children" in node && Array.isArray(node.children)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    intervals.sort((a, b) => a.start - b.start || a.end - b.end);
    return mergeLongEditableContentIntervals(intervals);
  }

  function isLongEditableWhitespaceContentNode(node, bounds) {
    if (!bounds || bounds.height <= 0.5) {
      return false;
    }
    if ("children" in node && Array.isArray(node.children) && isStructuralSegmentContainer(node)) {
      return false;
    }
    return bounds.height <= LONG_EDITABLE_SEGMENT_HEIGHT + LONG_EDITABLE_WHITESPACE_CAPTURE_MAX;
  }

  function mergeLongEditableContentIntervals(intervals) {
    const merged = [];
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      const previous = merged.length > 0 ? merged[merged.length - 1] : null;
      if (previous && interval.start <= previous.end + 0.5) {
        previous.end = Math.max(previous.end, interval.end);
      } else {
        merged.push({
          start: interval.start,
          end: interval.end
        });
      }
    }
    return merged;
  }

  function findPreviousLongEditableContentEnd(intervals, contentStart) {
    let previousEnd = null;
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.end >= contentStart - 0.5) {
        break;
      }
      previousEnd = interval.end;
    }
    return previousEnd;
  }

  function findNextLongEditableContentStart(intervals, contentEnd) {
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.start > contentEnd + 0.5) {
        return interval.start;
      }
    }
    return null;
  }

  function removeLongEditableSegmentOverlap(segments) {
    const resolved = segments.map(segment => buildLongEditableSegmentFromAbsoluteRange(
      segment.index,
      {
        x: segment.rect.x,
        y: segment.rect.y - segment.offsetY,
        width: segment.rect.width,
        height: 0
      },
      segment.rect.y,
      segment.rect.y + segment.rect.height
    ));

    for (let index = 1; index < resolved.length; index += 1) {
      const previous = resolved[index - 1];
      const current = resolved[index];
      if (!previous || !current || previous.rect.y + previous.rect.height <= current.rect.y + 0.5) {
        continue;
      }

      const boundary = current.rect.y;
      previous.height = Math.max(1, Math.round(boundary - previous.rect.y));
      previous.rect.height = previous.height;
    }

    return resolved;
  }

  function serializeLongEditableSegmentPlan(plan) {
    const firstSegment = plan.segments.length > 0 ? plan.segments[0] : null;
    return {
      rootName: plan.rootName,
      width: Math.round(plan.bounds.width),
      height: Math.round(plan.bounds.height),
      segmentCount: plan.segmentCount,
      approximateSegmentHeight: firstSegment ? Math.round(firstSegment.height) : 0,
      maxSegmentHeight: LONG_EDITABLE_SEGMENT_HEIGHT,
      maxSegmentArea: LONG_EDITABLE_SEGMENT_AREA,
      nodeCount: plan.complexity.nodeCount,
      textCount: plan.complexity.textCount,
      imagePaintCount: plan.complexity.imagePaintCount,
      effectCount: plan.complexity.effectCount,
      truncated: plan.complexity.truncated === true
    };
  }

  async function runLongEditableSegmentExport(message, plan) {
    cleanupExportMemory("before-long-editable");

    const previousSelection = figma.currentPage.selection ? figma.currentPage.selection.slice() : [];
    const frames = [];
    let splitMode = "fixed";

    try {
      const prepared = createLongEditableExportFrames(plan);
      splitMode = prepared.mode;
      for (let index = 0; index < prepared.frames.length; index += 1) {
        frames.push(prepared.frames[index]);
      }

      activeEditableSegmentSession = {
        frames: frames,
        previousSelection: previousSelection
      };

      figma.currentPage.selection = frames;
      if (splitMode === "section") {
        figma.notify("긴 PSD를 섹션 기준 " + frames.length + "개 프레임으로 나눠 순차 생성합니다.");
      } else {
        figma.notify("긴 PSD를 " + frames.length + "개 구간으로 나눠 순차 생성합니다.");
      }

      const segmentedMessage = Object.assign({}, message, {
        longEditableSegmentActive: true,
        longEditableSegmentConsent: false
      });

      return originalOnMessage(segmentedMessage);
    } catch (error) {
      cleanupNodes(frames);
      restoreSelection(previousSelection);
      activeEditableSegmentSession = null;
      const messageText = error instanceof Error && error.message ? error.message : "긴 PSD 구간 준비 중 오류가 발생했습니다.";
      figma.ui.postMessage({ type: "export-error", message: messageText });
      figma.notify(messageText, { error: true });
    }
  }

  function createLongEditableExportFrames(plan) {
    const sectionSplit = createLongEditableSectionSplitFrames(plan);
    if (sectionSplit && sectionSplit.frames && sectionSplit.frames.length >= 2) {
      return sectionSplit;
    }

    const frames = [];
    for (let index = 0; index < plan.segments.length; index += 1) {
      frames.push(createLongEditableSegmentFrame(plan, plan.segments[index]));
    }

    return {
      frames: frames,
      mode: "fixed"
    };
  }

  function createLongEditableSectionSplitFrames(plan) {
    const api = getSplitLongFrameApi();
    if (!api || typeof api.createFramesForNode !== "function") {
      return null;
    }

    try {
      const output = api.createFramesForNode(plan.root, {
        direction: "vertical",
        outputOffset: 120,
        outputGap: 48,
        selectCreatedFrames: false,
        scrollIntoView: false
      });

      if (!output || !Array.isArray(output.frames) || output.frames.length < 2) {
        cleanupLongEditableSplitOutput(output);
        return null;
      }

      return {
        frames: output.frames,
        mode: "section"
      };
    } catch (error) {
      try {
        console.warn("[pigma][long-editable-section-split]", error);
      } catch (warnError) {
      }
      return null;
    }
  }

  function getSplitLongFrameApi() {
    const api = globalScope.__PIGMA_SPLIT_LONG_FRAME_API__;
    return api && typeof api === "object" ? api : null;
  }

  function cleanupLongEditableSplitOutput(output) {
    if (!output || !Array.isArray(output.frames)) {
      return;
    }

    cleanupNodes(output.frames);
  }

  function createLongEditableSegmentFrame(plan, segment) {
    const root = plan.root;
    const bounds = plan.bounds;
    const frame = figma.createFrame();
    frame.name = buildNumberedSegmentFrameName(plan.rootName, segment.index);
    frame.clipsContent = true;
    frame.fills = [];
    frame.strokes = [];
    frame.x = Math.round(bounds.x + bounds.width + 120);
    frame.y = Math.round(bounds.y + segment.offsetY);
    frame.resize(Math.max(1, Math.round(bounds.width)), Math.max(1, Math.round(segment.height)));
    figma.currentPage.appendChild(frame);
    const frameRect = {
      x: frame.x,
      y: frame.y,
      width: segment.rect.width,
      height: segment.rect.height
    };

    appendRootVisualBackground(root, frame, bounds, segment);

    if ("children" in root) {
      for (let index = 0; index < root.children.length; index += 1) {
        const child = root.children[index];
        if (nodeIntersectsRectDeep(child, segment.rect)) {
          appendSegmentNodeForFrame(child, frame, segment.rect, frameRect);
        }
      }
    } else if (nodeIntersectsRectDeep(root, segment.rect)) {
      appendSegmentNodeForFrame(root, frame, segment.rect, frameRect);
    }

    return frame;
  }

  function buildNumberedSegmentFrameName(rootName, index) {
    const number = String(index + 1).padStart(3, "0");
    return number + "_" + sanitizeGeneratedFrameBaseName(rootName);
  }

  function sanitizeGeneratedFrameBaseName(name) {
    const cleaned = String(name || "")
      .replace(/[\\/:*?"<>|]+/g, " ")
      .replace(/\s+/g, " ")
      .trim();
    if (!cleaned) {
      return "\uC774\uB984 \uC5C6\uC74C";
    }
    return cleaned.length > 120 ? cleaned.slice(0, 120).trim() : cleaned;
  }

  function appendRootVisualBackground(root, frame, bounds, segment) {
    if (!hasVisiblePaints(root, "fills") && !hasVisiblePaints(root, "strokes")) {
      return;
    }

    const background = figma.createRectangle();
    background.name = getNodeName(root) + " background";
    background.resize(Math.max(1, Math.round(bounds.width)), Math.max(1, Math.round(bounds.height)));
    background.x = 0;
    background.y = -Math.round(segment.offsetY);
    copyPaintProperty(root, background, "fills");
    copyPaintProperty(root, background, "strokes");
    copyScalarProperty(root, background, "strokeWeight");
    copyScalarProperty(root, background, "strokeAlign");
    copyScalarProperty(root, background, "opacity");
    frame.appendChild(background);
  }

  function appendSegmentNodeForFrame(source, frame, sourceRect, frameRect) {
    if (shouldInlineSegmentContainer(source, sourceRect)) {
      appendInlineSegmentChildren(source, frame, sourceRect, frameRect);
      return null;
    }

    return appendSegmentClone(source, frame, sourceRect, frameRect);
  }

  function appendInlineSegmentChildren(container, frame, sourceRect, frameRect) {
    if (!container || !("children" in container)) {
      return;
    }

    for (let index = 0; index < container.children.length; index += 1) {
      const child = container.children[index];
      if (!nodeIntersectsRectDeep(child, sourceRect)) {
        continue;
      }

      appendSegmentNodeForFrame(child, frame, sourceRect, frameRect);
    }
  }

  function shouldInlineSegmentContainer(node, rect) {
    if (!isStructuralSegmentContainer(node)) {
      return false;
    }

    const bounds = getNodeBounds(node);
    if (!bounds) {
      return true;
    }

    return !rectContainsRect(rect, bounds);
  }

  function appendSegmentClone(source, frame, sourceRect, frameRect) {
    const clone = source.clone();
    frame.appendChild(clone);
    positionCloneInsideSegmentFrame(source, clone, sourceRect);
    pruneCloneToSegment(clone, frameRect);
    return clone;
  }

  function positionCloneInsideSegmentFrame(source, clone, rect) {
    if ("relativeTransform" in clone && "absoluteTransform" in source) {
      const sourceTransform = source.absoluteTransform;
      clone.relativeTransform = [
        [sourceTransform[0][0], sourceTransform[0][1], sourceTransform[0][2] - rect.x],
        [sourceTransform[1][0], sourceTransform[1][1], sourceTransform[1][2] - rect.y]
      ];
      return;
    }

    const bounds = getNodeBounds(source);
    if (bounds && "x" in clone && "y" in clone) {
      clone.x = Math.round(bounds.x - rect.x);
      clone.y = Math.round(bounds.y - rect.y);
    }
  }

  function pruneCloneToSegment(node, rect) {
    if (!("children" in node)) {
      return;
    }

    for (let index = node.children.length - 1; index >= 0; index -= 1) {
      const child = node.children[index];
      if (!nodeIntersectsRectDeep(child, rect)) {
        removeOrHideSegmentChild(child);
      } else {
        pruneCloneToSegment(child, rect);
        if ("children" in child && child.children.length === 0 && isEmptyStructuralSegmentContainer(child)) {
          removeOrHideSegmentChild(child);
        }
      }
    }
  }

  function removeOrHideSegmentChild(node) {
    try {
      node.remove();
      return;
    } catch (error) {
    }

    try {
      if ("visible" in node) {
        node.visible = false;
        return;
      }
    } catch (error) {
    }

    try {
      if ("opacity" in node) {
        node.opacity = 0;
      }
    } catch (error) {
    }
  }

  function analyzeLongEditableComplexity(root) {
    const result = {
      nodeCount: 0,
      textCount: 0,
      imagePaintCount: 0,
      effectCount: 0,
      truncated: false
    };
    const stack = [root];

    while (stack.length > 0) {
      const node = stack.pop();
      result.nodeCount += 1;

      if (node.type === "TEXT") {
        result.textCount += 1;
      }

      result.imagePaintCount += countImagePaints(node);
      result.effectCount += countVisibleEffects(node);

      if (result.nodeCount >= LONG_EDITABLE_ANALYSIS_LIMIT) {
        result.truncated = true;
        break;
      }

      if ("children" in node) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return result;
  }

  function nodeIntersectsRectDeep(node, rect) {
    const bounds = getNodeBounds(node);
    if (bounds && rectsIntersect(bounds, rect)) {
      return true;
    }

    if (!bounds && "children" in node) {
      for (let index = 0; index < node.children.length; index += 1) {
        if (nodeIntersectsRectDeep(node.children[index], rect)) {
          return true;
        }
      }
    }

    return false;
  }

  function rectsIntersect(a, b) {
    return a.x < b.x + b.width && a.x + a.width > b.x && a.y < b.y + b.height && a.y + a.height > b.y;
  }

  function rectContainsRect(outer, inner) {
    return inner.x >= outer.x - 0.5 &&
      inner.y >= outer.y - 0.5 &&
      inner.x + inner.width <= outer.x + outer.width + 0.5 &&
      inner.y + inner.height <= outer.y + outer.height + 0.5;
  }

  function getNodeBounds(node) {
    if (node && "absoluteBoundingBox" in node && isUsableBounds(node.absoluteBoundingBox)) {
      return normalizeBounds(node.absoluteBoundingBox);
    }

    if (node && "absoluteRenderBounds" in node && isUsableBounds(node.absoluteRenderBounds)) {
      return normalizeBounds(node.absoluteRenderBounds);
    }

    return null;
  }

  function normalizeBounds(bounds) {
    return {
      x: bounds.x,
      y: bounds.y,
      width: bounds.width,
      height: bounds.height
    };
  }

  function isUsableBounds(bounds) {
    return !!bounds && Number.isFinite(bounds.x) && Number.isFinite(bounds.y) && Number.isFinite(bounds.width) && Number.isFinite(bounds.height) && bounds.width > 0 && bounds.height > 0;
  }

  function getNodeName(node) {
    return node && typeof node.name === "string" && node.name.length > 0 ? node.name : "figma-export";
  }

  function countImagePaints(node) {
    return countImagePaintsForProperty(node, "fills") + countImagePaintsForProperty(node, "strokes");
  }

  function countImagePaintsForProperty(node, property) {
    if (!node || !(property in node) || !Array.isArray(node[property])) {
      return 0;
    }

    let count = 0;
    const paints = node[property];
    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (paint && paint.type === "IMAGE" && paint.visible !== false) {
        count += 1;
      }
    }
    return count;
  }

  function countVisibleEffects(node) {
    if (!node || !("effects" in node) || !Array.isArray(node.effects)) {
      return 0;
    }

    let count = 0;
    for (let index = 0; index < node.effects.length; index += 1) {
      const effect = node.effects[index];
      if (effect && effect.visible !== false) {
        count += 1;
      }
    }
    return count;
  }

  function hasVisiblePaints(node, property) {
    if (!node || !(property in node) || !Array.isArray(node[property])) {
      return false;
    }

    const paints = node[property];
    for (let index = 0; index < paints.length; index += 1) {
      if (paints[index] && paints[index].visible !== false) {
        return true;
      }
    }

    return false;
  }

  function isStructuralSegmentContainer(node) {
    if (!node || !("children" in node) || !Array.isArray(node.children) || !isEmptyStructuralSegmentContainer(node)) {
      return false;
    }

    const type = String(node.type || "");
    return type === "GROUP" || type === "FRAME" || type === "SECTION";
  }

  function isEmptyStructuralSegmentContainer(node) {
    if (!node || node.isMask === true || node.clipsContent === true) {
      return false;
    }

    const type = String(node.type || "");
    if (type === "INSTANCE" || type === "COMPONENT" || type === "COMPONENT_SET" || type === "BOOLEAN_OPERATION") {
      return false;
    }

    if (hasVisibleArrayProperty(node, "fills") || hasVisibleArrayProperty(node, "strokes") || hasVisibleArrayProperty(node, "effects")) {
      return false;
    }

    if (typeof node.opacity === "number" && node.opacity < 0.999) {
      return false;
    }

    const blendMode = String(node.blendMode || "NORMAL");
    return blendMode === "NORMAL" || blendMode === "PASS_THROUGH";
  }

  function hasVisibleArrayProperty(node, property) {
    if (!node || !(property in node) || !Array.isArray(node[property])) {
      return false;
    }

    return node[property].some(item => item && item.visible !== false);
  }

  function copyPaintProperty(source, target, property) {
    if (source && target && property in source && property in target && Array.isArray(source[property])) {
      try {
        target[property] = source[property];
      } catch (error) {
      }
    }
  }

  function copyScalarProperty(source, target, property) {
    if (source && target && property in source && property in target) {
      try {
        target[property] = source[property];
      } catch (error) {
      }
    }
  }

  function cleanupEditableSegmentSession() {
    if (!activeEditableSegmentSession) {
      return;
    }

    const session = activeEditableSegmentSession;
    activeEditableSegmentSession = null;
    const shouldRestoreSelection = shouldRestoreEditableSegmentSelection(session);
    cleanupNodes(session.frames);
    if (shouldRestoreSelection) {
      restoreSelection(session.previousSelection);
    }
  }

  function shouldRestoreEditableSegmentSelection(session) {
    try {
      const currentSelection = figma.currentPage && Array.isArray(figma.currentPage.selection)
        ? figma.currentPage.selection
        : [];
      if (!currentSelection.length) {
        return true;
      }

      const frames = Array.isArray(session.frames) ? session.frames : [];
      const frameIds = new Set(frames.map(node => node && node.id).filter(Boolean));
      if (!frameIds.size) {
        return true;
      }

      return currentSelection.some(node => node && frameIds.has(node.id));
    } catch (error) {
      return true;
    }
  }

  function cleanupExportMemory(reason) {
    cleanupEditableSegmentSession();
    cleanupOrphanPigmaExportNodes(reason);
  }

  function cleanupOrphanPigmaExportNodes(reason) {
    let removedCount = 0;
    try {
      const pages = figma.currentPage ? [figma.currentPage] : [];
      for (let index = 0; index < pages.length; index += 1) {
        removedCount += cleanupPigmaExportTempDescendants(pages[index]);
      }
    } catch (error) {
    }

    if (removedCount > 0) {
      try {
        console.info("[pigma][export-memory-cleanup]", reason || "export", "removed", removedCount);
      } catch (error) {
      }
    }
  }

  function cleanupPigmaExportTempDescendants(parent) {
    try {
      if (!parent || !("children" in parent) || !Array.isArray(parent.children)) {
        return 0;
      }

      let removedCount = 0;
      const children = Array.from(parent.children);
      for (let index = 0; index < children.length; index += 1) {
        const child = children[index];
        if (!child || child.removed) {
          continue;
        }

        if (isPigmaExportTempNode(child)) {
          if (removeNodeSafely(child)) {
            removedCount += 1;
          }
          continue;
        }

        removedCount += cleanupPigmaExportTempDescendants(child);
      }

      return removedCount;
    } catch (error) {
      return 0;
    }
  }

  function isPigmaExportTempNode(node) {
    try {
      const name = node && typeof node.name === "string" ? node.name : "";
      return PIGMA_EXPORT_TEMP_NODE_NAME_SET.has(name);
    } catch (error) {
      return false;
    }
  }

  function removeNodeSafely(node) {
    try {
      if (node && !node.removed) {
        node.remove();
        return true;
      }
    } catch (error) {
    }
    return false;
  }

  function cleanupNodes(nodes) {
    if (!nodes) {
      return;
    }

    for (let index = 0; index < nodes.length; index += 1) {
      const node = nodes[index];
      removeNodeSafely(node);
    }
  }

  function restoreSelection(selection) {
    if (!selection) {
      return;
    }

    const aliveSelection = [];
    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (node && !node.removed) {
        aliveSelection.push(node);
      }
    }

    try {
      figma.currentPage.selection = aliveSelection;
    } catch (error) {
    }
  }
})();

;(()=>{
  // PIGMA_AI_SETTINGS_STORAGE::SOURCE_OF_TRUTH
  // Keep AI settings persistence in plugin storage so the UI can survive
  // panel reloads and full Figma app restarts.
  const originalOnMessage = figma.ui.onmessage;
  const AI_SETTINGS_KEY = "pigma:ai-settings:v1";
  const DEFAULT_AI_SETTINGS = Object.freeze({
    enabled: false,
    provider: "openai",
    apiKey: "",
    openAiApiKey: "",
    geminiApiKey: "",
    dubApiKey: "",
    openAiImageModel: "gpt-image-1.5",
    openAiImageModelPinned: false,
    geminiImageModel: "gemini-3.1-flash-image-preview",
    promptDraftTotalTimeoutSec: 45,
    promptDraftAttemptTimeoutSec: 22,
    proofingLocale: "",
    userDictionary: [],
    protectedTerms: []
  });

  let cachedAiSettings = DEFAULT_AI_SETTINGS;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async message => {
    if (isAiSettingsMessage(message)) {
      if (message.type === "request-ai-settings") {
        await postAiSettings();
        return;
      }

      await writeAiSettings(message.settings);
      await postAiSettings();
      return;
    }

    return originalOnMessage(message);
  };

  function isAiSettingsMessage(message) {
    return !!message && (message.type === "request-ai-settings" || message.type === "update-ai-settings");
  }

  async function postAiSettings() {
    figma.ui.postMessage({
      type: "ai-settings",
      settings: await readAiSettings()
    });
  }

  async function readAiSettings() {
    try {
      cachedAiSettings = normalizeAiSettings(await figma.clientStorage.getAsync(AI_SETTINGS_KEY));
    } catch (error) {
      cachedAiSettings = normalizeAiSettings(null);
    }

    return cachedAiSettings;
  }

  async function writeAiSettings(settings) {
    cachedAiSettings = normalizeAiSettings(settings);

    try {
      await figma.clientStorage.setAsync(AI_SETTINGS_KEY, cachedAiSettings);
    } catch (error) {}

    return cachedAiSettings;
  }

  function normalizeAiSettings(value) {
    const source = value && typeof value === "object" ? value : {};
    const legacyProvider = source.provider === "gemini" ? "gemini" : DEFAULT_AI_SETTINGS.provider;
    const legacyApiKey = typeof source.apiKey === "string" ? sanitizeApiKey(source.apiKey) : DEFAULT_AI_SETTINGS.apiKey;
    const openAiApiKey =
      typeof source.openAiApiKey === "string"
        ? sanitizeApiKey(source.openAiApiKey)
        : legacyProvider === "openai"
          ? legacyApiKey
          : DEFAULT_AI_SETTINGS.openAiApiKey;
    const geminiApiKey =
      typeof source.geminiApiKey === "string"
        ? sanitizeApiKey(source.geminiApiKey)
        : legacyProvider === "gemini"
          ? legacyApiKey
          : DEFAULT_AI_SETTINGS.geminiApiKey;
    const dubApiKey =
      typeof source.dubApiKey === "string" ? sanitizeApiKey(source.dubApiKey) : DEFAULT_AI_SETTINGS.dubApiKey;
    const openAiImageModelPinned = source.openAiImageModelPinned === true;
    const rawOpenAiImageModel =
      typeof source.openAiImageModel === "string"
        ? normalizeModelId(source.openAiImageModel, DEFAULT_AI_SETTINGS.openAiImageModel)
        : DEFAULT_AI_SETTINGS.openAiImageModel;
    const openAiImageModel = rawOpenAiImageModel;
    const geminiImageModel =
      typeof source.geminiImageModel === "string"
        ? normalizeModelId(source.geminiImageModel, DEFAULT_AI_SETTINGS.geminiImageModel)
        : DEFAULT_AI_SETTINGS.geminiImageModel;
    const promptDraftTotalTimeoutSec = normalizePromptDraftTimeoutSec(
      source.promptDraftTotalTimeoutSec,
      DEFAULT_AI_SETTINGS.promptDraftTotalTimeoutSec,
      20,
      120
    );
    const promptDraftAttemptTimeoutSec = Math.min(
      normalizePromptDraftTimeoutSec(
        source.promptDraftAttemptTimeoutSec,
        DEFAULT_AI_SETTINGS.promptDraftAttemptTimeoutSec,
        8,
        60
      ),
      Math.max(8, promptDraftTotalTimeoutSec - 2)
    );
    const preferredProvider = legacyProvider === "gemini" ? "gemini" : "openai";
    const fallbackProvider = preferredProvider === "gemini" ? "openai" : "gemini";
    const provider =
      (preferredProvider === "gemini" ? geminiApiKey : openAiApiKey)
        ? preferredProvider
        : (fallbackProvider === "gemini" ? geminiApiKey : openAiApiKey)
          ? fallbackProvider
          : preferredProvider;
    const apiKey = provider === "gemini" ? geminiApiKey : openAiApiKey;

    return {
      enabled: source.enabled === true,
      provider,
      apiKey,
      openAiApiKey,
      geminiApiKey,
      dubApiKey,
      openAiImageModel,
      openAiImageModelPinned,
      geminiImageModel,
      promptDraftTotalTimeoutSec,
      promptDraftAttemptTimeoutSec,
      proofingLocale: normalizeProofingLocale(source.proofingLocale),
      userDictionary: normalizeTermList(source.userDictionary),
      protectedTerms: normalizeTermList(source.protectedTerms)
    };
  }

  function sanitizeApiKey(value) {
    return String(value || "")
      .replace(/[\u0000-\u001F\u007F-\u009F]/g, "")
      .replace(/[\u200B-\u200D\uFEFF]/g, "")
      .replace(/\s+/g, "")
      .replace(/^['"`]+|['"`]+$/g, "")
      .replace(/[^\x21-\x7E]/g, "")
      .trim();
  }

  function normalizePromptDraftTimeoutSec(value, fallback, min, max) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) {
      return fallback;
    }
    return Math.max(min, Math.min(max, Math.round(numeric)));
  }

  function normalizeModelId(value, fallback) {
    const next = String(value || "")
      .replace(/[\u0000-\u001F\u007F-\u009F]/g, "")
      .replace(/[\u200B-\u200D\uFEFF]/g, "")
      .trim();
    return next || fallback;
  }

  function normalizeProofingLocale(value) {
    const compact = String(value || "")
      .replace(/[^A-Za-z-]/g, "")
      .trim();
    if (!compact) {
      return "";
    }

    const parts = compact.split("-").filter(Boolean);
    if (parts.length === 1 && /^[A-Za-z]{2,3}$/.test(parts[0])) {
      return parts[0].toLowerCase();
    }

    if (parts.length === 2 && /^[A-Za-z]{2,3}$/.test(parts[0]) && /^[A-Za-z]{2,4}$/.test(parts[1])) {
      return `${parts[0].toLowerCase()}-${parts[1].toUpperCase()}`;
    }

    return "";
  }

  function normalizeTermList(value) {
    const terms = Array.isArray(value)
      ? value
      : typeof value === "string"
        ? value.split(/\r?\n|,/)
        : [];
    const normalized = [];
    const seen = new Set();

    for (const term of terms) {
      const next = String(term || "").replace(/\s+/g, " ").trim();
      if (!next) {
        continue;
      }

      const key = next.toLocaleLowerCase();
      if (seen.has(key)) {
        continue;
      }

      seen.add(key);
      normalized.push(next);
    }

    return normalized.slice(0, 200);
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_RESPONSIVE_MEMORY_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESPONSIVE_MEMORY_KEY = "pigma:responsive-memory:v1";
  const RESPONSIVE_MEMORY_SCHEMA_VERSION = 1;
  const DEFAULT_RESPONSIVE_MEMORY = Object.freeze({
    schemaVersion: RESPONSIVE_MEMORY_SCHEMA_VERSION,
    revision: "",
    updatedAt: "",
    records: [],
  });

  if (typeof originalOnMessage !== "function") {
    return;
  }

  globalScope.__PIGMA_RESPONSIVE_MEMORY__ = {
    readStoreAsync: readResponsiveMemoryStore,
    writeStoreAsync: writeResponsiveMemoryStore,
    appendRecordsAsync: appendResponsiveMemoryRecords,
    summarizeStore: summarizeResponsiveMemoryStore,
    serializeStore: serializeResponsiveMemoryStore,
    normalizeRecord: normalizeResponsiveMemoryRecord,
    normalizeErrorMessage: normalizeResponsiveMemoryError,
  };

  figma.ui.onmessage = async (message) => {
    if (isResponsiveMemoryMessage(message)) {
      await handleResponsiveMemoryMessage(message);
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_RESPONSIVE_MEMORY_PATCH__ = true;

  function isResponsiveMemoryMessage(message) {
    return (
      !!message &&
      (message.type === "request-responsive-memory-state" ||
        message.type === "export-responsive-memory-jsonl" ||
        message.type === "import-responsive-memory-jsonl" ||
        message.type === "clear-responsive-memory")
    );
  }

  async function handleResponsiveMemoryMessage(message) {
    try {
      if (message.type === "request-responsive-memory-state") {
        await postResponsiveMemoryState();
        return;
      }

      if (message.type === "export-responsive-memory-jsonl") {
        await postResponsiveMemoryExport();
        return;
      }

      if (message.type === "import-responsive-memory-jsonl") {
        await importResponsiveMemory(message);
        return;
      }

      if (message.type === "clear-responsive-memory") {
        await clearResponsiveMemory();
        return;
      }
    } catch (error) {
      postResponsiveMemoryError(normalizeResponsiveMemoryError(error));
    }
  }

  async function postResponsiveMemoryState() {
    const store = await readResponsiveMemoryStore();
    figma.ui.postMessage({
      type: "responsive-memory-state",
      state: summarizeResponsiveMemoryStore(store),
    });
  }

  async function postResponsiveMemoryExport() {
    const store = await readResponsiveMemoryStore();
    figma.ui.postMessage({
      type: "responsive-memory-export",
      content: serializeResponsiveMemoryStore(store),
      state: summarizeResponsiveMemoryStore(store),
    });
  }

  async function importResponsiveMemory(message) {
    const mode = normalizeImportMode(message && message.mode);
    const parsed = parseJsonLines(message && message.content);
    const currentStore = await readResponsiveMemoryStore();
    const nextStore = mode === "replace" ? buildResponsiveMemoryStore(parsed.records) : mergeResponsiveMemoryStores(currentStore, parsed.records);
    await writeResponsiveMemoryStore(nextStore);

    figma.ui.postMessage({
      type: "responsive-memory-import-result",
      mode: mode,
      importedCount: parsed.records.length,
      skippedCount: parsed.errors.length,
      errors: parsed.errors,
      state: summarizeResponsiveMemoryStore(nextStore),
    });
  }

  async function clearResponsiveMemory() {
    const emptyStore = cloneDefaultResponsiveMemory();
    await writeResponsiveMemoryStore(emptyStore);
    figma.ui.postMessage({
      type: "responsive-memory-clear-result",
      state: summarizeResponsiveMemoryStore(emptyStore),
    });
  }

  async function readResponsiveMemoryStore() {
    try {
      return normalizeResponsiveMemoryStore(await figma.clientStorage.getAsync(RESPONSIVE_MEMORY_KEY));
    } catch (error) {
      return cloneDefaultResponsiveMemory();
    }
  }

  async function writeResponsiveMemoryStore(value) {
    const normalized = normalizeResponsiveMemoryStore(value);
    await figma.clientStorage.setAsync(RESPONSIVE_MEMORY_KEY, normalized);
    return normalized;
  }

  async function appendResponsiveMemoryRecords(records) {
    const currentStore = await readResponsiveMemoryStore();
    const list = Array.isArray(records) ? records : [];
    const nextStore = mergeResponsiveMemoryStores(currentStore, list);
    await writeResponsiveMemoryStore(nextStore);
    return nextStore;
  }

  function cloneDefaultResponsiveMemory() {
    return {
      schemaVersion: DEFAULT_RESPONSIVE_MEMORY.schemaVersion,
      revision: DEFAULT_RESPONSIVE_MEMORY.revision,
      updatedAt: DEFAULT_RESPONSIVE_MEMORY.updatedAt,
      records: [],
    };
  }

  function normalizeResponsiveMemoryStore(value) {
    const source = value && typeof value === "object" ? value : {};
    const rawRecords = Array.isArray(source.records) ? source.records : [];
    const recordsByKey = new Map();

    for (const entry of rawRecords) {
      const normalizedEntry = normalizeResponsiveMemoryRecord(entry);
      if (!normalizedEntry) {
        continue;
      }

      recordsByKey.set(buildResponsiveMemoryRecordMergeKey(normalizedEntry), normalizedEntry);
    }

    const records = Array.from(recordsByKey.values());

    const updatedAt =
      typeof source.updatedAt === "string" && source.updatedAt ? source.updatedAt : records.length ? new Date().toISOString() : "";
    return {
      schemaVersion: RESPONSIVE_MEMORY_SCHEMA_VERSION,
      revision: buildResponsiveMemoryRevision(records, updatedAt),
      updatedAt: updatedAt,
      records: records,
    };
  }

  function buildResponsiveMemoryRevision(records, updatedAt) {
    const list = Array.isArray(records) ? records.slice() : [];
    list.sort((left, right) => buildResponsiveMemoryRecordMergeKey(left).localeCompare(buildResponsiveMemoryRecordMergeKey(right)));

    let hash = 2166136261;
    hash = updateResponsiveMemoryHash(hash, String(updatedAt || ""));

    for (let index = 0; index < list.length; index += 1) {
      hash = updateResponsiveMemoryHash(hash, serializeValueForResponsiveMemoryRevision(list[index]));
    }

    return "rm-" + String(list.length) + "-" + String(hash >>> 0).toString(36);
  }

  function updateResponsiveMemoryHash(seed, text) {
    let hash = seed >>> 0;
    const source = String(text || "");
    for (let index = 0; index < source.length; index += 1) {
      hash ^= source.charCodeAt(index);
      hash = Math.imul(hash, 16777619) >>> 0;
    }
    return hash >>> 0;
  }

  function serializeValueForResponsiveMemoryRevision(value) {
    if (value === null) {
      return "null";
    }
    if (typeof value === "string") {
      return JSON.stringify(value);
    }
    if (typeof value === "number") {
      return isFinite(value) ? String(value) : "null";
    }
    if (typeof value === "boolean") {
      return value ? "true" : "false";
    }
    if (Array.isArray(value)) {
      const parts = [];
      for (let index = 0; index < value.length; index += 1) {
        parts.push(serializeValueForResponsiveMemoryRevision(value[index]));
      }
      return "[" + parts.join(",") + "]";
    }
    if (!value || typeof value !== "object") {
      return "null";
    }

    const keys = Object.keys(value).sort();
    const parts = [];
    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      parts.push(JSON.stringify(key) + ":" + serializeValueForResponsiveMemoryRevision(value[key]));
    }
    return "{" + parts.join(",") + "}";
  }

  function normalizeResponsiveMemoryRecord(value) {
    if (!value || typeof value !== "object") {
      return null;
    }

    const type = typeof value.type === "string" ? value.type.trim() : "";
    const id = typeof value.id === "string" ? value.id.trim() : "";
    if (!type || !id) {
      return null;
    }

    const next = {};
    Object.keys(value).forEach((key) => {
      next[key] = value[key];
    });
    next.type = type;
    next.id = id;
    if (typeof next.createdAt !== "string" || !next.createdAt) {
      next.createdAt = new Date().toISOString();
    }
    return next;
  }

  function buildResponsiveMemoryRecordMergeKey(record) {
    if (!record || typeof record !== "object") {
      return "";
    }

    const type = typeof record.type === "string" ? record.type.trim() : "";
    const id = typeof record.id === "string" ? record.id.trim() : "";
    const profileKey = typeof record.profileKey === "string" ? record.profileKey.trim() : "";
    const direction = typeof record.direction === "string" ? record.direction.trim() : "";
    if (!type || !id) {
      return "";
    }

    if (isStableProfileRecordType(type) && profileKey) {
      return `${type}|${direction || "default"}|${profileKey}`;
    }

    return `${type}|${id}`;
  }

  function isStableProfileRecordType(type) {
    return type === "text-role-profile" || type === "frame-shape-profile" || type === "container-profile";
  }

  function summarizeResponsiveMemoryStore(store) {
    const safeStore = normalizeResponsiveMemoryStore(store);
    const countsByType = {};
    const aggregateRecords = [];
    const pairLabelById = new Map();
    const sectionStats = {
      totalCount: 0,
      typeCounts: {},
    };
    const aggregateStats = {
      totalCount: 0,
      repeatedCount: 0,
      conflictCount: 0,
      highConfidenceCount: 0,
      totalSupportCount: 0,
      averageSupportCount: 0,
      pairCount: 0,
    };

    for (const record of safeStore.records) {
      const type = record.type;
      countsByType[type] = (countsByType[type] || 0) + 1;

      if (type === "pair") {
        aggregateStats.pairCount += 1;
        pairLabelById.set(record.id, buildResponsiveMemoryPairLabel(record));
        continue;
      }

      if (type === "section-example") {
        sectionStats.totalCount += 1;
        const sectionType = typeof record.sectionType === "string" && record.sectionType ? record.sectionType : "section";
        sectionStats.typeCounts[sectionType] = (sectionStats.typeCounts[sectionType] || 0) + 1;
        continue;
      }

      if (type !== "aggregate-rule") {
        continue;
      }

      aggregateRecords.push(record);
      aggregateStats.totalCount += 1;
      const supportCount = typeof record.supportCount === "number" && isFinite(record.supportCount) ? record.supportCount : 0;
      aggregateStats.totalSupportCount += supportCount;
      if (isRepeatedAggregateRule(record)) {
        aggregateStats.repeatedCount += 1;
      }
      if (isHighConfidenceAggregateRule(record)) {
        aggregateStats.highConfidenceCount += 1;
      }
    }

    if (aggregateStats.totalCount > 0) {
      aggregateStats.averageSupportCount = roundAggregateValue(aggregateStats.totalSupportCount / aggregateStats.totalCount);
    }
    aggregateStats.conflictCount = buildMeaningfulConflictGroups(aggregateRecords).length;

    return {
      schemaVersion: safeStore.schemaVersion,
      revision: safeStore.revision,
      updatedAt: safeStore.updatedAt,
      recordCount: safeStore.records.length,
      countsByType: countsByType,
      sectionStats: sectionStats,
      aggregateStats: aggregateStats,
      aggregatePreview: buildAggregateDetailPreview(aggregateRecords, pairLabelById),
    };
  }

  function buildAggregateDetailPreview(aggregateRecords, pairLabelById) {
    const records = Array.isArray(aggregateRecords) ? aggregateRecords.slice() : [];
    const repeatedRules = records
      .filter((record) => isRepeatedAggregateRule(record))
      .sort(compareAggregateRecords)
      .slice(0, 3)
      .map((record) => buildAggregateRuleDetail(record, pairLabelById));

    const highConfidenceRules = records
      .filter((record) => isHighConfidenceAggregateRule(record))
      .sort(compareAggregateRecords)
      .slice(0, 3)
      .map((record) => buildAggregateRuleDetail(record, pairLabelById));

    const conflictingRules = buildMeaningfulConflictGroups(records)
      .sort(compareConflictGroups)
      .slice(0, 3)
      .map((group) => buildAggregateConflictDetail(group, pairLabelById));

    return {
      repeatedRules: repeatedRules,
      conflictingRules: conflictingRules,
      highConfidenceRules: highConfidenceRules,
    };
  }

  function buildAggregateRuleDetail(record, pairLabelById) {
    const safeRecord = record && typeof record === "object" ? record : {};
    const summary = typeof safeRecord.summary === "string" && safeRecord.summary.trim() ? safeRecord.summary.trim() : "aggregate rule";
    const supportCount = getNumericField(safeRecord, "supportCount");
    const avgConfidence = getNumericField(safeRecord, "avgConfidence");
    const pairIds = Array.isArray(safeRecord.pairIds) ? safeRecord.pairIds : [];
    return {
      title: summary,
      detail: `support ${supportCount} · 평균 신뢰 ${formatConfidencePercent(avgConfidence)}`,
      meta: buildPairSupportLabel(pairIds, pairLabelById),
    };
  }

  function buildAggregateConflictDetail(group, pairLabelById) {
    const list = Array.isArray(group) ? group.slice() : [];
    list.sort(compareAggregateRecords);
    const first = list[0] || {};
    const patternLabels = [];
    const pairIds = [];

    for (let index = 0; index < list.length; index += 1) {
      const record = list[index];
      if (patternLabels.length < 2) {
        patternLabels.push(`${record.summary} ×${getNumericField(record, "supportCount")}`);
      }
      if (Array.isArray(record.pairIds)) {
        for (let pairIndex = 0; pairIndex < record.pairIds.length; pairIndex += 1) {
          if (pairIds.indexOf(record.pairIds[pairIndex]) < 0) {
            pairIds.push(record.pairIds[pairIndex]);
          }
        }
      }
    }

    return {
      title: buildAggregateConflictTitle(first),
      detail: patternLabels.join(" · "),
      meta: `${list.length}패턴 · ${buildPairSupportLabel(pairIds, pairLabelById)}`,
    };
  }

  function buildAggregateConflictTitle(record) {
    const safeRecord = record && typeof record === "object" ? record : {};
    const scope = humanizeAggregateToken(safeRecord.scope);
    const ruleType = humanizeAggregateToken(safeRecord.ruleType);
    if (scope && ruleType) {
      return `${scope} ${ruleType}`;
    }
    if (ruleType) {
      return ruleType;
    }
    if (scope) {
      return scope;
    }
    return "aggregate conflict";
  }

  function isRepeatedAggregateRule(record) {
    return getNumericField(record, "supportCount") >= 2;
  }

  function isHighConfidenceAggregateRule(record) {
    return getNumericField(record, "supportCount") >= 3 && getNumericField(record, "avgConfidence") >= 0.9;
  }

  function buildMeaningfulConflictGroups(records) {
    const list = Array.isArray(records) ? records : [];
    const conflictGroups = new Map();

    for (let index = 0; index < list.length; index += 1) {
      const record = list[index];
      const conflictCount = getNumericField(record, "conflictCount");
      const bucketKey = typeof record.conflictBucketKey === "string" ? record.conflictBucketKey : "";
      if (conflictCount <= 1 || !bucketKey) {
        continue;
      }

      if (!conflictGroups.has(bucketKey)) {
        conflictGroups.set(bucketKey, []);
      }
      conflictGroups.get(bucketKey).push(record);
    }

    const meaningfulGroups = [];
    conflictGroups.forEach((group) => {
      const filtered = filterMeaningfulConflictGroupMembers(group);
      if (filtered.length >= 2) {
        meaningfulGroups.push(filtered);
      }
    });
    return meaningfulGroups;
  }

  function filterMeaningfulConflictGroupMembers(group) {
    const list = Array.isArray(group) ? group.slice() : [];
    list.sort(compareAggregateRecords);
    return list.filter((record) => isRepeatedAggregateRule(record));
  }

  function buildPairSupportLabel(pairIds, pairLabelById) {
    const ids = Array.isArray(pairIds) ? pairIds : [];
    const labels = [];

    for (let index = 0; index < ids.length; index += 1) {
      const pairId = ids[index];
      const label = pairLabelById && pairLabelById.has(pairId) ? pairLabelById.get(pairId) : "";
      if (label && labels.indexOf(label) < 0) {
        labels.push(label);
      }
    }

    const previewLabels = labels.slice(0, 2);
    const extraCount = labels.length - previewLabels.length;
    const labelParts = [];
    labelParts.push(`근거 pair ${ids.length}건`);
    if (previewLabels.length) {
      labelParts.push(previewLabels.join(" · "));
    }
    if (extraCount > 0) {
      labelParts.push(`+${extraCount}`);
    }
    return labelParts.join(" · ");
  }

  function buildResponsiveMemoryPairLabel(record) {
    const safeRecord = record && typeof record === "object" ? record : {};
    const pcName = typeof safeRecord.pcNodeName === "string" && safeRecord.pcNodeName.trim() ? safeRecord.pcNodeName.trim() : "PC";
    const moName = typeof safeRecord.moNodeName === "string" && safeRecord.moNodeName.trim() ? safeRecord.moNodeName.trim() : "MO";
    const pcWidth = getNumericField(safeRecord, "pcWidth");
    const moWidth = getNumericField(safeRecord, "moWidth");
    return `${pcName} ${pcWidth}px -> ${moName} ${moWidth}px`;
  }

  function compareAggregateRecords(left, right) {
    const rightSupport = getNumericField(right, "supportCount");
    const leftSupport = getNumericField(left, "supportCount");
    if (rightSupport !== leftSupport) {
      return rightSupport - leftSupport;
    }

    const rightConfidence = getNumericField(right, "avgConfidence");
    const leftConfidence = getNumericField(left, "avgConfidence");
    if (rightConfidence !== leftConfidence) {
      return rightConfidence - leftConfidence;
    }

    const rightConflict = getNumericField(right, "conflictCount");
    const leftConflict = getNumericField(left, "conflictCount");
    if (rightConflict !== leftConflict) {
      return rightConflict - leftConflict;
    }

    const leftSummary = typeof left.summary === "string" ? left.summary : "";
    const rightSummary = typeof right.summary === "string" ? right.summary : "";
    if (leftSummary < rightSummary) {
      return -1;
    }
    if (leftSummary > rightSummary) {
      return 1;
    }
    return 0;
  }

  function compareConflictGroups(left, right) {
    const leftList = Array.isArray(left) ? left : [];
    const rightList = Array.isArray(right) ? right : [];
    if (rightList.length !== leftList.length) {
      return rightList.length - leftList.length;
    }

    const leftBest = leftList.slice().sort(compareAggregateRecords)[0];
    const rightBest = rightList.slice().sort(compareAggregateRecords)[0];
    return compareAggregateRecords(leftBest, rightBest);
  }

  function humanizeAggregateToken(value) {
    const text = typeof value === "string" ? value.trim() : "";
    if (!text) {
      return "";
    }

    return text
      .replace(/[|/_-]+/g, " ")
      .replace(/\s+/g, " ")
      .trim();
  }

  function formatConfidencePercent(value) {
    const next = getNumericField({ value: value }, "value");
    return `${Math.round(next * 100)}%`;
  }

  function getNumericField(record, key) {
    const value = record && typeof record === "object" ? record[key] : 0;
    return typeof value === "number" && isFinite(value) ? value : 0;
  }

  function serializeResponsiveMemoryStore(store) {
    const safeStore = normalizeResponsiveMemoryStore(store);
    return safeStore.records.map((record) => JSON.stringify(record)).join("\n");
  }

  function parseJsonLines(content) {
    const text = typeof content === "string" ? content : "";
    const lines = text.split(/\r?\n/);
    const recordsByKey = new Map();
    const errors = [];

    for (let index = 0; index < lines.length; index += 1) {
      const rawLine = lines[index];
      const line = String(rawLine || "").trim();
      if (!line) {
        continue;
      }

      try {
        const parsed = JSON.parse(line);
        const normalized = normalizeResponsiveMemoryRecord(parsed);
        if (!normalized) {
          errors.push({
            line: index + 1,
            message: "Invalid responsive memory record.",
          });
          continue;
        }

        recordsByKey.set(buildResponsiveMemoryRecordMergeKey(normalized), normalized);
      } catch (error) {
        errors.push({
          line: index + 1,
          message: normalizeResponsiveMemoryError(error),
        });
      }
    }

    return {
      records: Array.from(recordsByKey.values()),
      errors: errors,
    };
  }

  function buildResponsiveMemoryStore(records) {
    return normalizeResponsiveMemoryStore({
      schemaVersion: RESPONSIVE_MEMORY_SCHEMA_VERSION,
      updatedAt: new Date().toISOString(),
      records: records,
    });
  }

  function mergeResponsiveMemoryStores(store, incomingRecords) {
    const currentStore = normalizeResponsiveMemoryStore(store);
    const map = new Map();

    for (const record of currentStore.records) {
      map.set(buildResponsiveMemoryRecordMergeKey(record), record);
    }

    for (const entry of incomingRecords) {
      const normalized = normalizeResponsiveMemoryRecord(entry);
      if (!normalized) {
        continue;
      }
      map.set(buildResponsiveMemoryRecordMergeKey(normalized), normalized);
    }

    return buildResponsiveMemoryStore(Array.from(map.values()));
  }

  function normalizeImportMode(value) {
    return value === "replace" ? "replace" : "merge";
  }

  function postResponsiveMemoryError(message) {
    figma.ui.postMessage({
      type: "responsive-memory-error",
      message: message,
    });
  }

  function normalizeResponsiveMemoryError(error) {
    if (error instanceof Error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return "Responsive memory operation failed.";
  }

  function roundAggregateValue(value) {
    if (typeof value !== "number" || !isFinite(value)) {
      return 0;
    }
    return Math.round(value * 100) / 100;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_RESPONSIVE_PAIR_ANALYZER_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const MOBILE_KNOWN_WIDTHS = [360, 375, 390, 393];
  const MOBILE_MAX_WIDTH = 480;
  const DESKTOP_MIN_WIDTH = 1024;
  const PATCH_VERSION = 5;
  const MAX_MATCH_RECORDS = 240;
  const MAX_CANDIDATE_MATCH_RECORDS = 64;
  const HIERARCHICAL_MATCH_THRESHOLD = 0.6;
  const FALLBACK_MATCH_THRESHOLD = 0.82;
  const CONFIRMED_MATCH_THRESHOLD = 0.9;
  const CANDIDATE_MATCH_THRESHOLD = 0.82;
  const LOW_CONFIDENCE_RULE_THRESHOLD = 0.82;
  const AGGREGATE_RULE_VERSION = 1;
  const TEXT_ROLE_PROFILE_VERSION = 1;
  const FRAME_SHAPE_PROFILE_VERSION = 1;
  const CONTAINER_PROFILE_VERSION = 1;
  const CONTAINER_TYPES = {
    FRAME: true,
    GROUP: true,
    SECTION: true,
    COMPONENT: true,
    COMPONENT_SET: true,
    INSTANCE: true,
  };
  const AUTO_NAME_PATTERNS = [
    /^frame \d+$/i,
    /^group \d+$/i,
    /^rectangle \d+$/i,
    /^text \d+$/i,
    /^vector \d+$/i,
    /^ellipse \d+$/i,
    /^polygon \d+$/i,
    /^star \d+$/i,
    /^line \d+$/i,
    /^image \d+$/i,
    /^component \d+$/i,
    /^instance \d+$/i,
    /^section \d+$/i,
    /^copy(?: of)? /i,
    /^untitled/i,
  ];
  const DECORATIVE_NAME_PATTERNS = [
    /^bg$/i,
    /^background$/i,
    /^mask(?: group)?$/i,
    /^pdf#?$/i,
    /^image$/i,
    /^\+$/,
    /^container(?: \d+)?$/i,
    /^content(?: \d+)?$/i,
    /^wrapper(?: \d+)?$/i,
    /^inner(?: \d+)?$/i,
    /^outer(?: \d+)?$/i,
    /^body(?: \d+)?$/i,
  ];
  const WEAK_TEXT_ROLE_PATTERNS = [
    /^description(?: text)?$/i,
    /^body text$/i,
    /^caption(?: text)?$/i,
    /^copy$/i,
    /^offer$/i,
    /^subtitle(?: text)?$/i,
    /^text$/i,
  ];
  const DECORATIVE_LEAF_TYPES = {
    RECTANGLE: true,
    VECTOR: true,
    ELLIPSE: true,
    LINE: true,
    POLYGON: true,
    STAR: true,
    BOOLEAN_OPERATION: true,
  };
  let activeExecution = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isResponsivePairAnalyzerMessage(message)) {
      await withExecutionLock(
        {
          status: "running",
          message: "선택한 두 프레임을 PC/MO 학습 메모리로 분석하는 중입니다.",
        },
        runResponsivePairAnalysis
      );
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_RESPONSIVE_PAIR_ANALYZER_PATCH__ = true;

  function isResponsivePairAnalyzerMessage(message) {
    return !!message && message.type === "run-responsive-pair-analysis";
  }

  async function withExecutionLock(execution, runner) {
    if (activeExecution) {
      postStatus(activeExecution.status, activeExecution.message);
      return false;
    }

    activeExecution = execution && typeof execution === "object" ? execution : { status: "running", message: "" };
    try {
      await runner();
      return true;
    } finally {
      activeExecution = null;
    }
  }

  async function runResponsivePairAnalysis() {
    postStatus("running", "선택한 두 프레임을 PC/MO 학습 메모리로 분석하는 중입니다.");

    try {
      const memory = getResponsiveMemoryHelper();
      const currentStore = await memory.readStoreAsync();
      const analysis = await analyzeCurrentPairSelection(currentStore);
      const nextStore = await memory.appendRecordsAsync(analysis.records);

      figma.ui.postMessage({
        type: "responsive-pair-analysis-result",
        result: analysis.result,
        state: memory.summarizeStore(nextStore),
      });

      figma.notify("PC/MO 학습 메모리에 저장했습니다.", { timeout: 1800 });
    } catch (error) {
      const message = normalizeErrorMessage(error);
      figma.ui.postMessage({
        type: "responsive-pair-analysis-error",
        message: message,
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "responsive-pair-analysis-status",
      status: status,
      message: message,
    });
  }

  async function analyzeCurrentPairSelection(currentStore) {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (selection.length !== 2) {
      throw new Error("학습하려면 루트 프레임 두 개를 함께 선택해 주세요.");
    }

    const first = buildRootDescriptor(selection[0]);
    const second = buildRootDescriptor(selection[1]);
    const classifiedPair = classifyResponsivePair(first, second);
    const createdAt = new Date().toISOString();
    const selectionSignature = getSelectionSignature(selection);
    const pairId = createRecordId("pair");
    const summaryId = createRecordId("summary");

    const pairRecord = {
      type: "pair",
      id: pairId,
      version: PATCH_VERSION,
      source: "responsive-pair-analyzer",
      selectionSignature: selectionSignature,
      pcNodeId: classifiedPair.pc.id,
      pcNodeName: classifiedPair.pc.name,
      pcWidth: classifiedPair.pc.width,
      pcHeight: classifiedPair.pc.height,
      moNodeId: classifiedPair.mo.id,
      moNodeName: classifiedPair.mo.name,
      moWidth: classifiedPair.mo.width,
      moHeight: classifiedPair.mo.height,
      detectionReason: classifiedPair.reason,
      createdAt: createdAt,
    };

    const rootMatchRecord = {
      type: "match",
      id: createRecordId("match"),
      version: PATCH_VERSION,
      pairId: pairId,
      scope: "root",
      fromNodeIds: [classifiedPair.pc.id],
      toNodeIds: [classifiedPair.mo.id],
      cardinality: "1:1",
      confidence: 1,
      evidence: {
        rootPair: true,
        widthClassification: true,
        structure: true,
      },
      createdAt: createdAt,
    };

    const matching = await buildNodeMatches(classifiedPair.pc, classifiedPair.mo, pairId, createdAt);
    const sectionRecords = buildSectionExampleRecords(classifiedPair, pairId, createdAt, matching);
    const frameTransformRecords = buildFrameTransformRecords(classifiedPair, pairId, createdAt);
    const frameShapeProfiles = buildFrameShapeProfileRecords(currentStore, frameTransformRecords, createdAt);
    const containerTransformRecords = buildContainerTransformRecords(classifiedPair, pairId, createdAt, matching, sectionRecords);
    const containerProfiles = buildContainerProfileRecords(currentStore, containerTransformRecords, createdAt);
    const nodeTransformRecords = buildNodeTransformRecords(classifiedPair, pairId, createdAt, matching, sectionRecords);
    const textRoleProfiles = buildTextRoleProfileRecords(currentStore, nodeTransformRecords, createdAt);
    const ruleRecords = buildRuleRecords(classifiedPair, pairId, createdAt, matching);
    const aggregate = buildAggregateRuleRecords(currentStore, ruleRecords, pairRecord, createdAt);
    const preview = buildAnalysisPreview(matching, sectionRecords, ruleRecords, aggregate.preview);
    const summaryRecord = {
      type: "summary",
      id: summaryId,
      version: PATCH_VERSION,
      pairId: pairId,
      content: `${classifiedPair.pc.name} (${classifiedPair.pc.width}) -> ${classifiedPair.mo.name} (${classifiedPair.mo.width})`,
      stats: {
        pcTotalNodes: classifiedPair.pc.stats.totalNodes,
        moTotalNodes: classifiedPair.mo.stats.totalNodes,
        pcTextNodes: classifiedPair.pc.stats.textNodes,
        moTextNodes: classifiedPair.mo.stats.textNodes,
        matchedNodeCount: matching.stats.matchedNodeCount,
        confirmedMatchCount: matching.stats.confirmedMatchCount,
        candidateMatchCount: matching.stats.candidateMatchCount,
        hierarchicalMatchCount: matching.stats.hierarchicalMatchCount,
        fallbackMatchCount: matching.stats.fallbackMatchCount,
        lowConfidenceMatchCount: matching.stats.lowConfidenceMatchCount,
        averageConfidence: matching.stats.averageConfidence,
        unmatchedPcCount: matching.stats.unmatchedPcCount,
        unmatchedMoCount: matching.stats.unmatchedMoCount,
        cappedAtMatchLimit: matching.stats.cappedAtMatchLimit,
        frameTransformCount: frameTransformRecords.length,
        containerTransformCount: containerTransformRecords.length,
        nodeTransformCount: nodeTransformRecords.length,
        sectionExampleCount: sectionRecords.length,
        ruleCount: ruleRecords.length,
        aggregateRuleCount: aggregate.records.length,
        frameShapeProfileCount: frameShapeProfiles.length,
        containerProfileCount: containerProfiles.length,
        textRoleProfileCount: textRoleProfiles.length,
      },
      createdAt: createdAt,
    };

    const records = [pairRecord, rootMatchRecord]
      .concat(matching.records)
      .concat(frameTransformRecords)
      .concat(containerTransformRecords)
      .concat(nodeTransformRecords)
      .concat(sectionRecords)
      .concat(ruleRecords)
      .concat(aggregate.records)
      .concat(frameShapeProfiles)
      .concat(containerProfiles)
      .concat(textRoleProfiles)
      .concat([summaryRecord]);

    return {
      records: records,
      result: {
        pairId: pairId,
        selectionSignature: selectionSignature,
        summary:
          `${classifiedPair.pc.name} ${classifiedPair.pc.width}px -> ${classifiedPair.mo.name} ${classifiedPair.mo.width}px` +
          ` · match ${matching.stats.matchedNodeCount}건 · rule ${ruleRecords.length}개`,
        detectionReason: classifiedPair.reason,
        recordsCreated: records.length,
        matchStats: matching.stats,
        matchCount: matching.stats.matchedNodeCount,
        confirmedMatchCount: matching.stats.confirmedMatchCount,
        candidateMatchCount: matching.stats.candidateMatchCount,
        lowConfidenceCount: matching.stats.lowConfidenceMatchCount,
        frameTransformCount: frameTransformRecords.length,
        containerTransformCount: containerTransformRecords.length,
        nodeTransformCount: nodeTransformRecords.length,
        sectionExampleCount: sectionRecords.length,
        ruleCount: ruleRecords.length,
        aggregateRuleCount: aggregate.records.length,
        frameShapeProfileCount: frameShapeProfiles.length,
        containerProfileCount: containerProfiles.length,
        textRoleProfileCount: textRoleProfiles.length,
        preview: preview,
        pc: simplifyRootDescriptor(classifiedPair.pc),
        mo: simplifyRootDescriptor(classifiedPair.mo),
      },
    };
  }

  function buildRootDescriptor(node) {
    const bounds = getNodeBounds(node);
    if (!bounds) {
      throw new Error(`"${safeName(node)}" 프레임의 크기를 읽지 못했습니다.`);
    }

    return {
      node: node,
      id: node.id,
      name: safeName(node),
      type: String(node.type || "UNKNOWN"),
      width: roundPixel(bounds.width),
      height: roundPixel(bounds.height),
      bounds: {
        x: bounds.x,
        y: bounds.y,
        width: bounds.width,
        height: bounds.height,
      },
      stats: collectNodeStats(node),
    };
  }

  function classifyResponsivePair(first, second) {
    const items = [first, second].sort((left, right) => left.width - right.width);
    const smaller = items[0];
    const larger = items[1];
    const smallerIsKnownMobile = MOBILE_KNOWN_WIDTHS.indexOf(smaller.width) >= 0;
    const smallerLooksMobile = smaller.width <= MOBILE_MAX_WIDTH;
    const largerLooksDesktop = larger.width >= DESKTOP_MIN_WIDTH;

    if (largerLooksDesktop && (smallerIsKnownMobile || smallerLooksMobile)) {
      return {
        pc: larger,
        mo: smaller,
        reason: smallerIsKnownMobile
          ? `모바일 기준 폭 ${smaller.width}px와 데스크톱 기준 폭 ${larger.width}px를 감지했습니다.`
          : `${larger.width}px는 PC, ${smaller.width}px는 MO 범위로 분류했습니다.`,
      };
    }

    if (first.width >= DESKTOP_MIN_WIDTH && second.width >= DESKTOP_MIN_WIDTH) {
      throw new Error("선택한 두 프레임이 모두 PC 범위입니다. MO 프레임을 함께 선택해 주세요.");
    }

    if (first.width <= MOBILE_MAX_WIDTH && second.width <= MOBILE_MAX_WIDTH) {
      throw new Error("선택한 두 프레임이 모두 MO 범위입니다. PC 프레임을 함께 선택해 주세요.");
    }

    if (!largerLooksDesktop) {
      throw new Error(`PC 프레임은 ${DESKTOP_MIN_WIDTH}px 이상이어야 합니다. 현재 큰 프레임은 ${larger.width}px입니다.`);
    }

    throw new Error(`MO 프레임은 ${MOBILE_MAX_WIDTH}px 이하 또는 대표 모바일 폭(360/375px)이어야 합니다. 현재 작은 프레임은 ${smaller.width}px입니다.`);
  }

  async function buildNodeMatches(pcRoot, moRoot, pairId, createdAt) {
    const pcTree = await collectTreeDescriptors(pcRoot.node, pcRoot.bounds);
    const moTree = await collectTreeDescriptors(moRoot.node, moRoot.bounds);
    const pcByParent = groupDescriptorsByParent(pcTree.descriptors);
    const moByParent = groupDescriptorsByParent(moTree.descriptors);
    const pcMatchedIds = new Set([pcRoot.id]);
    const moMatchedIds = new Set([moRoot.id]);
    const records = [];
    const stats = {
      matchedNodeCount: 0,
      confirmedMatchCount: 0,
      candidateMatchCount: 0,
      hierarchicalMatchCount: 0,
      fallbackMatchCount: 0,
      lowConfidenceMatchCount: 0,
      averageConfidence: 0,
      unmatchedPcCount: 0,
      unmatchedMoCount: 0,
      cappedAtMatchLimit: false,
    };
    const pairs = [];
    let totalConfidence = 0;

    await matchChildrenRecursive(pcRoot.id, moRoot.id);

    if (records.length < MAX_MATCH_RECORDS) {
      matchFallbackDescriptors();
    } else {
      stats.cappedAtMatchLimit = true;
    }

    const unmatchedPcDescriptors = getUnmatchedDescriptors(pcTree.descriptors, pcMatchedIds);
    const unmatchedMoDescriptors = getUnmatchedDescriptors(moTree.descriptors, moMatchedIds);
    stats.unmatchedPcCount = unmatchedPcDescriptors.length;
    stats.unmatchedMoCount = unmatchedMoDescriptors.length;

    return {
      records: records,
      stats: stats,
      pairs: pairs,
      pcUnmatchedDescriptors: unmatchedPcDescriptors,
      moUnmatchedDescriptors: unmatchedMoDescriptors,
    };

    async function matchChildrenRecursive(pcParentId, moParentId) {
      if (records.length >= MAX_MATCH_RECORDS) {
        stats.cappedAtMatchLimit = true;
        return;
      }

      const pcChildren = getUnmatchedChildren(pcByParent, pcParentId, pcMatchedIds);
      const moChildren = getUnmatchedChildren(moByParent, moParentId, moMatchedIds);
      if (!pcChildren.length || !moChildren.length) {
        return;
      }

      const localThreshold =
        pcParentId === pcRoot.id && moParentId === moRoot.id ? Math.max(HIERARCHICAL_MATCH_THRESHOLD, 0.72) : HIERARCHICAL_MATCH_THRESHOLD;
      const pairs = buildGreedyMatches(pcChildren, moChildren, localThreshold, true);
      for (let index = 0; index < pairs.length; index += 1) {
        const pair = pairs[index];
        if (records.length >= MAX_MATCH_RECORDS) {
          stats.cappedAtMatchLimit = true;
          return;
        }

        if (!registerPair(pair, "hierarchical")) {
          continue;
        }

        if (pair.pc.hasChildren && pair.mo.hasChildren && shouldRecurseIntoPair(pair)) {
          await matchChildrenRecursive(pair.pc.id, pair.mo.id);
        }
      }
    }

    function matchFallbackDescriptors() {
      const pcUnmatched = getUnmatchedDescriptors(pcTree.descriptors, pcMatchedIds);
      const moUnmatched = getUnmatchedDescriptors(moTree.descriptors, moMatchedIds);
      if (!pcUnmatched.length || !moUnmatched.length) {
        return;
      }

      const pairs = buildGreedyMatches(pcUnmatched, moUnmatched, FALLBACK_MATCH_THRESHOLD, false);
      for (let index = 0; index < pairs.length; index += 1) {
        const pair = pairs[index];
        if (records.length >= MAX_MATCH_RECORDS) {
          stats.cappedAtMatchLimit = true;
          return;
        }

        registerPair(pair, "fallback");
      }
    }

    function registerPair(pair, scope) {
      if (!shouldRetainMatchPair(pair, scope)) {
        return false;
      }

      const matchTier = getMatchTier(pair.score);
      if (matchTier === "candidate" && stats.candidateMatchCount >= MAX_CANDIDATE_MATCH_RECORDS) {
        return false;
      }

      pcMatchedIds.add(pair.pc.id);
      moMatchedIds.add(pair.mo.id);
      records.push(createMatchRecord(pairId, createdAt, pair, scope));
      pairs.push({
        pc: pair.pc,
        mo: pair.mo,
        score: pair.score,
        tier: matchTier,
        scope: scope,
        evidence: pair.evidence,
      });
      stats.matchedNodeCount += 1;
      if (matchTier === "confirmed") {
        stats.confirmedMatchCount += 1;
      } else if (matchTier === "candidate") {
        stats.candidateMatchCount += 1;
      }
      totalConfidence += pair.score;
      stats.averageConfidence = roundConfidence(totalConfidence / Math.max(1, stats.matchedNodeCount));
      if (pair.score < LOW_CONFIDENCE_RULE_THRESHOLD) {
        stats.lowConfidenceMatchCount += 1;
      }
      if (scope === "hierarchical") {
        stats.hierarchicalMatchCount += 1;
      } else {
        stats.fallbackMatchCount += 1;
      }
      return true;
    }

    function shouldRetainMatchPair(pair, scope) {
      if (!pair) {
        return false;
      }

      const weakRolePair =
        isWeakTextRoleDescriptor(pair.pc) ||
        isWeakTextRoleDescriptor(pair.mo) ||
        isDecorativeDescriptor(pair.pc) ||
        isDecorativeDescriptor(pair.mo);

      if (pair.score >= CANDIDATE_MATCH_THRESHOLD) {
        return weakRolePair
          ? pair.evidence.parentContext >= 0.76 || pair.evidence.component >= 0.9 || pair.evidence.textFingerprint >= 0.92
          : true;
      }

      if (hasStrongIdentitySignal(pair.pc, pair.mo)) {
        return weakRolePair ? pair.evidence.parentContext >= 0.8 || pair.evidence.component >= 0.9 : true;
      }

      const strongEvidence =
        pair.evidence.component >= 0.9 ||
        pair.evidence.textFingerprint >= 0.9 ||
        pair.evidence.variableFingerprint >= 0.88;
      if (strongEvidence) {
        return true;
      }

      if (
        scope === "hierarchical" &&
        pair.pc.hasChildren &&
        pair.mo.hasChildren &&
        pair.evidence.childContent >= 0.86 &&
        pair.evidence.parentContext >= 0.8 &&
        pair.evidence.layout >= 0.76
      ) {
        return true;
      }

      return false;
    }

    function shouldRecurseIntoPair(pair) {
      if (!pair || !pair.pc || !pair.mo || !pair.pc.hasChildren || !pair.mo.hasChildren) {
        return false;
      }

      if (pair.score >= CONFIRMED_MATCH_THRESHOLD) {
        return true;
      }

      if (hasStrongIdentitySignal(pair.pc, pair.mo)) {
        return true;
      }

      const strongContainerContext =
        pair.evidence.childContent >= 0.9 &&
        pair.evidence.parentContext >= 0.82 &&
        pair.evidence.layout >= 0.78;
      if (strongContainerContext) {
        return true;
      }

      return false;
    }
  }

  async function collectTreeDescriptors(rootNode, rootBounds) {
    const descriptors = [];

    await walk(rootNode, null, 0, 0, 1);

    return {
      descriptors: descriptors,
    };

    async function walk(node, parentDescriptor, depth, index, siblingCount) {
      const descriptor = await buildNodeDescriptor(node, parentDescriptor, rootBounds, depth, index, siblingCount);
      descriptors.push(descriptor);

      if (!descriptor.hasChildren) {
        return descriptor;
      }

      const childDescriptors = [];
      const children = Array.isArray(node.children) ? node.children : [];
      for (let childIndex = 0; childIndex < children.length; childIndex += 1) {
        const childDescriptor = await walk(children[childIndex], descriptor, depth + 1, childIndex, children.length || 1);
        if (childDescriptor) {
          childDescriptors.push(childDescriptor);
        }
      }

      finalizeContainerDescriptor(descriptor, childDescriptors);
      return descriptor;
    }
  }

  async function buildNodeDescriptor(node, parentDescriptor, rootBounds, depth, orderIndex, siblingCount) {
    const bounds = getNodeBounds(node);
    const name = safeName(node);
    const canonicalName = canonicalizeName(name);
    const componentInfo = await getComponentInfo(node);
    const textFingerprint = getTextFingerprint(node);
    const variableFingerprint = getVariableFingerprint(node);
    const layoutInfo = getLayoutInfo(node);
    const layoutSignature = buildLayoutSignature(layoutInfo);
    const visible = !node || node.visible !== false;
    const childCount = hasChildren(node) ? node.children.length : 0;
    const semanticSegment = componentInfo.componentFamily || canonicalName || String(node.type || "unknown").toLowerCase();
    const path = parentDescriptor ? `${parentDescriptor.path}/${name}` : name;
    const semanticPath = parentDescriptor ? `${parentDescriptor.semanticPath}/${semanticSegment}` : semanticSegment;
    const topLevelId = !parentDescriptor ? node.id : depth === 1 ? node.id : parentDescriptor.topLevelId;
    const topLevelSemanticSegment = !parentDescriptor
      ? semanticSegment
      : depth === 1
      ? semanticSegment
      : parentDescriptor.topLevelSemanticSegment;
    const topLevelSemanticPath = !parentDescriptor
      ? semanticPath
      : depth === 1
      ? semanticPath
      : parentDescriptor.topLevelSemanticPath;

    return {
      node: node,
      id: node.id,
      parentId: parentDescriptor ? parentDescriptor.id : "",
      name: name,
      canonicalName: canonicalName,
      isAutoNamed: isAutoName(name),
      type: String(node.type || "UNKNOWN"),
      depth: depth,
      orderIndex: orderIndex,
      siblingCount: siblingCount,
      hasChildren: childCount > 0,
      childCount: childCount,
      visible: visible,
      path: path,
      semanticSegment: semanticSegment,
      semanticPath: semanticPath,
      topLevelId: topLevelId,
      topLevelSemanticSegment: topLevelSemanticSegment,
      topLevelSemanticPath: topLevelSemanticPath,
      parentSemanticPath: parentDescriptor ? parentDescriptor.semanticPath : "",
      parentSemanticSegment: parentDescriptor ? parentDescriptor.semanticSegment : "",
      parentType: parentDescriptor ? parentDescriptor.type : "",
      textFingerprint: textFingerprint,
      variableFingerprint: variableFingerprint,
      componentInfo: componentInfo,
      layoutInfo: layoutInfo,
      layoutSignature: layoutSignature,
      childSemanticSignature: "",
      meaningfulChildCount: 0,
      bounds: bounds,
      relativeBounds: buildRelativeBounds(bounds, parentDescriptor ? parentDescriptor.bounds : rootBounds),
    };
  }

  function finalizeContainerDescriptor(descriptor, childDescriptors) {
    const children = Array.isArray(childDescriptors) ? childDescriptors : [];
    descriptor.meaningfulChildCount = countMeaningfulChildDescriptors(children);
    descriptor.childSemanticSignature = buildChildSemanticSignature(children);
  }

  function buildRelativeBounds(bounds, contextBounds) {
    if (!bounds || !contextBounds || !contextBounds.width || !contextBounds.height) {
      return null;
    }

    const localX = bounds.x - contextBounds.x;
    const localY = bounds.y - contextBounds.y;
    const contextWidth = Math.max(1, contextBounds.width);
    const contextHeight = Math.max(1, contextBounds.height);
    const leftGap = localX;
    const rightGap = contextWidth - (localX + bounds.width);
    const topGap = localY;
    const bottomGap = contextHeight - (localY + bounds.height);
    const horizontalCenterDelta = Math.abs(localX + bounds.width / 2 - contextWidth / 2);
    const verticalCenterDelta = Math.abs(localY + bounds.height / 2 - contextHeight / 2);

    let horizontalAnchor = "left";
    if (horizontalCenterDelta <= leftGap && horizontalCenterDelta <= rightGap) {
      horizontalAnchor = "center";
    } else if (rightGap < leftGap) {
      horizontalAnchor = "right";
    }

    let verticalAnchor = "top";
    if (verticalCenterDelta <= topGap && verticalCenterDelta <= bottomGap) {
      verticalAnchor = "middle";
    } else if (bottomGap < topGap) {
      verticalAnchor = "bottom";
    }

    return {
      xRatio: roundRatio(localX / contextWidth),
      yRatio: roundRatio(localY / contextHeight),
      widthRatio: roundRatio(bounds.width / contextWidth),
      heightRatio: roundRatio(bounds.height / contextHeight),
      horizontalAnchor: horizontalAnchor,
      verticalAnchor: verticalAnchor,
    };
  }

  function getTextFingerprint(node) {
    if (!node || typeof node.characters !== "string") {
      return "";
    }

    const normalized = node.characters
      .replace(/\s+/g, " ")
      .trim()
      .toLowerCase()
      .replace(/[0-9]+/g, "#");

    if (!normalized) {
      return "";
    }

    return normalized.length > 80 ? normalized.slice(0, 80) : normalized;
  }

  function getVariableFingerprint(node) {
    const values = [];

    if (node && node.boundVariables && typeof node.boundVariables === "object") {
      collectFingerprintValues(node.boundVariables, values, "bound", 0);
    }

    if (node && node.inferredVariables && typeof node.inferredVariables === "object") {
      collectFingerprintValues(node.inferredVariables, values, "inferred", 0);
    }

    values.sort();
    return values.join("|");
  }

  function collectFingerprintValues(source, values, prefix, depth) {
    if (!source || depth > 3 || values.length >= 24) {
      return;
    }

    if (Array.isArray(source)) {
      for (let index = 0; index < source.length; index += 1) {
        collectFingerprintValues(source[index], values, prefix, depth + 1);
      }
      return;
    }

    const sourceType = typeof source;
    if (sourceType === "string" || sourceType === "number" || sourceType === "boolean") {
      values.push(`${prefix}:${String(source).trim().toLowerCase()}`);
      return;
    }

    if (sourceType !== "object") {
      return;
    }

    const keys = Object.keys(source).sort();
    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      const value = source[key];
      const nextPrefix = prefix ? `${prefix}.${String(key).trim().toLowerCase()}` : String(key).trim().toLowerCase();
      if (key === "id" || key === "name" || key === "type" || key === "resolvedType") {
        if (typeof value === "string" && value.trim()) {
          values.push(`${nextPrefix}:${value.trim().toLowerCase()}`);
        }
        continue;
      }
      if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
        values.push(`${nextPrefix}:${String(value).trim().toLowerCase()}`);
        continue;
      }
      collectFingerprintValues(value, values, nextPrefix, depth + 1);
    }
  }

  async function getComponentInfo(node) {
    const info = {
      componentId: "",
      componentKey: "",
      componentName: "",
      componentFamily: "",
      propertySignature: "",
      referenceSignature: "",
    };

    let componentNode = null;
    const nodeType = String((node && node.type) || "");
    if (nodeType === "INSTANCE" && node && typeof node.getMainComponentAsync === "function") {
      componentNode = await getMainComponentSafeAsync(node);
    } else if (nodeType === "COMPONENT" || nodeType === "COMPONENT_SET") {
      componentNode = node;
    }

    if (componentNode) {
      info.componentId = typeof componentNode.id === "string" ? componentNode.id : "";
      info.componentKey = safeReadStringProperty(componentNode, "key");
      info.componentName = safeName(componentNode);
      info.componentFamily = canonicalizeName(info.componentName);
    }

    const properties = [];
    const componentProperties = safeReadObjectProperty(node, "componentProperties");
    if (componentProperties) {
      const keys = Object.keys(componentProperties).sort();
      for (let index = 0; index < keys.length; index += 1) {
        const key = keys[index];
        const item = componentProperties[key];
        let value = "";
        if (item && typeof item === "object" && typeof item.value !== "undefined") {
          value = String(item.value);
        } else if (typeof item !== "undefined") {
          value = String(item);
        }
        if (value) {
          properties.push(`${stripComponentPropertyKey(key)}:${value.trim().toLowerCase()}`);
        }
      }
    }

    const variantProperties = safeReadObjectProperty(node, "variantProperties");
    if (variantProperties) {
      const keys = Object.keys(variantProperties).sort();
      for (let index = 0; index < keys.length; index += 1) {
        const key = keys[index];
        const value = variantProperties[key];
        if (typeof value === "undefined" || value === null) {
          continue;
        }
        properties.push(`${stripComponentPropertyKey(key)}:${String(value).trim().toLowerCase()}`);
      }
    }

    info.referenceSignature = getComponentReferenceSignature(node);
    info.propertySignature = properties.sort().join("|");
    return info;
  }

  async function getMainComponentSafeAsync(node) {
    try {
      const componentNode = await node.getMainComponentAsync();
      return componentNode || null;
    } catch (error) {
      return null;
    }
  }

  function getComponentReferenceSignature(node) {
    const references = safeReadObjectProperty(node, "componentPropertyReferences");
    if (!references) {
      return "";
    }

    const values = [];
    const keys = Object.keys(references).sort();
    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      const value = references[key];
      if (typeof value === "string" && value.trim()) {
        values.push(`${stripComponentPropertyKey(key)}:${value.trim().toLowerCase()}`);
      }
    }

    return values.join("|");
  }

  function safeReadObjectProperty(target, key) {
    try {
      if (!target || typeof target !== "object") {
        return null;
      }

      const value = target[key];
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  function safeReadStringProperty(target, key) {
    try {
      if (!target || typeof target !== "object") {
        return "";
      }

      const value = target[key];
      return typeof value === "string" ? value : "";
    } catch (error) {
      return "";
    }
  }

  function stripComponentPropertyKey(value) {
    const text = typeof value === "string" ? value.trim() : "";
    if (!text) {
      return "";
    }

    const hashIndex = text.indexOf("#");
    const base = hashIndex >= 0 ? text.slice(0, hashIndex) : text;
    return base.trim().toLowerCase();
  }

  function getLayoutInfo(node) {
    const info = {
      mode: "NONE",
      source: "none",
      gap: 0,
      paddingTop: 0,
      paddingRight: 0,
      paddingBottom: 0,
      paddingLeft: 0,
      paddingX: 0,
      paddingY: 0,
      wrap: "",
      sizingHorizontal: "",
      sizingVertical: "",
      grow: 0,
      align: "",
      positioning: "",
    };

    const inferred = node && node.inferredAutoLayout && typeof node.inferredAutoLayout === "object" ? node.inferredAutoLayout : null;
    const layoutMode = typeof node.layoutMode === "string" ? node.layoutMode : "NONE";
    const inferredMode = inferred && typeof inferred.layoutMode === "string" ? inferred.layoutMode : "NONE";

    if (layoutMode && layoutMode !== "NONE") {
      info.mode = layoutMode;
      info.source = "auto-layout";
    } else if (inferredMode && inferredMode !== "NONE") {
      info.mode = inferredMode;
      info.source = "inferred";
    }

    const source = info.source === "inferred" ? inferred : node;
    if (source) {
      info.gap = roundPixel(typeof source.itemSpacing === "number" ? source.itemSpacing : 0);
      info.paddingTop = roundPixel(typeof source.paddingTop === "number" ? source.paddingTop : 0);
      info.paddingRight = roundPixel(typeof source.paddingRight === "number" ? source.paddingRight : 0);
      info.paddingBottom = roundPixel(typeof source.paddingBottom === "number" ? source.paddingBottom : 0);
      info.paddingLeft = roundPixel(typeof source.paddingLeft === "number" ? source.paddingLeft : 0);
      info.paddingX = roundPixel(info.paddingLeft + info.paddingRight);
      info.paddingY = roundPixel(info.paddingTop + info.paddingBottom);
      info.wrap = typeof source.layoutWrap === "string" ? source.layoutWrap : "";
    }

    info.sizingHorizontal = typeof node.layoutSizingHorizontal === "string" ? node.layoutSizingHorizontal : "";
    info.sizingVertical = typeof node.layoutSizingVertical === "string" ? node.layoutSizingVertical : "";
    info.grow = roundPixel(typeof node.layoutGrow === "number" ? node.layoutGrow : 0);
    info.align = typeof node.layoutAlign === "string" ? node.layoutAlign : "";
    info.positioning = typeof node.layoutPositioning === "string" ? node.layoutPositioning : "";

    return info;
  }

  function buildLayoutSignature(layoutInfo) {
    if (!layoutInfo) {
      return "";
    }

    return [
      String(layoutInfo.mode || "NONE"),
      String(layoutInfo.wrap || ""),
      bucketPixel(layoutInfo.gap),
      bucketPixel(layoutInfo.paddingX),
      bucketPixel(layoutInfo.paddingY),
      String(layoutInfo.sizingHorizontal || ""),
      String(layoutInfo.sizingVertical || ""),
      layoutInfo.grow ? "grow" : "",
      String(layoutInfo.align || ""),
      String(layoutInfo.positioning || ""),
    ]
      .filter(Boolean)
      .join("|");
  }

  function countMeaningfulChildDescriptors(descriptors) {
    let count = 0;

    for (let index = 0; index < descriptors.length; index += 1) {
      if (isMeaningfulDescriptorSignal(descriptors[index])) {
        count += 1;
      }
    }

    return count;
  }

  function buildChildSemanticSignature(descriptors) {
    const parts = [];

    for (let index = 0; index < descriptors.length; index += 1) {
      const descriptor = descriptors[index];
      const token = getDescriptorIdentityToken(descriptor);
      if (!token) {
        continue;
      }
      parts.push(token);
      if (parts.length >= 6) {
        break;
      }
    }

    return parts.join(">");
  }

  function isMeaningfulDescriptorSignal(descriptor) {
    if (!descriptor) {
      return false;
    }

    if (descriptor.componentInfo.componentKey || descriptor.textFingerprint || descriptor.variableFingerprint) {
      return true;
    }

    if (descriptor.canonicalName && !isGenericNamedDescriptor(descriptor)) {
      return true;
    }

    return !!(descriptor.hasChildren && descriptor.childCount > 0);
  }

  function getDescriptorIdentityToken(descriptor) {
    if (!descriptor) {
      return "";
    }

    if (descriptor.componentInfo.componentFamily) {
      return `cmp:${descriptor.componentInfo.componentFamily}`;
    }

    if (descriptor.textFingerprint) {
      return `txt:${descriptor.textFingerprint.split(" ").slice(0, 3).join("_")}`;
    }

    if (descriptor.variableFingerprint) {
      return `var:${descriptor.variableFingerprint.split("|").slice(0, 2).join("+")}`;
    }

    if (descriptor.canonicalName && !isGenericNamedDescriptor(descriptor)) {
      return `name:${descriptor.canonicalName}`;
    }

    if (descriptor.hasChildren && descriptor.childCount > 0 && descriptor.semanticSegment) {
      return `node:${descriptor.semanticSegment}`;
    }

    return "";
  }

  function groupDescriptorsByParent(descriptors) {
    const groups = new Map();

    for (let index = 0; index < descriptors.length; index += 1) {
      const descriptor = descriptors[index];
      const parentId = descriptor.parentId || "__root__";
      if (!groups.has(parentId)) {
        groups.set(parentId, []);
      }
      groups.get(parentId).push(descriptor);
    }

    return groups;
  }

  function getUnmatchedChildren(groupMap, parentId, matchedIds) {
    const list = groupMap.get(parentId) || [];
    const result = [];

    for (let index = 0; index < list.length; index += 1) {
      if (!matchedIds.has(list[index].id)) {
        result.push(list[index]);
      }
    }

    return result;
  }

  function getUnmatchedDescriptors(descriptors, matchedIds) {
    const result = [];

    for (let index = 0; index < descriptors.length; index += 1) {
      if (!matchedIds.has(descriptors[index].id)) {
        result.push(descriptors[index]);
      }
    }

    return result;
  }

  function countUnmatched(descriptors, matchedIds) {
    let count = 0;

    for (let index = 0; index < descriptors.length; index += 1) {
      if (!matchedIds.has(descriptors[index].id)) {
        count += 1;
      }
    }

    return count;
  }

  function buildGreedyMatches(pcDescriptors, moDescriptors, threshold, hierarchical) {
    const candidates = [];
    const maxCandidateCount = hierarchical ? 3200 : 6400;

    for (let pcIndex = 0; pcIndex < pcDescriptors.length; pcIndex += 1) {
      const pcDescriptor = pcDescriptors[pcIndex];
      if (shouldSkipDescriptorForMatching(pcDescriptor, hierarchical)) {
        continue;
      }
      for (let moIndex = 0; moIndex < moDescriptors.length; moIndex += 1) {
        const moDescriptor = moDescriptors[moIndex];
        if (shouldSkipDescriptorForMatching(moDescriptor, hierarchical)) {
          continue;
        }
        if (!hierarchical && !shouldAllowFallbackPair(pcDescriptor, moDescriptor)) {
          continue;
        }
        if (!areNodeTypesCompatible(pcDescriptor, moDescriptor)) {
          continue;
        }

        const candidate = scoreNodePair(pcDescriptor, moDescriptor, hierarchical);
        if (!candidate || candidate.score < threshold) {
          continue;
        }
        if (shouldRejectWeakCandidate(pcDescriptor, moDescriptor, candidate, hierarchical)) {
          continue;
        }

        candidates.push(candidate);
        if (candidates.length >= maxCandidateCount) {
          break;
        }
      }
      if (candidates.length >= maxCandidateCount) {
        break;
      }
    }

    candidates.sort((left, right) => {
      if (right.score !== left.score) {
        return right.score - left.score;
      }
      if (left.pc.depth !== right.pc.depth) {
        return left.pc.depth - right.pc.depth;
      }
      return left.pc.orderIndex - right.pc.orderIndex;
    });

    const usedPc = new Set();
    const usedMo = new Set();
    const matches = [];

    for (let index = 0; index < candidates.length; index += 1) {
      const candidate = candidates[index];
      if (usedPc.has(candidate.pc.id) || usedMo.has(candidate.mo.id)) {
        continue;
      }

      usedPc.add(candidate.pc.id);
      usedMo.add(candidate.mo.id);
      matches.push(candidate);
    }

    return matches;
  }

  function shouldSkipDescriptorForMatching(descriptor, hierarchical) {
    if (!descriptor) {
      return true;
    }

    if (
      hierarchical &&
      descriptor.depth >= 2 &&
      !descriptor.hasChildren &&
      !descriptor.textFingerprint &&
      !descriptor.componentInfo.componentKey &&
      !descriptor.variableFingerprint &&
      (DECORATIVE_LEAF_TYPES[descriptor.type] || isGenericNamedDescriptor(descriptor) || isWeakTextRoleDescriptor(descriptor))
    ) {
      return true;
    }

    if (hierarchical) {
      return false;
    }

    if (descriptor.hasChildren || descriptor.textFingerprint || descriptor.componentInfo.componentKey || descriptor.variableFingerprint) {
      return false;
    }

    if (descriptor.layoutInfo && descriptor.layoutInfo.mode && descriptor.layoutInfo.mode !== "NONE") {
      return false;
    }

    if (DECORATIVE_LEAF_TYPES[descriptor.type]) {
      return true;
    }

    return isGenericNamedDescriptor(descriptor);
  }

  function shouldRejectWeakCandidate(pcDescriptor, moDescriptor, candidate, hierarchical) {
    if (!candidate) {
      return true;
    }

    const weakTextPair =
      isWeakTextRoleDescriptor(pcDescriptor) &&
      isWeakTextRoleDescriptor(moDescriptor) &&
      !hasStrongIdentitySignal(pcDescriptor, moDescriptor) &&
      candidate.evidence.textFingerprint < 0.78 &&
      candidate.score < (hierarchical ? 0.9 : 0.93);
    if (weakTextPair) {
      return true;
    }

    if (hierarchical) {
      const weakLeafPair =
        !pcDescriptor.hasChildren &&
        !moDescriptor.hasChildren &&
        !hasStrongIdentitySignal(pcDescriptor, moDescriptor) &&
        isDecorativeDescriptor(pcDescriptor) &&
        isDecorativeDescriptor(moDescriptor) &&
        candidate.score < 0.92;
      if (weakLeafPair) {
        return true;
      }

      const weakGenericGroupPair =
        isGenericContainerDescriptor(pcDescriptor) &&
        isGenericContainerDescriptor(moDescriptor) &&
        !hasStrongIdentitySignal(pcDescriptor, moDescriptor) &&
        candidate.evidence.childContent < 0.78 &&
        candidate.evidence.parentContext < 0.72 &&
        candidate.score < 0.92;
      if (weakGenericGroupPair) {
        return true;
      }
    }

    return false;
  }

  function isDecorativeDescriptor(descriptor) {
    if (!descriptor) {
      return false;
    }

    if (DECORATIVE_LEAF_TYPES[descriptor.type]) {
      return true;
    }

    return isGenericNamedDescriptor(descriptor);
  }

  function isGenericContainerDescriptor(descriptor) {
    if (!descriptor) {
      return false;
    }

    const type = String(descriptor.type || "");
    if (!(type === "GROUP" || type === "FRAME" || type === "SECTION")) {
      return false;
    }

    return isGenericNamedDescriptor(descriptor);
  }

  function isWeakTextRoleDescriptor(descriptor) {
    if (!descriptor || descriptor.type !== "TEXT") {
      return false;
    }

    const normalizedName = String(descriptor.canonicalName || descriptor.name || "").trim();
    if (!normalizedName) {
      return true;
    }

    for (let index = 0; index < WEAK_TEXT_ROLE_PATTERNS.length; index += 1) {
      if (WEAK_TEXT_ROLE_PATTERNS[index].test(normalizedName)) {
        return true;
      }
    }

    return !!descriptor.isAutoNamed;
  }

  function isGenericNamedDescriptor(descriptor) {
    const normalizedName = String((descriptor && descriptor.name) || "").trim();
    if (!normalizedName) {
      return true;
    }

    for (let index = 0; index < DECORATIVE_NAME_PATTERNS.length; index += 1) {
      if (DECORATIVE_NAME_PATTERNS[index].test(normalizedName)) {
        return true;
      }
    }

    return !!(descriptor && descriptor.isAutoNamed);
  }

  function shouldAllowFallbackPair(pcDescriptor, moDescriptor) {
    if (!pcDescriptor || !moDescriptor) {
      return false;
    }

    if (hasStrongIdentitySignal(pcDescriptor, moDescriptor)) {
      return true;
    }

    const sameTopLevelSegment =
      !!pcDescriptor.topLevelSemanticSegment &&
      !!moDescriptor.topLevelSemanticSegment &&
      pcDescriptor.topLevelSemanticSegment === moDescriptor.topLevelSemanticSegment;
    const sameParentSegment =
      !!pcDescriptor.parentSemanticSegment &&
      !!moDescriptor.parentSemanticSegment &&
      pcDescriptor.parentSemanticSegment === moDescriptor.parentSemanticSegment;
    const sameTopLevelPathFamily =
      !!pcDescriptor.topLevelSemanticPath &&
      !!moDescriptor.topLevelSemanticPath &&
      computePathSimilarity(pcDescriptor.topLevelSemanticPath, moDescriptor.topLevelSemanticPath) >= 0.7;
    const compatibleLayout =
      pcDescriptor.layoutInfo.mode === moDescriptor.layoutInfo.mode ||
      pcDescriptor.layoutSignature === moDescriptor.layoutSignature ||
      (pcDescriptor.layoutInfo.mode === "NONE" && moDescriptor.layoutInfo.mode === "NONE");
    const sameNamedRole =
      !!pcDescriptor.canonicalName &&
      !!moDescriptor.canonicalName &&
      (pcDescriptor.canonicalName === moDescriptor.canonicalName ||
        computePathSimilarity(pcDescriptor.canonicalName, moDescriptor.canonicalName) >= 0.72);

    const weakRolePair =
      isGenericContainerDescriptor(pcDescriptor) ||
      isGenericContainerDescriptor(moDescriptor) ||
      isWeakTextRoleDescriptor(pcDescriptor) ||
      isWeakTextRoleDescriptor(moDescriptor);

    if ((sameTopLevelSegment || sameParentSegment || sameTopLevelPathFamily) && compatibleLayout) {
      return weakRolePair ? sameParentSegment : true;
    }

    if (sameNamedRole && (sameTopLevelSegment || sameTopLevelPathFamily)) {
      return weakRolePair ? sameParentSegment && compatibleLayout : true;
    }

    return false;
  }

  function hasStrongIdentitySignal(pcDescriptor, moDescriptor) {
    if (
      pcDescriptor.componentInfo.componentKey &&
      moDescriptor.componentInfo.componentKey &&
      pcDescriptor.componentInfo.componentKey === moDescriptor.componentInfo.componentKey
    ) {
      return true;
    }

    if (
      pcDescriptor.textFingerprint &&
      moDescriptor.textFingerprint &&
      pcDescriptor.textFingerprint === moDescriptor.textFingerprint
    ) {
      return true;
    }

    if (
      pcDescriptor.variableFingerprint &&
      moDescriptor.variableFingerprint &&
      pcDescriptor.variableFingerprint === moDescriptor.variableFingerprint
    ) {
      return true;
    }

    if (
      pcDescriptor.canonicalName &&
      moDescriptor.canonicalName &&
      pcDescriptor.canonicalName === moDescriptor.canonicalName &&
      !pcDescriptor.isAutoNamed &&
      !moDescriptor.isAutoNamed
    ) {
      return true;
    }

    return false;
  }

  function scoreNodePair(pcDescriptor, moDescriptor, hierarchical) {
    const evidence = {
      type: 0,
      component: 0,
      textFingerprint: 0,
      variableFingerprint: 0,
      nameAlias: 0,
      structure: 0,
      parentContext: 0,
      layout: 0,
      childContent: 0,
      path: 0,
      geometry: 0,
    };
    let totalWeight = 0;
    let totalScore = 0;

    addWeighted("type", 1.2, pcDescriptor.type === moDescriptor.type ? 1 : CONTAINER_TYPES[pcDescriptor.type] && CONTAINER_TYPES[moDescriptor.type] ? 0.74 : 0.55);

    const componentScore = getComponentScore(pcDescriptor.componentInfo, moDescriptor.componentInfo);
    if (componentScore >= 0) {
      addWeighted("component", 2.3, componentScore);
    }

    const textScore = getTextScore(pcDescriptor.textFingerprint, moDescriptor.textFingerprint);
    if (textScore >= 0) {
      addWeighted("textFingerprint", 2.1, textScore);
    }

    const variableScore = getVariableScore(pcDescriptor.variableFingerprint, moDescriptor.variableFingerprint);
    if (variableScore >= 0) {
      addWeighted("variableFingerprint", 1.35, variableScore);
    }

    const nameScore = getNameScore(pcDescriptor, moDescriptor);
    if (nameScore >= 0) {
      addWeighted("nameAlias", 1.0, nameScore);
    }

    addWeighted("structure", 1.15, computeStructureScore(pcDescriptor, moDescriptor));
    addWeighted("parentContext", hierarchical ? 0.95 : 1.2, computeParentContextScore(pcDescriptor, moDescriptor));
    addWeighted("layout", 1.05, computeLayoutScore(pcDescriptor, moDescriptor));
    addWeighted("childContent", hierarchical ? 1.45 : 1.65, computeChildContentScore(pcDescriptor, moDescriptor));
    addWeighted("path", hierarchical ? 1.2 : 1.0, computePathSimilarity(pcDescriptor.semanticPath, moDescriptor.semanticPath));
    addWeighted("geometry", hierarchical ? 0.95 : 0.8, computeGeometryScore(pcDescriptor, moDescriptor));

    if (!totalWeight) {
      return null;
    }

    const score = clampScore(totalScore / totalWeight);
    return {
      pc: pcDescriptor,
      mo: moDescriptor,
      score: roundConfidence(score),
      evidence: evidence,
    };

    function addWeighted(key, weight, value) {
      if (typeof value !== "number" || value < 0) {
        return;
      }
      evidence[key] = roundConfidence(value);
      totalWeight += weight;
      totalScore += weight * clampScore(value);
    }
  }

  function getComponentScore(pcComponent, moComponent) {
    if (!pcComponent || !moComponent) {
      return -1;
    }

    if (pcComponent.componentKey && pcComponent.componentKey === moComponent.componentKey) {
      if (
        pcComponent.propertySignature &&
        pcComponent.propertySignature === moComponent.propertySignature &&
        pcComponent.referenceSignature === moComponent.referenceSignature
      ) {
        return 1;
      }
      return 0.94;
    }

    if (pcComponent.componentFamily && pcComponent.componentFamily === moComponent.componentFamily) {
      if (
        pcComponent.propertySignature &&
        pcComponent.propertySignature === moComponent.propertySignature &&
        pcComponent.referenceSignature === moComponent.referenceSignature
      ) {
        return 0.9;
      }
      return 0.82;
    }

    if (
      pcComponent.propertySignature &&
      moComponent.propertySignature &&
      pcComponent.propertySignature === moComponent.propertySignature
    ) {
      return 0.72;
    }

    if (!pcComponent.componentKey && !moComponent.componentKey && !pcComponent.propertySignature && !moComponent.propertySignature) {
      return -1;
    }

    return 0.2;
  }

  function getVariableScore(pcFingerprint, moFingerprint) {
    if (!pcFingerprint && !moFingerprint) {
      return -1;
    }
    if (!pcFingerprint || !moFingerprint) {
      return 0.08;
    }
    if (pcFingerprint === moFingerprint) {
      return 1;
    }

    const pcTokens = pcFingerprint.split("|").filter(Boolean);
    const moTokens = moFingerprint.split("|").filter(Boolean);
    if (!pcTokens.length || !moTokens.length) {
      return 0.2;
    }

    let shared = 0;
    for (let index = 0; index < pcTokens.length; index += 1) {
      if (moTokens.indexOf(pcTokens[index]) >= 0) {
        shared += 1;
      }
    }

    return clampScore(shared / Math.max(pcTokens.length, moTokens.length));
  }

  function getTextScore(pcText, moText) {
    if (!pcText && !moText) {
      return -1;
    }
    if (!pcText || !moText) {
      return 0.12;
    }
    if (pcText === moText) {
      return 1;
    }
    if (pcText.length >= 4 && moText.length >= 4 && (pcText.indexOf(moText) >= 0 || moText.indexOf(pcText) >= 0)) {
      return 0.82;
    }

    const pcTokens = pcText.split(" ").filter(Boolean);
    const moTokens = moText.split(" ").filter(Boolean);
    if (!pcTokens.length || !moTokens.length) {
      return 0.3;
    }

    let shared = 0;
    for (let pcIndex = 0; pcIndex < pcTokens.length; pcIndex += 1) {
      if (moTokens.indexOf(pcTokens[pcIndex]) >= 0) {
        shared += 1;
      }
    }

    return clampScore(shared / Math.max(pcTokens.length, moTokens.length));
  }

  function getNameScore(pcDescriptor, moDescriptor) {
    if (pcDescriptor.isAutoNamed && moDescriptor.isAutoNamed) {
      return -1;
    }

    const pcName = pcDescriptor.canonicalName;
    const moName = moDescriptor.canonicalName;
    if (!pcName && !moName) {
      return -1;
    }
    if (!pcName || !moName) {
      return 0.1;
    }
    if (pcName === moName) {
      return 1;
    }
    if (pcName.indexOf(moName) >= 0 || moName.indexOf(pcName) >= 0) {
      return 0.78;
    }

    const pcTokens = pcName.split(" ").filter(Boolean);
    const moTokens = moName.split(" ").filter(Boolean);
    let shared = 0;
    for (let index = 0; index < pcTokens.length; index += 1) {
      if (moTokens.indexOf(pcTokens[index]) >= 0) {
        shared += 1;
      }
    }

    if (!shared) {
      return 0;
    }

    return clampScore(shared / Math.max(pcTokens.length, moTokens.length));
  }

  function computeStructureScore(pcDescriptor, moDescriptor) {
    const scores = [];
    const childMax = Math.max(1, pcDescriptor.childCount, moDescriptor.childCount);
    const childDiff = Math.abs(pcDescriptor.childCount - moDescriptor.childCount);
    scores.push(clampScore(1 - childDiff / childMax));

    const siblingMax = Math.max(1, pcDescriptor.siblingCount - 1, moDescriptor.siblingCount - 1);
    const orderDiff = Math.abs(pcDescriptor.orderIndex - moDescriptor.orderIndex);
    scores.push(clampScore(1 - orderDiff / siblingMax));

    const depthMax = Math.max(1, pcDescriptor.depth, moDescriptor.depth);
    const depthDiff = Math.abs(pcDescriptor.depth - moDescriptor.depth);
    scores.push(clampScore(1 - depthDiff / depthMax));

    scores.push(pcDescriptor.hasChildren === moDescriptor.hasChildren ? 1 : 0.45);
    scores.push(pcDescriptor.visible === moDescriptor.visible ? 1 : 0.55);

    return averageScore(scores);
  }

  function computeParentContextScore(pcDescriptor, moDescriptor) {
    const scores = [];

    if (pcDescriptor.parentType || moDescriptor.parentType) {
      scores.push(pcDescriptor.parentType === moDescriptor.parentType ? 1 : 0.45);
    }

    if (pcDescriptor.parentSemanticSegment || moDescriptor.parentSemanticSegment) {
      scores.push(
        pcDescriptor.parentSemanticSegment && moDescriptor.parentSemanticSegment
          ? computePathSimilarity(pcDescriptor.parentSemanticSegment, moDescriptor.parentSemanticSegment)
          : 0.18
      );
    }

    if (pcDescriptor.parentSemanticPath || moDescriptor.parentSemanticPath) {
      scores.push(
        pcDescriptor.parentSemanticPath && moDescriptor.parentSemanticPath
          ? computePathSimilarity(pcDescriptor.parentSemanticPath, moDescriptor.parentSemanticPath)
          : 0.12
      );
    }

    if (!scores.length) {
      return 0.5;
    }

    return averageScore(scores);
  }

  function computeLayoutScore(pcDescriptor, moDescriptor) {
    const pcLayout = pcDescriptor.layoutInfo;
    const moLayout = moDescriptor.layoutInfo;
    if (!pcLayout && !moLayout) {
      return 0.5;
    }

    const scores = [];
    scores.push(pcLayout.mode === moLayout.mode ? 1 : pcLayout.mode === "NONE" || moLayout.mode === "NONE" ? 0.52 : 0.28);
    scores.push(pcLayout.wrap === moLayout.wrap ? 1 : 0.55);
    scores.push(clampScore(1 - Math.abs(pcLayout.gap - moLayout.gap) / Math.max(8, pcLayout.gap, moLayout.gap, 1)));
    scores.push(clampScore(1 - Math.abs(pcLayout.paddingX - moLayout.paddingX) / Math.max(12, pcLayout.paddingX, moLayout.paddingX, 1)));
    scores.push(clampScore(1 - Math.abs(pcLayout.paddingY - moLayout.paddingY) / Math.max(12, pcLayout.paddingY, moLayout.paddingY, 1)));
    scores.push(pcLayout.sizingHorizontal === moLayout.sizingHorizontal ? 1 : 0.65);
    scores.push(pcLayout.sizingVertical === moLayout.sizingVertical ? 1 : 0.65);
    scores.push(pcLayout.positioning === moLayout.positioning ? 1 : 0.4);
    scores.push(pcDescriptor.layoutSignature === moDescriptor.layoutSignature ? 1 : 0.6);

    return averageScore(scores);
  }

  function computeChildContentScore(pcDescriptor, moDescriptor) {
    if (!pcDescriptor.hasChildren && !moDescriptor.hasChildren) {
      return -1;
    }

    const scores = [];
    const pcSignature = String(pcDescriptor.childSemanticSignature || "");
    const moSignature = String(moDescriptor.childSemanticSignature || "");
    const pcMeaningful = Number.isFinite(pcDescriptor.meaningfulChildCount) ? pcDescriptor.meaningfulChildCount : 0;
    const moMeaningful = Number.isFinite(moDescriptor.meaningfulChildCount) ? moDescriptor.meaningfulChildCount : 0;

    if (pcSignature && moSignature) {
      if (pcSignature === moSignature) {
        scores.push(1);
      } else {
        scores.push(computePathSimilarity(pcSignature.replace(/>/g, "/"), moSignature.replace(/>/g, "/")));
      }
    } else if (pcMeaningful || moMeaningful) {
      scores.push(0.18);
    }

    const countMax = Math.max(1, pcMeaningful, moMeaningful);
    scores.push(clampScore(1 - Math.abs(pcMeaningful - moMeaningful) / countMax));

    return averageScore(scores);
  }

  function computePathSimilarity(pcPath, moPath) {
    const pcSegments = String(pcPath || "")
      .split("/")
      .filter(Boolean);
    const moSegments = String(moPath || "")
      .split("/")
      .filter(Boolean);

    if (!pcSegments.length || !moSegments.length) {
      return 0;
    }

    let suffixMatches = 0;
    let pcIndex = pcSegments.length - 1;
    let moIndex = moSegments.length - 1;
    while (pcIndex >= 0 && moIndex >= 0) {
      if (pcSegments[pcIndex] !== moSegments[moIndex]) {
        break;
      }
      suffixMatches += 1;
      pcIndex -= 1;
      moIndex -= 1;
    }

    const unique = new Set(pcSegments.concat(moSegments));
    let overlap = 0;
    unique.forEach((segment) => {
      if (pcSegments.indexOf(segment) >= 0 && moSegments.indexOf(segment) >= 0) {
        overlap += 1;
      }
    });

    const suffixScore = suffixMatches / Math.max(pcSegments.length, moSegments.length);
    const overlapScore = overlap / Math.max(1, unique.size);
    return clampScore(suffixScore * 0.65 + overlapScore * 0.35);
  }

  function computeGeometryScore(pcDescriptor, moDescriptor) {
    const pcBounds = pcDescriptor.relativeBounds;
    const moBounds = moDescriptor.relativeBounds;
    if (!pcBounds || !moBounds) {
      return 0.4;
    }

    const widthScore = clampScore(1 - Math.abs(pcBounds.widthRatio - moBounds.widthRatio) * 1.8);
    const heightScore = clampScore(1 - Math.abs(pcBounds.heightRatio - moBounds.heightRatio) * 1.8);
    const yScore = clampScore(1 - Math.abs(pcBounds.yRatio - moBounds.yRatio) * 2.6);
    const xScore = clampScore(1 - Math.abs(pcBounds.xRatio - moBounds.xRatio) * 1.6);
    const horizontalAnchorScore = pcBounds.horizontalAnchor === moBounds.horizontalAnchor ? 1 : 0.55;
    const verticalAnchorScore = pcBounds.verticalAnchor === moBounds.verticalAnchor ? 1 : 0.6;

    return averageScore([widthScore, heightScore, yScore, xScore, horizontalAnchorScore, verticalAnchorScore]);
  }

  function createMatchRecord(pairId, createdAt, pair, scope) {
    return {
      type: "match",
      id: createRecordId("match"),
      version: PATCH_VERSION,
      pairId: pairId,
      scope: scope,
      matchTier: getMatchTier(pair.score),
      fromNodeIds: [pair.pc.id],
      toNodeIds: [pair.mo.id],
      cardinality: "1:1",
      confidence: roundConfidence(pair.score),
      evidence: buildEvidencePayload(pair),
      createdAt: createdAt,
    };
  }

  function buildEvidencePayload(pair) {
    return {
      component: pair.evidence.component >= 0.75,
      textFingerprint: pair.evidence.textFingerprint >= 0.75,
      variableFingerprint: pair.evidence.variableFingerprint >= 0.68,
      structure: pair.evidence.structure >= 0.65,
      parentContext: pair.evidence.parentContext >= 0.7,
      layout: pair.evidence.layout >= 0.7,
      childContent: pair.evidence.childContent >= 0.75,
      nameAlias: pair.evidence.nameAlias >= 0.72,
      scores: {
        type: pair.evidence.type,
        component: pair.evidence.component,
        textFingerprint: pair.evidence.textFingerprint,
        variableFingerprint: pair.evidence.variableFingerprint,
        nameAlias: pair.evidence.nameAlias,
        structure: pair.evidence.structure,
        parentContext: pair.evidence.parentContext,
        layout: pair.evidence.layout,
        childContent: pair.evidence.childContent,
        path: pair.evidence.path,
        geometry: pair.evidence.geometry,
      },
      from: {
        path: pair.pc.path,
        semanticPath: pair.pc.semanticPath,
        parentSemanticPath: pair.pc.parentSemanticPath,
        type: pair.pc.type,
        variableFingerprint: pair.pc.variableFingerprint,
        layoutSignature: pair.pc.layoutSignature,
        childSemanticSignature: pair.pc.childSemanticSignature,
      },
      to: {
        path: pair.mo.path,
        semanticPath: pair.mo.semanticPath,
        parentSemanticPath: pair.mo.parentSemanticPath,
        type: pair.mo.type,
        variableFingerprint: pair.mo.variableFingerprint,
        layoutSignature: pair.mo.layoutSignature,
        childSemanticSignature: pair.mo.childSemanticSignature,
      },
    };
  }

  function buildSectionExampleRecords(classifiedPair, pairId, createdAt, matching) {
    const pairs = matching && Array.isArray(matching.pairs) ? matching.pairs.slice() : [];
    const records = [];
    const seen = new Set();

    pairs.sort((left, right) => right.score - left.score);

    for (let index = 0; index < pairs.length; index += 1) {
      const pair = pairs[index];
      if (!pair || pair.tier !== "confirmed") {
        continue;
      }
      if (!pair.pc || !pair.mo || pair.pc.depth !== 1 || pair.mo.depth !== 1) {
        continue;
      }
      if (!isSectionLikeDescriptor(pair.pc) && !isSectionLikeDescriptor(pair.mo)) {
        continue;
      }

      const sectionSignature = buildSectionSignature(pair.pc) || buildSectionSignature(pair.mo) || buildRuleNodeSignatureFromPair(pair);
      const dedupeKey = `${sectionSignature}|${pair.pc.id}|${pair.mo.id}`;
      if (seen.has(dedupeKey)) {
        continue;
      }

      seen.add(dedupeKey);
      records.push(createSectionExampleRecord(classifiedPair, pairId, createdAt, pair, sectionSignature));
      if (records.length >= 12) {
        break;
      }
    }

    return records;
  }

  function createSectionExampleRecord(classifiedPair, pairId, createdAt, pair, sectionSignature) {
    const pcStats = collectNodeStats(pair.pc.node);
    const moStats = collectNodeStats(pair.mo.node);
    const sectionType = classifySectionExampleType(pair, pcStats, moStats);
    const mobilePattern = classifyMobileSectionPattern(pair, sectionType, pcStats, moStats);
    const sectionLabel = summarizePairLabel(pair.pc, pair.mo);
    const heroGuidance = sectionType === "hero" ? buildHeroSectionGuidance(pair.pc.node, pair.mo.node) : null;
    const sectionGuidance = buildSectionGuidance(sectionType, pair, pcStats, moStats, mobilePattern, heroGuidance);

    return {
      type: "section-example",
      id: createRecordId("section"),
      version: PATCH_VERSION,
      pairId: pairId,
      direction: "pc-to-mo",
      confidence: roundConfidence(pair.score),
      sectionType: sectionType,
      mobilePattern: mobilePattern,
      sectionSignature: sectionSignature,
      summary: `${sectionLabel} · ${sectionType} -> ${mobilePattern}`,
      heroGuidance: heroGuidance,
      sectionGuidance: sectionGuidance,
      pcNodeId: pair.pc.id,
      moNodeId: pair.mo.id,
      pc: buildSectionSidePayload(classifiedPair.pc, pair.pc, pcStats),
      mo: buildSectionSidePayload(classifiedPair.mo, pair.mo, moStats),
      createdAt: createdAt,
    };
  }

  function buildSectionSidePayload(rootDescriptor, descriptor, stats) {
    const bounds = descriptor && descriptor.bounds ? descriptor.bounds : null;
    const layout = descriptor && descriptor.layoutInfo ? descriptor.layoutInfo : getLayoutInfo(descriptor && descriptor.node);
    const width = bounds ? roundPixel(bounds.width) : 0;
    const height = bounds ? roundPixel(bounds.height) : 0;
    return {
      id: descriptor.id,
      name: descriptor.name,
      semanticPath: descriptor.semanticPath,
      topLevelSemanticPath: descriptor.topLevelSemanticPath,
      layoutMode: layout ? layout.mode : "",
      gap: layout ? layout.gap : 0,
      paddingX: layout ? layout.paddingX : 0,
      paddingY: layout ? layout.paddingY : 0,
      columns: estimateColumnCount(descriptor && descriptor.node),
      width: width,
      height: height,
      aspectRatio: getFrameAspectRatio(width, height),
      widthRatio: descriptor.relativeBounds ? descriptor.relativeBounds.widthRatio : 0,
      heightRatio: descriptor.relativeBounds ? descriptor.relativeBounds.heightRatio : 0,
      orderIndex: descriptor.orderIndex,
      childCount: descriptor.childCount,
      meaningfulChildCount: descriptor.meaningfulChildCount,
      textFingerprint: descriptor.textFingerprint,
      childSemanticSignature: descriptor.childSemanticSignature,
      stats: {
        totalNodes: stats.totalNodes,
        textNodes: stats.textNodes,
        imageFillNodes: stats.imageFillNodes,
        autoLayoutNodes: stats.autoLayoutNodes,
        containerNodes: stats.containerNodes,
      },
      rootWidth: rootDescriptor.width,
      rootHeight: rootDescriptor.height,
    };
  }

  function classifySectionExampleType(pair, pcStats, moStats) {
    const corpus = [
      pair.pc.name,
      pair.mo.name,
      pair.pc.canonicalName,
      pair.mo.canonicalName,
      pair.pc.componentInfo.componentName,
      pair.mo.componentInfo.componentName,
      pair.pc.semanticSegment,
      pair.mo.semanticSegment,
    ]
      .join(" ")
      .toLowerCase();
    const imageHeavy = Math.max(pcStats.imageFillNodes, moStats.imageFillNodes) >= 1;
    const textHeavy = Math.max(pcStats.textNodes, moStats.textNodes) >= 3;
    const repeatedChildren = Math.max(pair.pc.meaningfulChildCount, pair.mo.meaningfulChildCount) >= 3;
    const largeVisual = isLargeVisualSectionPair(pair);

    if (containsKeyword(corpus, ["hero", "banner", "kv", "masthead", "main visual"]) || (largeVisual && imageHeavy && textHeavy)) {
      return "hero";
    }
    if (containsKeyword(corpus, ["header", "gnb", "nav", "navigation"])) {
      return "header";
    }
    if (containsKeyword(corpus, ["footer"])) {
      return "footer";
    }
    if (containsKeyword(corpus, ["promo", "promotion", "offer", "coupon", "discount", "deal", "event"])) {
      return "promo";
    }
    if (containsKeyword(corpus, ["video", "media", "gallery", "carousel", "slider"])) {
      return "media";
    }
    if (repeatedChildren && containsKeyword(corpus, ["card", "product", "grid", "collection", "list"])) {
      return "card-list";
    }
    if (repeatedChildren && pair.pc.layoutInfo && pair.mo.layoutInfo && pair.pc.layoutInfo.mode === "HORIZONTAL" && pair.mo.layoutInfo.mode === "VERTICAL") {
      return "card-list";
    }
    if (imageHeavy && textHeavy) {
      return "editorial";
    }
    if (textHeavy) {
      return "article";
    }
    if (imageHeavy) {
      return "visual";
    }
    return "section";
  }

  function classifyMobileSectionPattern(pair, sectionType, pcStats, moStats) {
    const pcMode = pair.pc.layoutInfo ? pair.pc.layoutInfo.mode : "";
    const moMode = pair.mo.layoutInfo ? pair.mo.layoutInfo.mode : "";
    const imageHeavy = Math.max(pcStats.imageFillNodes, moStats.imageFillNodes) >= 1;
    const repeatedChildren = Math.max(pair.pc.meaningfulChildCount, pair.mo.meaningfulChildCount) >= 3;

    if (sectionType === "hero") {
      return imageHeavy ? "hero-crop-stack" : "hero-stack";
    }
    if (sectionType === "promo") {
      return "promo-card";
    }
    if (sectionType === "card-list") {
      return moMode === "VERTICAL" || pcMode === "HORIZONTAL" ? "single-column-stack" : "card-stack";
    }
    if (sectionType === "editorial" || sectionType === "article") {
      return "stacked-article";
    }
    if (sectionType === "media") {
      return repeatedChildren ? "media-stack" : "media-focus";
    }
    if (pcMode === "HORIZONTAL" && moMode === "VERTICAL") {
      return "horizontal-to-vertical";
    }
    if (pair.pc.orderIndex !== pair.mo.orderIndex) {
      return "reordered-stack";
    }
    return "mobile-fit";
  }

  function buildHeroSectionGuidance(pcNode, moNode) {
    const mobileCopy = findHeroCopyBlockMeta(moNode) || findHeroCopyBlockMeta(pcNode);
    const focalTargets = buildHeroFocalTargets(moNode, pcNode);
    return {
      headlinePreserve: true,
      copyIntegrity: "required",
      copyBlockPreferred: !!mobileCopy,
      overlayPosition: mobileCopy ? mobileCopy.overlayPosition : "top-center",
      focalTargets: focalTargets,
      cropPriority: "headline-first",
      compositionPreset: "overlay-copy-bottom-visual",
      textAlignment: "center",
      visualAnchor: "bottom",
    };
  }

  function buildSectionGuidance(sectionType, pair, pcStats, moStats, mobilePattern, heroGuidance) {
    const contentPriority = classifySectionContentPriority(sectionType, pair, mobilePattern);
    const reflowPattern = classifySectionReflowPattern(sectionType, pair, mobilePattern);
    return {
      transformPriority: buildSectionTransformPriority(),
      contentPriority: contentPriority,
      reflowPattern: reflowPattern,
      textGroupRoles: buildSectionTextGroupRoles(sectionType, pair),
      textPreservation: buildSectionTextPreservation(sectionType, heroGuidance),
      textLayoutGuidance: buildSectionTextLayoutGuidance(sectionType, mobilePattern, heroGuidance),
      visualRole: buildSectionVisualRoleGuidance(sectionType, mobilePattern),
      mobileAspectPreference: buildSectionMobileAspectPreference(sectionType, pair, mobilePattern),
      cropSafeZone: buildSectionCropSafeZone(sectionType, pair, heroGuidance, contentPriority),
      dropRules: buildSectionDropRules(sectionType, pair, mobilePattern, pcStats, moStats, contentPriority, reflowPattern),
    };
  }

  function buildSectionTransformPriority() {
    return ["text-content", "text-scale", "text-align", "image-size", "image-align"];
  }

  function buildFrameTransformRecords(classifiedPair, pairId, createdAt) {
    const pcSide = buildFrameTransformSidePayload(classifiedPair && classifiedPair.pc);
    const moSide = buildFrameTransformSidePayload(classifiedPair && classifiedPair.mo);
    const direct = createFrameTransformRecord(pairId, createdAt, "pc-to-mo", pcSide, moSide);
    const reverse = createFrameTransformRecord(pairId, createdAt, "mo-to-pc", moSide, pcSide);
    return [direct, reverse].filter(Boolean);
  }

  function createFrameTransformRecord(pairId, createdAt, direction, source, target) {
    if (!source || !target || !(source.width > 0) || !(target.width > 0)) {
      return null;
    }

    return {
      type: "frame-transform",
      id: createRecordId("frame-transform"),
      version: PATCH_VERSION,
      pairId: pairId,
      direction: direction,
      scope: "root",
      sourceShape: source.shape,
      targetShape: target.shape,
      sourceAspectBucket: source.aspectBucket,
      targetWidthBucket: bucketFrameWidth(target.width),
      sourceLayoutMode: source.layoutMode,
      sectionCountBucket: source.sectionCountBucket,
      profileKey: [
        direction,
        source.shape,
        source.aspectBucket,
        source.layoutMode,
        source.sectionCountBucket,
        bucketFrameWidth(target.width),
      ].join("|"),
      source: source,
      target: target,
      delta: {
        widthScaleRatio: roundRatio(target.width / Math.max(1, source.width)),
        heightScaleRatio: roundRatio(target.height / Math.max(1, source.height)),
        aspectRatioShift: roundRatio(target.aspectRatio - source.aspectRatio),
        shapeChanged: source.shape !== target.shape,
        layoutModeChanged: source.layoutMode !== target.layoutMode,
      },
      createdAt: createdAt,
    };
  }

  function buildFrameTransformSidePayload(rootDescriptor) {
    if (!rootDescriptor || !rootDescriptor.node) {
      return null;
    }

    const layout = getLayoutInfo(rootDescriptor.node);
    const childCount = Array.isArray(rootDescriptor.node.children) ? rootDescriptor.node.children.filter(Boolean).length : 0;
    const aspectRatio = getFrameAspectRatio(rootDescriptor.width, rootDescriptor.height);

    return {
      nodeId: rootDescriptor.id,
      name: rootDescriptor.name,
      width: roundPixel(rootDescriptor.width),
      height: roundPixel(rootDescriptor.height),
      aspectRatio: aspectRatio,
      shape: classifyFrameShape(aspectRatio),
      aspectBucket: bucketFrameAspectRatio(aspectRatio),
      layoutMode: normalizeFrameLayoutMode(layout.mode),
      columns: estimateColumnCount(rootDescriptor.node),
      childCount: childCount,
      sectionCount: childCount,
      sectionCountBucket: bucketFrameSectionCount(childCount),
      representativeFontSize: getRepresentativeFontSize(rootDescriptor.node),
    };
  }

  function buildFrameShapeProfileRecords(currentStore, currentFrameTransformRecords, createdAt) {
    const existingRecords = currentStore && Array.isArray(currentStore.records) ? currentStore.records : [];
    const observationRecords = [];
    const groupMap = new Map();

    for (let index = 0; index < existingRecords.length; index += 1) {
      if (isFrameTransformRecord(existingRecords[index])) {
        observationRecords.push(existingRecords[index]);
      }
    }
    for (let index = 0; index < currentFrameTransformRecords.length; index += 1) {
      if (isFrameTransformRecord(currentFrameTransformRecords[index])) {
        observationRecords.push(currentFrameTransformRecords[index]);
      }
    }

    for (let index = 0; index < observationRecords.length; index += 1) {
      const record = observationRecords[index];
      const key = buildFrameShapeProfileKey(record);
      if (!key) {
        continue;
      }

      if (!groupMap.has(key)) {
        groupMap.set(key, {
          key: key,
          direction: string(record.direction, "pc-to-mo"),
          sourceShape: string(record.sourceShape, "balanced"),
          sourceAspectBucket: string(record.sourceAspectBucket, "0.85-1.09"),
          sourceLayoutMode: string(record.sourceLayoutMode, "none"),
          sectionCountBucket: string(record.sectionCountBucket, "1"),
          targetWidthBucket: string(record.targetWidthBucket, "320-399"),
          sourceAspectRatios: [],
          targetAspectRatios: [],
          widthScaleRatios: [],
          heightScaleRatios: [],
          pairIds: [],
          observationIds: [],
          targetShapeCounts: {},
        });
      }

      const group = groupMap.get(key);
      group.sourceAspectRatios.push(numeric(record.source && record.source.aspectRatio));
      group.targetAspectRatios.push(numeric(record.target && record.target.aspectRatio));
      group.widthScaleRatios.push(numeric(record.delta && record.delta.widthScaleRatio));
      group.heightScaleRatios.push(numeric(record.delta && record.delta.heightScaleRatio));

      const targetShape = string(record.targetShape, "");
      if (targetShape) {
        group.targetShapeCounts[targetShape] = (group.targetShapeCounts[targetShape] || 0) + 1;
      }
      if (record.pairId && group.pairIds.indexOf(record.pairId) < 0) {
        group.pairIds.push(record.pairId);
      }
      if (record.id && group.observationIds.indexOf(record.id) < 0) {
        group.observationIds.push(record.id);
      }
    }

    const profiles = [];
    groupMap.forEach((group) => {
      const targetAspectSummary = buildRatioPercentileSummary(group.targetAspectRatios);
      const targetAspectSpread = Math.max(0, numeric(targetAspectSummary.p75) - numeric(targetAspectSummary.p25));
      profiles.push({
        type: "frame-shape-profile",
        id: `frame-shape-profile:${hashString(group.key)}`,
        version: FRAME_SHAPE_PROFILE_VERSION,
        profileKey: group.key,
        direction: group.direction,
        sourceShape: group.sourceShape,
        sourceAspectBucket: group.sourceAspectBucket,
        sourceLayoutMode: group.sourceLayoutMode,
        sectionCountBucket: group.sectionCountBucket,
        targetWidthBucket: group.targetWidthBucket,
        sampleCount: group.targetAspectRatios.length,
        pairCount: group.pairIds.length,
        dominantTargetShape: getDominantGroupValue(group.targetShapeCounts),
        targetShapeDistribution: buildShapeDistribution(group.targetShapeCounts),
        sourceAspectRatio: buildRatioPercentileSummary(group.sourceAspectRatios),
        targetAspectRatio: targetAspectSummary,
        widthScaleRatio: buildRatioPercentileSummary(group.widthScaleRatios),
        heightScaleRatio: buildRatioPercentileSummary(group.heightScaleRatios),
        confidence: computeFrameShapeProfileConfidence(group.targetAspectRatios.length, group.targetShapeCounts, targetAspectSpread),
        pairIds: group.pairIds.slice(0, 24),
        exampleFrameTransformIds: group.observationIds.slice(0, 12),
        createdAt: createdAt,
        updatedAt: createdAt,
      });
    });

    profiles.sort((left, right) => {
      if (right.sampleCount !== left.sampleCount) {
        return right.sampleCount - left.sampleCount;
      }
      if (right.confidence !== left.confidence) {
        return right.confidence - left.confidence;
      }
      return String(left.id || "").localeCompare(String(right.id || ""));
    });

    return profiles;
  }

  function isFrameTransformRecord(record) {
    return !!(record && record.type === "frame-transform" && record.scope === "root");
  }

  function buildFrameShapeProfileKey(record) {
    if (!record) {
      return "";
    }
    return [
      string(record.direction, "pc-to-mo"),
      string(record.sourceShape, "balanced"),
      string(record.sourceAspectBucket, "0.85-1.09"),
      string(record.sourceLayoutMode, "none"),
      string(record.sectionCountBucket, "1"),
      string(record.targetWidthBucket, "320-399"),
    ].join("|");
  }

  function computeFrameShapeProfileConfidence(sampleCount, targetShapeCounts, targetAspectSpread) {
    const safeCount = Math.max(0, numeric(sampleCount));
    const keys = targetShapeCounts && typeof targetShapeCounts === "object" ? Object.keys(targetShapeCounts) : [];
    let total = 0;
    let dominant = 0;
    for (let index = 0; index < keys.length; index += 1) {
      const count = Math.max(0, numeric(targetShapeCounts[keys[index]]));
      total += count;
      dominant = Math.max(dominant, count);
    }
    const dominance = total > 0 ? dominant / total : 0;
    const stability = clampScore(1 - Math.min(1, numeric(targetAspectSpread) / 0.9));
    return roundConfidence(Math.min(1, safeCount / 5) * 0.56 + dominance * 0.28 + stability * 0.16);
  }

  function buildShapeDistribution(counts) {
    const safeCounts = counts && typeof counts === "object" ? counts : {};
    const keys = Object.keys(safeCounts).filter(Boolean);
    let total = 0;
    for (let index = 0; index < keys.length; index += 1) {
      total += Math.max(0, numeric(safeCounts[keys[index]]));
    }
    const entries = keys
      .map((shape) => {
        const count = Math.max(0, numeric(safeCounts[shape]));
        return {
          shape: shape,
          count: count,
          ratio: total > 0 ? roundConfidence(count / total) : 0,
        };
      })
      .sort((left, right) => {
        if (right.count !== left.count) {
          return right.count - left.count;
        }
        return String(left.shape || "").localeCompare(String(right.shape || ""));
      });
    return {
      dominant: entries.length > 0 ? entries[0].shape : "",
      entries: entries,
    };
  }

  function getFrameAspectRatio(width, height) {
    return roundRatio(numeric(height) / Math.max(1, numeric(width)));
  }

  function classifyFrameShape(aspectRatio) {
    const ratio = numeric(aspectRatio);
    if (ratio >= 1.18) {
      return "tall";
    }
    if (ratio <= 0.82) {
      return "wide";
    }
    return "balanced";
  }

  function bucketFrameAspectRatio(aspectRatio) {
    const ratio = numeric(aspectRatio);
    if (ratio <= 0.64) {
      return "0.00-0.64";
    }
    if (ratio <= 0.84) {
      return "0.65-0.84";
    }
    if (ratio <= 1.09) {
      return "0.85-1.09";
    }
    if (ratio <= 1.39) {
      return "1.10-1.39";
    }
    if (ratio <= 1.79) {
      return "1.40-1.79";
    }
    return "1.80+";
  }

  function bucketFrameWidth(width) {
    const value = Math.max(0, numeric(width));
    if (value <= 399) {
      return "320-399";
    }
    if (value <= 767) {
      return "400-767";
    }
    if (value <= 1023) {
      return "768-1023";
    }
    if (value <= 1439) {
      return "1024-1439";
    }
    if (value <= 1919) {
      return "1440-1919";
    }
    return "1920+";
  }

  function bucketFrameSectionCount(count) {
    const value = Math.max(0, Math.round(numeric(count)));
    if (value <= 1) {
      return "1";
    }
    if (value <= 3) {
      return "2-3";
    }
    if (value <= 6) {
      return "4-6";
    }
    return "7+";
  }

  function normalizeFrameLayoutMode(mode) {
    const value = string(mode, "NONE").toUpperCase();
    if (value === "HORIZONTAL" || value === "VERTICAL" || value === "NONE") {
      return value.toLowerCase();
    }
    return "none";
  }

  function buildContainerTransformRecords(classifiedPair, pairId, createdAt, matching, sectionRecords) {
    const records = [];
    const seen = new Set();
    const topLevelSectionByKey = {};
    const topLevelSignatureByKey = {};
    const sectionList = Array.isArray(sectionRecords) ? sectionRecords : [];
    const pairs = matching && Array.isArray(matching.pairs) ? matching.pairs.slice() : [];

    for (let index = 0; index < sectionList.length; index += 1) {
      const record = sectionList[index];
      if (!record) {
        continue;
      }
      const key = `${string(record.pcNodeId, "")}|${string(record.moNodeId, "")}`;
      if (!key || key === "|") {
        continue;
      }
      topLevelSectionByKey[key] = string(record.sectionType, "section");
      topLevelSignatureByKey[key] = string(record.sectionSignature, "");
    }

    pairs.sort((left, right) => right.score - left.score);

    for (let index = 0; index < pairs.length; index += 1) {
      const pair = pairs[index];
      if (!pair || pair.tier !== "confirmed" || !pair.pc || !pair.mo) {
        continue;
      }
      if (!CONTAINER_TYPES[pair.pc.type] || !CONTAINER_TYPES[pair.mo.type]) {
        continue;
      }
      if (!shouldObserveContainerTransform(pair)) {
        continue;
      }

      const dedupeKey = `${pair.pc.id}|${pair.mo.id}`;
      if (seen.has(dedupeKey)) {
        continue;
      }

      const direct = createContainerTransformRecord(
        classifiedPair,
        pairId,
        createdAt,
        "pc-to-mo",
        pair,
        pair.pc,
        pair.mo,
        topLevelSectionByKey,
        topLevelSignatureByKey
      );
      if (direct) {
        records.push(direct);
        seen.add(dedupeKey);
      }

      const reverse = createContainerTransformRecord(
        classifiedPair,
        pairId,
        createdAt,
        "mo-to-pc",
        pair,
        pair.mo,
        pair.pc,
        topLevelSectionByKey,
        topLevelSignatureByKey
      );
      if (reverse) {
        records.push(reverse);
      }
    }

    return records;
  }

  function shouldObserveContainerTransform(pair) {
    const left = pair && pair.pc ? pair.pc : null;
    const right = pair && pair.mo ? pair.mo : null;
    if (!left || !right) {
      return false;
    }
    if (isSectionLikeDescriptor(left) || isSectionLikeDescriptor(right)) {
      return true;
    }
    const leftLayout = left.layoutInfo || {};
    const rightLayout = right.layoutInfo || {};
    if (left.meaningfulChildCount >= 2 || right.meaningfulChildCount >= 2) {
      return true;
    }
    if (leftLayout.mode !== "NONE" || rightLayout.mode !== "NONE") {
      return true;
    }
    const leftBounds = left.bounds;
    const rightBounds = right.bounds;
    return !!(
      leftBounds &&
      rightBounds &&
      Math.max(numeric(leftBounds.width), numeric(rightBounds.width)) >= 120 &&
      Math.max(numeric(leftBounds.height), numeric(rightBounds.height)) >= 72
    );
  }

  function createContainerTransformRecord(classifiedPair, pairId, createdAt, direction, pair, sourceDescriptor, targetDescriptor, topLevelSectionByKey, topLevelSignatureByKey) {
    const rootKey =
      direction === "pc-to-mo"
        ? `${string(pair.pc.topLevelId, pair.pc.id)}|${string(pair.mo.topLevelId, pair.mo.id)}`
        : `${string(pair.mo.topLevelId, pair.mo.id)}|${string(pair.pc.topLevelId, pair.pc.id)}`;
    const sectionType = string(topLevelSectionByKey[rootKey], "") || inferContainerSectionType(pair);
    const sectionSignature =
      string(topLevelSignatureByKey[rootKey], "") || buildSectionSignature(sourceDescriptor) || buildSectionSignature(targetDescriptor) || rootKey;
    const sourceRoot = direction === "pc-to-mo" ? classifiedPair.pc : classifiedPair.mo;
    const targetRoot = direction === "pc-to-mo" ? classifiedPair.mo : classifiedPair.pc;
    const source = buildContainerTransformSidePayload(sourceRoot, sourceDescriptor);
    const target = buildContainerTransformSidePayload(targetRoot, targetDescriptor);
    if (!source || !target || !(source.width > 0) || !(target.width > 0)) {
      return null;
    }

    return {
      type: "container-transform",
      id: createRecordId("container-transform"),
      version: PATCH_VERSION,
      pairId: pairId,
      direction: direction,
      nodeKind: "container",
      confidence: roundConfidence(pair.score),
      scope: pair.scope || "matched-node",
      sectionType: sectionType || "section",
      sectionSignature: sectionSignature,
      sourceLayoutMode: source.layoutMode,
      sourceWidthBucket: bucketContainerWidth(source.width),
      childCountBucket: bucketContainerChildCount(source.meaningfulChildCount || source.childCount),
      targetWidthBucket: bucketContainerWidth(target.width),
      profileKey: [
        direction,
        sectionType || "section",
        source.layoutMode,
        bucketContainerWidth(source.width),
        bucketContainerChildCount(source.meaningfulChildCount || source.childCount),
        bucketContainerWidth(target.width),
      ].join("|"),
      source: source,
      target: target,
      delta: {
        widthScaleRatio: roundRatio(target.width / Math.max(1, source.width)),
        heightScaleRatio: roundRatio(target.height / Math.max(1, source.height)),
        gapScaleRatio: source.gap > 0 ? roundRatio(target.gap / Math.max(1, source.gap)) : 0,
        paddingXScaleRatio: source.paddingX > 0 ? roundRatio(target.paddingX / Math.max(1, source.paddingX)) : 0,
        paddingYScaleRatio: source.paddingY > 0 ? roundRatio(target.paddingY / Math.max(1, source.paddingY)) : 0,
        aspectRatioShift: roundRatio(target.aspectRatio - source.aspectRatio),
        columnsDelta: Math.round(target.columns - source.columns),
        layoutChanged: source.layoutMode !== target.layoutMode,
      },
      createdAt: createdAt,
    };
  }

  function buildContainerTransformSidePayload(rootDescriptor, descriptor) {
    if (!rootDescriptor || !descriptor || !descriptor.node) {
      return null;
    }
    const bounds = descriptor.bounds || getNodeBounds(descriptor.node);
    const layout = descriptor.layoutInfo || getLayoutInfo(descriptor.node);
    const width = bounds ? roundPixel(bounds.width) : 0;
    const height = bounds ? roundPixel(bounds.height) : 0;
    return {
      nodeId: descriptor.id,
      name: descriptor.name,
      semanticPath: descriptor.semanticPath,
      topLevelSemanticPath: descriptor.topLevelSemanticPath,
      layoutMode: normalizeFrameLayoutMode(layout.mode),
      gap: numeric(layout.gap),
      paddingX: numeric(layout.paddingX),
      paddingY: numeric(layout.paddingY),
      width: width,
      height: height,
      aspectRatio: getFrameAspectRatio(width, height),
      widthRatio: descriptor.relativeBounds ? descriptor.relativeBounds.widthRatio : 0,
      heightRatio: descriptor.relativeBounds ? descriptor.relativeBounds.heightRatio : 0,
      columns: estimateColumnCount(descriptor.node),
      childCount: descriptor.childCount,
      meaningfulChildCount: descriptor.meaningfulChildCount,
      imageFillCount: numeric(descriptor.stats && descriptor.stats.imageFillNodes),
      rootWidth: rootDescriptor.width,
      rootHeight: rootDescriptor.height,
    };
  }

  function inferContainerSectionType(pair) {
    if (!pair || !pair.pc || !pair.mo) {
      return "section";
    }
    const stats = {
      pc: collectNodeStats(pair.pc.node),
      mo: collectNodeStats(pair.mo.node),
    };
    return classifySectionExampleType(pair, stats.pc, stats.mo);
  }

  function buildContainerProfileRecords(currentStore, currentContainerTransformRecords, createdAt) {
    const existingRecords = currentStore && Array.isArray(currentStore.records) ? currentStore.records : [];
    const observationRecords = [];
    const groupMap = new Map();

    for (let index = 0; index < existingRecords.length; index += 1) {
      if (isContainerTransformRecord(existingRecords[index])) {
        observationRecords.push(existingRecords[index]);
      }
    }
    for (let index = 0; index < currentContainerTransformRecords.length; index += 1) {
      if (isContainerTransformRecord(currentContainerTransformRecords[index])) {
        observationRecords.push(currentContainerTransformRecords[index]);
      }
    }

    for (let index = 0; index < observationRecords.length; index += 1) {
      const record = observationRecords[index];
      const key = buildContainerProfileKey(record);
      if (!key) {
        continue;
      }

      if (!groupMap.has(key)) {
        groupMap.set(key, {
          key: key,
          direction: string(record.direction, "pc-to-mo"),
          sectionType: string(record.sectionType, "section"),
          sourceLayoutMode: string(record.sourceLayoutMode, "none"),
          sourceWidthBucket: string(record.sourceWidthBucket, "0-159"),
          childCountBucket: string(record.childCountBucket, "1"),
          targetWidthBucket: string(record.targetWidthBucket, "0-159"),
          targetGapValues: [],
          targetPaddingXValues: [],
          targetPaddingYValues: [],
          targetAspectRatios: [],
          widthScaleRatios: [],
          heightScaleRatios: [],
          gapScaleRatios: [],
          paddingXScaleRatios: [],
          paddingYScaleRatios: [],
          pairIds: [],
          observationIds: [],
          targetLayoutCounts: {},
          targetColumnCounts: {},
        });
      }

      const group = groupMap.get(key);
      group.targetGapValues.push(numeric(record.target && record.target.gap));
      group.targetPaddingXValues.push(numeric(record.target && record.target.paddingX));
      group.targetPaddingYValues.push(numeric(record.target && record.target.paddingY));
      group.targetAspectRatios.push(numeric(record.target && record.target.aspectRatio));
      group.widthScaleRatios.push(numeric(record.delta && record.delta.widthScaleRatio));
      group.heightScaleRatios.push(numeric(record.delta && record.delta.heightScaleRatio));
      if (numeric(record.delta && record.delta.gapScaleRatio) > 0) {
        group.gapScaleRatios.push(numeric(record.delta && record.delta.gapScaleRatio));
      }
      if (numeric(record.delta && record.delta.paddingXScaleRatio) > 0) {
        group.paddingXScaleRatios.push(numeric(record.delta && record.delta.paddingXScaleRatio));
      }
      if (numeric(record.delta && record.delta.paddingYScaleRatio) > 0) {
        group.paddingYScaleRatios.push(numeric(record.delta && record.delta.paddingYScaleRatio));
      }

      const targetLayoutMode = string(record.target && record.target.layoutMode, "");
      if (targetLayoutMode) {
        group.targetLayoutCounts[targetLayoutMode] = (group.targetLayoutCounts[targetLayoutMode] || 0) + 1;
      }
      const targetColumns = String(Math.max(1, Math.round(numeric(record.target && record.target.columns) || 1)));
      group.targetColumnCounts[targetColumns] = (group.targetColumnCounts[targetColumns] || 0) + 1;

      if (record.pairId && group.pairIds.indexOf(record.pairId) < 0) {
        group.pairIds.push(record.pairId);
      }
      if (record.id && group.observationIds.indexOf(record.id) < 0) {
        group.observationIds.push(record.id);
      }
    }

    const profiles = [];
    groupMap.forEach((group) => {
      const aspectSummary = buildRatioPercentileSummary(group.targetAspectRatios);
      const aspectSpread = Math.max(0, numeric(aspectSummary.p75) - numeric(aspectSummary.p25));
      profiles.push({
        type: "container-profile",
        id: `container-profile:${hashString(group.key)}`,
        version: CONTAINER_PROFILE_VERSION,
        profileKey: group.key,
        direction: group.direction,
        sectionType: group.sectionType,
        sourceLayoutMode: group.sourceLayoutMode,
        sourceWidthBucket: group.sourceWidthBucket,
        childCountBucket: group.childCountBucket,
        targetWidthBucket: group.targetWidthBucket,
        sampleCount: group.targetAspectRatios.length,
        pairCount: group.pairIds.length,
        preferredTargetLayoutMode: getDominantGroupValue(group.targetLayoutCounts),
        preferredTargetColumns: Math.max(1, Math.round(numeric(getDominantGroupValue(group.targetColumnCounts)) || 1)),
        targetLayoutDistribution: buildCountDistribution(group.targetLayoutCounts, "layoutMode"),
        targetColumnDistribution: buildCountDistribution(group.targetColumnCounts, "columns"),
        targetGap: buildRangeSummary(group.targetGapValues),
        targetPaddingX: buildRangeSummary(group.targetPaddingXValues),
        targetPaddingY: buildRangeSummary(group.targetPaddingYValues),
        targetAspectRatio: aspectSummary,
        widthScaleRatio: buildRatioPercentileSummary(group.widthScaleRatios),
        heightScaleRatio: buildRatioPercentileSummary(group.heightScaleRatios),
        gapScaleRatio: buildRatioPercentileSummary(group.gapScaleRatios),
        paddingXScaleRatio: buildRatioPercentileSummary(group.paddingXScaleRatios),
        paddingYScaleRatio: buildRatioPercentileSummary(group.paddingYScaleRatios),
        collapseToSingleColumn:
          Math.max(1, Math.round(numeric(getDominantGroupValue(group.targetColumnCounts)) || 1)) <= 1 ||
          getDominantGroupValue(group.targetLayoutCounts) === "vertical",
        confidence: computeContainerProfileConfidence(group.targetAspectRatios.length, group.targetLayoutCounts, aspectSpread),
        pairIds: group.pairIds.slice(0, 24),
        exampleContainerTransformIds: group.observationIds.slice(0, 12),
        createdAt: createdAt,
        updatedAt: createdAt,
      });
    });

    profiles.sort((left, right) => {
      if (right.sampleCount !== left.sampleCount) {
        return right.sampleCount - left.sampleCount;
      }
      if (right.confidence !== left.confidence) {
        return right.confidence - left.confidence;
      }
      return String(left.id || "").localeCompare(String(right.id || ""));
    });

    return profiles;
  }

  function isContainerTransformRecord(record) {
    return !!(record && record.type === "container-transform" && record.nodeKind === "container");
  }

  function buildContainerProfileKey(record) {
    if (!record) {
      return "";
    }
    return [
      string(record.direction, "pc-to-mo"),
      string(record.sectionType, "section"),
      string(record.sourceLayoutMode, "none"),
      string(record.sourceWidthBucket, "0-159"),
      string(record.childCountBucket, "1"),
      string(record.targetWidthBucket, "0-159"),
    ].join("|");
  }

  function computeContainerProfileConfidence(sampleCount, targetLayoutCounts, targetAspectSpread) {
    const safeCount = Math.max(0, numeric(sampleCount));
    const keys = targetLayoutCounts && typeof targetLayoutCounts === "object" ? Object.keys(targetLayoutCounts) : [];
    let total = 0;
    let dominant = 0;
    for (let index = 0; index < keys.length; index += 1) {
      const count = Math.max(0, numeric(targetLayoutCounts[keys[index]]));
      total += count;
      dominant = Math.max(dominant, count);
    }
    const dominance = total > 0 ? dominant / total : 0;
    const stability = clampScore(1 - Math.min(1, numeric(targetAspectSpread) / 0.9));
    return roundConfidence(Math.min(1, safeCount / 6) * 0.52 + dominance * 0.28 + stability * 0.2);
  }

  function buildCountDistribution(counts, fieldName) {
    const safeCounts = counts && typeof counts === "object" ? counts : {};
    const keys = Object.keys(safeCounts).filter(Boolean);
    let total = 0;
    for (let index = 0; index < keys.length; index += 1) {
      total += Math.max(0, numeric(safeCounts[keys[index]]));
    }
    const field = string(fieldName, "value");
    const entries = keys
      .map((value) => {
        const count = Math.max(0, numeric(safeCounts[value]));
        const entry = {
          count: count,
          ratio: total > 0 ? roundConfidence(count / total) : 0,
        };
        entry[field] = value;
        return entry;
      })
      .sort((left, right) => {
        if (right.count !== left.count) {
          return right.count - left.count;
        }
        return String(left[field] || "").localeCompare(String(right[field] || ""));
      });
    const dominant = entries.length > 0 ? entries[0][field] : "";
    return {
      dominant: dominant,
      entries: entries,
    };
  }

  function bucketContainerWidth(width) {
    const value = Math.max(0, numeric(width));
    if (value < 160) {
      return "0-159";
    }
    if (value < 280) {
      return "160-279";
    }
    if (value < 400) {
      return "280-399";
    }
    if (value < 560) {
      return "400-559";
    }
    if (value < 800) {
      return "560-799";
    }
    if (value < 1120) {
      return "800-1119";
    }
    return "1120+";
  }

  function bucketContainerChildCount(count) {
    const value = Math.max(0, Math.round(numeric(count)));
    if (value <= 1) {
      return "1";
    }
    if (value <= 3) {
      return "2-3";
    }
    if (value <= 6) {
      return "4-6";
    }
    return "7+";
  }

  function buildNodeTransformRecords(classifiedPair, pairId, createdAt, matching, sectionRecords) {
    const records = [];
    const seen = new Set();
    const topLevelSectionByKey = {};
    const topLevelSignatureByKey = {};
    const sectionList = Array.isArray(sectionRecords) ? sectionRecords : [];
    const pairs = matching && Array.isArray(matching.pairs) ? matching.pairs.slice() : [];

    for (let index = 0; index < sectionList.length; index += 1) {
      const record = sectionList[index];
      if (!record) {
        continue;
      }
      const key = `${string(record.pcNodeId, "")}|${string(record.moNodeId, "")}`;
      if (!key || key === "|") {
        continue;
      }
      topLevelSectionByKey[key] = string(record.sectionType, "section");
      topLevelSignatureByKey[key] = string(record.sectionSignature, "");
    }

    pairs.sort((left, right) => right.score - left.score);

    for (let index = 0; index < pairs.length; index += 1) {
      const pair = pairs[index];
      if (!pair || pair.tier !== "confirmed") {
        continue;
      }
      if (!pair.pc || !pair.mo || pair.pc.type !== "TEXT" || pair.mo.type !== "TEXT") {
        continue;
      }

      const dedupeKey = `${pair.pc.id}|${pair.mo.id}`;
      if (seen.has(dedupeKey)) {
        continue;
      }

      const forwardRecord = createTextNodeTransformRecord(
        classifiedPair,
        pairId,
        createdAt,
        pair,
        topLevelSectionByKey,
        topLevelSignatureByKey,
        "pc-to-mo"
      );
      const reverseRecord = createTextNodeTransformRecord(
        classifiedPair,
        pairId,
        createdAt,
        pair,
        topLevelSectionByKey,
        topLevelSignatureByKey,
        "mo-to-pc"
      );
      if (!forwardRecord && !reverseRecord) {
        continue;
      }

      seen.add(dedupeKey);
      if (forwardRecord) {
        records.push(forwardRecord);
      }
      if (reverseRecord) {
        records.push(reverseRecord);
      }
    }

    return records;
  }

  function createTextNodeTransformRecord(classifiedPair, pairId, createdAt, pair, topLevelSectionByKey, topLevelSignatureByKey, direction) {
    const rootKey = `${string(pair.pc.topLevelId, pair.pc.id)}|${string(pair.mo.topLevelId, pair.mo.id)}`;
    const sectionType = string(topLevelSectionByKey[rootKey], "") || inferTextSectionType(pair);
    const sectionSignature = string(topLevelSignatureByKey[rootKey], "") || buildSectionSignature(pair.pc) || buildSectionSignature(pair.mo);
    const textRole = classifyResponsiveTextRole(pair, sectionType);
    const pcSide = buildTextTransformSidePayload(classifiedPair.pc, pair.pc, textRole);
    const moSide = buildTextTransformSidePayload(classifiedPair.mo, pair.mo, textRole);

    if (!pcSide || !moSide || !pcSide.fontSize || !moSide.fontSize) {
      return null;
    }

    const safeDirection = direction === "mo-to-pc" ? "mo-to-pc" : "pc-to-mo";
    const sourceSide = safeDirection === "mo-to-pc" ? moSide : pcSide;
    const targetSide = safeDirection === "mo-to-pc" ? pcSide : moSide;
    const fontScaleRatio = roundConfidence(targetSide.fontSize / Math.max(1, sourceSide.fontSize));
    const lineHeightRatio =
      sourceSide.lineHeight > 0 && targetSide.lineHeight > 0
        ? roundConfidence(targetSide.lineHeight / Math.max(1, sourceSide.lineHeight))
        : 0;
    const widthRatio =
      sourceSide.width > 0 && targetSide.width > 0 ? roundConfidence(targetSide.width / Math.max(1, sourceSide.width)) : 0;
    const charBucket = bucketTextCharacterCount(Math.max(sourceSide.charCount, targetSide.charCount));
    const sourceFontBucket = bucketTextFontSize(sourceSide.fontSize);
    const alignChanged = pcSide.textAlign && moSide.textAlign ? pcSide.textAlign !== moSide.textAlign : false;
    const componentFamily = string(pair.pc.componentInfo.componentFamily, "") || string(pair.mo.componentInfo.componentFamily, "");

    return {
      type: "node-transform",
      id: createRecordId("node-transform"),
      version: PATCH_VERSION,
      pairId: pairId,
      direction: safeDirection,
      nodeKind: "text",
      confidence: roundConfidence(pair.score),
      scope: pair.scope || "matched-node",
      sectionType: sectionType || "section",
      sectionSignature: sectionSignature || rootKey,
      textRole: textRole,
      componentFamily: componentFamily,
      sourceFontBucket: sourceFontBucket,
      charBucket: charBucket,
      profileKey: [safeDirection, sectionType || "section", textRole, sourceFontBucket, charBucket].join("|"),
      pc: pcSide,
      mo: moSide,
      delta: {
        fontScaleRatio: fontScaleRatio,
        lineHeightRatio: lineHeightRatio,
        widthRatio: widthRatio,
        lineCountDelta: targetSide.lineCount - sourceSide.lineCount,
        alignmentChanged: alignChanged,
      },
      createdAt: createdAt,
    };
  }

  function buildTextTransformSidePayload(rootDescriptor, descriptor, textRole) {
    const node = descriptor && descriptor.node ? descriptor.node : null;
    const bounds = descriptor && descriptor.bounds ? descriptor.bounds : getNodeBounds(node);
    const fontSize = getTextFontSize(node);
    const charCount = getTextCharacterCount(node);
    const lineHeight = getTextLineHeight(node, bounds, fontSize);
    const lineCount = estimateTextLineCount(node, bounds, fontSize, lineHeight);
    const fontInfo = getTextFontInfo(node);

    return {
      nodeId: descriptor.id,
      name: descriptor.name,
      semanticPath: descriptor.semanticPath,
      topLevelSemanticPath: descriptor.topLevelSemanticPath,
      textRole: textRole,
      width: bounds ? roundPixel(bounds.width) : 0,
      height: bounds ? roundPixel(bounds.height) : 0,
      widthRatio: descriptor.relativeBounds ? descriptor.relativeBounds.widthRatio : 0,
      heightRatio: descriptor.relativeBounds ? descriptor.relativeBounds.heightRatio : 0,
      fontSize: fontSize,
      lineHeight: lineHeight,
      charCount: charCount,
      lineCount: lineCount,
      textAlign: getTextAlignLabel(node),
      fontFamily: fontInfo.family,
      fontStyle: fontInfo.style,
      fontWeight: fontInfo.weight,
      rootWidth: rootDescriptor.width,
      rootHeight: rootDescriptor.height,
    };
  }

  function buildTextRoleProfileRecords(currentStore, currentNodeTransformRecords, createdAt) {
    const existingRecords = currentStore && Array.isArray(currentStore.records) ? currentStore.records : [];
    const observationRecords = [];
    const groupMap = new Map();

    for (let index = 0; index < existingRecords.length; index += 1) {
      if (isTextNodeTransformRecord(existingRecords[index])) {
        observationRecords.push(existingRecords[index]);
      }
    }
    for (let index = 0; index < currentNodeTransformRecords.length; index += 1) {
      if (isTextNodeTransformRecord(currentNodeTransformRecords[index])) {
        observationRecords.push(currentNodeTransformRecords[index]);
      }
    }

    for (let index = 0; index < observationRecords.length; index += 1) {
      const record = observationRecords[index];
      const key = buildTextRoleProfileKey(record);
      if (!key) {
        continue;
      }

      if (!groupMap.has(key)) {
        groupMap.set(key, {
          key: key,
          direction: string(record.direction, "pc-to-mo"),
          sectionType: string(record.sectionType, "section"),
          textRole: string(record.textRole, "body"),
          sourceFontBucket: string(record.sourceFontBucket, "0-15"),
          charBucket: string(record.charBucket, "1-5"),
          fontRatios: [],
          lineHeightRatios: [],
          widthRatios: [],
          targetFontSizes: [],
          targetLineCounts: [],
          pairIds: [],
          observationIds: [],
          alignCounts: {},
        });
      }

      const group = groupMap.get(key);
      const fontRatio = numeric(record.delta && record.delta.fontScaleRatio);
      const lineHeightRatio = numeric(record.delta && record.delta.lineHeightRatio);
      const widthRatio = numeric(record.delta && record.delta.widthRatio);
      const targetSide = getTextTransformTargetSide(record);
      const targetFontSize = numeric(targetSide && targetSide.fontSize);
      const targetLineCount = numeric(targetSide && targetSide.lineCount);
      const align = string(targetSide && targetSide.textAlign, "");

      if (fontRatio > 0) {
        group.fontRatios.push(fontRatio);
      }
      if (lineHeightRatio > 0) {
        group.lineHeightRatios.push(lineHeightRatio);
      }
      if (widthRatio > 0) {
        group.widthRatios.push(widthRatio);
      }
      if (targetFontSize > 0) {
        group.targetFontSizes.push(targetFontSize);
      }
      if (targetLineCount > 0) {
        group.targetLineCounts.push(targetLineCount);
      }
      if (align) {
        group.alignCounts[align] = (group.alignCounts[align] || 0) + 1;
      }
      if (group.pairIds.indexOf(record.pairId) < 0) {
        group.pairIds.push(record.pairId);
      }
      if (group.observationIds.length < 12) {
        group.observationIds.push(record.id);
      }
    }

    const profiles = [];
    groupMap.forEach((group) => {
      if (group.fontRatios.length === 0 || group.targetFontSizes.length === 0) {
        return;
      }

      const fontRatioSpread = Math.abs(getPercentile(group.fontRatios, 0.75) - getPercentile(group.fontRatios, 0.25));
      profiles.push({
        type: "text-role-profile",
        id: `text-role-profile:${hashString(group.key)}`,
        version: TEXT_ROLE_PROFILE_VERSION,
        direction: group.direction,
        sectionType: group.sectionType,
        textRole: group.textRole,
        sourceFontBucket: group.sourceFontBucket,
        charBucket: group.charBucket,
        sampleCount: group.fontRatios.length,
        pairCount: group.pairIds.length,
        confidence: computeTextRoleProfileConfidence(group.fontRatios.length, fontRatioSpread),
        preferredAlign: getDominantGroupValue(group.alignCounts),
        alignDistribution: buildAlignmentDistribution(group.alignCounts),
        fontScaleRatio: buildPercentileSummary(group.fontRatios),
        lineHeightRatio: buildPercentileSummary(group.lineHeightRatios),
        widthRatio: buildPercentileSummary(group.widthRatios),
        targetFontSize: buildRangeSummary(group.targetFontSizes),
        targetLineCount: buildIntegerRangeSummary(group.targetLineCounts),
        pairIds: group.pairIds.slice(0, 24),
        exampleNodeTransformIds: group.observationIds.slice(0, 12),
        createdAt: createdAt,
        updatedAt: createdAt,
      });
    });

    profiles.sort((left, right) => {
      if (right.sampleCount !== left.sampleCount) {
        return right.sampleCount - left.sampleCount;
      }
      if (right.confidence !== left.confidence) {
        return right.confidence - left.confidence;
      }
      return String(left.id || "").localeCompare(String(right.id || ""));
    });

    return profiles;
  }

  function isTextNodeTransformRecord(record) {
    return !!(record && record.type === "node-transform" && record.nodeKind === "text");
  }

  function getTextTransformTargetSide(record) {
    if (!record) {
      return null;
    }
    return string(record.direction, "pc-to-mo") === "mo-to-pc" ? record.pc : record.mo;
  }

  function buildTextRoleProfileKey(record) {
    if (!record) {
      return "";
    }
    return [
      string(record.direction, "pc-to-mo"),
      string(record.sectionType, "section"),
      string(record.textRole, "body"),
      string(record.sourceFontBucket, "0-15"),
      string(record.charBucket, "1-5"),
    ].join("|");
  }

  function inferTextSectionType(pair) {
    if (!pair || !pair.pc || !pair.mo) {
      return "section";
    }
    const corpus = [pair.pc.topLevelSemanticSegment, pair.mo.topLevelSemanticSegment, pair.pc.parentSemanticSegment, pair.mo.parentSemanticSegment]
      .join(" ")
      .toLowerCase();
    if (containsKeyword(corpus, ["hero", "banner", "kv", "masthead"])) {
      return "hero";
    }
    if (containsKeyword(corpus, ["promo", "promotion", "offer", "coupon", "deal", "event"])) {
      return "promo";
    }
    if (containsKeyword(corpus, ["article", "editorial", "story", "news"])) {
      return "article";
    }
    if (containsKeyword(corpus, ["header", "nav", "gnb"])) {
      return "header";
    }
    if (containsKeyword(corpus, ["footer"])) {
      return "footer";
    }
    return "section";
  }

  function classifyResponsiveTextRole(pair, sectionType) {
    const pcNode = pair && pair.pc ? pair.pc.node : null;
    const moNode = pair && pair.mo ? pair.mo.node : null;
    const corpus = [safeName(pcNode), safeName(moNode), string(pcNode && pcNode.characters, ""), string(moNode && moNode.characters, "")]
      .join(" ")
      .toLowerCase();
    const fontSize = Math.max(getTextFontSize(pcNode), getTextFontSize(moNode));
    const charCount = Math.max(getTextCharacterCount(pcNode), getTextCharacterCount(moNode));

    if (containsKeyword(corpus, ["cta", "button", "shop", "buy", "learn more", "discover", "apply"])) {
      return "cta";
    }
    if (containsKeyword(corpus, ["eyebrow", "meta", "date", "tag", "badge", "label"])) {
      return "meta";
    }
    if (containsKeyword(corpus, ["subtitle", "sub title", "subheading", "supporting copy"])) {
      return "subtitle";
    }
    if (sectionType === "hero" && fontSize >= 36) {
      return "headline";
    }
    if ((sectionType === "promo" || sectionType === "article") && fontSize >= 24) {
      return "headline";
    }
    if (fontSize >= 32) {
      return "headline";
    }
    if (fontSize >= 20 && charCount <= 28) {
      return "headline";
    }
    if (fontSize >= 18) {
      return "subtitle";
    }
    return "body";
  }

  function buildPercentileSummary(values) {
    const list = normalizeNumericArray(values);
    if (list.length === 0) {
      return { p25: 0, p50: 0, p75: 0 };
    }
    return {
      p25: roundConfidence(getPercentile(list, 0.25)),
      p50: roundConfidence(getPercentile(list, 0.5)),
      p75: roundConfidence(getPercentile(list, 0.75)),
    };
  }

  function buildRatioPercentileSummary(values) {
    const list = normalizeNumericArray(values);
    if (list.length === 0) {
      return { p25: 0, p50: 0, p75: 0 };
    }
    return {
      p25: roundRatio(getPercentile(list, 0.25)),
      p50: roundRatio(getPercentile(list, 0.5)),
      p75: roundRatio(getPercentile(list, 0.75)),
    };
  }

  function buildRangeSummary(values) {
    const list = normalizeNumericArray(values);
    if (list.length === 0) {
      return { min: 0, p50: 0, max: 0 };
    }
    return {
      min: roundPixel(list[0]),
      p50: roundPixel(getPercentile(list, 0.5)),
      max: roundPixel(list[list.length - 1]),
    };
  }

  function buildIntegerRangeSummary(values) {
    const list = normalizeNumericArray(values);
    if (list.length === 0) {
      return { min: 0, median: 0, max: 0 };
    }
    return {
      min: Math.max(1, Math.round(list[0])),
      median: Math.max(1, Math.round(getPercentile(list, 0.5))),
      max: Math.max(1, Math.round(list[list.length - 1])),
    };
  }

  function normalizeNumericArray(values) {
    const list = [];
    const source = Array.isArray(values) ? values : [];
    for (let index = 0; index < source.length; index += 1) {
      const value = numeric(source[index]);
      if (value > 0) {
        list.push(value);
      }
    }
    list.sort((left, right) => left - right);
    return list;
  }

  function getPercentile(values, ratio) {
    const list = normalizeNumericArray(values);
    if (list.length === 0) {
      return 0;
    }
    if (list.length === 1) {
      return list[0];
    }

    const clampedRatio = Math.max(0, Math.min(1, numeric(ratio)));
    const index = (list.length - 1) * clampedRatio;
    const lowerIndex = Math.floor(index);
    const upperIndex = Math.min(list.length - 1, Math.ceil(index));
    const weight = index - lowerIndex;
    if (lowerIndex === upperIndex) {
      return list[lowerIndex];
    }
    return list[lowerIndex] + (list[upperIndex] - list[lowerIndex]) * weight;
  }

  function computeTextRoleProfileConfidence(sampleCount, fontRatioSpread) {
    const countScore = Math.min(0.42, Math.max(0, sampleCount - 1) * 0.07);
    const spreadPenalty = Math.min(0.22, Math.max(0, fontRatioSpread) * 0.5);
    return roundConfidence(clampScore(0.44 + countScore - spreadPenalty));
  }

  function getDominantGroupValue(counts) {
    const source = counts && typeof counts === "object" ? counts : {};
    const keys = Object.keys(source);
    let bestKey = "";
    let bestCount = 0;
    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      const count = numeric(source[key]);
      if (count > bestCount) {
        bestKey = key;
        bestCount = count;
      }
    }
    return bestKey;
  }

  function buildAlignmentDistribution(counts) {
    const source = counts && typeof counts === "object" ? counts : {};
    const keys = Object.keys(source);
    const entries = [];
    let total = 0;

    for (let index = 0; index < keys.length; index += 1) {
      total += numeric(source[keys[index]]);
    }

    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      const count = numeric(source[key]);
      if (count <= 0 || total <= 0) {
        continue;
      }
      entries.push({
        align: key,
        count: count,
        ratio: roundConfidence(count / total),
      });
    }

    entries.sort((left, right) => {
      if (right.count !== left.count) {
        return right.count - left.count;
      }
      return String(left.align || "").localeCompare(String(right.align || ""));
    });

    return {
      totalCount: total,
      entries: entries,
    };
  }

  function getTextFontSize(node) {
    if (!node || String(node.type || "") !== "TEXT") {
      return 0;
    }
    if (typeof node.fontSize === "number" && isFinite(node.fontSize)) {
      return roundPixel(node.fontSize);
    }
    return 0;
  }

  function getTextCharacterCount(node) {
    const text = string(node && node.characters, "").replace(/\s+/g, " ").trim();
    return text ? text.length : 0;
  }

  function getTextLineHeight(node, bounds, fontSize) {
    const lineHeight = node && typeof node === "object" ? node.lineHeight : null;
    if (lineHeight && typeof lineHeight === "object") {
      const unit = string(lineHeight.unit, "").toUpperCase();
      const value = numeric(lineHeight.value);
      if (unit === "PIXELS" && value > 0) {
        return roundPixel(value);
      }
      if (unit === "PERCENT" && value > 0 && fontSize > 0) {
        return roundPixel((fontSize * value) / 100);
      }
    }
    if (bounds && fontSize > 0) {
      const explicitLines = countExplicitTextLines(node);
      if (explicitLines > 0) {
        return roundPixel(bounds.height / explicitLines);
      }
    }
    return 0;
  }

  function estimateTextLineCount(node, bounds, fontSize, lineHeight) {
    const explicit = countExplicitTextLines(node);
    if (explicit > 1) {
      return explicit;
    }
    if (bounds && lineHeight > 0) {
      return Math.max(1, Math.round(bounds.height / Math.max(1, lineHeight)));
    }
    if (bounds && fontSize > 0) {
      return Math.max(1, Math.round(bounds.height / Math.max(1, fontSize * 1.25)));
    }
    return 1;
  }

  function countExplicitTextLines(node) {
    const text = string(node && node.characters, "");
    if (!text) {
      return 0;
    }
    return text.split(/\r?\n/).length;
  }

  function getTextAlignLabel(node) {
    return string(node && node.textAlignHorizontal, "").toLowerCase();
  }

  function getTextFontInfo(node) {
    const info = {
      family: "",
      style: "",
      weight: "",
    };
    const fontName = node && node.fontName && node.fontName !== figma.mixed ? node.fontName : null;
    if (fontName && typeof fontName === "object") {
      info.family = string(fontName.family, "");
      info.style = string(fontName.style, "");
      info.weight = normalizeFontWeightLabel(info.style);
    }
    return info;
  }

  function normalizeFontWeightLabel(style) {
    const text = string(style, "").toLowerCase();
    if (!text) {
      return "";
    }
    if (text.indexOf("thin") >= 0) {
      return "100";
    }
    if (text.indexOf("extra light") >= 0 || text.indexOf("ultra light") >= 0) {
      return "200";
    }
    if (text.indexOf("light") >= 0) {
      return "300";
    }
    if (text.indexOf("medium") >= 0) {
      return "500";
    }
    if (text.indexOf("semi bold") >= 0 || text.indexOf("semibold") >= 0) {
      return "600";
    }
    if (text.indexOf("extra bold") >= 0 || text.indexOf("ultra bold") >= 0) {
      return "800";
    }
    if (text.indexOf("black") >= 0 || text.indexOf("heavy") >= 0) {
      return "900";
    }
    if (text.indexOf("bold") >= 0) {
      return "700";
    }
    return "400";
  }

  function bucketTextFontSize(size) {
    const value = numeric(size);
    if (value < 16) {
      return "0-15";
    }
    if (value < 24) {
      return "16-23";
    }
    if (value < 32) {
      return "24-31";
    }
    if (value < 48) {
      return "32-47";
    }
    if (value < 64) {
      return "48-63";
    }
    if (value < 80) {
      return "64-79";
    }
    if (value < 112) {
      return "80-111";
    }
    if (value < 160) {
      return "112-159";
    }
    if (value < 220) {
      return "160-219";
    }
    if (value < 320) {
      return "220-319";
    }
    return "320+";
  }

  function bucketTextCharacterCount(count) {
    const value = numeric(count);
    if (value <= 5) {
      return "1-5";
    }
    if (value <= 11) {
      return "6-11";
    }
    if (value <= 19) {
      return "12-19";
    }
    if (value <= 31) {
      return "20-31";
    }
    if (value <= 47) {
      return "32-47";
    }
    return "48+";
  }

  function buildSectionTextGroupRoles(sectionType, pair) {
    const entries = collectOrderedSectionTextEntries(pair && pair.mo ? pair.mo.node : null);
    const detected = [];
    const seen = new Set();

    for (let index = 0; index < entries.length; index += 1) {
      const role = classifySectionTextRole(entries[index], sectionType, index, entries.length);
      if (!role || seen.has(role)) {
        continue;
      }
      seen.add(role);
      detected.push(role);
    }

    if (!detected.length) {
      if (sectionType === "hero") {
        detected.push("eyebrow", "headline", "subtitle", "body");
      } else if (sectionType === "promo") {
        detected.push("meta", "headline", "body");
      } else {
        detected.push("headline", "body");
      }
    }

    const required = [];
    if (detected.indexOf("headline") >= 0) {
      required.push("headline");
    }
    if (detected.indexOf("body") >= 0) {
      required.push("body");
    }
    if (sectionType === "promo" && detected.indexOf("meta") >= 0) {
      required.unshift("meta");
    }
    if (sectionType === "hero" && detected.indexOf("subtitle") >= 0) {
      required.push("subtitle");
    }

    return {
      order: detected.slice(0, 6),
      required: uniqueValues(required),
      detected: detected.slice(0, 8),
    };
  }

  function buildSectionTextPreservation(sectionType, heroGuidance) {
    const strictHero = sectionType === "hero" || !!(heroGuidance && heroGuidance.headlinePreserve);
    return {
      allTextMustSurvive: true,
      preserveReadingOrder: true,
      preserveHeadlineVerbatim: strictHero,
      copyBlockPreferred: !!(heroGuidance && heroGuidance.copyBlockPreferred),
    };
  }

  function classifySectionContentPriority(sectionType, pair, mobilePattern) {
    if (sectionType === "hero" || sectionType === "promo") {
      return "text-first";
    }
    if (sectionType === "article" || sectionType === "editorial") {
      return "text-first";
    }
    if (containsKeyword(String(mobilePattern || ""), ["horizontal-to-vertical", "stack"])) {
      return "text-first";
    }
    if (pair && pair.pc && pair.mo && pair.pc.layoutInfo && pair.mo.layoutInfo && pair.pc.layoutInfo.mode === "HORIZONTAL" && pair.mo.layoutInfo.mode === "VERTICAL") {
      return "text-first";
    }
    return "balanced";
  }

  function classifySectionReflowPattern(sectionType, pair, mobilePattern) {
    if (sectionType === "hero") {
      return "overlay-copy-bottom-visual";
    }
    if (sectionType === "promo") {
      return "meta-title-body-product";
    }
    if (sectionType === "article" || sectionType === "editorial") {
      return "title-body-media";
    }
    if (containsKeyword(String(mobilePattern || ""), ["horizontal-to-vertical"])) {
      return "horizontal-to-vertical";
    }
    if (pair && pair.pc && pair.mo && pair.pc.layoutInfo && pair.mo.layoutInfo && pair.pc.layoutInfo.mode === "HORIZONTAL" && pair.mo.layoutInfo.mode === "VERTICAL") {
      return "horizontal-to-vertical";
    }
    return "mobile-fit";
  }

  function buildSectionTextLayoutGuidance(sectionType, mobilePattern, heroGuidance) {
    if (sectionType === "hero") {
      return {
        alignment: heroGuidance && heroGuidance.textAlignment ? heroGuidance.textAlignment : "center",
        rewrapRequired: true,
        headlineMaxWidthRatio: 0.88,
        preferredLineBreakCount: 3,
        maxLineCount: {
          eyebrow: 2,
          headline: 4,
          subtitle: 3,
          body: 5,
        },
        minFontSize: {
          headline: 40,
          subtitle: 22,
          body: 16,
          meta: 14,
        },
      };
    }
    if (sectionType === "promo") {
      return {
        alignment: "center",
        rewrapRequired: true,
        headlineMaxWidthRatio: 0.9,
        preferredLineBreakCount: 3,
        maxLineCount: {
          meta: 2,
          headline: 4,
          subtitle: 3,
          body: 5,
        },
        minFontSize: {
          headline: 28,
          subtitle: 18,
          body: 16,
          meta: 14,
        },
      };
    }
    if (sectionType === "article" || sectionType === "editorial") {
      return {
        alignment: "left",
        rewrapRequired: containsKeyword(String(mobilePattern || ""), ["stack"]),
        headlineMaxWidthRatio: 0.92,
        preferredLineBreakCount: 3,
        maxLineCount: {
          meta: 2,
          headline: 4,
          subtitle: 3,
          body: 7,
        },
        minFontSize: {
          headline: 24,
          subtitle: 18,
          body: 15,
          meta: 13,
        },
      };
    }
    return {
      alignment: "left",
      rewrapRequired: false,
      headlineMaxWidthRatio: 0.92,
      preferredLineBreakCount: 2,
      maxLineCount: {
        headline: 4,
        subtitle: 3,
        body: 5,
        meta: 2,
      },
      minFontSize: {
        headline: 22,
        subtitle: 16,
        body: 14,
        meta: 12,
      },
    };
  }

  function buildSectionVisualRoleGuidance(sectionType, mobilePattern) {
    if (sectionType === "hero") {
      return {
        primary: "background",
        focal: "product",
        anchor: "bottom",
      };
    }
    if (sectionType === "promo") {
      return {
        primary: "product",
        focal: "product",
        anchor: "bottom",
      };
    }
    if (sectionType === "article" || sectionType === "editorial") {
      return {
        primary: "supporting",
        focal: "supporting",
        anchor: containsKeyword(String(mobilePattern || ""), ["stack"]) ? "after-copy" : "inline",
      };
    }
    return {
      primary: "supporting",
      focal: "supporting",
      anchor: "inline",
    };
  }

  function buildSectionMobileAspectPreference(sectionType, pair, mobilePattern) {
    const moBounds = pair && pair.mo && pair.mo.bounds ? pair.mo.bounds : null;
    const width = moBounds ? numeric(moBounds.width) : 0;
    const height = moBounds ? numeric(moBounds.height) : 0;
    let preferredRatio = width > 0 && height > 0 ? roundPixel(height / width) : 0;

    if (!preferredRatio || !isFinite(preferredRatio)) {
      if (sectionType === "hero") {
        preferredRatio = 1.48;
      } else if (sectionType === "promo") {
        preferredRatio = 1.18;
      } else if (containsKeyword(String(mobilePattern || ""), ["stack"])) {
        preferredRatio = 1.02;
      } else {
        preferredRatio = 0.86;
      }
    }

    const minRatio = roundPixel(Math.max(0.7, preferredRatio - 0.24));
    const maxRatio = roundPixel(Math.min(2.4, preferredRatio + 0.32));
    return {
      preferredRatio: preferredRatio,
      minRatio: minRatio,
      maxRatio: maxRatio,
    };
  }

  function buildSectionCropSafeZone(sectionType, pair, heroGuidance, contentPriority) {
    const overlayPosition = string(heroGuidance && heroGuidance.overlayPosition, "");
    const horizontalBias = overlayPosition.indexOf("left") >= 0 ? "left" : overlayPosition.indexOf("right") >= 0 ? "right" : "center";
    const preserveTargets = Array.isArray(heroGuidance && heroGuidance.focalTargets) && heroGuidance.focalTargets.length
      ? heroGuidance.focalTargets.slice(0, 4)
      : sectionType === "hero"
        ? ["headline", "product"]
        : contentPriority === "text-first"
          ? ["headline", "body"]
          : ["product"];
    return {
      horizontalBias: sectionType === "hero" ? horizontalBias : "center",
      verticalBias: sectionType === "hero" ? "top" : contentPriority === "text-first" ? "top" : "center",
      preserveTargets: preserveTargets,
      allowBackgroundTrim: sectionType === "hero" || sectionType === "visual",
    };
  }

  function buildSectionDropRules(sectionType, pair, mobilePattern, pcStats, moStats, contentPriority, reflowPattern) {
    return {
      hideDecorative: countDecorativeLeafNodes(pair && pair.pc ? pair.pc.node : null) >= 3,
      hideDesktopOnlyElements: sectionType === "header" || sectionType === "footer",
      hideRedundantMeta: !!(pcStats && moStats && pcStats.textNodes > moStats.textNodes + 1),
      prioritizeTextOverVisual: contentPriority === "text-first",
      collapseHorizontalLayout: reflowPattern === "horizontal-to-vertical" || containsKeyword(String(mobilePattern || ""), ["stack", "vertical"]),
      preserveAllText: true,
    };
  }

  function collectOrderedSectionTextEntries(rootNode) {
    const entries = [];
    walk(rootNode);
    entries.sort(compareBoundsForReadingOrder);
    return entries;

    function walk(current) {
      if (!current) {
        return;
      }
      if (String(current.type || "") === "TEXT" && typeof current.characters === "string") {
        const text = current.characters.replace(/\s+/g, " ").trim();
        const bounds = getNodeBounds(current);
        if (text && bounds) {
          entries.push({
            node: current,
            name: safeName(current),
            text: text,
            bounds: bounds,
            fontSize: getNodeRepresentativeFontSize(current),
          });
        }
      }
      if (!hasChildren(current)) {
        return;
      }
      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function classifySectionTextRole(entry, sectionType, index, totalCount) {
    const name = canonicalizeName(entry && entry.name ? entry.name : "");
    const text = String(entry && entry.text ? entry.text : "");
    const fontSize = numeric(entry && entry.fontSize);

    if (containsKeyword(name, ["cta", "button", "shop", "buy", "copy"])) {
      return "cta";
    }
    if (containsKeyword(name, ["meta", "date", "eyebrow", "label", "tag"])) {
      return "meta";
    }
    if (containsKeyword(name, ["headline", "title", "hero", "kv"])) {
      return index === 0 && sectionType === "hero" ? "eyebrow" : "headline";
    }
    if (containsKeyword(name, ["subtitle", "sub title", "subheading"])) {
      return "subtitle";
    }
    if (fontSize >= 42) {
      return index === 0 && sectionType === "hero" && totalCount >= 3 ? "eyebrow" : "headline";
    }
    if (fontSize >= 24) {
      return index <= 1 ? "headline" : "subtitle";
    }
    if (fontSize >= 18 && text.length <= 40) {
      return index === 0 ? "meta" : "subtitle";
    }
    return "body";
  }

  function compareBoundsForReadingOrder(left, right) {
    const leftBounds = left && left.bounds ? left.bounds : null;
    const rightBounds = right && right.bounds ? right.bounds : null;
    if (!leftBounds || !rightBounds) {
      return 0;
    }
    const yDiff = numeric(leftBounds.y) - numeric(rightBounds.y);
    if (Math.abs(yDiff) > 6) {
      return yDiff;
    }
    const xDiff = numeric(leftBounds.x) - numeric(rightBounds.x);
    if (Math.abs(xDiff) > 4) {
      return xDiff;
    }
    return 0;
  }

  function countDecorativeLeafNodes(rootNode) {
    let count = 0;
    walk(rootNode);
    return count;

    function walk(current) {
      if (!current) {
        return;
      }
      if (!hasChildren(current) && DECORATIVE_LEAF_TYPES[String(current.type || "")]) {
        count += 1;
      }
      if (!hasChildren(current)) {
        return;
      }
      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function uniqueValues(values) {
    const out = [];
    const seen = new Set();
    const list = Array.isArray(values) ? values : [];
    for (let index = 0; index < list.length; index += 1) {
      const value = list[index];
      if (!value || seen.has(value)) {
        continue;
      }
      seen.add(value);
      out.push(value);
    }
    return out;
  }

  function buildHeroFocalTargets(moNode, pcNode) {
    const out = ["headline"];
    if (findNamedNode(moNode, ["refrigerator", "fridge", "instaview", "product", "oled", "tv", "monitor", "device"]) ||
        findNamedNode(pcNode, ["refrigerator", "fridge", "instaview", "product", "oled", "tv", "monitor", "device"])) {
      out.push("product");
    }
    if (findNamedNode(moNode, ["person", "model", "woman", "man", "people", "hand", "face", "arm"]) ||
        findNamedNode(pcNode, ["person", "model", "woman", "man", "people", "hand", "face", "arm"])) {
      out.push("person");
    }
    return out.slice(0, 3);
  }

  function findHeroCopyBlockMeta(rootNode) {
    const rootBounds = getNodeBounds(rootNode);
    if (!rootNode || !rootBounds) {
      return null;
    }

    let best = null;
    let bestScore = 0;
    walk(rootNode);
    return best;

    function walk(current) {
      if (!current || !hasChildren(current)) {
        return;
      }

      const bounds = getNodeBounds(current);
      if (bounds) {
        const info = measureHeroCopyCandidate(current, bounds, rootBounds);
        if (info && info.score > bestScore) {
          best = info;
          bestScore = info.score;
        }
      }

      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function measureHeroCopyCandidate(node, bounds, rootBounds) {
    const textStats = collectNodeTextStats(node);
    if (textStats.textCount < 2 || textStats.totalLength < 18) {
      return null;
    }

    const relativeX = (numeric(bounds.x) - numeric(rootBounds.x)) / Math.max(1, numeric(rootBounds.width));
    const relativeY = (numeric(bounds.y) - numeric(rootBounds.y)) / Math.max(1, numeric(rootBounds.height));
    const areaRatio = (numeric(bounds.width) * numeric(bounds.height)) / Math.max(1, numeric(rootBounds.width) * numeric(rootBounds.height));
    if (relativeY > 0.58 || relativeX > 0.62 || areaRatio <= 0 || areaRatio >= 0.58) {
      return null;
    }

    const corpus = String((node && node.name) || "").toLowerCase();
    let score = textStats.textCount * 1.8 + Math.min(1.2, textStats.totalLength / 80);
    if (containsKeyword(corpus, ["copy", "text", "title", "headline", "hero", "kv", "visual"])) {
      score += 1;
    }
    if (relativeY <= 0.18) {
      score += 0.5;
    }
    if (relativeX <= 0.18) {
      score += 0.35;
    }

    return {
      nodeId: String(node.id || ""),
      overlayPosition: relativeX <= 0.22 ? "top-left" : relativeX >= 0.58 ? "top-right" : "top-center",
      textCount: textStats.textCount,
      score: roundConfidence(score / 8),
    };
  }

  function collectNodeTextStats(rootNode) {
    const stats = {
      textCount: 0,
      totalLength: 0,
    };

    walk(rootNode);
    return stats;

    function walk(current) {
      if (!current) {
        return;
      }
      if (String(current.type || "") === "TEXT" && typeof current.characters === "string") {
        const text = current.characters.replace(/\s+/g, " ").trim();
        if (text) {
          stats.textCount += 1;
          stats.totalLength += text.length;
        }
      }
      if (!hasChildren(current)) {
        return;
      }
      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function findNamedNode(rootNode, keywords) {
    let found = false;
    walk(rootNode);
    return found;

    function walk(current) {
      if (!current || found) {
        return;
      }
      if (containsKeyword(String((current && current.name) || "").toLowerCase(), keywords)) {
        found = true;
        return;
      }
      if (!hasChildren(current)) {
        return;
      }
      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function isLargeVisualSectionPair(pair) {
    const pcBounds = pair && pair.pc ? pair.pc.bounds : null;
    const moBounds = pair && pair.mo ? pair.mo.bounds : null;
    const pcLarge = !!(pcBounds && pcBounds.width >= 640 && pcBounds.height >= 260);
    const moLarge = !!(moBounds && moBounds.width >= 280 && moBounds.height >= 180);
    const pcWide = pair && pair.pc && pair.pc.relativeBounds ? pair.pc.relativeBounds.widthRatio >= 0.68 : false;
    const moWide = pair && pair.mo && pair.mo.relativeBounds ? pair.mo.relativeBounds.widthRatio >= 0.82 : false;
    return (pcLarge || moLarge) && (pcWide || moWide);
  }

  function containsKeyword(text, keywords) {
    const corpus = typeof text === "string" ? text : "";
    for (let index = 0; index < keywords.length; index += 1) {
      if (corpus.indexOf(String(keywords[index]).toLowerCase()) >= 0) {
        return true;
      }
    }
    return false;
  }

  function buildRuleRecords(classifiedPair, pairId, createdAt, matching) {
    const records = [];
    const pcLayout = getLayoutInfo(classifiedPair.pc.node);
    const moLayout = getLayoutInfo(classifiedPair.mo.node);
    const widthRatio = roundRatio(classifiedPair.mo.width / Math.max(1, classifiedPair.pc.width));
    const pcAspectRatio = getFrameAspectRatio(classifiedPair.pc.width, classifiedPair.pc.height);
    const moAspectRatio = getFrameAspectRatio(classifiedPair.mo.width, classifiedPair.mo.height);
    const pcShape = classifyFrameShape(pcAspectRatio);
    const moShape = classifyFrameShape(moAspectRatio);
    const rootChildCount = Array.isArray(classifiedPair && classifiedPair.pc && classifiedPair.pc.node && classifiedPair.pc.node.children)
      ? classifiedPair.pc.node.children.filter(Boolean).length
      : 0;
    const rootRuleContext = {
      rootSourceShape: pcShape,
      rootSourceAspectBucket: bucketFrameAspectRatio(pcAspectRatio),
      rootSourceLayoutMode: pcLayout.mode,
      rootSectionCountBucket: bucketFrameSectionCount(rootChildCount),
      rootTargetWidthBucket: bucketFrameWidth(classifiedPair.mo.width),
    };

    records.push(
      createRuleRecord(
        pairId,
        createdAt,
        "viewport-width",
        "root",
        1,
        "deterministic",
        `${classifiedPair.pc.width}px viewport to ${classifiedPair.mo.width}px viewport`,
        {
          pcWidth: classifiedPair.pc.width,
          moWidth: classifiedPair.mo.width,
          widthRatio: widthRatio,
          rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
        }
      )
    );

    if (pcAspectRatio && moAspectRatio && Math.abs(pcAspectRatio - moAspectRatio) >= 0.08) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "frame-aspect-ratio",
          "root",
          0.94,
          "deterministic",
          `frame aspect ${pcAspectRatio} to ${moAspectRatio}`,
          {
            pcAspectRatio: pcAspectRatio,
            moAspectRatio: moAspectRatio,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    if (pcShape !== moShape) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "frame-shape",
          "root",
          0.96,
          "deterministic",
          `${pcShape} frame to ${moShape} frame`,
          {
            pcShape: pcShape,
            moShape: moShape,
            pcAspectRatio: pcAspectRatio,
            moAspectRatio: moAspectRatio,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    if (pcLayout.mode !== moLayout.mode && (pcLayout.mode !== "NONE" || moLayout.mode !== "NONE")) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "layout-axis",
          "root",
          0.94,
          "deterministic",
          `${pcLayout.mode.toLowerCase()} layout to ${moLayout.mode.toLowerCase()} layout`,
          {
            pcMode: pcLayout.mode,
            moMode: moLayout.mode,
            pcSource: pcLayout.source,
            moSource: moLayout.source,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    if (pcLayout.gap || moLayout.gap) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "gap-scale",
          "root",
          0.86,
          "deterministic",
          `gap ${pcLayout.gap}px to ${moLayout.gap}px`,
          {
            pcGap: pcLayout.gap,
            moGap: moLayout.gap,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    if (pcLayout.paddingX || pcLayout.paddingY || moLayout.paddingX || moLayout.paddingY) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "padding-scale",
          "root",
          0.84,
          "deterministic",
          `padding ${pcLayout.paddingX}x${pcLayout.paddingY}px to ${moLayout.paddingX}x${moLayout.paddingY}px`,
          {
            pcPaddingX: pcLayout.paddingX,
            pcPaddingY: pcLayout.paddingY,
            moPaddingX: moLayout.paddingX,
            moPaddingY: moLayout.paddingY,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    const pcColumns = estimateColumnCount(classifiedPair.pc.node);
    const moColumns = estimateColumnCount(classifiedPair.mo.node);
    if (pcColumns && moColumns && pcColumns !== moColumns) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "estimated-columns",
          "root",
          0.8,
          "deterministic",
          `estimated columns ${pcColumns} to ${moColumns}`,
          {
            pcColumns: pcColumns,
            moColumns: moColumns,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    const pcFontSize = getRepresentativeFontSize(classifiedPair.pc.node);
    const moFontSize = getRepresentativeFontSize(classifiedPair.mo.node);
    if (pcFontSize && moFontSize && Math.abs(pcFontSize - moFontSize) >= 0.5) {
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "font-scale",
          "root",
          0.58,
          "deterministic",
          `representative font ${pcFontSize}px to ${moFontSize}px`,
          {
            pcFontSize: pcFontSize,
            moFontSize: moFontSize,
            rootSourceShape: rootRuleContext.rootSourceShape,
            rootSourceAspectBucket: rootRuleContext.rootSourceAspectBucket,
            rootSourceLayoutMode: rootRuleContext.rootSourceLayoutMode,
            rootSectionCountBucket: rootRuleContext.rootSectionCountBucket,
            rootTargetWidthBucket: rootRuleContext.rootTargetWidthBucket,
          }
        )
      );
    }

    appendMatchedNodeRules(
      records,
      matching && Array.isArray(matching.pairs) ? matching.pairs.filter((pair) => pair.tier === "confirmed") : [],
      pairId,
      createdAt
    );
    appendSectionPresenceRules(records, matching, pairId, createdAt);
    return records;
  }

  function buildAnalysisPreview(matching, sectionRecords, ruleRecords, aggregatePreview) {
    const pairs = matching && Array.isArray(matching.pairs) ? matching.pairs.slice() : [];
    const sections = Array.isArray(sectionRecords) ? sectionRecords.slice() : [];
    const rules = Array.isArray(ruleRecords) ? ruleRecords.slice() : [];
    const aggregate = aggregatePreview && typeof aggregatePreview === "object" ? aggregatePreview : {};
    const lowConfidenceMatches = pairs
      .filter((pair) => pair.score < 0.82)
      .sort((left, right) => left.score - right.score)
      .slice(0, 3)
      .map((pair) => `${summarizePairLabel(pair.pc, pair.mo)} (${roundConfidence(pair.score)})`);
    const representativeMatches = pairs
      .slice()
      .sort((left, right) => right.score - left.score)
      .slice(0, 3)
      .map((pair) => `${summarizePairLabel(pair.pc, pair.mo)} (${roundConfidence(pair.score)})`);
    const representativeRules = rules
      .slice()
      .sort((left, right) => {
        const leftConfidence = typeof left.confidence === "number" ? left.confidence : 0;
        const rightConfidence = typeof right.confidence === "number" ? right.confidence : 0;
        return rightConfidence - leftConfidence;
      })
      .slice(0, 4)
      .map((rule) => String(rule.summary || "").trim())
      .filter(Boolean);
    const representativeSections = sections
      .slice()
      .sort((left, right) => {
        const leftConfidence = typeof left.confidence === "number" ? left.confidence : 0;
        const rightConfidence = typeof right.confidence === "number" ? right.confidence : 0;
        return rightConfidence - leftConfidence;
      })
      .slice(0, 4)
      .map((record) => String(record.summary || "").trim())
      .filter(Boolean);
    const countsByRuleType = {};

    for (let index = 0; index < rules.length; index += 1) {
      const ruleType = typeof rules[index].ruleType === "string" ? rules[index].ruleType : "unknown";
      countsByRuleType[ruleType] = (countsByRuleType[ruleType] || 0) + 1;
    }

    return {
      lowConfidenceCount: matching && matching.stats ? matching.stats.lowConfidenceMatchCount : 0,
      lowConfidenceMatches: lowConfidenceMatches,
      representativeMatches: representativeMatches,
      representativeSections: representativeSections,
      representativeRules: representativeRules,
      countsByRuleType: countsByRuleType,
      repeatedRules: Array.isArray(aggregate.repeatedRules) ? aggregate.repeatedRules : [],
      conflictingRules: Array.isArray(aggregate.conflictingRules) ? aggregate.conflictingRules : [],
      lowConfidenceRules: Array.isArray(aggregate.lowConfidenceRules) ? aggregate.lowConfidenceRules : [],
      repeatedRuleCount: Number.isFinite(aggregate.repeatedRuleCount) ? aggregate.repeatedRuleCount : 0,
      conflictingRuleCount: Number.isFinite(aggregate.conflictingRuleCount) ? aggregate.conflictingRuleCount : 0,
      lowConfidenceRuleCount: Number.isFinite(aggregate.lowConfidenceRuleCount) ? aggregate.lowConfidenceRuleCount : 0,
    };
  }

  function appendMatchedNodeRules(records, pairs, pairId, createdAt) {
    const seen = new Set();
    const sortedPairs = pairs.slice().sort((left, right) => right.score - left.score);
    const maxDetailedRules = 48;

    for (let index = 0; index < sortedPairs.length; index += 1) {
      if (records.length >= maxDetailedRules) {
        return;
      }

      const pair = sortedPairs[index];
      appendVisibilityRule(pair);
      appendVariantRule(pair);
      appendAbsoluteAnchorRule(pair);
      appendSizingRule(pair);
    }

    appendChildReorderRules(sortedPairs);

    function appendVisibilityRule(pair) {
      if (pair.pc.visible === pair.mo.visible) {
        return;
      }

      const action = pair.pc.visible && !pair.mo.visible ? "hide" : "show";
      const nodeSignature = buildRuleNodeSignatureFromPair(pair);
      pushRule(
        `visibility|${nodeSignature}|${action}`,
        "visibility-change",
        deriveRuleScopeFromPair(pair),
        Math.max(0.72, pair.score),
        `${summarizePairLabel(pair.pc, pair.mo)} ${action} on mobile`,
        {
          fromNodeId: pair.pc.id,
          toNodeId: pair.mo.id,
          pcVisible: pair.pc.visible,
          moVisible: pair.mo.visible,
          nodeSignature: nodeSignature,
          nodeLabel: buildRuleNodeLabelFromPair(pair),
          parentSignature: buildParentRuleSignature(pair),
        }
      );
    }

    function appendVariantRule(pair) {
      const pcInfo = pair.pc.componentInfo;
      const moInfo = pair.mo.componentInfo;
      const sameFamily =
        (pcInfo.componentKey && pcInfo.componentKey === moInfo.componentKey) ||
        (pcInfo.componentFamily && pcInfo.componentFamily === moInfo.componentFamily);

      if (!sameFamily) {
        return;
      }

      const propertyChanged = pcInfo.propertySignature !== moInfo.propertySignature;
      const referenceChanged = pcInfo.referenceSignature !== moInfo.referenceSignature;
      if (!propertyChanged && !referenceChanged) {
        return;
      }

      const componentLabel = pcInfo.componentName || pair.pc.name || pair.mo.name || "component";
      pushRule(
        `variant|${pcInfo.componentKey || pcInfo.componentFamily}|${pcInfo.propertySignature}|${moInfo.propertySignature}|${pcInfo.referenceSignature}|${moInfo.referenceSignature}`,
        "variant-switch",
        deriveRuleScopeFromPair(pair),
        Math.max(0.78, pair.score),
        `${componentLabel} variant/property switch`,
        {
          fromNodeId: pair.pc.id,
          toNodeId: pair.mo.id,
          componentKey: pcInfo.componentKey || moInfo.componentKey,
          componentFamily: pcInfo.componentFamily || moInfo.componentFamily,
          pcPropertySignature: pcInfo.propertySignature,
          moPropertySignature: moInfo.propertySignature,
          pcReferenceSignature: pcInfo.referenceSignature,
          moReferenceSignature: moInfo.referenceSignature,
          nodeLabel: buildRuleNodeLabelFromPair(pair),
        }
      );
    }

    function appendAbsoluteAnchorRule(pair) {
      const pcBounds = pair.pc.relativeBounds;
      const moBounds = pair.mo.relativeBounds;
      if (!pcBounds || !moBounds) {
        return;
      }

      const hasAbsolute =
        pair.pc.layoutInfo.positioning === "ABSOLUTE" || pair.mo.layoutInfo.positioning === "ABSOLUTE";
      const sameHorizontal = pcBounds.horizontalAnchor === moBounds.horizontalAnchor;
      const sameVertical = pcBounds.verticalAnchor === moBounds.verticalAnchor;
      if (!hasAbsolute || !sameHorizontal || !sameVertical) {
        return;
      }

      const anchorLabel = `${pcBounds.horizontalAnchor}-${pcBounds.verticalAnchor}`;
      const nodeSignature = buildRuleNodeSignatureFromPair(pair);
      pushRule(
        `absolute|${anchorLabel}|${nodeSignature}`,
        "absolute-anchor",
        deriveRuleScopeFromPair(pair),
        Math.max(0.8, pair.score),
        `${summarizePairLabel(pair.pc, pair.mo)} absolute ${anchorLabel} anchor preserved`,
        {
          fromNodeId: pair.pc.id,
          toNodeId: pair.mo.id,
          anchor: anchorLabel,
          pcPositioning: pair.pc.layoutInfo.positioning,
          moPositioning: pair.mo.layoutInfo.positioning,
          nodeSignature: nodeSignature,
          nodeLabel: buildRuleNodeLabelFromPair(pair),
          parentSignature: buildParentRuleSignature(pair),
        }
      );
    }

    function appendSizingRule(pair) {
      const pcLayout = pair.pc.layoutInfo;
      const moLayout = pair.mo.layoutInfo;
      const sizingChanged =
        pcLayout.sizingHorizontal !== moLayout.sizingHorizontal ||
        pcLayout.sizingVertical !== moLayout.sizingVertical ||
        pcLayout.grow !== moLayout.grow;

      if (!sizingChanged) {
        return;
      }

      const nodeSignature = buildRuleNodeSignatureFromPair(pair);
      pushRule(
        `sizing|${nodeSignature}|${pcLayout.sizingHorizontal}|${pcLayout.sizingVertical}|${pcLayout.grow}|${moLayout.sizingHorizontal}|${moLayout.sizingVertical}|${moLayout.grow}`,
        "sizing-mode",
        deriveRuleScopeFromPair(pair),
        Math.max(0.7, pair.score),
        `${summarizePairLabel(pair.pc, pair.mo)} sizing ${formatSizingLabel(pcLayout)} -> ${formatSizingLabel(moLayout)}`,
        {
          fromNodeId: pair.pc.id,
          toNodeId: pair.mo.id,
          pcSizingHorizontal: pcLayout.sizingHorizontal,
          pcSizingVertical: pcLayout.sizingVertical,
          pcGrow: pcLayout.grow,
          moSizingHorizontal: moLayout.sizingHorizontal,
          moSizingVertical: moLayout.sizingVertical,
          moGrow: moLayout.grow,
          nodeSignature: nodeSignature,
          nodeLabel: buildRuleNodeLabelFromPair(pair),
          parentSignature: buildParentRuleSignature(pair),
          nodeType: pair.pc.type || pair.mo.type,
        }
      );
    }

    function appendChildReorderRules(confirmedPairs) {
      const groups = new Map();

      for (let index = 0; index < confirmedPairs.length; index += 1) {
        const pair = confirmedPairs[index];
        if (!pair || !pair.pc || !pair.mo || !pair.pc.parentId || !pair.mo.parentId) {
          continue;
        }

        const groupKey = `${pair.pc.parentId}|${pair.mo.parentId}`;
        if (!groups.has(groupKey)) {
          groups.set(groupKey, []);
        }
        groups.get(groupKey).push(pair);
      }

      groups.forEach((groupPairs) => {
        if (records.length >= maxDetailedRules || !Array.isArray(groupPairs) || groupPairs.length < 2) {
          return;
        }

        const reorder = buildChildReorderSummary(groupPairs);
        if (!reorder) {
          return;
        }

        const referencePair = groupPairs[0];
        const scope = deriveParentRuleScope(referencePair);
        const parentSignature = buildParentRuleSignature(referencePair);
        const parentLabel = summarizeScopeLabel(scope);
        pushRule(
          `child-reorder|${parentSignature}|${reorder.moOrderPattern}`,
          "child-reorder",
          scope,
          Math.max(0.8, reorder.confidence),
          `${parentLabel} child order reorders on mobile`,
          {
            parentPcId: referencePair.pc.parentId,
            parentMoId: referencePair.mo.parentId,
            parentSignature: parentSignature,
            matchedChildCount: reorder.matchedChildCount,
            movedCount: reorder.movedCount,
            inversionRatio: reorder.inversionRatio,
            averageShift: reorder.averageShift,
            pcOrderPattern: reorder.pcOrderPattern,
            moOrderPattern: reorder.moOrderPattern,
            pcOrderPreview: reorder.pcOrderPreview,
            moOrderPreview: reorder.moOrderPreview,
          }
        );
      });
    }

    function buildChildReorderSummary(groupPairs) {
      const sortablePairs = [];

      for (let index = 0; index < groupPairs.length; index += 1) {
        const pair = groupPairs[index];
        if (!pair || isWeakReorderPair(pair)) {
          continue;
        }
        sortablePairs.push(pair);
      }

      if (sortablePairs.length < 2) {
        return null;
      }

      const pcSorted = sortablePairs.slice().sort((left, right) => left.pc.orderIndex - right.pc.orderIndex);
      const moSorted = sortablePairs.slice().sort((left, right) => left.mo.orderIndex - right.mo.orderIndex);
      const moPositionByKey = new Map();
      const pcOrderPreview = [];
      const moOrderPreview = [];

      for (let index = 0; index < moSorted.length; index += 1) {
        moPositionByKey.set(`${moSorted[index].pc.id}|${moSorted[index].mo.id}`, index);
        moOrderPreview.push(buildReorderPreviewToken(moSorted[index], index));
      }

      if (!moPositionByKey.size) {
        return null;
      }

      let movedCount = 0;
      let totalShift = 0;
      let confidenceTotal = 0;
      const moRanksInPcOrder = [];

      for (let index = 0; index < pcSorted.length; index += 1) {
        const pair = pcSorted[index];
        const pairKey = `${pair.pc.id}|${pair.mo.id}`;
        const moPosition = moPositionByKey.get(pairKey);
        if (!Number.isFinite(moPosition)) {
          continue;
        }

        pcOrderPreview.push(buildReorderPreviewToken(pair, index));
        moRanksInPcOrder.push(moPosition);
        confidenceTotal += pair.score;
        if (moPosition !== index) {
          movedCount += 1;
          totalShift += Math.abs(moPosition - index);
        }
      }

      if (pcOrderPreview.join("|") === moOrderPreview.join("|")) {
        return null;
      }

      const averageShift = movedCount ? totalShift / movedCount : 0;
      const inversionRatio = computeInversionRatio(moRanksInPcOrder);
      if (movedCount < 2 && inversionRatio < 0.34 && averageShift < 1) {
        return null;
      }

      const averagePairConfidence = confidenceTotal / Math.max(1, pcSorted.length);
      const reorderStrength = clampScore(inversionRatio * 0.7 + clampScore(averageShift / Math.max(1, pcSorted.length - 1)) * 0.3);

      return {
        matchedChildCount: pcSorted.length,
        movedCount: movedCount,
        inversionRatio: roundConfidence(inversionRatio),
        averageShift: roundRatio(averageShift),
        pcOrderPattern: buildSequentialPattern(pcSorted.length),
        moOrderPattern: moRanksInPcOrder.join(">"),
        pcOrderPreview: limitPreviewTokens(pcOrderPreview),
        moOrderPreview: limitPreviewTokens(moOrderPreview),
        confidence: clampScore(averagePairConfidence * 0.78 + reorderStrength * 0.22),
      };
    }

    function isWeakReorderPair(pair) {
      if (!pair || !pair.pc || !pair.mo) {
        return true;
      }

      const pcToken = getDescriptorIdentityToken(pair.pc);
      const moToken = getDescriptorIdentityToken(pair.mo);
      const genericPair =
        (!pcToken && !moToken) ||
        (isGenericNamedDescriptor(pair.pc) && isGenericNamedDescriptor(pair.mo) && !pair.pc.textFingerprint && !pair.mo.textFingerprint);
      return genericPair && !pair.pc.componentInfo.componentKey && !pair.mo.componentInfo.componentKey;
    }

    function buildReorderPreviewToken(pair, index) {
      const token = getDescriptorIdentityToken(pair.pc) || getDescriptorIdentityToken(pair.mo) || summarizePairLabel(pair.pc, pair.mo) || "child";
      return `${token}#${index + 1}`;
    }

    function deriveParentRuleScope(pair) {
      if (pair.pc.parentSemanticSegment && !pair.pc.parentSemanticSegment.startsWith("unknown")) {
        return pair.pc.parentSemanticSegment;
      }
      if (pair.pc.topLevelSemanticSegment) {
        return pair.pc.topLevelSemanticSegment;
      }
      return deriveRuleScopeFromPair(pair);
    }

    function buildParentRuleSignature(pair) {
      return normalizeAggregateToken(
        pair.pc.parentSemanticPath || pair.mo.parentSemanticPath || pair.pc.topLevelSemanticPath || pair.mo.topLevelSemanticPath || pair.pc.parentId
      );
    }

    function buildRuleNodeSignatureFromPair(pair) {
      const componentKey =
        pair.pc.componentInfo.componentKey ||
        pair.mo.componentInfo.componentKey ||
        pair.pc.componentInfo.componentFamily ||
        pair.mo.componentInfo.componentFamily;
      if (componentKey) {
        return normalizeAggregateToken(componentKey);
      }

      const textFingerprint = pair.pc.textFingerprint || pair.mo.textFingerprint;
      if (textFingerprint) {
        return normalizeAggregateToken(textFingerprint.split(" ").slice(0, 4).join(" "));
      }

      const stableName = !isGenericNamedDescriptor(pair.pc)
        ? pair.pc.canonicalName
        : !isGenericNamedDescriptor(pair.mo)
        ? pair.mo.canonicalName
        : "";
      if (stableName) {
        return normalizeAggregateToken(stableName);
      }

      return normalizeAggregateToken(pair.pc.semanticPath || pair.mo.semanticPath || pair.pc.type || "node");
    }

    function buildRuleNodeLabelFromPair(pair) {
      return summarizePairLabel(pair.pc, pair.mo) || pair.pc.type || pair.mo.type || "node";
    }

    function computeInversionRatio(orderValues) {
      if (!Array.isArray(orderValues) || orderValues.length < 2) {
        return 0;
      }

      let inversions = 0;
      let comparisons = 0;
      for (let leftIndex = 0; leftIndex < orderValues.length; leftIndex += 1) {
        for (let rightIndex = leftIndex + 1; rightIndex < orderValues.length; rightIndex += 1) {
          comparisons += 1;
          if (orderValues[leftIndex] > orderValues[rightIndex]) {
            inversions += 1;
          }
        }
      }

      return comparisons ? inversions / comparisons : 0;
    }

    function buildSequentialPattern(count) {
      const values = [];
      for (let index = 0; index < count; index += 1) {
        values.push(String(index));
      }
      return values.join(">");
    }

    function limitPreviewTokens(tokens) {
      return Array.isArray(tokens) ? tokens.slice(0, 6) : [];
    }

    function pushRule(signature, ruleType, scope, confidence, summary, payload) {
      if (seen.has(signature)) {
        return;
      }
      seen.add(signature);
      records.push(createRuleRecord(pairId, createdAt, ruleType, scope, confidence, "deterministic", summary, payload));
    }
  }

  function deriveRuleScopeFromPair(pair) {
    if (pair.pc.parentSemanticSegment && !pair.pc.parentSemanticSegment.startsWith("unknown")) {
      return pair.pc.parentSemanticSegment;
    }
    if (pair.pc.semanticSegment) {
      return pair.pc.semanticSegment;
    }
    return "node";
  }

  function summarizeScopeLabel(scope) {
    const text = typeof scope === "string" ? scope.replace(/[-_/]+/g, " ").trim() : "";
    return text || "section";
  }

  function appendSectionPresenceRules(records, matching, pairId, createdAt) {
    const seen = new Set();
    const pcUnmatched = matching && Array.isArray(matching.pcUnmatchedDescriptors) ? matching.pcUnmatchedDescriptors : [];
    const moUnmatched = matching && Array.isArray(matching.moUnmatchedDescriptors) ? matching.moUnmatchedDescriptors : [];
    const pcSections = collectUnmatchedTopLevelSections(pcUnmatched);
    const moSections = collectUnmatchedTopLevelSections(moUnmatched);
    const sharedSectionSignatures = new Set();

    for (let pcIndex = 0; pcIndex < pcSections.length; pcIndex += 1) {
      const pcSignature = buildSectionSignature(pcSections[pcIndex]);
      if (!pcSignature) {
        continue;
      }

      for (let moIndex = 0; moIndex < moSections.length; moIndex += 1) {
        if (pcSignature === buildSectionSignature(moSections[moIndex])) {
          sharedSectionSignatures.add(pcSignature);
          break;
        }
      }
    }

    for (let index = 0; index < pcSections.length; index += 1) {
      pushSectionRule(pcSections[index], "collapse");
    }

    for (let index = 0; index < moSections.length; index += 1) {
      pushSectionRule(moSections[index], "expand");
    }

    function pushSectionRule(descriptor, action) {
      const sectionSignature = buildSectionSignature(descriptor);
      const signature = `section-presence|${sectionSignature}|${action}`;
      if (!sectionSignature || seen.has(signature) || sharedSectionSignatures.has(sectionSignature)) {
        return;
      }

      seen.add(signature);
      records.push(
        createRuleRecord(
          pairId,
          createdAt,
          "section-presence",
          descriptor.semanticSegment || "section",
          calculateSectionPresenceConfidence(descriptor),
          "deterministic",
          action === "collapse"
            ? `${summarizeSectionLabel(descriptor)} section collapses on mobile`
            : `${summarizeSectionLabel(descriptor)} section expands on mobile`,
          {
            action: action,
            nodeId: descriptor.id,
            sectionSignature: sectionSignature,
            sectionLabel: summarizeSectionLabel(descriptor),
            sectionSemanticPath: descriptor.semanticPath,
            topLevelSemanticPath: descriptor.topLevelSemanticPath,
            childCount: descriptor.childCount,
            meaningfulChildCount: descriptor.meaningfulChildCount,
            layoutMode: descriptor.layoutInfo ? descriptor.layoutInfo.mode : "",
          }
        )
      );
    }
  }

  function collectUnmatchedTopLevelSections(descriptors) {
    const result = [];

    for (let index = 0; index < descriptors.length; index += 1) {
      const descriptor = descriptors[index];
      if (!descriptor || descriptor.depth !== 1) {
        continue;
      }
      if (!isSectionLikeDescriptor(descriptor)) {
        continue;
      }
      result.push(descriptor);
    }

    return result;
  }

  function isSectionLikeDescriptor(descriptor) {
    if (!descriptor || descriptor.visible === false) {
      return false;
    }

    if (!descriptor.hasChildren || descriptor.childCount < 1) {
      return false;
    }

    if (descriptor.meaningfulChildCount >= 2) {
      return true;
    }

    if (descriptor.componentInfo.componentKey || descriptor.componentInfo.componentFamily) {
      return true;
    }

    if (descriptor.canonicalName && !isGenericNamedDescriptor(descriptor)) {
      return true;
    }

    return !!descriptor.childSemanticSignature;
  }

  function buildSectionSignature(descriptor) {
    if (!descriptor) {
      return "";
    }

    return normalizeAggregateToken(
      descriptor.componentInfo.componentFamily ||
        descriptor.canonicalName ||
        descriptor.childSemanticSignature ||
        descriptor.semanticSegment ||
        descriptor.topLevelSemanticSegment
    );
  }

  function summarizeSectionLabel(descriptor) {
    if (!descriptor) {
      return "section";
    }

    return (
      descriptor.componentInfo.componentName ||
      descriptor.name ||
      descriptor.canonicalName ||
      descriptor.semanticSegment ||
      descriptor.topLevelSemanticSegment ||
      "section"
    );
  }

  function calculateSectionPresenceConfidence(descriptor) {
    const scores = [];
    scores.push(descriptor.meaningfulChildCount >= 2 ? 0.94 : descriptor.meaningfulChildCount === 1 ? 0.84 : 0.74);
    scores.push(descriptor.componentInfo.componentKey || descriptor.componentInfo.componentFamily ? 0.92 : 0.76);
    scores.push(descriptor.canonicalName && !isGenericNamedDescriptor(descriptor) ? 0.88 : 0.72);
    scores.push(descriptor.layoutInfo && descriptor.layoutInfo.mode !== "NONE" ? 0.86 : 0.76);
    return roundConfidence(clampScore(averageScore(scores)));
  }

  function summarizePairLabel(pcDescriptor, moDescriptor) {
    const pcLabel = pcDescriptor && pcDescriptor.canonicalName ? pcDescriptor.canonicalName : pcDescriptor ? pcDescriptor.name : "";
    const moLabel = moDescriptor && moDescriptor.canonicalName ? moDescriptor.canonicalName : moDescriptor ? moDescriptor.name : "";
    const base = pcLabel || moLabel || "node";
    return String(base).replace(/\s+/g, " ").trim();
  }

  function formatSizingLabel(layoutInfo) {
    const horizontal = layoutInfo && layoutInfo.sizingHorizontal ? layoutInfo.sizingHorizontal.toLowerCase() : "none";
    const vertical = layoutInfo && layoutInfo.sizingVertical ? layoutInfo.sizingVertical.toLowerCase() : "none";
    const grow = layoutInfo && layoutInfo.grow ? "+grow" : "";
    return `${horizontal}/${vertical}${grow}`;
  }

  function createRuleRecord(pairId, createdAt, ruleType, scope, confidence, source, summary, payload) {
    return {
      type: "rule",
      id: createRecordId("rule"),
      version: PATCH_VERSION,
      pairId: pairId,
      direction: "pc-to-mo",
      ruleType: ruleType,
      scope: scope,
      confidence: roundConfidence(confidence),
      source: source,
      summary: summary,
      payload: payload,
      createdAt: createdAt,
    };
  }

  function buildAggregateRuleRecords(currentStore, currentRuleRecords, currentPairRecord, createdAt) {
    const existingRecords = currentStore && Array.isArray(currentStore.records) ? currentStore.records : [];
    const allRuleRecords = [];
    const pairSignatureById = new Map();

    for (let index = 0; index < existingRecords.length; index += 1) {
      const existingRecord = existingRecords[index];
      if (!existingRecord || existingRecord.type !== "pair") {
        continue;
      }

      pairSignatureById.set(
        existingRecord.id,
        typeof existingRecord.selectionSignature === "string" && existingRecord.selectionSignature ? existingRecord.selectionSignature : existingRecord.id
      );
    }

    if (currentPairRecord && currentPairRecord.id) {
      pairSignatureById.set(
        currentPairRecord.id,
        typeof currentPairRecord.selectionSignature === "string" && currentPairRecord.selectionSignature ? currentPairRecord.selectionSignature : currentPairRecord.id
      );
    }

    for (let index = 0; index < existingRecords.length; index += 1) {
      if (existingRecords[index] && existingRecords[index].type === "rule") {
        allRuleRecords.push(existingRecords[index]);
      }
    }

    for (let index = 0; index < currentRuleRecords.length; index += 1) {
      allRuleRecords.push(currentRuleRecords[index]);
    }

    const aggregateMap = new Map();
    const bucketMembers = new Map();

    for (let index = 0; index < allRuleRecords.length; index += 1) {
      const normalized = normalizeRuleForAggregation(allRuleRecords[index], pairSignatureById);
      if (!normalized) {
        continue;
      }

      if (!aggregateMap.has(normalized.canonicalKey)) {
        aggregateMap.set(normalized.canonicalKey, {
          type: "aggregate-rule",
          id: createAggregateRuleId(normalized.canonicalKey),
          version: AGGREGATE_RULE_VERSION,
          canonicalKey: normalized.canonicalKey,
          conflictBucketKey: normalized.conflictBucketKey,
          direction: normalized.direction,
          ruleType: normalized.ruleType,
          scope: normalized.scope,
          summary: normalized.summary,
          supportCount: 0,
          avgConfidence: 0,
          maxConfidence: 0,
          minConfidence: 1,
          supportConfidenceMap: new Map(),
          pairIds: [],
          exampleRuleIds: [],
          status: "single",
          conflictCount: 0,
          createdAt: createdAt,
          updatedAt: createdAt,
        });
      }

      const aggregateRecord = aggregateMap.get(normalized.canonicalKey);
      aggregateRecord.supportConfidenceMap.set(normalized.pairSupportKey, normalized.confidence);
      aggregateRecord.supportCount = aggregateRecord.supportConfidenceMap.size;
      aggregateRecord.avgConfidence = roundConfidence(computeAverageConfidence(aggregateRecord.supportConfidenceMap));
      aggregateRecord.maxConfidence = roundConfidence(Math.max(aggregateRecord.maxConfidence, normalized.confidence));
      aggregateRecord.minConfidence = roundConfidence(Math.min(aggregateRecord.minConfidence, normalized.confidence));
      aggregateRecord.updatedAt = createdAt;
      if (aggregateRecord.pairIds.indexOf(normalized.pairId) < 0) {
        aggregateRecord.pairIds.push(normalized.pairId);
      }
      if (aggregateRecord.exampleRuleIds.length < 8 && aggregateRecord.exampleRuleIds.indexOf(normalized.ruleId) < 0) {
        aggregateRecord.exampleRuleIds.push(normalized.ruleId);
      }

      if (!bucketMembers.has(normalized.conflictBucketKey)) {
        bucketMembers.set(normalized.conflictBucketKey, new Set());
      }
      bucketMembers.get(normalized.conflictBucketKey).add(normalized.canonicalKey);
    }

    const aggregateRecords = [];
    aggregateMap.forEach((aggregateRecord) => {
      const bucketSet = bucketMembers.get(aggregateRecord.conflictBucketKey);
      aggregateRecord.status = aggregateRecord.supportCount >= 2 ? "repeated" : "single";
      aggregateRecord.conflictCount = bucketSet ? bucketSet.size : 0;
      delete aggregateRecord.supportConfidenceMap;
      aggregateRecords.push(aggregateRecord);
    });

    return {
      records: aggregateRecords,
      preview: buildAggregatePreview(currentRuleRecords, aggregateRecords),
    };

    function computeAverageConfidence(confidenceMap) {
      let total = 0;
      let count = 0;

      confidenceMap.forEach((value) => {
        total += value;
        count += 1;
      });

      return count ? total / count : 0;
    }
  }

  function buildAggregatePreview(currentRuleRecords, aggregateRecords) {
    const aggregateByKey = new Map();
    const conflictCounts = new Map();

    for (let index = 0; index < aggregateRecords.length; index += 1) {
      const aggregateRecord = aggregateRecords[index];
      aggregateByKey.set(aggregateRecord.canonicalKey, aggregateRecord);
      conflictCounts.set(aggregateRecord.conflictBucketKey, aggregateRecord.conflictCount);
    }

    const repeatedRules = [];
    const conflictingRules = [];
    const lowConfidenceRules = [];
    const seenRepeated = new Set();
    const seenConflicts = new Set();

    for (let index = 0; index < currentRuleRecords.length; index += 1) {
      const normalized = normalizeRuleForAggregation(currentRuleRecords[index], null);
      if (!normalized) {
        continue;
      }

      const aggregateRecord = aggregateByKey.get(normalized.canonicalKey);
      if (aggregateRecord && aggregateRecord.supportCount >= 2 && !seenRepeated.has(normalized.canonicalKey)) {
        seenRepeated.add(normalized.canonicalKey);
        repeatedRules.push(`${aggregateRecord.summary} ×${aggregateRecord.supportCount}`);
      }

      const conflictCount = conflictCounts.get(normalized.conflictBucketKey) || 0;
      if (conflictCount > 1 && !seenConflicts.has(normalized.conflictBucketKey)) {
        seenConflicts.add(normalized.conflictBucketKey);
        conflictingRules.push(`${buildConflictLabel(currentRuleRecords[index])} (${conflictCount}패턴)`);
      }

      if (normalized.confidence < LOW_CONFIDENCE_RULE_THRESHOLD) {
        lowConfidenceRules.push(`${normalized.summary} (${roundConfidence(normalized.confidence)})`);
      }
    }

    return {
      repeatedRules: repeatedRules.slice(0, 3),
      conflictingRules: conflictingRules.slice(0, 3),
      lowConfidenceRules: lowConfidenceRules.slice(0, 3),
      repeatedRuleCount: repeatedRules.length,
      conflictingRuleCount: conflictingRules.length,
      lowConfidenceRuleCount: lowConfidenceRules.length,
    };
  }

  function normalizeRuleForAggregation(ruleRecord, pairSignatureById) {
    if (!ruleRecord || ruleRecord.type !== "rule") {
      return null;
    }

    const direction = typeof ruleRecord.direction === "string" && ruleRecord.direction ? ruleRecord.direction : "pc-to-mo";
    const ruleType = typeof ruleRecord.ruleType === "string" && ruleRecord.ruleType ? ruleRecord.ruleType : "unknown";
    const scope = normalizeAggregateToken(ruleRecord.scope);
    const payload = ruleRecord.payload && typeof ruleRecord.payload === "object" ? ruleRecord.payload : {};
    const subjectSignature = buildRuleSubjectSignature(ruleType, scope, payload);
    const outcomeSignature = buildRuleOutcomeSignature(ruleType, payload, ruleRecord.summary);
    const canonicalKey = [direction, ruleType, scope, subjectSignature, outcomeSignature].join("|");
    const conflictBucketKey = [direction, ruleType, scope, subjectSignature].join("|");
    const confidence = typeof ruleRecord.confidence === "number" ? clampScore(ruleRecord.confidence) : 0;

    return {
      canonicalKey: canonicalKey,
      conflictBucketKey: conflictBucketKey,
      direction: direction,
      ruleType: ruleType,
      scope: scope,
      summary: typeof ruleRecord.summary === "string" && ruleRecord.summary.trim() ? ruleRecord.summary.trim() : `${ruleType} rule`,
      confidence: confidence,
      pairId: typeof ruleRecord.pairId === "string" ? ruleRecord.pairId : "",
      pairSupportKey:
        pairSignatureById && pairSignatureById.has(ruleRecord.pairId)
          ? pairSignatureById.get(ruleRecord.pairId)
          : typeof ruleRecord.pairId === "string"
          ? ruleRecord.pairId
          : "",
      ruleId: typeof ruleRecord.id === "string" ? ruleRecord.id : "",
    };
  }

  function buildRuleSubjectSignature(ruleType, scope, payload) {
    if (scope === "root" && isContextualRootAggregateRuleType(ruleType)) {
      const parts = [
        payload.rootSourceShape,
        payload.rootSourceAspectBucket,
        payload.rootSourceLayoutMode,
        payload.rootSectionCountBucket,
        payload.rootTargetWidthBucket,
      ]
        .map((value) => normalizeAggregateToken(value))
        .filter(Boolean);
      if (parts.length > 0) {
        return parts.join("|");
      }
    }

    if (ruleType === "variant-switch") {
      return normalizeAggregateToken(payload.componentFamily || payload.componentKey || scope);
    }

    if (ruleType === "child-reorder") {
      return normalizeAggregateToken(payload.parentSignature || scope);
    }

    if (ruleType === "section-presence") {
      return normalizeAggregateToken(payload.sectionSignature || scope);
    }

    if (ruleType === "absolute-anchor") {
      return normalizeAggregateToken(payload.parentSignature || payload.nodeSignature || scope);
    }

    if (ruleType === "visibility-change") {
      return normalizeAggregateToken(payload.parentSignature || payload.nodeSignature || scope);
    }

    if (ruleType === "sizing-mode") {
      return normalizeAggregateToken(payload.nodeSignature || payload.parentSignature || scope);
    }

    return normalizeAggregateToken(scope);
  }

  function isContextualRootAggregateRuleType(ruleType) {
    return (
      ruleType === "frame-aspect-ratio" ||
      ruleType === "frame-shape" ||
      ruleType === "layout-axis" ||
      ruleType === "gap-scale" ||
      ruleType === "padding-scale" ||
      ruleType === "estimated-columns" ||
      ruleType === "font-scale"
    );
  }

  function buildRuleOutcomeSignature(ruleType, payload, summary) {
    if (ruleType === "viewport-width") {
      return `${bucketPixel(payload.pcWidth)}->${bucketPixel(payload.moWidth)}`;
    }

    if (ruleType === "frame-aspect-ratio") {
      return `${bucketFrameAspectRatio(payload.pcAspectRatio)}->${bucketFrameAspectRatio(payload.moAspectRatio)}`;
    }

    if (ruleType === "frame-shape") {
      return `${normalizeAggregateToken(payload.pcShape)}->${normalizeAggregateToken(payload.moShape)}`;
    }

    if (ruleType === "layout-axis") {
      return `${normalizeAggregateToken(payload.pcMode)}->${normalizeAggregateToken(payload.moMode)}`;
    }

    if (ruleType === "gap-scale") {
      return `${bucketPixel(payload.pcGap)}->${bucketPixel(payload.moGap)}`;
    }

    if (ruleType === "padding-scale") {
      return `${bucketPixel(payload.pcPaddingX)}x${bucketPixel(payload.pcPaddingY)}->${bucketPixel(payload.moPaddingX)}x${bucketPixel(payload.moPaddingY)}`;
    }

    if (ruleType === "estimated-columns") {
      return `${normalizeAggregateToken(payload.pcColumns)}->${normalizeAggregateToken(payload.moColumns)}`;
    }

    if (ruleType === "font-scale") {
      return `${bucketPixel(payload.pcFontSize)}->${bucketPixel(payload.moFontSize)}`;
    }

    if (ruleType === "child-reorder") {
      return `${normalizeAggregateToken(payload.pcOrderPattern)}->${normalizeAggregateToken(payload.moOrderPattern)}`;
    }

    if (ruleType === "section-presence") {
      return normalizeAggregateToken(payload.action);
    }

    if (ruleType === "visibility-change") {
      return `${payload.pcVisible ? "show" : "hide"}->${payload.moVisible ? "show" : "hide"}`;
    }

    if (ruleType === "variant-switch") {
      return [
        normalizeAggregateToken(payload.pcPropertySignature),
        normalizeAggregateToken(payload.moPropertySignature),
        normalizeAggregateToken(payload.pcReferenceSignature),
        normalizeAggregateToken(payload.moReferenceSignature),
      ].join("->");
    }

    if (ruleType === "absolute-anchor") {
      return `${normalizeAggregateToken(payload.anchor)}|${normalizeAggregateToken(payload.pcPositioning)}->${normalizeAggregateToken(payload.moPositioning)}`;
    }

    if (ruleType === "sizing-mode") {
      return [
        normalizeAggregateToken(payload.pcSizingHorizontal),
        normalizeAggregateToken(payload.pcSizingVertical),
        normalizeAggregateToken(payload.pcGrow),
      ].join("/") +
        "->" +
        [
          normalizeAggregateToken(payload.moSizingHorizontal),
          normalizeAggregateToken(payload.moSizingVertical),
          normalizeAggregateToken(payload.moGrow),
        ].join("/");
    }

    return normalizeAggregateToken(summary);
  }

  function buildConflictLabel(ruleRecord) {
    if (!ruleRecord) {
      return "conflicting rule";
    }

    const scope = typeof ruleRecord.scope === "string" && ruleRecord.scope ? ruleRecord.scope : "scope";
    const ruleType = typeof ruleRecord.ruleType === "string" && ruleRecord.ruleType ? ruleRecord.ruleType : "rule";
    const payload = ruleRecord.payload && typeof ruleRecord.payload === "object" ? ruleRecord.payload : {};
    const nodeLabel = typeof payload.nodeLabel === "string" && payload.nodeLabel ? payload.nodeLabel : "";
    const sectionLabel = typeof payload.sectionLabel === "string" && payload.sectionLabel ? payload.sectionLabel : "";
    if (ruleType === "child-reorder") {
      return `${scope} child order`;
    }
    if (ruleType === "section-presence") {
      return `${sectionLabel || scope} section presence`;
    }
    if (ruleType === "sizing-mode") {
      return `${nodeLabel || scope} sizing`;
    }
    if (ruleType === "visibility-change") {
      return `${nodeLabel || scope} visibility`;
    }
    if (ruleType === "absolute-anchor") {
      return `${nodeLabel || scope} anchor`;
    }
    return `${scope} ${ruleType}`;
  }

  function createAggregateRuleId(canonicalKey) {
    return `aggregate-rule:${hashString(canonicalKey)}`;
  }

  function normalizeAggregateToken(value) {
    if (typeof value === "number" && isFinite(value)) {
      return String(roundPixel(value));
    }

    const text = typeof value === "string" ? value.trim().toLowerCase() : value === true ? "true" : value === false ? "false" : "";
    if (!text) {
      return "na";
    }

    return text.replace(/\s+/g, "-");
  }

  function hashString(value) {
    const text = String(value || "");
    let hash = 2166136261;

    for (let index = 0; index < text.length; index += 1) {
      hash ^= text.charCodeAt(index);
      hash = Math.imul(hash, 16777619);
    }

    return (hash >>> 0).toString(36);
  }

  function simplifyRootDescriptor(root) {
    const layout = getLayoutInfo(root.node);
    const aspectRatio = getFrameAspectRatio(root.width, root.height);

    return {
      id: root.id,
      name: root.name,
      type: root.type,
      width: root.width,
      height: root.height,
      aspectRatio: aspectRatio,
      frameShape: classifyFrameShape(aspectRatio),
      estimatedColumns: estimateColumnCount(root.node),
      representativeFontSize: getRepresentativeFontSize(root.node),
      layout: {
        mode: layout.mode,
        gap: layout.gap,
        paddingX: layout.paddingX,
        paddingY: layout.paddingY,
        source: layout.source,
      },
      stats: root.stats,
    };
  }

  function collectNodeStats(node) {
    const stats = {
      totalNodes: 0,
      textNodes: 0,
      visibleNodes: 0,
      containerNodes: 0,
      autoLayoutNodes: 0,
      imageFillNodes: 0,
    };

    walk(node);
    return stats;

    function walk(current) {
      if (!current) {
        return;
      }

      stats.totalNodes += 1;
      if (current.visible !== false) {
        stats.visibleNodes += 1;
      }
      if (String(current.type || "") === "TEXT") {
        stats.textNodes += 1;
      }
      if (CONTAINER_TYPES[String(current.type || "")]) {
        stats.containerNodes += 1;
      }
      if (typeof current.layoutMode === "string" && current.layoutMode && current.layoutMode !== "NONE") {
        stats.autoLayoutNodes += 1;
      }
      if (Array.isArray(current.fills)) {
        for (let fillIndex = 0; fillIndex < current.fills.length; fillIndex += 1) {
          const fill = current.fills[fillIndex];
          if (fill && fill.visible !== false && fill.type === "IMAGE") {
            stats.imageFillNodes += 1;
            break;
          }
        }
      }

      if (!hasChildren(current)) {
        return;
      }

      for (let index = 0; index < current.children.length; index += 1) {
        walk(current.children[index]);
      }
    }
  }

  function getResponsiveMemoryHelper() {
    const memory = globalScope.__PIGMA_RESPONSIVE_MEMORY__;
    if (!memory || typeof memory.appendRecordsAsync !== "function" || typeof memory.summarizeStore !== "function") {
      throw new Error("Responsive memory 저장소를 찾지 못했습니다. 메모리 패치를 먼저 로드해 주세요.");
    }
    return memory;
  }

  function areNodeTypesCompatible(pcDescriptor, moDescriptor) {
    if (pcDescriptor.type === moDescriptor.type) {
      return true;
    }

    if (
      pcDescriptor.componentInfo.componentKey &&
      moDescriptor.componentInfo.componentKey &&
      pcDescriptor.componentInfo.componentKey === moDescriptor.componentInfo.componentKey
    ) {
      return true;
    }

    if (
      pcDescriptor.componentInfo.componentFamily &&
      moDescriptor.componentInfo.componentFamily &&
      pcDescriptor.componentInfo.componentFamily === moDescriptor.componentInfo.componentFamily
    ) {
      return true;
    }

    if (CONTAINER_TYPES[pcDescriptor.type] && CONTAINER_TYPES[moDescriptor.type]) {
      return true;
    }

    if (
      pcDescriptor.canonicalName &&
      moDescriptor.canonicalName &&
      pcDescriptor.canonicalName === moDescriptor.canonicalName
    ) {
      return true;
    }

    if (pcDescriptor.textFingerprint && moDescriptor.textFingerprint) {
      return true;
    }

    if (pcDescriptor.variableFingerprint && moDescriptor.variableFingerprint) {
      return true;
    }

    return false;
  }

  function isAutoName(name) {
    const text = typeof name === "string" ? name.trim() : "";
    if (!text) {
      return true;
    }

    for (let index = 0; index < AUTO_NAME_PATTERNS.length; index += 1) {
      if (AUTO_NAME_PATTERNS[index].test(text)) {
        return true;
      }
    }

    return false;
  }

  function canonicalizeName(name) {
    const text = typeof name === "string" ? name.trim().toLowerCase() : "";
    if (!text || isAutoName(text)) {
      return "";
    }

    return text
      .replace(/[._:/\\-]+/g, " ")
      .replace(/[0-9]+/g, "#")
      .replace(/\s+/g, " ")
      .trim();
  }

  function getNodeBounds(node) {
    if (!node || typeof node !== "object") {
      return null;
    }

    if (
      typeof node.x === "number" &&
      typeof node.y === "number" &&
      typeof node.width === "number" &&
      typeof node.height === "number"
    ) {
      return {
        x: roundPixel(node.x),
        y: roundPixel(node.y),
        width: roundPixel(node.width),
        height: roundPixel(node.height),
      };
    }

    if (
      node.absoluteRenderBounds &&
      typeof node.absoluteRenderBounds.x === "number" &&
      typeof node.absoluteRenderBounds.y === "number" &&
      typeof node.absoluteRenderBounds.width === "number" &&
      typeof node.absoluteRenderBounds.height === "number"
    ) {
      return {
        x: roundPixel(node.absoluteRenderBounds.x),
        y: roundPixel(node.absoluteRenderBounds.y),
        width: roundPixel(node.absoluteRenderBounds.width),
        height: roundPixel(node.absoluteRenderBounds.height),
      };
    }

    return null;
  }

  function getSelectionSignature(selection) {
    const items = Array.isArray(selection) ? selection.slice() : [];
    items.sort((left, right) => {
      if (left.id < right.id) {
        return -1;
      }
      if (left.id > right.id) {
        return 1;
      }
      return 0;
    });

    return items
      .map((node) => {
        const bounds = getNodeBounds(node);
        const width = bounds ? bounds.width : 0;
        return `${node.id}:${width}`;
      })
      .join("|");
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }
    return String((node && node.type) || "Unnamed");
  }

  function hasChildren(node) {
    return !!(node && Array.isArray(node.children) && node.children.length);
  }

  function numeric(value) {
    return typeof value === "number" && isFinite(value) ? value : 0;
  }

  function string(value, fallback) {
    return typeof value === "string" && value.trim() ? value.trim() : fallback;
  }

  function roundPixel(value) {
    const number = typeof value === "number" && isFinite(value) ? value : 0;
    return Math.round(number * 100) / 100;
  }

  function bucketPixel(value) {
    const rounded = roundPixel(value);
    if (!rounded) {
      return "0";
    }
    if (rounded <= 4) {
      return String(Math.round(rounded));
    }
    return String(Math.round(rounded / 4) * 4);
  }

  function roundRatio(value) {
    const number = typeof value === "number" && isFinite(value) ? value : 0;
    return Math.round(number * 1000) / 1000;
  }

  function roundConfidence(value) {
    const number = typeof value === "number" && isFinite(value) ? value : 0;
    return Math.round(clampScore(number) * 1000) / 1000;
  }

  function getMatchTier(score) {
    const normalized = clampScore(score);
    if (normalized >= CONFIRMED_MATCH_THRESHOLD) {
      return "confirmed";
    }
    if (normalized >= CANDIDATE_MATCH_THRESHOLD) {
      return "candidate";
    }
    return "low-confidence";
  }

  function clampScore(value) {
    const number = typeof value === "number" && isFinite(value) ? value : 0;
    if (number < 0) {
      return 0;
    }
    if (number > 1) {
      return 1;
    }
    return number;
  }

  function averageScore(values) {
    let total = 0;
    let count = 0;

    for (let index = 0; index < values.length; index += 1) {
      if (typeof values[index] !== "number" || values[index] < 0) {
        continue;
      }
      total += values[index];
      count += 1;
    }

    return count ? clampScore(total / count) : 0;
  }

  function estimateColumnCount(rootNode) {
    if (!rootNode || !hasChildren(rootNode)) {
      return 1;
    }

    const rootBounds = getNodeBounds(rootNode);
    if (!rootBounds || !rootBounds.width) {
      return 1;
    }

    const buckets = [];
    const children = rootNode.children;
    for (let index = 0; index < children.length; index += 1) {
      const child = children[index];
      if (!child || child.visible === false) {
        continue;
      }

      const bounds = getNodeBounds(child);
      if (!bounds) {
        continue;
      }

      const ratio = roundRatio((bounds.x - rootBounds.x) / Math.max(1, rootBounds.width));
      let matchedBucket = false;
      for (let bucketIndex = 0; bucketIndex < buckets.length; bucketIndex += 1) {
        if (Math.abs(buckets[bucketIndex] - ratio) <= 0.08) {
          matchedBucket = true;
          break;
        }
      }
      if (!matchedBucket) {
        buckets.push(ratio);
      }
    }

    return Math.max(1, buckets.length || 1);
  }

  function getRepresentativeFontSize(rootNode) {
    const values = [];
    collectFontSizes(rootNode, values);
    if (!values.length) {
      return 0;
    }

    values.sort((left, right) => left - right);
    const middle = Math.floor(values.length / 2);
    if (values.length % 2) {
      return roundPixel(values[middle]);
    }

    return roundPixel((values[middle - 1] + values[middle]) / 2);
  }

  function getNodeRepresentativeFontSize(node) {
    return getRepresentativeFontSize(node);
  }

  function collectFontSizes(node, values) {
    if (!node) {
      return;
    }

    if (String(node.type || "") === "TEXT" && typeof node.fontSize === "number" && isFinite(node.fontSize)) {
      values.push(node.fontSize);
    }

    if (!hasChildren(node)) {
      return;
    }

    for (let index = 0; index < node.children.length; index += 1) {
      collectFontSizes(node.children[index], values);
    }
  }

  function createRecordId(prefix) {
    const timestamp = Date.now().toString(36);
    const random = Math.floor(Math.random() * 1679616)
      .toString(36)
      .padStart(4, "0");
    return `${prefix}:${timestamp}:${random}`;
  }

  function normalizeErrorMessage(error) {
    const memory = globalScope.__PIGMA_RESPONSIVE_MEMORY__;
    if (memory && typeof memory.normalizeErrorMessage === "function") {
      return memory.normalizeErrorMessage(error);
    }

    if (error instanceof Error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return "Responsive pair analysis failed.";
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_LLM_CLIENT_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const AI_SETTINGS_KEY = "pigma:ai-settings:v1";
  const AI_LLM_RUN_LOG_KEY = "pigma:ai-llm-run-log:v1";
  const AI_LLM_RUN_LOG_LIMIT = 40;
  const AI_LLM_REQUEST_TIMEOUT_MS = 45000;
  const DEFAULT_AI_SETTINGS = Object.freeze({
    enabled: false,
    provider: "openai",
    apiKey: "",
    openAiApiKey: "",
    geminiApiKey: "",
    proofingLocale: "",
    userDictionary: [],
    protectedTerms: [],
  });
  const DEFAULT_MODEL_BY_PROVIDER = Object.freeze({
    openai: "gpt-5-mini",
    gemini: "gemini-2.5-flash",
  });
  const FALLBACK_MODELS_BY_PROVIDER = Object.freeze({
    openai: ["gpt-5-mini", "gpt-4.1-mini"],
    gemini: ["gemini-2.5-flash"],
  });
  const TEXT_AI_PROVIDERS = Object.freeze(["openai", "gemini"]);
  const pendingUiRequests = new Map();

  globalScope.__PIGMA_AI_LLM__ = {
    getAiSettingsAsync,
    getAiRunLogAsync,
    getResolvedRunInfo,
    hasConfiguredAiAsync,
    requestJsonTask,
    normalizeErrorMessage,
    uniqueStrings,
  };

  if (typeof originalOnMessage === "function") {
    figma.ui.onmessage = async (message) => {
      if (isAiRunLogMessage(message)) {
        await postAiRunLog();
        return;
      }
      if (isAiUiBridgeResponse(message)) {
        resolveUiBridgeResponse(message);
        return;
      }

      return originalOnMessage(message);
    };
  }

  globalScope.__PIGMA_AI_LLM_CLIENT_PATCH__ = true;

  async function getAiSettingsAsync() {
    try {
      return normalizeAiSettings(await figma.clientStorage.getAsync(AI_SETTINGS_KEY));
    } catch (error) {
      return normalizeAiSettings(null);
    }
  }

  async function hasConfiguredAiAsync() {
    const settings = await getAiSettingsAsync();
    const runInfo = getResolvedRunInfo(settings);
    return settings.enabled === true && runInfo.apiKey.length > 0;
  }

  async function requestJsonTask(options) {
    const settings = await getAiSettingsAsync();
    if (settings.enabled !== true) {
      return null;
    }
    const attemptRunInfos = buildTextTaskRunInfos(settings, options);
    if (!attemptRunInfos.length) {
      return null;
    }

    let lastFailure = null;

    for (let index = 0; index < attemptRunInfos.length; index += 1) {
      const runInfo = attemptRunInfos[index];
      const provider = runInfo.provider;
      const model = runInfo.model;
      const requestMeta = buildRequestMeta(options, runInfo);
      const prompt = buildPrompt(options, requestMeta);
      const startedAt = Date.now();

      try {
        let result = null;
        result = await requestJsonTaskViaUiBridge({
          provider,
          model,
          apiKey: runInfo.apiKey,
          prompt,
          meta: requestMeta,
        });
        const durationMs = Math.max(0, Date.now() - startedAt);
        const quality = summarizeJsonResponseQuality(result, options && options.schema);

        if (result && typeof result === "object") {
          result._provider = provider;
          result._model = model;
          result._taskType = requestMeta.taskType;
          result._taskContext = requestMeta.taskContext;
          result._plannerVersion = requestMeta.plannerVersion;
          result._providerProfile = requestMeta.providerProfile;
          result._durationMs = durationMs;
          result._responseQualityScore = quality.score;
          result._responseQualityLabel = quality.label;
          result._responseQualityNotes = quality.notes.slice(0, 4);
        }

        const notes = quality.notes.slice(0, 4);
        if (index > 0) {
          notes.push(`fallback after ${attemptRunInfos[index - 1].provider}`);
        }

        await appendAiRunLogAsync({
          ok: true,
          provider: provider,
          model: model,
          taskType: requestMeta.taskType,
          taskContext: requestMeta.taskContext,
          plannerVersion: requestMeta.plannerVersion,
          providerProfile: requestMeta.providerProfile,
          durationMs: durationMs,
          qualityScore: quality.score,
          qualityLabel: quality.label,
          notes,
        });

        return result;
      } catch (error) {
        const durationMs = Math.max(0, Date.now() - startedAt);
        const failureType = classifyAiFailureType(error);
        const message = normalizeErrorMessage(error, "AI 응답을 받지 못했습니다.");
        const notes = index + 1 < attemptRunInfos.length ? [`retrying with ${attemptRunInfos[index + 1].provider}`] : [];
        await appendAiRunLogAsync({
          ok: false,
          provider: provider,
          model: model,
          taskType: requestMeta.taskType,
          taskContext: requestMeta.taskContext,
          plannerVersion: requestMeta.plannerVersion,
          providerProfile: requestMeta.providerProfile,
          durationMs: durationMs,
          failureType: failureType,
          message: message,
          notes,
        });

        lastFailure = {
          error,
          message,
          requestMeta,
          runInfo,
          durationMs,
          failureType,
        };
      }
    }

    if (lastFailure) {
      throw createAiClientError(
        lastFailure.error,
        lastFailure.message,
        lastFailure.requestMeta,
        lastFailure.runInfo,
        lastFailure.durationMs,
        lastFailure.failureType
      );
    }

    return null;
  }

  async function getAiRunLogAsync() {
    try {
      const stored = await figma.clientStorage.getAsync(AI_LLM_RUN_LOG_KEY);
      return normalizeAiRunLog(stored);
    } catch (error) {
      return [];
    }
  }

  async function appendAiRunLogAsync(entry) {
    try {
      const current = await getAiRunLogAsync();
      current.unshift(normalizeAiRunLogEntry(entry));
      await figma.clientStorage.setAsync(AI_LLM_RUN_LOG_KEY, current.slice(0, AI_LLM_RUN_LOG_LIMIT));
    } catch (error) {}
  }

  function getResolvedRunInfo(settingsOrOptions, maybeOptions) {
    const hasSettingsShape =
      settingsOrOptions &&
      typeof settingsOrOptions === "object" &&
      (Object.prototype.hasOwnProperty.call(settingsOrOptions, "provider") ||
        Object.prototype.hasOwnProperty.call(settingsOrOptions, "apiKey") ||
        Object.prototype.hasOwnProperty.call(settingsOrOptions, "enabled"));
    const settings = hasSettingsShape ? settingsOrOptions : normalizeAiSettings(null);
    const options = hasSettingsShape ? maybeOptions : settingsOrOptions;
    const provider = resolveTextTaskProvider(settings);
    const apiKey = getApiKeyForProvider(settings, provider);
    const model =
      options && typeof options.modelByProvider === "object" && options.modelByProvider && options.modelByProvider[provider]
        ? options.modelByProvider[provider]
        : DEFAULT_MODEL_BY_PROVIDER[provider];

    return {
      provider,
      apiKey,
      model,
      providerProfile: buildProviderProfile("", provider, model, "", ""),
    };
  }

  function buildTextTaskRunInfos(settings, options) {
    const source = settings && typeof settings === "object" ? settings : {};
    const preferredProvider = resolveTextTaskProvider(source);
    const providers = [];

    if (preferredProvider) {
      providers.push(preferredProvider);
    }

    for (const provider of TEXT_AI_PROVIDERS) {
      if (providers.indexOf(provider) >= 0) {
        continue;
      }
      if (!getApiKeyForProvider(source, provider)) {
        continue;
      }
      providers.push(provider);
    }

    const runInfos = [];

    for (const provider of providers) {
      const apiKey = getApiKeyForProvider(source, provider);
      if (!apiKey) {
        continue;
      }

      const preferredModel =
        options && typeof options.modelByProvider === "object" && options.modelByProvider && options.modelByProvider[provider]
          ? options.modelByProvider[provider]
          : DEFAULT_MODEL_BY_PROVIDER[provider];
      const modelCandidates = uniqueStrings(
        [preferredModel].concat(Array.isArray(FALLBACK_MODELS_BY_PROVIDER[provider]) ? FALLBACK_MODELS_BY_PROVIDER[provider] : []),
        4
      );

      for (const model of modelCandidates) {
        runInfos.push({
          provider,
          apiKey,
          model,
          providerProfile: buildProviderProfile("", provider, model, "", ""),
        });
      }
    }

    return runInfos;
  }

  function normalizeAiSettings(value) {
    const source = value && typeof value === "object" ? value : {};
    const legacyProvider = source.provider === "gemini" ? "gemini" : DEFAULT_AI_SETTINGS.provider;
    const legacyApiKey = typeof source.apiKey === "string" ? sanitizeApiKey(source.apiKey) : DEFAULT_AI_SETTINGS.apiKey;
    const openAiApiKey =
      typeof source.openAiApiKey === "string"
        ? sanitizeApiKey(source.openAiApiKey)
        : legacyProvider === "openai"
          ? legacyApiKey
          : DEFAULT_AI_SETTINGS.openAiApiKey;
    const geminiApiKey =
      typeof source.geminiApiKey === "string"
        ? sanitizeApiKey(source.geminiApiKey)
        : legacyProvider === "gemini"
          ? legacyApiKey
          : DEFAULT_AI_SETTINGS.geminiApiKey;
    const provider = resolveTextTaskProvider({
      provider: legacyProvider,
      openAiApiKey,
      geminiApiKey,
    });
    const apiKey = getApiKeyForProvider({ openAiApiKey, geminiApiKey }, provider);

    return {
      enabled: source.enabled === true,
      provider,
      apiKey,
      openAiApiKey,
      geminiApiKey,
      proofingLocale: normalizeProofingLocale(source.proofingLocale),
      userDictionary: normalizeTermList(source.userDictionary),
      protectedTerms: normalizeTermList(source.protectedTerms),
    };
  }

  function getApiKeyForProvider(settings, provider) {
    const source = settings && typeof settings === "object" ? settings : {};
    return provider === "gemini" ? sanitizeApiKey(source.geminiApiKey) : sanitizeApiKey(source.openAiApiKey);
  }

  function resolveTextTaskProvider(settings) {
    const source = settings && typeof settings === "object" ? settings : {};
    const preferred = source.provider === "gemini" ? "gemini" : "openai";
    if (getApiKeyForProvider(source, preferred)) {
      return preferred;
    }
    const fallback = preferred === "gemini" ? "openai" : "gemini";
    if (getApiKeyForProvider(source, fallback)) {
      return fallback;
    }
    return preferred;
  }

  function buildPrompt(options, requestMeta) {
    const instructions = options && typeof options.instructions === "string" ? options.instructions.trim() : "";
    const payload = options && options.payload ? options.payload : {};
    const schema = options && options.schema ? options.schema : null;
    const blocks = [
      "Return exactly one JSON object.",
      "Do not wrap the JSON in markdown fences.",
      "Do not add any explanation outside the JSON.",
    ];

    if (instructions) {
      blocks.push(instructions);
    }

    if (requestMeta && typeof requestMeta === "object") {
      blocks.push("Task metadata:");
      blocks.push(
        JSON.stringify(
          {
            taskType: requestMeta.taskType,
            taskContext: requestMeta.taskContext,
            plannerVersion: requestMeta.plannerVersion,
            providerProfile: requestMeta.providerProfile,
          },
          null,
          2
        )
      );
    }

    if (schema) {
      blocks.push("Target JSON shape:");
      blocks.push(JSON.stringify(schema, null, 2));
    }

    blocks.push("Input payload:");
    blocks.push(JSON.stringify(payload, null, 2));
    return blocks.join("\n\n");
  }

  function isAiUiBridgeResponse(message) {
    return !!message && message.type === "ai-llm-ui-response" && typeof message.requestId === "string";
  }

  function isAiRunLogMessage(message) {
    return !!message && message.type === "request-ai-llm-run-log";
  }

  async function postAiRunLog() {
    figma.ui.postMessage({
      type: "ai-llm-run-log",
      logs: await getAiRunLogAsync(),
    });
  }

  function resolveUiBridgeResponse(message) {
    const pending = pendingUiRequests.get(message.requestId);
    if (!pending) {
      return;
    }

    pendingUiRequests.delete(message.requestId);
    clearTimeout(pending.timeoutId);

    if (message.ok === true) {
      pending.resolve(message.result);
      return;
    }

    pending.reject(new Error(typeof message.error === "string" && message.error ? message.error : "AI UI bridge 요청에 실패했습니다."));
  }

  async function requestJsonTaskViaUiBridge(payload) {
    if (!figma.ui || typeof figma.ui.postMessage !== "function") {
      throw new Error("Figma UI bridge를 사용할 수 없습니다.");
    }

    const requestId = createRequestId();
    return await new Promise((resolve, reject) => {
      const timeoutId = setTimeout(() => {
        pendingUiRequests.delete(requestId);
        reject(new Error("AI UI bridge 응답이 시간 내에 오지 않았습니다."));
      }, AI_LLM_REQUEST_TIMEOUT_MS);

      pendingUiRequests.set(requestId, {
        resolve,
        reject,
        timeoutId,
      });

      figma.ui.postMessage({
        type: "ai-llm-ui-request",
        requestId,
        payload,
      });
    });
  }

  function createRequestId() {
    return "ai-" + Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 10);
  }

  function sanitizeApiKey(value) {
    return normalizeApiKeyCharacters(value);
  }

  function normalizeProofingLocale(value) {
    const compact = String(value || "")
      .replace(/[^A-Za-z-]/g, "")
      .trim();
    if (!compact) {
      return "";
    }

    const parts = compact.split("-").filter(Boolean);
    if (parts.length === 1 && /^[A-Za-z]{2,3}$/.test(parts[0])) {
      return parts[0].toLowerCase();
    }

    if (parts.length === 2 && /^[A-Za-z]{2,3}$/.test(parts[0]) && /^[A-Za-z]{2,4}$/.test(parts[1])) {
      return `${parts[0].toLowerCase()}-${parts[1].toUpperCase()}`;
    }

    return "";
  }

  function normalizeTermList(value) {
    const terms = Array.isArray(value)
      ? value
      : typeof value === "string"
        ? value.split(/\r?\n|,/)
        : [];
    const normalized = [];
    const seen = new Set();

    for (const term of terms) {
      const next = String(term || "").replace(/\s+/g, " ").trim();
      if (!next) {
        continue;
      }

      const key = next.toLocaleLowerCase();
      if (seen.has(key)) {
        continue;
      }

      seen.add(key);
      normalized.push(next);
    }

    return normalized.slice(0, 200);
  }

  function sanitizeHeaderValue(value) {
    return String(value || "")
      .replace(/[\u0000-\u001F\u007F-\u009F]/g, "")
      .replace(/[\u200B-\u200D\uFEFF]/g, "")
      .trim();
  }

  function normalizeApiKeyCharacters(value) {
    return String(value || "")
      .replace(/[\u0000-\u001F\u007F-\u009F]/g, "")
      .replace(/[\u200B-\u200D\uFEFF]/g, "")
      .replace(/\s+/g, "")
      .replace(/^['"`]+|['"`]+$/g, "")
      .replace(/[^\x21-\x7E]/g, "")
      .trim();
  }

  function buildRequestMeta(options, runInfo) {
    const taskType = normalizeTaskType(options && options.taskType);
    const taskContext = normalizeTaskContext(options && options.taskContext);
    const plannerVersion = normalizePlannerVersion(options && options.plannerVersion);
    const provider = runInfo && runInfo.provider === "gemini" ? "gemini" : "openai";
    const model = runInfo && typeof runInfo.model === "string" ? runInfo.model : DEFAULT_MODEL_BY_PROVIDER[provider];
    return {
      taskType: taskType,
      taskContext: taskContext,
      plannerVersion: plannerVersion,
      providerProfile: buildProviderProfile(options && options.providerProfile, provider, model, taskType, plannerVersion),
    };
  }

  function normalizeTaskType(value) {
    const normalized = String(value || "")
      .toLowerCase()
      .replace(/[^a-z0-9/_-]+/g, "-")
      .replace(/-{2,}/g, "-")
      .replace(/^[-/]+|[-/]+$/g, "")
      .trim();
    return normalized || "generic-json-task";
  }

  function normalizeTaskContext(value) {
    const normalized = String(value || "")
      .toLowerCase()
      .replace(/[^a-z0-9/_-]+/g, "-")
      .replace(/\/{2,}/g, "/")
      .replace(/-{2,}/g, "-")
      .replace(/^[-/]+|[-/]+$/g, "")
      .trim();
    return normalized;
  }

  function normalizePlannerVersion(value) {
    const normalized = String(value || "").replace(/\s+/g, " ").trim();
    return normalized || "unspecified";
  }

  function buildProviderProfile(value, provider, model, taskType, plannerVersion) {
    const normalized = String(value || "").replace(/\s+/g, " ").trim();
    if (normalized) {
      return normalized;
    }
    const safeProvider = provider === "gemini" ? "gemini" : "openai";
    const safeModel = String(model || DEFAULT_MODEL_BY_PROVIDER[safeProvider]).replace(/\s+/g, "-").trim();
    const safeTaskType = normalizeTaskType(taskType);
    const safePlannerVersion = normalizePlannerVersion(plannerVersion).replace(/\s+/g, "-");
    return `${safeProvider}/${safeModel}/${safeTaskType}/${safePlannerVersion}`;
  }

  function summarizeJsonResponseQuality(result, schema) {
    const required = schema && Array.isArray(schema.required) ? schema.required : [];
    if (!result || typeof result !== "object") {
      return {
        score: 0,
        label: "empty",
        notes: ["No JSON object returned"],
      };
    }
    if (required.length === 0) {
      return {
        score: 100,
        label: "high",
        notes: ["Schema does not define required fields"],
      };
    }

    let matched = 0;
    const missing = [];
    for (let index = 0; index < required.length; index += 1) {
      const key = required[index];
      if (Object.prototype.hasOwnProperty.call(result, key) && hasPresentJsonValue(result[key])) {
        matched += 1;
      } else {
        missing.push(String(key));
      }
    }

    const score = Math.max(0, Math.min(100, Math.round((matched / required.length) * 100)));
    const label = score >= 90 ? "high" : score >= 60 ? "medium" : score > 0 ? "low" : "empty";
    const notes = missing.length > 0 ? [`Missing required: ${missing.slice(0, 4).join(", ")}`] : ["Required fields complete"];
    return {
      score: score,
      label: label,
      notes: notes,
    };
  }

  function hasPresentJsonValue(value) {
    if (value === null || typeof value === "undefined") {
      return false;
    }
    if (typeof value === "string") {
      return value.trim().length > 0;
    }
    if (Array.isArray(value)) {
      return value.length > 0;
    }
    if (typeof value === "object") {
      return Object.keys(value).length > 0;
    }
    return true;
  }

  function classifyAiFailureType(error) {
    const message = normalizeErrorMessage(error, "").toLowerCase();
    if (!message) {
      return "unknown";
    }
    if (message.indexOf("time") >= 0 || message.indexOf("timeout") >= 0 || message.indexOf("시간 내") >= 0) {
      return "timeout";
    }
    if (message.indexOf("api key") >= 0 || message.indexOf("missing an api key") >= 0) {
      return "configuration";
    }
    if (message.indexOf("401") >= 0 || message.indexOf("403") >= 0 || message.indexOf("unauthorized") >= 0) {
      return "auth";
    }
    if (message.indexOf("429") >= 0 || message.indexOf("rate") >= 0) {
      return "rate-limit";
    }
    if (message.indexOf("bridge") >= 0) {
      return "bridge";
    }
    if (message.indexOf("network") >= 0 || message.indexOf("fetch") >= 0) {
      return "network";
    }
    return "provider";
  }

  function createAiClientError(error, fallbackMessage, requestMeta, runInfo, durationMs, failureType) {
    const wrapped = new Error(normalizeErrorMessage(error, fallbackMessage));
    wrapped.pigmaAiFailureType = failureType || "unknown";
    wrapped.pigmaAiTaskType = requestMeta && requestMeta.taskType ? requestMeta.taskType : "generic-json-task";
    wrapped.pigmaAiTaskContext = requestMeta && requestMeta.taskContext ? requestMeta.taskContext : "";
    wrapped.pigmaAiPlannerVersion = requestMeta && requestMeta.plannerVersion ? requestMeta.plannerVersion : "unspecified";
    wrapped.pigmaAiProviderProfile = requestMeta && requestMeta.providerProfile ? requestMeta.providerProfile : "";
    wrapped.pigmaAiProvider = runInfo && runInfo.provider ? runInfo.provider : "openai";
    wrapped.pigmaAiModel = runInfo && runInfo.model ? runInfo.model : "";
    wrapped.pigmaAiDurationMs = Math.max(0, Math.round(Number(durationMs) || 0));
    return wrapped;
  }

  function normalizeAiRunLog(value) {
    const items = Array.isArray(value) ? value : [];
    const out = [];
    for (let index = 0; index < items.length && out.length < AI_LLM_RUN_LOG_LIMIT; index += 1) {
      const normalized = normalizeAiRunLogEntry(items[index]);
      if (normalized.createdAt) {
        out.push(normalized);
      }
    }
    return out;
  }

  function normalizeAiRunLogEntry(value) {
    const source = value && typeof value === "object" ? value : {};
    return {
      id: String(source.id || createRequestId()),
      createdAt: String(source.createdAt || new Date().toISOString()),
      ok: source.ok === true,
      provider: source.provider === "gemini" ? "gemini" : "openai",
      model: String(source.model || ""),
      taskType: normalizeTaskType(source.taskType),
      taskContext: normalizeTaskContext(source.taskContext),
      plannerVersion: normalizePlannerVersion(source.plannerVersion),
      providerProfile: String(source.providerProfile || "").trim(),
      durationMs: Math.max(0, Math.round(Number(source.durationMs) || 0)),
      qualityScore: Math.max(0, Math.min(100, Math.round(Number(source.qualityScore) || 0))),
      qualityLabel: String(source.qualityLabel || "").trim(),
      failureType: String(source.failureType || "").trim(),
      message: String(source.message || "").trim(),
      notes: Array.isArray(source.notes) ? uniqueStrings(source.notes, 4) : [],
    };
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }

  function uniqueStrings(values, limit) {
    const result = [];
    const seen = new Set();
    const max = typeof limit === "number" && limit > 0 ? limit : Number.POSITIVE_INFINITY;
    const items = Array.isArray(values) ? values : [];

    for (const value of items) {
      const normalized = String(value || "").replace(/\s+/g, " ").trim();
      if (!normalized || seen.has(normalized)) {
        continue;
      }

      seen.add(normalized);
      result.push(normalized);
      if (result.length >= max) {
        break;
      }
    }

    return result;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_ACCESSIBILITY_DIAG_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const AI_DESIGN_READ_CACHE_KEY = "pigma:ai-design-read-cache:v1";
  const AI_ACCESSIBILITY_CACHE_KEY = "pigma:ai-accessibility-diagnosis-cache:v1";
  const ACCESSIBILITY_MESSAGE_TYPES = {
    requestCache: "request-ai-accessibility-cache",
    runDiagnosis: "run-ai-accessibility-diagnosis",
    applyFix: "run-ai-accessibility-apply-fix",
    result: "ai-accessibility-result",
    error: "ai-accessibility-error",
    cache: "ai-accessibility-cache",
    status: "ai-accessibility-status",
  };
  const LEGACY_ACCESSIBILITY_MESSAGE_TYPES = {
    requestCache: "request-ai-design-read-cache",
    runDiagnosis: "run-ai-design-read",
    applyFix: "run-ai-design-read-apply-fix",
    result: "ai-design-read-result",
    error: "ai-design-read-error",
    cache: "ai-design-read-cache",
    status: "ai-design-read-status",
  };
  const PATCH_VERSION = 3;
  const ANNOTATION_CATEGORY_LABEL = "웹 접근성 진단";
  const ANNOTATION_CATEGORY_COLOR = "green";
  const ANNOTATION_LABEL_PREFIX = "[웹 접근성 진단]";
  const MAX_ISSUE_COUNT = 24;
  const MIN_TAP_TARGET = 44;
  const CRITICAL_TAP_TARGET = 24;
  const MIN_BODY_FONT_SIZE = 14;
  const MIN_ACTION_FONT_SIZE = 16;
  const NORMAL_TEXT_CONTRAST_RATIO = 4.5;
  const LARGE_TEXT_CONTRAST_RATIO = 3;
  const CRITICAL_NORMAL_TEXT_CONTRAST_RATIO = 2.2;
  const CRITICAL_LARGE_TEXT_CONTRAST_RATIO = 1.8;
  const VECTOR_TYPES = new Set([
    "VECTOR",
    "BOOLEAN_OPERATION",
    "STAR",
    "LINE",
    "ELLIPSE",
    "POLYGON",
    "REGULAR_POLYGON",
    "RECTANGLE",
  ]);
  const AUTO_NAME_PATTERNS = [
    /^frame \d+$/i,
    /^group \d+$/i,
    /^rectangle \d+$/i,
    /^text \d+$/i,
    /^vector \d+$/i,
    /^ellipse \d+$/i,
    /^polygon \d+$/i,
    /^star \d+$/i,
    /^line \d+$/i,
    /^image \d+$/i,
    /^component \d+$/i,
    /^instance \d+$/i,
    /^section \d+$/i,
    /^copy(?: of)? /i,
    /^untitled/i,
  ];
  const CONTROL_NAME_PATTERN = /(button|btn|cta|chip|tab|pill|toggle|switch|checkbox|radio|link|menu|nav|button|버튼|탭|칩|토글|스위치|체크|링크|메뉴)/i;
  const ACTION_TEXT_PATTERN =
    /^(ok|go|next|done|start|login|sign in|sign up|signup|save|apply|cancel|submit|buy|open|menu|search|confirm|delete|continue|확인|다음|완료|시작|저장|적용|취소|구매|열기|메뉴|검색|삭제|계속)$/i;
  let activeExecution = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isAccessibilityMessage(message)) {
      const messageMode = getAccessibilityMessageMode(message.type);
      if (!messageMode) {
        return originalOnMessage(message);
      }

      if (isAccessibilityCacheRequestType(message.type)) {
        await postCachedResult(messageMode);
        return;
      }

      if (isAccessibilityApplyFixType(message.type)) {
        await withExecutionLock(
          {
            status: "applying-fix",
            message: "선택한 접근성 수정안을 적용하고 있습니다.",
            extra: {
              issueId: message && typeof message.issueId === "string" ? message.issueId.trim() : "",
            },
          },
          () => applyIssueFix(message, messageMode),
          messageMode
        );
        return;
      }

      await withExecutionLock(
        {
          status: "running",
          message: "현재 선택의 웹 접근성을 진단하고 있습니다.",
        },
        () => runAccessibilityDiagnosis({ messageMode: messageMode }),
        messageMode
      );
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_ACCESSIBILITY_DIAG_PATCH__ = true;

  function isAccessibilityMessage(message) {
    return !!message && !!getAccessibilityMessageMode(message.type);
  }

  function getAccessibilityMessageMode(type) {
    if (
      type === ACCESSIBILITY_MESSAGE_TYPES.requestCache ||
      type === ACCESSIBILITY_MESSAGE_TYPES.runDiagnosis ||
      type === ACCESSIBILITY_MESSAGE_TYPES.applyFix
    ) {
      return "accessibility";
    }

    if (
      type === LEGACY_ACCESSIBILITY_MESSAGE_TYPES.requestCache ||
      type === LEGACY_ACCESSIBILITY_MESSAGE_TYPES.runDiagnosis ||
      type === LEGACY_ACCESSIBILITY_MESSAGE_TYPES.applyFix
    ) {
      return "legacy";
    }

    return "";
  }

  function resolveAccessibilityMessageMode(value) {
    return value === "accessibility" ? "accessibility" : "legacy";
  }

  function getAccessibilityMessageTypes(mode) {
    return resolveAccessibilityMessageMode(mode) === "accessibility"
      ? ACCESSIBILITY_MESSAGE_TYPES
      : LEGACY_ACCESSIBILITY_MESSAGE_TYPES;
  }

  function isAccessibilityCacheRequestType(type) {
    return (
      type === ACCESSIBILITY_MESSAGE_TYPES.requestCache ||
      type === LEGACY_ACCESSIBILITY_MESSAGE_TYPES.requestCache
    );
  }

  function isAccessibilityApplyFixType(type) {
    return type === ACCESSIBILITY_MESSAGE_TYPES.applyFix || type === LEGACY_ACCESSIBILITY_MESSAGE_TYPES.applyFix;
  }

  async function withExecutionLock(execution, runner, messageMode) {
    if (activeExecution) {
      postStatus(activeExecution.status, activeExecution.message, activeExecution.extra, messageMode);
      return false;
    }

    activeExecution =
      execution && typeof execution === "object" ? execution : { status: "running", message: "", extra: {} };
    try {
      await runner();
      return true;
    } finally {
      activeExecution = null;
    }
  }

  async function runAccessibilityDiagnosis(options) {
    const runOptions = options && typeof options === "object" ? options : {};
    const messageMode = resolveAccessibilityMessageMode(runOptions.messageMode);
    const messageTypes = getAccessibilityMessageTypes(messageMode);
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    if (!runOptions.skipStatus) {
      postStatus("running", "현재 선택의 웹 접근성을 진단하고 있습니다.");
    }
    if (!runOptions.skipStatus && messageMode === "accessibility") {
      figma.ui.postMessage({ type: ACCESSIBILITY_MESSAGE_TYPES.status, status: "running", message: "" });
    }

    try {
      const analysis = analyzeCurrentSelection();
      let result = analysis.result;
      const annotationSupported = Array.isArray(analysis.annotationNodes) && analysis.annotationNodes.length > 0;
      const category =
        annotationSupported && supportsAnnotations(analysis.annotationNodes)
          ? await ensureAnnotationCategory(ANNOTATION_CATEGORY_COLOR)
          : null;
      const annotationResult = category
        ? applyAccessibilityAnnotations(analysis.annotationNodes, result.accessibility.issues, category)
        : buildResultOnlyAnnotation(result.accessibility.issues, "Dev Mode 주석을 사용할 수 없습니다.");

      result.accessibility.annotations = {
        annotationCount: annotationResult.annotationCount || 0,
        annotatedNodeCount: annotationResult.annotatedNodeCount || 0,
        clearedNodeCount: annotationResult.clearedNodeCount || 0,
        removedAnnotationCount: annotationResult.removedAnnotationCount || 0,
        categoryLabel: annotationResult.categoryLabel || ANNOTATION_CATEGORY_LABEL,
        modeLabel: annotationResult.modeLabel || "Result only",
      };
      result.summary.issueCount = result.accessibility.issueCount;
      result.summary.fixableCount = result.accessibility.fixableCount;
      result.summary.annotationCount = result.accessibility.annotations.annotationCount;

      result = await enrichDiagnosisWithAi(result);
      await writeCachedResult(result);

      figma.ui.postMessage({
        type: messageTypes.result,
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      if (runOptions.notify !== false) {
        figma.notify(runOptions.notifyMessage || "웹 접근성 진단 완료", { timeout: 1800 });
      }
    } catch (error) {
      const message = normalizeErrorMessage(error, "웹 접근성 진단에 실패했습니다.");
      figma.ui.postMessage({
        type: messageTypes.error,
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function applyIssueFix(message, messageMode) {
    const issueId = message && typeof message.issueId === "string" ? message.issueId.trim() : "";
    if (!issueId) {
      throw new Error("적용할 접근성 수정안을 찾지 못했습니다.");
    }

    postStatus("applying-fix", "선택한 접근성 수정안을 적용하고 있습니다.", {
      issueId,
    });
    if (resolveAccessibilityMessageMode(messageMode) === "accessibility") {
      figma.ui.postMessage({
        type: ACCESSIBILITY_MESSAGE_TYPES.status,
        status: "applying-fix",
        message: "",
        issueId: issueId,
      });
    }

    const cachedResult = await readCachedResult();
    const issues =
      cachedResult && cachedResult.accessibility && Array.isArray(cachedResult.accessibility.issues)
        ? cachedResult.accessibility.issues
        : [];
    const issue = issues.find((entry) => entry && entry.id === issueId);

    if (!issue || !issue.fixPlan) {
      throw new Error("선택한 수정안을 다시 찾지 못했습니다. 웹 접근성 진단을 다시 실행해 주세요.");
    }

    await applyFixPlan(issue.fixPlan);
    figma.notify("접근성 수정 적용 완료", { timeout: 1600 });
    await runAccessibilityDiagnosis({
      skipStatus: true,
      messageMode: messageMode,
      notifyMessage: "접근성 수정 적용 후 재진단 완료",
    });
  }

  async function postCachedResult(messageMode) {
    const messageTypes = getAccessibilityMessageTypes(messageMode);
    const result = await readCachedResult();
    figma.ui.postMessage({
      type: messageTypes.cache,
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  function postStatus(status, message, extra, messageMode) {
    const messageTypes = getAccessibilityMessageTypes(messageMode);
    const payload = extra && typeof extra === "object" ? extra : {};
    const body = {
      type: messageTypes.status,
      status,
      message,
    };
    Object.keys(payload).forEach((key) => {
      body[key] = payload[key];
    });
    figma.ui.postMessage(body);
  }

  async function readCachedResult() {
    const dedicated = await readCacheValue(AI_ACCESSIBILITY_CACHE_KEY);
    if (dedicated) {
      return normalizeCachedResult(dedicated);
    }

    const legacy = await readCacheValue(AI_DESIGN_READ_CACHE_KEY);
    return normalizeCachedResult(legacy);
  }

  async function writeCachedResult(result) {
    const normalized = normalizeCachedResult(result);
    await writeCacheValue(AI_ACCESSIBILITY_CACHE_KEY, normalized);
    await writeCacheValue(AI_DESIGN_READ_CACHE_KEY, normalized);
    return normalized;
  }

  async function readCacheValue(cacheKey) {
    try {
      return await figma.clientStorage.getAsync(cacheKey);
    } catch (error) {
      return null;
    }
  }

  async function writeCacheValue(cacheKey, value) {
    try {
      await figma.clientStorage.setAsync(cacheKey, value);
    } catch (error) {}
  }

  function normalizeCachedResult(value) {
    if (!value || typeof value !== "object") {
      return null;
    }
    return value;
  }

  function matchesCurrentSelection(result) {
    return !!result && result.selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function matchesSelectionSignature(selectionSignature) {
    return typeof selectionSignature === "string" && selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function analyzeCurrentSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택해 주세요.");
    }

    const rootBounds = [];
    const rootSummaries = [];
    const typeCounts = new Map();
    const colorCounts = new Map();
    const fontFamilyCounts = new Map();
    const fontSizeCounts = new Map();
    const nameCounts = new Map();
    const scriptCounts = {
      korean: 0,
      latin: 0,
      japanese: 0,
      cjk: 0,
      digit: 0,
      other: 0,
    };
    const suspiciousExamples = [];
    const duplicateExamples = [];
    const textSamples = [];
    const rootNames = [];
    const fractionalExamples = [];
    const fractionalNodeIds = new Set();
    const annotationNodes = [];
    const nodeEntries = [];
    const textEntries = [];
    const solidFillEntries = [];
    const nodeInsights = [];
    let orderCounter = 0;
    let suspiciousCount = 0;
    let fractionalValueCount = 0;
    const typeStats = {
      totalNodes: 0,
      rootCount: selection.length,
      frameCount: 0,
      groupCount: 0,
      textNodeCount: 0,
      vectorCount: 0,
      instanceCount: 0,
      componentCount: 0,
      sectionCount: 0,
      imageFillCount: 0,
      solidPaintCount: 0,
      effectNodeCount: 0,
      maskCount: 0,
      maxDepth: 0,
      textCharacterCount: 0,
      buttonLikeCount: 0,
    };

    const stack = [];
    for (let index = selection.length - 1; index >= 0; index -= 1) {
      const node = selection[index];
      stack.push({ node, depth: 1, rootId: node.id });

      const bounds = getNodeBounds(node);
      if (bounds) {
        rootBounds.push(bounds);
      }

      rootSummaries.push({
        id: node.id,
        name: safeName(node),
        type: String(node.type || "UNKNOWN"),
        width: bounds ? roundPixel(bounds.width) : null,
        height: bounds ? roundPixel(bounds.height) : null,
        childCount: hasChildren(node) ? node.children.length : 0,
      });
      rootNames.push(safeName(node));
    }

    while (stack.length > 0) {
      const current = stack.pop();
      if (!current || !current.node || !isNodeVisible(current.node)) {
        continue;
      }

      const node = current.node;
      const depth = current.depth;
      const rootId = current.rootId;
      const type = String(node.type || "UNKNOWN");
      const name = safeName(node);
      const bounds = getNodeBounds(node);
      const entry = {
        id: node.id,
        node,
        name,
        type,
        depth,
        rootId,
        order: orderCounter++,
        bounds,
      };

      nodeEntries.push(entry);
      if (supportsAnnotationsOnNode(node)) {
        annotationNodes.push(node);
      }

      typeStats.totalNodes += 1;
      typeStats.maxDepth = Math.max(typeStats.maxDepth, depth);
      bumpCount(typeCounts, type);
      bumpCount(nameCounts, canonicalizeName(name));

      if (type === "FRAME") {
        typeStats.frameCount += 1;
      } else if (type === "GROUP") {
        typeStats.groupCount += 1;
      } else if (type === "TEXT") {
        typeStats.textNodeCount += 1;
      } else if (type === "INSTANCE") {
        typeStats.instanceCount += 1;
      } else if (type === "SECTION") {
        typeStats.sectionCount += 1;
      } else if (type === "COMPONENT" || type === "COMPONENT_SET") {
        typeStats.componentCount += 1;
      }

      if (VECTOR_TYPES.has(type)) {
        typeStats.vectorCount += 1;
      }

      if (isMaskNode(node)) {
        typeStats.maskCount += 1;
      }

      if (hasVisibleEffects(node)) {
        typeStats.effectNodeCount += 1;
      }

      if (isSuspiciousName(name, type)) {
        suspiciousCount += 1;
        if (suspiciousExamples.length < 6) {
          suspiciousExamples.push(name);
        }
      }

      fractionalValueCount += inspectNumericFields(node, name, fractionalExamples, fractionalNodeIds);
      inspectPaintArray(node.fills, colorCounts, typeStats);
      inspectPaintArray(node.strokes, colorCounts, typeStats);

      const solidFill = getFirstVisibleSolidPaint(node.fills);
      if (solidFill && bounds && type !== "TEXT" && solidFill.opacity >= 0.95) {
        solidFillEntries.push(Object.assign({}, entry, {
          paintIndex: solidFill.paintIndex,
          color: cloneColor(solidFill.color),
          opacity: solidFill.opacity,
          hex: solidFill.hex,
        }));
      }

      if (type === "TEXT") {
        collectTextNodeSummary(node, textSamples, scriptCounts, fontFamilyCounts, fontSizeCounts, typeStats);
        const textEntry = buildTextEntry(node, entry);
        if (textEntry) {
          textEntries.push(textEntry);
        }
      }

      if (looksLikeButton(node)) {
        typeStats.buttonLikeCount += 1;
      }

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push({
            node: node.children[index],
            depth: depth + 1,
            rootId,
          });
        }
      }
    }

    const duplicateEntries = sortCountEntries(nameCounts).filter((entry) => entry.count > 1);
    for (let index = 0; index < duplicateEntries.length && duplicateExamples.length < 5; index += 1) {
      const entry = duplicateEntries[index];
      duplicateExamples.push(`${entry.key} ×${entry.count}`);
    }

    const selectionBounds = combineBounds(rootBounds);
    const languageSummary = summarizeLanguage(scriptCounts);
    const contextSummary = inferDesignContext({
      rootNames,
      textSamples,
      typeStats,
      selectionBounds,
      topTypes: sortCountEntries(typeCounts),
    });
    const topTypes = sortCountEntries(typeCounts)
      .slice(0, 5)
      .map((entry) => ({
        type: entry.key,
        label: formatNodeType(entry.key),
        count: entry.count,
      }));
    const topColors = sortCountEntries(colorCounts)
      .slice(0, 6)
      .map((entry) => ({
        value: entry.key,
        count: entry.count,
      }));
    const topFonts = sortCountEntries(fontFamilyCounts)
      .slice(0, 4)
      .map((entry) => ({
        value: entry.key,
        count: entry.count,
      }));
    const topFontSizes = sortCountEntries(fontSizeCounts)
      .slice(0, 4)
      .map((entry) => ({
        value: entry.key,
        count: entry.count,
      }));

    const accessibility = buildAccessibilitySummary(textEntries, nodeEntries, solidFillEntries);

    if (languageSummary.descriptor) {
      nodeInsights.push(`언어 추정: ${languageSummary.descriptor}`);
    }
    nodeInsights.push(`레이어 ${typeStats.totalNodes}개, 텍스트 ${typeStats.textNodeCount}개, 깊이 ${typeStats.maxDepth}단계`);
    if (contextSummary.label) {
      nodeInsights.push(`맥락 추정: ${contextSummary.label}`);
    }
    if (fractionalNodeIds.size > 0) {
      nodeInsights.push(`픽셀 퍼펙트 후보: ${fractionalNodeIds.size}개 레이어에서 소수점 값 ${fractionalValueCount}건 감지`);
    } else {
      nodeInsights.push("픽셀 퍼펙트 후보: 핵심 좌표/크기 값은 대부분 정수 기준");
    }
    if (suspiciousCount > 0 || duplicateEntries.length > 0) {
      nodeInsights.push(`리네이밍 후보: 기본 이름 ${suspiciousCount}개, 중복 이름 ${duplicateEntries.length}종`);
    }
    if (accessibility.issueCount > 0) {
      nodeInsights.push(
        `접근성 진단: 대비 ${formatContrastInsight(accessibility)} · 폰트 ${accessibility.fontSizeIssueCount}건 · 터치 ${accessibility.tapTargetIssueCount}건`
      );
      if (accessibility.fixableCount > 0) {
        nodeInsights.push(`즉시 적용 가능: ${accessibility.fixableCount}건`);
      }
    } else {
      nodeInsights.push("접근성 진단: 즉시 수정이 필요한 이슈를 찾지 못했습니다.");
    }

    const result = {
      version: PATCH_VERSION,
      source: "local-heuristic",
      selectionSignature: getSelectionSignature(selection),
      analyzedAt: new Date().toISOString(),
      roots: rootSummaries,
      selectionBounds: selectionBounds
        ? {
            width: roundPixel(selectionBounds.width),
            height: roundPixel(selectionBounds.height),
          }
        : null,
      summary: {
        selectionLabel: formatSelectionLabel(rootSummaries),
        languageLabel: languageSummary.descriptor,
        languageReason: languageSummary.reason,
        contextLabel: contextSummary.label,
        contextReason: contextSummary.reason,
        aiStatusLabel: "로컬 휴리스틱",
        aiProviderLabel: "",
        aiModelLabel: "",
        issueCount: accessibility.issueCount,
        fixableCount: accessibility.fixableCount,
        annotationCount: 0,
      },
      stats: {
        rootCount: rootSummaries.length,
        totalNodes: typeStats.totalNodes,
        textNodeCount: typeStats.textNodeCount,
        frameCount: typeStats.frameCount,
        groupCount: typeStats.groupCount,
        vectorCount: typeStats.vectorCount,
        instanceCount: typeStats.instanceCount,
        componentCount: typeStats.componentCount,
        sectionCount: typeStats.sectionCount,
        imageFillCount: typeStats.imageFillCount,
        effectNodeCount: typeStats.effectNodeCount,
        maskCount: typeStats.maskCount,
        maxDepth: typeStats.maxDepth,
        textCharacterCount: typeStats.textCharacterCount,
        buttonLikeCount: typeStats.buttonLikeCount,
        topTypes,
      },
      naming: {
        suspiciousCount,
        suspiciousExamples,
        duplicateNameCount: duplicateEntries.length,
        duplicateExamples,
      },
      pixel: {
        fractionalNodeCount: fractionalNodeIds.size,
        fractionalValueCount,
        examples: fractionalExamples.slice(0, 8),
      },
      typography: {
        topFonts,
        topFontSizes,
        textSamples: textSamples.slice(0, 6),
      },
      colors: {
        topColors,
      },
      accessibility,
      insights: nodeInsights.slice(0, 6),
    };

    return {
      result,
      annotationNodes,
    };
  }

  function buildAccessibilitySummary(textEntries, nodeEntries, solidFillEntries) {
    const contrastIssues = analyzeContrastIssues(textEntries, solidFillEntries);
    const fontSizeIssues = analyzeFontSizeIssues(textEntries);
    const tapTargetAnalysis = analyzeTapTargetIssues(nodeEntries);
    const contrastSummary = summarizeContrastIssues(contrastIssues);
    const issues = dedupeIssues(contrastIssues.concat(fontSizeIssues, tapTargetAnalysis.issues))
      .sort(compareIssues)
      .slice(0, MAX_ISSUE_COUNT);
    const fontSizes = fontSizeIssues
      .map((issue) => issue.currentFontSize)
      .filter((value) => typeof value === "number" && Number.isFinite(value));

    const smallestTapTarget = tapTargetAnalysis.issues.reduce((best, issue) => {
      if (!issue || !issue.currentSize) {
        return best;
      }
      if (!best) {
        return issue.currentSize;
      }
      const bestArea = best.width * best.height;
      const issueArea = issue.currentSize.width * issue.currentSize.height;
      return issueArea < bestArea ? issue.currentSize : best;
    }, null);

    return {
      standardLabel: "WCAG 2.2 AA",
      issueCount: issues.length,
      fixableCount: issues.filter((issue) => issue && issue.canApply).length,
      contrastIssueCount: contrastIssues.length,
      bodyTextContrastIssueCount: contrastSummary.bodyTextIssueCount,
      actionTextContrastIssueCount: contrastSummary.actionTextIssueCount,
      largeTextContrastIssueCount: contrastSummary.largeTextIssueCount,
      fontSizeIssueCount: fontSizeIssues.length,
      tapTargetIssueCount: tapTargetAnalysis.issues.length,
      minimumContrastRatio: contrastSummary.minimumContrastRatio,
      minimumBodyTextContrastRatio: contrastSummary.minimumBodyTextContrastRatio,
      minimumActionTextContrastRatio: contrastSummary.minimumActionTextContrastRatio,
      minimumLargeTextContrastRatio: contrastSummary.minimumLargeTextContrastRatio,
      smallestFontSize: fontSizes.length
        ? fontSizes.reduce((smallest, value) => (value < smallest ? value : smallest), fontSizes[0])
        : null,
      smallestTapTargetLabel: smallestTapTarget
        ? `${roundPixel(smallestTapTarget.width)} x ${roundPixel(smallestTapTarget.height)}px`
        : "",
      evaluatedTextCount: textEntries.length,
      tapTargetCandidateCount: tapTargetAnalysis.candidateCount,
      issues,
      annotations: {
        annotationCount: 0,
        annotatedNodeCount: 0,
        clearedNodeCount: 0,
        removedAnnotationCount: 0,
        categoryLabel: ANNOTATION_CATEGORY_LABEL,
        modeLabel: "Result only",
      },
    };
  }

  function summarizeContrastIssues(issues) {
    const summary = {
      bodyTextIssueCount: 0,
      actionTextIssueCount: 0,
      largeTextIssueCount: 0,
      minimumContrastRatio: null,
      minimumBodyTextContrastRatio: null,
      minimumActionTextContrastRatio: null,
      minimumLargeTextContrastRatio: null,
    };

    const trackMinimum = (field, value) => {
      if (typeof value !== "number" || !Number.isFinite(value)) {
        return;
      }
      summary[field] =
        typeof summary[field] === "number" && Number.isFinite(summary[field])
          ? Math.min(summary[field], value)
          : value;
      summary.minimumContrastRatio =
        typeof summary.minimumContrastRatio === "number" && Number.isFinite(summary.minimumContrastRatio)
          ? Math.min(summary.minimumContrastRatio, value)
          : value;
    };

    for (const issue of Array.isArray(issues) ? issues : []) {
      if (!issue || typeof issue.contrastKind !== "string") {
        continue;
      }

      if (issue.contrastKind === "action-text") {
        summary.actionTextIssueCount += 1;
        trackMinimum("minimumActionTextContrastRatio", issue.currentRatio);
        continue;
      }
      if (issue.contrastKind === "large-text") {
        summary.largeTextIssueCount += 1;
        trackMinimum("minimumLargeTextContrastRatio", issue.currentRatio);
        continue;
      }

      summary.bodyTextIssueCount += 1;
      trackMinimum("minimumBodyTextContrastRatio", issue.currentRatio);
    }

    return summary;
  }

  function formatContrastInsight(accessibility) {
    if (!accessibility || !accessibility.contrastIssueCount) {
      return "0건";
    }

    const parts = [];
    if (accessibility.bodyTextContrastIssueCount > 0) {
      parts.push(`본문 ${accessibility.bodyTextContrastIssueCount}건`);
    }
    if (accessibility.actionTextContrastIssueCount > 0) {
      parts.push(`버튼 ${accessibility.actionTextContrastIssueCount}건`);
    }
    if (accessibility.largeTextContrastIssueCount > 0) {
      parts.push(`큰 텍스트 ${accessibility.largeTextContrastIssueCount}건`);
    }

    return parts.length ? `${accessibility.contrastIssueCount}건 (${parts.join(" · ")})` : `${accessibility.contrastIssueCount}건`;
  }

  function analyzeContrastIssues(textEntries, solidFillEntries) {
    const issues = [];
    for (const textEntry of textEntries) {
      if (!textEntry || !textEntry.bounds || !textEntry.fill || textEntry.fill.opacity < 0.95) {
        continue;
      }

      const background = findBackgroundForText(textEntry, solidFillEntries);
      if (!background || background.opacity < 0.95) {
        continue;
      }

      const threshold = getContrastTarget(textEntry);
      const ratio = contrastRatio(textEntry.fill.color, background.color);
      if (!Number.isFinite(ratio) || ratio + 0.01 >= threshold) {
        continue;
      }

      const fixPlan = buildContrastFixPlan(textEntry, background, threshold);
      const contrastMeta = getContrastIssueMeta(textEntry, threshold);
      const suggestion = fixPlan
        ? buildContrastSuggestionText(fixPlan, threshold)
        : "배경색 또는 텍스트 색을 더 선명하게 조정하면 기준에 가까워질 수 있습니다.";

      issues.push({
        id: createIssueId(`contrast:${contrastMeta.kind}`, textEntry.node.id),
        type: contrastMeta.type,
        severity: resolveContrastSeverity(ratio, threshold),
        wcag: threshold >= NORMAL_TEXT_CONTRAST_RATIO ? "WCAG 1.4.3" : "WCAG 1.4.3 (큰 텍스트)",
        nodeId: textEntry.node.id,
        nodeName: textEntry.name,
        nodeType: textEntry.type,
        summary: `${contrastMeta.label} 대비 ${ratio.toFixed(2)}:1`,
        detail: `${contrastMeta.label} 대비가 권장 기준 ${threshold}:1에 못 미칩니다.`,
        suggestion,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "제안 적용" : "",
        fixPlan,
        contrastKind: contrastMeta.kind,
        currentRatio: ratio,
        requiredContrastRatio: threshold,
        currentSize: null,
        currentFontSize: null,
      });
    }

    return issues;
  }

  function analyzeFontSizeIssues(textEntries) {
    const issues = [];
    for (const textEntry of textEntries) {
      if (!textEntry || typeof textEntry.fontSize !== "number" || !Number.isFinite(textEntry.fontSize)) {
        continue;
      }

      const recommendedSize = textEntry.isActionText ? MIN_ACTION_FONT_SIZE : MIN_BODY_FONT_SIZE;
      if (textEntry.fontSize + 0.01 >= recommendedSize) {
        continue;
      }

      const fixPlan = canAdjustFontSize(textEntry.node)
        ? {
            action: "set-font-size",
            targetNodeId: textEntry.node.id,
            targetFontSize: recommendedSize,
          }
        : null;

      issues.push({
        id: createIssueId("font-size", textEntry.node.id),
        type: "font-size",
        severity: textEntry.fontSize < Math.max(12, recommendedSize - 2) ? "warning" : "info",
        wcag: "가독성 권장",
        nodeId: textEntry.node.id,
        nodeName: textEntry.name,
        nodeType: textEntry.type,
        summary: `폰트 크기 ${roundValue(textEntry.fontSize)}px`,
        detail: textEntry.isActionText
          ? `버튼/액션 텍스트는 ${recommendedSize}px 이상에서 읽기와 탭 정확도가 좋아집니다.`
          : `본문 텍스트는 ${recommendedSize}px 이상에서 읽기 편한 경우가 많습니다.`,
        suggestion: `폰트 크기를 ${recommendedSize}px로 키우면 가독성이 좋아집니다.`,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "크기 적용" : "",
        fixPlan,
        currentRatio: null,
        currentSize: null,
        currentFontSize: textEntry.fontSize,
      });
    }

    return issues;
  }

  function analyzeTapTargetIssues(nodeEntries) {
    const issues = [];
    const seen = new Set();
    let candidateCount = 0;

    for (const entry of nodeEntries) {
      const candidate = resolveTapTargetCandidate(entry);
      if (!candidate) {
        continue;
      }

      if (seen.has(candidate.id)) {
        continue;
      }
      seen.add(candidate.id);
      candidateCount += 1;

      const bounds = candidate.bounds;
      if (!bounds) {
        continue;
      }

      if (bounds.width + 0.01 >= MIN_TAP_TARGET && bounds.height + 0.01 >= MIN_TAP_TARGET) {
        continue;
      }

      const targetWidth = Math.max(MIN_TAP_TARGET, roundPixel(bounds.width));
      const targetHeight = Math.max(MIN_TAP_TARGET, roundPixel(bounds.height));
      const fixPlan = canResizeNode(candidate.node)
        ? {
            action: "resize-node",
            targetNodeId: candidate.node.id,
            targetWidth,
            targetHeight,
          }
        : null;

      issues.push({
        id: createIssueId("tap-target", candidate.node.id),
        type: "tap-target",
        severity: resolveTapTargetSeverity(bounds),
        wcag: "WCAG 2.5.8",
        nodeId: candidate.node.id,
        nodeName: candidate.name,
        nodeType: candidate.type,
        summary: `터치 영역 ${roundPixel(bounds.width)} x ${roundPixel(bounds.height)}px`,
        detail: "모바일 웹/앱에서 터치 대상은 44 x 44px 이상일 때 누르기 편합니다.",
        suggestion: `터치 영역을 ${targetWidth} x ${targetHeight}px 이상으로 키우면 탭 정확도가 좋아집니다.`,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "영역 적용" : "",
        fixPlan,
        currentRatio: null,
        currentSize: {
          width: bounds.width,
          height: bounds.height,
        },
        currentFontSize: null,
      });
    }

    return {
      issues,
      candidateCount,
    };
  }

  async function enrichDiagnosisWithAi(localResult) {
    const ai = getAiHelper();
    if (!ai) {
      localResult.summary.aiStatusLabel = "로컬 휴리스틱";
      return localResult;
    }

    let configured = false;
    let runInfo = {
      provider: "",
      model: "",
    };
    try {
      const settings = typeof ai.getAiSettingsAsync === "function" ? await ai.getAiSettingsAsync() : null;
      if (settings && typeof ai.getResolvedRunInfo === "function") {
        runInfo = ai.getResolvedRunInfo(settings);
      }
      configured = await ai.hasConfiguredAiAsync();
    } catch (error) {
      configured = false;
    }

    if (!configured) {
      localResult.summary.aiStatusLabel = "로컬 휴리스틱";
      return localResult;
    }

    localResult.summary.aiStatusLabel = "AI + 로컬";
    localResult.summary.aiProviderLabel = formatProviderLabel(runInfo.provider);
    localResult.summary.aiModelLabel = runInfo.model || "";

    const payload = {
      selectionLabel: localResult.summary ? localResult.summary.selectionLabel : "",
      roots: Array.isArray(localResult.roots) ? localResult.roots.slice(0, 6) : [],
      selectionBounds: localResult.selectionBounds || null,
      stats: localResult.stats || {},
      naming: localResult.naming || {},
      pixel: localResult.pixel || {},
      typography: localResult.typography || {},
      colors: localResult.colors || {},
      currentSummary: localResult.summary || {},
      currentInsights: Array.isArray(localResult.insights) ? localResult.insights.slice(0, 6) : [],
      accessibility: {
        issueCount: localResult.accessibility ? localResult.accessibility.issueCount : 0,
        contrastIssueCount: localResult.accessibility ? localResult.accessibility.contrastIssueCount : 0,
        bodyTextContrastIssueCount: localResult.accessibility ? localResult.accessibility.bodyTextContrastIssueCount || 0 : 0,
        actionTextContrastIssueCount: localResult.accessibility ? localResult.accessibility.actionTextContrastIssueCount || 0 : 0,
        largeTextContrastIssueCount: localResult.accessibility ? localResult.accessibility.largeTextContrastIssueCount || 0 : 0,
        fontSizeIssueCount: localResult.accessibility ? localResult.accessibility.fontSizeIssueCount : 0,
        tapTargetIssueCount: localResult.accessibility ? localResult.accessibility.tapTargetIssueCount : 0,
        issues: Array.isArray(localResult.accessibility && localResult.accessibility.issues)
          ? localResult.accessibility.issues.slice(0, 6).map((issue) => ({
              id: issue.id,
              type: issue.type,
              summary: issue.summary,
              detail: issue.detail,
              suggestion: issue.suggestion,
            }))
          : [],
      },
    };
    const schema = {
      type: "object",
      properties: {
        languageLabel: { type: "string" },
        contextLabel: { type: "string" },
        contextReason: { type: "string" },
        contentSummary: { type: "string" },
        namingSummary: { type: "string" },
        pixelSummary: { type: "string" },
        accessibilitySummary: { type: "string" },
        prioritySummary: { type: "string" },
        insights: {
          type: "array",
          items: { type: "string" },
        },
      },
      required: [
        "languageLabel",
        "contextLabel",
        "contextReason",
        "contentSummary",
        "namingSummary",
        "pixelSummary",
        "accessibilitySummary",
        "prioritySummary",
        "insights",
      ],
    };

    try {
      const response = await ai.requestJsonTask({
        instructions:
          "You analyze structured Figma metadata plus accessibility findings for a design correction plugin. Return concise Korean JSON. Keep labels short and practical. Infer the screen context, summarize naming and pixel issues, and explain accessibility priorities without inventing numeric values that are not already in the payload.",
        schema,
        payload,
      });
      if (!response || typeof response !== "object") {
        return localResult;
      }

      if (typeof response.languageLabel === "string" && response.languageLabel.trim()) {
        localResult.summary.languageLabel = response.languageLabel.trim();
      }
      if (typeof response.contextLabel === "string" && response.contextLabel.trim()) {
        localResult.summary.contextLabel = response.contextLabel.trim();
      }
      if (typeof response.contextReason === "string" && response.contextReason.trim()) {
        localResult.summary.contextReason = response.contextReason.trim();
      }
      if (typeof response.contentSummary === "string" && response.contentSummary.trim()) {
        localResult.summary.contentSummary = response.contentSummary.trim();
      }
      if (typeof response.namingSummary === "string" && response.namingSummary.trim()) {
        localResult.naming.aiSummary = response.namingSummary.trim();
      }
      if (typeof response.pixelSummary === "string" && response.pixelSummary.trim()) {
        localResult.pixel.aiSummary = response.pixelSummary.trim();
      }
      if (typeof response.accessibilitySummary === "string" && response.accessibilitySummary.trim()) {
        localResult.accessibility.aiSummary = response.accessibilitySummary.trim();
      }
      if (typeof response.prioritySummary === "string" && response.prioritySummary.trim()) {
        localResult.accessibility.prioritySummary = response.prioritySummary.trim();
      }

      localResult.summary.aiProviderLabel = formatProviderLabel(response._provider || runInfo.provider);
      localResult.summary.aiModelLabel = response._model || runInfo.model || "";
      localResult.source = typeof response._provider === "string" ? response._provider : "hybrid-ai";
      localResult.insights = mergeInsights(ai, localResult, response);
    } catch (error) {
      localResult.aiError = normalizeErrorMessage(error, "AI 접근성 요약에 실패했습니다.");
    }

    return localResult;
  }

  function mergeInsights(ai, localResult, response) {
    const aiInsights = [];
    if (typeof response.contentSummary === "string" && response.contentSummary.trim()) {
      aiInsights.push("콘텐츠 요약: " + response.contentSummary.trim());
    }
    if (typeof response.namingSummary === "string" && response.namingSummary.trim()) {
      aiInsights.push("AI 네이밍 판단: " + response.namingSummary.trim());
    }
    if (typeof response.pixelSummary === "string" && response.pixelSummary.trim()) {
      aiInsights.push("AI 픽셀 판단: " + response.pixelSummary.trim());
    }
    if (typeof response.accessibilitySummary === "string" && response.accessibilitySummary.trim()) {
      aiInsights.push("AI 접근성 요약: " + response.accessibilitySummary.trim());
    }
    if (typeof response.prioritySummary === "string" && response.prioritySummary.trim()) {
      aiInsights.push("우선 순위: " + response.prioritySummary.trim());
    }
    if (Array.isArray(response.insights)) {
      for (const item of response.insights) {
        if (typeof item === "string" && item.trim()) {
          aiInsights.push(item.trim());
        }
      }
    }
    return ai.uniqueStrings(aiInsights.concat(Array.isArray(localResult.insights) ? localResult.insights : []), 6);
  }

  async function applyFixPlan(plan) {
    if (!plan || typeof plan !== "object" || typeof plan.action !== "string") {
      throw new Error("적용 가능한 수정안 정보가 없습니다.");
    }

    if (plan.action === "set-solid-fill-color") {
      applySolidFillColorPlan(plan);
      return;
    }
    if (plan.action === "set-font-size") {
      await applyFontSizePlan(plan);
      return;
    }
    if (plan.action === "resize-node") {
      applyResizePlan(plan);
      return;
    }

    throw new Error("아직 지원하지 않는 수정안입니다.");
  }

  function applySolidFillColorPlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    const index = typeof plan.paintIndex === "number" ? plan.paintIndex : 0;
    if (!canEditSolidPaint(node, index)) {
      throw new Error("색상을 변경할 레이어를 찾지 못했습니다.");
    }

    const fills = node.fills.map((paint) => clonePlainObject(paint));
    const paint = fills[index];
    if (!paint || paint.type !== "SOLID") {
      throw new Error("색상을 적용할 수 있는 SOLID fill이 없습니다.");
    }

    paint.color = cloneColor(plan.color);
    fills[index] = paint;
    node.fills = fills;
  }

  async function applyFontSizePlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    if (!canAdjustFontSize(node)) {
      throw new Error("폰트 크기를 조정할 텍스트 레이어를 찾지 못했습니다.");
    }

    await loadFontsForTextNode(node);
    const targetFontSize = typeof plan.targetFontSize === "number" ? plan.targetFontSize : null;
    if (!targetFontSize || !Number.isFinite(targetFontSize)) {
      throw new Error("적용할 폰트 크기 값이 올바르지 않습니다.");
    }

    if (typeof node.fontSize === "number" && node.fontSize !== figma.mixed) {
      node.fontSize = targetFontSize;
      return;
    }

    if (typeof node.setRangeFontSize === "function" && typeof node.characters === "string") {
      node.setRangeFontSize(0, node.characters.length, targetFontSize);
      return;
    }

    throw new Error("이 텍스트 레이어는 폰트 크기를 직접 조정할 수 없습니다.");
  }

  function applyResizePlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    if (!node || !canResizeNode(node)) {
      throw new Error("터치 영역을 조정할 레이어를 찾지 못했습니다.");
    }

    const width = Math.max(1, Number(plan.targetWidth) || 0);
    const height = Math.max(1, Number(plan.targetHeight) || 0);
    if (typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(width, height);
      return;
    }
    if (typeof node.resize === "function") {
      node.resize(width, height);
      return;
    }

    throw new Error("이 레이어는 크기 조정을 지원하지 않습니다.");
  }

  function supportsAnnotations(nodes) {
    const sampleNode = Array.isArray(nodes) && nodes.length > 0 ? nodes[0] : null;
    return !!sampleNode && "annotations" in sampleNode;
  }

  async function ensureAnnotationCategory(requestedColor) {
    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return null;
    }

    try {
      const nextColor =
        typeof requestedColor === "string" && requestedColor.trim()
          ? requestedColor.trim().toLowerCase()
          : ANNOTATION_CATEGORY_COLOR;
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      const existing = Array.isArray(categories)
        ? categories.find((category) => category && category.label === ANNOTATION_CATEGORY_LABEL)
        : null;
      if (existing) {
        if (typeof existing.setColor === "function" && existing.color !== nextColor) {
          existing.setColor(nextColor);
        }
        return existing;
      }

      if (typeof figma.annotations.addAnnotationCategoryAsync !== "function") {
        return null;
      }

      return await figma.annotations.addAnnotationCategoryAsync({
        label: ANNOTATION_CATEGORY_LABEL,
        color: nextColor,
      });
    } catch (error) {
      return null;
    }
  }

  function applyAccessibilityAnnotations(nodes, issues, category) {
    const issuesByNode = new Map();
    const skipped = [];
    let annotationCount = 0;
    let annotatedNodeCount = 0;
    let clearedNodeCount = 0;
    let removedAnnotationCount = 0;

    for (const issue of issues) {
      const bucket = issuesByNode.get(issue.nodeId) || [];
      bucket.push(issue);
      issuesByNode.set(issue.nodeId, bucket);
    }

    for (const node of nodes) {
      const nodeIssues = issuesByNode.get(node.id) || [];
      try {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        const preserved = existing.filter((annotation) => !isManagedAccessibilityAnnotation(annotation, category));
        const removedCount = Math.max(0, existing.length - preserved.length);

        if (removedCount > 0) {
          removedAnnotationCount += removedCount;
        }
        if (removedCount > 0 && nodeIssues.length === 0) {
          clearedNodeCount += 1;
        }

        node.annotations = preserved.concat(nodeIssues.map((issue) => buildAccessibilityAnnotation(issue, category)));
        if (nodeIssues.length > 0) {
          annotatedNodeCount += 1;
          annotationCount += nodeIssues.length;
        }
      } catch (error) {
        if (nodeIssues.length > 0) {
          skipped.push({
            label: safeName(node),
            reason: normalizeErrorMessage(error, "접근성 주석을 남기지 못했습니다."),
          });
        }
      }
    }

    return {
      skipped,
      annotationCount,
      annotatedNodeCount,
      clearedNodeCount,
      removedAnnotationCount,
      modeLabel: annotationCount > 0 ? "Green Dev Mode annotation" : "Result only",
      categoryLabel: category && category.label ? category.label : ANNOTATION_CATEGORY_LABEL,
    };
  }

  function buildAccessibilityAnnotation(issue, category) {
    const lines = [`[중요도 : ${getIssuePriorityLabel(issue)}]`];
    if (issue.summary) {
      lines.push(`- ${issue.summary}`);
    }
    if (issue.suggestion) {
      lines.push(`- ${issue.suggestion}`);
    }

    const annotation = {
      label: lines.join("\n"),
    };
    if (category && category.id) {
      annotation.categoryId = category.id;
    }
    return annotation;
  }

  function getIssuePriorityLabel(issue) {
    const severity = issue && typeof issue.severity === "string" ? issue.severity : "info";
    if (severity === "error") {
      return "상";
    }
    if (severity === "warning") {
      return "중";
    }
    return "하";
  }

  function isManagedAccessibilityAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }

    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }

    const label =
      typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return typeof label === "string" && (label.startsWith(ANNOTATION_LABEL_PREFIX) || label.startsWith(`[${ANNOTATION_LABEL_PREFIX}]`));
  }

  function buildResultOnlyAnnotation(issues, reason) {
    const skipped = [];
    if (Array.isArray(issues) && issues.length > 0 && reason) {
      skipped.push({
        label: "주석 미적용",
        reason,
      });
    }

    return {
      skipped,
      annotationCount: 0,
      annotatedNodeCount: 0,
      clearedNodeCount: 0,
      removedAnnotationCount: 0,
      modeLabel: "Result only",
      categoryLabel: ANNOTATION_CATEGORY_LABEL,
    };
  }

  function getAiHelper() {
    const helper = globalScope.__PIGMA_AI_LLM__;
    return helper &&
      typeof helper.requestJsonTask === "function" &&
      typeof helper.hasConfiguredAiAsync === "function" &&
      typeof helper.uniqueStrings === "function"
      ? helper
      : null;
  }

  function buildTextEntry(node, entry) {
    const bounds = entry.bounds;
    if (!bounds) {
      return null;
    }

    const fill = getFirstVisibleSolidPaint(node.fills);
    const textValue = getTextValue(node);
    const fontSize = typeof node.fontSize === "number" && Number.isFinite(node.fontSize) ? node.fontSize : null;
    const fontWeight = getFontWeight(node);
    return Object.assign({}, entry, {
      text: textValue,
      fill,
      fontSize,
      fontWeight,
      isActionText: looksLikeActionText(textValue) || looksLikeButton(node),
    });
  }

  function getTextValue(node) {
    const value = typeof node.characters === "string" ? node.characters.replace(/\s+/g, " ").trim() : "";
    return value || safeName(node);
  }

  function getFontWeight(node) {
    if (typeof node.fontWeight === "number" && Number.isFinite(node.fontWeight)) {
      return node.fontWeight;
    }
    if (node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      return fontWeightFromStyle(node.fontName.style);
    }
    return 400;
  }

  function fontWeightFromStyle(style) {
    const value = String(style || "").toLowerCase();
    if (/black|heavy|extrabold|extra bold|bold/.test(value)) {
      return 700;
    }
    if (/semibold|semi bold|demi/.test(value)) {
      return 600;
    }
    if (/medium/.test(value)) {
      return 500;
    }
    return 400;
  }

  function getContrastTarget(textEntry) {
    const fontSize = typeof textEntry.fontSize === "number" ? textEntry.fontSize : 0;
    const fontWeight = typeof textEntry.fontWeight === "number" ? textEntry.fontWeight : 400;
    return fontSize >= 24 || (fontSize >= 18.5 && fontWeight >= 700)
      ? LARGE_TEXT_CONTRAST_RATIO
      : NORMAL_TEXT_CONTRAST_RATIO;
  }

  function getContrastIssueMeta(textEntry, threshold) {
    if (textEntry && textEntry.isActionText) {
      return {
        kind: "action-text",
        type: "contrast-action-text",
        label: "버튼/액션 텍스트",
      };
    }

    if (threshold < NORMAL_TEXT_CONTRAST_RATIO) {
      return {
        kind: "large-text",
        type: "contrast-large-text",
        label: "큰 텍스트",
      };
    }

    return {
      kind: "body-text",
      type: "contrast-body-text",
      label: "본문 텍스트",
    };
  }

  function resolveTapTargetCandidate(entry) {
    if (!entry || !entry.node || !entry.bounds) {
      return null;
    }

    if (entry.type === "PAGE" || entry.type === "SECTION" || entry.type === "COMPONENT_SET") {
      return null;
    }

    if (entry.type === "TEXT") {
      const textValue = getTextValue(entry.node);
      if (!looksLikeActionText(textValue)) {
        return null;
      }

      const parent = entry.node.parent;
      if (!parent || parent.type === "PAGE" || parent.type === "SECTION") {
        return null;
      }

      const parentBounds = getNodeBounds(parent);
      if (!parentBounds) {
        return null;
      }

      return {
        id: parent.id,
        node: parent,
        name: safeName(parent),
        type: String(parent.type || "UNKNOWN"),
        bounds: parentBounds,
      };
    }

    const bounds = entry.bounds;
    if (bounds.width > 360 || bounds.height > 160) {
      return null;
    }

    if (hasReactions(entry.node)) {
      return entry;
    }

    const name = safeName(entry.node);
    if (CONTROL_NAME_PATTERN.test(name)) {
      return entry;
    }

    const textValue = truncateText(collectNodeTexts(entry.node, 2, 2).join(" "), 36);
    if (looksLikeActionText(textValue) && (hasVisibleFillOrStroke(entry.node) || hasCornerRadius(entry.node) || hasLayout(entry.node))) {
      return entry;
    }

    return null;
  }

  function findBackgroundForText(textEntry, solidFillEntries) {
    let current = textEntry.node.parent;
    while (current && current.type !== "PAGE") {
      const bounds = getNodeBounds(current);
      const fill = getFirstVisibleSolidPaint(current.fills);
      if (bounds && fill && fill.opacity >= 0.95 && containsBounds(bounds, textEntry.bounds)) {
        return {
          node: current,
          name: safeName(current),
          type: String(current.type || "UNKNOWN"),
          bounds,
          color: cloneColor(fill.color),
          opacity: fill.opacity,
          paintIndex: fill.paintIndex,
          hex: fill.hex,
        };
      }
      current = current.parent;
    }

    let best = null;
    for (const entry of solidFillEntries) {
      if (!entry || entry.rootId !== textEntry.rootId || entry.id === textEntry.id) {
        continue;
      }
      if (entry.order >= textEntry.order || !entry.bounds) {
        continue;
      }

      const contains = containsBounds(entry.bounds, textEntry.bounds);
      const overlapRatio = overlapArea(entry.bounds, textEntry.bounds) / Math.max(1, textEntry.bounds.width * textEntry.bounds.height);
      if (!contains && overlapRatio < 0.6) {
        continue;
      }

      if (!best) {
        best = { entry, contains };
        continue;
      }

      const currentArea = entry.bounds.width * entry.bounds.height;
      const bestArea = best.entry.bounds.width * best.entry.bounds.height;
      if (contains && !best.contains) {
        best = { entry, contains };
        continue;
      }
      if (contains === best.contains && currentArea < bestArea) {
        best = { entry, contains };
      }
    }

    return best
      ? {
          node: best.entry.node,
          name: best.entry.name,
          type: best.entry.type,
          bounds: best.entry.bounds,
          color: cloneColor(best.entry.color),
          opacity: best.entry.opacity,
          paintIndex: best.entry.paintIndex,
          hex: best.entry.hex,
        }
      : null;
  }

  function buildContrastFixPlan(textEntry, background, threshold) {
    const candidates = [];
    if (textEntry.fill && canEditSolidPaint(textEntry.node, textEntry.fill.paintIndex)) {
      const color = findAccessibleReplacement(background.color, textEntry.fill.color, threshold);
      if (color) {
        candidates.push({
          action: "set-solid-fill-color",
          targetNodeId: textEntry.node.id,
          paintIndex: textEntry.fill.paintIndex,
          color,
          colorHex: rgbToHex(color),
          targetLabel: "텍스트 색상",
          previewRatio: contrastRatio(color, background.color),
        });
      }
    }

    if (background && canEditSolidPaint(background.node, background.paintIndex)) {
      const color = findAccessibleReplacement(textEntry.fill.color, background.color, threshold);
      if (color) {
        candidates.push({
          action: "set-solid-fill-color",
          targetNodeId: background.node.id,
          paintIndex: background.paintIndex,
          color,
          colorHex: rgbToHex(color),
          targetLabel: "배경색",
          previewRatio: contrastRatio(textEntry.fill.color, color),
        });
      }
    }

    if (!candidates.length) {
      return null;
    }

    candidates.sort((left, right) => {
      const leftBase = left.targetLabel === "배경색" ? background.color : textEntry.fill.color;
      const rightBase = right.targetLabel === "배경색" ? background.color : textEntry.fill.color;
      const leftDistance = colorDistance(left.color, leftBase);
      const rightDistance = colorDistance(right.color, rightBase);
      if (leftDistance !== rightDistance) {
        return leftDistance - rightDistance;
      }
      if (left.targetLabel === "배경색" && right.targetLabel !== "배경색") {
        return -1;
      }
      if (left.targetLabel !== "배경색" && right.targetLabel === "배경색") {
        return 1;
      }
      return 0;
    });

    return candidates[0];
  }

  function buildContrastSuggestionText(fixPlan, threshold) {
    const ratioText =
      typeof fixPlan.previewRatio === "number" && Number.isFinite(fixPlan.previewRatio)
        ? ` (${fixPlan.previewRatio.toFixed(2)}:1)`
        : "";
    return `${fixPlan.targetLabel}을 ${fixPlan.colorHex}로 변경하면 명도 대비 ${threshold}:1 기준을 통과할 수 있습니다${ratioText}.`;
  }

  function dedupeIssues(issues) {
    const map = new Map();
    for (const issue of issues) {
      if (!issue || !issue.id || map.has(issue.id)) {
        continue;
      }
      map.set(issue.id, issue);
    }
    return Array.from(map.values());
  }

  function compareIssues(left, right) {
    const severityOrder = {
      error: 0,
      warning: 1,
      info: 2,
    };
    const leftSeverityKey = left && left.severity ? left.severity : "info";
    const rightSeverityKey = right && right.severity ? right.severity : "info";
    const leftSeverityValue = severityOrder[leftSeverityKey];
    const rightSeverityValue = severityOrder[rightSeverityKey];
    const leftSeverity = typeof leftSeverityValue === "number" ? leftSeverityValue : 3;
    const rightSeverity = typeof rightSeverityValue === "number" ? rightSeverityValue : 3;
    if (leftSeverity !== rightSeverity) {
      return leftSeverity - rightSeverity;
    }
    return String(left.summary || "").localeCompare(String(right.summary || ""));
  }

  function resolveContrastSeverity(ratio, threshold) {
    if (!Number.isFinite(ratio)) {
      return "warning";
    }

    const criticalThreshold =
      threshold >= NORMAL_TEXT_CONTRAST_RATIO ? CRITICAL_NORMAL_TEXT_CONTRAST_RATIO : CRITICAL_LARGE_TEXT_CONTRAST_RATIO;
    return ratio <= criticalThreshold ? "error" : "warning";
  }

  function resolveTapTargetSeverity(bounds) {
    if (!bounds) {
      return "warning";
    }

    return bounds.width < CRITICAL_TAP_TARGET || bounds.height < CRITICAL_TAP_TARGET ? "error" : "warning";
  }

  function findAccessibleReplacement(fixedColor, currentColor, threshold) {
    if (contrastRatio(fixedColor, currentColor) >= threshold) {
      return cloneColor(currentColor);
    }

    const endpoints = [
      { r: 0, g: 0, b: 0 },
      { r: 1, g: 1, b: 1 },
    ];
    const candidates = [];

    for (const endpoint of endpoints) {
      if (contrastRatio(fixedColor, endpoint) + 0.0001 < threshold) {
        continue;
      }

      let low = 0;
      let high = 1;
      for (let index = 0; index < 24; index += 1) {
        const mid = (low + high) / 2;
        const candidate = mixColors(currentColor, endpoint, mid);
        if (contrastRatio(fixedColor, candidate) >= threshold) {
          high = mid;
        } else {
          low = mid;
        }
      }
      candidates.push(mixColors(currentColor, endpoint, high));
    }

    if (!candidates.length) {
      return null;
    }

    candidates.sort((left, right) => colorDistance(left, currentColor) - colorDistance(right, currentColor));
    return candidates[0];
  }

  function contrastRatio(left, right) {
    const leftL = relativeLuminance(left);
    const rightL = relativeLuminance(right);
    const lighter = Math.max(leftL, rightL);
    const darker = Math.min(leftL, rightL);
    return (lighter + 0.05) / (darker + 0.05);
  }

  function relativeLuminance(color) {
    const toLinear = (value) => {
      const channel = Math.max(0, Math.min(1, Number(value) || 0));
      return channel <= 0.03928 ? channel / 12.92 : Math.pow((channel + 0.055) / 1.055, 2.4);
    };
    const red = toLinear(color.r);
    const green = toLinear(color.g);
    const blue = toLinear(color.b);
    return 0.2126 * red + 0.7152 * green + 0.0722 * blue;
  }

  function mixColors(source, target, amount) {
    const ratio = Math.max(0, Math.min(1, amount));
    return {
      r: source.r + (target.r - source.r) * ratio,
      g: source.g + (target.g - source.g) * ratio,
      b: source.b + (target.b - source.b) * ratio,
    };
  }

  function colorDistance(left, right) {
    const red = left.r - right.r;
    const green = left.g - right.g;
    const blue = left.b - right.b;
    return Math.sqrt(red * red + green * green + blue * blue);
  }

  function supportsAnnotationsOnNode(node) {
    return !!node && "annotations" in node;
  }

  function canEditSolidPaint(node, paintIndex) {
    if (!isDirectlyEditableNode(node)) {
      return false;
    }
    if (!("fills" in node) || !Array.isArray(node.fills) || typeof paintIndex !== "number" || paintIndex < 0) {
      return false;
    }
    const paint = node.fills[paintIndex];
    return !!paint && paint.visible !== false && paint.type === "SOLID" && !!paint.color;
  }

  function canAdjustFontSize(node) {
    return (
      !!node &&
      node.type === "TEXT" &&
      isDirectlyEditableNode(node) &&
      typeof node.characters === "string" &&
      node.characters.length > 0 &&
      typeof node.setRangeFontSize === "function" &&
      collectEditableFontNames(node).length > 0
    );
  }

  function canResizeNode(node) {
    if (!isDirectlyEditableNode(node)) {
      return false;
    }
    if (node.type === "GROUP" || node.type === "TEXT" || node.type === "LINE") {
      return false;
    }
    if (!canResizeInParentAutoLayout(node)) {
      return false;
    }
    return typeof node.resizeWithoutConstraints === "function" || typeof node.resize === "function";
  }

  function isDirectlyEditableNode(node) {
    return !!node && !hasLockedAncestor(node) && !hasInstanceAncestor(node);
  }

  function hasLockedAncestor(node) {
    let current = node;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.locked === true) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function hasInstanceAncestor(node) {
    let current = node;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.type === "INSTANCE") {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function canResizeInParentAutoLayout(node) {
    if (!node || !node.parent || typeof node.parent.layoutMode !== "string" || node.parent.layoutMode === "NONE") {
      return true;
    }
    if (node.layoutPositioning === "ABSOLUTE") {
      return true;
    }

    const horizontalMode =
      typeof node.layoutSizingHorizontal === "string" ? node.layoutSizingHorizontal : "FIXED";
    const verticalMode = typeof node.layoutSizingVertical === "string" ? node.layoutSizingVertical : "FIXED";
    return horizontalMode === "FIXED" && verticalMode === "FIXED";
  }

  function hasVisibleFillOrStroke(node) {
    return hasVisibleSolidPaint(node.fills) || hasVisibleSolidPaint(node.strokes);
  }

  function hasVisibleSolidPaint(paints) {
    return !!getFirstVisibleSolidPaint(paints);
  }

  function hasCornerRadius(node) {
    const fields = [
      node.cornerRadius,
      node.topLeftRadius,
      node.topRightRadius,
      node.bottomRightRadius,
      node.bottomLeftRadius,
    ];
    return fields.some((value) => typeof value === "number" && Number.isFinite(value) && value > 0);
  }

  function hasLayout(node) {
    return !!node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function hasReactions(node) {
    return !!node && Array.isArray(node.reactions) && node.reactions.length > 0;
  }

  function looksLikeActionText(value) {
    const normalized = String(value || "").replace(/\s+/g, " ").trim();
    if (!normalized || normalized.length > 24) {
      return false;
    }
    return ACTION_TEXT_PATTERN.test(normalized.toLowerCase());
  }

  function collectNodeTexts(node, limit, depth) {
    const texts = [];
    const maxCount = typeof limit === "number" ? limit : 2;
    const maxDepth = typeof depth === "number" ? depth : 2;

    const walk = (current, currentDepth) => {
      if (!current || texts.length >= maxCount || currentDepth > maxDepth) {
        return;
      }
      if (current.type === "TEXT") {
        const value = getTextValue(current);
        if (value) {
          texts.push(value);
        }
        return;
      }
      if (!hasChildren(current)) {
        return;
      }
      for (const child of current.children) {
        if (texts.length >= maxCount) {
          break;
        }
        walk(child, currentDepth + 1);
      }
    };

    walk(node, 0);
    return texts;
  }

  function truncateText(value, limit) {
    const normalized = String(value || "").replace(/\s+/g, " ").trim();
    const maxLength = typeof limit === "number" ? limit : 48;
    if (!normalized) {
      return "";
    }
    return normalized.length > maxLength ? `${normalized.slice(0, maxLength - 3)}...` : normalized;
  }

  function collectTextNodeSummary(node, textSamples, scriptCounts, fontFamilyCounts, fontSizeCounts, typeStats) {
    const value = typeof node.characters === "string" ? node.characters.trim() : "";
    if (value) {
      if (textSamples.length < 6 && !textSamples.includes(value)) {
        textSamples.push(value.length > 72 ? `${value.slice(0, 69)}...` : value);
      }
      addScriptCounts(scriptCounts, value);
      typeStats.textCharacterCount += value.length;
    }

    collectFontNames(node, fontFamilyCounts);
    if (typeof node.fontSize === "number" && Number.isFinite(node.fontSize)) {
      bumpCount(fontSizeCounts, String(roundValue(node.fontSize)));
    }
  }

  function collectFontNames(node, fontFamilyCounts) {
    if (node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      bumpCount(fontFamilyCounts, node.fontName.family || "Unknown");
      return;
    }

    if (node.fontName === figma.mixed && typeof node.getRangeAllFontNames === "function") {
      try {
        const fontNames = node.getRangeAllFontNames(0, typeof node.characters === "string" ? node.characters.length : 0);
        const uniqueFamilies = new Set();
        for (const fontName of fontNames) {
          if (!fontName || typeof fontName !== "object" || typeof fontName.family !== "string") {
            continue;
          }
          uniqueFamilies.add(fontName.family);
          if (uniqueFamilies.size >= 6) {
            break;
          }
        }
        uniqueFamilies.forEach((family) => bumpCount(fontFamilyCounts, family));
      } catch (error) {
        bumpCount(fontFamilyCounts, "Mixed");
      }
    }
  }

  function inspectPaintArray(paints, colorCounts, typeStats) {
    if (!Array.isArray(paints)) {
      return;
    }
    for (const paint of paints) {
      if (!paint || paint.visible === false) {
        continue;
      }
      if (paint.type === "IMAGE") {
        typeStats.imageFillCount += 1;
        continue;
      }
      if (paint.type !== "SOLID" || !paint.color) {
        continue;
      }
      const hex = rgbToHex(paint.color);
      bumpCount(colorCounts, hex);
      typeStats.solidPaintCount += 1;
    }
  }

  function inspectNumericFields(node, nodeName, examples, nodeIds) {
    const fields = [];
    maybeTrackNumber(fields, "x", node.x);
    maybeTrackNumber(fields, "y", node.y);
    maybeTrackNumber(fields, "width", node.width);
    maybeTrackNumber(fields, "height", node.height);
    maybeTrackNumber(fields, "rotation", node.rotation);
    maybeTrackNumber(fields, "strokeWeight", node.strokeWeight);
    maybeTrackNumber(fields, "cornerRadius", node.cornerRadius);
    maybeTrackNumber(fields, "topLeftRadius", node.topLeftRadius);
    maybeTrackNumber(fields, "topRightRadius", node.topRightRadius);
    maybeTrackNumber(fields, "bottomLeftRadius", node.bottomLeftRadius);
    maybeTrackNumber(fields, "bottomRightRadius", node.bottomRightRadius);
    if (!fields.length) {
      return 0;
    }

    nodeIds.add(node.id);
    if (examples.length < 12) {
      examples.push({
        name: nodeName,
        fields,
      });
    }
    return fields.length;
  }

  function maybeTrackNumber(fields, fieldName, value) {
    if (typeof value !== "number" || !Number.isFinite(value)) {
      return;
    }
    if (Math.abs(value - Math.round(value)) <= 0.0001) {
      return;
    }
    fields.push(`${fieldName} ${roundValue(value)}`);
  }

  function addScriptCounts(scriptCounts, text) {
    for (const character of text) {
      const codePoint = character.codePointAt(0);
      if (typeof codePoint !== "number") {
        continue;
      }
      if (
        (codePoint >= 0xac00 && codePoint <= 0xd7af) ||
        (codePoint >= 0x1100 && codePoint <= 0x11ff) ||
        (codePoint >= 0x3130 && codePoint <= 0x318f)
      ) {
        scriptCounts.korean += 1;
      } else if (
        (codePoint >= 0x3040 && codePoint <= 0x309f) ||
        (codePoint >= 0x30a0 && codePoint <= 0x30ff)
      ) {
        scriptCounts.japanese += 1;
      } else if (codePoint >= 0x4e00 && codePoint <= 0x9fff) {
        scriptCounts.cjk += 1;
      } else if ((codePoint >= 0x0041 && codePoint <= 0x005a) || (codePoint >= 0x0061 && codePoint <= 0x007a)) {
        scriptCounts.latin += 1;
      } else if (codePoint >= 0x0030 && codePoint <= 0x0039) {
        scriptCounts.digit += 1;
      } else if (!/\s/.test(character)) {
        scriptCounts.other += 1;
      }
    }
  }

  function summarizeLanguage(scriptCounts) {
    const ordered = Object.entries(scriptCounts)
      .filter((entry) => entry[0] !== "digit" && entry[0] !== "other" && entry[1] > 0)
      .sort((left, right) => right[1] - left[1]);
    if (!ordered.length) {
      return {
        descriptor: "텍스트 언어 미감지",
        reason: "텍스트 레이어가 없거나 언어 판별에 충분한 문자가 없습니다.",
      };
    }

    const primary = ordered[0];
    const secondary = ordered[1] && primary[1] > 0 && ordered[1][1] / primary[1] >= 0.25 ? ordered[1] : null;
    const descriptor = secondary
      ? `${languageLabel(primary[0])} 중심 / ${languageLabel(secondary[0])} 혼합`
      : `${languageLabel(primary[0])} 중심`;

    return {
      descriptor,
      reason: `${languageLabel(primary[0])} 비중이 가장 높습니다.`,
    };
  }

  function inferDesignContext(options) {
    const corpus = `${options.rootNames.join(" ")} ${options.textSamples.join(" ")}`.toLowerCase();
    const categories = [
      { label: "인증/폼 화면", reason: "로그인, 가입, 입력, 확인 계열 텍스트 신호가 많습니다.", keywords: ["login", "sign in", "sign up", "email", "password", "submit", "continue", "로그인", "회원가입", "이메일", "비밀번호", "입력", "제출", "확인", "인증"] },
      { label: "대시보드/데이터 화면", reason: "지표, 표, 차트 계열 레이어와 숫자 중심 텍스트가 보입니다.", keywords: ["dashboard", "analytics", "chart", "table", "report", "metric", "stats", "data", "대시보드", "분석", "차트", "테이블", "리포트", "지표", "매출"] },
      { label: "랜딩/프로모션 화면", reason: "짧은 CTA 문구와 버튼형 신호가 많아 홍보형 화면으로 보입니다.", keywords: ["get started", "learn more", "shop", "book", "download", "discover", "contact", "지금", "시작", "구매", "예약", "자세히", "다운로드", "문의"] },
      { label: "모달/다이얼로그", reason: "선택 범위가 상대적으로 작고 확인/취소 계열 문구가 보입니다.", keywords: ["cancel", "confirm", "delete", "ok", "취소", "삭제", "닫기", "확인"] },
      { label: "앱/웹 내비게이션 화면", reason: "메뉴, 설정, 홈, 프로필 등 탐색성 텍스트가 많습니다.", keywords: ["home", "profile", "settings", "menu", "search", "notification", "홈", "프로필", "설정", "메뉴", "검색", "알림"] },
    ];

    let best = null;
    for (const category of categories) {
      let score = 0;
      for (const keyword of category.keywords) {
        if (corpus.includes(keyword)) {
          score += 1;
        }
      }
      if (category.label === "랜딩/프로모션 화면" && options.typeStats.buttonLikeCount >= 2) {
        score += 1;
      }
      if (category.label === "모달/다이얼로그" && options.selectionBounds && options.selectionBounds.width <= 720) {
        score += 1;
      }
      if (category.label === "대시보드/데이터 화면" && options.typeStats.textNodeCount >= 8) {
        score += 1;
      }
      if (!best || score > best.score) {
        best = {
          label: category.label,
          reason: category.reason,
          score,
        };
      }
    }

    if (best && best.score > 0) {
      return best;
    }
    if (options.typeStats.componentCount + options.typeStats.instanceCount >= Math.max(2, options.typeStats.rootCount || 1)) {
      return {
        label: "컴포넌트/변형 세트",
        reason: "컴포넌트와 인스턴스 비중이 높습니다.",
        score: 0,
      };
    }
    if (options.topTypes.length > 0) {
      return {
        label: "일반 UI 화면",
        reason: `${formatNodeType(options.topTypes[0].key || options.topTypes[0].type)} 중심 구조입니다.`,
        score: 0,
      };
    }
    return {
      label: "일반 선택 영역",
      reason: "구조 분석은 가능하지만 특정 화면 맥락 신호는 약합니다.",
      score: 0,
    };
  }

  function getFirstVisibleSolidPaint(paints) {
    if (!Array.isArray(paints)) {
      return null;
    }
    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (!paint || paint.visible === false || paint.type !== "SOLID" || !paint.color) {
        continue;
      }
      return {
        paintIndex: index,
        color: cloneColor(paint.color),
        opacity: typeof paint.opacity === "number" && Number.isFinite(paint.opacity) ? paint.opacity : 1,
        hex: rgbToHex(paint.color),
      };
    }
    return null;
  }

  function looksLikeButton(node) {
    const name = safeName(node).toLowerCase();
    if (CONTROL_NAME_PATTERN.test(name)) {
      return true;
    }
    if (node.type === "TEXT" && typeof node.characters === "string") {
      return looksLikeActionText(node.characters);
    }
    return false;
  }

  function combineBounds(boundsList) {
    if (!boundsList.length) {
      return null;
    }
    let left = boundsList[0].x;
    let top = boundsList[0].y;
    let right = boundsList[0].x + boundsList[0].width;
    let bottom = boundsList[0].y + boundsList[0].height;
    for (let index = 1; index < boundsList.length; index += 1) {
      const bounds = boundsList[index];
      left = Math.min(left, bounds.x);
      top = Math.min(top, bounds.y);
      right = Math.max(right, bounds.x + bounds.width);
      bottom = Math.max(bottom, bounds.y + bounds.height);
    }
    return {
      x: left,
      y: top,
      width: Math.max(0, right - left),
      height: Math.max(0, bottom - top),
    };
  }

  function getNodeBounds(node) {
    if (node && node.absoluteBoundingBox) {
      return node.absoluteBoundingBox;
    }
    if (node && node.absoluteRenderBounds) {
      return node.absoluteRenderBounds;
    }
    if (
      node &&
      typeof node.x === "number" &&
      typeof node.y === "number" &&
      typeof node.width === "number" &&
      typeof node.height === "number"
    ) {
      return {
        x: node.x,
        y: node.y,
        width: node.width,
        height: node.height,
      };
    }
    return null;
  }

  function overlapArea(left, right) {
    const overlapWidth = Math.max(0, Math.min(left.x + left.width, right.x + right.width) - Math.max(left.x, right.x));
    const overlapHeight = Math.max(0, Math.min(left.y + left.height, right.y + right.height) - Math.max(left.y, right.y));
    return overlapWidth * overlapHeight;
  }

  function containsBounds(outer, inner) {
    return (
      outer.x <= inner.x + 0.5 &&
      outer.y <= inner.y + 0.5 &&
      outer.x + outer.width >= inner.x + inner.width - 0.5 &&
      outer.y + outer.height >= inner.y + inner.height - 0.5
    );
  }

  function getSelectionSignature(selection) {
    const ids = Array.from(selection || [])
      .map((node) => node.id)
      .sort();
    return `${figma.currentPage.id}:${ids.join(",")}`;
  }

  function formatSelectionLabel(roots) {
    if (!roots.length) {
      return "선택 없음";
    }
    if (roots.length === 1) {
      return roots[0].name;
    }
    return `${roots[0].name} 외 ${roots.length - 1}개`;
  }

  function sortCountEntries(map) {
    return Array.from(map.entries())
      .map((entry) => ({
        key: entry[0],
        count: entry[1],
      }))
      .sort((left, right) => {
        if (right.count !== left.count) {
          return right.count - left.count;
        }
        return String(left.key).localeCompare(String(right.key));
      });
  }

  function bumpCount(map, key) {
    if (!key) {
      return;
    }
    map.set(key, (map.get(key) || 0) + 1);
  }

  function canonicalizeName(name) {
    return String(name || "Unnamed").trim().toLowerCase();
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim().length > 0) {
      return node.name.trim();
    }
    return String((node && node.type) || "Unnamed");
  }

  function hasChildren(node) {
    return !!node && Array.isArray(node.children);
  }

  function hasVisibleEffects(node) {
    return !!node && Array.isArray(node.effects) && node.effects.some((effect) => effect && effect.visible !== false);
  }

  function isMaskNode(node) {
    return !!node && node.isMask === true;
  }

  function isSuspiciousName(name, type) {
    if (!name) {
      return true;
    }
    const normalized = String(name).trim();
    if (!normalized) {
      return true;
    }
    if (normalized.toUpperCase() === String(type || "").toUpperCase()) {
      return true;
    }
    return AUTO_NAME_PATTERNS.some((pattern) => pattern.test(normalized));
  }

  function isNodeVisible(node) {
    return !!node && node.visible !== false;
  }

  function cloneColor(color) {
    return {
      r: Number(color.r) || 0,
      g: Number(color.g) || 0,
      b: Number(color.b) || 0,
    };
  }

  function clonePlainObject(value) {
    if (!value || typeof value !== "object") {
      return value;
    }
    if (Array.isArray(value)) {
      return value.map((item) => clonePlainObject(item));
    }
    const clone = {};
    Object.keys(value).forEach((key) => {
      clone[key] = clonePlainObject(value[key]);
    });
    return clone;
  }

  function rgbToHex(color) {
    const red = toHexChannel(color.r);
    const green = toHexChannel(color.g);
    const blue = toHexChannel(color.b);
    return `#${red}${green}${blue}`;
  }

  function toHexChannel(value) {
    const channel = Math.max(0, Math.min(255, Math.round(value * 255)));
    return channel.toString(16).padStart(2, "0").toUpperCase();
  }

  function roundValue(value) {
    return Math.round(value * 100) / 100;
  }

  function roundPixel(value) {
    return Math.round(value);
  }

  function languageLabel(language) {
    switch (language) {
      case "korean":
        return "한국어";
      case "latin":
        return "영어/라틴";
      case "japanese":
        return "일본어";
      case "cjk":
        return "중국어/한자";
      default:
        return "기타";
    }
  }

  function formatNodeType(type) {
    switch (String(type || "").toUpperCase()) {
      case "FRAME":
        return "프레임";
      case "GROUP":
        return "그룹";
      case "TEXT":
        return "텍스트";
      case "SECTION":
        return "섹션";
      case "COMPONENT":
        return "컴포넌트";
      case "COMPONENT_SET":
        return "컴포넌트 세트";
      case "INSTANCE":
        return "인스턴스";
      case "VECTOR":
        return "벡터";
      case "BOOLEAN_OPERATION":
        return "불리언";
      case "RECTANGLE":
        return "사각형";
      case "ELLIPSE":
        return "타원";
      default:
        return type;
    }
  }

  async function loadFontsForTextNode(node) {
    if (!node || node.type !== "TEXT") {
      return;
    }
    const fontNames = collectEditableFontNames(node);
    for (const fontName of fontNames) {
      await figma.loadFontAsync(fontName);
    }
  }

  function collectEditableFontNames(node) {
    const fontNames = [];
    const seen = new Set();
    const pushFont = (fontName) => {
      if (!fontName || typeof fontName !== "object" || typeof fontName.family !== "string" || typeof fontName.style !== "string") {
        return;
      }
      const key = `${fontName.family}::${fontName.style}`;
      if (seen.has(key)) {
        return;
      }
      seen.add(key);
      fontNames.push({
        family: fontName.family,
        style: fontName.style,
      });
    };

    if (typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string" && node.characters.length > 0) {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        for (const fontName of rangeFonts) {
          pushFont(fontName);
        }
      } catch (error) {}
    }

    if (fontNames.length === 0 && node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      pushFont(node.fontName);
    }

    return fontNames;
  }

  function createIssueId(type, nodeId) {
    return `${type}:${nodeId}`;
  }

  function formatProviderLabel(provider) {
    switch (String(provider || "").toLowerCase()) {
      case "openai":
        return "OpenAI";
      case "gemini":
        return "Gemini";
      default:
        return provider ? String(provider) : "";
    }
  }

  function normalizeErrorMessage(error, fallback) {
    if (error instanceof Error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_DESIGN_CONSISTENCY_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  if (typeof originalOnMessage !== "function") {
    return;
  }

  const AI_DESIGN_CONSISTENCY_CACHE_KEY = "pigma:ai-design-consistency-cache:v1";
  const AI_DESIGN_READ_CACHE_KEY = "pigma:ai-design-read-cache:v1";
  const PATCH_VERSION = 1;
  const ANNOTATION_CATEGORY_LABEL = "디자인 일관성";
  const ANNOTATION_CATEGORY_COLOR = "blue";
  const ANNOTATION_LABEL_PREFIX = "[디자인 일관성]";
  const ACCESSIBILITY_ANNOTATION_CATEGORY_LABEL = "웹 접근성 진단";
  const ACCESSIBILITY_ANNOTATION_LABEL_PREFIX = "[웹 접근성 진단]";
  const DIAGNOSIS_CLEAR_CATEGORY_LABEL = "웹 접근성 진단 + 디자인 일관성";
  const MAX_ISSUE_COUNT = 24;
  const COLOR_DISTANCE_THRESHOLD = 0.11;
  const COLOR_TOKEN_LIMIT = 6;
  const FONT_SIZE_TOKEN_LIMIT = 6;
  const SPACING_TOKEN_LIMIT = 6;
  const MIN_TOKEN_COUNT = 2;
  const SPACING_PLAN_FIELDS = new Set(["itemSpacing", "paddingTop", "paddingRight", "paddingBottom", "paddingLeft"]);
  const PALETTE_ROLES = {
    fills: "배경",
    strokes: "보더",
    text: "텍스트",
  };
  let activeExecution = null;

  figma.ui.onmessage = async (message) => {
    if (isConsistencyMessage(message)) {
      if (message.type === "request-ai-design-consistency-cache") {
        await postCachedResult();
        return;
      }

      if (message.type === "run-ai-design-consistency-clear") {
        await withExecutionLock(
          {
            status: "clearing-annotations",
            message: "현재 선택 범위에 남아 있는 웹 접근성 진단과 디자인 일관성 Dev Mode 주석을 정리하고 있습니다.",
            extra: {},
          },
          runDesignConsistencyClear
        );
        return;
      }

      if (message.type === "run-ai-design-consistency-apply-fix") {
        await withExecutionLock(
          {
            status: "applying-fix",
            message: "선택한 일관성 제안을 적용하고 있습니다.",
            extra: {
              issueId: message && typeof message.issueId === "string" ? message.issueId.trim() : "",
            },
          },
          () => applyIssueFix(message)
        );
        return;
      }

      await withExecutionLock(
        {
          status: "running",
          message: "현재 선택을 기준으로 디자인 일관성을 검사하고 있습니다.",
          extra: {},
        },
        runDesignConsistencyDiagnosis
      );
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_DESIGN_CONSISTENCY_PATCH__ = true;

  function isConsistencyMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-design-consistency-cache" ||
        message.type === "run-ai-design-consistency" ||
        message.type === "run-ai-design-consistency-clear" ||
        message.type === "run-ai-design-consistency-apply-fix")
    );
  }

  async function withExecutionLock(execution, runner) {
    if (activeExecution) {
      postStatus(activeExecution.status, activeExecution.message, activeExecution.extra);
      return false;
    }

    activeExecution =
      execution && typeof execution === "object" ? execution : { status: "running", message: "", extra: {} };
    try {
      await runner();
      return true;
    } finally {
      activeExecution = null;
    }
  }

  async function runDesignConsistencyDiagnosis(options) {
    const runOptions = options && typeof options === "object" ? options : {};
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    if (!runOptions.skipStatus) {
      postStatus("running", "현재 선택을 기준으로 디자인 일관성을 검사하고 있습니다.");
    }

    try {
      const analysis = analyzeCurrentSelection();
      let result = analysis.result;
      const annotationSupported = Array.isArray(analysis.annotationNodes) && analysis.annotationNodes.length > 0;
      const category =
        annotationSupported && supportsAnnotations(analysis.annotationNodes)
          ? await ensureAnnotationCategory(ANNOTATION_CATEGORY_COLOR)
          : null;
      const annotationResult = category
        ? applyConsistencyAnnotations(analysis.annotationNodes, result.consistency.issues, category)
        : buildResultOnlyAnnotation(result.consistency.issues, "Dev Mode 주석을 지원하지 않는 선택입니다.");

      result.consistency.annotations = {
        annotationCount: annotationResult.annotationCount || 0,
        annotatedNodeCount: annotationResult.annotatedNodeCount || 0,
        clearedNodeCount: annotationResult.clearedNodeCount || 0,
        removedAnnotationCount: annotationResult.removedAnnotationCount || 0,
        categoryLabel: annotationResult.categoryLabel || ANNOTATION_CATEGORY_LABEL,
        modeLabel: annotationResult.modeLabel || "Result only",
      };
      result.summary.issueCount = result.consistency.issueCount;
      result.summary.fixableCount = result.consistency.fixableCount;
      result.summary.annotationCount = result.consistency.annotations.annotationCount;

      result = await enrichConsistencyWithAi(result);
      await writeCachedResult(result);

      figma.ui.postMessage({
        type: "ai-design-consistency-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      if (runOptions.notify !== false) {
        figma.notify(runOptions.notifyMessage || "디자인 일관성 진단 완료", { timeout: 1800 });
      }
    } catch (error) {
      const message = normalizeErrorMessage(error, "디자인 일관성 진단에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-design-consistency-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function applyIssueFix(message) {
    const issueId = message && typeof message.issueId === "string" ? message.issueId.trim() : "";
    if (!issueId) {
      throw new Error("적용할 디자인 일관성 제안을 찾지 못했습니다.");
    }

    postStatus("applying-fix", "선택한 일관성 제안을 적용하고 있습니다.", {
      issueId,
    });

    const cachedResult = await readCachedResult();
    const issues =
      cachedResult && cachedResult.consistency && Array.isArray(cachedResult.consistency.issues)
        ? cachedResult.consistency.issues
        : [];
    const issue = issues.find((entry) => entry && entry.id === issueId);

    if (!issue || !issue.fixPlan) {
      throw new Error("선택한 제안을 다시 찾지 못했습니다. 디자인 일관성 진단을 다시 실행해 주세요.");
    }

    await applyFixPlan(issue.fixPlan);
    figma.notify("디자인 일관성 제안 적용 완료", { timeout: 1600 });
    await runDesignConsistencyDiagnosis({
      skipStatus: true,
      notifyMessage: "제안 적용 후 디자인 일관성을 다시 확인했습니다.",
    });
  }

  async function runDesignConsistencyClear() {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postStatus("clearing-annotations", "현재 선택 범위에 남아 있는 웹 접근성 진단과 디자인 일관성 Dev Mode 주석을 정리하고 있습니다.");

    try {
      const analysis = analyzeCurrentSelection();
      const managedCategories =
        Array.isArray(analysis.annotationNodes) && analysis.annotationNodes.length > 0
          ? await getManagedDiagnosisCategories()
          : { consistency: null, accessibility: null };
      const cleared = removeDiagnosisAnnotations(analysis.annotationNodes, managedCategories);
      const result = buildConsistencyClearResult(analysis.result, cleared, managedCategories);
      await syncDiagnosisCachesAfterClear(result.selectionSignature, result.summary);

      figma.ui.postMessage({
        type: "ai-design-consistency-clear-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      if ((result.summary.removedAnnotationCount || 0) > 0) {
        figma.notify(`디자인 진단 주석 정리 완료 (${result.summary.removedAnnotationCount || 0}건 제거)`, {
          timeout: 2200,
        });
      } else {
        figma.notify("현재 선택 범위에서 삭제할 디자인 진단 주석을 찾지 못했습니다.", { timeout: 1800 });
      }
    } catch (error) {
      const message = normalizeErrorMessage(error, "디자인 진단 주석 정리에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-design-consistency-clear-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function postCachedResult() {
    const result = await readCachedResult();
    figma.ui.postMessage({
      type: "ai-design-consistency-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  function postStatus(status, message, extra) {
    const payload = extra && typeof extra === "object" ? extra : {};
    const body = {
      type: "ai-design-consistency-status",
      status,
      message,
    };
    Object.keys(payload).forEach((key) => {
      body[key] = payload[key];
    });
    figma.ui.postMessage(body);
  }

  function buildConsistencyClearResult(baseResult, cleared, managedCategories) {
    const result = baseResult && typeof baseResult === "object" ? baseResult : {};
    const summary = result.summary && typeof result.summary === "object" ? result.summary : {};
    const stats = result.stats && typeof result.stats === "object" ? result.stats : {};
    const clearedResult = cleared && typeof cleared === "object" ? cleared : {};
    const clearedItems = Array.isArray(clearedResult.cleared) ? clearedResult.cleared : [];
    const skipped = Array.isArray(clearedResult.skipped) ? clearedResult.skipped : [];
    const categories = managedCategories && typeof managedCategories === "object" ? managedCategories : {};
    const categoryLabels = [];

    if ((clearedResult.removedAccessibilityAnnotationCount || 0) > 0 || categories.accessibility) {
      categoryLabels.push(ACCESSIBILITY_ANNOTATION_CATEGORY_LABEL);
    }
    if ((clearedResult.removedConsistencyAnnotationCount || 0) > 0 || categories.consistency) {
      categoryLabels.push(ANNOTATION_CATEGORY_LABEL);
    }

    return {
      version: PATCH_VERSION,
      source: "managed-annotation-clear",
      selectionSignature: result.selectionSignature || getSelectionSignature(figma.currentPage.selection),
      analyzedAt: new Date().toISOString(),
      selectionBounds: result.selectionBounds || null,
      summary: {
        selectionLabel: summary.selectionLabel || "선택",
        contextLabel: summary.contextLabel || "일반 UI 화면",
        aiStatusLabel: "주석 정리",
        aiProviderLabel: "",
        aiModelLabel: "",
        removedAnnotationCount: clearedResult.removedAnnotationCount || 0,
        removedAccessibilityAnnotationCount: clearedResult.removedAccessibilityAnnotationCount || 0,
        removedConsistencyAnnotationCount: clearedResult.removedConsistencyAnnotationCount || 0,
        clearedNodeCount: clearedResult.clearedNodeCount || 0,
        skippedCount: skipped.length,
        categoryLabel: categoryLabels.length ? categoryLabels.join(" + ") : DIAGNOSIS_CLEAR_CATEGORY_LABEL,
        mode: "annotation-clear",
        modeLabel: "Green/Blue Dev Mode annotation clear",
      },
      stats: {
        totalNodes: typeof stats.totalNodes === "number" && Number.isFinite(stats.totalNodes) ? stats.totalNodes : 0,
        textNodeCount: typeof stats.textNodeCount === "number" && Number.isFinite(stats.textNodeCount) ? stats.textNodeCount : 0,
        autoLayoutCount:
          typeof stats.autoLayoutCount === "number" && Number.isFinite(stats.autoLayoutCount) ? stats.autoLayoutCount : 0,
      },
      cleared: clearedItems.slice(0, 12),
      skipped: skipped.slice(0, 8),
    };
  }

  async function readCachedResult() {
    try {
      const value = await figma.clientStorage.getAsync(AI_DESIGN_CONSISTENCY_CACHE_KEY);
      return normalizeCachedResult(value);
    } catch (error) {
      return null;
    }
  }

  async function writeCachedResult(result) {
    const normalized = normalizeCachedResult(result);
    try {
      await figma.clientStorage.setAsync(AI_DESIGN_CONSISTENCY_CACHE_KEY, normalized);
    } catch (error) {}
    return normalized;
  }

  async function syncDiagnosisCachesAfterClear(selectionSignature, clearSummary) {
    const summary = clearSummary && typeof clearSummary === "object" ? clearSummary : {};
    await syncCachedAnnotationState(
      AI_DESIGN_CONSISTENCY_CACHE_KEY,
      selectionSignature,
      "consistency",
      summary.removedConsistencyAnnotationCount || 0
    );
    await syncCachedAnnotationState(
      AI_DESIGN_READ_CACHE_KEY,
      selectionSignature,
      "accessibility",
      summary.removedAccessibilityAnnotationCount || 0
    );
  }

  async function syncCachedAnnotationState(cacheKey, selectionSignature, sectionKey, removedAnnotationCount) {
    try {
      const cached = await figma.clientStorage.getAsync(cacheKey);
      if (!cached || typeof cached !== "object" || cached.selectionSignature !== selectionSignature) {
        return;
      }

      const section = cached[sectionKey];
      if (!section || typeof section !== "object" || !section.annotations || typeof section.annotations !== "object") {
        return;
      }

      section.annotations.annotationCount = 0;
      section.annotations.annotatedNodeCount = 0;
      section.annotations.clearedNodeCount = 0;
      section.annotations.removedAnnotationCount = removedAnnotationCount;
      section.annotations.modeLabel = "Result only";

      if (cached.summary && typeof cached.summary === "object") {
        cached.summary.annotationCount = 0;
      }

      await figma.clientStorage.setAsync(cacheKey, cached);
    } catch (error) {}
  }

  function normalizeCachedResult(value) {
    if (!value || typeof value !== "object") {
      return null;
    }
    return value;
  }

  function matchesCurrentSelection(result) {
    return !!result && result.selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function matchesSelectionSignature(selectionSignature) {
    return typeof selectionSignature === "string" && selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function analyzeCurrentSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택해 주세요.");
    }

    const rootBounds = [];
    const rootSummaries = [];
    const rootNames = [];
    const textSamples = [];
    const annotationNodes = [];
    const paintEntries = [];
    const textEntries = [];
    const spacingEntries = [];
    const nodeEntries = [];
    const typeCounts = new Map();
    let orderCounter = 0;

    const stats = {
      rootCount: selection.length,
      totalNodes: 0,
      frameCount: 0,
      groupCount: 0,
      textNodeCount: 0,
      instanceCount: 0,
      componentCount: 0,
      autoLayoutCount: 0,
      maxDepth: 0,
    };

    const stack = [];
    for (let index = selection.length - 1; index >= 0; index -= 1) {
      const node = selection[index];
      stack.push({ node, depth: 1, rootId: node.id });
      const bounds = getNodeBounds(node);
      if (bounds) {
        rootBounds.push(bounds);
      }
      rootSummaries.push({
        id: node.id,
        name: safeName(node),
        type: String(node.type || "UNKNOWN"),
        width: bounds ? roundPixel(bounds.width) : null,
        height: bounds ? roundPixel(bounds.height) : null,
        childCount: hasChildren(node) ? node.children.length : 0,
      });
      rootNames.push(safeName(node));
    }

    while (stack.length > 0) {
      const current = stack.pop();
      if (!current || !current.node || !isNodeVisible(current.node)) {
        continue;
      }

      const node = current.node;
      const depth = current.depth;
      const bounds = getNodeBounds(node);
      const type = String(node.type || "UNKNOWN");
      const name = safeName(node);
      const entry = {
        id: node.id,
        node,
        bounds,
        type,
        name,
        depth,
        rootId: current.rootId,
        parentId: node.parent && typeof node.parent.id === "string" ? node.parent.id : "",
        order: orderCounter++,
      };
      nodeEntries.push(entry);

      stats.totalNodes += 1;
      stats.maxDepth = Math.max(stats.maxDepth, depth);
      bumpCount(typeCounts, type);

      if (supportsAnnotationsOnNode(node)) {
        annotationNodes.push(node);
      }

      if (type === "FRAME") {
        stats.frameCount += 1;
      } else if (type === "GROUP") {
        stats.groupCount += 1;
      } else if (type === "TEXT") {
        stats.textNodeCount += 1;
      } else if (type === "INSTANCE") {
        stats.instanceCount += 1;
      } else if (type === "COMPONENT" || type === "COMPONENT_SET") {
        stats.componentCount += 1;
      }

      collectPaintEntries(node, entry, paintEntries);
      collectSpacingEntries(node, entry, spacingEntries);

      if (hasLayout(node)) {
        stats.autoLayoutCount += 1;
      }

      if (type === "TEXT") {
        const textEntry = buildTextEntry(node, entry);
        if (textEntry) {
          textEntries.push(textEntry);
          if (textSamples.length < 6 && textEntry.text) {
            textSamples.push(textEntry.text);
          }
        }
      }

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push({
            node: node.children[index],
            depth: depth + 1,
            rootId: current.rootId,
          });
        }
      }
    }

    const selectionBounds = combineBounds(rootBounds);
    const topTypes = sortCountEntries(typeCounts)
      .slice(0, 5)
      .map((item) => ({
        type: item.key,
        label: formatNodeType(item.key),
        count: item.count,
      }));
    const contextSummary = inferDesignContext({
      rootNames,
      textSamples,
      stats,
      selectionBounds,
      topTypes,
    });
    const consistency = buildConsistencySummary(paintEntries, textEntries, spacingEntries, nodeEntries);
    const insights = buildInsights(consistency);

    const result = {
      version: PATCH_VERSION,
      source: "local-heuristic",
      selectionSignature: getSelectionSignature(selection),
      analyzedAt: new Date().toISOString(),
      roots: rootSummaries,
      selectionBounds: selectionBounds
        ? {
            width: roundPixel(selectionBounds.width),
            height: roundPixel(selectionBounds.height),
          }
        : null,
      summary: {
        selectionLabel: formatSelectionLabel(rootSummaries),
        contextLabel: contextSummary.label,
        contextReason: contextSummary.reason,
        aiStatusLabel: "로컬 휴리스틱",
        aiProviderLabel: "",
        aiModelLabel: "",
        issueCount: consistency.issueCount,
        fixableCount: consistency.fixableCount,
        annotationCount: 0,
      },
      stats: Object.assign({}, stats, {
        topTypes,
      }),
      consistency,
      insights,
    };

    return {
      result,
      annotationNodes,
    };
  }

  function buildConsistencySummary(paintEntries, textEntries, spacingEntries, nodeEntries) {
    const colorTokens = buildColorTokens(paintEntries);
    const typographyTokens = buildTypographyTokens(textEntries);
    const spacingTokens = buildSpacingTokens(spacingEntries);

    const colorIssues = analyzeColorIssues(paintEntries, colorTokens);
    const typographyIssues = analyzeTypographyIssues(textEntries, typographyTokens);
    const spacingIssues = analyzeSpacingIssues(spacingEntries, spacingTokens);
    const semanticSpacingIssues = analyzeSemanticSpacingIssues(nodeEntries, textEntries);
    const repeatedPatternIssues = analyzeRepeatedPatternIssues(nodeEntries, textEntries);
    const formIssues = analyzeFormConsistencyIssues(nodeEntries, textEntries);

    const issues = dedupeIssues(
      colorIssues.concat(typographyIssues, spacingIssues, semanticSpacingIssues, repeatedPatternIssues, formIssues)
    )
      .sort(compareIssues)
      .slice(0, MAX_ISSUE_COUNT);

    return {
      standardLabel: "색상 · 타이포 · 여백 · 맥락",
      issueCount: issues.length,
      fixableCount: issues.filter((issue) => issue && issue.canApply).length,
      colorIssueCount: colorIssues.length,
      typographyIssueCount: typographyIssues.length,
      spacingIssueCount: spacingIssues.length,
      semanticIssueCount: semanticSpacingIssues.length,
      repetitionIssueCount: repeatedPatternIssues.length,
      formIssueCount: formIssues.length,
      colorTokenSummary: colorTokens.summary,
      typographyTokenSummary: typographyTokens.summary,
      spacingTokenSummary: spacingTokens.summary,
      colorTokens: colorTokens.items.map((item) => item.hex),
      typographyTokens: {
        families: typographyTokens.familyItems.map((item) => item.value),
        sizes: typographyTokens.sizeItems.map((item) => item.value),
      },
      spacingTokens: spacingTokens.items.map((item) => item.value),
      issues,
      annotations: {
        annotationCount: 0,
        annotatedNodeCount: 0,
        clearedNodeCount: 0,
        removedAnnotationCount: 0,
        categoryLabel: ANNOTATION_CATEGORY_LABEL,
        modeLabel: "Result only",
      },
    };
  }

  function buildColorTokens(paintEntries) {
    const counts = new Map();
    for (const entry of paintEntries) {
      if (!entry || !entry.hex) {
        continue;
      }
      bumpCount(counts, entry.hex);
    }

    let items = sortCountEntries(counts)
      .filter((item) => item.count >= MIN_TOKEN_COUNT)
      .slice(0, COLOR_TOKEN_LIMIT)
      .map((item) => ({
        hex: item.key,
        count: item.count,
        color: paintEntries.find((entry) => entry.hex === item.key).color,
      }));

    if (!items.length) {
      items = sortCountEntries(counts)
        .slice(0, Math.min(COLOR_TOKEN_LIMIT, 4))
        .map((item) => ({
          hex: item.key,
          count: item.count,
          color: paintEntries.find((entry) => entry.hex === item.key).color,
        }));
    }

    return {
      items,
      summary: items.length ? items.map((item) => `${item.hex} (${item.count})`).join(" · ") : "대표 색상 감지 없음",
    };
  }

  function buildTypographyTokens(textEntries) {
    const familyCounts = new Map();
    const sizeCounts = new Map();

    for (const entry of textEntries) {
      if (entry.fontFamily && entry.fontFamily !== "Mixed") {
        bumpCount(familyCounts, entry.fontFamily);
      }
      if (typeof entry.fontSize === "number" && Number.isFinite(entry.fontSize)) {
        bumpCount(sizeCounts, String(roundValue(entry.fontSize)));
      }
    }

    const familyItems = sortCountEntries(familyCounts)
      .filter((item) => item.count >= MIN_TOKEN_COUNT)
      .slice(0, 3)
      .map((item) => ({
        value: item.key,
        count: item.count,
      }));
    const sizeItems = sortCountEntries(sizeCounts)
      .filter((item) => item.count >= MIN_TOKEN_COUNT)
      .slice(0, FONT_SIZE_TOKEN_LIMIT)
      .map((item) => ({
        value: Number(item.key),
        count: item.count,
      }));

    const familySummary = familyItems.length ? familyItems.map((item) => item.value).join(" · ") : "대표 폰트 감지 없음";
    const sizeSummary = sizeItems.length ? sizeItems.map((item) => `${item.value}px`).join(" · ") : "대표 크기 감지 없음";

    return {
      familyItems,
      sizeItems,
      dominantFamily: familyItems.length ? familyItems[0].value : "",
      summary: `${familySummary} / ${sizeSummary}`,
    };
  }

  function buildSpacingTokens(spacingEntries) {
    const counts = new Map();
    for (const entry of spacingEntries) {
      if (!entry || typeof entry.value !== "number" || !Number.isFinite(entry.value)) {
        continue;
      }
      bumpCount(counts, String(roundValue(entry.value)));
    }

    let items = sortCountEntries(counts)
      .filter((item) => item.count >= MIN_TOKEN_COUNT)
      .slice(0, SPACING_TOKEN_LIMIT)
      .map((item) => ({
        value: Number(item.key),
        count: item.count,
      }));

    if (!items.length) {
      items = sortCountEntries(counts)
        .slice(0, Math.min(SPACING_TOKEN_LIMIT, 4))
        .map((item) => ({
          value: Number(item.key),
          count: item.count,
        }));
    }

    return {
      items,
      summary: items.length ? items.map((item) => `${item.value}px`).join(" · ") : "대표 간격 감지 없음",
    };
  }

  function analyzeColorIssues(paintEntries, colorTokens) {
    if (!colorTokens.items.length) {
      return [];
    }

    const tokenSet = new Set(colorTokens.items.map((item) => item.hex));
    const colorCounts = new Map();
    for (const entry of paintEntries) {
      bumpCount(colorCounts, entry.hex);
    }

    const issues = [];
    for (const entry of paintEntries) {
      if (!entry || !entry.hex || tokenSet.has(entry.hex)) {
        continue;
      }

      const nearest = findNearestColorToken(entry.color, colorTokens.items);
      if (!nearest || nearest.distance > COLOR_DISTANCE_THRESHOLD) {
        continue;
      }

      const currentCount = colorCounts.get(entry.hex) || 0;
      const shouldFlag = currentCount <= 1 || nearest.count >= Math.max(2, currentCount + 1);
      if (!shouldFlag) {
        continue;
      }

      const fixPlan = canEditPaintEntry(entry)
        ? {
            action: "set-solid-paint-color",
            targetNodeId: entry.nodeId,
            property: entry.property,
            paintIndex: entry.paintIndex,
            color: cloneColor(nearest.color),
          }
        : null;

      issues.push({
        id: createIssueId(`color:${entry.property}:${entry.paintIndex}`, entry.nodeId),
        category: "color",
        type: "color-token",
        severity: nearest.distance <= 0.05 ? "warning" : "info",
        guideline: "색상 토큰",
        nodeId: entry.nodeId,
        nodeName: entry.nodeName,
        nodeType: entry.nodeType,
        summary: `${PALETTE_ROLES[entry.role] || "색상"} ${entry.hex}`,
        detail: `${PALETTE_ROLES[entry.role] || "색상"} 값이 대표 팔레트 ${nearest.hex}과 거의 같은데 따로 사용되고 있습니다.`,
        suggestion: `${nearest.hex}로 맞추면 색상 일관성이 더 좋아집니다.`,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "색상 적용" : "",
        fixPlan,
      });
    }

    return issues;
  }

  function analyzeTypographyIssues(textEntries, typographyTokens) {
    const issues = [];
    const familyCounts = new Map();
    const sizeCounts = new Map();

    for (const entry of textEntries) {
      if (entry.fontFamily && entry.fontFamily !== "Mixed") {
        bumpCount(familyCounts, entry.fontFamily);
      }
      if (typeof entry.fontSize === "number" && Number.isFinite(entry.fontSize)) {
        bumpCount(sizeCounts, String(roundValue(entry.fontSize)));
      }
    }

    for (const entry of textEntries) {
      if (entry.fontFamily && entry.fontFamily !== "Mixed" && typographyTokens.dominantFamily) {
        const dominantCount = familyCounts.get(typographyTokens.dominantFamily) || 0;
        const currentCount = familyCounts.get(entry.fontFamily) || 0;
        if (
          entry.fontFamily !== typographyTokens.dominantFamily &&
          dominantCount >= 3 &&
          currentCount <= 1
        ) {
          issues.push({
            id: createIssueId("type-family", entry.nodeId),
            category: "typography",
            type: "font-family",
            severity: "warning",
            guideline: "대표 폰트",
            nodeId: entry.nodeId,
            nodeName: entry.nodeName,
            nodeType: entry.nodeType,
            summary: `폰트 ${entry.fontFamily}`,
            detail: `이 텍스트는 대표 폰트 ${typographyTokens.dominantFamily} 대신 ${entry.fontFamily}를 단독으로 사용하고 있습니다.`,
            suggestion: `${typographyTokens.dominantFamily} 계열로 맞추면 타이포 일관성이 높아집니다.`,
            canApply: false,
            applyLabel: "",
            fixPlan: null,
          });
        }
      }

      if (typeof entry.fontSize !== "number" || !Number.isFinite(entry.fontSize) || !typographyTokens.sizeItems.length) {
        continue;
      }

      const nearest = findNearestNumericToken(entry.fontSize, typographyTokens.sizeItems.map((item) => item.value));
      if (!nearest) {
        continue;
      }

      const currentCount = sizeCounts.get(String(roundValue(entry.fontSize))) || 0;
      if (Math.abs(nearest - entry.fontSize) < 0.5 || Math.abs(nearest - entry.fontSize) > 4 || currentCount > 1) {
        continue;
      }

      const fixPlan = canAdjustFontSize(entry.node)
        ? {
            action: "set-font-size",
            targetNodeId: entry.nodeId,
            targetFontSize: nearest,
          }
        : null;

      issues.push({
        id: createIssueId("type-size", entry.nodeId),
        category: "typography",
        type: "font-size",
        severity: "info",
        guideline: "대표 폰트 크기",
        nodeId: entry.nodeId,
        nodeName: entry.nodeName,
        nodeType: entry.nodeType,
        summary: `폰트 크기 ${roundValue(entry.fontSize)}px`,
        detail: `현재 크기가 대표 크기 스케일과 어긋나 있습니다. 가장 가까운 토큰은 ${nearest}px입니다.`,
        suggestion: `${nearest}px로 맞추면 화면 전체의 타이포 리듬이 더 안정적입니다.`,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "크기 적용" : "",
        fixPlan,
      });
    }

    return issues;
  }

  function analyzeSpacingIssues(spacingEntries, spacingTokens) {
    if (!spacingTokens.items.length) {
      return [];
    }

    const counts = new Map();
    for (const entry of spacingEntries) {
      bumpCount(counts, String(roundValue(entry.value)));
    }

    const tokenValues = spacingTokens.items.map((item) => item.value);
    const issues = [];
    for (const entry of spacingEntries) {
      if (!entry || typeof entry.value !== "number" || !Number.isFinite(entry.value)) {
        continue;
      }

      const currentValue = roundValue(entry.value);
      if (tokenValues.some((value) => Math.abs(value - currentValue) < 0.01)) {
        continue;
      }

      const nearestToken = findNearestNumericToken(currentValue, tokenValues);
      const gridTarget = Math.max(0, Math.round(currentValue / 4) * 4);
      const targetValue =
        typeof nearestToken === "number" && Math.abs(nearestToken - currentValue) <= 4
          ? nearestToken
          : gridTarget;

      if (Math.abs(targetValue - currentValue) < 1) {
        continue;
      }

      const currentCount = counts.get(String(currentValue)) || 0;
      if (currentCount > 1 && isOnFourPointGrid(currentValue)) {
        continue;
      }

      const fixPlan = canAdjustSpacingFields(entry.node, entry.fields)
        ? {
            action: "set-spacing-value",
            targetNodeId: entry.nodeId,
            changes: entry.fields.map((field) => ({
              field,
              value: targetValue,
            })),
          }
        : null;

      issues.push({
        id: createIssueId(`spacing:${entry.kind}`, entry.nodeId),
        category: "spacing",
        type: "spacing",
        severity: isOnFourPointGrid(currentValue) ? "info" : "warning",
        guideline: "간격 스케일",
        nodeId: entry.nodeId,
        nodeName: entry.nodeName,
        nodeType: entry.nodeType,
        summary: `${entry.label} ${currentValue}px`,
        detail: `${entry.label} 값이 대표 간격 스케일과 어긋나 있습니다.`,
        suggestion: `${targetValue}px로 맞추면 여백 규칙이 더 또렷해집니다.`,
        canApply: !!fixPlan,
        applyLabel: fixPlan ? "간격 적용" : "",
        fixPlan,
      });
    }

    return issues;
  }

  function analyzeSemanticSpacingIssues(nodeEntries, textEntries) {
    const nodeEntryMap = buildNodeEntryMap(nodeEntries);
    const textEntryMap = buildTextEntryMap(textEntries);
    const titleBodyCandidates = [];
    const sectionCandidates = [];

    for (const parentEntry of nodeEntries) {
      const childEntries = sortEntriesVertically(listVisibleChildEntries(parentEntry, nodeEntryMap));
      if (childEntries.length < 2) {
        continue;
      }

      const maxTextSize = getMaxDirectChildTextSize(childEntries, textEntryMap);
      for (let index = 0; index < childEntries.length - 1; index += 1) {
        const current = childEntries[index];
        const next = childEntries[index + 1];
        const gap = measureVerticalGap(current, next);
        if (gap === null || gap < 0 || gap > 120) {
          continue;
        }

        if (parentEntry.node.layoutMode !== "VERTICAL" && !hasAlignedLeadingEdge(current, next, 48)) {
          continue;
        }

        const currentText = textEntryMap.get(current.id) || null;
        const nextText = textEntryMap.get(next.id) || null;

        if (currentText && nextText && isLikelyHeadingToBodyPair(currentText, nextText)) {
          titleBodyCandidates.push({
            groupKey: String(parentEntry.rootId || parentEntry.id) + ":title-body",
            nodeId: current.id,
            nodeName: current.name,
            nodeType: current.type,
            value: gap,
            label: truncateText(currentText.text || current.name, 28),
          });
          continue;
        }

        if (currentText && !nextText && isLikelySectionHeaderText(currentText, maxTextSize) && isLikelySectionContent(next)) {
          sectionCandidates.push({
            groupKey: String(parentEntry.rootId || parentEntry.id) + ":section-gap",
            nodeId: current.id,
            nodeName: current.name,
            nodeType: current.type,
            value: gap,
            label: truncateText(currentText.text || current.name, 28),
          });
        }
      }
    }

    return createGroupedNumericConsistencyIssues(titleBodyCandidates, {
      tolerance: 2,
      maxDistance: 24,
      buildIssue: function (candidate, nearest, currentCount, groupSize) {
        return {
          id: createIssueId("semantic-title-gap:" + roundValue(candidate.value), candidate.nodeId),
          category: "context",
          type: "title-body-gap",
          severity: Math.abs(nearest - candidate.value) >= 8 ? "warning" : "info",
          guideline: "제목-본문 간격",
          nodeId: candidate.nodeId,
          nodeName: candidate.nodeName,
          nodeType: candidate.nodeType,
          summary: "제목 아래 " + roundValue(candidate.value) + "px",
          detail:
            "같은 화면의 제목-본문 간격은 주로 " +
            nearest +
            "px인데, " +
            candidate.label +
            " 아래 간격만 " +
            roundValue(candidate.value) +
            "px입니다. (" +
            groupSize +
            "개 패턴 비교)",
          suggestion: nearest + "px 기준으로 맞추면 텍스트 위계 리듬이 더 안정적입니다.",
          canApply: false,
          applyLabel: "",
          fixPlan: null,
        };
      },
    }).concat(
      createGroupedNumericConsistencyIssues(sectionCandidates, {
        tolerance: 2,
        maxDistance: 32,
        buildIssue: function (candidate, nearest, currentCount, groupSize) {
          return {
            id: createIssueId("semantic-section-gap:" + roundValue(candidate.value), candidate.nodeId),
            category: "context",
            type: "section-gap",
            severity: Math.abs(nearest - candidate.value) >= 10 ? "warning" : "info",
            guideline: "섹션 간격",
            nodeId: candidate.nodeId,
            nodeName: candidate.nodeName,
            nodeType: candidate.nodeType,
            summary: "섹션 아래 " + roundValue(candidate.value) + "px",
            detail:
              "섹션 제목 아래 첫 콘텐츠까지 간격은 주로 " +
              nearest +
              "px인데, " +
              candidate.label +
              " 아래만 " +
              roundValue(candidate.value) +
              "px입니다. (" +
              groupSize +
              "개 섹션 비교)",
            suggestion: nearest + "px로 맞추면 섹션 구분 리듬이 더 또렷해집니다.",
            canApply: false,
            applyLabel: "",
            fixPlan: null,
          };
        },
      })
    );
  }

  function analyzeRepeatedPatternIssues(nodeEntries, textEntries) {
    const nodeEntryMap = buildNodeEntryMap(nodeEntries);
    const textEntryMap = buildTextEntryMap(textEntries);
    const paddingCandidates = [];
    const gapCandidates = [];
    const titleSizeCandidates = [];

    for (const parentEntry of nodeEntries) {
      const childEntries = listVisibleChildEntries(parentEntry, nodeEntryMap);
      if (childEntries.length < 2) {
        continue;
      }

      const groups = new Map();
      for (const childEntry of childEntries) {
        const summary = buildRepeatedPatternSummary(childEntry, nodeEntryMap, textEntryMap);
        if (!summary) {
          continue;
        }

        const bucket = groups.get(summary.signature) || [];
        bucket.push(summary);
        groups.set(summary.signature, bucket);
      }

      for (const group of groups.values()) {
        if (!group || group.length < 2) {
          continue;
        }

        const clusterKey = String(parentEntry.id) + "::" + String(group[0].signature || "cluster");
        for (const summary of group) {
          if (typeof summary.paddingValue === "number") {
            paddingCandidates.push({
              groupKey: clusterKey + ":padding",
              nodeId: summary.nodeId,
              nodeName: summary.nodeName,
              nodeType: summary.nodeType,
              value: summary.paddingValue,
              clusterSize: group.length,
              label: summary.label,
              fixPlan:
                summary.paddingFields && summary.paddingFields.length
                  ? {
                      action: "set-spacing-value",
                      targetNodeId: summary.nodeId,
                      changes: summary.paddingFields.map(function (field) {
                        return {
                          field: field,
                          value: summary.paddingValue,
                        };
                      }),
                    }
                  : null,
            });
          }

          if (typeof summary.gapValue === "number") {
            gapCandidates.push({
              groupKey: clusterKey + ":gap",
              nodeId: summary.nodeId,
              nodeName: summary.nodeName,
              nodeType: summary.nodeType,
              value: summary.gapValue,
              clusterSize: group.length,
              label: summary.label,
              fixPlan: {
                action: "set-spacing-value",
                targetNodeId: summary.nodeId,
                changes: [
                  {
                    field: "itemSpacing",
                    value: summary.gapValue,
                  },
                ],
              },
            });
          }

          if (summary.titleTextEntry && typeof summary.titleTextEntry.fontSize === "number") {
            titleSizeCandidates.push({
              groupKey: clusterKey + ":title-size",
              nodeId: summary.titleTextEntry.nodeId,
              nodeName: summary.nodeName,
              nodeType: summary.nodeType,
              value: summary.titleTextEntry.fontSize,
              clusterSize: group.length,
              label: summary.label,
              titleNodeId: summary.titleTextEntry.nodeId,
              titleNode: summary.titleTextEntry.node,
            });
          }
        }
      }
    }

    return createGroupedNumericConsistencyIssues(paddingCandidates, {
      tolerance: 2,
      maxDistance: 24,
      buildIssue: function (candidate, nearest, currentCount, groupSize) {
        return {
          id: createIssueId("repeat-padding:" + roundValue(candidate.value), candidate.nodeId),
          category: "component",
          type: "repeat-padding",
          severity: Math.abs(nearest - candidate.value) >= 8 ? "warning" : "info",
          guideline: "반복 카드/리스트 패딩",
          nodeId: candidate.nodeId,
          nodeName: candidate.nodeName,
          nodeType: candidate.nodeType,
          summary: "내부 패딩 " + roundValue(candidate.value) + "px",
          detail:
            "같은 구조의 카드/리스트 " +
            groupSize +
            "개를 비교하면 " +
            candidate.label +
            "만 패딩이 " +
            roundValue(candidate.value) +
            "px이고, 기준 패턴은 " +
            nearest +
            "px입니다.",
          suggestion: nearest + "px로 맞추면 반복 블록의 내부 리듬이 더 안정적입니다.",
          canApply: false,
          applyLabel: "",
          fixPlan: null,
        };
      },
    }).concat(
      createGroupedNumericConsistencyIssues(gapCandidates, {
        tolerance: 2,
        maxDistance: 20,
        buildIssue: function (candidate, nearest, currentCount, groupSize) {
          return {
            id: createIssueId("repeat-gap:" + roundValue(candidate.value), candidate.nodeId),
            category: "component",
            type: "repeat-gap",
            severity: Math.abs(nearest - candidate.value) >= 8 ? "warning" : "info",
            guideline: "반복 카드/리스트 간격",
            nodeId: candidate.nodeId,
            nodeName: candidate.nodeName,
            nodeType: candidate.nodeType,
            summary: "내부 간격 " + roundValue(candidate.value) + "px",
            detail:
              "같은 구조의 카드/리스트 " +
              groupSize +
              "개 중 " +
              candidate.label +
              "만 내부 간격이 " +
              roundValue(candidate.value) +
              "px입니다. 주로 쓰인 값은 " +
              nearest +
              "px입니다.",
            suggestion: nearest + "px로 맞추면 반복 패턴의 밀도가 더 일정해집니다.",
            canApply: false,
            applyLabel: "",
            fixPlan: null,
          };
        },
      })
    ).concat(
      createGroupedNumericConsistencyIssues(titleSizeCandidates, {
        tolerance: 1,
        maxDistance: 6,
        buildIssue: function (candidate, nearest, currentCount, groupSize) {
          const canApply = canAdjustFontSize(candidate.titleNode);
          return {
            id: createIssueId("repeat-title-size:" + roundValue(candidate.value), candidate.nodeId),
            category: "component",
            type: "repeat-title-size",
            severity: Math.abs(nearest - candidate.value) >= 3 ? "warning" : "info",
            guideline: "반복 카드/리스트 타이포",
            nodeId: candidate.nodeId,
            nodeName: candidate.nodeName,
            nodeType: candidate.nodeType,
            summary: "대표 텍스트 " + roundValue(candidate.value) + "px",
            detail:
              "같은 구조의 카드/리스트 " +
              groupSize +
              "개를 비교하면 " +
              candidate.label +
              "의 대표 텍스트 크기만 " +
              roundValue(candidate.value) +
              "px이고, 나머지는 주로 " +
              nearest +
              "px입니다.",
            suggestion: nearest + "px로 맞추면 반복 블록의 제목 위계가 더 안정적입니다.",
            canApply: canApply,
            applyLabel: canApply ? "크기 적용" : "",
            fixPlan: canApply
              ? {
                  action: "set-font-size",
                  targetNodeId: candidate.titleNodeId,
                  targetFontSize: nearest,
                }
              : null,
          };
        },
      })
    );
  }

  function analyzeFormConsistencyIssues(nodeEntries, textEntries) {
    const nodeEntryMap = buildNodeEntryMap(nodeEntries);
    const textEntryMap = buildTextEntryMap(textEntries);
    const blocks = collectFormBlockCandidates(nodeEntries, nodeEntryMap, textEntryMap);
    const blocksByRoot = new Map();

    for (const block of blocks) {
      const bucket = blocksByRoot.get(block.rootId) || [];
      bucket.push(block);
      blocksByRoot.set(block.rootId, bucket);
    }

    const labelGapCandidates = [];
    const helpGapCandidates = [];
    const helpSizeCandidates = [];

    for (const group of blocksByRoot.values()) {
      if (!group || group.length < 2) {
        continue;
      }

      for (const block of group) {
        labelGapCandidates.push({
          groupKey: String(block.rootId) + ":label-field-gap",
          nodeId: block.fieldNodeId,
          nodeName: block.nodeName,
          nodeType: block.nodeType,
          value: block.labelFieldGap,
          label: block.labelText,
        });

        if (typeof block.fieldHelpGap === "number") {
          helpGapCandidates.push({
            groupKey: String(block.rootId) + ":field-help-gap",
            nodeId: block.helpNodeId,
            nodeName: block.nodeName,
            nodeType: block.nodeType,
            value: block.fieldHelpGap,
            label: block.labelText,
          });
        }

        if (typeof block.helpFontSize === "number") {
          helpSizeCandidates.push({
            groupKey: String(block.rootId) + ":help-font-size",
            nodeId: block.helpNodeId,
            nodeName: block.nodeName,
            nodeType: block.nodeType,
            value: block.helpFontSize,
            label: block.labelText,
          });
        }
      }
    }

    return createGroupedNumericConsistencyIssues(labelGapCandidates, {
      tolerance: 2,
      maxDistance: 16,
      buildIssue: function (candidate, nearest) {
        return {
          id: createIssueId("form-label-gap:" + roundValue(candidate.value), candidate.nodeId),
          category: "form",
          type: "form-label-gap",
          severity: Math.abs(nearest - candidate.value) >= 6 ? "warning" : "info",
          guideline: "폼 라벨-인풋 간격",
          nodeId: candidate.nodeId,
          nodeName: candidate.nodeName,
          nodeType: candidate.nodeType,
          summary: "라벨-인풋 " + roundValue(candidate.value) + "px",
          detail:
            "같은 화면의 폼에서 라벨 아래 인풋 간격은 주로 " +
            nearest +
            "px인데, " +
            candidate.label +
            " 필드만 " +
            roundValue(candidate.value) +
            "px입니다.",
          suggestion: nearest + "px 기준으로 맞추면 폼 입력 리듬이 더 안정적입니다.",
          canApply: false,
          applyLabel: "",
          fixPlan: null,
        };
      },
    }).concat(
      createGroupedNumericConsistencyIssues(helpGapCandidates, {
        tolerance: 2,
        maxDistance: 12,
        buildIssue: function (candidate, nearest) {
          return {
            id: createIssueId("form-help-gap:" + roundValue(candidate.value), candidate.nodeId),
            category: "form",
            type: "form-help-gap",
            severity: Math.abs(nearest - candidate.value) >= 4 ? "warning" : "info",
            guideline: "폼 인풋-헬프텍스트 간격",
            nodeId: candidate.nodeId,
            nodeName: candidate.nodeName,
            nodeType: candidate.nodeType,
            summary: "인풋-헬프텍스트 " + roundValue(candidate.value) + "px",
            detail:
              "폼 헬프텍스트 간격은 주로 " +
              nearest +
              "px인데, " +
              candidate.label +
              " 필드 아래만 " +
              roundValue(candidate.value) +
              "px입니다.",
            suggestion: nearest + "px로 맞추면 도움말 리듬이 더 안정적입니다.",
            canApply: false,
            applyLabel: "",
            fixPlan: null,
          };
        },
      })
    ).concat(
      createGroupedNumericConsistencyIssues(helpSizeCandidates, {
        tolerance: 1,
        maxDistance: 4,
        buildIssue: function (candidate, nearest) {
          return {
            id: createIssueId("form-help-size:" + roundValue(candidate.value), candidate.nodeId),
            category: "form",
            type: "form-help-size",
            severity: Math.abs(nearest - candidate.value) >= 2 ? "warning" : "info",
            guideline: "폼 헬프텍스트 크기",
            nodeId: candidate.nodeId,
            nodeName: candidate.nodeName,
            nodeType: candidate.nodeType,
            summary: "헬프텍스트 " + roundValue(candidate.value) + "px",
            detail:
              "폼 도움말 텍스트는 주로 " +
              nearest +
              "px인데, " +
              candidate.label +
              " 필드의 헬프텍스트만 " +
              roundValue(candidate.value) +
              "px입니다.",
            suggestion: nearest + "px로 맞추면 라벨-인풋-헬프텍스트 위계가 더 또렷해집니다.",
            canApply: false,
            applyLabel: "",
            fixPlan: null,
          };
        },
      })
    );
  }

  function createGroupedNumericConsistencyIssues(candidates, options) {
    const groups = new Map();
    const issues = [];
    const tolerance = options && typeof options.tolerance === "number" ? options.tolerance : 1;
    const maxDistance = options && typeof options.maxDistance === "number" ? options.maxDistance : 16;

    for (const candidate of candidates) {
      if (!candidate || typeof candidate.groupKey !== "string") {
        continue;
      }
      const bucket = groups.get(candidate.groupKey) || [];
      bucket.push(candidate);
      groups.set(candidate.groupKey, bucket);
    }

    for (const group of groups.values()) {
      if (!group || group.length < 2) {
        continue;
      }

      const counts = new Map();
      for (const candidate of group) {
        bumpCount(counts, String(roundValue(candidate.value)));
      }

      const tokenItems = sortCountEntries(counts).filter(function (item) {
        return item.count >= 2;
      });
      if (!tokenItems.length) {
        continue;
      }

      const tokenValues = [];
      for (const item of tokenItems) {
        tokenValues.push(Number(item.key));
      }

      for (const candidate of group) {
        const currentValue = roundValue(candidate.value);
        const currentCount = counts.get(String(currentValue)) || 0;
        const nearest = findNearestNumericToken(currentValue, tokenValues);
        if (typeof nearest !== "number" || !Number.isFinite(nearest)) {
          continue;
        }
        if (Math.abs(nearest - currentValue) <= tolerance) {
          continue;
        }
        if (currentCount > 1) {
          continue;
        }
        if (Math.abs(nearest - currentValue) > maxDistance) {
          continue;
        }

        const issue =
          options && typeof options.buildIssue === "function"
            ? options.buildIssue(candidate, nearest, currentCount, group.length)
            : null;
        if (issue) {
          issues.push(issue);
        }
      }
    }

    return issues;
  }

  function buildNodeEntryMap(entries) {
    const map = new Map();
    for (const entry of Array.isArray(entries) ? entries : []) {
      if (!entry || typeof entry.id !== "string") {
        continue;
      }
      map.set(entry.id, entry);
    }
    return map;
  }

  function buildTextEntryMap(entries) {
    const map = new Map();
    for (const entry of Array.isArray(entries) ? entries : []) {
      if (!entry || typeof entry.nodeId !== "string") {
        continue;
      }
      map.set(entry.nodeId, entry);
    }
    return map;
  }

  function listVisibleChildEntries(parentEntry, nodeEntryMap) {
    const result = [];
    if (!parentEntry || !parentEntry.node || !hasChildren(parentEntry.node)) {
      return result;
    }

    for (const child of parentEntry.node.children) {
      const entry = nodeEntryMap.get(child.id) || null;
      if (!entry) {
        continue;
      }
      result.push(entry);
    }
    return result;
  }

  function sortEntriesVertically(entries) {
    return Array.isArray(entries)
      ? entries.slice().sort(function (left, right) {
          const leftY = left && left.bounds ? left.bounds.y : Number.POSITIVE_INFINITY;
          const rightY = right && right.bounds ? right.bounds.y : Number.POSITIVE_INFINITY;
          if (leftY !== rightY) {
            return leftY - rightY;
          }

          const leftX = left && left.bounds ? left.bounds.x : Number.POSITIVE_INFINITY;
          const rightX = right && right.bounds ? right.bounds.x : Number.POSITIVE_INFINITY;
          if (leftX !== rightX) {
            return leftX - rightX;
          }

          return Number(left && left.order ? left.order : 0) - Number(right && right.order ? right.order : 0);
        })
      : [];
  }

  function measureVerticalGap(upper, lower) {
    if (!upper || !lower || !upper.bounds || !lower.bounds) {
      return null;
    }
    return roundValue(lower.bounds.y - (upper.bounds.y + upper.bounds.height));
  }

  function hasAlignedLeadingEdge(left, right, maxOffset) {
    if (!left || !right || !left.bounds || !right.bounds) {
      return false;
    }
    const offset = typeof maxOffset === "number" ? maxOffset : 32;
    return Math.abs(left.bounds.x - right.bounds.x) <= offset;
  }

  function getMaxDirectChildTextSize(childEntries, textEntryMap) {
    let maxSize = 0;
    for (const childEntry of Array.isArray(childEntries) ? childEntries : []) {
      const textEntry = textEntryMap.get(childEntry.id) || null;
      if (!textEntry || typeof textEntry.fontSize !== "number" || !Number.isFinite(textEntry.fontSize)) {
        continue;
      }
      maxSize = Math.max(maxSize, textEntry.fontSize);
    }
    return maxSize;
  }

  function isLikelyHeadingToBodyPair(titleText, bodyText) {
    if (
      !titleText ||
      !bodyText ||
      typeof titleText.fontSize !== "number" ||
      !Number.isFinite(titleText.fontSize) ||
      typeof bodyText.fontSize !== "number" ||
      !Number.isFinite(bodyText.fontSize)
    ) {
      return false;
    }

    if (titleText.fontSize < 14 || bodyText.fontSize < 10) {
      return false;
    }

    if (titleText.fontSize < bodyText.fontSize + 1.5) {
      return false;
    }

    const titleLength = typeof titleText.text === "string" ? titleText.text.length : 0;
    const bodyLength = typeof bodyText.text === "string" ? bodyText.text.length : 0;
    if (titleLength > 64 && bodyLength < 16) {
      return false;
    }

    return true;
  }

  function isLikelySectionHeaderText(textEntry, maxTextSize) {
    if (!textEntry || typeof textEntry.fontSize !== "number" || !Number.isFinite(textEntry.fontSize)) {
      return false;
    }

    if (textEntry.fontSize < 14) {
      return false;
    }

    const textLength = typeof textEntry.text === "string" ? textEntry.text.length : 0;
    if (textLength > 60) {
      return false;
    }

    return textEntry.fontSize >= Math.max(14, maxTextSize - 1);
  }

  function isLikelySectionContent(entry) {
    if (!entry || !entry.bounds) {
      return false;
    }
    if (entry.type === "TEXT") {
      return false;
    }
    return entry.bounds.width >= 48 && entry.bounds.height >= 16;
  }

  function buildRepeatedPatternSummary(entry, nodeEntryMap, textEntryMap) {
    if (!entry || !entry.bounds || entry.type === "TEXT" || !hasChildren(entry.node)) {
      return null;
    }

    if (entry.bounds.width < 80 || entry.bounds.height < 32 || entry.bounds.height > 420) {
      return null;
    }

    const childEntries = listVisibleChildEntries(entry, nodeEntryMap);
    if (childEntries.length < 2) {
      return null;
    }

    const signature = buildRepeatedPatternSignature(entry, childEntries, nodeEntryMap, textEntryMap);
    if (!signature) {
      return null;
    }

    const paddingValue = getUniformPaddingValue(entry.node);
    const titleTextEntry = findPrimaryTextDescendant(entry, 2, nodeEntryMap, textEntryMap);
    const gapValue =
      typeof entry.node.itemSpacing === "number" && Number.isFinite(entry.node.itemSpacing)
        ? roundValue(entry.node.itemSpacing)
        : null;

    return {
      nodeId: entry.id,
      nodeName: entry.name,
      nodeType: entry.type,
      label: truncateText(entry.name, 24),
      signature: signature,
      paddingValue: paddingValue,
      paddingFields:
        typeof paddingValue === "number"
          ? ["paddingTop", "paddingRight", "paddingBottom", "paddingLeft"]
          : null,
      gapValue: gapValue,
      titleTextEntry: titleTextEntry,
    };
  }

  function buildRepeatedPatternSignature(entry, childEntries, nodeEntryMap, textEntryMap) {
    const orderedChildren = entry.node.layoutMode === "NONE" ? sortEntriesVertically(childEntries) : childEntries.slice();
    const kinds = [];
    const countLabel = orderedChildren.length > 6 ? "6+" : String(orderedChildren.length);

    for (let index = 0; index < orderedChildren.length && index < 4; index += 1) {
      kinds.push(describePatternChildKind(orderedChildren[index], nodeEntryMap, textEntryMap));
    }

    return [String(entry.node.layoutMode || "NONE"), countLabel, kinds.join("-")].join("|");
  }

  function describePatternChildKind(entry, nodeEntryMap, textEntryMap) {
    if (!entry) {
      return "U";
    }
    if (textEntryMap.has(entry.id)) {
      return "T";
    }
    if (isLikelyIconEntry(entry)) {
      return "I";
    }
    if (findPrimaryTextDescendant(entry, 1, nodeEntryMap, textEntryMap)) {
      return "B";
    }
    return hasChildren(entry.node) ? "C" : String(entry.type || "N").slice(0, 1);
  }

  function isLikelyIconEntry(entry) {
    if (!entry || !entry.bounds) {
      return false;
    }
    if (entry.bounds.width > 40 || entry.bounds.height > 40) {
      return false;
    }

    return (
      entry.type === "VECTOR" ||
      entry.type === "BOOLEAN_OPERATION" ||
      entry.type === "ELLIPSE" ||
      entry.type === "RECTANGLE" ||
      entry.type === "POLYGON" ||
      entry.type === "STAR"
    );
  }

  function getUniformPaddingValue(node) {
    if (!hasLayout(node)) {
      return null;
    }

    const fields = [node.paddingTop, node.paddingRight, node.paddingBottom, node.paddingLeft];
    for (const value of fields) {
      if (typeof value !== "number" || !Number.isFinite(value)) {
        return null;
      }
    }

    const rounded = roundValue(fields[0]);
    for (let index = 1; index < fields.length; index += 1) {
      if (Math.abs(roundValue(fields[index]) - rounded) > 0.01) {
        return null;
      }
    }

    return rounded;
  }

  function findPrimaryTextDescendant(entry, maxDepth, nodeEntryMap, textEntryMap) {
    if (!entry || typeof maxDepth !== "number" || maxDepth < 0) {
      return null;
    }

    let best = null;
    const selfText = textEntryMap.get(entry.id) || null;
    if (selfText && typeof selfText.fontSize === "number" && Number.isFinite(selfText.fontSize)) {
      best = selfText;
    }

    if (!hasChildren(entry.node) || maxDepth === 0) {
      return best;
    }

    const stack = [];
    for (let index = entry.node.children.length - 1; index >= 0; index -= 1) {
      const childEntry = nodeEntryMap.get(entry.node.children[index].id) || null;
      if (!childEntry) {
        continue;
      }
      stack.push({
        entry: childEntry,
        depth: 1,
      });
    }

    while (stack.length > 0) {
      const current = stack.pop();
      const currentEntry = current && current.entry ? current.entry : null;
      if (!currentEntry) {
        continue;
      }

      const textEntry = textEntryMap.get(currentEntry.id) || null;
      if (textEntry && typeof textEntry.fontSize === "number" && Number.isFinite(textEntry.fontSize)) {
        best = choosePreferredTextEntry(best, textEntry);
      }

      if (current.depth >= maxDepth || !hasChildren(currentEntry.node)) {
        continue;
      }

      for (let index = currentEntry.node.children.length - 1; index >= 0; index -= 1) {
        const childEntry = nodeEntryMap.get(currentEntry.node.children[index].id) || null;
        if (!childEntry) {
          continue;
        }
        stack.push({
          entry: childEntry,
          depth: current.depth + 1,
        });
      }
    }

    return best;
  }

  function choosePreferredTextEntry(current, next) {
    if (!current) {
      return next;
    }
    if (!next) {
      return current;
    }

    const currentSize = typeof current.fontSize === "number" ? current.fontSize : -1;
    const nextSize = typeof next.fontSize === "number" ? next.fontSize : -1;
    if (currentSize !== nextSize) {
      return nextSize > currentSize ? next : current;
    }

    const currentOrder = typeof current.order === "number" ? current.order : Number.POSITIVE_INFINITY;
    const nextOrder = typeof next.order === "number" ? next.order : Number.POSITIVE_INFINITY;
    return nextOrder < currentOrder ? next : current;
  }

  function collectFormBlockCandidates(nodeEntries, nodeEntryMap, textEntryMap) {
    const blocks = [];
    const seen = new Set();

    for (const parentEntry of nodeEntries) {
      const childEntries = sortEntriesVertically(listVisibleChildEntries(parentEntry, nodeEntryMap));
      if (childEntries.length < 2) {
        continue;
      }

      for (let index = 0; index < childEntries.length - 1; index += 1) {
        const labelEntry = childEntries[index];
        const fieldEntry = childEntries[index + 1];
        const labelText = textEntryMap.get(labelEntry.id) || null;
        if (!labelText || !isLikelyFormLabelText(labelText)) {
          continue;
        }
        if (!isLikelyInputLikeEntry(fieldEntry, textEntryMap)) {
          continue;
        }

        const labelFieldGap = measureVerticalGap(labelEntry, fieldEntry);
        if (labelFieldGap === null || labelFieldGap < 0 || labelFieldGap > 40) {
          continue;
        }
        if (!hasAlignedLeadingEdge(labelEntry, fieldEntry, 40)) {
          continue;
        }

        let helpEntry = null;
        let helpText = null;
        let fieldHelpGap = null;
        if (index + 2 < childEntries.length) {
          const nextEntry = childEntries[index + 2];
          const nextText = textEntryMap.get(nextEntry.id) || null;
          const nextGap = measureVerticalGap(fieldEntry, nextEntry);
          if (
            nextText &&
            isLikelyFormHelpText(nextText, labelText) &&
            nextGap !== null &&
            nextGap >= 0 &&
            nextGap <= 24 &&
            hasAlignedLeadingEdge(fieldEntry, nextEntry, 40)
          ) {
            helpEntry = nextEntry;
            helpText = nextText;
            fieldHelpGap = nextGap;
          }
        }

        const key = String(parentEntry.id) + ":" + String(labelEntry.id) + ":" + String(fieldEntry.id);
        if (seen.has(key)) {
          continue;
        }
        seen.add(key);

        blocks.push({
          rootId: String(parentEntry.rootId || parentEntry.id),
          nodeName: fieldEntry.name,
          nodeType: fieldEntry.type,
          fieldNodeId: fieldEntry.id,
          helpNodeId: helpEntry ? helpEntry.id : "",
          labelText: truncateText(labelText.text || labelEntry.name, 28),
          labelFieldGap: labelFieldGap,
          fieldHelpGap: fieldHelpGap,
          helpFontSize: helpText && typeof helpText.fontSize === "number" ? helpText.fontSize : null,
        });
      }
    }

    return blocks;
  }

  function isLikelyFormLabelText(textEntry) {
    if (
      !textEntry ||
      typeof textEntry.text !== "string" ||
      typeof textEntry.fontSize !== "number" ||
      !Number.isFinite(textEntry.fontSize)
    ) {
      return false;
    }

    const length = textEntry.text.trim().length;
    if (length < 1 || length > 48) {
      return false;
    }

    return textEntry.fontSize >= 11 && textEntry.fontSize <= 18;
  }

  function isLikelyFormHelpText(helpText, labelText) {
    if (!helpText || typeof helpText.text !== "string") {
      return false;
    }

    const helpLength = helpText.text.trim().length;
    const labelLength = labelText && typeof labelText.text === "string" ? labelText.text.trim().length : 0;
    const helpSize = typeof helpText.fontSize === "number" && Number.isFinite(helpText.fontSize) ? helpText.fontSize : 0;
    const labelSize = labelText && typeof labelText.fontSize === "number" && Number.isFinite(labelText.fontSize) ? labelText.fontSize : 0;

    if (helpLength < 4 || helpLength > 120) {
      return false;
    }
    if (helpSize > 0 && labelSize > 0 && helpSize > labelSize + 2) {
      return false;
    }
    if (helpLength <= labelLength && helpSize >= labelSize) {
      return false;
    }
    return true;
  }

  function isLikelyInputLikeEntry(entry, textEntryMap) {
    if (!entry || !entry.bounds || textEntryMap.has(entry.id)) {
      return false;
    }

    if (entry.bounds.width < 120 || entry.bounds.height < 28 || entry.bounds.height > 72) {
      return false;
    }

    if (entry.type === "VECTOR" || entry.type === "LINE") {
      return false;
    }

    return (
      entry.type === "FRAME" ||
      entry.type === "GROUP" ||
      entry.type === "INSTANCE" ||
      entry.type === "COMPONENT" ||
      entry.type === "COMPONENT_SET" ||
      entry.type === "RECTANGLE" ||
      hasChildren(entry.node)
    );
  }

  function dedupeIssues(issues) {
    const map = new Map();
    for (const issue of issues) {
      if (!issue || !issue.id) {
        continue;
      }
      if (!map.has(issue.id)) {
        map.set(issue.id, issue);
      }
    }
    return Array.from(map.values());
  }

  function compareIssues(left, right) {
    const severityRank = {
      warning: 0,
      info: 1,
    };
    const leftSeverityKey = left && left.severity ? left.severity : "info";
    const rightSeverityKey = right && right.severity ? right.severity : "info";
    const leftSeverityValue = severityRank[leftSeverityKey];
    const rightSeverityValue = severityRank[rightSeverityKey];
    const leftSeverity = typeof leftSeverityValue === "number" ? leftSeverityValue : 99;
    const rightSeverity = typeof rightSeverityValue === "number" ? rightSeverityValue : 99;
    if (leftSeverity !== rightSeverity) {
      return leftSeverity - rightSeverity;
    }

    const categoryRank = {
      color: 0,
      typography: 1,
      context: 2,
      component: 3,
      form: 4,
      spacing: 5,
    };
    const leftCategoryKey = left && left.category ? left.category : "spacing";
    const rightCategoryKey = right && right.category ? right.category : "spacing";
    const leftCategoryValue = categoryRank[leftCategoryKey];
    const rightCategoryValue = categoryRank[rightCategoryKey];
    const leftCategory = typeof leftCategoryValue === "number" ? leftCategoryValue : 99;
    const rightCategory = typeof rightCategoryValue === "number" ? rightCategoryValue : 99;
    if (leftCategory !== rightCategory) {
      return leftCategory - rightCategory;
    }

    return String(left && left.nodeName ? left.nodeName : "").localeCompare(String(right && right.nodeName ? right.nodeName : ""));
  }

  function buildInsights(consistency) {
    const insights = [];
    if (consistency.colorTokenSummary) {
      insights.push(`색상 토큰: ${consistency.colorTokenSummary}`);
    }
    if (consistency.typographyTokenSummary) {
      insights.push(`타이포 토큰: ${consistency.typographyTokenSummary}`);
    }
    if (consistency.spacingTokenSummary) {
      insights.push(`여백 토큰: ${consistency.spacingTokenSummary}`);
    }
    insights.push(
      `일관성 이슈: 색상 ${consistency.colorIssueCount}건 · 타이포 ${consistency.typographyIssueCount}건 · 여백 ${consistency.spacingIssueCount}건`
    );
    if (
      (consistency.semanticIssueCount || 0) > 0 ||
      (consistency.repetitionIssueCount || 0) > 0 ||
      (consistency.formIssueCount || 0) > 0
    ) {
      insights.push(
        `맥락 이슈: 제목/섹션 ${consistency.semanticIssueCount || 0}건 · 반복 블록 ${consistency.repetitionIssueCount || 0}건 · 폼 ${consistency.formIssueCount || 0}건`
      );
    }
    if (consistency.fixableCount > 0) {
      insights.push(`즉시 적용 가능: ${consistency.fixableCount}건`);
    } else {
      insights.push("즉시 적용 가능한 제안은 없습니다.");
    }
    return insights.slice(0, 6);
  }

  async function enrichConsistencyWithAi(localResult) {
    const ai = getAiHelper();
    if (!ai) {
      localResult.summary.aiStatusLabel = "로컬 휴리스틱";
      return localResult;
    }

    let configured = false;
    let runInfo = {
      provider: "",
      model: "",
    };

    try {
      const settings = typeof ai.getAiSettingsAsync === "function" ? await ai.getAiSettingsAsync() : null;
      if (settings && typeof ai.getResolvedRunInfo === "function") {
        runInfo = ai.getResolvedRunInfo(settings);
      }
      configured = await ai.hasConfiguredAiAsync();
    } catch (error) {
      configured = false;
    }

    if (!configured) {
      localResult.summary.aiStatusLabel = "로컬 휴리스틱";
      return localResult;
    }

    localResult.summary.aiStatusLabel = "AI + 로컬";
    localResult.summary.aiProviderLabel = formatProviderLabel(runInfo.provider);
    localResult.summary.aiModelLabel = runInfo.model || "";

    const payload = {
      selectionLabel: localResult.summary ? localResult.summary.selectionLabel : "",
      roots: Array.isArray(localResult.roots) ? localResult.roots.slice(0, 6) : [],
      selectionBounds: localResult.selectionBounds || null,
      stats: localResult.stats || {},
      currentSummary: localResult.summary || {},
      consistency: {
        colorTokenSummary: localResult.consistency ? localResult.consistency.colorTokenSummary : "",
        typographyTokenSummary: localResult.consistency ? localResult.consistency.typographyTokenSummary : "",
        spacingTokenSummary: localResult.consistency ? localResult.consistency.spacingTokenSummary : "",
        colorIssueCount: localResult.consistency ? localResult.consistency.colorIssueCount : 0,
        typographyIssueCount: localResult.consistency ? localResult.consistency.typographyIssueCount : 0,
        spacingIssueCount: localResult.consistency ? localResult.consistency.spacingIssueCount : 0,
        semanticIssueCount: localResult.consistency ? localResult.consistency.semanticIssueCount : 0,
        repetitionIssueCount: localResult.consistency ? localResult.consistency.repetitionIssueCount : 0,
        formIssueCount: localResult.consistency ? localResult.consistency.formIssueCount : 0,
        issues: Array.isArray(localResult.consistency && localResult.consistency.issues)
          ? localResult.consistency.issues.slice(0, 6).map((issue) => ({
              id: issue.id,
              category: issue.category,
              summary: issue.summary,
              detail: issue.detail,
              suggestion: issue.suggestion,
            }))
          : [],
      },
      currentInsights: Array.isArray(localResult.insights) ? localResult.insights.slice(0, 6) : [],
    };
    const schema = {
      type: "object",
      properties: {
        contextLabel: { type: "string" },
        colorSummary: { type: "string" },
        typographySummary: { type: "string" },
        spacingSummary: { type: "string" },
        prioritySummary: { type: "string" },
        insights: {
          type: "array",
          items: { type: "string" },
        },
      },
      required: ["contextLabel", "colorSummary", "typographySummary", "spacingSummary", "prioritySummary", "insights"],
    };

    try {
      const response = await ai.requestJsonTask({
        instructions:
          "You analyze Figma design metadata for a design consistency diagnosis feature. Return concise Korean JSON only. Keep labels practical. Summarize color, typography, and spacing consistency using only values already present in the payload. Do not invent design system tokens that are not implied by the payload.",
        schema,
        payload,
      });
      if (!response || typeof response !== "object") {
        return localResult;
      }

      if (typeof response.contextLabel === "string" && response.contextLabel.trim()) {
        localResult.summary.contextLabel = response.contextLabel.trim();
      }
      if (typeof response.colorSummary === "string" && response.colorSummary.trim()) {
        localResult.consistency.aiColorSummary = response.colorSummary.trim();
      }
      if (typeof response.typographySummary === "string" && response.typographySummary.trim()) {
        localResult.consistency.aiTypographySummary = response.typographySummary.trim();
      }
      if (typeof response.spacingSummary === "string" && response.spacingSummary.trim()) {
        localResult.consistency.aiSpacingSummary = response.spacingSummary.trim();
      }
      if (typeof response.prioritySummary === "string" && response.prioritySummary.trim()) {
        localResult.consistency.prioritySummary = response.prioritySummary.trim();
      }

      localResult.summary.aiProviderLabel = formatProviderLabel(response._provider || runInfo.provider);
      localResult.summary.aiModelLabel = response._model || runInfo.model || "";
      localResult.source = typeof response._provider === "string" ? response._provider : "hybrid-ai";
      localResult.insights = mergeAiInsights(localResult, response);
    } catch (error) {
      localResult.aiError = normalizeErrorMessage(error, "AI 디자인 일관성 요약에 실패했습니다.");
    }

    return localResult;
  }

  function mergeAiInsights(localResult, response) {
    const next = [];
    if (typeof response.colorSummary === "string" && response.colorSummary.trim()) {
      next.push("AI 색상 판단: " + response.colorSummary.trim());
    }
    if (typeof response.typographySummary === "string" && response.typographySummary.trim()) {
      next.push("AI 타이포 판단: " + response.typographySummary.trim());
    }
    if (typeof response.spacingSummary === "string" && response.spacingSummary.trim()) {
      next.push("AI 여백 판단: " + response.spacingSummary.trim());
    }
    if (typeof response.prioritySummary === "string" && response.prioritySummary.trim()) {
      next.push("우선 수정: " + response.prioritySummary.trim());
    }
    if (Array.isArray(response.insights)) {
      for (const item of response.insights) {
        if (typeof item === "string" && item.trim()) {
          next.push(item.trim());
        }
      }
    }
    return uniqueStrings(next.concat(Array.isArray(localResult.insights) ? localResult.insights : []), 6);
  }

  async function applyFixPlan(plan) {
    if (!plan || typeof plan !== "object" || typeof plan.action !== "string") {
      throw new Error("적용 가능한 수정 정보가 없습니다.");
    }

    if (plan.action === "set-solid-paint-color") {
      applySolidPaintColorPlan(plan);
      return;
    }
    if (plan.action === "set-font-size") {
      await applyFontSizePlan(plan);
      return;
    }
    if (plan.action === "set-spacing-value") {
      applySpacingPlan(plan);
      return;
    }

    throw new Error("아직 지원하지 않는 수정 유형입니다.");
  }

  function applySolidPaintColorPlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    const property = plan.property === "strokes" ? "strokes" : "fills";
    const paintIndex = typeof plan.paintIndex === "number" ? plan.paintIndex : -1;
    if (!canEditSolidPaintProperty(node, property, paintIndex)) {
      throw new Error("색상을 변경할 레이어를 찾지 못했습니다.");
    }

    const paints = node[property].map((paint) => clonePlainObject(paint));
    const target = paints[paintIndex];
    if (!target || target.type !== "SOLID") {
      throw new Error("변경할 색상 페인트를 찾지 못했습니다.");
    }

    target.color = cloneColor(plan.color || {});
    node[property] = paints;
  }

  async function applyFontSizePlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    if (!canAdjustFontSize(node)) {
      throw new Error("폰트 크기를 변경할 텍스트를 찾지 못했습니다.");
    }

    const targetFontSize = Number(plan.targetFontSize);
    if (!Number.isFinite(targetFontSize) || targetFontSize <= 0) {
      throw new Error("적용할 폰트 크기가 올바르지 않습니다.");
    }

    await loadFontsForTextNode(node);
    if (typeof node.characters === "string" && node.characters.length > 0 && typeof node.setRangeFontSize === "function") {
      node.setRangeFontSize(0, node.characters.length, targetFontSize);
      return;
    }

    throw new Error("이 텍스트는 폰트 크기를 직접 조정할 수 없습니다.");
  }

  function applySpacingPlan(plan) {
    const node = figma.getNodeById(plan.targetNodeId);
    if (!node) {
      throw new Error("간격을 조정할 레이어를 찾지 못했습니다.");
    }

    const changes = Array.isArray(plan.changes) ? plan.changes : [];
    if (!changes.length) {
      throw new Error("적용할 간격 값이 비어 있습니다.");
    }

    const fields = changes
      .filter((change) => !!change && typeof change.field === "string")
      .map((change) => change.field);
    if (!canAdjustSpacingFields(node, fields)) {
      throw new Error("현재 선택에서는 간격 값을 직접 적용할 수 없습니다.");
    }

    for (const change of changes) {
      if (!change || typeof change.field !== "string") {
        continue;
      }
      if (!(change.field in node)) {
        continue;
      }
      const nextValue = Math.max(0, Number(change.value) || 0);
      node[change.field] = nextValue;
    }
  }

  function supportsAnnotations(nodes) {
    const sampleNode = Array.isArray(nodes) && nodes.length > 0 ? nodes[0] : null;
    return !!sampleNode && "annotations" in sampleNode;
  }

  async function ensureAnnotationCategory(requestedColor, options) {
    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return null;
    }

    try {
      const createIfMissing = !options || options.createIfMissing !== false;
      const nextColor =
        typeof requestedColor === "string" && requestedColor.trim()
          ? requestedColor.trim().toLowerCase()
          : ANNOTATION_CATEGORY_COLOR;
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      const existing = Array.isArray(categories)
        ? categories.find((category) => category && category.label === ANNOTATION_CATEGORY_LABEL)
        : null;
      if (existing) {
        if (typeof existing.setColor === "function" && existing.color !== nextColor) {
          existing.setColor(nextColor);
        }
        return existing;
      }

      if (!createIfMissing || typeof figma.annotations.addAnnotationCategoryAsync !== "function") {
        return null;
      }

      return await figma.annotations.addAnnotationCategoryAsync({
        label: ANNOTATION_CATEGORY_LABEL,
        color: nextColor,
      });
    } catch (error) {
      return null;
    }
  }

  async function getManagedDiagnosisCategories() {
    const managed = {
      consistency: null,
      accessibility: null,
    };

    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return managed;
    }

    try {
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      if (!Array.isArray(categories)) {
        return managed;
      }

      for (const category of categories) {
        if (!category || typeof category.label !== "string") {
          continue;
        }
        if (category.label === ANNOTATION_CATEGORY_LABEL) {
          managed.consistency = category;
          continue;
        }
        if (category.label === ACCESSIBILITY_ANNOTATION_CATEGORY_LABEL) {
          managed.accessibility = category;
        }
      }
    } catch (error) {}

    return managed;
  }

  function applyConsistencyAnnotations(nodes, issues, category) {
    const issuesByNode = new Map();
    const skipped = [];
    let annotationCount = 0;
    let annotatedNodeCount = 0;
    let clearedNodeCount = 0;
    let removedAnnotationCount = 0;

    for (const issue of issues) {
      const bucket = issuesByNode.get(issue.nodeId) || [];
      bucket.push(issue);
      issuesByNode.set(issue.nodeId, bucket);
    }

    for (const node of nodes) {
      const nodeIssues = issuesByNode.get(node.id) || [];
      try {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        const preserved = existing.filter((annotation) => !isManagedConsistencyAnnotation(annotation, category));
        const removedCount = Math.max(0, existing.length - preserved.length);

        if (removedCount > 0) {
          removedAnnotationCount += removedCount;
        }
        if (removedCount > 0 && nodeIssues.length === 0) {
          clearedNodeCount += 1;
        }

        node.annotations = preserved.concat(nodeIssues.map((issue) => buildConsistencyAnnotation(issue, category)));
        if (nodeIssues.length > 0) {
          annotatedNodeCount += 1;
          annotationCount += nodeIssues.length;
        }
      } catch (error) {
        if (nodeIssues.length > 0) {
          skipped.push({
            label: safeName(node),
            reason: normalizeErrorMessage(error, "디자인 일관성 주석을 추가하지 못했습니다."),
          });
        }
      }
    }

    return {
      skipped,
      annotationCount,
      annotatedNodeCount,
      clearedNodeCount,
      removedAnnotationCount,
      modeLabel: annotationCount > 0 ? "Blue Dev Mode annotation" : "Result only",
      categoryLabel: category && category.label ? category.label : ANNOTATION_CATEGORY_LABEL,
    };
  }

  function removeDiagnosisAnnotations(nodes, managedCategories) {
    const cleared = [];
    const skipped = [];
    let clearedNodeCount = 0;
    let removedAnnotationCount = 0;
    let removedAccessibilityAnnotationCount = 0;
    let removedConsistencyAnnotationCount = 0;
    const categories = managedCategories && typeof managedCategories === "object" ? managedCategories : {};
    const consistencyCategory = categories.consistency || null;
    const accessibilityCategory = categories.accessibility || null;

    for (const node of Array.isArray(nodes) ? nodes : []) {
      let managedAnnotations = [];
      let removedNodeAccessibilityCount = 0;
      let removedNodeConsistencyCount = 0;

      try {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        managedAnnotations = existing.filter(
          (annotation) =>
            isManagedConsistencyAnnotation(annotation, consistencyCategory) ||
            isManagedAccessibilityAnnotation(annotation, accessibilityCategory)
        );
        if (!managedAnnotations.length) {
          continue;
        }

        managedAnnotations.forEach((annotation) => {
          if (isManagedConsistencyAnnotation(annotation, consistencyCategory)) {
            removedNodeConsistencyCount += 1;
            return;
          }
          if (isManagedAccessibilityAnnotation(annotation, accessibilityCategory)) {
            removedNodeAccessibilityCount += 1;
          }
        });

        const preserved = existing.filter(
          (annotation) =>
            !isManagedConsistencyAnnotation(annotation, consistencyCategory) &&
            !isManagedAccessibilityAnnotation(annotation, accessibilityCategory)
        );
        node.annotations = preserved;
        clearedNodeCount += 1;
        removedAnnotationCount += managedAnnotations.length;
        removedAccessibilityAnnotationCount += removedNodeAccessibilityCount;
        removedConsistencyAnnotationCount += removedNodeConsistencyCount;
        cleared.push({
          nodeId: node.id,
          nodeName: safeName(node),
          removedCount: managedAnnotations.length,
          removedAccessibilityCount: removedNodeAccessibilityCount,
          removedConsistencyCount: removedNodeConsistencyCount,
        });
      } catch (error) {
        if (!managedAnnotations.length) {
          continue;
        }

        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "디자인 진단 주석을 정리하지 못했습니다."),
        });
      }
    }

    return {
      cleared,
      skipped,
      clearedNodeCount,
      removedAnnotationCount,
      removedAccessibilityAnnotationCount,
      removedConsistencyAnnotationCount,
    };
  }

  function buildConsistencyAnnotation(issue, category) {
    const lines = [`[중요도 : ${getIssuePriorityLabel(issue)}]`];
    if (issue.summary) {
      lines.push(`- ${issue.summary}`);
    }
    if (issue.suggestion) {
      lines.push(`- ${issue.suggestion}`);
    }

    const annotation = {
      label: lines.join("\n"),
    };
    if (category && category.id) {
      annotation.categoryId = category.id;
    }
    return annotation;
  }

  function getIssuePriorityLabel(issue) {
    const severity = issue && typeof issue.severity === "string" ? issue.severity : "info";
    if (severity === "error") {
      return "상";
    }
    if (severity === "warning") {
      return "중";
    }
    return "하";
  }

  function isManagedConsistencyAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }

    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }

    const label =
      typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return typeof label === "string" && (label.startsWith(ANNOTATION_LABEL_PREFIX) || label.startsWith(`[${ANNOTATION_LABEL_PREFIX}]`));
  }

  function isManagedAccessibilityAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }

    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }

    const label =
      typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return (
      typeof label === "string" &&
      (label.startsWith(ACCESSIBILITY_ANNOTATION_LABEL_PREFIX) ||
        label.startsWith(`[${ACCESSIBILITY_ANNOTATION_LABEL_PREFIX}]`))
    );
  }

  function buildResultOnlyAnnotation(issues, reason) {
    const skipped = [];
    if (Array.isArray(issues) && issues.length > 0 && reason) {
      skipped.push({
        label: "주석 미지원",
        reason,
      });
    }

    return {
      skipped,
      annotationCount: 0,
      annotatedNodeCount: 0,
      clearedNodeCount: 0,
      removedAnnotationCount: 0,
      modeLabel: "Result only",
      categoryLabel: ANNOTATION_CATEGORY_LABEL,
    };
  }

  function collectPaintEntries(node, entry, bucket) {
    collectPaintEntriesFromArray(node, entry, "fills", node.fills, bucket, node.type === "TEXT" ? "text" : "fills");
    if (node.type !== "TEXT") {
      collectPaintEntriesFromArray(node, entry, "strokes", node.strokes, bucket, "strokes");
    }
  }

  function collectPaintEntriesFromArray(node, entry, property, paints, bucket, role) {
    if (!Array.isArray(paints)) {
      return;
    }

    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (!paint || paint.visible === false || paint.type !== "SOLID" || !paint.color) {
        continue;
      }

      const opacity = typeof paint.opacity === "number" && Number.isFinite(paint.opacity) ? paint.opacity : 1;
      if (opacity < 0.95) {
        continue;
      }

      bucket.push({
        node,
        nodeId: node.id,
        nodeName: entry.name,
        nodeType: entry.type,
        property,
        paintIndex: index,
        role,
        color: cloneColor(paint.color),
        hex: rgbToHex(paint.color),
        opacity,
        order: entry.order,
      });
    }
  }

  function buildTextEntry(node, entry) {
    const value = getTextValue(node);
    return {
      node,
      nodeId: node.id,
      nodeName: entry.name,
      nodeType: entry.type,
      text: value,
      fontFamily: resolveUniformFontFamily(node),
      fontSize: resolveUniformFontSize(node),
      bounds: entry.bounds,
      order: entry.order,
      depth: entry.depth,
      rootId: entry.rootId,
      parentId: entry.parentId,
    };
  }

  function collectSpacingEntries(node, entry, bucket) {
    if (!hasLayout(node)) {
      return;
    }

    if (typeof node.itemSpacing === "number" && Number.isFinite(node.itemSpacing) && node.itemSpacing > 0) {
      bucket.push({
        node,
        nodeId: node.id,
        nodeName: entry.name,
        nodeType: entry.type,
        kind: "gap",
        label: "간격",
        value: roundValue(node.itemSpacing),
        fields: ["itemSpacing"],
      });
    }

    const paddingFields = [
      { field: "paddingTop", label: "상단 여백", value: node.paddingTop },
      { field: "paddingRight", label: "우측 여백", value: node.paddingRight },
      { field: "paddingBottom", label: "하단 여백", value: node.paddingBottom },
      { field: "paddingLeft", label: "좌측 여백", value: node.paddingLeft },
    ].filter((item) => typeof item.value === "number" && Number.isFinite(item.value) && item.value > 0);

    if (paddingFields.length === 4) {
      const uniqueValues = uniqueNumbers(paddingFields.map((item) => roundValue(item.value)));
      if (uniqueValues.length === 1) {
        bucket.push({
          node,
          nodeId: node.id,
          nodeName: entry.name,
          nodeType: entry.type,
          kind: "padding",
          label: "패딩",
          value: uniqueValues[0],
          fields: ["paddingTop", "paddingRight", "paddingBottom", "paddingLeft"],
        });
        return;
      }
    }

    for (const item of paddingFields) {
      bucket.push({
        node,
        nodeId: node.id,
        nodeName: entry.name,
        nodeType: entry.type,
        kind: item.field,
        label: item.label,
        value: roundValue(item.value),
        fields: [item.field],
      });
    }
  }

  function supportsAnnotationsOnNode(node) {
    return !!node && "annotations" in node;
  }

  function canEditPaintEntry(entry) {
    return !!entry && canEditSolidPaintProperty(entry.node, entry.property, entry.paintIndex);
  }

  function canAdjustFontSize(node) {
    return (
      !!node &&
      node.type === "TEXT" &&
      isDirectlyEditableNode(node) &&
      typeof node.characters === "string" &&
      node.characters.length > 0 &&
      typeof node.setRangeFontSize === "function" &&
      collectEditableFontNames(node).length > 0
    );
  }

  function canEditSolidPaintProperty(node, property, paintIndex) {
    if (!isDirectlyEditableNode(node)) {
      return false;
    }
    if ((property !== "fills" && property !== "strokes") || !(property in node) || !Array.isArray(node[property])) {
      return false;
    }
    if (typeof paintIndex !== "number" || paintIndex < 0) {
      return false;
    }
    const paint = node[property][paintIndex];
    return !!paint && paint.visible !== false && paint.type === "SOLID" && !!paint.color;
  }

  function canAdjustSpacingFields(node, fields) {
    if (!isDirectlyEditableNode(node) || !Array.isArray(fields) || !fields.length) {
      return false;
    }
    for (const field of fields) {
      if (!SPACING_PLAN_FIELDS.has(field) || typeof node[field] !== "number" || !Number.isFinite(node[field])) {
        return false;
      }
    }
    return true;
  }

  function isDirectlyEditableNode(node) {
    return !!node && !hasLockedAncestor(node) && !hasInstanceAncestor(node);
  }

  function hasLockedAncestor(node) {
    let current = node;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.locked === true) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function hasInstanceAncestor(node) {
    let current = node;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.type === "INSTANCE") {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function hasLayout(node) {
    return !!node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function getTextValue(node) {
    return typeof node.characters === "string" ? truncateText(node.characters, 72) : "";
  }

  function truncateText(value, limit) {
    const normalized = String(value || "").replace(/\s+/g, " ").trim();
    const maxLength = typeof limit === "number" ? limit : 48;
    if (!normalized) {
      return "";
    }
    return normalized.length > maxLength ? `${normalized.slice(0, maxLength - 3)}...` : normalized;
  }

  function resolveUniformFontFamily(node) {
    if (!node || node.type !== "TEXT") {
      return "";
    }
    if (node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      return node.fontName.family || "";
    }

    if (node.fontName === figma.mixed && typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string") {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        const families = Array.from(
          new Set(
            rangeFonts
              .filter((fontName) => fontName && typeof fontName.family === "string" && fontName.family.trim())
              .map((fontName) => fontName.family.trim())
          )
        );
        return families.length === 1 ? families[0] : "Mixed";
      } catch (error) {
        return "Mixed";
      }
    }

    return "";
  }

  function resolveUniformFontSize(node) {
    if (!node || node.type !== "TEXT") {
      return null;
    }
    if (typeof node.fontSize === "number" && Number.isFinite(node.fontSize)) {
      return roundValue(node.fontSize);
    }
    return null;
  }

  function findNearestColorToken(color, tokenItems) {
    let best = null;
    for (const item of tokenItems) {
      const distance = colorDistance(color, item.color);
      if (!best || distance < best.distance) {
        best = Object.assign({}, item, {
          distance,
        });
      }
    }
    return best;
  }

  function findNearestNumericToken(value, tokenValues) {
    let best = null;
    for (const candidate of tokenValues) {
      if (typeof candidate !== "number" || !Number.isFinite(candidate)) {
        continue;
      }
      const distance = Math.abs(candidate - value);
      if (best === null || distance < Math.abs(best - value)) {
        best = candidate;
      }
    }
    return best;
  }

  function isOnFourPointGrid(value) {
    return Math.abs(value - Math.round(value / 4) * 4) < 0.01;
  }

  function colorDistance(left, right) {
    if (!left || !right) {
      return Number.POSITIVE_INFINITY;
    }
    const red = Number(left.r || 0) - Number(right.r || 0);
    const green = Number(left.g || 0) - Number(right.g || 0);
    const blue = Number(left.b || 0) - Number(right.b || 0);
    return Math.sqrt(red * red + green * green + blue * blue);
  }

  function uniqueNumbers(values) {
    const seen = new Set();
    const result = [];
    for (const value of values) {
      const key = String(value);
      if (seen.has(key)) {
        continue;
      }
      seen.add(key);
      result.push(value);
    }
    return result;
  }

  function uniqueStrings(values, limit) {
    const seen = new Set();
    const result = [];
    for (const value of values) {
      const normalized = typeof value === "string" ? value.trim() : "";
      if (!normalized || seen.has(normalized)) {
        continue;
      }
      seen.add(normalized);
      result.push(normalized);
      if (typeof limit === "number" && result.length >= limit) {
        break;
      }
    }
    return result;
  }

  function getAiHelper() {
    const helper = globalScope.__PIGMA_AI_LLM__;
    return helper &&
      typeof helper.requestJsonTask === "function" &&
      typeof helper.hasConfiguredAiAsync === "function"
      ? helper
      : null;
  }

  function getSelectionSignature(selection) {
    const ids = Array.from(selection || [])
      .map((node) => node.id)
      .sort();
    return `${figma.currentPage.id}:${ids.join(",")}`;
  }

  function formatSelectionLabel(roots) {
    if (!roots.length) {
      return "선택 없음";
    }
    if (roots.length === 1) {
      return roots[0].name;
    }
    return `${roots[0].name} 외 ${roots.length - 1}개`;
  }

  function inferDesignContext(options) {
    const corpus = `${options.rootNames.join(" ")} ${options.textSamples.join(" ")}`.toLowerCase();
    const categories = [
      {
        label: "로그인/인증 화면",
        reason: "로그인, 이메일, 비밀번호 계열 텍스트가 함께 보입니다.",
        keywords: ["login", "sign in", "sign up", "email", "password", "로그인", "이메일", "비밀번호", "회원가입"],
      },
      {
        label: "대시보드/리포트",
        reason: "숫자, 표, 카드형 정보가 많은 업무용 화면처럼 보입니다.",
        keywords: ["dashboard", "analytics", "report", "chart", "table", "대시보드", "리포트", "차트", "테이블"],
      },
      {
        label: "랜딩/프로모션",
        reason: "CTA 문구와 소개성 텍스트가 함께 나타납니다.",
        keywords: ["get started", "learn more", "shop", "download", "discover", "시작", "자세히", "다운로드", "구매"],
      },
      {
        label: "모달/다이얼로그",
        reason: "짧은 안내 텍스트와 확인/취소 계열 버튼이 보입니다.",
        keywords: ["cancel", "confirm", "delete", "ok", "취소", "확인", "삭제"],
      },
    ];

    let best = null;
    for (const category of categories) {
      let score = 0;
      for (const keyword of category.keywords) {
        if (corpus.includes(keyword)) {
          score += 1;
        }
      }
      if (category.label === "모달/다이얼로그" && options.selectionBounds && options.selectionBounds.width <= 720) {
        score += 1;
      }
      if (category.label === "대시보드/리포트" && options.stats.textNodeCount >= 8) {
        score += 1;
      }
      if (!best || score > best.score) {
        best = {
          label: category.label,
          reason: category.reason,
          score,
        };
      }
    }

    if (best && best.score > 0) {
      return best;
    }

    if (options.topTypes.length > 0) {
      return {
        label: "일반 UI 화면",
        reason: `${formatNodeType(options.topTypes[0].type || options.topTypes[0].key)} 중심 레이아웃입니다.`,
        score: 0,
      };
    }

    return {
      label: "선택 영역",
      reason: "선택된 레이어 집합을 기준으로 검사했습니다.",
      score: 0,
    };
  }

  function sortCountEntries(map) {
    return Array.from(map.entries())
      .map((entry) => ({
        key: entry[0],
        count: entry[1],
      }))
      .sort((left, right) => {
        if (right.count !== left.count) {
          return right.count - left.count;
        }
        return String(left.key).localeCompare(String(right.key));
      });
  }

  function bumpCount(map, key) {
    if (!key && key !== 0) {
      return;
    }
    map.set(key, (map.get(key) || 0) + 1);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim().length > 0) {
      return node.name.trim();
    }
    return String((node && node.type) || "Unnamed");
  }

  function hasChildren(node) {
    return !!node && Array.isArray(node.children);
  }

  function isNodeVisible(node) {
    return !!node && node.visible !== false;
  }

  function combineBounds(boundsList) {
    if (!boundsList.length) {
      return null;
    }
    let left = boundsList[0].x;
    let top = boundsList[0].y;
    let right = boundsList[0].x + boundsList[0].width;
    let bottom = boundsList[0].y + boundsList[0].height;
    for (let index = 1; index < boundsList.length; index += 1) {
      const bounds = boundsList[index];
      left = Math.min(left, bounds.x);
      top = Math.min(top, bounds.y);
      right = Math.max(right, bounds.x + bounds.width);
      bottom = Math.max(bottom, bounds.y + bounds.height);
    }
    return {
      x: left,
      y: top,
      width: Math.max(0, right - left),
      height: Math.max(0, bottom - top),
    };
  }

  function getNodeBounds(node) {
    if (node && node.absoluteBoundingBox) {
      return node.absoluteBoundingBox;
    }
    if (node && node.absoluteRenderBounds) {
      return node.absoluteRenderBounds;
    }
    if (
      node &&
      typeof node.x === "number" &&
      typeof node.y === "number" &&
      typeof node.width === "number" &&
      typeof node.height === "number"
    ) {
      return {
        x: node.x,
        y: node.y,
        width: node.width,
        height: node.height,
      };
    }
    return null;
  }

  function cloneColor(color) {
    return {
      r: Number(color.r) || 0,
      g: Number(color.g) || 0,
      b: Number(color.b) || 0,
    };
  }

  function clonePlainObject(value) {
    if (!value || typeof value !== "object") {
      return value;
    }
    if (Array.isArray(value)) {
      return value.map((item) => clonePlainObject(item));
    }
    const clone = {};
    Object.keys(value).forEach((key) => {
      clone[key] = clonePlainObject(value[key]);
    });
    return clone;
  }

  function rgbToHex(color) {
    const red = toHexChannel(color.r);
    const green = toHexChannel(color.g);
    const blue = toHexChannel(color.b);
    return `#${red}${green}${blue}`;
  }

  function toHexChannel(value) {
    const channel = Math.max(0, Math.min(255, Math.round(value * 255)));
    return channel.toString(16).padStart(2, "0").toUpperCase();
  }

  function roundValue(value) {
    return Math.round(value * 100) / 100;
  }

  function roundPixel(value) {
    return Math.round(value);
  }

  async function loadFontsForTextNode(node) {
    if (!node || node.type !== "TEXT") {
      return;
    }
    const fontNames = collectEditableFontNames(node);
    for (const fontName of fontNames) {
      await figma.loadFontAsync(fontName);
    }
  }

  function collectEditableFontNames(node) {
    const fontNames = [];
    const seen = new Set();
    const pushFont = (fontName) => {
      if (!fontName || typeof fontName !== "object" || typeof fontName.family !== "string" || typeof fontName.style !== "string") {
        return;
      }
      const key = `${fontName.family}::${fontName.style}`;
      if (seen.has(key)) {
        return;
      }
      seen.add(key);
      fontNames.push({
        family: fontName.family,
        style: fontName.style,
      });
    };

    if (typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string" && node.characters.length > 0) {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        for (const fontName of rangeFonts) {
          pushFont(fontName);
        }
      } catch (error) {}
    }

    if (fontNames.length === 0 && node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      pushFont(node.fontName);
    }

    return fontNames;
  }

  function createIssueId(type, nodeId) {
    return `${type}:${nodeId}`;
  }

  function formatProviderLabel(provider) {
    switch (String(provider || "").toLowerCase()) {
      case "openai":
        return "OpenAI";
      case "gemini":
        return "Gemini";
      default:
        return provider ? String(provider) : "";
    }
  }

  function formatNodeType(type) {
    switch (String(type || "").toUpperCase()) {
      case "FRAME":
        return "프레임";
      case "GROUP":
        return "그룹";
      case "TEXT":
        return "텍스트";
      case "SECTION":
        return "섹션";
      case "COMPONENT":
        return "컴포넌트";
      case "COMPONENT_SET":
        return "컴포넌트 세트";
      case "INSTANCE":
        return "인스턴스";
      case "VECTOR":
        return "벡터";
      case "BOOLEAN_OPERATION":
        return "불리언";
      case "RECTANGLE":
        return "사각형";
      case "ELLIPSE":
        return "원형";
      default:
        return type;
    }
  }

  function normalizeErrorMessage(error, fallback) {
    if (error instanceof Error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_TYPO_AUDIT_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const AI_DESIGN_READ_CACHE_KEY = "pigma:ai-design-read-cache:v1";
  const AI_TYPO_AUDIT_CACHE_KEY = "pigma:ai-typo-audit-cache:v2";
  const AI_TYPO_FIX_CACHE_KEY = "pigma:ai-typo-fix-cache:v2";
  const AI_TYPO_CLEAR_CACHE_KEY = "pigma:ai-typo-clear-cache:v1";
  const AI_TRANSLATE_CACHE_KEY = "pigma:ai-translate-cache:v1";
  const AI_TRANSLATE_MEMORY_KEY = "pigma:ai-translate-memory:v1";
  const AI_TEXT_HIGHLIGHT_DEFAULT_COLOR = "#F5FF74";
  const AI_TEXT_HIGHLIGHT_DEFAULT_TEXT_COLOR = "#111111";
  const AI_TEXT_HIGHLIGHT_DEFAULT_RADIUS = 0;
  const AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE = 3;
  const AI_TEXT_HIGHLIGHT_DEFAULT_BOX_PADDING_PX = 0;
  const AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_MIN_PX = 2;
  const AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_MAX_PX = 6;
  const AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_RATIO = 0.04;
  const AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_MIN_PX = 3;
  const AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_MAX_PX = 10;
  const AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_RATIO = 0.06;
  const AI_TEXT_HIGHLIGHT_MICRO_FONT_FULL_TIGHTEN_PX = 12;
  const AI_TEXT_HIGHLIGHT_MICRO_FONT_NO_TIGHTEN_PX = 16;
  const AI_TEXT_HIGHLIGHT_SMALL_FONT_FULL_TIGHTEN_PX = 32;
  const AI_TEXT_HIGHLIGHT_SMALL_FONT_NO_TIGHTEN_PX = 96;
  const AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS = 0;
  const AI_TEXT_HIGHLIGHT_MEASURE_COLOR = "#FF00FF";
  const AI_TEXT_HIGHLIGHT_GROUP_NAME = "#high-light-text";
  const AI_TEXT_HIGHLIGHT_GROUP_PLUGIN_KEY = "pigma:text-highlight-group";
  const AI_TEXT_HIGHLIGHT_GROUP_TEXT_NODE_KEY = "pigma:text-highlight-text-node-id";
  const AI_TEXT_HIGHLIGHT_GROUP_WIDTH_KEY = "pigma:text-highlight-container-width";
  const AI_TEXT_HIGHLIGHT_GROUP_HEIGHT_KEY = "pigma:text-highlight-container-height";
  const PATCH_VERSION = 13;
  const TYPO_AUDIT_MODEL_BY_PROVIDER = Object.freeze({
    openai: "gpt-5-mini",
    gemini: "gemini-2.5-pro",
  });
  const AI_TRANSLATE_MODEL_BY_PROVIDER = Object.freeze({
    openai: "gpt-4.1-mini",
    gemini: "gemini-2.5-flash",
  });
  const AI_TRANSLATE_MAX_CHUNK_ITEMS = 24;
  const AI_TRANSLATE_MAX_CHUNK_CHARS = 3600;
  const AI_TRANSLATE_MEMORY_LIMIT = 400;
  const AI_TRANSLATE_PREVIEW_LIMIT = 120;
  const loadedFontPromiseCache = new Map();
  const pendingTextHighlightMeasureRequests = new Map();
  let textHighlightMeasureRequestSequence = 0;
  let pendingTranslatePreviewState = null;
  const ANNOTATION_PREFIX = "[Ai 판단]";
  const LEGACY_ANNOTATION_PREFIXES = ["[AI Typo]", ANNOTATION_PREFIX, "[Pigma Ai Audit]"];
  const ANNOTATION_CATEGORY_LABEL = "Pigma Ai Audit";
  const ANNOTATION_CATEGORY_COLOR = "yellow";
  const TRANSLATION_LANGUAGE_METADATA = Object.freeze({
    "en-US": { label: "영어 (미국)", aiLabel: "English (United States)", latinLocaleHint: "english" },
    "ja-JP": { label: "일본어 (일본)", aiLabel: "Japanese (Japan)", latinLocaleHint: "" },
    "zh-CN": { label: "중국어 간체 (중국)", aiLabel: "Chinese Simplified (China)", latinLocaleHint: "" },
    "ko-KR": { label: "한국어 (대한민국)", aiLabel: "Korean (South Korea)", latinLocaleHint: "" },
    "es-ES": { label: "스페인어 (스페인)", aiLabel: "Spanish (Spain)", latinLocaleHint: "spanish" },
    "fr-FR": { label: "프랑스어 (프랑스)", aiLabel: "French (France)", latinLocaleHint: "french" },
    "de-DE": { label: "독일어 (독일)", aiLabel: "German (Germany)", latinLocaleHint: "german" },
    "pt-BR": { label: "포르투갈어 (브라질)", aiLabel: "Portuguese (Brazil)", latinLocaleHint: "portuguese" },
  });
  const TYPO_REPLACEMENT_RULES = [
    {
      id: "bangapseubnida-direct",
      pattern: /방\s*갑\s*스\s*비\s*난[.!?]?/g,
      replacement: "반갑습니다.",
      reason: "문장 전체가 '반갑습니다.'의 오타로 보여 직접 교정 후보로 표시했습니다.",
      kind: "오타",
    },
    {
      id: "bangapseumnida",
      pattern: /방\s*갑\s*습\s*니\s*다[.!?]?/g,
      replacement: "반갑습니다.",
      reason: "'반갑습니다.' 표현의 대표 오타입니다.",
      kind: "오타",
    },
    {
      id: "login-spacing",
      pattern: /로그\s+인/g,
      replacement: "로그인",
      reason: "UI 용어는 '로그인'을 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "signup-spacing",
      pattern: /회원\s+가입/g,
      replacement: "회원가입",
      reason: "UI 용어는 '회원가입'을 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "password-spacing",
      pattern: /비밀\s+번호/g,
      replacement: "비밀번호",
      reason: "UI 용어는 '비밀번호'를 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "download-spacing",
      pattern: /다운\s+로드/g,
      replacement: "다운로드",
      reason: "UI 용어는 '다운로드'를 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "mypage-spacing",
      pattern: /마이\s+페이지/g,
      replacement: "마이페이지",
      reason: "서비스 메뉴 명칭은 '마이페이지'처럼 붙여 쓰는 경우가 많습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "cart-spacing",
      pattern: /장바\s+구니/g,
      replacement: "장바구니",
      reason: "커머스 UI 용어는 '장바구니'를 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
    {
      id: "id-spacing",
      pattern: /아이\s+디/g,
      replacement: "아이디",
      reason: "계정 식별자 표기는 '아이디'를 붙여 쓰는 편이 자연스럽습니다.",
      kind: "띄어쓰기",
    },
  ];
  const CONSISTENCY_RULES = [
    {
      id: "email-term",
      canonical: "이메일",
      variants: ["이메일", "이 메일", "e-mail", "email"],
      reason: "선택 내부에서 이메일 표기를 한 가지 방식으로 통일하면 읽기가 쉬워집니다.",
    },
    {
      id: "login-term",
      canonical: "로그인",
      variants: ["로그인", "로그 인"],
      reason: "선택 내부에서 로그인 표기를 한 가지 방식으로 통일하면 읽기가 쉬워집니다.",
    },
    {
      id: "signup-term",
      canonical: "회원가입",
      variants: ["회원가입", "회원 가입"],
      reason: "선택 내부에서 회원가입 표기를 한 가지 방식으로 통일하면 읽기가 쉬워집니다.",
    },
    {
      id: "email-term-en",
      canonical: "email",
      variants: ["email", "e-mail", "Email", "E-mail"],
      reason: "Use one email term consistently across the same UI.",
    },
    {
      id: "favorite-term-en",
      canonical: "favorite",
      variants: ["favorite", "favourite", "Favorite", "Favourite"],
      reason: "Use one English locale spelling consistently across the same UI.",
    },
  ];
  const TYPO_PHRASE_HINTS = [
    {
      canonical: "반갑습니다",
      suggestion: "반갑습니다.",
      maxDistance: 4,
      reason: "문장 전체가 '반갑습니다.'의 오타로 보여 교정 후보로 표시했습니다.",
      kind: "오타",
    },
  ];
  const LATIN_UI_TOKEN_HINTS_BY_LOCALE = Object.freeze({
    english: Object.freeze([
      "welcome",
      "continue",
      "cancel",
      "confirm",
      "search",
      "settings",
      "profile",
      "account",
      "password",
      "email",
      "username",
      "download",
      "upload",
      "favorite",
      "favourite",
      "notification",
      "privacy",
      "security",
      "submit",
      "message",
      "success",
      "error",
      "retry",
      "delete",
      "create",
      "update",
      "save",
      "close",
      "open",
      "language",
      "address",
      "country",
      "payment",
      "required",
      "optional",
      "optimal",
      "remember",
      "forgot",
      "subscribe",
      "analytics",
      "dashboard",
      "support",
      "help",
      "find",
      "monthly",
      "login",
      "logout",
      "signin",
      "signup",
    ]),
    spanish: Object.freeze([
      "bienvenido",
      "continuar",
      "cancelar",
      "confirmar",
      "buscar",
      "correo",
      "usuario",
      "configuracion",
      "configuración",
      "perfil",
      "contrasena",
      "contraseña",
      "guardar",
      "cerrar",
    ]),
    french: Object.freeze([
      "bonjour",
      "continuer",
      "annuler",
      "rechercher",
      "parametres",
      "paramètres",
      "profil",
      "enregistrer",
      "fermer",
    ]),
    german: Object.freeze([
      "hallo",
      "weiter",
      "abbrechen",
      "bestatigen",
      "bestätigen",
      "einstellungen",
      "konto",
      "passwort",
      "finde",
      "deine",
      "perfekte",
      "lösungen",
      "loesungen",
      "bessere",
      "balance",
      "angebot",
      "solange",
      "vorrat",
      "reicht",
      "jetzt",
      "kaufen",
      "februar",
      "monatlich",
      "monatliche",
      "speichern",
      "schliessen",
      "schließen",
    ]),
    portuguese: Object.freeze([
      "olá",
      "ola",
      "pesquisar",
      "configuracoes",
      "configurações",
      "senha",
      "endereco",
      "endereço",
      "fechar",
      "salvar",
    ]),
    italian: Object.freeze([
      "ciao",
      "continua",
      "annulla",
      "conferma",
      "ricerca",
      "impostazioni",
      "profilo",
      "salva",
      "chiudi",
    ]),
    dutch: Object.freeze([
      "welkom",
      "doorgaan",
      "annuleren",
      "zoeken",
      "instellingen",
      "profiel",
      "opslaan",
      "sluiten",
    ]),
  });
  const LATIN_UI_TOKEN_HINTS = Object.freeze(
    []
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.english)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.spanish)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.french)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.german)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.portuguese)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.italian)
      .concat(LATIN_UI_TOKEN_HINTS_BY_LOCALE.dutch)
  );
  const LATIN_UI_TOKEN_SET = new Set(LATIN_UI_TOKEN_HINTS.map((token) => normalizeLatinTokenForLookup(token)).filter(Boolean));
  const LATIN_COMMON_SHORT_WORDS = new Set([
    "a",
    "an",
    "as",
    "at",
    "be",
    "by",
    "do",
    "go",
    "he",
    "i",
    "if",
    "in",
    "is",
    "it",
    "me",
    "my",
    "no",
    "of",
    "on",
    "or",
    "so",
    "to",
    "up",
    "us",
    "we",
  ]);
  const PROOFING_LOCALE_METADATA = Object.freeze({
    "cs-CZ": { label: "체코어 (체코)", aiLabel: "Czech (Czech Republic)", latinLocaleHint: "" },
    "da-DK": { label: "덴마크어 (덴마크)", aiLabel: "Danish (Denmark)", latinLocaleHint: "" },
    "de-DE": { label: "독일어 (독일)", aiLabel: "German (Germany)", latinLocaleHint: "german" },
    "el-GR": { label: "그리스어 (그리스)", aiLabel: "Greek (Greece)", latinLocaleHint: "" },
    "en-GB": { label: "영어 (영국)", aiLabel: "English (United Kingdom)", latinLocaleHint: "english" },
    "en-GY": { label: "영어 (가이아나)", aiLabel: "English (Guyana)", latinLocaleHint: "english" },
    "en-IE": { label: "영어 (아일랜드)", aiLabel: "English (Ireland)", latinLocaleHint: "english" },
    "es-AR": { label: "스페인어 (아르헨티나)", aiLabel: "Spanish (Argentina)", latinLocaleHint: "spanish" },
    "es-BO": { label: "스페인어 (볼리비아)", aiLabel: "Spanish (Bolivia)", latinLocaleHint: "spanish" },
    "es-CL": { label: "스페인어 (칠레)", aiLabel: "Spanish (Chile)", latinLocaleHint: "spanish" },
    "es-CO": { label: "스페인어 (콜롬비아)", aiLabel: "Spanish (Colombia)", latinLocaleHint: "spanish" },
    "es-EC": { label: "스페인어 (에콰도르)", aiLabel: "Spanish (Ecuador)", latinLocaleHint: "spanish" },
    "es-ES": { label: "스페인어 (스페인)", aiLabel: "Spanish (Spain)", latinLocaleHint: "spanish" },
    "es-PE": { label: "스페인어 (페루)", aiLabel: "Spanish (Peru)", latinLocaleHint: "spanish" },
    "es-PY": { label: "스페인어 (파라과이)", aiLabel: "Spanish (Paraguay)", latinLocaleHint: "spanish" },
    "es-UY": { label: "스페인어 (우루과이)", aiLabel: "Spanish (Uruguay)", latinLocaleHint: "spanish" },
    "es-VE": { label: "스페인어 (베네수엘라)", aiLabel: "Spanish (Venezuela)", latinLocaleHint: "spanish" },
    "fi-FI": { label: "핀란드어 (핀란드)", aiLabel: "Finnish (Finland)", latinLocaleHint: "" },
    "fr-FR": { label: "프랑스어 (프랑스)", aiLabel: "French (France)", latinLocaleHint: "french" },
    "fr-GF": { label: "프랑스어 (프랑스령 기아나)", aiLabel: "French (French Guiana)", latinLocaleHint: "french" },
    "hu-HU": { label: "헝가리어 (헝가리)", aiLabel: "Hungarian (Hungary)", latinLocaleHint: "" },
    "it-IT": { label: "이탈리아어 (이탈리아)", aiLabel: "Italian (Italy)", latinLocaleHint: "italian" },
    "nl-NL": { label: "네덜란드어 (네덜란드)", aiLabel: "Dutch (Netherlands)", latinLocaleHint: "dutch" },
    "nl-SR": { label: "네덜란드어 (수리남)", aiLabel: "Dutch (Suriname)", latinLocaleHint: "dutch" },
    "no-NO": { label: "노르웨이어 (노르웨이)", aiLabel: "Norwegian (Norway)", latinLocaleHint: "" },
    "pl-PL": { label: "폴란드어 (폴란드)", aiLabel: "Polish (Poland)", latinLocaleHint: "" },
    "pt-BR": { label: "포르투갈어 (브라질)", aiLabel: "Portuguese (Brazil)", latinLocaleHint: "portuguese" },
    "pt-PT": { label: "포르투갈어 (포르투갈)", aiLabel: "Portuguese (Portugal)", latinLocaleHint: "portuguese" },
    "ro-RO": { label: "루마니아어 (루마니아)", aiLabel: "Romanian (Romania)", latinLocaleHint: "" },
    "sk-SK": { label: "슬로바키아어 (슬로바키아)", aiLabel: "Slovak (Slovakia)", latinLocaleHint: "" },
    "sv-SE": { label: "스웨덴어 (스웨덴)", aiLabel: "Swedish (Sweden)", latinLocaleHint: "" },
    "tr-TR": { label: "터키어 (튀르키예)", aiLabel: "Turkish (Turkey)", latinLocaleHint: "" },
    "uk-UA": { label: "우크라이나어 (우크라이나)", aiLabel: "Ukrainian (Ukraine)", latinLocaleHint: "" },
  });
  let activeTypoTask = "";
  let queuedTypoTask = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isAiTypoMessage(message)) {
      if (message.type === "request-ai-typo-audit-cache") {
        await postCachedTypoAuditResult();
        return;
      }

      if (message.type === "request-ai-typo-fix-cache") {
        await postCachedTypoFixResult();
        return;
      }

      if (message.type === "request-ai-typo-clear-cache") {
        await postCachedTypoClearResult();
        return;
      }

      if (message.type === "request-ai-translate-cache") {
        await postCachedTranslateResult();
        return;
      }

      if (message.type === "request-ai-text-highlight-source") {
        await postTextHighlightSource(message);
        return;
      }

      if (message.type === "measure-ai-text-highlight-alpha-bounds-result") {
        resolveTextHighlightAlphaBounds(message);
        return;
      }

      if (message.type === "run-ai-typo-fix") {
        await withTypoTaskLock("fix", runTypoFix);
        return;
      }

      if (message.type === "run-ai-typo-clear") {
        await withTypoTaskLock("clear", runTypoClear);
        return;
      }

      if (message.type === "run-ai-translate") {
        await withTypoTaskLock("translate", async () => {
          await runAiTranslate(message);
        });
        return;
      }

      if (message.type === "apply-ai-translate-preview") {
        await withTypoTaskLock("translate", async () => {
          await runApplyAiTranslatePreview(message);
        });
        return;
      }

      if (message.type === "apply-ai-text-highlight") {
        await withTypoTaskLock("highlight", async () => {
          await runTextHighlight(message);
        });
        return;
      }

      if (message.type === "run-ai-typo-audit") {
        await withTypoTaskLock("audit", async () => {
          await runTypoAudit(message);
        });
        return;
      }

      return;
    }
    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_TYPO_AUDIT_PATCH__ = true;

  function isAiTypoMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-typo-audit-cache" ||
        message.type === "run-ai-typo-audit" ||
        message.type === "request-ai-typo-fix-cache" ||
        message.type === "run-ai-typo-fix" ||
        message.type === "request-ai-typo-clear-cache" ||
        message.type === "run-ai-typo-clear" ||
        message.type === "request-ai-translate-cache" ||
        message.type === "run-ai-translate" ||
        message.type === "apply-ai-translate-preview" ||
        message.type === "request-ai-text-highlight-source" ||
        message.type === "apply-ai-text-highlight" ||
        message.type === "measure-ai-text-highlight-alpha-bounds-result")
    );
  }

  async function withTypoTaskLock(task, runner) {
    const selectionSnapshot = createTypoTaskSelectionSnapshot();

    if (activeTypoTask) {
      if (activeTypoTask === task) {
        postTypoTaskStatus(task, "running", getTypoTaskRunningMessage(task));
      } else if (!queuedTypoTask) {
        queuedTypoTask = {
          task,
          runner,
          selectionSnapshot,
        };
        postTypoTaskStatus(task, "queued", getTypoTaskQueuedMessage(task, activeTypoTask));
      } else if (queuedTypoTask.task === task) {
        postTypoTaskStatus(task, "queued", getTypoTaskQueuedMessage(task, activeTypoTask));
      } else {
        postTypoTaskError(
          task,
          "다른 텍스트 작업이 이미 대기 중입니다. 현재 작업과 대기 작업이 끝난 뒤 다시 실행해 주세요."
        );
      }
      return false;
    }

    await runTypoTaskWithSnapshot(task, runner, selectionSnapshot);
    return true;
  }

  async function runTypoTaskWithSnapshot(task, runner, selectionSnapshot) {
    activeTypoTask = task;
    try {
      if (!matchesTypoTaskSelectionSnapshot(selectionSnapshot)) {
        postTypoTaskError(
          task,
          "대기 중 선택 영역이나 텍스트가 바뀌어서 작업을 건너뛰었습니다. 현재 선택으로 다시 실행해 주세요."
        );
        return false;
      }

      await runner();
      return true;
    } catch (error) {
      postTypoTaskError(task, normalizeErrorMessage(error, "텍스트 작업을 완료하지 못했습니다."));
      return false;
    } finally {
      activeTypoTask = "";
      const nextQueuedTask = queuedTypoTask;
      queuedTypoTask = null;
      if (nextQueuedTask) {
        Promise.resolve().then(() => {
          runTypoTaskWithSnapshot(
            nextQueuedTask.task,
            nextQueuedTask.runner,
            nextQueuedTask.selectionSnapshot
          );
        });
      }
    }
  }

  function createTypoTaskSelectionSnapshot() {
    const selection = Array.from(figma.currentPage.selection || []);
    const selectedTextRange = getSelectedTextRangeSnapshot();
    return {
      selectionSignature: getSelectionSignature(selection),
      textContentSignature: getSelectionTextContentSignature(selection),
      selectedTextRangeSignature: selectedTextRange ? selectedTextRange.signature : "",
    };
  }

  function matchesTypoTaskSelectionSnapshot(snapshot) {
    if (!snapshot) {
      return true;
    }

    const current = createTypoTaskSelectionSnapshot();
    return (
      current.selectionSignature === snapshot.selectionSignature &&
      current.textContentSignature === snapshot.textContentSignature &&
      current.selectedTextRangeSignature === snapshot.selectedTextRangeSignature
    );
  }

  function getSelectedTextRangeSnapshot() {
    const page = figma.currentPage;
    const selectedTextRange = page && "selectedTextRange" in page ? page.selectedTextRange : null;
    if (!selectedTextRange || !selectedTextRange.node || selectedTextRange.node.removed || selectedTextRange.node.type !== "TEXT") {
      return null;
    }

    const characters = typeof selectedTextRange.node.characters === "string" ? selectedTextRange.node.characters : "";
    const start = Math.max(0, Math.floor(Number(selectedTextRange.start) || 0));
    const end = Math.max(start, Math.min(characters.length, Math.floor(Number(selectedTextRange.end) || 0)));
    const text = normalizeLineEndings(characters.slice(start, end));
    return {
      signature: [selectedTextRange.node.id, start, end, hashTypoTaskSnapshotText(text)].join(":"),
    };
  }

  function getSelectionTextContentSignature(selection) {
    const textNodes = collectTextNodes(selection, { includeHidden: true, includeLocked: true });
    return textNodes
      .map((node) => `${node.id}:${hashTypoTaskSnapshotText(getTextValue(node))}`)
      .sort()
      .join("|");
  }

  function hashTypoTaskSnapshotText(value) {
    const text = normalizeLineEndings(value);
    let hash = 2166136261;
    for (let index = 0; index < text.length; index += 1) {
      hash ^= text.charCodeAt(index);
      hash = Math.imul(hash, 16777619);
    }
    return `${text.length}:${(hash >>> 0).toString(36)}`;
  }

  function getTypoTaskRunningMessage(task) {
    if (task === "highlight") {
      return "선택한 텍스트에 하이라이트를 적용하는 중입니다.";
    }
    if (task === "fix") {
      return "오타 후보를 찾아 현재 선택의 텍스트를 직접 수정하고, 고친 부분에는 주석을 남기는 중입니다.";
    }
    if (task === "clear") {
      return "현재 선택 범위에 남아 있는 AI 오타 주석을 정리하는 중입니다.";
    }
    if (task === "translate") {
      return "선택한 화면의 텍스트를 번역하고 있습니다. 선택이 없으면 전체 페이지 번역을 실행하지 않습니다.";
    }
    return "오타 후보를 찾고 Dev Mode 주석 또는 결과 패널로 정리하는 중입니다.";
  }

  function getTypoTaskQueuedMessage(task, activeTask) {
    return `${getTypoTaskLabel(activeTask)} 작업이 끝나면 ${getTypoTaskLabel(task)} 작업을 이어서 실행합니다. 선택이 바뀌면 자동 실행하지 않습니다.`;
  }

  function getTypoTaskLabel(task) {
    if (task === "highlight") {
      return "텍스트 하이라이트";
    }
    if (task === "fix") {
      return "오타 자동 수정";
    }
    if (task === "clear") {
      return "오타 주석 정리";
    }
    if (task === "translate") {
      return "번역";
    }
    return "오타 검사";
  }

  function postTypoTaskStatus(task, status, message) {
    if (task === "highlight") {
      postTextHighlightStatus(status, message);
      return;
    }
    if (task === "fix") {
      postFixStatus(status, message);
      return;
    }
    if (task === "clear") {
      postClearStatus(status, message);
      return;
    }
    if (task === "translate") {
      postTranslateStatus(status, message);
      return;
    }
    postStatus(status, message);
  }

  function postTypoTaskError(task, message) {
    if (task === "highlight") {
      figma.ui.postMessage({
        type: "ai-text-highlight-error",
        message,
      });
      return;
    }
    if (task === "fix") {
      figma.ui.postMessage({
        type: "ai-typo-fix-error",
        message,
      });
      return;
    }
    if (task === "clear") {
      figma.ui.postMessage({
        type: "ai-typo-clear-error",
        message,
      });
      return;
    }
    if (task === "translate") {
      figma.ui.postMessage({
        type: "ai-translate-error",
        message,
      });
      return;
    }
    figma.ui.postMessage({
      type: "ai-typo-audit-error",
      message,
    });
  }

  function normalizeTypoAuditProfile(value) {
    return value === "speed" || value === "quality" ? value : "quality";
  }

  function getTypoAuditProfileLabel(profile) {
    return normalizeTypoAuditProfile(profile) === "speed" ? "속도용" : "품질용";
  }

  function getTypoAuditProfileStrategyLabel(profile, strategy) {
    const normalizedProfile = normalizeTypoAuditProfile(profile);
    if (normalizedProfile === "speed") {
      return "속도용 로컬 검수";
    }
    return strategy === "ai-primary" ? "품질용 AI 우선 + 로컬 보완" : "품질용 로컬 fallback";
  }

  async function runTypoAudit(options) {
    const auditProfile = normalizeTypoAuditProfile(options && options.auditProfile);
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postStatus(
      "running",
      `${getTypoAuditProfileLabel(auditProfile)} 오타 후보를 찾고 Dev Mode 주석 또는 결과 패널로 정리하는 중입니다.`
    );

    try {
      const designReadResult = await readDesignReadCache();
      const result = await applyTypoAudit(designReadResult, { auditProfile });
      await writeTypoAuditCache(result);

      figma.ui.postMessage({
        type: "ai-typo-audit-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      figma.notify(
        result.summary && result.summary.mode === "figma-dev-annotation"
          ? `오타 검수 완료 (${result.summary.annotationCount}건 주석, Dev Mode에서 확인)`
          : `오타 검수 완료 (${result.summary.issueCount}건 후보, 결과 패널 확인)`,
        { timeout: 2200 }
      );
    } catch (error) {
      const message = normalizeErrorMessage(error, "오타 검수에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-typo-audit-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function runTypoFix() {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postFixStatus("running", "오타 후보를 찾아 현재 선택의 텍스트를 직접 수정하고, 고친 부분에는 주석을 남기는 중입니다.");

    try {
      const designReadResult = await readDesignReadCache();
      const result = await applyTypoFix(designReadResult);
      await writeTypoFixCache(result);

      figma.ui.postMessage({
        type: "ai-typo-fix-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      figma.notify(
        result.summary && result.summary.appliedCount > 0
          ? `오타 자동 수정 완료 (${result.summary.appliedCount}개 텍스트 수정, ${result.summary.annotationCount || 0}건 주석)`
          : `오타 자동 수정 완료 (${result.summary.issueCount}건 후보, 직접 수정 없음)`,
        { timeout: 2200 }
      );
    } catch (error) {
      const message = normalizeErrorMessage(error, "오타 자동 수정에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-typo-fix-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function runTypoClear() {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postClearStatus("running", "현재 선택 범위에 남아 있는 AI 오타 주석을 정리하는 중입니다.");

    try {
      const designReadResult = await readDesignReadCache();
      const result = await clearManagedTypoAnnotations(designReadResult);
      await writeTypoClearCache(result);

      figma.ui.postMessage({
        type: "ai-typo-clear-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      figma.notify(`오타 주석 정리 완료 (${result.summary.removedAnnotationCount || 0}건 제거)`, {
        timeout: 2200,
      });
    } catch (error) {
      const message = normalizeErrorMessage(error, "오타 주석 정리에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-typo-clear-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function runAiTranslate(message) {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    const targetLanguage = getTranslationLanguageMetadata(message && message.targetLanguage);
    postTranslateStatus("running", `${targetLanguage.label}로 번역하는 중입니다.`);

    try {
      const designReadResult = await readDesignReadCache();
      const result = await applyAiTranslation(designReadResult, targetLanguage);

      figma.ui.postMessage({
        type: "ai-translate-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      figma.notify(
        result.summary && result.summary.translatedCount > 0
          ? `번역 적용 완료 (${result.summary.translatedCount}개 텍스트, ${result.summary.targetLanguageLabel})`
          : `번역 완료 (${result.summary ? result.summary.textNodeCount || 0 : 0}개 확인, 변경 없음)`,
        { timeout: 2200 }
      );
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "AI 번역에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-translate-error",
        message: messageText,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  async function runApplyAiTranslatePreview(message) {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postTranslateStatus("running", "선택한 번역 제안을 적용하는 중입니다.");

    try {
      const selectedIds = normalizeTranslatePreviewSelection(message && message.selectedIds);
      if (!selectedIds.length) {
        throw new Error("적용할 번역 제안을 하나 이상 선택해주세요.");
      }

      const result = await applyAiTranslationPreviewSelection(selectedIds);
      await writeTranslateCache(result);

      figma.ui.postMessage({
        type: "ai-translate-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      figma.notify(
        result.summary && result.summary.translatedCount > 0
          ? `번역 적용 완료 (${result.summary.translatedCount}개 텍스트)`
          : "적용할 번역 변경이 없습니다.",
        { timeout: 2200 }
      );
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "번역 제안을 적용하지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-translate-error",
        message: messageText,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  async function postTextHighlightSource(message) {
    const requestId = message && typeof message.requestId === "string" ? message.requestId : "";
    try {
      const source = prepareTextHighlightSource();
      figma.ui.postMessage({
        type: "ai-text-highlight-source-ready",
        requestId,
        source,
      });
    } catch (error) {
      figma.ui.postMessage({
        type: "ai-text-highlight-source-error",
        requestId,
        message: normalizeErrorMessage(error, "드래그한 텍스트 범위를 먼저 선택해 주세요."),
      });
    }
  }

  function resolveTextHighlightMode(message) {
    const rawMode = message && typeof message.highlightMode === "string" ? message.highlightMode.trim().toLowerCase() : "";
    if (rawMode === "line" || rawMode === "strike") {
      return rawMode;
    }
    return "box";
  }

  async function runTextHighlight(message) {
    const highlightMode = resolveTextHighlightMode(message);
    if (highlightMode === "line") {
      await runTextHighlightLine(message);
      return;
    }
    if (highlightMode === "strike") {
      await runTextHighlightStrike(message);
      return;
    }
    await runTextHighlightBox(message);
  }

  async function runTextHighlightBox(message) {
    try {
      const range = await resolveTextHighlightRange(message);
      const parent = range.node.parent;
      const colorSnapshot = extractTextRangeColorSnapshot(range.node, range.start, range.end);
      const highlightColorHex = sanitizeHexColor(message && message.highlightColorHex, AI_TEXT_HIGHLIGHT_DEFAULT_COLOR);
      const textColorHex = sanitizeHexColor(message && message.textColorHex, colorSnapshot.hex);
      const shouldApplyTextColor = shouldApplyTextHighlightColor(message);
      const cornerRadius = sanitizeTextHighlightRadius(
        message && message.cornerRadius,
        AI_TEXT_HIGHLIGHT_DEFAULT_RADIUS
      );
      const boxPaddingPx = sanitizeTextHighlightBoxPaddingPx(
        message && message.decorationScale,
        AI_TEXT_HIGHLIGHT_DEFAULT_BOX_PADDING_PX
      );

      if (!parent) {
        throw new Error("하이라이트를 만들 부모 레이어를 찾지 못했습니다.");
      }

      postTextHighlightStatus("running", "텍스트 하이라이트 도형을 만드는 중입니다.");

      const measurement = await measureTextHighlightBounds(range.node, range.start, range.end, textColorHex);
      let boundsList = getTextHighlightMeasurementBoundsList(measurement);
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위를 정확히 측정하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }
      if (hasMergedTextHighlightBlockBounds(boundsList, measurement.fontSize)) {
        boundsList = splitMergedTextHighlightBlockBounds(
          boundsList,
          measurement.fontSize,
          measurement.lineHeight
        );
      }
      boundsList = normalizeTextHighlightBoundsListForApply(
        range.node,
        range.start,
        range.end,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      boundsList = stabilizeTextHighlightBoxBoundsToTypographyRows(
        range.node,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      boundsList = tightenTextHighlightBoxBoundsToVisualRows(
        range.node,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위의 위치를 안전하게 계산하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }

      if (shouldApplyTextColor) {
        await applyTextHighlightColor(range.node, range.start, range.end, textColorHex);
      }

      const container = prepareTextHighlightLayerContainer(parent, range.node);
      const highlightParent = container.parent;
      const nodeIndex = container.nodeIndex;
      const anchorNodeId = container.anchorNodeId || range.node.id;
      const rects = [];
      const boxTargetHeightPx = buildTextHighlightBoxTargetHeight(
        measurement.fontSize,
        measurement.lineHeight,
        boxPaddingPx,
        boundsList
      );
      for (let index = 0; index < boundsList.length; index += 1) {
        const rect = createTextHighlightRect(
          range.node,
          highlightParent,
          boundsList[index],
          measurement.fontSize,
          highlightColorHex,
          cornerRadius,
          boxPaddingPx,
          boxTargetHeightPx
        );
        rect.name = boundsList.length > 1 ? `Highlight ${index + 1}` : "Highlight";
        placeTextHighlightRectBehindNode(highlightParent, rect, anchorNodeId, nodeIndex >= 0 ? nodeIndex + index : -1);
        rects.push(rect);
      }
      finalizeTextHighlightLayerContainer(highlightParent, range.node, container);

      const group = groupTextHighlightSelection(highlightParent, range.node, rects, nodeIndex, container);

      figma.ui.postMessage({
        type: "ai-text-highlight-result",
        result: {
          groupId: group.id,
          groupName: group.name,
          nodeId: range.node.id,
          selectionLabel: safeName(range.node),
          textPreview: compactText(range.text) || previewText(range.text, 72),
          highlightColorHex,
          textColorHex,
          rectCount: rects.length,
          mode: "box",
        },
      });
      figma.notify("박스형 텍스트 하이라이트를 만들었습니다.", { timeout: 2200 });
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "텍스트 하이라이트를 적용하지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-text-highlight-error",
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  async function runTextHighlightLine(message) {
    try {
      const range = await resolveTextHighlightRange(message);
      const parent = range.node.parent;
      const colorSnapshot = extractTextRangeColorSnapshot(range.node, range.start, range.end);
      const fontSize = getTextRangeFontSize(range.node, range.start, range.end);
      const lineHeight = getTextRangeLineHeight(range.node, range.start, range.end, fontSize);
      const highlightColorHex = sanitizeHexColor(message && message.highlightColorHex, AI_TEXT_HIGHLIGHT_DEFAULT_COLOR);
      const textColorHex = sanitizeHexColor(message && message.textColorHex, colorSnapshot.hex);
      const shouldApplyTextColor = shouldApplyTextHighlightColor(message);
      const decorationScale = sanitizeTextHighlightDecorationScale(
        message && message.decorationScale,
        buildTextHighlightAutoDecorationScale(fontSize, "line", lineHeight)
      );
      const lineCapRadius = sanitizeTextHighlightRadius(
        message && message.strikeCapRadius,
        AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS
      );

      if (!parent) {
        throw new Error("하이라이트를 만들 부모 레이어를 찾지 못했습니다.");
      }

      postTextHighlightStatus("running", "라인형 하이라이트를 만드는 중입니다.");

      const measurement = await measureTextHighlightBounds(
        range.node,
        range.start,
        range.end,
        textColorHex,
        { preferAlignmentSensitiveFallback: true }
      );
      let boundsList = getTextHighlightMeasurementBoundsList(measurement);
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위를 정확히 측정하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }
      if (hasMergedTextHighlightBlockBounds(boundsList, measurement.fontSize)) {
        boundsList = splitMergedTextHighlightBlockBounds(
          boundsList,
          measurement.fontSize,
          measurement.lineHeight
        );
      }
      boundsList = normalizeTextHighlightBoundsListForApply(
        range.node,
        range.start,
        range.end,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위의 위치를 안전하게 계산하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }

      clearTextHighlightDecoration(range.node, range.start, range.end);
      if (shouldApplyTextColor) {
        await applyTextHighlightColor(range.node, range.start, range.end, textColorHex);
      }

      const container = prepareTextHighlightLayerContainer(parent, range.node);
      const highlightParent = container.parent;
      const nodeIndex = container.nodeIndex;
      const anchorNodeId = container.anchorNodeId || range.node.id;
      const rects = [];
      boundsList = stabilizeTextHighlightBoxBoundsToTypographyRows(
        range.node,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      const thickness = buildTextHighlightDecorationThickness(measurement.fontSize, "line", decorationScale);
      for (let index = 0; index < boundsList.length; index += 1) {
        const rect = createTextHighlightLineRect(
          range.node,
          highlightParent,
          boundsList[index],
          measurement.fontSize,
          thickness,
          highlightColorHex,
          lineCapRadius
        );
        rect.name = boundsList.length > 1 ? `Line Highlight ${index + 1}` : "Line Highlight";
        placeTextHighlightRectBehindNode(highlightParent, rect, anchorNodeId, nodeIndex >= 0 ? nodeIndex + index : -1);
        rects.push(rect);
      }
      finalizeTextHighlightLayerContainer(highlightParent, range.node, container);

      const group = groupTextHighlightSelection(highlightParent, range.node, rects, nodeIndex, container);

      figma.ui.postMessage({
        type: "ai-text-highlight-result",
        result: {
          groupId: group.id,
          groupName: group.name,
          nodeId: range.node.id,
          selectionLabel: safeName(range.node),
          textPreview: compactText(range.text) || previewText(range.text, 72),
          highlightColorHex,
          textColorHex,
          rectCount: rects.length,
          mode: "line",
        },
      });
      figma.notify("라인형 텍스트 하이라이트를 만들었습니다.", { timeout: 2200 });
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "텍스트 하이라이트를 적용하지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-text-highlight-error",
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  async function runTextHighlightStrike(message) {
    try {
      const range = await resolveTextHighlightRange(message);
      const parent = range.node.parent;
      const colorSnapshot = extractTextRangeColorSnapshot(range.node, range.start, range.end);
      const fontSize = getTextRangeFontSize(range.node, range.start, range.end);
      const lineHeight = getTextRangeLineHeight(range.node, range.start, range.end, fontSize);
      const highlightColorHex = sanitizeHexColor(message && message.highlightColorHex, AI_TEXT_HIGHLIGHT_DEFAULT_COLOR);
      const textColorHex = sanitizeHexColor(message && message.textColorHex, colorSnapshot.hex);
      const shouldApplyTextColor = shouldApplyTextHighlightColor(message);
      const decorationScale = sanitizeTextHighlightDecorationScale(
        message && message.decorationScale,
        buildTextHighlightAutoDecorationScale(fontSize, "strike", lineHeight)
      );
      const strikeCapRadius = sanitizeTextHighlightRadius(
        message && message.strikeCapRadius,
        AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS
      );

      if (!parent) {
        throw new Error("하이라이트를 만들 부모 레이어를 찾지 못했습니다.");
      }

      postTextHighlightStatus("running", "취소선형 하이라이트 라인을 만드는 중입니다.");

      const measurement = await measureTextHighlightBounds(range.node, range.start, range.end, textColorHex);
      let boundsList = getTextHighlightMeasurementBoundsList(measurement);
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위를 정확히 측정하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }
      if (hasMergedTextHighlightBlockBounds(boundsList, measurement.fontSize)) {
        boundsList = splitMergedTextHighlightBlockBounds(
          boundsList,
          measurement.fontSize,
          measurement.lineHeight
        );
      }
      boundsList = normalizeTextHighlightBoundsListForApply(
        range.node,
        range.start,
        range.end,
        boundsList,
        measurement.fontSize,
        measurement.lineHeight
      );
      if (!boundsList.length) {
        throw new Error("선택한 텍스트 범위의 위치를 안전하게 계산하지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
      }

      clearTextHighlightDecoration(range.node, range.start, range.end);
      if (shouldApplyTextColor) {
        await applyTextHighlightColor(range.node, range.start, range.end, textColorHex);
      }

      const container = prepareTextHighlightLayerContainer(parent, range.node);
      const highlightParent = container.parent;
      const nodeIndex = container.nodeIndex;
      const anchorNodeId = container.anchorNodeId || range.node.id;
      const rects = [];
      const thickness = buildTextHighlightDecorationThickness(measurement.fontSize, "strike", decorationScale);
      for (let index = 0; index < boundsList.length; index += 1) {
        const rect = createTextHighlightStrikeRect(
          range.node,
          highlightParent,
          boundsList[index],
          measurement.fontSize,
          thickness,
          highlightColorHex,
          strikeCapRadius
        );
        rect.name = boundsList.length > 1 ? `Strike Highlight ${index + 1}` : "Strike Highlight";
        placeTextHighlightRectBehindNode(highlightParent, rect, anchorNodeId, nodeIndex >= 0 ? nodeIndex + index : -1);
        rects.push(rect);
      }
      finalizeTextHighlightLayerContainer(highlightParent, range.node, container);

      const group = groupTextHighlightSelection(highlightParent, range.node, rects, nodeIndex, container);

      figma.ui.postMessage({
        type: "ai-text-highlight-result",
        result: {
          groupId: group.id,
          groupName: group.name,
          nodeId: range.node.id,
          selectionLabel: safeName(range.node),
          textPreview: compactText(range.text) || previewText(range.text, 72),
          highlightColorHex,
          textColorHex,
          rectCount: rects.length,
          mode: "strike",
        },
      });
      figma.notify("취소선형 텍스트 하이라이트를 만들었습니다.", { timeout: 2200 });
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "텍스트 하이라이트를 적용하지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-text-highlight-error",
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  async function runTextHighlightNative(message, highlightMode) {
    try {
      const resolvedMode = highlightMode === "strike" ? "strike" : "line";
      const range = await resolveTextHighlightRange(message);
      const colorSnapshot = extractTextRangeColorSnapshot(range.node, range.start, range.end);
      const highlightColorHex = sanitizeHexColor(message && message.highlightColorHex, AI_TEXT_HIGHLIGHT_DEFAULT_COLOR);
      const textColorHex = sanitizeHexColor(message && message.textColorHex, colorSnapshot.hex);
      const shouldApplyTextColor = shouldApplyTextHighlightColor(message);
      const decorationScale = sanitizeTextHighlightDecorationScale(
        message && message.decorationScale,
        AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE
      );

      postTextHighlightStatus("running", "텍스트 하이라이트를 적용하는 중입니다.");
      await applyTextHighlightDecoration(range.node, range.start, range.end, highlightColorHex, resolvedMode, decorationScale);
      if (shouldApplyTextColor) {
        await applyTextHighlightColor(range.node, range.start, range.end, textColorHex);
      }
      figma.currentPage.selection = [range.node];
      figma.viewport.scrollAndZoomIntoView([range.node]);

      figma.ui.postMessage({
        type: "ai-text-highlight-result",
        result: {
          groupId: "",
          groupName: "",
          nodeId: range.node.id,
          selectionLabel: safeName(range.node),
          textPreview: compactText(range.text) || previewText(range.text, 72),
          highlightColorHex,
          textColorHex,
          mode: resolvedMode,
        },
      });
      figma.notify(
        resolvedMode === "strike" ? "취소선형 텍스트 하이라이트를 적용했습니다." : "라인형 텍스트 하이라이트를 적용했습니다.",
        { timeout: 2200 }
      );
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "텍스트 하이라이트를 적용하지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-text-highlight-error",
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  function clearTextHighlightDecoration(node, start, end) {
    if (!node || typeof node.setRangeTextDecoration !== "function") {
      return;
    }

    try {
      node.setRangeTextDecoration(start, end, "NONE");
    } catch (error) {}
  }

  async function applyTextHighlightDecoration(node, start, end, highlightColorHex, highlightMode, decorationScale) {
    if (!node || node.type !== "TEXT") {
      throw new Error("텍스트 하이라이트를 적용할 텍스트 레이어를 찾지 못했습니다.");
    }

    if (typeof node.setRangeTextDecoration !== "function") {
      throw new Error("현재 Figma 버전에서는 텍스트 데코 하이라이트를 지원하지 않습니다.");
    }

    try {
      await loadFontsForTextNode(node);
    } catch (error) {}

    const fontSize = getTextRangeFontSize(node, start, end);
    const resolvedMode = highlightMode === "strike" ? "strike" : "line";
    const resolvedScale = sanitizeTextHighlightDecorationScale(decorationScale, AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE);
    const thicknessValue = buildTextHighlightDecorationThickness(fontSize, resolvedMode, resolvedScale);
    const offsetValue = buildTextHighlightDecorationOffset(fontSize, resolvedMode, resolvedScale, thicknessValue);
    const decorationKind = resolvedMode === "strike" ? "STRIKETHROUGH" : "UNDERLINE";

    node.setRangeTextDecoration(start, end, decorationKind);

    if (typeof node.setRangeTextDecorationStyle === "function") {
      try {
        node.setRangeTextDecorationStyle(start, end, "SOLID");
      } catch (error) {}
    }

    if (typeof node.setRangeTextDecorationColor === "function") {
      try {
        node.setRangeTextDecorationColor(start, end, {
          value: createSolidPaint(highlightColorHex),
        });
      } catch (error) {}
    }

    if (typeof node.setRangeTextDecorationSkipInk === "function") {
      try {
        node.setRangeTextDecorationSkipInk(start, end, false);
      } catch (error) {}
    }

    if (typeof node.setRangeTextDecorationThickness === "function") {
      try {
        node.setRangeTextDecorationThickness(start, end, {
          unit: "PIXELS",
          value: thicknessValue,
        });
      } catch (error) {}
    }

    if (typeof node.setRangeTextDecorationOffset === "function") {
      try {
        node.setRangeTextDecorationOffset(start, end, {
          unit: "PIXELS",
          value: offsetValue,
        });
      } catch (error) {}
    }
  }

  function buildTextHighlightDecorationThickness(fontSize, highlightMode, decorationScale) {
    const size = Math.max(12, Number(fontSize) || 16);
    const scale = sanitizeTextHighlightDecorationScale(decorationScale, AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE);
    if (highlightMode === "strike") {
      return roundTextHighlightThickness(Math.max(3, Math.min(size * 1.45, size * 0.22 * scale)));
    }
    return roundTextHighlightThickness(Math.max(2, Math.min(36, size * 0.1 * scale)));
  }

  function buildTextHighlightAutoDecorationScale(fontSize, highlightMode, lineHeight) {
    const size = Math.max(12, Number(fontSize) || 16);
    const mode = highlightMode === "line" ? "line" : "strike";
    const baseThickness = buildTextHighlightDecorationThickness(size, mode, 1);
    if (mode === "strike") {
      const targetThickness = buildTextHighlightMinimumBoxThickness(size, lineHeight);
      return Math.max(1, Math.min(10, Math.ceil(targetThickness / Math.max(1, baseThickness))));
    }

    const targetThickness = Math.max(2, Math.min(14, size * 0.1));
    return sanitizeTextHighlightDecorationScale(
      targetThickness / Math.max(1, baseThickness),
      AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE
    );
  }

  function buildTextHighlightMinimumBoxThickness(fontSize, lineHeight) {
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    return ceilTextHighlightThickness(Math.max(size * 0.78, Math.min(resolvedLineHeight, size)));
  }

  function buildTextHighlightDecorationOffset(fontSize, highlightMode, decorationScale, thicknessValue) {
    const size = Math.max(12, Number(fontSize) || 16);
    if (highlightMode === "strike") {
      return roundTextHighlightMetric(0);
    }

    const scale = sanitizeTextHighlightDecorationScale(decorationScale, AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE);
    const thickness =
      Number(thicknessValue) > 0
        ? Number(thicknessValue)
        : buildTextHighlightDecorationThickness(size, highlightMode, scale);
    const baseThickness = buildTextHighlightDecorationThickness(size, highlightMode, 1);
    const baseCenterOffset = Math.max(3, Math.min(14, size * 0.2));
    const fixedBottomOffset = baseCenterOffset + baseThickness / 2;
    const centeredOffset = fixedBottomOffset - thickness / 2;
    return roundTextHighlightMetric(Math.max(-size * 0.35, Math.min(baseCenterOffset, centeredOffset)));
  }

  function getManagedAnnotationCategoryColor() {
    return ANNOTATION_CATEGORY_COLOR;
  }

  async function postCachedTypoAuditResult() {
    const result = await readTypoAuditCache();
    figma.ui.postMessage({
      type: "ai-typo-audit-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  async function postCachedTypoFixResult() {
    const result = await readTypoFixCache();
    figma.ui.postMessage({
      type: "ai-typo-fix-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  async function postCachedTypoClearResult() {
    const result = await readTypoClearCache();
    figma.ui.postMessage({
      type: "ai-typo-clear-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  async function postCachedTranslateResult() {
    const result = await readTranslateCache();
    figma.ui.postMessage({
      type: "ai-translate-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-typo-audit-status",
      status,
      message,
    });
  }

  function postFixStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-typo-fix-status",
      status,
      message,
    });
  }

  function postClearStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-typo-clear-status",
      status,
      message,
    });
  }

  function postTranslateStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-translate-status",
      status,
      message,
    });
  }

  function postTextHighlightStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-text-highlight-status",
      status,
      message,
    });
  }

  async function readDesignReadCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_DESIGN_READ_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readTypoAuditCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_TYPO_AUDIT_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readTypoFixCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_TYPO_FIX_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readTypoClearCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_TYPO_CLEAR_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readTranslateCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_TRANSLATE_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readTranslateMemory() {
    try {
      return normalizeTranslateMemory(await figma.clientStorage.getAsync(AI_TRANSLATE_MEMORY_KEY));
    } catch (error) {
      return [];
    }
  }

  async function writeTypoAuditCache(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_TYPO_AUDIT_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  async function writeTypoFixCache(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_TYPO_FIX_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  async function writeTypoClearCache(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_TYPO_CLEAR_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  async function writeTranslateCache(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_TRANSLATE_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  async function writeTranslateMemory(entries) {
    const normalized = normalizeTranslateMemory(entries);
    try {
      await figma.clientStorage.setAsync(AI_TRANSLATE_MEMORY_KEY, normalized);
    } catch (error) {}
    return normalized;
  }

  function matchesCurrentSelection(result) {
    return !!result && result.selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function matchesSelectionSignature(selectionSignature) {
    return typeof selectionSignature === "string" && selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  async function readProofingSettings() {
    const ai = getAiHelper();
    if (!ai || typeof ai.getAiSettingsAsync !== "function") {
      return normalizeProofingSettings(null);
    }

    try {
      return normalizeProofingSettings(await ai.getAiSettingsAsync());
    } catch (error) {
      return normalizeProofingSettings(null);
    }
  }

  function normalizeProofingSettings(value) {
    const source = value && typeof value === "object" ? value : {};
    const userDictionary = normalizeTermList(source.userDictionary);
    const protectedTerms = normalizeTermList(source.protectedTerms);
    const proofingLocale = normalizeKnownProofingLocale(source.proofingLocale);
    const localeMeta = getProofingLocaleMetadata(proofingLocale);
    return {
      proofingLocale,
      proofingLocaleLabel: localeMeta ? localeMeta.label : "",
      proofingLocaleAiLabel: localeMeta ? localeMeta.aiLabel : "",
      preferredLatinLocaleHint: localeMeta ? localeMeta.latinLocaleHint : "",
      userDictionary,
      protectedTerms,
      exactBlockedSet: new Set(userDictionary.concat(protectedTerms).map((term) => compactText(term).toLocaleLowerCase())),
      protectedTermsLower: protectedTerms.map((term) => term.toLocaleLowerCase()),
      userDictionaryLower: userDictionary.map((term) => term.toLocaleLowerCase()),
    };
  }

  function normalizeTermList(value) {
    const source = Array.isArray(value)
      ? value
      : typeof value === "string"
        ? value.split(/\r?\n|,/)
        : [];
    const normalized = [];
    const seen = new Set();

    for (const entry of source) {
      const term = String(entry || "").replace(/\s+/g, " ").trim();
      if (!term) {
        continue;
      }

      const key = term.toLocaleLowerCase();
      if (seen.has(key)) {
        continue;
      }

      seen.add(key);
      normalized.push(term);
    }

    return normalized.slice(0, 200);
  }

  function normalizeKnownProofingLocale(value) {
    const compact = String(value || "")
      .replace(/[^A-Za-z-]/g, "")
      .trim();
    if (!compact) {
      return "";
    }

    const parts = compact.split("-").filter(Boolean);
    let normalized = "";
    if (parts.length === 1 && /^[A-Za-z]{2,3}$/.test(parts[0])) {
      normalized = parts[0].toLowerCase();
    } else if (parts.length === 2 && /^[A-Za-z]{2,3}$/.test(parts[0]) && /^[A-Za-z]{2,4}$/.test(parts[1])) {
      normalized = `${parts[0].toLowerCase()}-${parts[1].toUpperCase()}`;
    }

    return normalized && Object.prototype.hasOwnProperty.call(PROOFING_LOCALE_METADATA, normalized) ? normalized : "";
  }

  function getProofingLocaleMetadata(locale) {
    if (!locale || !Object.prototype.hasOwnProperty.call(PROOFING_LOCALE_METADATA, locale)) {
      return null;
    }

    return Object.assign(
      {
        code: locale,
      },
      PROOFING_LOCALE_METADATA[locale]
    );
  }

  function getTranslationLanguageMetadata(code) {
    const normalized = typeof code === "string" ? code.trim() : "";
    const fallback = "en-US";
    const resolved = normalized && Object.prototype.hasOwnProperty.call(TRANSLATION_LANGUAGE_METADATA, normalized) ? normalized : fallback;
    return Object.assign(
      {
        code: resolved,
      },
      TRANSLATION_LANGUAGE_METADATA[resolved]
    );
  }

  function isWholeTextProtected(text, proofingSettings) {
    if (!proofingSettings || !(proofingSettings.exactBlockedSet instanceof Set)) {
      return false;
    }

    const key = compactText(text).toLocaleLowerCase();
    return !!key && proofingSettings.exactBlockedSet.has(key);
  }

  function issueRespectsProofingTerms(currentText, suggestion, proofingSettings) {
    if (!proofingSettings) {
      return true;
    }

    if (isWholeTextProtected(currentText, proofingSettings)) {
      return false;
    }

    const currentValue = normalizeLineEndings(currentText);
    const suggestionValue = normalizeLineEndings(suggestion);
    if (!currentValue || !suggestionValue) {
      return true;
    }

    const guardedTerms = normalizeTermList(
      []
        .concat(proofingSettings.userDictionary || [])
        .concat(proofingSettings.protectedTerms || [])
    );

    for (const term of guardedTerms) {
      const currentExactCount = countTermOccurrences(currentValue, term, false);
      if (currentExactCount > 0) {
        if (countTermOccurrences(suggestionValue, term, false) < currentExactCount) {
          return false;
        }
        continue;
      }

      const currentLooseCount = countTermOccurrences(currentValue, term, true);
      if (currentLooseCount > countTermOccurrences(suggestionValue, term, true)) {
        return false;
      }
    }

    return true;
  }

  function filterIssuesForProofing(issues, proofingSettings) {
    if (!Array.isArray(issues) || !issues.length) {
      return [];
    }

    return issues.filter((issue) => {
      if (!issue || typeof issue !== "object") {
        return false;
      }

      return (
        !isWholeTextProtected(issue.currentText, proofingSettings) &&
        issueRespectsProofingTerms(issue.currentText, issue.suggestion, proofingSettings) &&
        changeMatchesCorrectionPolicy(issue.currentText, issue.suggestion, issue.kind)
      );
    });
  }

  function changeMatchesCorrectionPolicy(currentText, suggestion, kind) {
    const currentValue = normalizeLineEndings(currentText);
    const suggestionValue = normalizeLineEndings(suggestion);
    if (!currentValue || !suggestionValue || currentValue === suggestionValue) {
      return false;
    }

    const kindLabels = getAnnotationKindLabels({
      currentText: currentValue,
      suggestion: suggestionValue,
      kind,
    });
    if (kindLabels.includes("용어 통일")) {
      return false;
    }

    if (countTextLines(currentValue) !== countTextLines(suggestionValue)) {
      return false;
    }

    if (isLowSignalCosmeticCorrection(currentValue, suggestionValue, kindLabels)) {
      return false;
    }

    if (isSpacingOrPunctuationOnlyCorrection(currentValue, suggestionValue)) {
      return looksLikeCriticalBrokenLatinSpacing(currentValue, suggestionValue);
    }

    const currentFamilies = getActiveScriptFamilies(currentValue);
    const suggestionFamilies = getActiveScriptFamilies(suggestionValue);
    if (!areScriptFamiliesCompatible(currentFamilies, suggestionFamilies)) {
      return false;
    }

    return tokensLookLikeMinorCorrections(currentValue, suggestionValue, kindLabels);
  }

  function isLowSignalCosmeticCorrection(currentText, suggestion, kindLabels) {
    const currentValue = normalizeLineEndings(currentText);
    const suggestionValue = normalizeLineEndings(suggestion);
    if (!currentValue || !suggestionValue || currentValue === suggestionValue) {
      return false;
    }

    if (!containsSubstantiveText(currentValue) && !containsSubstantiveText(suggestionValue)) {
      return true;
    }

    if (
      stripCosmeticCharacters(currentValue) === stripCosmeticCharacters(suggestionValue) &&
      !looksLikeCriticalBrokenLatinSpacing(currentValue, suggestionValue)
    ) {
      return true;
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("띄어쓰기") && !looksLikeCriticalBrokenLatinSpacing(currentValue, suggestionValue)) {
      return true;
    }

    return false;
  }

  function isSpacingOrPunctuationOnlyCorrection(currentText, suggestion) {
    const currentTokens = tokenizeScriptTokens(currentText);
    const suggestionTokens = tokenizeScriptTokens(suggestion);
    if (!currentTokens.length || !suggestionTokens.length) {
      return false;
    }

    return flattenComparableTokens(currentTokens) === flattenComparableTokens(suggestionTokens);
  }

  function containsSubstantiveText(value) {
    return /[0-9A-Za-z\u00C0-\u024F\u0400-\u04FF\u0590-\u05FF\u0600-\u06FF\u0900-\u097F\u0E00-\u0E7F\u3040-\u30FF\u3400-\u9FFF\uAC00-\uD7AF]/.test(
      String(value || "")
    );
  }

  function stripCosmeticCharacters(value) {
    return String(value || "")
      .replace(/[\s()[\]{}<>.,;:!?"'`~\-_/\\]+/g, "")
      .trim();
  }

  function looksLikeCriticalBrokenLatinSpacing(currentText, suggestion) {
    const currentValue = normalizeLineEndings(currentText);
    const suggestionValue = normalizeLineEndings(suggestion);
    if (!currentValue || !suggestionValue || currentValue === suggestionValue) {
      return false;
    }

    if (!/\s/.test(currentValue) || /\s/.test(suggestionValue)) {
      return false;
    }

    const currentWords = getLatinWordRuns(currentValue);
    const suggestionWords = getLatinWordRuns(suggestionValue);
    if (currentWords.length < 2 || suggestionWords.length !== 1) {
      return false;
    }

    const joinedCurrent = normalizeLatinTokenForLookup(currentWords.join(""));
    const joinedSuggestion = normalizeLatinTokenForLookup(suggestionWords[0]);
    if (!joinedCurrent || !joinedSuggestion || joinedCurrent !== joinedSuggestion) {
      return false;
    }

    if (joinedSuggestion.length < 5) {
      return false;
    }

    let hasSuspiciousShortFragment = false;
    for (const word of currentWords) {
      if (word.length <= 2) {
        const normalizedWord = normalizeLatinTokenForLookup(word);
        if (normalizedWord && !LATIN_COMMON_SHORT_WORDS.has(normalizedWord)) {
          hasSuspiciousShortFragment = true;
          break;
        }
      }
    }

    if (hasSuspiciousShortFragment) {
      return true;
    }

    return currentWords.length >= 3 && currentWords.some((word) => word.length === 1);
  }

  function getLatinWordRuns(value) {
    const matches = String(value || "").match(/[A-Za-z\u00C0-\u024F]+/g);
    return Array.isArray(matches) ? matches : [];
  }

  function areScriptFamiliesCompatible(currentFamilies, suggestionFamilies) {
    if (!currentFamilies.length || !suggestionFamilies.length) {
      return true;
    }

    for (const family of suggestionFamilies) {
      if (!currentFamilies.includes(family)) {
        return false;
      }
    }

    if (currentFamilies.length > 1) {
      for (const family of currentFamilies) {
        if (!suggestionFamilies.includes(family)) {
          return false;
        }
      }
    }

    return true;
  }

  function tokensLookLikeMinorCorrections(currentText, suggestion, kindLabels) {
    const currentTokens = tokenizeScriptTokens(currentText);
    const suggestionTokens = tokenizeScriptTokens(suggestion);
    const latinLocaleHint = inferLatinLocaleHint(currentText);
    if (!currentTokens.length || !suggestionTokens.length) {
      return assessDirectReplacementSafety(currentText, suggestion).safe;
    }

    if (flattenComparableTokens(currentTokens) === flattenComparableTokens(suggestionTokens)) {
      return true;
    }

    if (currentTokens.length !== suggestionTokens.length) {
      return false;
    }

    let changedCount = 0;
    let totalDistance = 0;
    for (let index = 0; index < currentTokens.length; index += 1) {
      const currentToken = currentTokens[index];
      const suggestionToken = suggestionTokens[index];
      if (currentToken.family !== suggestionToken.family) {
        return false;
      }

      if (currentToken.normalized === suggestionToken.normalized) {
        continue;
      }

      if (looksLikeLocaleSwap(currentToken, suggestionToken)) {
        return false;
      }

      const distance = getEditDistance(currentToken.normalized, suggestionToken.normalized);
      if (!isMinorTokenEdit(currentToken, suggestionToken, kindLabels, distance, latinLocaleHint)) {
        return false;
      }

      changedCount += 1;
      totalDistance += distance;
    }

    if (changedCount === 0) {
      return false;
    }

    const editBudget =
      currentTokens.length <= 4
        ? Math.max(6, currentTokens.length * 2)
        : Math.max(8, Math.ceil(currentTokens.length * 2.2));
    return totalDistance <= editBudget;
  }

  function isMinorTokenEdit(currentToken, suggestionToken, kindLabels, precomputedDistance, latinLocaleHint) {
    const currentValue = currentToken && currentToken.normalized ? currentToken.normalized : "";
    const suggestionValue = suggestionToken && suggestionToken.normalized ? suggestionToken.normalized : "";
    if (!currentValue || !suggestionValue) {
      return false;
    }

    const maxLength = Math.max(currentValue.length, suggestionValue.length, 1);
    const minLength = Math.max(1, Math.min(currentValue.length, suggestionValue.length));
    const distance =
      typeof precomputedDistance === "number" && Number.isFinite(precomputedDistance)
        ? precomputedDistance
        : getEditDistance(currentValue, suggestionValue);
    const similarity = 1 - distance / maxLength;
    const grammarMode = Array.isArray(kindLabels) && kindLabels.includes("문법");

    let allowedDistance = grammarMode ? 3 : 2;
    if (maxLength <= 4) {
      allowedDistance = grammarMode ? 2 : 1;
    } else if (maxLength >= 12) {
      allowedDistance = grammarMode ? 5 : 4;
    } else if (maxLength >= 8) {
      allowedDistance = grammarMode ? 4 : 3;
    }

    if (distance > allowedDistance) {
      return false;
    }

    if (minLength / maxLength < (grammarMode ? 0.5 : 0.62)) {
      return false;
    }

    if (similarity < (grammarMode ? 0.35 : 0.5)) {
      return false;
    }

    if (shouldRejectConservativeLatinWordSwap(currentToken, suggestionToken, kindLabels, distance, latinLocaleHint)) {
      return false;
    }

    if (currentToken.family === "latin" && currentValue.length > 2 && suggestionValue.length > 2) {
      if (currentValue[0] !== suggestionValue[0] && currentValue.slice(0, 2) !== suggestionValue.slice(0, 2)) {
        return false;
      }
    }

    return true;
  }

  function looksLikeLocaleSwap(currentToken, suggestionToken) {
    if (!currentToken || !suggestionToken || currentToken.family !== "latin" || suggestionToken.family !== "latin") {
      return false;
    }

    const currentValue = currentToken.normalized;
    const suggestionValue = suggestionToken.normalized;
    if (!currentValue || !suggestionValue || currentValue === suggestionValue) {
      return false;
    }

    return LATIN_UI_TOKEN_SET.has(currentValue) && LATIN_UI_TOKEN_SET.has(suggestionValue);
  }

  function shouldRejectConservativeLatinWordSwap(currentToken, suggestionToken, kindLabels, distance, latinLocaleHint) {
    if (!currentToken || !suggestionToken || currentToken.family !== "latin" || suggestionToken.family !== "latin") {
      return false;
    }

    const currentValue = currentToken.normalized;
    const suggestionValue = suggestionToken.normalized;
    if (!currentValue || !suggestionValue || currentValue === suggestionValue || distance <= 1) {
      return false;
    }

    if (!/^[a-z]+$/.test(currentValue) || !/^[a-z]+$/.test(suggestionValue)) {
      return false;
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("문법")) {
      return false;
    }

    return !isKnownLatinReferenceToken(currentValue, latinLocaleHint) && !isKnownLatinReferenceToken(suggestionValue, latinLocaleHint);
  }

  function isKnownLatinReferenceToken(value, latinLocaleHint) {
    const normalized = normalizeLatinTokenForLookup(value);
    if (!normalized) {
      return false;
    }

    if (LATIN_UI_TOKEN_SET.has(normalized)) {
      return true;
    }

    const hintPool = getLatinTokenHintPool(latinLocaleHint);
    for (const token of hintPool) {
      if (normalizeLatinTokenForLookup(token) === normalized) {
        return true;
      }
    }

    return false;
  }

  function flattenComparableTokens(tokens) {
    return (Array.isArray(tokens) ? tokens : []).map((token) => token.normalized).join("");
  }

  function tokenizeScriptTokens(text) {
    const tokens = [];
    let buffer = "";
    let family = "";

    const flush = () => {
      if (!buffer || !family) {
        buffer = "";
        family = "";
        return;
      }

      const normalized = normalizeTokenForComparison(buffer, family);
      if (normalized) {
        tokens.push({
          text: buffer,
          family,
          normalized,
        });
      }

      buffer = "";
      family = "";
    };

    for (const character of normalizeLineEndings(text)) {
      const charFamily = detectCharacterScriptFamily(character);
      if (!charFamily) {
        if (buffer && /[0-9'’\-]/.test(character)) {
          buffer += character;
          continue;
        }

        flush();
        continue;
      }

      if (!buffer) {
        buffer = character;
        family = charFamily;
        continue;
      }

      if (charFamily === family) {
        buffer += character;
        continue;
      }

      flush();
      buffer = character;
      family = charFamily;
    }

    flush();
    return tokens;
  }

  function normalizeTokenForComparison(value, family) {
    const source = String(value || "").trim();
    if (!source) {
      return "";
    }

    if (family === "latin") {
      return normalizeLatinTokenForLookup(source);
    }

    return source.replace(/[\s'’\-]/g, "");
  }

  function getActiveScriptFamilies(text) {
    const counts = {
      korean: 0,
      latin: 0,
      japanese: 0,
      cjk: 0,
      cyrillic: 0,
      arabic: 0,
      devanagari: 0,
      thai: 0,
      hebrew: 0,
      greek: 0,
      other: 0,
    };
    addScriptCounts(counts, text);
    return Object.keys(counts).filter((key) => key !== "other" && counts[key] > 0);
  }

  function countTermOccurrences(text, term, caseInsensitive) {
    const source = String(normalizeLineEndings(text) || "");
    const needle = String(term || "");
    if (!source || !needle) {
      return 0;
    }

    const haystack = caseInsensitive ? source.toLocaleLowerCase() : source;
    const target = caseInsensitive ? needle.toLocaleLowerCase() : needle;
    if (!target) {
      return 0;
    }

    let count = 0;
    let index = 0;
    while (index <= haystack.length) {
      const nextIndex = haystack.indexOf(target, index);
      if (nextIndex < 0) {
        break;
      }

      count += 1;
      index = nextIndex + target.length;
    }

    return count;
  }

  async function applyTypoAudit(designReadResult, options) {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택하세요.");
    }

    const auditProfile = normalizeTypoAuditProfile(options && options.auditProfile);
    const proofingSettings = await readProofingSettings();
    const context = buildSelectionContext(selection, designReadResult, proofingSettings);
    const textNodes = collectTextNodes(selection);
    if (!textNodes.length) {
      throw new Error("텍스트 레이어가 포함된 선택을 먼저 선택하세요.");
    }

    const issueResult = await buildTypoIssues(textNodes, context, proofingSettings, { auditProfile });
    const issues = issueResult.issues.slice(0, 24);
    const annotationNodes = getAnnotatableTextNodes(textNodes);
    const annotationSupported = annotationNodes.length > 0;
    const category = annotationSupported ? await ensureAnnotationCategory(getManagedAnnotationCategoryColor()) : null;
    const applied = annotationSupported
      ? applyAnnotations(annotationNodes, issues, category)
      : buildResultOnlyApplication(
          issues,
          "현재 환경에서는 Figma Annotation API를 사용할 수 없어 결과 패널에만 표시했습니다."
        );
    if (issueResult.aiError) {
      applied.skipped.unshift({
        label: "AI 검수 경고",
        reason: issueResult.aiError,
      });
    }
    const auditUsesAnnotations = applied.annotationCount > 0;
    const insights = buildInsights(context, textNodes, issues, applied, auditUsesAnnotations, issueResult.aiMeta);

    return {
      version: PATCH_VERSION,
      source:
        issueResult.strategy === "ai-primary"
          ? "ai-primary-annotation"
          : issueResult.strategy === "speed-local"
            ? "speed-local-annotation"
            : "local-fallback-annotation",
      mode: auditUsesAnnotations ? "figma-dev-annotation" : "result-only",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel,
        contextLabel: context.contextLabel,
        textNodeCount: textNodes.length,
        issueCount: issues.length,
        annotatedNodeCount: applied.annotatedNodeCount,
        annotationCount: applied.annotationCount,
        clearedNodeCount: applied.clearedNodeCount,
        skippedCount: applied.skipped.length,
        mode: auditUsesAnnotations ? "figma-dev-annotation" : "result-only",
        modeLabel: auditUsesAnnotations ? "Figma Dev Mode 주석" : "결과 패널만",
        auditProfile,
        auditProfileLabel: getTypoAuditProfileLabel(auditProfile),
        reviewStrategy: getTypoAuditProfileStrategyLabel(auditProfile, issueResult.strategy),
        aiStatusLabel: issueResult.aiMeta ? issueResult.aiMeta.statusLabel : "AI 상태 미확인",
        aiProviderLabel: issueResult.aiMeta ? issueResult.aiMeta.providerLabel : "",
        aiModelLabel: issueResult.aiMeta ? issueResult.aiMeta.modelLabel : "",
        categoryLabel:
          auditUsesAnnotations && category && category.label ? category.label : auditUsesAnnotations ? ANNOTATION_CATEGORY_LABEL : "결과 패널만",
      },
      issues: summarizeIssueResults(issues).slice(0, 12),
      skipped: applied.skipped.slice(0, 8),
      insights: insights.slice(0, 6),
    };
  }

  async function applyTypoFix(designReadResult) {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택하세요.");
    }

    const proofingSettings = await readProofingSettings();
    const context = buildSelectionContext(selection, designReadResult, proofingSettings);
    const textNodes = collectTextNodes(selection);
    if (!textNodes.length) {
      throw new Error("텍스트 레이어가 포함된 선택을 먼저 선택하세요.");
    }

    const issueResult = await buildTypoIssues(textNodes, context, proofingSettings);
    const applied = await applyDirectTextFixes(textNodes, issueResult, context, proofingSettings);
    const annotationNodes = getAnnotatableTextNodes(textNodes);
    const annotationSupported = annotationNodes.length > 0;
    const category = annotationSupported ? await ensureAnnotationCategory(getManagedAnnotationCategoryColor()) : null;
    const annotationApplied = annotationSupported
      ? applyAnnotations(annotationNodes, applied.annotationIssues, category)
      : applied.annotationIssues.length > 0
        ? buildResultOnlyApplication(
            applied.annotationIssues,
            "현재 환경에서는 Figma Annotation API를 사용할 수 없어 수정 결과를 패널에만 표시합니다."
          )
        : {
            applied: [],
            cleared: [],
            skipped: [],
            annotatedNodeCount: 0,
            annotationCount: 0,
            clearedNodeCount: 0,
            removedAnnotationCount: 0,
          };
    if (issueResult.aiError) {
      applied.skipped.unshift({
        label: "AI 검수 경고",
        reason: issueResult.aiError,
      });
    }
    const issues = summarizeIssueResults(issueResult.issues).slice(0, 12);
    const skipped = [...applied.skipped, ...annotationApplied.skipped];
    const fixUsesAnnotations = annotationApplied.annotationCount > 0;
    const insights = buildFixInsights(context, textNodes, issueResult.issues, applied, annotationApplied, issueResult.aiMeta);

    return {
      version: PATCH_VERSION,
      source: issueResult.strategy === "ai-primary" ? "ai-primary-direct-fix-annotation" : "local-fallback-direct-fix-annotation",
      mode: fixUsesAnnotations ? "direct-text-edit-with-annotation" : "direct-text-edit",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel,
        contextLabel: context.contextLabel,
        textNodeCount: textNodes.length,
        issueCount: issueResult.issues.length,
        appliedCount: applied.appliedCount,
        unchangedCount: applied.unchangedCount,
        annotationCount: annotationApplied.annotationCount,
        annotatedNodeCount: annotationApplied.annotatedNodeCount,
        clearedNodeCount: annotationApplied.clearedNodeCount,
        skippedCount: skipped.length,
        mode: fixUsesAnnotations ? "direct-text-edit-with-annotation" : "direct-text-edit",
        modeLabel: fixUsesAnnotations ? "직접 수정 + Dev Mode 주석" : "직접 수정",
        reviewStrategy: issueResult.strategy === "ai-primary" ? "AI 우선 + 로컬 보완" : "로컬 fallback",
        aiStatusLabel: issueResult.aiMeta ? issueResult.aiMeta.statusLabel : "AI 상태 미확인",
        aiProviderLabel: issueResult.aiMeta ? issueResult.aiMeta.providerLabel : "",
        aiModelLabel: issueResult.aiMeta ? issueResult.aiMeta.modelLabel : "",
        categoryLabel: fixUsesAnnotations ? "직접 수정 후 Dev Mode 주석" : "직접 수정",
      },
      issues,
      applied: applied.applied.slice(0, 12),
      annotations: annotationApplied.applied.slice(0, 12),
      skipped: skipped.slice(0, 8),
      insights: insights.slice(0, 6),
    };
  }

  async function applyAiTranslation(designReadResult, targetLanguage) {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      pendingTranslatePreviewState = null;
      throw new Error("번역할 프레임, 그룹, 텍스트 레이어를 먼저 선택해주세요. 안전을 위해 선택이 없을 때 전체 페이지 번역은 실행하지 않습니다.");
    }
    const translationRoots = selection;

    const proofingSettings = await readProofingSettings();
    const context = buildSelectionContext(translationRoots, designReadResult, proofingSettings);
    const textNodes = collectTextNodes(translationRoots);
    if (!textNodes.length) {
      throw new Error("텍스트가 포함된 프레임이나 페이지를 먼저 선택하세요.");
    }

    const translationResult = await requestAiTranslations(textNodes, context, proofingSettings, targetLanguage);
    const applied = await applyTranslatedText(textNodes, translationResult.issues, proofingSettings, targetLanguage);
    const sourceLanguageLabel =
      context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지";
    const insights = buildTranslateInsights(
      context,
      textNodes,
      applied,
      translationResult.aiMeta,
      targetLanguage,
      translationResult.cacheStats
    );

    pendingTranslatePreviewState = null;
    return {
      version: PATCH_VERSION,
      source: "ai-translation-direct-edit",
      mode: "direct-text-translation",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel,
        contextLabel: context.contextLabel,
        textNodeCount: textNodes.length,
        translatedCount: applied.appliedCount,
        previewCount: 0,
        unchangedCount: applied.unchangedCount,
        skippedCount: applied.skipped.length,
        reusedCount: translationResult.cacheStats ? translationResult.cacheStats.reusedCount : 0,
        requestedTextCount: translationResult.cacheStats ? translationResult.cacheStats.requestedCount : textNodes.length,
        uniqueRequestedTextCount: translationResult.cacheStats ? translationResult.cacheStats.uniqueRequestedCount : textNodes.length,
        sourceLanguageLabel,
        targetLanguageCode: targetLanguage.code,
        targetLanguageLabel: targetLanguage.label,
        mode: "direct-text-translation",
        modeLabel: "현재 선택 번역",
        requiresApply: false,
        aiStatusLabel: translationResult.aiMeta ? translationResult.aiMeta.statusLabel : "AI 상태 미확인",
        aiProviderLabel: translationResult.aiMeta ? translationResult.aiMeta.providerLabel : "",
        aiModelLabel: translationResult.aiMeta ? translationResult.aiMeta.modelLabel : "",
      },
      preview: [],
      applied: applied.applied.slice(0, 12),
      skipped: applied.skipped.slice(0, 8),
      insights: insights.slice(0, 6),
    };
  }

  async function applyAiTranslationPreviewSelection(selectedIds) {
    const state = pendingTranslatePreviewState;
    if (!state || !Array.isArray(state.textNodes) || !Array.isArray(state.issues)) {
      throw new Error("적용할 번역 미리보기가 없습니다. 먼저 번역 미리보기를 다시 생성해주세요.");
    }

    if (!matchesSelectionSignature(state.selectionSignature)) {
      throw new Error("현재 선택이 미리보기 생성 시점과 다릅니다. 안전하게 다시 미리보기를 생성해주세요.");
    }

    const selectedIdSet = new Set(selectedIds);
    const selectedIssues = state.issues.filter((issue) => {
      return !!(issue && issue.node && selectedIdSet.has(issue.node.id));
    });
    if (!selectedIssues.length) {
      throw new Error("선택한 항목에서 적용할 번역 제안을 찾지 못했습니다.");
    }

    const applied = await applyTranslatedText(state.textNodes, selectedIssues, state.proofingSettings, state.targetLanguage);
    const insights = buildTranslateInsights(
      state.context,
      state.textNodes,
      applied,
      state.aiMeta,
      state.targetLanguage,
      state.cacheStats
    );
    const result = {
      version: PATCH_VERSION,
      source: "ai-translation-selected-apply",
      mode: "direct-text-translation",
      selectionSignature: state.selectionSignature,
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: state.context.selectionLabel,
        contextLabel: state.context.contextLabel,
        textNodeCount: state.textNodes.length,
        selectedCount: selectedIssues.length,
        translatedCount: applied.appliedCount,
        previewCount: 0,
        unchangedCount: applied.unchangedCount,
        skippedCount: applied.skipped.length,
        reusedCount: state.cacheStats ? state.cacheStats.reusedCount : 0,
        requestedTextCount: state.cacheStats ? state.cacheStats.requestedCount : state.textNodes.length,
        uniqueRequestedTextCount: state.cacheStats ? state.cacheStats.uniqueRequestedCount : state.textNodes.length,
        sourceLanguageLabel: state.sourceLanguageLabel,
        targetLanguageCode: state.targetLanguage.code,
        targetLanguageLabel: state.targetLanguage.label,
        mode: "direct-text-translation",
        modeLabel: "선택 번역 적용",
        requiresApply: false,
        aiStatusLabel: state.aiMeta ? state.aiMeta.statusLabel : "AI 상태 미확인",
        aiProviderLabel: state.aiMeta ? state.aiMeta.providerLabel : "",
        aiModelLabel: state.aiMeta ? state.aiMeta.modelLabel : "",
      },
      preview: [],
      applied: applied.applied.slice(0, 12),
      skipped: applied.skipped.slice(0, 8),
      insights: insights.slice(0, 6),
    };

    pendingTranslatePreviewState = null;
    return result;
  }

  async function requestAiTranslations(textNodes, context, proofingSettings, targetLanguage) {
    const ai = getAiHelper();
    if (!ai) {
      throw new Error("AI 번역을 사용하려면 AI 설정에서 API 키를 먼저 입력해 주세요.");
    }

    let configured = false;
    let runInfo = { provider: "", model: "" };
    try {
      configured = await ai.hasConfiguredAiAsync();
      if (typeof ai.getAiSettingsAsync === "function" && typeof ai.getResolvedRunInfo === "function") {
        const settings = await ai.getAiSettingsAsync();
        runInfo = ai.getResolvedRunInfo(settings, { modelByProvider: AI_TRANSLATE_MODEL_BY_PROVIDER });
      }
    } catch (error) {
      configured = false;
    }

    if (!configured) {
      throw new Error("AI 번역을 사용하려면 AI 설정에서 API 키를 먼저 입력해 주세요.");
    }

    const candidates = buildAiTranslateCandidates(textNodes, proofingSettings);
    if (!candidates.length) {
      return {
        issues: [],
        aiMeta: createAiMeta("skipped", runInfo.provider, runInfo.model, "번역할 텍스트 후보가 없습니다."),
      };
    }

    const translationMemoryMap = buildTranslateMemoryMap(await readTranslateMemory());
    const pendingCandidateMap = new Map();
    const pendingCandidates = [];
    const issues = [];
    let cachedCount = 0;
    let requestedCount = 0;
    let translationMemoryDirty = false;

    for (const candidate of candidates) {
      const currentText = normalizeLineEndings(candidate.currentText || "");
      const memoryKey = buildTranslateMemoryKey(targetLanguage.code, currentText);
      const cachedEntry = translationMemoryMap.get(memoryKey);
      if (cachedEntry && cachedEntry.translatedText) {
        const cachedSuggestion = normalizeLineEndings(cachedEntry.translatedText);
        if (
          cachedSuggestion &&
          compactText(currentText) !== compactText(cachedSuggestion) &&
          issueRespectsProofingTerms(currentText, cachedSuggestion, proofingSettings) &&
          translationPreservesCriticalTokens(currentText, cachedSuggestion)
        ) {
          issues.push(
            createIssue(
              candidate.node,
              currentText,
              cachedSuggestion,
              "번역",
              cachedEntry.reason || "이전 AI 번역 결과를 다시 사용했습니다.",
              "ai"
            )
          );
          cachedCount += 1;
          continue;
        }
      }

      requestedCount += 1;
      let bucket = pendingCandidateMap.get(memoryKey);
      if (!bucket) {
        bucket = {
          id: candidate.id,
          memoryKey,
          node: candidate.node,
          currentText,
          text: currentText,
          members: [],
        };
        pendingCandidateMap.set(memoryKey, bucket);
        pendingCandidates.push(bucket);
      }
      bucket.members.push(candidate);
    }

    if (!pendingCandidates.length) {
      return {
        issues,
        aiMeta: createAiMeta("success", runInfo.provider, runInfo.model, ""),
        cacheStats: {
          reusedCount: cachedCount,
          requestedCount,
          uniqueRequestedCount: 0,
        },
      };
    }

    const schema = {
      type: "object",
      properties: {
        translations: {
          type: "array",
          items: {
            type: "object",
            properties: {
              id: { type: "string" },
              text: { type: "string" },
              reason: { type: "string" },
            },
            required: ["id", "text", "reason"],
          },
        },
      },
      required: ["translations"],
    };
    const instructions =
      "Translate each text node into the requested target language. Preserve product names, brand names, protectedTerms, URLs, emails, placeholders such as {{name}}, ${name}, {name}, printf tokens such as %s or %1$d, numbers, HTML-like tags, slash commands, and code-like identifiers exactly. Keep punctuation and line breaks whenever possible. Do not summarize, censor, or add explanations. Omit entries that should stay unchanged. Return short reasons in Korean when possible.";
    const candidateChunks = buildTranslateChunks(pendingCandidates);
    const chunkCount = Math.max(1, candidateChunks.length);
    let resolvedProvider = runInfo.provider;
    let resolvedModel = runInfo.model;

    for (let chunkIndex = 0; chunkIndex < candidateChunks.length; chunkIndex += 1) {
      const chunk = candidateChunks[chunkIndex];
      postTranslateStatus("running", `${targetLanguage.label}로 번역하는 중입니다. (${chunkIndex + 1}/${chunkCount})`);

      const payload = {
        selectionLabel: context.selectionLabel,
        contextLabel: context.contextLabel,
        sourceLanguageLabel:
          context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "Auto detect",
        targetLanguageCode: targetLanguage.code,
        targetLanguageLabel: targetLanguage.aiLabel || targetLanguage.label,
        latinLocaleHint: targetLanguage.latinLocaleHint || context.latinLocaleHint || "",
        protectedTerms: proofingSettings ? proofingSettings.protectedTerms : [],
        userDictionary: proofingSettings ? proofingSettings.userDictionary : [],
        textNodes: chunk.map((entry) => ({
          id: entry.id,
          text: entry.text,
        })),
      };

      let response = null;
      try {
        try {
          response = await ai.requestJsonTask({
            instructions,
            schema,
            payload,
            modelByProvider: AI_TRANSLATE_MODEL_BY_PROVIDER,
          });
        } catch (error) {
          if (!shouldRetryTypoAuditWithDefaultModel(error)) {
            throw error;
          }

          response = await ai.requestJsonTask({
            instructions,
            schema,
            payload,
          });
        }
      } catch (error) {
        throw new Error(normalizeErrorMessage(error, "AI 번역 요청에 실패했습니다."));
      }

      if (response && response._provider) {
        resolvedProvider = response._provider;
      }
      if (response && response._model) {
        resolvedModel = response._model;
      }

      const rows = response && Array.isArray(response.translations) ? response.translations : [];
      const candidateMap = new Map();
      for (const candidate of chunk) {
        candidateMap.set(candidate.id, candidate);
      }

      for (const row of rows) {
        if (!row || typeof row !== "object" || typeof row.id !== "string" || typeof row.text !== "string") {
          continue;
        }

        const candidate = candidateMap.get(row.id);
        if (!candidate || !Array.isArray(candidate.members) || !candidate.members.length) {
          continue;
        }

        const currentText = normalizeLineEndings(candidate.currentText || "");
        const suggestion = normalizeLineEndings(String(row.text || ""));
        if (!currentText || !suggestion || compactText(currentText) === compactText(suggestion)) {
          continue;
        }

        if (!issueRespectsProofingTerms(currentText, suggestion, proofingSettings)) {
          continue;
        }

        if (!translationPreservesCriticalTokens(currentText, suggestion)) {
          continue;
        }

        issues.push(
          createIssue(
            candidate.node,
            currentText,
            suggestion,
            "번역",
            typeof row.reason === "string" && row.reason.trim() ? row.reason.trim() : "AI가 선택한 언어로 번역했습니다.",
            "ai"
          )
        );
      }
    }

    for (const issue of issues) {
      if (!issue) {
        continue;
      }
      upsertTranslateMemoryEntry(
        translationMemoryMap,
        targetLanguage.code,
        normalizeLineEndings(issue.currentText || ""),
        normalizeLineEndings(issue.suggestion || ""),
        typeof issue.reason === "string" ? issue.reason : ""
      );
      translationMemoryDirty = true;
    }

    if (translationMemoryDirty) {
      await writeTranslateMemory(collectTranslateMemoryEntries(translationMemoryMap));
    }

    return {
      issues: expandTranslateIssuesForDuplicateCandidates(issues, pendingCandidates),
      aiMeta: createAiMeta("success", resolvedProvider, resolvedModel, ""),
      cacheStats: {
        reusedCount: cachedCount,
        requestedCount,
        uniqueRequestedCount: pendingCandidates.length,
      },
    };
  }

  function buildTranslateChunks(candidates) {
    const chunks = [];
    let currentChunk = [];
    let currentChars = 0;

    for (const candidate of Array.isArray(candidates) ? candidates : []) {
      if (!candidate) {
        continue;
      }

      const nextChars = currentChars + String(candidate.text || "").length;
      const exceedsItemLimit = currentChunk.length >= AI_TRANSLATE_MAX_CHUNK_ITEMS;
      const exceedsCharLimit = currentChunk.length > 0 && nextChars > AI_TRANSLATE_MAX_CHUNK_CHARS;

      if (exceedsItemLimit || exceedsCharLimit) {
        chunks.push(currentChunk);
        currentChunk = [];
        currentChars = 0;
      }

      currentChunk.push(candidate);
      currentChars += String(candidate.text || "").length;
    }

    if (currentChunk.length > 0) {
      chunks.push(currentChunk);
    }

    return chunks;
  }

  function buildTranslateMemoryMap(entries) {
    const map = new Map();
    for (const entry of normalizeTranslateMemory(entries)) {
      map.set(entry.key, entry);
    }
    return map;
  }

  function collectTranslateMemoryEntries(memoryMap) {
    const entries = [];
    if (!memoryMap || typeof memoryMap.values !== "function") {
      return entries;
    }

    for (const entry of memoryMap.values()) {
      entries.push(entry);
    }

    return normalizeTranslateMemory(entries);
  }

  function normalizeTranslateMemory(entries) {
    const source =
      Array.isArray(entries)
        ? entries
        : entries && typeof entries === "object" && Array.isArray(entries.entries)
          ? entries.entries
          : [];
    const normalized = [];
    const seen = new Set();

    for (const item of source) {
      const entry = normalizeTranslateMemoryEntry(item);
      if (!entry || seen.has(entry.key)) {
        continue;
      }
      seen.add(entry.key);
      normalized.push(entry);
      if (normalized.length >= AI_TRANSLATE_MEMORY_LIMIT) {
        break;
      }
    }

    return normalized;
  }

  function normalizeTranslateMemoryEntry(value) {
    const source = value && typeof value === "object" ? value : null;
    if (!source) {
      return null;
    }

    const targetLanguageCode = String(source.targetLanguageCode || "").trim();
    const sourceText = normalizeLineEndings(source.sourceText || "");
    const translatedText = normalizeLineEndings(source.translatedText || "");
    if (!targetLanguageCode || !sourceText || !translatedText) {
      return null;
    }

    return {
      key: buildTranslateMemoryKey(targetLanguageCode, sourceText),
      targetLanguageCode,
      sourceText,
      translatedText,
      reason: typeof source.reason === "string" ? source.reason.trim() : "",
      updatedAt:
        typeof source.updatedAt === "string" && source.updatedAt.trim() ? source.updatedAt.trim() : new Date().toISOString(),
    };
  }

  function buildTranslateMemoryKey(targetLanguageCode, sourceText) {
    return `${String(targetLanguageCode || "").trim().toLowerCase()}::${normalizeLineEndings(sourceText || "")}`;
  }

  function upsertTranslateMemoryEntry(memoryMap, targetLanguageCode, sourceText, translatedText, reason) {
    if (!memoryMap || typeof memoryMap.set !== "function") {
      return;
    }

    const entry = normalizeTranslateMemoryEntry({
      targetLanguageCode,
      sourceText,
      translatedText,
      reason,
      updatedAt: new Date().toISOString(),
    });
    if (!entry) {
      return;
    }

    if (memoryMap.has(entry.key)) {
      memoryMap.delete(entry.key);
    }
    memoryMap.set(entry.key, entry);
  }

  function expandTranslateIssuesForDuplicateCandidates(issues, pendingCandidates) {
    const expanded = [];
    const pendingByNodeId = new Map();

    for (const candidate of Array.isArray(pendingCandidates) ? pendingCandidates : []) {
      if (!candidate || !candidate.node || !candidate.node.id) {
        continue;
      }
      pendingByNodeId.set(candidate.node.id, candidate);
    }

    for (const issue of Array.isArray(issues) ? issues : []) {
      if (!issue || !issue.node || !issue.node.id) {
        continue;
      }

      const candidate = pendingByNodeId.get(issue.node.id);
      const members = candidate && Array.isArray(candidate.members) ? candidate.members : [];
      if (!members.length) {
        expanded.push(issue);
        continue;
      }

      for (const member of members) {
        if (!member || !member.node) {
          continue;
        }
        expanded.push(
          createIssue(
            member.node,
            issue.currentText,
            issue.suggestion,
            issue.kind || "번역",
            issue.reason || "",
            issue.source || "ai"
          )
        );
      }
    }

    return expanded;
  }

  function buildAiTranslateCandidates(textNodes, proofingSettings) {
    const candidates = [];

    for (const node of textNodes) {
      const currentText = getTextValue(node);
      if (!shouldTranslateTextNodeValue(currentText, proofingSettings)) {
        continue;
      }

      candidates.push({
        id: node.id,
        node,
        nodeId: node.id,
        name: safeName(node),
        currentText,
        text: currentText,
      });
    }

    return candidates;
  }

  function shouldTranslateTextNodeValue(text, proofingSettings) {
    const currentText = normalizeLineEndings(text);
    const compactValue = compactText(currentText);
    if (!compactValue) {
      return false;
    }

    if (isWholeTextProtected(currentText, proofingSettings)) {
      return false;
    }

    if (getActiveScriptFamilies(currentText).length === 0) {
      return false;
    }

    if (/^(https?:\/\/|www\.)/i.test(compactValue)) {
      return false;
    }

    if (/^[\d\s.,:/\-+%()[\]{}<>#@_*"'!?&|=]+$/.test(compactValue)) {
      return false;
    }

    return true;
  }

  function normalizeTranslatePreviewSelection(value) {
    const source = Array.isArray(value) ? value : [];
    const selected = [];
    const seen = new Set();

    for (const item of source) {
      const id = typeof item === "string" ? item.trim() : "";
      if (!id || seen.has(id)) {
        continue;
      }
      seen.add(id);
      selected.push(id);
    }

    return selected;
  }

  function buildTranslatedTextPreview(textNodes, issues, proofingSettings, targetLanguage) {
    const issuesByNode = new Map();
    const preview = [];
    const skipped = [];
    let unchangedCount = 0;

    for (const issue of Array.isArray(issues) ? issues : []) {
      if (!issue || !issue.node || !issue.node.id) {
        continue;
      }

      issuesByNode.set(issue.node.id, issue);
    }

    for (const node of Array.isArray(textNodes) ? textNodes : []) {
      const currentText = getTextValue(node);
      if (!currentText) {
        unchangedCount += 1;
        continue;
      }

      const issue = issuesByNode.get(node.id);
      if (!issue) {
        unchangedCount += 1;
        continue;
      }

      const nextText = normalizeTranslatedSuggestion(currentText, issue.suggestion);
      if (!nextText || nextText === currentText) {
        unchangedCount += 1;
        continue;
      }

      if (!issueRespectsProofingTerms(currentText, nextText, proofingSettings)) {
        skipped.push({
          label: safeName(node),
          reason: "보호된 용어가 바뀔 수 있어 번역 적용을 건너뜁니다.",
        });
        unchangedCount += 1;
        continue;
      }

      if (!translationPreservesCriticalTokens(currentText, nextText)) {
        skipped.push({
          label: safeName(node),
          reason: "URL, 플레이스홀더, 코드 토큰이 바뀔 수 있어 번역 적용을 건너뜁니다.",
        });
        unchangedCount += 1;
        continue;
      }

      if (countTextLines(currentText) > 1 && countNonEmptyLines(nextText) < Math.max(1, countNonEmptyLines(currentText) - 1)) {
        skipped.push({
          label: safeName(node),
          reason: "여러 줄 텍스트 구조가 크게 바뀌어 번역 적용을 건너뜁니다.",
        });
        unchangedCount += 1;
        continue;
      }

      preview.push(
        formatIssueResult(
          createIssue(
            node,
            currentText,
            nextText,
            "번역",
            issue.reason || `${targetLanguage.label}로 번역합니다.`,
            issue.source || "ai"
          )
        )
      );
    }

    return {
      preview,
      skipped,
      previewCount: preview.length,
      unchangedCount,
    };
  }

  async function applyTranslatedText(textNodes, issues, proofingSettings, targetLanguage) {
    const issuesByNode = new Map();
    const applied = [];
    const skipped = [];
    let appliedCount = 0;
    let unchangedCount = 0;

    for (const issue of Array.isArray(issues) ? issues : []) {
      if (!issue || !issue.node || !issue.node.id) {
        continue;
      }

      issuesByNode.set(issue.node.id, issue);
    }

    await preloadFontsForTextNodes(textNodes, issuesByNode);

    for (const node of textNodes) {
      const currentText = getTextValue(node);
      if (!currentText) {
        unchangedCount += 1;
        continue;
      }

      const issue = issuesByNode.get(node.id);
      if (!issue) {
        unchangedCount += 1;
        continue;
      }

      const nextText = normalizeTranslatedSuggestion(currentText, issue.suggestion);
      if (!nextText || nextText === currentText) {
        unchangedCount += 1;
        continue;
      }

      if (!issueRespectsProofingTerms(currentText, nextText, proofingSettings)) {
        skipped.push({
          label: safeName(node),
          reason: "보호된 용어가 변경될 수 있어 번역 적용을 건너뛰었습니다.",
        });
        unchangedCount += 1;
        continue;
      }

      if (!translationPreservesCriticalTokens(currentText, nextText)) {
        skipped.push({
          label: safeName(node),
          reason: "URL, 플레이스홀더, 코드형 토큰이 바뀔 수 있어 번역 적용을 건너뛰었습니다.",
        });
        unchangedCount += 1;
        continue;
      }

      if (countTextLines(currentText) > 1 && countNonEmptyLines(nextText) < Math.max(1, countNonEmptyLines(currentText) - 1)) {
        skipped.push({
          label: safeName(node),
          reason: "여러 줄 텍스트 구조가 크게 바뀌어 번역 적용을 건너뛰었습니다.",
        });
        unchangedCount += 1;
        continue;
      }

      try {
        await loadFontsForTextNode(node);
        node.characters = nextText;
        appliedCount += 1;
        applied.push(
          formatIssueResult(
            createIssue(
              node,
              currentText,
              nextText,
              "번역",
              issue.reason || `${targetLanguage.label}로 번역했습니다.`,
              issue.source || "ai"
            )
          )
        );
      } catch (error) {
        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "텍스트를 번역 결과로 바꾸지 못했습니다."),
        });
        unchangedCount += 1;
      }
    }

    return {
      applied,
      skipped,
      appliedCount,
      unchangedCount,
    };
  }

  function normalizeTranslatedSuggestion(currentText, suggestion) {
    let next = normalizeLineEndings(suggestion).trim();
    if (!next) {
      return "";
    }

    if (countTextLines(currentText) === 1) {
      next = next.replace(/\s*\n+\s*/g, " ");
    }

    return next;
  }

  function translationPreservesCriticalTokens(currentText, suggestion) {
    const tokens = collectCriticalTokens(currentText);
    if (!tokens.length) {
      return true;
    }

    for (const token of tokens) {
      if (countTermOccurrences(suggestion, token, false) < countTermOccurrences(currentText, token, false)) {
        return false;
      }
    }

    return true;
  }

  function collectCriticalTokens(text) {
    const value = normalizeLineEndings(text);
    const tokens = [];
    const patterns = [
      /https?:\/\/[^\s)]+/g,
      /\b[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}\b/g,
      /\{\{[^{}]+\}\}/g,
      /\$\{[^{}]+\}/g,
      /\{[^{}\n]+\}/g,
      /%[0-9]*\$?[sdif]/g,
      /<[^>\n]+>/g,
    ];

    for (const pattern of patterns) {
      const matches = value.match(pattern);
      if (!matches || !matches.length) {
        continue;
      }

      for (const match of matches) {
        tokens.push(match);
      }
    }

    return normalizeTermList(tokens);
  }

  function buildTranslateInsights(context, textNodes, applied, aiMeta, targetLanguage, cacheStats) {
    const sourceLanguageLabel =
      context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지";
    const insights = [
      `맥락 기준: ${context.contextLabel}`,
      `원본 언어: ${sourceLanguageLabel}`,
      `목표 언어: ${targetLanguage.label}`,
      `텍스트 레이어 ${textNodes.length}개 확인`,
      `번역 적용 ${applied.appliedCount}개`,
    ];

    if (applied.unchangedCount > 0) {
      insights.push(`변경 없이 유지 ${applied.unchangedCount}개`);
    }

    if (applied.skipped.length > 0) {
      insights.push(`안전 검사로 건너뜀 ${applied.skipped.length}개`);
    }

    if (cacheStats && cacheStats.reusedCount > 0) {
      insights.push(`재사용 캐시 ${cacheStats.reusedCount}개`);
    }

    if (cacheStats && cacheStats.requestedCount > 0 && cacheStats.uniqueRequestedCount > 0) {
      insights.push(`AI 요청 ${cacheStats.requestedCount}개 -> 중복 제거 후 ${cacheStats.uniqueRequestedCount}개`);
    }

    if (aiMeta && aiMeta.statusLabel) {
      insights.push(`AI 상태: ${aiMeta.statusLabel}${aiMeta.providerLabel ? ` · ${aiMeta.providerLabel}` : ""}`);
    }

    if (applied.applied.length > 0) {
      const firstApplied = applied.applied[0];
      insights.push(`대표 변경: ${previewText(firstApplied.currentText, 32)} -> ${previewText(firstApplied.suggestion, 32)}`);
    } else {
      insights.push("번역 결과가 없어 기존 텍스트를 유지했습니다.");
    }

    return insights;
  }

  async function clearManagedTypoAnnotations(designReadResult) {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택하세요.");
    }

    const proofingSettings = await readProofingSettings();
    const context = buildSelectionContext(selection, designReadResult, proofingSettings);
    const textNodes = collectTextNodes(selection, { includeHidden: true, includeLocked: true });
    const annotationNodes = getAnnotatableTextNodes(textNodes);
    const annotationSupported = annotationNodes.length > 0;
    const category = annotationSupported ? await ensureAnnotationCategory(getManagedAnnotationCategoryColor()) : null;
    const applied = annotationSupported
      ? applyAnnotations(annotationNodes, [], category)
      : buildResultOnlyApplication([], "현재 환경에서는 Figma Annotation API를 사용할 수 없어 주석을 지울 수 없습니다.");

    if (textNodes.length === 0) {
      applied.skipped.unshift({
        label: "텍스트 없음",
        reason: "선택한 범위 안에서 주석을 정리할 텍스트 레이어를 찾지 못했습니다.",
      });
    }

    const insights = buildClearInsights(context, textNodes, applied, annotationSupported);

    return {
      version: PATCH_VERSION,
      source: "managed-annotation-clear",
      mode: annotationSupported ? "annotation-clear" : "result-only",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel,
        contextLabel: context.contextLabel,
        textNodeCount: textNodes.length,
        clearedNodeCount: applied.clearedNodeCount,
        removedAnnotationCount: applied.removedAnnotationCount || 0,
        skippedCount: applied.skipped.length,
        mode: annotationSupported ? "annotation-clear" : "result-only",
        modeLabel: annotationSupported ? "Dev Mode 주석 정리" : "결과 패널만",
        categoryLabel: annotationSupported ? "AI 오타 주석 정리" : "결과 패널만",
      },
      cleared: Array.isArray(applied.cleared) ? applied.cleared.slice(0, 12) : [],
      skipped: applied.skipped.slice(0, 8),
      insights: insights.slice(0, 6),
    };
  }

  function supportsAnnotations(textNodes) {
    return getAnnotatableTextNodes(textNodes).length > 0;
  }

  function getAnnotatableTextNodes(textNodes) {
    const result = [];
    for (const node of Array.isArray(textNodes) ? textNodes : []) {
      if (supportsAnnotationsOnNode(node)) {
        result.push(node);
      }
    }
    return result;
  }

  function supportsAnnotationsOnNode(node) {
    return !!node && "annotations" in node;
  }

  async function ensureAnnotationCategory(requestedColor) {
    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return null;
    }

    try {
      const nextColor =
        typeof requestedColor === "string" && requestedColor.trim()
          ? requestedColor.trim().toLowerCase()
          : ANNOTATION_CATEGORY_COLOR;
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      const existing = Array.isArray(categories)
        ? categories.find((category) => category && category.label === ANNOTATION_CATEGORY_LABEL)
        : null;
      if (existing) {
        if (typeof existing.setColor === "function" && existing.color !== nextColor) {
          existing.setColor(nextColor);
        }
        return existing;
      }

      if (typeof figma.annotations.addAnnotationCategoryAsync !== "function") {
        return null;
      }

      return await figma.annotations.addAnnotationCategoryAsync({
        label: ANNOTATION_CATEGORY_LABEL,
        color: nextColor,
      });
    } catch (error) {
      return null;
    }
  }

  function applyAnnotations(textNodes, issues, category) {
    const issuesByNode = new Map();
    const applied = [];
    const cleared = [];
    const skipped = [];
    const availableNodeIds = new Set();
    const unsupportedIssueNodeIds = new Set();
    let annotatedNodeCount = 0;
    let annotationCount = 0;
    let clearedNodeCount = 0;
    let removedAnnotationCount = 0;

    for (const node of textNodes) {
      if (node && node.id) {
        availableNodeIds.add(node.id);
      }
    }

    for (const issue of issues) {
      for (const fragment of getIssueAnnotationFragments(issue)) {
        if (!fragment || !fragment.node || !fragment.node.id) {
          continue;
        }

        if (!availableNodeIds.has(fragment.node.id)) {
          if (!unsupportedIssueNodeIds.has(fragment.node.id)) {
            unsupportedIssueNodeIds.add(fragment.node.id);
            skipped.push({
              label: safeName(fragment.node),
              reason: "이 텍스트 레이어는 Dev Mode 주석을 지원하지 않아 결과 패널에만 표시했습니다.",
            });
          }
          continue;
        }

        const bucket = issuesByNode.get(fragment.node.id) || [];
        bucket.push(fragment);
        issuesByNode.set(fragment.node.id, bucket);
      }
    }

    for (const node of textNodes) {
      const nodeIssues = issuesByNode.get(node.id) || [];
      try {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        const preserved = existing.filter((annotation) => !isManagedAnnotation(annotation, category));
        const nextAnnotations = preserved.concat(nodeIssues.map((issue) => buildAnnotation(issue, category)));
        const removedCount = Math.max(0, existing.length - preserved.length);
        if (removedCount > 0) {
          removedAnnotationCount += removedCount;
        }

        if (removedCount > 0 && nodeIssues.length === 0) {
          clearedNodeCount += 1;
          cleared.push({
            nodeId: node.id,
            nodeName: safeName(node),
            removedCount,
          });
        }

        node.annotations = nextAnnotations;

        if (nodeIssues.length > 0) {
          annotatedNodeCount += 1;
          annotationCount += nodeIssues.length;
          for (const issue of nodeIssues) {
            applied.push(formatIssueResult(issue));
          }
        }
      } catch (error) {
        if (!nodeIssues.length) {
          continue;
        }

        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "주석을 남기지 못했습니다."),
        });
      }
    }

    return {
      applied,
      cleared,
      skipped,
      annotatedNodeCount,
      annotationCount,
      clearedNodeCount,
      removedAnnotationCount,
    };
  }

  function buildResultOnlyApplication(issues, reason) {
    const skipped = [];
    if (reason) {
      skipped.push({
        label: "주석 미적용",
        reason,
      });
    }

    return {
      applied: summarizeIssueResults(issues),
      cleared: [],
      skipped,
      annotatedNodeCount: 0,
      annotationCount: 0,
      clearedNodeCount: 0,
      removedAnnotationCount: 0,
    };
  }

  async function applyDirectTextFixes(textNodes, issueResult, context, proofingSettings) {
    const issuesByNode = new Map();
    const localRepairMap = buildLocalRepairMap(textNodes, context, proofingSettings);
    const applied = [];
    const annotationIssues = [];
    const skipped = [];
    let appliedCount = 0;
    let unchangedCount = 0;

    for (const issue of issueResult.issues || []) {
      const bucket = issuesByNode.get(issue.node.id) || [];
      bucket.push(issue);
      issuesByNode.set(issue.node.id, bucket);
    }

    for (const node of textNodes) {
      const currentText = getTextValue(node);
      if (!currentText) {
        unchangedCount += 1;
        continue;
      }

      const resolution = stabilizeDirectFixResolution(
        node,
        currentText,
        resolveDirectFix(node, currentText, issuesByNode.get(node.id) || [], localRepairMap),
        localRepairMap.get(node.id) || ""
      );
      if (resolution && resolution.skipReason) {
        skipped.push({
          label: safeName(node),
          reason: resolution.skipReason,
        });
        unchangedCount += 1;
        continue;
      }
      if (!resolution || !resolution.nextText || resolution.nextText === currentText) {
        unchangedCount += 1;
        continue;
      }

      try {
        await loadFontsForTextNode(node);
        node.characters = resolution.nextText;
        appliedCount += 1;
        const appliedIssue =
          resolution.issue ||
          createIssue(node, currentText, resolution.nextText, "자동 수정", "오타 후보를 직접 수정했습니다.", resolution.source);
        for (const fragment of getIssueAnnotationFragments(appliedIssue)) {
          applied.push(formatIssueResult(fragment));
        }
        annotationIssues.push(appliedIssue);
      } catch (error) {
        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "텍스트를 직접 수정하지 못했습니다."),
        });
      }
    }

    return {
      applied,
      annotationIssues,
      skipped,
      appliedCount,
      unchangedCount,
    };
  }

  function resolveDirectFix(node, currentText, nodeIssues, localRepairMap) {
    const aiIssue = nodeIssues.find((issue) => issue && issue.source === "ai" && issue.suggestion && issue.suggestion !== currentText);
    if (aiIssue) {
      return {
        nextText: aiIssue.suggestion,
        issue: aiIssue,
        source: "ai",
      };
    }

    const localSuggestion = localRepairMap.get(node.id);
    if (localSuggestion && localSuggestion !== currentText) {
      const localIssue =
        nodeIssues.find((issue) => issue && issue.suggestion === localSuggestion) ||
        createIssue(node, currentText, localSuggestion, "자동 수정", "로컬 규칙으로 직접 수정했습니다.", "local");
      return {
        nextText: localSuggestion,
        issue: localIssue,
        source: "local",
      };
    }

    const fallbackIssue = nodeIssues.find((issue) => issue && issue.suggestion && issue.suggestion !== currentText);
    if (fallbackIssue) {
      return {
        nextText: fallbackIssue.suggestion,
        issue: fallbackIssue,
        source: fallbackIssue.source || "local",
      };
    }

    return null;
  }

  function stabilizeDirectFixResolution(node, currentText, resolution, localSuggestion) {
    if (!resolution || !currentText) {
      return resolution;
    }

    const safetyCheck = assessDirectReplacementSafety(currentText, resolution.nextText);
    const currentLineCount = countTextLines(currentText);
    const nextLineCount = countTextLines(resolution.nextText);
    const currentVisibleLines = countNonEmptyLines(currentText);
    const nextVisibleLines = countNonEmptyLines(resolution.nextText);
    const isMultiline = currentLineCount > 1 || currentVisibleLines > 1;

    if (!safetyCheck.safe) {
      if (isSafeLocalSuggestion(currentText, localSuggestion)) {
        return {
          nextText: localSuggestion,
          issue: createIssue(
            node,
            currentText,
            localSuggestion,
            "안전 교정",
            "AI 제안의 변화 폭이 커서, 더 안전한 로컬 교정만 직접 반영했습니다.",
            "local"
          ),
          source: "local",
        };
      }

      return {
        nextText: "",
        issue: null,
        source: resolution.source || "ai",
        skipReason: safetyCheck.reason,
      };
    }

    if (!isMultiline) {
      return resolution;
    }

    const lineStructureReduced = nextLineCount < currentLineCount || nextVisibleLines < currentVisibleLines;
    if (!lineStructureReduced) {
      return resolution;
    }

    if (
      localSuggestion &&
      localSuggestion !== currentText &&
      countTextLines(localSuggestion) === currentLineCount &&
      countNonEmptyLines(localSuggestion) >= currentVisibleLines
    ) {
      return {
        nextText: localSuggestion,
        issue: createIssue(
          node,
          currentText,
          localSuggestion,
          "멀티라인 보호",
          "AI 제안이 줄 구조를 줄여서, 줄 수를 유지하는 로컬 교정만 직접 반영했습니다.",
          "local"
        ),
        source: "local",
      };
    }

    return {
      nextText: "",
      issue: null,
      source: resolution.source || "ai",
      skipReason: `멀티라인 텍스트 보호: ${currentLineCount}줄 텍스트를 ${nextLineCount}줄 제안으로 바꾸려 해 자동 적용을 건너뛰었습니다.`,
    };
  }

  function countTextLines(value) {
    return normalizeLineEndings(value).split("\n").length;
  }

  function countNonEmptyLines(value) {
    return normalizeLineEndings(value)
      .split("\n")
      .filter((line) => line.replace(/\s+/g, "").length > 0).length;
  }

  function assessDirectReplacementSafety(currentText, nextText) {
    const currentCompact = compactText(currentText);
    const nextCompact = compactText(nextText);
    if (!currentCompact || !nextCompact || currentCompact === nextCompact) {
      return { safe: true, reason: "" };
    }

    const maxLength = Math.max(currentCompact.length, nextCompact.length, 1);
    const minLength = Math.max(1, Math.min(currentCompact.length, nextCompact.length));
    const similarity = 1 - getEditDistance(currentCompact, nextCompact) / maxLength;
    const lengthRatio = minLength / maxLength;

    if (lengthRatio < 0.45) {
      return {
        safe: false,
        reason: "변경 폭이 너무 커서 자동 적용을 건너뛰었습니다. 결과 패널에서 후보를 먼저 확인해 주세요.",
      };
    }

    if (similarity < 0.35) {
      return {
        safe: false,
        reason: "원문과 제안의 차이가 커서 자동 적용을 건너뛰었습니다. 결과 패널에서 후보를 먼저 확인해 주세요.",
      };
    }

    return { safe: true, reason: "" };
  }

  function isSafeLocalSuggestion(currentText, localSuggestion) {
    if (!localSuggestion || localSuggestion === currentText) {
      return false;
    }

    return assessDirectReplacementSafety(currentText, localSuggestion).safe;
  }

  function buildLocalRepairMap(textNodes, context, proofingSettings) {
    const map = new Map();

    for (const node of textNodes) {
      const original = getTextValue(node);
      if (!original) {
        continue;
      }

      if (isWholeTextProtected(original, proofingSettings)) {
        continue;
      }

      const repaired = repairTextLocally(node, original, context, []);
      if (
        repaired &&
        repaired !== original &&
        issueRespectsProofingTerms(original, repaired, proofingSettings) &&
        changeMatchesCorrectionPolicy(original, repaired, "로컬 교정")
      ) {
        map.set(node.id, repaired);
      }
    }

    return map;
  }

  function repairTextLocally(node, text, context, replacements) {
    let next = String(text || "");
    if (!next) {
      return next;
    }

    const languageFamily = detectLanguageFamilyFromText(next) || detectLanguageFamilyLabel(context && context.detectedLanguageLabel);
    const latinLocaleHint = resolveLatinTokenLocaleHint(context, next);
    next = normalizeInlineWhitespace(next);
    next = normalizePunctuationSpacing(next, languageFamily);
    next = normalizeNoSpaceScriptSpacing(next, languageFamily);
    next = normalizeRepeatedPunctuation(next);

    next = applyTypoReplacementRules(next);

    next = applyPhraseHintSuggestion(next) || next;
    next = repairLatinTokensInText(next, languageFamily, latinLocaleHint);

    for (const replacement of replacements) {
      next = replaceAllLiteral(next, replacement.variant, replacement.canonical);
    }

    return next;
  }

  function buildConsistencyReplacementMap(textNodes) {
    const replacementsByNode = new Map();

    for (const rule of CONSISTENCY_RULES) {
      const matched = [];
      for (const node of textNodes) {
        const text = compactText(getTextValue(node));
        if (!text) {
          continue;
        }

        const variant = rule.variants.find((entry) => text.includes(entry));
        if (variant) {
          matched.push({ node, variant });
        }
      }

      const hasCanonical = matched.some((entry) => entry.variant === rule.canonical);
      if (!hasCanonical) {
        continue;
      }

      for (const entry of matched) {
        if (entry.variant === rule.canonical) {
          continue;
        }

        const bucket = replacementsByNode.get(entry.node.id) || [];
        bucket.push({
          variant: entry.variant,
          canonical: rule.canonical,
        });
        replacementsByNode.set(entry.node.id, bucket);
      }
    }

    return replacementsByNode;
  }

  function replaceAllLiteral(value, search, replacement) {
    const source = String(value || "");
    const token = String(search || "");
    if (!token) {
      return source;
    }

    return source.split(token).join(String(replacement || ""));
  }

  function applyPhraseHintSuggestion(text) {
    const normalizedText = normalizeLineEndings(text);
    const lines = normalizedText.split("\n");
    let changed = false;
    const nextLines = lines.map((line) => {
      const hint = getPhraseHintForLine(line);
      if (!hint) {
        return line;
      }

      changed = true;
      return hint.suggestion;
    });

    return changed ? nextLines.join("\n") : "";
  }

  function repairLatinTokensInText(text, languageFamily, latinLocaleHint) {
    if (languageFamily !== "latin") {
      return text;
    }

    const hintPool = getLatinTokenHintPool(latinLocaleHint);
    if (!hintPool.length) {
      return text;
    }

    const matches = Array.from(String(text || "").matchAll(/\b[A-Za-z\u00C0-\u024F][A-Za-z\u00C0-\u024F'-]{3,}\b/g));
    if (!matches.length) {
      return text;
    }

    let next = String(text || "");
    for (let index = matches.length - 1; index >= 0; index -= 1) {
      const match = matches[index];
      const token = match && typeof match[0] === "string" ? match[0] : "";
      const start = match && typeof match.index === "number" ? match.index : -1;
      if (!token || start < 0) {
        continue;
      }

      const bestHint = findBestLatinTokenHint(token, latinLocaleHint);
      if (!bestHint) {
        continue;
      }

      const replacement = applyLatinTokenCase(bestHint.canonical, token);
      if (!replacement || replacement === token) {
        continue;
      }

      next = next.slice(0, start) + replacement + next.slice(start + token.length);
    }

    return next;
  }

  async function loadFontsForTextNode(node) {
    if (!node || node.type !== "TEXT") {
      return;
    }

    const fontNames = collectEditableFontNames(node);
    for (const fontName of fontNames) {
      await loadFontWithCache(fontName);
    }
  }

  async function preloadFontsForTextNodes(textNodes, issuesByNode) {
    const fontNames = [];
    const seen = new Set();

    for (const node of Array.isArray(textNodes) ? textNodes : []) {
      if (!node || !node.id) {
        continue;
      }
      if (issuesByNode && !issuesByNode.has(node.id)) {
        continue;
      }

      const editableFontNames = collectEditableFontNames(node);
      for (const fontName of editableFontNames) {
        const key = getFontCacheKey(fontName);
        if (!key || seen.has(key)) {
          continue;
        }

        seen.add(key);
        fontNames.push({
          family: fontName.family,
          style: fontName.style,
        });
      }
    }

    for (const fontName of fontNames) {
      await loadFontWithCache(fontName);
    }
  }

  function getFontCacheKey(fontName) {
    if (!fontName || typeof fontName !== "object") {
      return "";
    }
    if (typeof fontName.family !== "string" || typeof fontName.style !== "string") {
      return "";
    }
    return `${fontName.family}::${fontName.style}`;
  }

  async function loadFontWithCache(fontName) {
    const key = getFontCacheKey(fontName);
    if (!key) {
      return;
    }

    let pending = loadedFontPromiseCache.get(key);
    if (!pending) {
      pending = figma.loadFontAsync({
        family: fontName.family,
        style: fontName.style,
      });
      loadedFontPromiseCache.set(key, pending);
    }

    try {
      await pending;
    } catch (error) {
      loadedFontPromiseCache.delete(key);
      throw error;
    }
  }

  function collectEditableFontNames(node) {
    const fontNames = [];
    const seen = new Set();
    const pushFont = (fontName) => {
      if (!fontName || typeof fontName !== "object" || typeof fontName.family !== "string" || typeof fontName.style !== "string") {
        return;
      }

      const key = `${fontName.family}::${fontName.style}`;
      if (seen.has(key)) {
        return;
      }

      seen.add(key);
      fontNames.push({
        family: fontName.family,
        style: fontName.style,
      });
    };

    if (typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string" && node.characters.length > 0) {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        for (const fontName of rangeFonts) {
          pushFont(fontName);
        }
      } catch (error) {}
    }

    if (fontNames.length === 0 && node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      pushFont(node.fontName);
    }

    return fontNames;
  }

  function summarizeIssueResults(issues) {
    if (!Array.isArray(issues)) {
      return [];
    }

    const results = [];
    for (const issue of issues) {
      for (const fragment of getIssueAnnotationFragments(issue)) {
        results.push(formatIssueResult(fragment));
      }
    }
    return results;
  }

  function formatIssueResult(issue) {
    return {
      id: issue && issue.node ? issue.node.id : "",
      nodeName: safeName(issue && issue.node),
      currentText: previewVisibleText(issue && issue.currentText ? issue.currentText : "", 72),
      suggestion: previewVisibleText(issue && issue.suggestion ? issue.suggestion : "", 72),
      reason: issue && issue.reason ? issue.reason : "",
      kind: issue && issue.kind ? issue.kind : "오타",
      source: issue && issue.source ? issue.source : "local",
    };
  }

  function buildAnnotation(issue, category) {
    const kindLabels = getAnnotationKindLabels(issue);
    const label = [
      `[Pigma Ai Audit] ${kindLabels.join(", ")}`,
      `전 : ${previewText(issue.currentText, 72)}`,
      `후 : ${previewText(issue.suggestion, 72)}`,
      `이유 : ${buildAnnotationReason(issue, kindLabels)}`,
    ].join("\n");

    return category && category.id
      ? {
          label,
          categoryId: category.id,
        }
      : {
          label,
        };
  }

  function isManagedAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }

    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }

    const label = typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return LEGACY_ANNOTATION_PREFIXES.some((prefix) => label.startsWith(prefix));
  }

  function getAnnotationKindLabels(issue) {
    const tokens = String(issue && issue.kind ? issue.kind : "")
      .split(/[,+/|]/)
      .map((token) => token.trim())
      .filter(Boolean);
    const resolved = [];

    for (const token of tokens) {
      const label = normalizeAnnotationKindLabel(token, issue);
      if (label && !resolved.includes(label)) {
        resolved.push(label);
      }
    }

    if (resolved.length > 0) {
      return resolved;
    }

    return [inferAnnotationKindFromIssue(issue)];
  }

  function normalizeAnnotationKindLabel(kind, issue) {
    const raw = String(kind || "").trim();
    if (!raw) {
      return "";
    }

    const compact = raw.replace(/\s+/g, "");
    const lower = compact.toLowerCase();

    if (
      compact.includes("띄어쓰기") ||
      compact.includes("공백") ||
      lower.includes("space") ||
      lower.includes("whitespace")
    ) {
      return "띄어쓰기";
    }

    if (compact.includes("문법") || lower.includes("grammar")) {
      return "문법";
    }

    if (compact.includes("문장부호") || lower.includes("punct")) {
      return "문장부호";
    }

    if (
      compact.includes("용어통일") ||
      compact.includes("표기통일") ||
      compact.includes("일관성") ||
      lower.includes("terminology") ||
      lower.includes("consisten")
    ) {
      return "용어 통일";
    }

    if (
      compact.includes("오타") ||
      compact.includes("철자") ||
      compact.includes("자동수정") ||
      compact.includes("안전교정") ||
      compact.includes("멀티라인보호") ||
      lower.includes("spell") ||
      lower.includes("typo")
    ) {
      return "철자";
    }

    return inferAnnotationKindFromIssue(issue);
  }

  function inferAnnotationKindFromIssue(issue) {
    const currentText = String(issue && issue.currentText ? issue.currentText : "");
    const suggestion = String(issue && issue.suggestion ? issue.suggestion : "");
    const currentNoSpace = currentText.replace(/\s+/g, "");
    const suggestionNoSpace = suggestion.replace(/\s+/g, "");
    if (currentNoSpace && currentNoSpace === suggestionNoSpace) {
      return "띄어쓰기";
    }

    const currentNoPunctuation = currentText.replace(/[.,!?;:'"()\-_[\]{}]/g, "");
    const suggestionNoPunctuation = suggestion.replace(/[.,!?;:'"()\-_[\]{}]/g, "");
    if (currentNoPunctuation && currentNoPunctuation === suggestionNoPunctuation) {
      return "문장부호";
    }

    return "철자";
  }

  function buildAnnotationReason(issue, kindLabels) {
    const explicitReason = String(issue && issue.reason ? issue.reason : "").trim();
    if (explicitReason) {
      return explicitReason;
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("용어 통일")) {
      return "같은 용어를 다른 말로 바꾸지 않도록 유지했습니다.";
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("띄어쓰기")) {
      return "띄어쓰기를 바로잡았습니다.";
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("문법")) {
      return "문법상 어색한 부분을 바로잡았습니다.";
    }

    if (Array.isArray(kindLabels) && kindLabels.includes("문장부호")) {
      return "문장부호를 바로잡았습니다.";
    }
    return "명백한 오타를 바로잡았습니다.";
  }

  function getIssueAnnotationFragments(issue) {
    if (!issue || !issue.node) {
      return [];
    }

    if (issue.annotationFragment === true) {
      return [issue];
    }

    if (Array.isArray(issue.annotationFragments) && issue.annotationFragments.length > 0) {
      return issue.annotationFragments;
    }

    const fragments = buildIssueAnnotationFragments(issue);
    issue.annotationFragments = fragments.length > 0 ? fragments : [createFallbackIssueFragment(issue)];
    return issue.annotationFragments;
  }

  function buildIssueAnnotationFragments(issue) {
    const currentText = normalizeLineEndings(issue && issue.currentText ? issue.currentText : "");
    const suggestion = normalizeLineEndings(issue && issue.suggestion ? issue.suggestion : "");
    if (!currentText && !suggestion) {
      return [];
    }

    const currentLines = currentText.split("\n");
    const suggestionLines = suggestion.split("\n");
    const fragments = [];

    if (currentLines.length === suggestionLines.length) {
      for (let index = 0; index < currentLines.length; index += 1) {
        if (currentLines[index] === suggestionLines[index]) {
          continue;
        }

        fragments.push(...buildPairIssueFragments(issue, currentLines[index], suggestionLines[index]));
      }
    } else {
      fragments.push(...buildPairIssueFragments(issue, currentText, suggestion));
    }

    return fragments.filter(Boolean);
  }

  function buildPairIssueFragments(issue, currentText, suggestion) {
    const currentValue = normalizeLineEndings(currentText);
    const suggestionValue = normalizeLineEndings(suggestion);
    if (currentValue === suggestionValue) {
      return [];
    }

    const clusters = buildDiffClusters(currentValue, suggestionValue);
    if (!clusters.length) {
      return [createFallbackIssueFragment(issue, currentValue, suggestionValue)];
    }

    return clusters.map((cluster, index) =>
      createIssueFragmentFromCluster(issue, currentValue, suggestionValue, cluster, index, clusters.length)
    );
  }

  function buildDiffClusters(currentText, suggestion) {
    const left = String(currentText || "");
    const right = String(suggestion || "");
    if (left === right) {
      return [];
    }

    if (left.length * right.length > 120000) {
      return [buildFallbackDiffCluster(left, right)];
    }

    const segments = buildDiffSegments(left, right);
    if (!segments.length) {
      return [buildFallbackDiffCluster(left, right)];
    }

    const clusters = [];
    let currentCluster = null;

    for (const segment of segments) {
      if (segment.type === "equal") {
        if (!currentCluster) {
          continue;
        }

        if (shouldMergeDiffBridge(segment.text)) {
          currentCluster.leftEnd = segment.leftEnd;
          currentCluster.rightEnd = segment.rightEnd;
          continue;
        }

        clusters.push(currentCluster);
        currentCluster = null;
        continue;
      }

      if (!currentCluster) {
        currentCluster = {
          leftStart: segment.leftStart,
          leftEnd: segment.leftEnd,
          rightStart: segment.rightStart,
          rightEnd: segment.rightEnd,
        };
        continue;
      }

      currentCluster.leftEnd = segment.leftEnd;
      currentCluster.rightEnd = segment.rightEnd;
    }

    if (currentCluster) {
      clusters.push(currentCluster);
    }

    return clusters.length ? clusters : [buildFallbackDiffCluster(left, right)];
  }

  function buildDiffSegments(currentText, suggestion) {
    const leftChars = String(currentText || "").split("");
    const rightChars = String(suggestion || "").split("");
    const rows = leftChars.length + 1;
    const cols = rightChars.length + 1;
    const table = Array.from({ length: rows }, () => new Array(cols).fill(0));

    for (let row = 0; row < rows; row += 1) {
      table[row][0] = row;
    }

    for (let col = 0; col < cols; col += 1) {
      table[0][col] = col;
    }

    for (let row = 1; row < rows; row += 1) {
      for (let col = 1; col < cols; col += 1) {
        if (leftChars[row - 1] === rightChars[col - 1]) {
          table[row][col] = table[row - 1][col - 1];
        } else {
          table[row][col] = Math.min(table[row - 1][col - 1], table[row - 1][col], table[row][col - 1]) + 1;
        }
      }
    }

    const raw = [];
    let row = leftChars.length;
    let col = rightChars.length;
    while (row > 0 || col > 0) {
      if (row > 0 && col > 0 && leftChars[row - 1] === rightChars[col - 1]) {
        raw.push({ type: "equal", leftText: leftChars[row - 1], rightText: rightChars[col - 1] });
        row -= 1;
        col -= 1;
        continue;
      }

      const replaceCost = row > 0 && col > 0 ? table[row - 1][col - 1] : Number.POSITIVE_INFINITY;
      const deleteCost = row > 0 ? table[row - 1][col] : Number.POSITIVE_INFINITY;
      const insertCost = col > 0 ? table[row][col - 1] : Number.POSITIVE_INFINITY;
      const bestCost = Math.min(replaceCost, deleteCost, insertCost);

      if (row > 0 && col > 0 && replaceCost === bestCost) {
        raw.push({ type: "replace", leftText: leftChars[row - 1], rightText: rightChars[col - 1] });
        row -= 1;
        col -= 1;
      } else if (row > 0 && deleteCost === bestCost) {
        raw.push({ type: "delete", leftText: leftChars[row - 1], rightText: "" });
        row -= 1;
      } else {
        raw.push({ type: "insert", leftText: "", rightText: rightChars[col - 1] });
        col -= 1;
      }
    }

    raw.reverse();

    const segments = [];
    let leftOffset = 0;
    let rightOffset = 0;
    for (const entry of raw) {
      const leftText = entry.leftText || "";
      const rightText = entry.rightText || "";
      const previous = segments[segments.length - 1];
      if (previous && previous.type === entry.type) {
        previous.leftText += leftText;
        previous.rightText += rightText;
        previous.leftEnd += leftText.length;
        previous.rightEnd += rightText.length;
        previous.text = previous.type === "equal" ? previous.leftText : "";
      } else {
        segments.push({
          type: entry.type,
          text: entry.type === "equal" ? leftText : "",
          leftText,
          rightText,
          leftStart: leftOffset,
          leftEnd: leftOffset + leftText.length,
          rightStart: rightOffset,
          rightEnd: rightOffset + rightText.length,
        });
      }

      leftOffset += leftText.length;
      rightOffset += rightText.length;
    }

    return segments;
  }

  function shouldMergeDiffBridge(value) {
    const text = String(value || "");
    return !!text && text.length <= 4 && !/[\s.,!?;:()[\]{}"'“”‘’<>]/.test(text);
  }

  function buildFallbackDiffCluster(currentText, suggestion) {
    const left = String(currentText || "");
    const right = String(suggestion || "");
    let prefix = 0;
    while (prefix < left.length && prefix < right.length && left[prefix] === right[prefix]) {
      prefix += 1;
    }

    let suffix = 0;
    while (
      suffix < left.length - prefix &&
      suffix < right.length - prefix &&
      left[left.length - suffix - 1] === right[right.length - suffix - 1]
    ) {
      suffix += 1;
    }

    return {
      leftStart: prefix,
      leftEnd: Math.max(prefix, left.length - suffix),
      rightStart: prefix,
      rightEnd: Math.max(prefix, right.length - suffix),
    };
  }

  function createIssueFragmentFromCluster(issue, currentText, suggestion, cluster, index, total) {
    const snippetCurrent = buildSnippetForCluster(currentText, cluster.leftStart, cluster.leftEnd);
    const snippetSuggestion = buildSnippetForCluster(suggestion, cluster.rightStart, cluster.rightEnd);
    const changeCurrent = currentText.slice(cluster.leftStart, cluster.leftEnd);
    const changeSuggestion = suggestion.slice(cluster.rightStart, cluster.rightEnd);
    return {
      node: issue.node,
      currentText: snippetCurrent,
      suggestion: snippetSuggestion,
      kind: issue.kind,
      reason: buildDetailedFragmentReason(issue, changeCurrent, changeSuggestion),
      source: issue.source || "local",
      annotationFragment: true,
      fragmentIndex: index,
      fragmentCount: total,
    };
  }

  function createFallbackIssueFragment(issue, currentText, suggestion) {
    const currentValue = typeof currentText === "string" ? currentText : String(issue && issue.currentText ? issue.currentText : "");
    const suggestionValue = typeof suggestion === "string" ? suggestion : String(issue && issue.suggestion ? issue.suggestion : "");
    return {
      node: issue.node,
      currentText: currentValue,
      suggestion: suggestionValue,
      kind: issue.kind,
      reason: buildDetailedFragmentReason(issue, currentValue, suggestionValue),
      source: issue.source || "local",
      annotationFragment: true,
      fragmentIndex: 0,
      fragmentCount: 1,
    };
  }

  function buildSnippetForCluster(text, start, end) {
    const value = normalizeLineEndings(text);
    const lineStart = value.lastIndexOf("\n", Math.max(0, start - 1)) + 1;
    const nextBreak = value.indexOf("\n", end);
    const lineEnd = nextBreak >= 0 ? nextBreak : value.length;
    if (lineEnd - lineStart <= 48) {
      return value.slice(lineStart, lineEnd);
    }

    const contextStart = findSnippetStart(value, start, lineStart);
    const contextEnd = findSnippetEnd(value, end, lineEnd);
    const prefix = contextStart > lineStart ? "..." : "";
    const suffix = contextEnd < lineEnd ? "..." : "";
    return `${prefix}${value.slice(contextStart, contextEnd)}${suffix}`;
  }

  function findSnippetStart(text, start, lineStart) {
    const min = Math.max(lineStart, start - 18);
    for (let index = start - 1; index >= min; index -= 1) {
      if (/\s/.test(text[index])) {
        return index + 1;
      }
    }

    return min;
  }

  function findSnippetEnd(text, end, lineEnd) {
    const max = Math.min(lineEnd, end + 18);
    for (let index = end; index < max; index += 1) {
      if (/\s/.test(text[index])) {
        return index;
      }
    }

    return max;
  }

  function buildDetailedFragmentReason(issue, currentText, suggestion) {
    const currentValue = String(currentText || "");
    const suggestionValue = String(suggestion || "");
    const kindLabels = getAnnotationKindLabels(issue);

    if (!currentValue && suggestionValue) {
      if (isWhitespaceOnly(suggestionValue) || kindLabels.includes("띄어쓰기")) {
        return `띄어쓰기 ${formatReasonToken(suggestionValue)} 추가`;
      }

      if (isPunctuationOnly(suggestionValue) || kindLabels.includes("문장부호")) {
        return `문장부호 ${formatReasonToken(suggestionValue)} 추가`;
      }

      return `오타 ${formatReasonToken(suggestionValue)} 소실`;
    }

    if (currentValue && !suggestionValue) {
      if (isWhitespaceOnly(currentValue) || kindLabels.includes("띄어쓰기")) {
        return `띄어쓰기 ${formatReasonToken(currentValue)} 제거`;
      }

      if (isPunctuationOnly(currentValue) || kindLabels.includes("문장부호")) {
        return `문장부호 ${formatReasonToken(currentValue)} 제거`;
      }

      return `불필요한 ${formatReasonToken(currentValue)} 제거`;
    }

    if (kindLabels.includes("용어 통일")) {
      return `표현 ${formatReasonToken(currentValue)} → ${formatReasonToken(suggestionValue)} 통일`;
    }

    if (kindLabels.includes("문법")) {
      return `문법 ${formatReasonToken(currentValue)} → ${formatReasonToken(suggestionValue)} 교정`;
    }

    if (
      kindLabels.includes("띄어쓰기") ||
      isWhitespaceOnly(currentValue) ||
      isWhitespaceOnly(suggestionValue) ||
      collapseWhitespace(currentValue) === collapseWhitespace(suggestionValue)
    ) {
      return `띄어쓰기 ${formatReasonToken(currentValue)} → ${formatReasonToken(suggestionValue)} 조정`;
    }

    if (kindLabels.includes("문장부호") || isPunctuationOnly(currentValue) || isPunctuationOnly(suggestionValue)) {
      return `문장부호 ${formatReasonToken(currentValue)} → ${formatReasonToken(suggestionValue)} 교정`;
    }

    return `오타 ${formatReasonToken(currentValue)} → ${formatReasonToken(suggestionValue)} 교정`;
  }

  function formatReasonToken(value) {
    const text = normalizeLineEndings(value);
    if (!text) {
      return "[empty]";
    }

    if (text === " ") {
      return "[ ]";
    }

    if (text === "\t") {
      return "[tab]";
    }

    if (text === "\n") {
      return "[\\n]";
    }

    return `[${previewText(text, 24)}]`;
  }

  function isWhitespaceOnly(value) {
    return !!String(value || "") && !/\S/.test(String(value || ""));
  }

  function isPunctuationOnly(value) {
    return !!String(value || "") && /^[.,!?;:()[\]{}"'“”‘’<>،؛؟。、，．！？：；-]+$/.test(String(value || ""));
  }

  function collapseWhitespace(value) {
    return String(value || "").replace(/\s+/g, "");
  }

  async function buildTypoIssues(textNodes, context, proofingSettings, options) {
    const issues = [];
    const seen = new Set();
    let aiError = "";
    const auditProfile = normalizeTypoAuditProfile(options && options.auditProfile);
    const useSpeedProfile = auditProfile === "speed";
    const aiPromise = useSpeedProfile ? null : requestAiTypoIssues(textNodes, context, proofingSettings);
    const localIssues = [];

    for (const node of textNodes) {
      const text = getTextValue(node);
      if (!text) {
        continue;
      }

      if (isWholeTextProtected(text, proofingSettings)) {
        continue;
      }

      for (const issue of collectRuleIssues(node, text, context)) {
        if (
          issueRespectsProofingTerms(issue.currentText, issue.suggestion, proofingSettings) &&
          changeMatchesCorrectionPolicy(issue.currentText, issue.suggestion, issue.kind)
        ) {
          localIssues.push(issue);
        }
      }
    }

    const aiResult = aiPromise
      ? await aiPromise
      : {
          issues: [],
          aiError: "",
          aiMeta: createAiMeta("local-only", "", "", "속도용 검수는 AI 호출 없이 로컬 규칙만 사용합니다."),
        };
    aiResult.issues = filterIssuesForProofing(aiResult.issues, proofingSettings);
    aiError = aiResult.aiError || "";
    const useAiPrimary = !useSpeedProfile && Array.isArray(aiResult.issues) && aiResult.issues.length > 0;
    const primaryIssues = useAiPrimary ? aiResult.issues : localIssues;
    const secondaryIssues = useAiPrimary ? localIssues : [];

    for (const issue of primaryIssues) {
      pushUniqueIssue(issues, seen, issue);
    }

    for (const issue of secondaryIssues) {
      pushUniqueIssue(issues, seen, issue);
    }

    return {
      issues: mergeIssuesByNode(issues),
      aiError,
      strategy: useSpeedProfile ? "speed-local" : useAiPrimary ? "ai-primary" : "local-fallback",
      aiMeta: aiResult.aiMeta || createAiMeta("unknown", "", "", ""),
    };
  }

  function pushUniqueIssue(target, seen, issue) {
    if (!issue || !issue.node || !issue.node.id) {
      return;
    }

    const key = `${issue.node.id}:${issue.suggestion}:${issue.reason}`;
    if (seen.has(key)) {
      return;
    }

    seen.add(key);
    target.push(issue);
  }

  function mergeIssuesByNode(issues) {
    if (!Array.isArray(issues) || !issues.length) {
      return [];
    }

    const grouped = new Map();
    for (const issue of issues) {
      if (!issue || !issue.node || !issue.node.id) {
        continue;
      }

      const bucket = grouped.get(issue.node.id) || [];
      bucket.push(issue);
      grouped.set(issue.node.id, bucket);
    }

    const merged = [];
    for (const nodeIssues of grouped.values()) {
      merged.push(mergeNodeIssues(nodeIssues));
    }

    return merged.filter(Boolean);
  }

  function mergeNodeIssues(nodeIssues) {
    const sourceIssues = Array.isArray(nodeIssues) ? nodeIssues.filter(Boolean) : [];
    if (!sourceIssues.length) {
      return null;
    }

    if (sourceIssues.length === 1) {
      return sourceIssues[0];
    }

    const baseIssue = sourceIssues[0];
    const baseText = normalizeLineEndings(baseIssue.currentText);
    if (!baseText) {
      return baseIssue;
    }

    const accepted = [];
    let order = 0;
    for (const issue of sourceIssues) {
      if (!issue || normalizeLineEndings(issue.currentText) !== baseText) {
        continue;
      }

      const clusters = buildDiffClusters(baseText, issue.suggestion);
      if (!clusters.length) {
        continue;
      }

      for (const cluster of clusters) {
        const candidate = {
          leftStart: cluster.leftStart,
          leftEnd: cluster.leftEnd,
          rightText: issue.suggestion.slice(cluster.rightStart, cluster.rightEnd),
          issue,
          order: order++,
        };
        if (hasConflictingMergedCluster(accepted, candidate)) {
          continue;
        }

        accepted.push(candidate);
      }
    }

    if (!accepted.length) {
      return baseIssue;
    }

    if (accepted.length === 1 && sourceIssues.length > 0) {
      return accepted[0].issue || baseIssue;
    }

    accepted.sort((left, right) => left.leftStart - right.leftStart || left.order - right.order);

    let mergedText = "";
    let cursor = 0;
    for (const entry of accepted) {
      mergedText += baseText.slice(cursor, entry.leftStart);
      mergedText += entry.rightText;
      cursor = entry.leftEnd;
    }
    mergedText += baseText.slice(cursor);

    if (!mergedText || mergedText === baseText) {
      return baseIssue;
    }

    const fragments = accepted
      .map((entry) => buildPairIssueFragments(entry.issue, baseText.slice(entry.leftStart, entry.leftEnd), entry.rightText)[0] || null)
      .filter(Boolean);
    const mergedIssue = createIssue(
      baseIssue.node,
      baseText,
      mergedText,
      accepted.some((entry) => entry.issue && entry.issue.kind === "문법") ? "문법" : baseIssue.kind,
      "같은 텍스트 노드의 여러 언어 교정 후보를 함께 병합했습니다.",
      accepted.some((entry) => entry.issue && entry.issue.source === "ai") ? "ai" : baseIssue.source || "local"
    );
    if (fragments.length > 0) {
      mergedIssue.annotationFragments = fragments;
    }

    return mergedIssue;
  }

  function hasConflictingMergedCluster(accepted, candidate) {
    for (const entry of accepted) {
      const leftOverlaps = candidate.leftStart < entry.leftEnd && entry.leftStart < candidate.leftEnd;
      const sameInsertionPoint =
        candidate.leftStart === candidate.leftEnd &&
        entry.leftStart === entry.leftEnd &&
        candidate.leftStart === entry.leftStart;
      if (leftOverlaps || sameInsertionPoint) {
        return true;
      }
    }

    return false;
  }

  function buildAiTypoCandidates(textNodes, proofingSettings) {
    const candidates = [];

    for (const node of textNodes) {
      const text = String(getTextValue(node) || "");
      if (!text) {
        continue;
      }

      if (isWholeTextProtected(text, proofingSettings)) {
        continue;
      }

      for (const candidate of splitTextNodeIntoAiTypoCandidates(node, text)) {
        candidates.push(candidate);
      }
    }

    return candidates;
  }

  function splitTextNodeIntoAiTypoCandidates(node, text) {
    const currentText = normalizeLineEndings(text);
    const lines = currentText.split("\n");
    const nonEmptyLineIndexes = [];

    for (let index = 0; index < lines.length; index += 1) {
      if (compactText(lines[index])) {
        nonEmptyLineIndexes.push(index);
      }
    }

    if (nonEmptyLineIndexes.length <= 1) {
      return [
        {
          id: node.id,
          node,
          nodeId: node.id,
          name: safeName(node),
          text: currentText,
          currentText,
          scope: "node",
        },
      ];
    }

    return nonEmptyLineIndexes.map((lineIndex) => ({
      id: `${node.id}::line:${lineIndex}`,
      node,
      nodeId: node.id,
      name: safeName(node),
      text: lines[lineIndex],
      currentText,
      scope: "line",
      lineIndex,
    }));
  }

  function applyAiTypoCandidateSuggestion(candidate, suggestion) {
    if (!candidate || typeof candidate !== "object") {
      return "";
    }

    const currentText = normalizeLineEndings(candidate.currentText || "");
    const suggestionText = normalizeLineEndings(suggestion);
    if (!currentText || !suggestionText) {
      return "";
    }

    if (candidate.scope !== "line") {
      return suggestionText;
    }

    if (countTextLines(suggestionText) !== 1 || typeof candidate.lineIndex !== "number") {
      return "";
    }

    const lines = currentText.split("\n");
    if (candidate.lineIndex < 0 || candidate.lineIndex >= lines.length) {
      return "";
    }

    lines[candidate.lineIndex] = suggestionText;
    return lines.join("\n");
  }

  async function requestAiTypoIssues(textNodes, context, proofingSettings) {
    const ai = getAiHelper();
    if (!ai) {
      return { issues: [], aiError: "", aiMeta: createAiMeta("unavailable", "", "", "AI helper unavailable") };
    }

    let configured = false;
    let runInfo = { provider: "", model: "" };
    try {
      configured = await ai.hasConfiguredAiAsync();
      if (typeof ai.getAiSettingsAsync === "function" && typeof ai.getResolvedRunInfo === "function") {
        const settings = await ai.getAiSettingsAsync();
        runInfo = ai.getResolvedRunInfo(settings, { modelByProvider: TYPO_AUDIT_MODEL_BY_PROVIDER });
      }
    } catch (error) {
      configured = false;
    }

    if (!configured) {
      return {
        issues: [],
        aiError: "AI 설정이 꺼져 있거나 API 키가 없어 로컬 규칙만 사용했습니다.",
        aiMeta: createAiMeta("not-configured", runInfo.provider, runInfo.model, "AI 설정이 비어 있습니다."),
      };
    }

    const candidates = buildAiTypoCandidates(textNodes, proofingSettings);

    if (!candidates.length) {
      return {
        issues: [],
        aiError: "",
        aiMeta: createAiMeta("skipped", runInfo.provider, runInfo.model, "검수할 텍스트 후보가 없습니다."),
      };
    }

    const schema = {
      type: "object",
      properties: {
        issues: {
          type: "array",
          items: {
            type: "object",
            properties: {
              id: { type: "string" },
              suggestion: { type: "string" },
              reason: { type: "string" },
              kind: { type: "string" },
            },
            required: ["id", "suggestion", "reason", "kind"],
          },
        },
      },
      required: ["issues"],
    };
    const instructions =
      "You review UI copy inside a Figma plugin. Detect only obvious spelling mistakes, broken words, malformed polite endings, spacing errors, punctuation errors, and clearly broken grammar. Be conservative: if a word is already a valid standard word in its language, keep it unless the surrounding sentence is clearly wrong with that exact word. Never replace one valid word with another valid word just because the spellings are similar. Example: keep 'optimal motion' unchanged and never change it to 'optional motion'. Preserve the original language, script, wording, product naming, and marketing intent of each string. Do not translate, localize, rewrite into the dominant language of the screen, unify terminology, smooth tone, or replace brand/product names unless the product name itself has an obvious typo. In mixed-language strings, preserve every language segment as-is and only fix the clearly broken part inside that same language segment. Some textNodes are full node strings, while others are single extracted lines from a multiline node. If an item is a single extracted line, return a corrected single line only and do not rewrite neighboring lines. If preferredLocaleHint is provided, use it only for Latin-script nodes or segments that actually fit that locale. Do not suppress Korean, English, or other script corrections just because another preferred locale is set. If latinLocaleHint is provided, keep corrections inside that locale only for matching Latin-script segments. Ignore cosmetic whitespace cleanup such as empty brackets, bracket spacing, or punctuation-only cleanup. Only flag spacing when a word is visibly broken into fragments, for example 'app le' -> 'apple'. Example: German copy with an English brand should stay mixed as needed: 'Find e deine perfekte Work-Life-Balanfce mit LG' -> 'Finde deine perfekte Work-Life-Balance mit LG', 'Jedtazt kaufenq' -> 'Jetzt kaufen', while keeping 'LG' untouched. Return the full corrected string in suggestion for the given item, never only a changed token. If the original text contains multiple lines, preserve every line and keep the same line count unless the user text itself clearly intends a merge. Never drop unrelated lower lines. Never edit any protectedTerms under any circumstances. Never mark userDictionary terms as mistakes. Example: '방갑스비난.' should be corrected to the full string '반갑습니다.' If the text looks fine, omit it. Return concise reasons in Korean when possible.";
    const payload = {
      languageHint: context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지",
      preferredLocaleHint: context.proofingLocale || "",
      preferredLocaleLabel: context.proofingLocaleAiLabel || context.proofingLocaleLabel || "",
      languageFamilyHint: detectLanguageFamilyLabel(context.detectedLanguageLabel || context.languageLabel || ""),
      contextLabel: context.contextLabel,
      latinLocaleHint: context.latinLocaleHint || inferLatinLocaleHint(candidates.map((entry) => entry.text).join("\n")),
      userDictionary: proofingSettings ? proofingSettings.userDictionary : [],
      protectedTerms: proofingSettings ? proofingSettings.protectedTerms : [],
      textNodes: candidates.slice(0, 48).map((entry) => ({
        id: entry.id,
        name: entry.name,
        text: entry.text,
        lineCount: countTextLines(entry.text),
        lines: String(entry.text || "").split(/\r?\n/),
        scriptFamilies: getActiveScriptFamilies(entry.text),
        mixedLanguage: getActiveScriptFamilies(entry.text).length > 1,
        latinLocaleHint: context.latinLocaleHint || inferLatinLocaleHint(entry.text),
        segmentScope: entry.scope || "node",
        lineIndex: typeof entry.lineIndex === "number" ? entry.lineIndex : -1,
      })),
    };

    try {
      let response = null;

      try {
        response = await ai.requestJsonTask({
        instructions: instructions ||
          "You review UI copy inside a Figma plugin. Detect only obvious spelling mistakes, broken words, malformed polite endings, spacing errors, punctuation errors, and clearly broken grammar. Preserve the original language, script, wording, and product naming of each string. Do not translate, localize, rewrite into the dominant language of the screen, unify terminology, smooth tone, or replace brand/product names unless the product name itself has an obvious typo. In mixed-language strings, preserve every language segment as-is and only fix the clearly broken part inside that same language segment. If preferredLocaleHint is provided, treat it as the primary locale/country hint. If latinLocaleHint is provided, keep corrections inside that locale. Ignore cosmetic whitespace cleanup such as empty brackets, bracket spacing, or punctuation-only cleanup. Only flag spacing when a word is visibly broken into fragments, for example 'app le' -> 'apple'. Example: German copy with an English brand should stay mixed as needed: 'Find e deine perfekte Work-Life-Balanfce mit LG' -> 'Finde deine perfekte Work-Life-Balance mit LG', 'Jedtazt kaufenq' -> 'Jetzt kaufen', while keeping 'LG' untouched. Return the full corrected string in suggestion, never only a changed token. If the original text contains multiple lines, preserve every line and keep the same line count unless the user text itself clearly intends a merge. Never drop unrelated lower lines. Never edit any protectedTerms under any circumstances. Never mark userDictionary terms as mistakes. Example: '방갑스비난.' should be corrected to the full string '반갑습니다.' If the text looks fine, omit it. Return concise reasons in Korean when possible.",
        schema: schema,
        payload: payload || {
          languageHint:
            context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지",
          preferredLocaleHint: context.proofingLocale || "",
          preferredLocaleLabel: context.proofingLocaleAiLabel || context.proofingLocaleLabel || "",
          languageFamilyHint: detectLanguageFamilyLabel(context.detectedLanguageLabel || context.languageLabel || ""),
          contextLabel: context.contextLabel,
          latinLocaleHint: context.latinLocaleHint || inferLatinLocaleHint(candidates.map((entry) => entry.text).join("\n")),
          userDictionary: proofingSettings ? proofingSettings.userDictionary : [],
          protectedTerms: proofingSettings ? proofingSettings.protectedTerms : [],
          textNodes: candidates.slice(0, 48).map((entry) => ({
            id: entry.id,
            name: entry.name,
            text: entry.text,
            lineCount: countTextLines(entry.text),
            lines: String(entry.text || "").split(/\r?\n/),
            scriptFamilies: getActiveScriptFamilies(entry.text),
            mixedLanguage: getActiveScriptFamilies(entry.text).length > 1,
            latinLocaleHint: context.latinLocaleHint || inferLatinLocaleHint(entry.text),
            segmentScope: entry.scope || "node",
            lineIndex: typeof entry.lineIndex === "number" ? entry.lineIndex : -1,
          })),
        },
        modelByProvider: TYPO_AUDIT_MODEL_BY_PROVIDER,
      });
      } catch (error) {
        if (!shouldRetryTypoAuditWithDefaultModel(error)) {
          throw error;
        }

        response = await ai.requestJsonTask({
          instructions,
          schema,
          payload,
        });
      }

      const rows = response && Array.isArray(response.issues) ? response.issues : [];
      const candidateMap = new Map();
      for (const candidate of candidates) {
        candidateMap.set(candidate.id, candidate);
      }

      const results = [];
      for (const row of rows) {
        if (!row || typeof row !== "object" || typeof row.id !== "string" || typeof row.suggestion !== "string") {
          continue;
        }

        const candidate = candidateMap.get(row.id);
        if (!candidate || !candidate.node) {
          continue;
        }

        const node = candidate.node;
        const currentText = normalizeLineEndings(candidate.currentText || "");
        const suggestion = applyAiTypoCandidateSuggestion(candidate, String(row.suggestion || ""));
        if (!currentText || !suggestion || compactText(currentText) === compactText(suggestion)) {
          continue;
        }

        const kind = typeof row.kind === "string" && row.kind.trim() ? row.kind.trim() : "AI 검수";
        if (!changeMatchesCorrectionPolicy(currentText, suggestion, kind)) {
          continue;
        }

        results.push(createIssue(node, currentText, suggestion, kind, typeof row.reason === "string" && row.reason.trim() ? row.reason.trim() : "AI 검수 후보", "ai"));
      }

      return {
        issues: results,
        aiError: "",
        aiMeta: createAiMeta("success", response && response._provider ? response._provider : runInfo.provider, response && response._model ? response._model : runInfo.model, ""),
      };
    } catch (error) {
      const message = normalizeErrorMessage(error, "AI 오타 검수 호출에 실패했습니다.");
      return {
        issues: [],
        aiError: message,
        aiMeta: createAiMeta("error", runInfo.provider, runInfo.model, message),
      };
    }
  }

  function shouldRetryTypoAuditWithDefaultModel(error) {
    const compact = String(normalizeErrorMessage(error, "") || "")
      .toLowerCase()
      .replace(/\s+/g, " ")
      .trim();
    if (!compact || !compact.includes("model")) {
      return false;
    }

    return (
      compact.includes("does not exist") ||
      compact.includes("not found") ||
      compact.includes("unsupported") ||
      compact.includes("not supported") ||
      compact.includes("invalid model") ||
      compact.includes("permission") ||
      compact.includes("access") ||
      compact.includes("not available") ||
      compact.includes("unavailable")
    );
  }

  function createAiMeta(status, provider, model, error) {
    const normalizedStatus = String(status || "unknown");
    const providerLabel = provider ? String(provider) : "provider 미확인";
    const modelLabel = model ? String(model) : "model 미확인";
    let statusLabel = "AI 상태 미확인";

    if (normalizedStatus === "success") {
      statusLabel = "AI 호출 성공";
    } else if (normalizedStatus === "error") {
      statusLabel = "AI 호출 실패";
    } else if (normalizedStatus === "not-configured") {
      statusLabel = "AI 설정 없음";
    } else if (normalizedStatus === "skipped") {
      statusLabel = "AI 호출 건너뜀";
    } else if (normalizedStatus === "local-only") {
      statusLabel = "AI 호출 안 함";
    } else if (normalizedStatus === "unavailable") {
      statusLabel = "AI helper 없음";
    }

    return {
      status: normalizedStatus,
      statusLabel,
      providerLabel,
      modelLabel,
      error: error ? String(error) : "",
    };
  }

  function collectRuleIssues(node, text, context) {
    const issues = [];
    const normalizedText = String(text || "");
    const languageFamily = detectLanguageFamilyFromText(normalizedText) || detectLanguageFamilyLabel(context && context.detectedLanguageLabel);

    const singleSpace = normalizeInlineWhitespace(normalizedText);
    if (singleSpace !== normalizedText) {
      issues.push(createIssue(node, normalizedText, singleSpace, "중복 공백", "연속된 공백을 한 칸으로 바로잡았습니다."));
    }

    const punctuationNormalized = normalizePunctuationSpacing(normalizedText, languageFamily);
    if (punctuationNormalized !== normalizedText) {
      issues.push(
        createIssue(
          node,
          normalizedText,
          punctuationNormalized,
          "문장부호 간격",
          getPunctuationSpacingReason(languageFamily)
        )
      );
    }

    const noSpaceScriptNormalized = normalizeNoSpaceScriptSpacing(normalizedText, languageFamily);
    if (noSpaceScriptNormalized !== normalizedText) {
      issues.push(
        createIssue(node, normalizedText, noSpaceScriptNormalized, "공백 정리", getNoSpaceScriptReason(languageFamily))
      );
    }

    const repeatedPunctuation = normalizeRepeatedPunctuation(normalizedText);
    if (repeatedPunctuation !== normalizedText) {
      issues.push(
        createIssue(node, normalizedText, repeatedPunctuation, "반복 문장부호", "반복된 문장부호를 바로잡았습니다.")
      );
    }

    for (const issue of collectTypoReplacementIssues(node, normalizedText)) {
      issues.push(issue);
    }

    for (const issue of collectPhraseHintIssues(node, normalizedText)) {
      issues.push(issue);
    }

    for (const issue of collectLatinTokenIssues(node, normalizedText, languageFamily, context && context.latinLocaleHint)) {
      issues.push(issue);
    }

    return issues;
  }

  function collectPhraseHintIssues(node, text) {
    const issues = [];
    const normalizedText = normalizeLineEndings(text);
    const lines = normalizedText.split("\n");
    for (let index = 0; index < lines.length; index += 1) {
      const hint = getPhraseHintForLine(lines[index]);
      if (!hint) {
        continue;
      }

      const nextLines = lines.slice();
      nextLines[index] = hint.suggestion;
      issues.push(createIssue(node, normalizedText, nextLines.join("\n"), hint.kind, hint.reason));
    }

    return issues;
  }

  function getPhraseHintForLine(text) {
    const compactHangul = String(text || "")
      .replace(/[.!?,;:~"'`()\[\]{}<>]/g, "")
      .replace(/\s+/g, "")
      .trim();
    if (!compactHangul || !/^[가-힣]+$/.test(compactHangul)) {
      return null;
    }

    for (const hint of TYPO_PHRASE_HINTS) {
      const canonical = String(hint.canonical || "").trim();
      if (!canonical || compactHangul === canonical) {
        continue;
      }

      if (Math.abs(compactHangul.length - canonical.length) > 2) {
        continue;
      }

      const distance = getEditDistance(compactHangul, canonical);
      if (distance > hint.maxDistance) {
        continue;
      }

      return hint;
    }

    return null;
  }

  function getEditDistance(left, right) {
    const a = String(left || "");
    const b = String(right || "");
    const rows = a.length + 2;
    const cols = b.length + 2;
    const maxDistance = a.length + b.length;
    const table = Array.from({ length: rows }, () => new Array(cols).fill(0));
    const lastSeen = new Map();

    table[0][0] = maxDistance;
    for (let row = 0; row <= a.length; row += 1) {
      table[row + 1][0] = maxDistance;
      table[row + 1][1] = row;
    }

    for (let col = 0; col <= b.length; col += 1) {
      table[0][col + 1] = maxDistance;
      table[1][col + 1] = col;
    }

    for (let row = 1; row <= a.length; row += 1) {
      let lastMatchColumn = 0;
      for (let col = 1; col <= b.length; col += 1) {
        const previousRow = lastSeen.get(b[col - 1]) || 0;
        const previousColumn = lastMatchColumn;
        const cost = a[row - 1] === b[col - 1] ? 0 : 1;
        if (cost === 0) {
          lastMatchColumn = col;
        }

        table[row + 1][col + 1] = Math.min(
          table[row][col] + cost,
          table[row + 1][col] + 1,
          table[row][col + 1] + 1,
          table[previousRow][previousColumn] + (row - previousRow - 1) + 1 + (col - previousColumn - 1)
        );
      }

      lastSeen.set(a[row - 1], row);
    }

    return table[a.length + 1][b.length + 1];
  }

  function collectLatinTokenIssues(node, text, languageFamily, latinLocaleHint) {
    if (languageFamily !== "latin") {
      return [];
    }

    const hintPool = getLatinTokenHintPool(latinLocaleHint);
    if (!hintPool.length) {
      return [];
    }

    const issues = [];
    const value = String(text || "");
    const matches = value.matchAll(/\b[A-Za-z\u00C0-\u024F][A-Za-z\u00C0-\u024F'-]{3,}\b/g);

    for (const match of matches) {
      const token = match && typeof match[0] === "string" ? match[0] : "";
      const index = match && typeof match.index === "number" ? match.index : -1;
      if (!token || index < 0) {
        continue;
      }

      const bestHint = findBestLatinTokenHint(token, latinLocaleHint);
      if (!bestHint) {
        continue;
      }

      const replacement = applyLatinTokenCase(bestHint.canonical, token);
      if (!replacement || replacement === token) {
        continue;
      }

      const suggestion = value.slice(0, index) + replacement + value.slice(index + token.length);
      issues.push(
        createIssue(
          node,
          value,
          suggestion,
          "Latin spelling",
          `라틴 문자 UI 단어 '${token}'가 '${replacement}'의 오타처럼 보여 후보로 표시했습니다.`
        )
      );
    }

    return issues;
  }

  function findBestLatinTokenHint(token, latinLocaleHint) {
    const normalizedToken = normalizeLatinTokenForLookup(token);
    if (!normalizedToken || normalizedToken.length < 4) {
      return null;
    }

    const hintPool = getLatinTokenHintPool(latinLocaleHint);
    if (!hintPool.length) {
      return null;
    }

    let best = null;
    let second = null;
    for (const canonical of hintPool) {
      const normalizedCanonical = normalizeLatinTokenForLookup(canonical);
      if (!normalizedCanonical || normalizedCanonical === normalizedToken) {
        continue;
      }

      if (normalizedCanonical[0] !== normalizedToken[0]) {
        continue;
      }

      if (Math.abs(normalizedCanonical.length - normalizedToken.length) > 2) {
        continue;
      }

      const distance = getEditDistance(normalizedToken, normalizedCanonical);
      const threshold = normalizedToken.length >= 7 ? 2 : 1;
      if (distance > threshold) {
        continue;
      }

      if (
        distance === 2 &&
        normalizedToken.length < 7 &&
        normalizedCanonical.slice(0, 2) !== normalizedToken.slice(0, 2)
      ) {
        continue;
      }

      const candidate = {
        canonical,
        normalizedCanonical,
        distance,
      };

      if (!best || candidate.distance < best.distance) {
        second = best;
        best = candidate;
      } else if (!second || candidate.distance < second.distance) {
        second = candidate;
      }
    }

    if (!best) {
      return null;
    }

    if (second && second.distance === best.distance && second.normalizedCanonical !== best.normalizedCanonical) {
      return null;
    }

    return best;
  }

  function getLatinTokenHintPool(latinLocaleHint) {
    const normalizedHint = String(latinLocaleHint || "").trim().toLowerCase();
    if (!normalizedHint) {
      return [];
    }

    return Object.prototype.hasOwnProperty.call(LATIN_UI_TOKEN_HINTS_BY_LOCALE, normalizedHint)
      ? LATIN_UI_TOKEN_HINTS_BY_LOCALE[normalizedHint]
      : [];
  }

  function resolveLatinTokenLocaleHint(context, text) {
    const contextHint = String(context && context.latinLocaleHint ? context.latinLocaleHint : "").trim().toLowerCase();
    if (contextHint) {
      return contextHint;
    }

    return inferLatinLocaleHint(text);
  }

  function normalizeLatinTokenForLookup(value) {
    return String(value || "")
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/['’-]/g, "")
      .toLowerCase()
      .trim();
  }

  function inferLatinLocaleHint(text) {
    const normalized = normalizeLatinTokenForLookup(text).replace(/[^a-z0-9\s]/g, " ");
    if (!normalized) {
      return "";
    }

    const tokens = normalized.split(/\s+/).filter(Boolean);
    if (!tokens.length) {
      return "";
    }

    const locales = [
      ["german", ["der", "die", "das", "und", "mit", "deine", "dein", "finde", "jetzt", "kaufen", "angebot", "gilt", "solange", "vorrat", "reicht", "februar", "monatlich", "monatliche"]],
      ["french", ["bonjour", "continuer", "annuler", "rechercher", "parametres", "profil", "enregistrer", "fermer"]],
      ["spanish", ["hola", "continuar", "cancelar", "buscar", "correo", "usuario", "guardar", "cerrar"]],
      ["portuguese", ["ola", "pesquisar", "configuracoes", "senha", "endereco", "fechar", "salvar"]],
      ["italian", ["ciao", "continua", "annulla", "conferma", "ricerca", "impostazioni", "profilo", "salva", "chiudi"]],
      ["dutch", ["welkom", "doorgaan", "annuleren", "zoeken", "instellingen", "profiel", "opslaan", "sluiten"]],
      ["english", ["welcome", "continue", "cancel", "search", "settings", "profile", "account", "password", "download", "upload", "monthly", "find"]],
    ];

    let bestLocale = "";
    let bestScore = 0;
    for (const [locale, keywords] of locales) {
      let score = 0;
      for (const keyword of keywords) {
        if (tokens.includes(keyword)) {
          score += 1;
        }
      }

      if (score > bestScore) {
        bestLocale = locale;
        bestScore = score;
      }
    }

    return bestScore >= 2 ? bestLocale : "";
  }

  function applyLatinTokenCase(canonical, original) {
    const value = String(canonical || "");
    const sample = String(original || "");
    if (!value) {
      return value;
    }

    if (sample === sample.toUpperCase()) {
      return value.toUpperCase();
    }

    if (sample[0] && sample[0] === sample[0].toUpperCase()) {
      return value.charAt(0).toUpperCase() + value.slice(1);
    }

    return value;
  }

  function normalizePunctuationSpacing(text, languageFamily) {
    const value = String(text || "");
    if (!value) {
      return value;
    }

    if (languageFamily === "japanese" || languageFamily === "cjk") {
      return value
        .replace(/[^\S\r\n]+([、。，．！？：；])/g, "$1")
        .replace(/([（【「『《〈])[^\S\r\n]+/g, "$1")
        .replace(/[^\S\r\n]+([）】」』》〉])/g, "$1");
    }

    if (languageFamily === "arabic") {
      return value
        .replace(/[^\S\r\n]+([،؛؟!.,:;])/g, "$1")
        .replace(/([(\[])[^\S\r\n]+/g, "$1")
        .replace(/[^\S\r\n]+([)\]])/g, "$1");
    }

    return value
      .replace(/[^\S\r\n]+([,.;:!?])/g, "$1")
      .replace(/([(\[])[^\S\r\n]+/g, "$1")
      .replace(/[^\S\r\n]+([)\]])/g, "$1");
  }

  function normalizeNoSpaceScriptSpacing(text, languageFamily) {
    const value = String(text || "");
    if (!value) {
      return value;
    }

    if (languageFamily === "japanese") {
      return value.replace(/([\u3040-\u30ff\u3400-\u9fff])[^\S\r\n]+([\u3040-\u30ff\u3400-\u9fff])/g, "$1$2");
    }

    if (languageFamily === "cjk") {
      return value.replace(/([\u3400-\u9fff])[^\S\r\n]+([\u3400-\u9fff])/g, "$1$2");
    }

    if (languageFamily === "thai") {
      return value.replace(/([\u0E00-\u0E7F])[^\S\r\n]+([\u0E00-\u0E7F])/g, "$1$2");
    }

    return value;
  }

  function normalizeRepeatedPunctuation(text) {
    return String(text || "")
      .replace(/([!?.,])\1{2,}/g, "$1$1")
      .replace(/([！？。、，．])\1{2,}/g, "$1$1")
      .replace(/([،؛؟])\1{2,}/g, "$1$1");
  }

  function normalizeInlineWhitespace(text) {
    return String(text || "").replace(/[^\S\r\n]{2,}/g, " ");
  }

  function applyTypoReplacementRules(text) {
    return mapTextLines(text, (line) => {
      let next = line;
      for (const rule of TYPO_REPLACEMENT_RULES) {
        next = next.replace(cloneRegex(rule.pattern), rule.replacement);
      }
      return next;
    });
  }

  function collectTypoReplacementIssues(node, text) {
    const issues = [];
    const normalizedText = normalizeLineEndings(text);
    const lines = normalizedText.split("\n");
    for (let lineIndex = 0; lineIndex < lines.length; lineIndex += 1) {
      const line = lines[lineIndex];
      for (const rule of TYPO_REPLACEMENT_RULES) {
        const suggestionLine = line.replace(cloneRegex(rule.pattern), rule.replacement);
        if (suggestionLine === line) {
          continue;
        }

        const nextLines = lines.slice();
        nextLines[lineIndex] = suggestionLine;
        issues.push(createIssue(node, normalizedText, nextLines.join("\n"), rule.kind, rule.reason));
      }
    }

    return issues;
  }

  function cloneRegex(pattern) {
    return pattern instanceof RegExp ? new RegExp(pattern.source, pattern.flags) : new RegExp(String(pattern || ""));
  }

  function mapTextLines(text, iteratee) {
    const lines = normalizeLineEndings(text).split("\n");
    return lines.map((line, index) => iteratee(line, index)).join("\n");
  }

  function getPunctuationSpacingReason(languageFamily) {
    if (languageFamily === "japanese" || languageFamily === "cjk") {
      return "동아시아 문장부호 앞뒤 공백이 어색해서 정리 후보로 표시했습니다.";
    }

    if (languageFamily === "arabic") {
      return "아랍 문자권 문장부호 앞뒤 공백이 어색해서 정리 후보로 표시했습니다.";
    }

    return "문장부호 앞뒤 간격이 어색해서 정리 후보로 표시했습니다.";
  }

  function getNoSpaceScriptReason(languageFamily) {
    if (languageFamily === "japanese") {
      return "일본어 문장 안의 불필요한 공백이 보여 정리 후보로 표시했습니다.";
    }

    if (languageFamily === "cjk") {
      return "중국어/한자권 문장 안의 불필요한 공백이 보여 정리 후보로 표시했습니다.";
    }

    if (languageFamily === "thai") {
      return "태국어 문장 안의 불필요한 공백이 보여 정리 후보로 표시했습니다.";
    }

    return "문장 안의 불필요한 공백이 보여 정리 후보로 표시했습니다.";
  }

  function collectConsistencyIssues(textNodes) {
    const issues = [];
    for (const rule of CONSISTENCY_RULES) {
      const matched = [];
      for (const node of textNodes) {
        const text = compactText(getTextValue(node));
        if (!text) {
          continue;
        }

        const variant = rule.variants.find((entry) => text.includes(entry));
        if (variant) {
          matched.push({ node, text, variant });
        }
      }

      const hasCanonical = matched.some((entry) => entry.variant === rule.canonical);
      if (!hasCanonical) {
        continue;
      }

      for (const entry of matched) {
        if (entry.variant === rule.canonical) {
          continue;
        }

        issues.push(
          createIssue(
            entry.node,
            entry.text,
            entry.text.replace(entry.variant, rule.canonical),
            "용어 통일",
            rule.reason
          )
        );
      }
    }

    return issues;
  }

  function createIssue(node, currentText, suggestion, kind, reason, source) {
    return {
      node,
      currentText: String(currentText || ""),
      suggestion: String(suggestion || ""),
      kind,
      reason,
      source: source || "local",
    };
  }

  function buildInsights(context, textNodes, issues, applied, usesAnnotations, aiMeta) {
    const insights = [
      `맥락 기준: ${context.contextLabel}`,
      `언어 기준: ${context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지"}`,
      `텍스트 레이어 ${textNodes.length}개 검사`,
      usesAnnotations ? `주석 ${applied.annotationCount}건 적용` : "주석 대신 결과 패널에만 표시",
    ];

    if (aiMeta && aiMeta.statusLabel) {
      insights.push(`AI 상태: ${aiMeta.statusLabel}${aiMeta.providerLabel ? ` · ${aiMeta.providerLabel}` : ""}`);
    }

    if (issues.length === 0) {
      insights.push(
        usesAnnotations
          ? "뚜렷한 오타 후보를 찾지 못해 기존 AI 오타 주석만 정리했습니다."
          : "뚜렷한 오타 후보를 찾지 못했습니다."
      );
      return insights;
    }

    const firstIssue = issues[0];
    insights.push(`대표 후보: ${previewText(firstIssue.currentText, 32)} -> ${previewText(firstIssue.suggestion, 32)}`);
    insights.push(`가장 많은 규칙: ${summarizeKinds(issues)}`);

    if (applied.skipped.length > 0) {
      insights.push(`일부 주석은 구조 제한으로 건너뜀 ${applied.skipped.length}건`);
    } else if (usesAnnotations) {
      insights.push("Figma 주석은 Dev Mode에서 확인할 수 있습니다.");
    }

    return insights;
  }

  function buildFixInsights(context, textNodes, issues, applied, annotationApplied, aiMeta) {
    const insights = [
      `맥락 기준: ${context.contextLabel}`,
      `언어 기준: ${context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지"}`,
      `텍스트 레이어 ${textNodes.length}개 검사`,
      `직접 수정 ${applied.appliedCount}개 적용`,
    ];

    if (annotationApplied && typeof annotationApplied.annotationCount === "number") {
      insights.push(
        annotationApplied.annotationCount > 0
          ? `수정된 텍스트에 주석 ${annotationApplied.annotationCount}건 적용`
          : "수정된 텍스트 주석은 없거나 현재 환경에서 지원되지 않았습니다."
      );
    }

    if (aiMeta && aiMeta.statusLabel) {
      insights.push(`AI 상태: ${aiMeta.statusLabel}${aiMeta.providerLabel ? ` · ${aiMeta.providerLabel}` : ""}`);
    }

    if (issues.length === 0) {
      insights.push("뚜렷한 오타 후보를 찾지 못했습니다.");
      return insights;
    }

    const firstApplied = applied.applied[0];
    if (firstApplied) {
      insights.push(`대표 수정: ${previewText(firstApplied.currentText, 32)} -> ${previewText(firstApplied.suggestion, 32)}`);
    } else {
      const firstIssue = issues[0];
      insights.push(`대표 후보: ${previewText(firstIssue.currentText, 32)} -> ${previewText(firstIssue.suggestion, 32)}`);
    }

    insights.push(`가장 많은 규칙: ${summarizeKinds(issues)}`);

    const skippedCount =
      (applied && Array.isArray(applied.skipped) ? applied.skipped.length : 0) +
      (annotationApplied && Array.isArray(annotationApplied.skipped) ? annotationApplied.skipped.length : 0);
    if (skippedCount > 0) {
      insights.push(`일부 텍스트는 구조 제한으로 건너뜀 ${skippedCount}건`);
    } else if (applied.appliedCount > 0) {
      insights.push("현재 선택 텍스트에 직접 반영했습니다.");
    }

    return insights;
  }

  function buildClearInsights(context, textNodes, applied, annotationSupported) {
    const insights = [
      `맥락 기준: ${context.contextLabel}`,
      `언어 기준: ${context.languageHintLabel || context.detectedLanguageLabel || context.languageLabel || "자동 감지"}`,
      `텍스트 레이어 ${textNodes.length}개 확인`,
      annotationSupported
        ? `AI 주석 ${applied.removedAnnotationCount || 0}건 제거`
        : "현재 환경에서는 주석 제거를 지원하지 않음",
    ];

    if (Array.isArray(applied.cleared) && applied.cleared.length > 0) {
      const firstCleared = applied.cleared[0];
      insights.push(`정리 예시: ${firstCleared.nodeName} · ${firstCleared.removedCount}건 제거`);
    } else if (annotationSupported) {
      insights.push("정리할 AI 오타 주석을 찾지 못했습니다.");
    }

    if (Array.isArray(applied.skipped) && applied.skipped.length > 0) {
      insights.push(`구조 또는 환경 제한으로 ${applied.skipped.length}건 건너뜀`);
    }

    return insights;
  }

  function summarizeKinds(issues) {
    const counts = new Map();
    for (const issue of issues) {
      counts.set(issue.kind, (counts.get(issue.kind) || 0) + 1);
    }

    const top = Array.from(counts.entries()).sort((left, right) => right[1] - left[1])[0];
    return top ? `${top[0]} ${top[1]}건` : "없음";
  }

  function collectTextNodes(selection, options) {
    const settings = options && typeof options === "object" ? options : {};
    const includeHidden = settings.includeHidden === true;
    const includeLocked = settings.includeLocked === true;
    const nodes = [];
    const stack = [...selection];
    while (stack.length > 0) {
      const node = stack.pop();
      if (!node) {
        continue;
      }

      if ((!includeHidden && node.visible === false) || (!includeLocked && node.locked === true)) {
        continue;
      }

      if (node.type === "TEXT") {
        nodes.push(node);
      }

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return nodes;
  }

  function buildSelectionContext(selection, designReadResult, proofingSettings) {
    const roots = selection.map((node) => ({
      id: node.id,
      name: safeName(node),
      type: String(node.type || "UNKNOWN"),
    }));
    const selectionLabel = formatSelectionLabel(roots);
    const selectionSignature = getSelectionSignature(selection);
    const textSamples = collectSelectionTextSamples(selection, 8);
    const localeMeta = getProofingLocaleMetadata(proofingSettings ? proofingSettings.proofingLocale : "");
    const fallbackLatinLocaleHint = inferLatinLocaleHint(textSamples.join("\n"));
    if (
      designReadResult &&
      designReadResult.selectionSignature === selectionSignature &&
      designReadResult.summary &&
      typeof designReadResult.summary.contextLabel === "string"
    ) {
      const rawLanguageLabel =
        typeof designReadResult.summary.languageLabel === "string" ? designReadResult.summary.languageLabel : "";
      const detectedLanguageLabel = normalizeDetectedLanguage(rawLanguageLabel);
      return {
        selectionLabel,
        contextLabel: designReadResult.summary.contextLabel || "일반 UI 화면",
        languageLabel:
          typeof designReadResult.summary.languageLabel === "string" ? designReadResult.summary.languageLabel : "텍스트 언어 미감지",
        detectedLanguageLabel: normalizeDetectedLanguage(
          typeof designReadResult.summary.languageLabel === "string" ? designReadResult.summary.languageLabel : ""
        ),
        languageHintLabel: localeMeta ? localeMeta.label : detectedLanguageLabel,
        proofingLocale: localeMeta ? localeMeta.code : "",
        proofingLocaleLabel: localeMeta ? localeMeta.label : "",
        proofingLocaleAiLabel: localeMeta ? localeMeta.aiLabel : "",
        latinLocaleHint: localeMeta && localeMeta.latinLocaleHint ? localeMeta.latinLocaleHint : fallbackLatinLocaleHint,
      };
    }

    const detectedLanguageLabel = detectLanguageFromSamples(textSamples);
    return {
      selectionLabel,
      contextLabel: inferSelectionContext(selection, textSamples),
      languageLabel: detectedLanguageLabel,
      detectedLanguageLabel: detectedLanguageLabel,
      languageHintLabel: localeMeta ? localeMeta.label : detectedLanguageLabel,
      proofingLocale: localeMeta ? localeMeta.code : "",
      proofingLocaleLabel: localeMeta ? localeMeta.label : "",
      proofingLocaleAiLabel: localeMeta ? localeMeta.aiLabel : "",
      latinLocaleHint: localeMeta && localeMeta.latinLocaleHint ? localeMeta.latinLocaleHint : fallbackLatinLocaleHint,
    };
  }

  function collectSelectionTextSamples(selection, limit) {
    const samples = [];
    const stack = [...selection];
    while (stack.length > 0 && samples.length < limit) {
      const node = stack.pop();
      if (!node) {
        continue;
      }

      if (node.type === "TEXT") {
        const text = getTextValue(node);
        if (text && !samples.includes(text)) {
          samples.push(text);
        }
      }

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return samples;
  }

  function inferSelectionContext(selection, textSamples) {
    const rootNames = selection.map((node) => safeName(node).toLowerCase());
    const corpus = `${rootNames.join(" ")} ${textSamples.join(" ")}`.toLowerCase();
    const categories = [
      ["인증/폼 화면", ["login", "sign in", "sign up", "email", "password", "로그인", "이메일", "비밀번호", "입력"]],
      ["대시보드/데이터 화면", ["dashboard", "analytics", "chart", "table", "report", "metric", "지표", "차트", "테이블"]],
      ["랜딩/프로모션 화면", ["get started", "learn more", "buy", "download", "contact", "시작", "구매", "문의"]],
      ["모달/다이얼로그", ["cancel", "confirm", "delete", "ok", "취소", "삭제", "닫기", "확인"]],
      ["앱/웹 내비게이션 화면", ["home", "profile", "settings", "menu", "search", "홈", "프로필", "설정", "검색"]],
    ];

    let bestLabel = "일반 UI 화면";
    let bestScore = 0;
    for (const [label, keywords] of categories) {
      let score = 0;
      for (const keyword of keywords) {
        if (corpus.includes(keyword)) {
          score += 1;
        }
      }

      if (score > bestScore) {
        bestScore = score;
        bestLabel = label;
      }
    }

    return bestLabel;
  }

  function detectLanguageFromSamples(textSamples) {
    const counts = {
      korean: 0,
      latin: 0,
      japanese: 0,
      cjk: 0,
      cyrillic: 0,
      arabic: 0,
      devanagari: 0,
      thai: 0,
      hebrew: 0,
      greek: 0,
      other: 0,
    };
    const samples = Array.isArray(textSamples) ? textSamples : [];
    for (const text of samples) {
      addScriptCounts(counts, text);
    }

    const ordered = Object.entries(counts)
      .filter((entry) => entry[1] > 0)
      .sort((left, right) => right[1] - left[1]);
    if (!ordered.length) {
      return "텍스트 언어 미감지";
    }

    if (ordered.length > 1 && ordered[1][1] >= ordered[0][1] * 0.55) {
      return "다국어 혼합";
    }

    const primary = ordered[0][0];
    switch (primary) {
      case "korean":
        return "한국어 중심";
      case "latin":
        return "영어/라틴 문자 중심";
      case "japanese":
        return "일본어 중심";
      case "cjk":
        return "중국어 한자권 중심";
      case "cyrillic":
        return "키릴 문자권 중심";
      case "arabic":
        return "아랍 문자권 중심";
      case "devanagari":
        return "데바나가리권 중심";
      case "thai":
        return "태국어 중심";
      case "hebrew":
        return "히브리어 중심";
      case "greek":
        return "그리스어 중심";
      default:
        return "다국어 혼합";
    }
  }

  function normalizeDetectedLanguage(label) {
    const normalized = String(label || "").trim();
    if (!normalized) {
      return "텍스트 언어 미감지";
    }

    if (normalized.includes("한국어")) {
      return "한국어 중심";
    }

    if (normalized.includes("영어") || normalized.includes("라틴")) {
      return "영어/라틴 문자 중심";
    }

    if (normalized.includes("일본어")) {
      return "일본어 중심";
    }

    if (normalized.includes("중국어") || normalized.includes("한자")) {
      return "중국어 한자권 중심";
    }

    if (normalized.includes("키릴")) {
      return "키릴 문자권 중심";
    }

    if (normalized.includes("아랍")) {
      return "아랍 문자권 중심";
    }

    if (normalized.includes("데바나가리")) {
      return "데바나가리권 중심";
    }

    if (normalized.includes("태국")) {
      return "태국어 중심";
    }

    if (normalized.includes("히브리")) {
      return "히브리어 중심";
    }

    if (normalized.includes("그리스")) {
      return "그리스어 중심";
    }

    if (normalized.includes("다국어")) {
      return "다국어 혼합";
    }

    return normalized;
  }

  function detectLanguageFamilyLabel(label) {
    const normalized = normalizeDetectedLanguage(label);
    if (normalized.includes("한국어")) {
      return "korean";
    }
    if (normalized.includes("영어") || normalized.includes("라틴")) {
      return "latin";
    }
    if (normalized.includes("일본어")) {
      return "japanese";
    }
    if (normalized.includes("중국어") || normalized.includes("한자")) {
      return "cjk";
    }
    if (normalized.includes("키릴")) {
      return "cyrillic";
    }
    if (normalized.includes("아랍")) {
      return "arabic";
    }
    if (normalized.includes("데바나가리")) {
      return "devanagari";
    }
    if (normalized.includes("태국")) {
      return "thai";
    }
    if (normalized.includes("히브리")) {
      return "hebrew";
    }
    if (normalized.includes("그리스")) {
      return "greek";
    }
    return "";
  }

  function detectLanguageFamilyFromText(text) {
    const counts = {
      korean: 0,
      latin: 0,
      japanese: 0,
      cjk: 0,
      cyrillic: 0,
      arabic: 0,
      devanagari: 0,
      thai: 0,
      hebrew: 0,
      greek: 0,
      other: 0,
    };
    addScriptCounts(counts, text);
    const ordered = Object.entries(counts)
      .filter((entry) => entry[1] > 0)
      .sort((left, right) => right[1] - left[1]);
    return ordered.length ? ordered[0][0] : "";
  }

  function addScriptCounts(scriptCounts, text) {
    const value = String(text || "");
    for (const character of value) {
      const family = detectCharacterScriptFamily(character);
      if (family) {
        scriptCounts[family] += 1;
      } else if (!/\s/.test(character)) {
        scriptCounts.other += 1;
      }
    }
  }

  function detectCharacterScriptFamily(character) {
    const codePoint = character.codePointAt(0);
    if (typeof codePoint !== "number") {
      return "";
    }

    if (
      (codePoint >= 0xac00 && codePoint <= 0xd7af) ||
      (codePoint >= 0x1100 && codePoint <= 0x11ff) ||
      (codePoint >= 0x3130 && codePoint <= 0x318f)
    ) {
      return "korean";
    }

    if (
      (codePoint >= 0x3040 && codePoint <= 0x309f) ||
      (codePoint >= 0x30a0 && codePoint <= 0x30ff)
    ) {
      return "japanese";
    }

    if (codePoint >= 0x4e00 && codePoint <= 0x9fff) {
      return "cjk";
    }

    if (codePoint >= 0x0400 && codePoint <= 0x052f) {
      return "cyrillic";
    }

    if (codePoint >= 0x0600 && codePoint <= 0x06ff) {
      return "arabic";
    }

    if (codePoint >= 0x0900 && codePoint <= 0x097f) {
      return "devanagari";
    }

    if (codePoint >= 0x0e00 && codePoint <= 0x0e7f) {
      return "thai";
    }

    if (codePoint >= 0x0590 && codePoint <= 0x05ff) {
      return "hebrew";
    }

    if (codePoint >= 0x0370 && codePoint <= 0x03ff) {
      return "greek";
    }

    if (
      (codePoint >= 0x0041 && codePoint <= 0x005a) ||
      (codePoint >= 0x0061 && codePoint <= 0x007a) ||
      (codePoint >= 0x00c0 && codePoint <= 0x024f)
    ) {
      return "latin";
    }

    return "";
  }

  function formatSelectionLabel(roots) {
    if (!Array.isArray(roots) || roots.length === 0) {
      return "선택 없음";
    }

    if (roots.length === 1) {
      return `${roots[0].name} / ${normalizeTypeLabel(roots[0].type)}`;
    }

    return `${roots[0].name} 외 ${roots.length - 1}개`;
  }

  function getSelectionSignature(selection) {
    return Array.from(selection || [])
      .map((node) => String(node.id || ""))
      .sort()
      .join("|");
  }

  function safeName(node) {
    return node && typeof node.name === "string" && node.name.trim()
      ? node.name.trim()
      : normalizeTypeLabel(node && node.type);
  }

  function getTextValue(node) {
    return node && typeof node.characters === "string" ? normalizeLineEndings(node.characters) : "";
  }

  function previewText(value, limit) {
    const visible = makeWhitespaceVisible(value);
    if (visible.length <= limit) {
      return visible;
    }

    return `${visible.slice(0, limit - 3)}...`;
  }

  function previewVisibleText(value, limit) {
    return previewText(value, limit);
  }

  function makeWhitespaceVisible(value) {
    const normalized = normalizeLineEndings(value);
    if (!normalized) {
      return "[empty]";
    }

    return normalized.replace(/ /g, "[ ]").replace(/\t/g, "[tab]").replace(/\n/g, "[\\n]");
  }

  function compactText(value) {
    return String(value || "").replace(/\s+/g, " ").trim();
  }

  function normalizeLineEndings(value) {
    return String(value || "").replace(/\r\n?/g, "\n").replace(/[\u2028\u2029]/g, "\n");
  }

  function hasChildren(node) {
    return !!node && Array.isArray(node.children) && node.children.length > 0;
  }

  function normalizeTypeLabel(type) {
    switch (String(type || "").toUpperCase()) {
      case "PAGE":
        return "페이지";
      case "FRAME":
        return "프레임";
      case "GROUP":
        return "그룹";
      case "TEXT":
        return "텍스트";
      case "SECTION":
        return "섹션";
      case "COMPONENT":
        return "컴포넌트";
      case "COMPONENT_SET":
        return "컴포넌트 세트";
      case "INSTANCE":
        return "인스턴스";
      default:
        return String(type || "레이어");
    }
  }

  function prepareTextHighlightSource() {
    const range = resolveSelectedTextHighlightRange();
    const colorSnapshot = extractTextRangeColorSnapshot(range.node, range.start, range.end);
    const fontSize = getTextRangeFontSize(range.node, range.start, range.end);
    const lineHeight = getTextRangeLineHeight(range.node, range.start, range.end, fontSize);

    return {
      nodeId: range.node.id,
      nodeName: safeName(range.node),
      selectionLabel: safeName(range.node),
      start: range.start,
      end: range.end,
      text: range.text,
      textPreview: compactText(range.text) || previewText(range.text, 72),
      fontSize,
      lineHeight,
      textColorHex: colorSnapshot.hex,
      highlightColorHex: AI_TEXT_HIGHLIGHT_DEFAULT_COLOR,
      cornerRadius: sanitizeTextHighlightRadius(
        AI_TEXT_HIGHLIGHT_DEFAULT_RADIUS,
        AI_TEXT_HIGHLIGHT_DEFAULT_RADIUS
      ),
      decorationScale: AI_TEXT_HIGHLIGHT_DEFAULT_BOX_PADDING_PX,
      lineDecorationScale: buildTextHighlightAutoDecorationScale(fontSize, "line", lineHeight),
      strikeCapRadius: sanitizeTextHighlightRadius(
        AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS,
        AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS
      ),
      hasMixedTextColor: colorSnapshot.mixed === true,
    };
  }

  function requestTextHighlightAlphaBounds(payload) {
    return new Promise((resolve, reject) => {
      const requestId = `text-highlight-alpha-${Date.now()}-${(textHighlightMeasureRequestSequence += 1)}`;
      const timeoutId = setTimeout(() => {
        pendingTextHighlightMeasureRequests.delete(requestId);
        reject(new Error("Text highlight measurement timed out."));
      }, 15000);

      pendingTextHighlightMeasureRequests.set(requestId, {
        resolve,
        reject,
        timeoutId,
      });

      figma.ui.postMessage({
        type: "measure-ai-text-highlight-alpha-bounds",
        requestId,
        payload,
      });
    });
  }

  function resolveTextHighlightAlphaBounds(message) {
    const requestId = message && typeof message.requestId === "string" ? message.requestId : "";
    if (!requestId || !pendingTextHighlightMeasureRequests.has(requestId)) {
      return;
    }

    const pending = pendingTextHighlightMeasureRequests.get(requestId);
    pendingTextHighlightMeasureRequests.delete(requestId);
    if (pending && pending.timeoutId) {
      clearTimeout(pending.timeoutId);
    }

    if (!pending) {
      return;
    }

    if (message && typeof message.error === "string" && message.error) {
      pending.reject(new Error(message.error));
      return;
    }

    const bounds = message && message.bounds && typeof message.bounds === "object" ? message.bounds : null;
    const segments = message && Array.isArray(message.segments) ? message.segments : [];
    if (!bounds && !segments.length) {
      pending.resolve(null);
      return;
    }

    pending.resolve({
      bounds,
      segments,
    });
  }

  async function resolveTextHighlightRange(message) {
    const hasExplicitRange =
      message &&
      typeof message.nodeId === "string" &&
      message.nodeId &&
      Number.isFinite(Number(message.start)) &&
      Number.isFinite(Number(message.end));
    const explicitStart = hasExplicitRange ? Math.max(0, Math.floor(Number(message.start) || 0)) : 0;
    const explicitEnd = hasExplicitRange ? Math.max(explicitStart, Math.floor(Number(message.end) || 0)) : explicitStart;
    const liveRange = tryResolveLiveSelectedTextHighlightRange(hasExplicitRange ? message : null);

    if (message && message.preferLiveSelection === true && !hasExplicitRange) {
      if (liveRange) {
        return liveRange;
      }
      return resolveSelectedTextHighlightRange();
    }

    if (!hasExplicitRange) {
      if (liveRange) {
        return liveRange;
      }
      return resolveSelectedTextHighlightRange();
    }

    const node =
      typeof figma.getNodeByIdAsync === "function"
        ? await figma.getNodeByIdAsync(message.nodeId)
        : typeof figma.getNodeById === "function"
          ? figma.getNodeById(message.nodeId)
          : null;
    if (!node || node.removed || node.type !== "TEXT") {
      throw new Error("선택한 텍스트 레이어를 다시 찾지 못했습니다. 다시 드래그한 뒤 시도해 주세요.");
    }

    const characters = typeof node.characters === "string" ? node.characters : "";
    const start = Math.min(characters.length, explicitStart);
    const end = Math.max(start, Math.min(characters.length, explicitEnd));
    const text = normalizeLineEndings(characters.slice(start, end));
    const sourceText = normalizeLineEndings(message && typeof message.sourceText === "string" ? message.sourceText : "");
    if (end <= start || !compactText(text)) {
      throw new Error("하이라이트할 텍스트 범위를 먼저 드래그해 주세요.");
    }

    if (sourceText && compactText(sourceText) && text !== sourceText) {
      const matchingLiveRange = tryResolveLiveSelectedTextHighlightRange(message);
      if (matchingLiveRange && matchingLiveRange.text === sourceText) {
        return matchingLiveRange;
      }
    }

    if (
      liveRange &&
      liveRange.node.id === node.id &&
      liveRange.start === start &&
      liveRange.end === end
    ) {
      return liveRange;
    }

    return {
      node,
      start,
      end,
      text,
    };
  }

  function tryResolveLiveSelectedTextHighlightRange(message) {
    const page = figma.currentPage;
    const selectedTextRange = page && "selectedTextRange" in page ? page.selectedTextRange : null;
    if (!selectedTextRange || !selectedTextRange.node || selectedTextRange.node.removed || selectedTextRange.node.type !== "TEXT") {
      return null;
    }

    const characters = typeof selectedTextRange.node.characters === "string" ? selectedTextRange.node.characters : "";
    const start = Math.max(0, Math.floor(Number(selectedTextRange.start) || 0));
    const end = Math.max(start, Math.min(characters.length, Math.floor(Number(selectedTextRange.end) || 0)));
    const text = normalizeLineEndings(characters.slice(start, end));
    if (end <= start || !compactText(text)) {
      return null;
    }

    if (message && typeof message.nodeId === "string" && message.nodeId && message.nodeId !== selectedTextRange.node.id) {
      return null;
    }

    return {
      node: selectedTextRange.node,
      start,
      end,
      text,
    };
  }

  function resolveSelectedTextHighlightRange() {
    const page = figma.currentPage;
    const selectedTextRange = page && "selectedTextRange" in page ? page.selectedTextRange : null;
    if (!selectedTextRange || !selectedTextRange.node || selectedTextRange.node.removed || selectedTextRange.node.type !== "TEXT") {
      throw new Error("하이라이트할 텍스트를 Figma에서 드래그한 뒤 다시 시도해 주세요.");
    }

    const characters = typeof selectedTextRange.node.characters === "string" ? selectedTextRange.node.characters : "";
    const start = Math.max(0, Math.floor(Number(selectedTextRange.start) || 0));
    const end = Math.max(start, Math.min(characters.length, Math.floor(Number(selectedTextRange.end) || 0)));
    const text = normalizeLineEndings(characters.slice(start, end));
    if (end <= start || !compactText(text)) {
      throw new Error("하이라이트할 텍스트 범위를 먼저 드래그해 주세요.");
    }

    return {
      node: selectedTextRange.node,
      start,
      end,
      text,
    };
  }

  function isWholeTextHighlightRange(node, start, end) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    if (!(characterCount > 0)) {
      return false;
    }

    const rangeStart = Math.max(0, Math.floor(Number(start) || 0));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    return rangeStart <= 0 && rangeEnd >= characterCount;
  }

  function extractTextRangeColorSnapshot(node, start, end) {
    const fallbackHex = AI_TEXT_HIGHLIGHT_DEFAULT_TEXT_COLOR;
    const segments = getTextHighlightStyledSegments(node, start, end, ["fills"]);
    const seen = {};
    let firstHex = "";
    let uniqueCount = 0;

    for (const segment of segments) {
      const hex = extractVisibleSolidPaintHex(segment && segment.fills);
      if (!hex) {
        continue;
      }

      if (!firstHex) {
        firstHex = hex;
      }
      if (!seen[hex]) {
        seen[hex] = true;
        uniqueCount += 1;
      }
    }

    if (firstHex) {
      return {
        hex: firstHex,
        mixed: uniqueCount > 1,
      };
    }

    const rangeHex = extractVisibleSolidPaintHex(getTextHighlightPaints(node, start, end));
    if (rangeHex) {
      return {
        hex: rangeHex,
        mixed: false,
      };
    }

    const singleCharHex = extractVisibleSolidPaintHex(getTextHighlightPaints(node, start, Math.min(end, start + 1)));
    if (singleCharHex) {
      return {
        hex: singleCharHex,
        mixed: false,
      };
    }

    const nodeHex = extractVisibleSolidPaintHex(node && node.fills);
    return {
      hex: nodeHex || fallbackHex,
      mixed: false,
    };
  }

  async function measureTextHighlightBounds(node, start, end, textColorHex, options) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    const fontSize = getTextRangeFontSize(node, rangeStart, rangeEnd);
    const lineHeight = getTextRangeLineHeight(node, rangeStart, rangeEnd, fontSize);
    const selectedText = characters.slice(rangeStart, rangeEnd);
    const isPartialSingleLineSelection =
      !!selectedText &&
      !/[\r\n]/.test(selectedText) &&
      !isWholeTextHighlightRange(node, rangeStart, rangeEnd);
    const isAlignmentSensitivePartialSingleLineSelection =
      isPartialSingleLineSelection && isAlignmentSensitiveTextHighlightNode(node);
    const preferAlignmentSensitiveFallback = !!(options && options.preferAlignmentSensitiveFallback);

    const directMeasurement = await measureExactTextHighlightBounds(
      node,
      rangeStart,
      rangeEnd,
      textColorHex,
      fontSize,
      lineHeight
    );
    const directBoundsList = getTextHighlightMeasurementBoundsList(directMeasurement);
    const directBoundsAreSafe =
      directBoundsList.length &&
      !hasSuspiciousTextHighlightDirectBounds(node, directBoundsList, rangeStart, rangeEnd, fontSize, lineHeight);
    if (isPartialSingleLineSelection && directBoundsList.length > 1 && directBoundsAreSafe) {
      return {
        bounds: mergeTextHighlightBoundsList(directBoundsList),
        boundsList: directBoundsList,
        segments: directBoundsList.slice(),
        fontSize,
        lineHeight,
      };
    }
    if (isAlignmentSensitivePartialSingleLineSelection && directBoundsAreSafe && !preferAlignmentSensitiveFallback) {
      return {
        bounds: mergeTextHighlightBoundsList(directBoundsList),
        boundsList: directBoundsList,
        segments: directBoundsList.slice(),
        fontSize,
        lineHeight,
      };
    }

    const underlineGeometryMeasurement = await measureTextHighlightUnderlineGeometryBounds(
      node,
      rangeStart,
      rangeEnd,
      fontSize,
      lineHeight
    );
    const underlineGeometryBoundsList = getTextHighlightMeasurementBoundsList(underlineGeometryMeasurement);
    if (underlineGeometryBoundsList.length) {
      const underlineLineBoxBoundsList = sortTextHighlightBoundsList(underlineGeometryBoundsList);
      if (
        !isAlignmentSensitivePartialSingleLineSelection &&
        underlineLineBoxBoundsList.length &&
        !hasOverwideTextHighlightSelectionRows(underlineLineBoxBoundsList, selectedText, fontSize, lineHeight) &&
        !hasUnderwideTextHighlightSelectionRows(underlineLineBoxBoundsList, selectedText, fontSize)
      ) {
        const underlineSelectionBoundsList = isPartialSingleLineSelection
          ? await constrainTextHighlightBoundsListToExactSelection(
              node,
              rangeStart,
              rangeEnd,
              textColorHex,
              fontSize,
              lineHeight,
              underlineLineBoxBoundsList,
              underlineLineBoxBoundsList
            )
          : underlineLineBoxBoundsList;
        if (
          underlineSelectionBoundsList.length &&
          !hasOverwideTextHighlightSelectionRows(underlineSelectionBoundsList, selectedText, fontSize, lineHeight) &&
          !hasUnderwideTextHighlightSelectionRows(underlineSelectionBoundsList, selectedText, fontSize)
        ) {
          return {
            bounds: mergeTextHighlightBoundsList(underlineSelectionBoundsList),
            boundsList: underlineSelectionBoundsList,
            segments: underlineSelectionBoundsList.slice(),
            fontSize,
            lineHeight,
          };
        }

        return {
          bounds: mergeTextHighlightBoundsList(underlineLineBoxBoundsList),
          boundsList: underlineLineBoxBoundsList,
          segments: underlineLineBoxBoundsList.slice(),
          fontSize,
          lineHeight,
        };
      }

      const glyphAwareBoundsList = await refineTextHighlightBoundsWithGlyphBounds(
        node,
        rangeStart,
        rangeEnd,
        textColorHex,
        fontSize,
        lineHeight,
        underlineGeometryBoundsList
      );
      if (!isAlignmentSensitivePartialSingleLineSelection && glyphAwareBoundsList.length) {
        return {
          bounds: mergeTextHighlightBoundsList(glyphAwareBoundsList),
          boundsList: glyphAwareBoundsList,
          segments: glyphAwareBoundsList.slice(),
          fontSize,
          lineHeight,
        };
      }
    }

    if (!isAlignmentSensitivePartialSingleLineSelection && directBoundsAreSafe) {
      return {
        bounds: mergeTextHighlightBoundsList(directBoundsList),
        boundsList: directBoundsList,
        segments: directBoundsList.slice(),
        fontSize,
        lineHeight,
      };
    }

    const canUsePrefixMeasurement =
      !isAlignmentSensitivePartialSingleLineSelection || isWholeTextHighlightRange(node, rangeStart, rangeEnd);
    const endMeasurement = canUsePrefixMeasurement
      ? await measureTextHighlightPrefixBounds(node, rangeEnd, textColorHex, fontSize, lineHeight)
      : null;
    const endBoundsList =
      canUsePrefixMeasurement && Array.isArray(endMeasurement && endMeasurement.boundsList)
        ? endMeasurement.boundsList
        : [];
    let boundsList = rangeStart > 0 ? [] : endBoundsList.slice();
    let startMeasurement = null;
    let usedAlignmentSensitiveFallback = false;

    if (endBoundsList.length && rangeStart > 0) {
      startMeasurement = await measureTextHighlightPrefixBounds(
        node,
        rangeStart,
        textColorHex,
        fontSize,
        lineHeight
      );
      const startBoundsList = Array.isArray(startMeasurement && startMeasurement.boundsList)
        ? startMeasurement.boundsList
        : [];
      if (startBoundsList.length) {
        if (isPartialSingleLineSelection) {
          boundsList = await buildTextHighlightSelectionRowsFromPrefixStart(
            node,
            rangeStart,
            rangeEnd,
            textColorHex,
            fontSize,
            lineHeight,
            endBoundsList,
            startBoundsList,
            selectedText
          );
        }
        if (!boundsList.length) {
          boundsList = subtractTextHighlightPrefixBounds(endBoundsList, startBoundsList, fontSize, lineHeight);
        }
        if (!boundsList.length) {
          boundsList = extractTrailingTextHighlightBounds(endBoundsList, startBoundsList, fontSize, lineHeight);
        }
      }
    }

    if (!boundsList.length && !isAlignmentSensitivePartialSingleLineSelection) {
      if (directBoundsList.length && !hasSuspiciousTextHighlightDirectBounds(node, directBoundsList, rangeStart, rangeEnd, fontSize, lineHeight)) {
        boundsList = directBoundsList;
      }
    }

    if (!boundsList.length && isAlignmentSensitivePartialSingleLineSelection) {
      boundsList = await measureAlignmentSensitiveTextHighlightSelectionFromLeftLayout(
        node,
        rangeStart,
        rangeEnd,
        textColorHex,
        fontSize,
        lineHeight,
        selectedText
      );
      usedAlignmentSensitiveFallback = boundsList.length > 0;
    }

    if (!boundsList.length && isPartialSingleLineSelection) {
      throw new Error("Could not safely measure the selected text highlight range.");
    }

    if (!boundsList.length) {
      boundsList = buildConservativeTextHighlightBounds(
        node,
        rangeStart,
        rangeEnd,
        fontSize,
        lineHeight,
        startMeasurement && startMeasurement.fallbackBounds,
        endMeasurement && endMeasurement.fallbackBounds
      );
    }

    if (!boundsList.length) {
      boundsList = buildEmergencyTextHighlightBounds(node, rangeStart, rangeEnd, fontSize, lineHeight);
    }

    if (!boundsList.length) {
      throw new Error("선택한 텍스트 범위의 렌더 영역을 찾지 못했습니다.");
    }

    if (!usedAlignmentSensitiveFallback) {
      boundsList = await constrainTextHighlightBoundsListToExactSelection(
        node,
        rangeStart,
        rangeEnd,
        textColorHex,
        fontSize,
        lineHeight,
        boundsList,
        endBoundsList
      );
    }
    if (!boundsList.length) {
      throw new Error("\uc120\ud0dd \ud14d\uc2a4\ud2b8 \ubc94\uc704\ub97c \uc548\uc804\ud558\uac8c \uce21\uc815\ud558\uc9c0 \ubabb\ud588\uc2b5\ub2c8\ub2e4.");
    }
    const sortedBoundsList = sortTextHighlightBoundsList(boundsList);
    const isSoftWrappedPartialSelection =
      isPartialSingleLineSelection &&
      isPlausibleSoftWrappedTextHighlightSelectionRows(
        node,
        sortedBoundsList,
        selectedText,
        fontSize,
        lineHeight
      );
    if (isPartialSingleLineSelection && sortedBoundsList.length !== 1 && !isSoftWrappedPartialSelection) {
      throw new Error("Could not safely measure the selected text highlight range.");
    }
    if (
      isAlignmentSensitivePartialSingleLineSelection &&
      hasOverwideAlignmentSensitiveTextHighlightFallbackRow(
        sortedBoundsList[0],
        selectedText,
        fontSize,
        getTextHighlightAlignmentReferenceWidth(node)
      )
    ) {
      throw new Error("Could not safely measure the selected text highlight range.");
    }
    boundsList = sortedBoundsList;
    if (
      !isSoftWrappedPartialSelection &&
      hasOverwideTextHighlightSelectionRows(boundsList, selectedText, fontSize, lineHeight)
    ) {
      throw new Error("Could not safely measure the selected text highlight range.");
    }

    return {
      bounds: mergeTextHighlightBoundsList(boundsList),
      boundsList,
      segments: boundsList.slice(),
      fontSize,
      lineHeight,
    };
  }

  async function measureTextHighlightUnderlineGeometryBounds(node, start, end, fontSize, lineHeight) {
    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart) || !node || typeof node.clone !== "function" || typeof node.setRangeTextDecoration !== "function") {
      return {
        bounds: null,
        boundsList: [],
        segments: [],
        fontSize,
        lineHeight,
      };
    }

    const clone = node.clone();
    try {
      if ("effects" in clone && Array.isArray(clone.effects)) {
        clone.effects = [];
      }
      if ("strokes" in clone && Array.isArray(clone.strokes)) {
        clone.strokes = [];
      }
      if ("opacity" in clone && typeof clone.opacity === "number") {
        clone.opacity = 1;
      }
      if ("blendMode" in clone) {
        clone.blendMode = "NORMAL";
      }

      try {
        await loadFontsForTextNode(clone);
      } catch (error) {}

      clearTextHighlightDecoration(clone, 0, characterCount);
      if (typeof clone.setRangeFills === "function") {
        try {
          clone.setRangeFills(0, characterCount, [createTransparentPaint()]);
        } catch (error) {}
      }
      clone.setRangeTextDecoration(rangeStart, rangeEnd, "UNDERLINE");
      if (typeof clone.setRangeTextDecorationStyle === "function") {
        try {
          clone.setRangeTextDecorationStyle(rangeStart, rangeEnd, "SOLID");
        } catch (error) {}
      }
      if (typeof clone.setRangeTextDecorationColor === "function") {
        try {
          clone.setRangeTextDecorationColor(rangeStart, rangeEnd, {
            value: createSolidPaint(AI_TEXT_HIGHLIGHT_MEASURE_COLOR),
          });
        } catch (error) {}
      }
      if (typeof clone.setRangeTextDecorationSkipInk === "function") {
        try {
          clone.setRangeTextDecorationSkipInk(rangeStart, rangeEnd, false);
        } catch (error) {}
      }

      const underlineLocalBoundsList = await waitForTextHighlightUnderlineGeometryBounds(clone, fontSize, lineHeight);
      const boundsList = underlineLocalBoundsList
        .map((localBounds) => buildTextHighlightBoundsFromUnderlineLocalBounds(node, localBounds, fontSize, lineHeight))
        .filter(Boolean);

      return {
        bounds: mergeTextHighlightBoundsList(boundsList),
        boundsList,
        segments: boundsList.slice(),
        fontSize,
        lineHeight,
      };
    } catch (error) {
      return {
        bounds: null,
        boundsList: [],
        segments: [],
        fontSize,
        lineHeight,
      };
    } finally {
      if (clone && !clone.removed) {
        clone.remove();
      }
    }
  }

  async function waitForTextHighlightUnderlineGeometryBounds(node, fontSize, lineHeight) {
    const attempts = 10;
    for (let attempt = 0; attempt < attempts; attempt += 1) {
      const boundsList = getTextHighlightUnderlineGeometryLocalBounds(node, fontSize, lineHeight);
      if (boundsList.length) {
        return boundsList;
      }
      await waitForTextHighlightGeometryTick(attempt < 2 ? 1 : 16);
    }

    return [];
  }

  function waitForTextHighlightGeometryTick(ms) {
    return new Promise((resolve) => {
      setTimeout(resolve, Math.max(1, Math.floor(Number(ms) || 1)));
    });
  }

  function getTextHighlightUnderlineGeometryLocalBounds(node, fontSize, lineHeight) {
    const geometry = node && Array.isArray(node.fillGeometry) ? node.fillGeometry : [];
    if (!geometry.length) {
      return [];
    }

    const boundsList = [];
    for (let index = 0; index < geometry.length; index += 1) {
      const path = geometry[index];
      const pathBoundsList = parseTextHighlightSvgPathBoundsList(path && path.data, fontSize, lineHeight);
      for (const pathBounds of pathBoundsList) {
        boundsList.push(pathBounds);
      }
    }

    return mergeTextHighlightLocalBoundsByRows(boundsList, fontSize, lineHeight);
  }

  function mergeTextHighlightLocalBoundsByRows(boundsList, fontSize, lineHeight) {
    const sortedBoundsList = sortTextHighlightBoundsList(boundsList);
    if (sortedBoundsList.length < 2) {
      return sortedBoundsList;
    }

    const rowTolerance = getTextHighlightRowTolerance(fontSize, lineHeight);
    const rows = [];
    for (const bounds of sortedBoundsList) {
      const normalizedBounds = normalizeTextHighlightWorldBounds(bounds);
      if (!normalizedBounds) {
        continue;
      }

      let matchedRow = null;
      for (const row of rows) {
        if (doTextHighlightBoundsShareRow(row, normalizedBounds, rowTolerance)) {
          matchedRow = row;
          break;
        }
      }

      if (!matchedRow) {
        rows.push({
          x: normalizedBounds.x,
          y: normalizedBounds.y,
          width: normalizedBounds.width,
          height: normalizedBounds.height,
        });
        continue;
      }

      const minX = Math.min(matchedRow.x, normalizedBounds.x);
      const minY = Math.min(matchedRow.y, normalizedBounds.y);
      const maxX = Math.max(matchedRow.x + matchedRow.width, normalizedBounds.x + normalizedBounds.width);
      const maxY = Math.max(matchedRow.y + matchedRow.height, normalizedBounds.y + normalizedBounds.height);
      matchedRow.x = roundTextHighlightMetric(minX);
      matchedRow.y = roundTextHighlightMetric(minY);
      matchedRow.width = roundTextHighlightMetric(Math.max(1, maxX - minX));
      matchedRow.height = roundTextHighlightMetric(Math.max(0.5, maxY - minY));
    }

    return sortTextHighlightBoundsList(rows);
  }

  function parseTextHighlightSvgPathBoundsList(pathData, fontSize, lineHeight) {
    if (typeof pathData !== "string" || !pathData.trim()) {
      return [];
    }

    const chunks = pathData
      .split(/[zZ]/)
      .map((chunk) => chunk.trim())
      .filter(Boolean);
    const boundsList = [];
    for (const chunk of chunks) {
      const bounds = parseTextHighlightSvgPathChunkBounds(chunk);
      if (!bounds) {
        continue;
      }

      const minimumWidth = getTextHighlightMinimumWidth(fontSize);
      const minimumHeight = Math.max(0.5, Math.min(Math.max(1, Number(lineHeight) || 1), Math.max(1, Number(fontSize) || 12) * 0.2));
      if (bounds.width < minimumWidth || bounds.height < minimumHeight * 0.05) {
        continue;
      }

      boundsList.push(bounds);
    }

    return boundsList;
  }

  function parseTextHighlightSvgPathChunkBounds(pathChunk) {
    const matches = String(pathChunk || "").match(/[-+]?(?:\d*\.\d+|\d+)(?:e[-+]?\d+)?/gi);
    if (!matches || matches.length < 4) {
      return null;
    }

    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (let index = 0; index + 1 < matches.length; index += 2) {
      const x = Number(matches[index]);
      const y = Number(matches[index + 1]);
      if (!Number.isFinite(x) || !Number.isFinite(y)) {
        continue;
      }

      minX = Math.min(minX, x);
      minY = Math.min(minY, y);
      maxX = Math.max(maxX, x);
      maxY = Math.max(maxY, y);
    }

    if (!Number.isFinite(minX) || !Number.isFinite(minY) || maxX <= minX || maxY < minY) {
      return null;
    }

    return {
      x: roundTextHighlightMetric(minX),
      y: roundTextHighlightMetric(minY),
      width: roundTextHighlightMetric(Math.max(1, maxX - minX)),
      height: roundTextHighlightMetric(Math.max(0.5, maxY - minY)),
    };
  }

  function buildTextHighlightBoundsFromUnderlineLocalBounds(node, underlineLocalBounds, fontSize, lineHeight) {
    const localBounds = normalizeTextHighlightWorldBounds(underlineLocalBounds);
    if (!localBounds) {
      return null;
    }

    const metrics = buildTextHighlightBoxGlyphMetrics(fontSize, lineHeight);
    const underlineTop = localBounds.y;
    const textTop = roundTextHighlightMetric(underlineTop - metrics.ascent);
    const highlightLocalBounds = {
      x: localBounds.x,
      y: textTop,
      width: localBounds.width,
      height: metrics.height,
    };

    return getTextHighlightWorldBoundsFromLocalBounds(node, highlightLocalBounds);
  }

  function buildTextHighlightBoxGlyphMetrics(fontSize, lineHeight) {
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const ascent = Math.max(size * 0.82, Math.min(resolvedLineHeight * 0.74, size * 0.92));
    const descent = Math.max(size * 0.14, Math.min(resolvedLineHeight * 0.18, size * 0.22));
    return {
      ascent: roundTextHighlightMetric(ascent),
      descent: roundTextHighlightMetric(descent),
      height: roundTextHighlightMetric(Math.max(1, ascent + descent)),
    };
  }

  function buildTextHighlightBoxVisualRowHeight(fontSize, lineHeight) {
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const metrics = buildTextHighlightBoxGlyphMetrics(size, resolvedLineHeight);
    const largeFontBlend = getTextHighlightLargeFontCompactBlend(size);
    const compactHeight = Math.max(size * 0.86, Math.min(resolvedLineHeight * 0.78, size * 0.95));
    const visualHeight = metrics.height * (1 - largeFontBlend) + compactHeight * largeFontBlend;
    return roundTextHighlightMetric(Math.max(1, Math.min(resolvedLineHeight, visualHeight)));
  }

  function getTextHighlightLargeFontCompactBlend(fontSize) {
    const size = Math.max(1, Number(fontSize) || 16);
    return Math.max(0, Math.min(1, (size - 32) / 24));
  }

  async function refineTextHighlightBoundsWithGlyphBounds(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    boundsList
  ) {
    const baseRows = sortTextHighlightBoundsList(boundsList);
    if (!baseRows.length) {
      return [];
    }

    let glyphMeasurement = null;
    try {
      glyphMeasurement = await measureExactTextHighlightBounds(node, start, end, textColorHex, fontSize, lineHeight);
    } catch (error) {}

    let glyphRows = getTextHighlightMeasurementBoundsList(glyphMeasurement);
    if (hasMergedTextHighlightBlockBounds(glyphRows, fontSize)) {
      glyphRows = splitMergedTextHighlightBlockBounds(glyphRows, fontSize, lineHeight);
    }
    glyphRows = filterTextHighlightGlyphBoundsRows(glyphRows, fontSize, lineHeight);
    if (!glyphRows.length) {
      return [];
    }

    const refinedRows = [];
    const usedGlyphRows = {};
    for (const baseRow of baseRows) {
      const glyphRow = findTextHighlightGlyphBoundsForBaseRow(
        baseRow,
        glyphRows,
        usedGlyphRows,
        fontSize,
        lineHeight
      );
      if (!glyphRow) {
        continue;
      }

      const constrainedBaseRow = constrainTextHighlightBaseRowToGlyphRow(baseRow, glyphRow, fontSize);
      refinedRows.push(expandTextHighlightBoundsToGlyphHeight(constrainedBaseRow, glyphRow, fontSize, lineHeight));
    }

    return refinedRows;
  }

  async function constrainTextHighlightBoundsListToExactSelection(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    boundsList,
    endBoundsList
  ) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length || isWholeTextHighlightRange(node, start, end)) {
      return rows;
    }

    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    const selectedText = characters.slice(rangeStart, rangeEnd);
    if (!selectedText || /[\r\n]/.test(selectedText)) {
      return rows;
    }

    let exactMeasurement = null;
    try {
      exactMeasurement = await measureExactTextHighlightBounds(
        node,
        rangeStart,
        rangeEnd,
        textColorHex,
        fontSize,
        lineHeight
      );
    } catch (error) {}

    let exactRows = getTextHighlightMeasurementBoundsList(exactMeasurement);
    if (hasMergedTextHighlightBlockBounds(exactRows, fontSize)) {
      exactRows = splitMergedTextHighlightBlockBounds(exactRows, fontSize, lineHeight);
    }
    exactRows = filterTextHighlightGlyphBoundsRows(exactRows, fontSize, lineHeight).filter((row) =>
      isReasonableTextHighlightSelectionRow(row, selectedText, fontSize)
    );
    if (!exactRows.length) {
      return rows;
    }

    const refinedRows = [];
    const usedExactRows = {};
    for (const row of rows) {
      const exactRow = findTextHighlightGlyphBoundsForBaseRow(
        row,
        exactRows,
        usedExactRows,
        fontSize,
        lineHeight
      );
      if (!exactRow) {
        continue;
      }

      const constrainedRow = constrainTextHighlightBaseRowToGlyphRow(row, exactRow, fontSize);
      refinedRows.push(expandTextHighlightBoundsToGlyphHeight(constrainedRow, exactRow, fontSize, lineHeight));
    }

    if (refinedRows.length) {
      return refinedRows;
    }

    return exactRows;
  }

  async function buildTextHighlightSelectionRowsFromPrefixEnd(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    rows,
    endBoundsList,
    selectedText
  ) {
    const candidateRows = sortTextHighlightBoundsList(rows);
    if (!candidateRows.length) {
      return [];
    }

    const referenceRows = sortTextHighlightBoundsList(endBoundsList && endBoundsList.length ? endBoundsList : candidateRows);
    const targetRow = candidateRows[candidateRows.length - 1];
    const referenceRow = findTextHighlightReferenceRowForTarget(targetRow, referenceRows, fontSize, lineHeight) || targetRow;
    const selectedWidth = await measureTextHighlightSelectedWidth(
      node,
      start,
      end,
      textColorHex,
      fontSize,
      lineHeight,
      selectedText,
      referenceRow.width
    );

    if (!(selectedWidth > 0)) {
      return [];
    }

    const minimumWidth = getTextHighlightMinimumWidth(fontSize);
    const endRight = referenceRow.x + referenceRow.width;
    const nextWidth = roundTextHighlightMetric(
      Math.max(minimumWidth, Math.min(referenceRow.width, selectedWidth))
    );
    const nextX = roundTextHighlightMetric(Math.max(referenceRow.x, endRight - nextWidth));

    return [
      {
        x: nextX,
        y: targetRow.y,
        width: nextWidth,
        height: targetRow.height,
      },
    ];
  }

  async function buildAlignmentSensitiveTextHighlightSelectionBounds(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    selectedText
  ) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    const lineRange = getTextHighlightLineRangeForSelection(characters, rangeStart, rangeEnd);
    if (!lineRange || !(lineRange.end > lineRange.start)) {
      return [];
    }

    const lineText = characters.slice(lineRange.start, lineRange.end);
    const selectedLineText = selectedText || characters.slice(rangeStart, rangeEnd);
    if (!selectedLineText || /[\r\n]/.test(selectedLineText)) {
      return [];
    }

    const throughSelectionRows = await measureAlignmentSensitiveTextHighlightRangeRows(
      node,
      lineRange.start,
      rangeEnd,
      textColorHex,
      fontSize,
      lineHeight
    );
    const minimumWidth = getTextHighlightMinimumWidth(fontSize);
    const size = Math.max(12, Number(fontSize) || 16);
    const estimatedSelectedWidth = estimateTextHighlightInlineTextWidth(selectedLineText, fontSize);
    const minimumExpectedWidth = Math.max(
      minimumWidth,
      Math.min(estimatedSelectedWidth * 0.32, Math.max(minimumWidth, size * 0.7))
    );
    let targetRow = throughSelectionRows.length ? throughSelectionRows[throughSelectionRows.length - 1] : null;
    let beforeRow = null;
    if (targetRow && rangeStart > lineRange.start) {
      const beforeRows = await measureAlignmentSensitiveTextHighlightRangeRows(
        node,
        lineRange.start,
        rangeStart,
        textColorHex,
        fontSize,
        lineHeight
      );
      beforeRow = findTextHighlightSameRowForTarget(targetRow, beforeRows, fontSize, lineHeight);
    }

    if (!targetRow) {
      targetRow = await measureTextHighlightStableLineRow(
        node,
        lineRange.start,
        lineRange.end,
        textColorHex,
        fontSize,
        lineHeight,
        lineText
      );
    }

    const normalizedTargetRow = normalizeTextHighlightWorldBounds(targetRow);
    if (!normalizedTargetRow) {
      return [];
    }

    const selectionRows = await measureAlignmentSensitiveTextHighlightRangeRows(
      node,
      rangeStart,
      rangeEnd,
      textColorHex,
      fontSize,
      lineHeight
    );
    const selectionRow =
      findTextHighlightSameRowForTarget(normalizedTargetRow, selectionRows, fontSize, lineHeight) ||
      (selectionRows.length === 1 ? selectionRows[0] : null);
    const normalizedSelectionRow = normalizeTextHighlightWorldBounds(selectionRow);
    const canUseSelectionRow =
      normalizedSelectionRow &&
      !hasUnderwideTextHighlightSelectionRows([normalizedSelectionRow], selectedLineText, fontSize) &&
      !hasOverwideTextHighlightSelectionRows([normalizedSelectionRow], selectedLineText, fontSize, lineHeight);

    const targetRight = normalizedTargetRow.x + normalizedTargetRow.width;
    const normalizedBeforeRow = normalizeTextHighlightWorldBounds(beforeRow);
    let nextX = normalizedBeforeRow
      ? normalizedBeforeRow.x + normalizedBeforeRow.width
      : normalizedTargetRow.x;
    let nextWidth = Math.max(0, targetRight - nextX);
    if (
      canUseSelectionRow &&
      (nextWidth < minimumExpectedWidth ||
        Math.abs(normalizedSelectionRow.width - estimatedSelectedWidth) <
          Math.abs(nextWidth - estimatedSelectedWidth))
    ) {
      nextX = normalizedSelectionRow.x;
      nextWidth = normalizedSelectionRow.width;
    }

    if (nextWidth < minimumExpectedWidth && estimatedSelectedWidth > 0) {
      nextWidth = Math.min(Math.max(minimumExpectedWidth, estimatedSelectedWidth), Math.max(minimumWidth, targetRight - nextX));
    }

    nextX = roundTextHighlightMetric(Math.max(normalizedTargetRow.x, Math.min(targetRight - minimumWidth, nextX)));
    nextWidth = roundTextHighlightMetric(Math.max(minimumWidth, Math.min(nextWidth, targetRight - nextX)));
    const row = {
      x: nextX,
      y: normalizedTargetRow.y,
      width: nextWidth,
      height: normalizedTargetRow.height,
    };

    if (hasUnderwideTextHighlightSelectionRows([row], selectedLineText, fontSize)) {
      return [];
    }
    if (hasOverwideAlignmentSensitiveTextHighlightFallbackRow(row, selectedLineText, fontSize, normalizedTargetRow.width)) {
      return [];
    }

    return hasOverwideTextHighlightSelectionRows([row], selectedLineText, fontSize, lineHeight) ? [] : [row];
  }

  function hasOverwideAlignmentSensitiveTextHighlightFallbackRow(row, selectedText, fontSize, referenceWidth) {
    const bounds = normalizeTextHighlightWorldBounds(row);
    if (!bounds || !selectedText || /[\r\n]/.test(selectedText)) {
      return false;
    }

    const compactSelectedText = compactText(selectedText);
    if (compactSelectedText.length <= 1) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const estimatedWidth = estimateTextHighlightInlineTextWidth(compactSelectedText, size);
    const maximumExpectedWidth = Math.max(size * 3, estimatedWidth * 1.38);
    const reference = Math.max(0, Number(referenceWidth) || 0);
    const looksLikeWholeRow = reference > 0 && bounds.width >= reference * 0.86;
    return looksLikeWholeRow && bounds.width > maximumExpectedWidth;
  }

  function getTextHighlightAlignmentReferenceWidth(node) {
    const nodeWidth = Number(node && node.width);
    if (Number.isFinite(nodeWidth) && nodeWidth > 0) {
      return nodeWidth;
    }

    const bounds = getNodeRenderBounds(node);
    const boundsWidth = Number(bounds && bounds.width);
    return Number.isFinite(boundsWidth) && boundsWidth > 0 ? boundsWidth : 0;
  }

  function buildEstimatedAlignmentSensitiveTextHighlightSelectionBounds(
    node,
    start,
    end,
    fontSize,
    lineHeight,
    selectedText
  ) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    const selectionText = selectedText || characters.slice(rangeStart, rangeEnd);
    if (!(rangeEnd > rangeStart) || !selectionText || /[\r\n]/.test(selectionText)) {
      return [];
    }

    const alignment = getTextHighlightHorizontalAlignment(node);
    if (alignment !== "CENTER" && alignment !== "RIGHT") {
      return [];
    }

    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      return [];
    }

    const layoutLocalBounds = getTextHighlightTypographyLocalBounds(node, inverseNodeTransform);
    if (!layoutLocalBounds || !(layoutLocalBounds.width > 0)) {
      return [];
    }

    const hardLineRange = getTextHighlightLineRangeForSelection(characters, rangeStart, rangeEnd);
    if (!hardLineRange || !(hardLineRange.end > hardLineRange.start)) {
      return [];
    }

    const hardLineText = characters.slice(hardLineRange.start, hardLineRange.end);
    const localSelectionStart = rangeStart - hardLineRange.start;
    const localSelectionEnd = rangeEnd - hardLineRange.start;
    const softLines = buildEstimatedTextHighlightSoftLines(hardLineText, layoutLocalBounds.width, fontSize);
    if (!softLines.length) {
      return [];
    }

    const selectionMidpoint = (localSelectionStart + localSelectionEnd) / 2;
    let softLineIndex = softLines.findIndex(
      (line) => selectionMidpoint >= line.start && selectionMidpoint <= Math.max(line.start, line.end)
    );
    if (softLineIndex < 0) {
      softLineIndex = softLines.findIndex(
        (line) => localSelectionStart >= line.start && localSelectionStart <= Math.max(line.start, line.end)
      );
    }
    if (softLineIndex < 0) {
      return [];
    }

    const softLine = softLines[softLineIndex];
    const selectedStartInLine = Math.max(softLine.start, Math.min(softLine.end, localSelectionStart));
    const selectedEndInLine = Math.max(selectedStartInLine, Math.min(softLine.end, localSelectionEnd));
    const selectedLineText = hardLineText.slice(selectedStartInLine, selectedEndInLine) || selectionText;
    if (!compactText(selectedLineText)) {
      return [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const metrics = buildTextHighlightBoxGlyphMetrics(size, resolvedLineHeight);
    const lineWidth = Math.max(
      getTextHighlightMinimumWidth(size),
      Math.min(layoutLocalBounds.width, softLine.width)
    );
    const prefixWidth = Math.max(
      0,
      Math.min(lineWidth, estimateTextHighlightLayoutInlineWidth(hardLineText.slice(softLine.start, selectedStartInLine), size))
    );
    const selectedWidth = Math.max(
      getTextHighlightMinimumWidth(size),
      Math.min(layoutLocalBounds.width - prefixWidth, estimateTextHighlightLayoutInlineWidth(selectedLineText, size))
    );
    if (!(selectedWidth > 0)) {
      return [];
    }

    const previousSoftLineCount = countEstimatedTextHighlightSoftLinesBefore(
      characters,
      hardLineRange.start,
      layoutLocalBounds.width,
      size
    );
    const absoluteSoftLineIndex = previousSoftLineCount + softLineIndex;
    const lineLocalX =
      alignment === "RIGHT"
        ? layoutLocalBounds.x + Math.max(0, layoutLocalBounds.width - lineWidth)
        : layoutLocalBounds.x + Math.max(0, (layoutLocalBounds.width - lineWidth) / 2);
    const localBounds = {
      x: roundTextHighlightMetric(lineLocalX + prefixWidth),
      y: roundTextHighlightMetric(
        layoutLocalBounds.y +
          absoluteSoftLineIndex * resolvedLineHeight +
          Math.max(0, (resolvedLineHeight - metrics.height) / 2)
      ),
      width: roundTextHighlightMetric(selectedWidth),
      height: metrics.height,
    };
    const worldBounds = normalizeTextHighlightWorldBounds(
      getTextHighlightWorldBoundsFromLocalBounds(node, localBounds)
    );
    if (!worldBounds) {
      return [];
    }

    if (
      hasOverwideTextHighlightSelectionRows([worldBounds], selectedLineText, size, resolvedLineHeight) ||
      hasUnderwideTextHighlightSelectionRows([worldBounds], selectedLineText, size)
    ) {
      return [];
    }

    return [worldBounds];
  }

  function buildEstimatedTextHighlightSoftLines(text, maximumWidth, fontSize) {
    const value = String(text || "");
    const widthLimit = Math.max(1, Number(maximumWidth) || 1);
    if (!value.length) {
      return [{ start: 0, end: 0, width: 0 }];
    }

    const tokens = [];
    const tokenPattern = /\S+\s*|\s+/g;
    let match = null;
    while ((match = tokenPattern.exec(value))) {
      tokens.push({
        start: match.index,
        end: match.index + match[0].length,
        text: match[0],
      });
    }
    if (!tokens.length) {
      return [{ start: 0, end: value.length, width: estimateTextHighlightLayoutInlineWidth(value, fontSize) }];
    }

    const lines = [];
    let lineStart = tokens[0].start;
    let lineEnd = tokens[0].start;
    let lineWidth = 0;
    for (const token of tokens) {
      const tokenWidth = estimateTextHighlightLayoutInlineWidth(token.text, fontSize);
      if (lineEnd > lineStart && lineWidth + tokenWidth > widthLimit) {
        lines.push(buildEstimatedTextHighlightSoftLine(value, lineStart, lineEnd, fontSize));
        lineStart = token.start;
        lineEnd = token.end;
        lineWidth = tokenWidth;
      } else {
        lineEnd = token.end;
        lineWidth += tokenWidth;
      }
    }

    if (lineEnd >= lineStart) {
      lines.push(buildEstimatedTextHighlightSoftLine(value, lineStart, lineEnd, fontSize));
    }

    return lines.length ? lines : [{ start: 0, end: value.length, width: estimateTextHighlightLayoutInlineWidth(value, fontSize) }];
  }

  function buildEstimatedTextHighlightSoftLine(text, start, end, fontSize) {
    const value = String(text || "");
    const rangeStart = Math.max(0, Math.min(value.length, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(value.length, Math.floor(Number(end) || 0)));
    const renderedText = value.slice(rangeStart, rangeEnd).replace(/\s+$/g, "");
    return {
      start: rangeStart,
      end: rangeEnd,
      width: estimateTextHighlightLayoutInlineWidth(renderedText, fontSize),
    };
  }

  function countEstimatedTextHighlightSoftLinesBefore(characters, end, maximumWidth, fontSize) {
    const prefixEnd = Math.max(0, Math.min(String(characters || "").length, Math.floor(Number(end) || 0)));
    const prefix = normalizeLineEndings(String(characters || "").slice(0, prefixEnd));
    if (!prefix) {
      return 0;
    }

    const hardLines = prefix.split("\n");
    if (prefix.endsWith("\n")) {
      hardLines.pop();
    }

    let lineCount = 0;
    for (const hardLine of hardLines) {
      lineCount += Math.max(1, buildEstimatedTextHighlightSoftLines(hardLine, maximumWidth, fontSize).length);
    }
    return lineCount;
  }

  function estimateTextHighlightLayoutInlineWidth(text, fontSize) {
    return estimateTextHighlightInlineTextWidth(
      String(text || "").replace(/[\u200B-\u200D\uFEFF]/g, ""),
      fontSize
    );
  }

  async function measureAlignmentSensitiveTextHighlightSelectionFromLeftLayout(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    selectedText
  ) {
    if (!node || typeof node.clone !== "function") {
      return [];
    }

    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart)) {
      return [];
    }

    const leftClone = node.clone();
    try {
      try {
        await loadFontsForTextNode(leftClone);
      } catch (error) {}
      if ("textAlignHorizontal" in leftClone) {
        try {
          leftClone.textAlignHorizontal = "LEFT";
        } catch (error) {}
      }
      await waitForTextHighlightGeometryTick(16);

      const leftSelectedRows = await measureLeftAlignedTextHighlightSelectionRows(
        leftClone,
        rangeStart,
        rangeEnd,
        textColorHex,
        fontSize,
        lineHeight,
        selectedText
      );
      if (
        !leftSelectedRows.length ||
        hasOverwideTextHighlightSelectionRows(leftSelectedRows, selectedText, fontSize, lineHeight) ||
        hasUnderwideTextHighlightSelectionRows(leftSelectedRows, selectedText, fontSize)
      ) {
        return [];
      }

      const leftLineRows = await measureTextHighlightAllSoftLineRows(
        leftClone,
        textColorHex,
        fontSize,
        lineHeight,
        { preferGlyphBounds: true }
      );
      if (!leftLineRows.length) {
        return [];
      }
      const nodeTransform = getAbsoluteTransformMatrix(node);
      const inverseNodeTransform = invertAffineTransform(nodeTransform);
      const leftCloneTransform = getAbsoluteTransformMatrix(leftClone);
      const inverseLeftCloneTransform = invertAffineTransform(leftCloneTransform);
      const layoutLocalBounds = inverseNodeTransform
        ? getTextHighlightTypographyLocalBounds(node, inverseNodeTransform)
        : null;
      const alignment = getTextHighlightHorizontalAlignment(node);
      if (
        !inverseNodeTransform ||
        !inverseLeftCloneTransform ||
        !layoutLocalBounds ||
        (alignment !== "CENTER" && alignment !== "RIGHT")
      ) {
        return [];
      }

      const estimatedLineIndex = getEstimatedTextHighlightSoftLineIndexForSelection(
        node,
        rangeStart,
        rangeEnd,
        fontSize
      );
      const translatedRows = [];
      for (const selectedRow of leftSelectedRows) {
        const measuredLineIndex = findTextHighlightSoftLineIndexForRow(
          selectedRow,
          leftLineRows,
          fontSize,
          lineHeight
        );
        // The cloned node has the real Figma wrap row; the estimator is only a fallback.
        let lineIndex = measuredLineIndex;
        if (lineIndex < 0 && estimatedLineIndex >= 0 && estimatedLineIndex < leftLineRows.length) {
          lineIndex = estimatedLineIndex;
        }
        if (lineIndex < 0) {
          continue;
        }

        const leftLineRow = normalizeTextHighlightWorldBounds(leftLineRows[lineIndex]);
        const selectedBounds = normalizeTextHighlightWorldBounds(selectedRow);
        if (!leftLineRow || !selectedBounds) {
          continue;
        }

        const leftLineLocalBounds = getTextHighlightLocalBounds(inverseLeftCloneTransform, leftLineRow);
        const selectedLocalBounds = getTextHighlightLocalBounds(inverseLeftCloneTransform, selectedBounds);
        const lineRight = leftLineLocalBounds.x + leftLineLocalBounds.width;
        const lineCenter = leftLineLocalBounds.x + leftLineLocalBounds.width / 2;
        const layoutRight = layoutLocalBounds.x + layoutLocalBounds.width;
        const layoutCenter = layoutLocalBounds.x + layoutLocalBounds.width / 2;
        const offsetX =
          alignment === "RIGHT"
            ? layoutRight - lineRight
            : layoutCenter - lineCenter;
        const localHeight = selectedLocalBounds.height;
        const alignedLineWorldBounds = getTextHighlightWorldBoundsFromLocalBounds(node, {
          x: leftLineLocalBounds.x + offsetX,
          y: leftLineLocalBounds.y,
          width: leftLineLocalBounds.width,
          height: leftLineLocalBounds.height,
        });
        const alignedSelectedWorldBounds = getTextHighlightWorldBoundsFromLocalBounds(node, {
          x: selectedLocalBounds.x + offsetX,
          y: selectedLocalBounds.y,
          width: selectedLocalBounds.width,
          height: localHeight,
        });
        const alignedLineBounds = normalizeTextHighlightWorldBounds(alignedLineWorldBounds);
        const alignedSelectedBounds = normalizeTextHighlightWorldBounds(alignedSelectedWorldBounds);
        if (!alignedLineBounds || !alignedSelectedBounds) {
          continue;
        }

        const minimumWidth = getTextHighlightMinimumWidth(fontSize);
        const alignedRight = alignedLineBounds.x + alignedLineBounds.width;
        let nextX = roundTextHighlightMetric(
          Math.max(alignedLineBounds.x, Math.min(alignedRight - minimumWidth, alignedSelectedBounds.x))
        );
        const nextWidth = roundTextHighlightMetric(
          Math.max(minimumWidth, Math.min(alignedSelectedBounds.width, alignedRight - nextX))
        );
        if (alignment === "RIGHT") {
          nextX = roundTextHighlightMetric(
            Math.max(alignedLineBounds.x, nextX - getRightAlignedTextHighlightNudge(fontSize))
          );
        }
        translatedRows.push({
          x: nextX,
          y: alignedSelectedBounds.y,
          width: nextWidth,
          height: alignedSelectedBounds.height,
        });
      }

      const estimatedRows = buildEstimatedAlignmentSensitiveTextHighlightSelectionBounds(
        node,
        rangeStart,
        rangeEnd,
        fontSize,
        lineHeight,
        selectedText
      );
      const reconciledRows = reconcileAlignmentSensitiveTextHighlightRowsWithEstimate(
        translatedRows,
        estimatedRows,
        fontSize
      );
      const safeRows = sortTextHighlightBoundsList(reconciledRows).filter(
        (row) =>
          !hasOverwideTextHighlightSelectionRows([row], selectedText, fontSize, lineHeight) &&
          !hasUnderwideTextHighlightSelectionRows([row], selectedText, fontSize)
      );
      return safeRows;
    } catch (error) {
      return [];
    } finally {
      if (leftClone && !leftClone.removed) {
        leftClone.remove();
      }
    }
  }

  function reconcileAlignmentSensitiveTextHighlightRowsWithEstimate(rows, estimatedRows, fontSize) {
    const sortedRows = sortTextHighlightBoundsList(rows);
    const sortedEstimatedRows = sortTextHighlightBoundsList(estimatedRows);
    if (sortedRows.length !== 1 || sortedEstimatedRows.length !== 1) {
      return sortedRows;
    }

    const row = normalizeTextHighlightWorldBounds(sortedRows[0]);
    const estimated = normalizeTextHighlightWorldBounds(sortedEstimatedRows[0]);
    if (!row || !estimated) {
      return sortedRows;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const tolerance = Math.max(2, size * 0.28);
    const xMismatch = Math.abs(row.x - estimated.x);
    const yMismatch = Math.abs(row.y - estimated.y);
    if (xMismatch <= tolerance && yMismatch <= tolerance) {
      return sortedRows;
    }
    if (yMismatch > tolerance) {
      return sortedRows;
    }

    return sortedRows;
  }

  function getEstimatedTextHighlightSoftLineIndexForSelection(node, start, end, fontSize) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart)) {
      return -1;
    }

    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    const layoutLocalBounds = inverseNodeTransform
      ? getTextHighlightTypographyLocalBounds(node, inverseNodeTransform)
      : null;
    if (!layoutLocalBounds || !(layoutLocalBounds.width > 0)) {
      return -1;
    }

    const hardLineRange = getTextHighlightLineRangeForSelection(characters, rangeStart, rangeEnd);
    if (!hardLineRange || !(hardLineRange.end > hardLineRange.start)) {
      return -1;
    }

    const hardLineText = characters.slice(hardLineRange.start, hardLineRange.end);
    const localSelectionStart = rangeStart - hardLineRange.start;
    const localSelectionEnd = rangeEnd - hardLineRange.start;
    const softLines = buildEstimatedTextHighlightSoftLines(hardLineText, layoutLocalBounds.width, fontSize);
    if (!softLines.length) {
      return -1;
    }

    const selectionMidpoint = (localSelectionStart + localSelectionEnd) / 2;
    let softLineIndex = softLines.findIndex(
      (line) => selectionMidpoint >= line.start && selectionMidpoint <= Math.max(line.start, line.end)
    );
    if (softLineIndex < 0) {
      softLineIndex = softLines.findIndex(
        (line) => localSelectionStart >= line.start && localSelectionStart <= Math.max(line.start, line.end)
      );
    }
    if (softLineIndex < 0) {
      return -1;
    }

    return (
      countEstimatedTextHighlightSoftLinesBefore(
        characters,
        hardLineRange.start,
        layoutLocalBounds.width,
        fontSize
      ) + softLineIndex
    );
  }

  function getRightAlignedTextHighlightNudge(fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    return roundTextHighlightMetric(Math.max(1, Math.min(1.5, size * 0.06)));
  }

  async function measureLeftAlignedTextHighlightSelectionRows(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    selectedText
  ) {
    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart)) {
      return [];
    }

    const endMeasurement = await measureTextHighlightPrefixBounds(node, rangeEnd, textColorHex, fontSize, lineHeight);
    const endBoundsList = Array.isArray(endMeasurement && endMeasurement.boundsList) ? endMeasurement.boundsList : [];
    if (!endBoundsList.length) {
      return [];
    }

    if (rangeStart <= 0) {
      return sortTextHighlightBoundsList(endBoundsList);
    }

    const startMeasurement = await measureTextHighlightPrefixBounds(node, rangeStart, textColorHex, fontSize, lineHeight);
    const startBoundsList = Array.isArray(startMeasurement && startMeasurement.boundsList) ? startMeasurement.boundsList : [];
    if (!startBoundsList.length) {
      return [];
    }

    let rows = await buildTextHighlightSelectionRowsFromPrefixStart(
      node,
      rangeStart,
      rangeEnd,
      textColorHex,
      fontSize,
      lineHeight,
      endBoundsList,
      startBoundsList,
      selectedText
    );
    if (!rows.length) {
      rows = subtractTextHighlightPrefixBounds(endBoundsList, startBoundsList, fontSize, lineHeight);
    }
    if (!rows.length) {
      rows = extractTrailingTextHighlightBounds(endBoundsList, startBoundsList, fontSize, lineHeight);
    }

    return sortTextHighlightBoundsList(rows);
  }

  async function measureTextHighlightAllSoftLineRows(node, textColorHex, fontSize, lineHeight, options) {
    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    if (!(characterCount > 0)) {
      return [];
    }

    let measurement = null;
    const preferGlyphBounds = !!(options && options.preferGlyphBounds);
    if (preferGlyphBounds) {
      try {
        measurement = await measureExactTextHighlightBounds(node, 0, characterCount, textColorHex, fontSize, lineHeight);
      } catch (error) {}
      let glyphRows = getTextHighlightMeasurementBoundsList(measurement);
      if (hasMergedTextHighlightBlockBounds(glyphRows, fontSize)) {
        glyphRows = splitMergedTextHighlightBlockBounds(glyphRows, fontSize, lineHeight);
      }
      glyphRows = sortTextHighlightBoundsList(glyphRows);
      if (glyphRows.length) {
        return glyphRows;
      }
    }

    try {
      measurement = await measureTextHighlightUnderlineGeometryBounds(node, 0, characterCount, fontSize, lineHeight);
    } catch (error) {}

    let rows = sortTextHighlightBoundsList(getTextHighlightMeasurementBoundsList(measurement));
    if (!rows.length || hasMergedTextHighlightBlockBounds(rows, fontSize)) {
      try {
        measurement = await measureExactTextHighlightBounds(node, 0, characterCount, textColorHex, fontSize, lineHeight);
      } catch (error) {}
      rows = getTextHighlightMeasurementBoundsList(measurement);
      if (hasMergedTextHighlightBlockBounds(rows, fontSize)) {
        rows = splitMergedTextHighlightBlockBounds(rows, fontSize, lineHeight);
      }
      rows = sortTextHighlightBoundsList(rows);
    }

    return rows;
  }

  function findTextHighlightSoftLineIndexForRow(targetRow, lineRows, fontSize, lineHeight) {
    const target = normalizeTextHighlightWorldBounds(targetRow);
    const rows = sortTextHighlightBoundsList(lineRows);
    if (!target || !rows.length) {
      return -1;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const rowTolerance = Math.max(getTextHighlightRowTolerance(fontSize, lineHeight), size * 0.28);
    const targetCenter = target.y + target.height / 2;
    let bestIndex = -1;
    let bestDistance = Infinity;
    for (let index = 0; index < rows.length; index += 1) {
      const row = normalizeTextHighlightWorldBounds(rows[index]);
      if (!row || !doTextHighlightBoundsShareRow(target, row, rowTolerance)) {
        continue;
      }

      const distance = Math.abs(targetCenter - (row.y + row.height / 2));
      if (distance < bestDistance) {
        bestDistance = distance;
        bestIndex = index;
      }
    }

    return bestIndex;
  }

  async function measureAlignmentSensitiveTextHighlightRangeRows(node, start, end, textColorHex, fontSize, lineHeight) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart)) {
      return [];
    }

    let measurement = null;
    try {
      measurement = await measureExactTextHighlightBounds(node, rangeStart, rangeEnd, textColorHex, fontSize, lineHeight);
    } catch (error) {}

    let rows = getTextHighlightMeasurementBoundsList(measurement);
    if (hasMergedTextHighlightBlockBounds(rows, fontSize)) {
      rows = splitMergedTextHighlightBlockBounds(rows, fontSize, lineHeight);
    }

    return filterTextHighlightGlyphBoundsRows(rows, fontSize, lineHeight);
  }

  function findTextHighlightSameRowForTarget(targetRow, referenceRows, fontSize, lineHeight) {
    const target = normalizeTextHighlightWorldBounds(targetRow);
    const rows = sortTextHighlightBoundsList(referenceRows);
    if (!target || !rows.length) {
      return null;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const rowTolerance = Math.max(getTextHighlightRowTolerance(fontSize, lineHeight), size * 0.28);
    const targetCenter = target.y + target.height / 2;
    let bestRow = null;
    let bestDistance = Infinity;
    for (const row of rows) {
      const bounds = normalizeTextHighlightWorldBounds(row);
      if (!bounds || !doTextHighlightBoundsShareRow(target, bounds, rowTolerance)) {
        continue;
      }

      const distance = Math.abs(targetCenter - (bounds.y + bounds.height / 2));
      if (distance < bestDistance) {
        bestDistance = distance;
        bestRow = bounds;
      }
    }

    return bestRow;
  }

  function getTextHighlightLineRangeForSelection(characters, start, end) {
    if (typeof characters !== "string" || !characters.length) {
      return null;
    }

    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    let lineStart = rangeStart;
    while (lineStart > 0 && !isTextHighlightLineBreakCharacter(characters[lineStart - 1])) {
      lineStart -= 1;
    }

    let lineEnd = rangeEnd;
    while (lineEnd < characterCount && !isTextHighlightLineBreakCharacter(characters[lineEnd])) {
      lineEnd += 1;
    }

    return {
      start: lineStart,
      end: Math.max(lineStart, lineEnd),
    };
  }

  function isTextHighlightLineBreakCharacter(character) {
    return character === "\n" || character === "\r" || character === "\u2028" || character === "\u2029";
  }

  async function measureTextHighlightStableLineRow(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    lineText
  ) {
    let lineMeasurement = null;
    try {
      lineMeasurement = await measureTextHighlightUnderlineGeometryBounds(node, start, end, fontSize, lineHeight);
    } catch (error) {}

    let lineRows = sortTextHighlightBoundsList(getTextHighlightMeasurementBoundsList(lineMeasurement));
    if (!lineRows.length || hasOverwideTextHighlightSelectionRows(lineRows, lineText, fontSize, lineHeight)) {
      try {
        lineMeasurement = await measureExactTextHighlightBounds(node, start, end, textColorHex, fontSize, lineHeight);
      } catch (error) {}
      lineRows = sortTextHighlightBoundsList(getTextHighlightMeasurementBoundsList(lineMeasurement));
    }

    if (lineRows.length) {
      return normalizeTextHighlightWorldBounds(mergeTextHighlightBoundsList(lineRows));
    }

    return buildAlignmentSensitiveTextHighlightFallbackLineBounds(node, start, fontSize, lineHeight, lineText);
  }

  function buildAlignmentSensitiveTextHighlightFallbackLineBounds(node, lineStart, fontSize, lineHeight, lineText) {
    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      return null;
    }

    const layoutLocalBounds = getTextHighlightTypographyLocalBounds(node, inverseNodeTransform);
    if (!layoutLocalBounds) {
      return null;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const metrics = buildTextHighlightBoxGlyphMetrics(size, resolvedLineHeight);
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const lineIndex = Math.max(0, countTextLines(characters.slice(0, Math.max(0, lineStart))) - 1);
    const lineWidth = Math.max(
      getTextHighlightMinimumWidth(size),
      Math.min(layoutLocalBounds.width, estimateTextHighlightInlineTextWidth(lineText, size))
    );
    const alignment = getTextHighlightHorizontalAlignment(node);
    let localX = layoutLocalBounds.x;
    if (alignment === "RIGHT") {
      localX = layoutLocalBounds.x + Math.max(0, layoutLocalBounds.width - lineWidth);
    } else if (alignment === "CENTER") {
      localX = layoutLocalBounds.x + Math.max(0, (layoutLocalBounds.width - lineWidth) / 2);
    }

    const localY =
      layoutLocalBounds.y +
      lineIndex * resolvedLineHeight +
      Math.max(0, (resolvedLineHeight - metrics.height) / 2);
    return getTextHighlightWorldBoundsFromLocalBounds(node, {
      x: roundTextHighlightMetric(localX),
      y: roundTextHighlightMetric(localY),
      width: roundTextHighlightMetric(lineWidth),
      height: metrics.height,
    });
  }

  async function measureTextHighlightInlineRangeWidth(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    text,
    maximumWidth
  ) {
    const size = Math.max(12, Number(fontSize) || 16);
    const maxWidth = Math.max(0, Number(maximumWidth) || 0);
    if (!text) {
      return 0;
    }

    const estimatedWidth = estimateTextHighlightInlineTextWidth(text, size);
    if (!compactText(text)) {
      return roundTextHighlightMetric(Math.min(maxWidth || estimatedWidth, estimatedWidth));
    }

    const measuredWidth = await measureTextHighlightSelectedWidth(
      node,
      start,
      end,
      textColorHex,
      fontSize,
      lineHeight,
      text,
      maxWidth || estimatedWidth
    );
    const nextWidth = measuredWidth > 0 ? measuredWidth : estimatedWidth;
    return roundTextHighlightMetric(Math.min(maxWidth || nextWidth, Math.max(nextWidth, estimatedWidth * 0.35)));
  }

  function estimateTextHighlightInlineTextWidth(text, fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    let width = 0;
    for (const character of String(text || "")) {
      const codePoint = character.codePointAt(0);
      if (character === "\t") {
        width += size * 1.12;
      } else if (character === " ") {
        width += size * 0.28;
      } else if (typeof codePoint === "number" && isWideTextHighlightCodePoint(codePoint)) {
        width += size * 0.95;
      } else if (/[ilI.,'`|!]/.test(character)) {
        width += size * 0.28;
      } else if (/[mwMW@#%&]/.test(character)) {
        width += size * 0.78;
      } else {
        width += size * 0.56;
      }
    }

    return roundTextHighlightMetric(Math.max(0, width));
  }

  function isWideTextHighlightCodePoint(codePoint) {
    return (
      (codePoint >= 0x1100 && codePoint <= 0x11ff) ||
      (codePoint >= 0x2e80 && codePoint <= 0xa4cf) ||
      (codePoint >= 0xac00 && codePoint <= 0xd7af) ||
      (codePoint >= 0xf900 && codePoint <= 0xfaff) ||
      (codePoint >= 0xff01 && codePoint <= 0xff60)
    );
  }

  function getTextHighlightHorizontalAlignment(node) {
    if (!node || !("textAlignHorizontal" in node)) {
      return "LEFT";
    }

    try {
      const alignment = String(node.textAlignHorizontal || "").trim().toUpperCase();
      return alignment || "LEFT";
    } catch (error) {}

    return "LEFT";
  }

  async function buildTextHighlightSelectionRowsFromPrefixStart(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    endBoundsList,
    startBoundsList,
    selectedText
  ) {
    if (!selectedText || /[\r\n]/.test(selectedText)) {
      return [];
    }

    const endRows = sortTextHighlightBoundsList(endBoundsList);
    if (!endRows.length) {
      return [];
    }

    const startRows = sortTextHighlightBoundsList(startBoundsList);
    const targetRowIndex = endRows.length - 1;
    const targetRow = endRows[targetRowIndex];
    if (!targetRow) {
      return [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const rowTolerance = getTextHighlightRowTolerance(fontSize, lineHeight);
    const minimumWidth = getTextHighlightMinimumWidth(fontSize);
    const endRight = targetRow.x + targetRow.width;
    let startRight = -Infinity;

    if ((Number(start) || 0) <= 0) {
      startRight = targetRow.x;
    } else if (startRows.length === endRows.length) {
      startRight = findTextHighlightMatchedStartRight(
        targetRow,
        startRows,
        rowTolerance,
        fontSize,
        lineHeight
      );
      if (!Number.isFinite(startRight)) {
        startRight = findTextHighlightIndexMatchedStartRight(
          targetRow,
          startRows,
          targetRowIndex,
          fontSize,
          lineHeight
        );
      }
      if (Number.isFinite(startRight)) {
        startRight += getTextHighlightWhitespaceAdvanceBeforeSelection(node, start, fontSize);
      }
    } else if (endRows.length === startRows.length + 1) {
      startRight = targetRow.x;
    }

    if (!Number.isFinite(startRight)) {
      return [];
    }

    const nodeBounds = normalizeTextHighlightWorldBounds(getNodeRenderBounds(node));
    const maximumWidth =
      nodeBounds && Number.isFinite(nodeBounds.width)
        ? Math.max(targetRow.width, nodeBounds.width)
        : Math.max(targetRow.width, endRight - startRight);
    const selectedWidth = await measureTextHighlightSelectedWidth(
      node,
      start,
      end,
      textColorHex,
      fontSize,
      lineHeight,
      selectedText,
      maximumWidth
    );
    if (!(selectedWidth > 0)) {
      return [];
    }

    const prefixWidth = endRight - startRight;
    const selectedWidthLooksUnderwide = hasUnderwideTextHighlightSelectionRows(
      [
        {
          x: targetRow.x,
          y: targetRow.y,
          width: selectedWidth,
          height: targetRow.height,
        },
      ],
      selectedText,
      fontSize
    );
    const trustPrefixWidth =
      prefixWidth >= minimumWidth &&
      (selectedWidthLooksUnderwide ||
        prefixWidth <= Math.max(selectedWidth * 1.28, selectedWidth + size * 0.65));
    const nextX = roundTextHighlightMetric(Math.max(targetRow.x, Math.min(startRight, endRight - minimumWidth)));
    let nextWidth = trustPrefixWidth ? prefixWidth : selectedWidth;
    if (nodeBounds) {
      const maxRight = nodeBounds.x + nodeBounds.width + Math.max(1, size * 0.2);
      nextWidth = Math.min(nextWidth, Math.max(minimumWidth, maxRight - nextX));
    }
    nextWidth = roundTextHighlightMetric(Math.max(minimumWidth, nextWidth));

    const row = {
      x: nextX,
      y: targetRow.y,
      width: nextWidth,
      height: targetRow.height,
    };

    if (hasOverwideTextHighlightSelectionRows([row], selectedText, fontSize, lineHeight)) {
      return [];
    }

    return [row];
  }

  function getTextHighlightWhitespaceAdvanceBeforeSelection(node, start, fontSize) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const rangeStart = Math.max(0, Math.min(characters.length, Math.floor(Number(start) || 0)));
    if (rangeStart <= 0) {
      return 0;
    }

    let whitespaceCount = 0;
    for (let index = rangeStart - 1; index >= 0; index -= 1) {
      const character = characters[index];
      if (character === "\n" || character === "\r") {
        break;
      }
      if (!/\s/.test(character)) {
        break;
      }
      whitespaceCount += 1;
    }

    if (!whitespaceCount) {
      return 0;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    return roundTextHighlightMetric(whitespaceCount * size * 0.28);
  }

  function findTextHighlightReferenceRowForTarget(targetRow, referenceRows, fontSize, lineHeight) {
    const target = normalizeTextHighlightWorldBounds(targetRow);
    const rows = sortTextHighlightBoundsList(referenceRows);
    if (!target || !rows.length) {
      return null;
    }

    const rowTolerance = Math.max(getTextHighlightRowTolerance(fontSize, lineHeight), Math.max(12, Number(fontSize) || 16) * 0.28);
    let bestRow = null;
    let bestDistance = Infinity;
    for (const row of rows) {
      if (!doTextHighlightBoundsShareRow(target, row, rowTolerance)) {
        continue;
      }

      const targetCenter = target.y + target.height / 2;
      const rowCenter = row.y + row.height / 2;
      const distance = Math.abs(targetCenter - rowCenter);
      if (distance < bestDistance) {
        bestDistance = distance;
        bestRow = row;
      }
    }

    return bestRow || rows[rows.length - 1];
  }

  async function measureTextHighlightSelectedWidth(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    selectedText,
    maximumWidth
  ) {
    try {
      const measurement = await measureTextHighlightSelectedOnlyBounds(
        node,
        start,
        end,
        textColorHex,
        fontSize,
        lineHeight
      );
      const measuredBounds = normalizeTextHighlightWorldBounds(
        mergeTextHighlightBoundsList(getTextHighlightMeasurementBoundsList(measurement))
      );
      if (measuredBounds && measuredBounds.width > 0 && measuredBounds.width <= Math.max(1, Number(maximumWidth) || Infinity)) {
        return measuredBounds.width;
      }
    } catch (error) {}

    const size = Math.max(12, Number(fontSize) || 16);
    const selectedLength = Math.max(1, compactText(selectedText).length);
    const estimatedWidth = selectedLength * size * 0.56;
    return roundTextHighlightMetric(Math.max(getTextHighlightMinimumWidth(fontSize), Math.min(Number(maximumWidth) || estimatedWidth, estimatedWidth)));
  }

  function hasOverwideTextHighlightSelectionRows(boundsList, selectedText, fontSize, lineHeight) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length || !selectedText || /[\r\n]/.test(selectedText)) {
      return false;
    }
    if (rows.length > 1) {
      return true;
    }

    const mergedBounds = normalizeTextHighlightWorldBounds(mergeTextHighlightBoundsList(rows));
    if (!mergedBounds) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const selectedLength = Math.max(1, compactText(selectedText).length);
    const maxExpectedWidth = Math.max(size * 2.8, selectedLength * size * 1.65);
    const maxExpectedHeight = Math.max(resolvedLineHeight * 1.6, size * 2);
    return mergedBounds.width > maxExpectedWidth && mergedBounds.height <= maxExpectedHeight;
  }

  function isPlausibleSoftWrappedTextHighlightSelectionRows(node, boundsList, selectedText, fontSize, lineHeight) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (rows.length < 2 || !selectedText || /[\r\n]/.test(selectedText)) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const nodeBounds = normalizeTextHighlightWorldBounds(getNodeRenderBounds(node));
    const estimatedTextWidth = Math.max(
      getTextHighlightMinimumWidth(size),
      estimateTextHighlightInlineTextWidth(selectedText, size)
    );
    const wrapWidth = nodeBounds && nodeBounds.width > 0 ? Math.max(size, nodeBounds.width) : estimatedTextWidth;
    const maximumExpectedRows = Math.max(2, Math.ceil(estimatedTextWidth / Math.max(size, wrapWidth * 0.86)) + 2);
    if (rows.length > maximumExpectedRows) {
      return false;
    }

    const rowHeightLimit = Math.max(resolvedLineHeight * 1.75, size * 2.1);
    const rowWidthLimit = nodeBounds ? nodeBounds.width + size * 2 : estimatedTextWidth + size * 2;
    const expandedNodeBounds = nodeBounds
      ? {
          x: nodeBounds.x - size,
          y: nodeBounds.y - resolvedLineHeight,
          width: nodeBounds.width + size * 2,
          height: nodeBounds.height + resolvedLineHeight * 2,
        }
      : null;
    let totalWidth = 0;
    for (const row of rows) {
      const bounds = normalizeTextHighlightWorldBounds(row);
      if (!bounds || bounds.height > rowHeightLimit || bounds.width > rowWidthLimit) {
        return false;
      }
      if (expandedNodeBounds && !doTextHighlightBoundsIntersect(bounds, expandedNodeBounds)) {
        return false;
      }
      totalWidth += bounds.width;
    }

    const maximumTotalWidth = Math.max(estimatedTextWidth * 1.55, wrapWidth * rows.length + size);
    return totalWidth <= maximumTotalWidth;
  }

  function hasUnderwideTextHighlightSelectionRows(boundsList, selectedText, fontSize) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length || !selectedText || /[\r\n]/.test(selectedText) || rows.length > 1) {
      return false;
    }

    const compactSelectedText = compactText(selectedText);
    if (compactSelectedText.length <= 1) {
      return false;
    }

    const bounds = normalizeTextHighlightWorldBounds(rows[0]);
    if (!bounds) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const estimatedWidth = estimateTextHighlightInlineTextWidth(compactSelectedText, size);
    const ratio = compactSelectedText.length <= 3 ? 0.28 : compactSelectedText.length <= 8 ? 0.36 : 0.45;
    const minimumExpectedWidth = Math.max(
      getTextHighlightMinimumWidth(size),
      estimatedWidth * ratio
    );
    return bounds.width < minimumExpectedWidth;
  }

  function isReasonableTextHighlightSelectionRow(row, selectedText, fontSize) {
    const bounds = normalizeTextHighlightWorldBounds(row);
    if (!bounds) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const selectedLength = Math.max(1, compactText(selectedText).length);
    const maxExpectedWidth = Math.max(size * 2.2, selectedLength * size * 1.25);
    return bounds.width <= maxExpectedWidth;
  }

  function constrainTextHighlightBaseRowToGlyphRow(baseRow, glyphRow, fontSize) {
    const base = normalizeTextHighlightWorldBounds(baseRow);
    const glyph = normalizeTextHighlightWorldBounds(glyphRow);
    if (!base || !glyph) {
      return base;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const baseRight = base.x + base.width;
    const glyphRight = glyph.x + glyph.width;
    const allowedOverflow = Math.max(2, Math.min(size * 0.5, 18));
    const widthTooWide = base.width > Math.max(glyph.width + size * 1.2, glyph.width * 1.35);
    const leaksOutsideGlyph =
      base.x < glyph.x - allowedOverflow ||
      baseRight > glyphRight + allowedOverflow;

    if (!widthTooWide && !leaksOutsideGlyph) {
      return base;
    }

    return {
      x: glyph.x,
      y: base.y,
      width: glyph.width,
      height: base.height,
    };
  }

  function filterTextHighlightGlyphBoundsRows(boundsList, fontSize, lineHeight) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length) {
      return [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const maxGlyphRowHeight = Math.max(size * 1.75, resolvedLineHeight * 1.35);
    return rows.filter((row) => row.height <= maxGlyphRowHeight);
  }

  function findTextHighlightGlyphBoundsForBaseRow(
    baseRow,
    glyphRows,
    usedGlyphRows,
    fontSize,
    lineHeight
  ) {
    const base = normalizeTextHighlightWorldBounds(baseRow);
    if (!base || !Array.isArray(glyphRows) || !glyphRows.length) {
      return null;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const rowTolerance = Math.max(getTextHighlightRowTolerance(fontSize, lineHeight), resolvedLineHeight * 0.32);
    const baseCenterY = base.y + base.height / 2;
    const baseRight = base.x + base.width;
    let bestIndex = -1;
    let bestScore = Infinity;

    for (let index = 0; index < glyphRows.length; index += 1) {
      if (usedGlyphRows[index]) {
        continue;
      }

      const glyph = normalizeTextHighlightWorldBounds(glyphRows[index]);
      if (!glyph) {
        continue;
      }

      const glyphRight = glyph.x + glyph.width;
      const glyphCenterY = glyph.y + glyph.height / 2;
      const verticalDistance = Math.abs(baseCenterY - glyphCenterY);
      const horizontalGap = Math.max(0, Math.max(base.x - glyphRight, glyph.x - baseRight));
      const sharesRow = doTextHighlightBoundsShareRow(base, glyph, rowTolerance) || verticalDistance <= rowTolerance;
      const isNearX = horizontalGap <= size;
      if (!sharesRow || !isNearX) {
        continue;
      }

      const score = verticalDistance + horizontalGap * 0.2;
      if (score < bestScore) {
        bestScore = score;
        bestIndex = index;
      }
    }

    if (bestIndex < 0) {
      return null;
    }

    usedGlyphRows[bestIndex] = true;
    return glyphRows[bestIndex];
  }

  function expandTextHighlightBoundsToGlyphHeight(baseRow, glyphRow, fontSize, lineHeight) {
    const base = normalizeTextHighlightWorldBounds(baseRow);
    const glyph = normalizeTextHighlightWorldBounds(glyphRow);
    if (!base || !glyph) {
      return base;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const targetTop = Math.min(base.y, glyph.y);
    const targetBottom = Math.max(base.y + base.height, glyph.y + glyph.height);
    const maxHeight = Math.max(size * 0.95, Math.min(size * 1.22, resolvedLineHeight * 1.05));
    const nextHeight = Math.min(maxHeight, Math.max(base.height, glyph.height, targetBottom - targetTop));
    let nextY = targetTop;
    if (nextY + nextHeight < glyph.y + glyph.height) {
      nextY = Math.min(base.y, glyph.y + glyph.height - nextHeight);
    }

    return {
      x: base.x,
      y: roundTextHighlightMetric(nextY),
      width: base.width,
      height: roundTextHighlightMetric(Math.max(1, nextHeight)),
    };
  }

  async function measureExactTextHighlightBounds(node, start, end, textColorHex, fontSize, lineHeight) {
    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (!(rangeEnd > rangeStart)) {
      return {
        bounds: null,
        boundsList: [],
        segments: [],
        fontSize,
        lineHeight,
      };
    }

    try {
      const clone = await createTextHighlightSelectionMeasurementClone(node, rangeStart, rangeEnd);
      const measurement = await measureTextHighlightCloneBounds(node, clone, fontSize, lineHeight, {
        targetColorHex: AI_TEXT_HIGHLIGHT_MEASURE_COLOR,
      });
      const boundsList = getTextHighlightMeasurementBoundsList(measurement);
      if (boundsList.length) {
        return measurement;
      }

      const fallbackBounds = normalizeTextHighlightWorldBounds(measurement && measurement.fallbackBounds);
      if (isPlausibleExactTextHighlightFallbackBounds(node, fallbackBounds, rangeStart, rangeEnd, fontSize, lineHeight)) {
        return {
          bounds: fallbackBounds,
          boundsList: [fallbackBounds],
          segments: [fallbackBounds],
          fallbackBounds,
          fontSize,
          lineHeight,
        };
      }

      return measurement;
    } catch (error) {
      return {
        bounds: null,
        boundsList: [],
        segments: [],
        fontSize,
        lineHeight,
      };
    }
  }

  async function measureTextHighlightPrefixBounds(node, end, textColorHex, fontSize, lineHeight) {
    const characterCount = node && typeof node.characters === "string" ? node.characters.length : 0;
    const prefixEnd = Math.max(
      0,
      Math.min(characterCount, Math.floor(Number(end) || 0))
    );
    if (!(prefixEnd > 0)) {
      return {
        bounds: null,
        boundsList: [],
        segments: [],
        fontSize,
        lineHeight,
      };
    }

    const clone = await createTextHighlightMeasurementClone(node, prefixEnd, textColorHex);
    try {
      const geometryMeasurement = await measureTextHighlightCloneGeometryBounds(node, clone, fontSize, lineHeight);
      if (getTextHighlightMeasurementBoundsList(geometryMeasurement).length) {
        return geometryMeasurement;
      }

      return await measureTextHighlightCloneBounds(node, clone, fontSize, lineHeight);
    } finally {
      if (clone && !clone.removed) {
        clone.remove();
      }
    }
  }

  async function measureTextHighlightCloneBounds(node, clone, fontSize, lineHeight, options) {
    const probeBounds = getNodeRenderBounds(node);
    const probe = figma.createFrame();
    let boundsList = [];
    let flattenedNode = null;

    try {
      probe.resize(
        Math.max(1, roundTextHighlightMetric(probeBounds ? probeBounds.width : Number(node && node.width) || 1)),
        Math.max(1, roundTextHighlightMetric(probeBounds ? probeBounds.height : Number(node && node.height) || 1))
      );
      probe.clipsContent = false;
      probe.fills = [];
      probe.strokes = [];
      probe.name = "__pigma_text_highlight_measure__";
      probe.x = roundTextHighlightMetric(probeBounds ? probeBounds.x : 0);
      probe.y = roundTextHighlightMetric(probeBounds ? probeBounds.y : 0);
      figma.currentPage.appendChild(probe);

      moveTextHighlightMeasureClone(node, clone, probe, probe.x, probe.y);
      boundsList = await measureTextHighlightProbeAlphaBounds(probe, fontSize, lineHeight, options);
      if (!boundsList.length && clone && typeof figma.flatten === "function") {
        try {
          flattenedNode = figma.flatten([clone], probe);
        } catch (error) {}
        boundsList = await measureTextHighlightProbeAlphaBounds(probe, fontSize, lineHeight, options);
      }

      const fallbackBounds =
        getVisibleTextHighlightBounds(flattenedNode && !flattenedNode.removed ? flattenedNode : clone) ||
        getVisibleTextHighlightBounds(probe);

      return {
        bounds: mergeTextHighlightBoundsList(boundsList),
        boundsList,
        segments: boundsList.slice(),
        fallbackBounds,
        fontSize,
        lineHeight,
      };
    } finally {
      if (flattenedNode && !flattenedNode.removed) {
        flattenedNode.remove();
      }
      if (probe && !probe.removed) {
        probe.remove();
      }
    }
  }

  async function measureTextHighlightCloneGeometryBounds(sourceNode, clone, fontSize, lineHeight) {
    const localBoundsList = await waitForTextHighlightFillGeometryBounds(clone, fontSize, lineHeight);
    const boundsList = localBoundsList
      .map((localBounds) => getTextHighlightWorldBoundsFromLocalBounds(sourceNode, localBounds))
      .filter(Boolean);

    return {
      bounds: mergeTextHighlightBoundsList(boundsList),
      boundsList,
      segments: boundsList.slice(),
      fontSize,
      lineHeight,
    };
  }

  async function waitForTextHighlightFillGeometryBounds(node, fontSize, lineHeight) {
    const attempts = 8;
    for (let attempt = 0; attempt < attempts; attempt += 1) {
      const boundsList = getTextHighlightFillGeometryLocalBounds(node, fontSize, lineHeight);
      if (boundsList.length) {
        return boundsList;
      }
      await waitForTextHighlightGeometryTick(attempt < 2 ? 1 : 16);
    }

    return [];
  }

  function getTextHighlightFillGeometryLocalBounds(node, fontSize, lineHeight) {
    const geometry = node && Array.isArray(node.fillGeometry) ? node.fillGeometry : [];
    if (!geometry.length) {
      return [];
    }

    const boundsList = [];
    for (let index = 0; index < geometry.length; index += 1) {
      const path = geometry[index];
      const pathBoundsList = parseTextHighlightSvgPathBoundsList(path && path.data, fontSize, lineHeight);
      for (const pathBounds of pathBoundsList) {
        boundsList.push(pathBounds);
      }
    }

    return mergeTextHighlightLocalBoundsByRows(boundsList, fontSize, lineHeight);
  }

  async function measureTextHighlightProbeAlphaBounds(probe, fontSize, lineHeight, options) {
    let alphaMeasurement = null;

    try {
      const pngBytes = await probe.exportAsync({
        format: "PNG",
        useAbsoluteBounds: false,
      });
      alphaMeasurement = await requestTextHighlightAlphaBounds({
        pngBytes,
        probeX: probe.x,
        probeY: probe.y,
        probeWidth: probe.width,
        probeHeight: probe.height,
        fontSize,
        lineHeight,
        targetColor: buildTextHighlightMeasureTargetColor(options && options.targetColorHex),
      });
    } catch (error) {}

    return getTextHighlightMeasurementBoundsList(alphaMeasurement);
  }

  function buildTextHighlightMeasureTargetColor(hexColor) {
    const hex = sanitizeHexColor(hexColor, "");
    if (!hex) {
      return null;
    }

    const rgb = hexToFigmaRgb(hex);
    return {
      r: Math.round(rgb.r * 255),
      g: Math.round(rgb.g * 255),
      b: Math.round(rgb.b * 255),
    };
  }

  async function createTextHighlightMeasurementClone(node, end, textColorHex) {
    if (!node || typeof node.clone !== "function") {
      throw new Error("텍스트 범위를 측정할 복제 레이어를 만들지 못했습니다.");
    }

    const clone = node.clone();

    if ("effects" in clone && Array.isArray(clone.effects)) {
      clone.effects = [];
    }
    if ("strokes" in clone && Array.isArray(clone.strokes)) {
      clone.strokes = [];
    }
    if ("opacity" in clone && typeof clone.opacity === "number") {
      clone.opacity = 1;
    }
    if ("blendMode" in clone) {
      clone.blendMode = "NORMAL";
    }
    if (typeof clone.setRangeFills !== "function") {
      throw new Error("선택 텍스트의 하이라이트 측정을 지원하지 않는 텍스트 레이어입니다.");
    }

    try {
      await loadFontsForTextNode(clone);
    } catch (error) {}

    const characterCount = typeof clone.characters === "string" ? clone.characters.length : 0;
    if (characterCount > 0) {
      truncateTextHighlightMeasurementClone(clone, end);
      const nextCharacterCount = typeof clone.characters === "string" ? clone.characters.length : 0;
      if (nextCharacterCount > 0) {
        clone.setRangeFills(0, nextCharacterCount, [createSolidPaint(textColorHex)]);
      }
    }
    return clone;
  }

  async function createTextHighlightSelectionMeasurementClone(node, start, end) {
    if (!node || typeof node.clone !== "function") {
      throw new Error("선택한 텍스트 범위를 측정할 복제 레이어를 만들지 못했습니다.");
    }

    const clone = node.clone();

    if ("effects" in clone && Array.isArray(clone.effects)) {
      clone.effects = [];
    }
    if ("strokes" in clone && Array.isArray(clone.strokes)) {
      clone.strokes = [];
    }
    if ("opacity" in clone && typeof clone.opacity === "number") {
      clone.opacity = 1;
    }
    if ("blendMode" in clone) {
      clone.blendMode = "NORMAL";
    }
    if (typeof clone.setRangeFills !== "function") {
      throw new Error("선택 텍스트의 하이라이트 측정을 지원하지 않는 텍스트 레이어입니다.");
    }

    try {
      await loadFontsForTextNode(clone);
    } catch (error) {}

    const characterCount = typeof clone.characters === "string" ? clone.characters.length : 0;
    if (characterCount > 0) {
      const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
      const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
      clearTextHighlightDecoration(clone, 0, characterCount);
      clone.setRangeFills(0, characterCount, [createTransparentPaint()]);
      if (rangeEnd > rangeStart) {
        clone.setRangeFills(rangeStart, rangeEnd, [createSolidPaint(AI_TEXT_HIGHLIGHT_MEASURE_COLOR)]);
      }
    }

    return clone;
  }

  async function refineSingleLineTextHighlightBoundsWithSelectedWidth(
    node,
    start,
    end,
    textColorHex,
    fontSize,
    lineHeight,
    boundsList,
    endBoundsList
  ) {
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const characterCount = characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    const selectedText = characters.slice(rangeStart, rangeEnd);
    if (!selectedText || /[\r\n]/.test(selectedText)) {
      return boundsList;
    }

    const sortedEndBoundsList = sortTextHighlightBoundsList(endBoundsList);
    const endRowBounds = sortedEndBoundsList.length ? sortedEndBoundsList[sortedEndBoundsList.length - 1] : null;
    if (!endRowBounds) {
      return boundsList;
    }

    const selectedOnlyMeasurement = await measureTextHighlightSelectedOnlyBounds(
      node,
      rangeStart,
      rangeEnd,
      textColorHex,
      fontSize,
      lineHeight
    );
    const selectedOnlyBounds = mergeTextHighlightBoundsList(
      getTextHighlightMeasurementBoundsList(selectedOnlyMeasurement)
    );
    const normalizedSelectedBounds = normalizeTextHighlightWorldBounds(selectedOnlyBounds);
    if (!normalizedSelectedBounds) {
      return boundsList;
    }

    const minimumWidth = getTextHighlightMinimumWidth(fontSize);
    const selectedWidth = roundTextHighlightMetric(
      Math.max(minimumWidth, Math.min(endRowBounds.width, normalizedSelectedBounds.width))
    );
    const endRight = endRowBounds.x + endRowBounds.width;
    const nextX = roundTextHighlightMetric(Math.max(endRowBounds.x, endRight - selectedWidth));
    const nextWidth = roundTextHighlightMetric(Math.max(minimumWidth, endRight - nextX));

    return [
      {
        x: nextX,
        y: endRowBounds.y,
        width: nextWidth,
        height: endRowBounds.height,
      },
    ];
  }

  async function measureTextHighlightSelectedOnlyBounds(node, start, end, textColorHex, fontSize, lineHeight) {
    const clone = await createTextHighlightSelectedOnlyMeasurementClone(node, start, end, textColorHex);
    try {
      const geometryMeasurement = await measureTextHighlightCloneGeometryBounds(node, clone, fontSize, lineHeight);
      if (getTextHighlightMeasurementBoundsList(geometryMeasurement).length) {
        return geometryMeasurement;
      }

      return await measureTextHighlightCloneBounds(node, clone, fontSize, lineHeight);
    } finally {
      if (clone && !clone.removed) {
        clone.remove();
      }
    }
  }

  async function createTextHighlightSelectedOnlyMeasurementClone(node, start, end, textColorHex) {
    if (!node || typeof node.clone !== "function") {
      throw new Error("선택 텍스트의 폭을 측정할 복제 레이어를 만들지 못했습니다.");
    }

    const clone = node.clone();

    if ("effects" in clone && Array.isArray(clone.effects)) {
      clone.effects = [];
    }
    if ("strokes" in clone && Array.isArray(clone.strokes)) {
      clone.strokes = [];
    }
    if ("opacity" in clone && typeof clone.opacity === "number") {
      clone.opacity = 1;
    }
    if ("blendMode" in clone) {
      clone.blendMode = "NORMAL";
    }
    if (typeof clone.setRangeFills !== "function") {
      throw new Error("선택 텍스트의 폭 측정을 지원하지 않는 텍스트 레이어입니다.");
    }

    try {
      await loadFontsForTextNode(clone);
    } catch (error) {}

    const characterCount = typeof clone.characters === "string" ? clone.characters.length : 0;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (rangeEnd < characterCount) {
      deleteTextHighlightMeasurementCloneRange(clone, rangeEnd, characterCount);
    }
    if (rangeStart > 0) {
      deleteTextHighlightMeasurementCloneRange(clone, 0, rangeStart);
    }

    const nextCharacterCount = typeof clone.characters === "string" ? clone.characters.length : 0;
    if (nextCharacterCount > 0) {
      clearTextHighlightDecoration(clone, 0, nextCharacterCount);
      clone.setRangeFills(0, nextCharacterCount, [createSolidPaint(textColorHex)]);
    }

    return clone;
  }

  function deleteTextHighlightMeasurementCloneRange(node, start, end) {
    if (!node || typeof node.characters !== "string") {
      return;
    }

    const characterCount = node.characters.length;
    const rangeStart = Math.max(0, Math.min(characterCount, Math.floor(Number(start) || 0)));
    const rangeEnd = Math.max(rangeStart, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (rangeEnd <= rangeStart) {
      return;
    }

    if (typeof node.deleteCharacters === "function") {
      try {
        node.deleteCharacters(rangeStart, rangeEnd);
        return;
      } catch (error) {}
    }

    try {
      node.characters = node.characters.slice(0, rangeStart) + node.characters.slice(rangeEnd);
    } catch (error) {}
  }

  function isPlausibleExactTextHighlightFallbackBounds(node, bounds, start, end, fontSize, lineHeight) {
    const fallbackBounds = normalizeTextHighlightWorldBounds(bounds);
    if (!fallbackBounds) {
      return false;
    }

    if (isWholeTextHighlightRange(node, start, end)) {
      return true;
    }

    const nodeBounds = normalizeTextHighlightWorldBounds(getNodeRenderBounds(node));
    if (!nodeBounds) {
      return true;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const nearFullWidth = fallbackBounds.width >= nodeBounds.width * 0.96;
    const nearFullHeight = fallbackBounds.height >= nodeBounds.height * 0.9;
    const looksLikeSingleTextRow = fallbackBounds.height <= Math.max(resolvedLineHeight * 1.35, size * 1.7);
    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const selectedText = characters.slice(
      Math.max(0, Math.min(characters.length, Math.floor(Number(start) || 0))),
      Math.max(0, Math.min(characters.length, Math.floor(Number(end) || 0)))
    );
    if (selectedText && !/[\r\n]/.test(selectedText) && looksLikeSingleTextRow) {
      const selectedLength = Math.max(1, compactText(selectedText).length);
      const maxExpectedWidth = Math.max(size * 2.6, selectedLength * size * 1.45);
      if (fallbackBounds.width > maxExpectedWidth) {
        return false;
      }
    }

    return !nearFullHeight || (!nearFullWidth && looksLikeSingleTextRow);
  }

  function hasSuspiciousTextHighlightDirectBounds(node, boundsList, start, end, fontSize, lineHeight) {
    const normalizedBoundsList = sortTextHighlightBoundsList(boundsList);
    if (!normalizedBoundsList.length || isWholeTextHighlightRange(node, start, end)) {
      return false;
    }

    const characters = node && typeof node.characters === "string" ? node.characters : "";
    const selectedText = characters.slice(
      Math.max(0, Math.min(characters.length, Math.floor(Number(start) || 0))),
      Math.max(0, Math.min(characters.length, Math.floor(Number(end) || 0)))
    );
    if (/[\r\n]/.test(selectedText)) {
      return false;
    }
    if (normalizedBoundsList.length > 1) {
      return !isPlausibleSoftWrappedTextHighlightSelectionRows(
        node,
        normalizedBoundsList,
        selectedText,
        fontSize,
        lineHeight
      );
    }

    const nodeBounds = normalizeTextHighlightWorldBounds(getNodeRenderBounds(node));
    if (!nodeBounds) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const mergedBounds = mergeTextHighlightBoundsList(normalizedBoundsList);
    const bounds = normalizeTextHighlightWorldBounds(mergedBounds);
    if (!bounds) {
      return false;
    }

    const selectedLength = compactText(selectedText).length;
    const estimatedMaxWidth = Math.max(size * 2, selectedLength * size * 0.95);
    const looksLikeWholeRow = bounds.height <= resolvedLineHeight * 1.35 && bounds.width >= nodeBounds.width * 0.72;
    return (
      (looksLikeWholeRow && bounds.width > estimatedMaxWidth) ||
      hasUnderwideTextHighlightSelectionRows(normalizedBoundsList, selectedText, fontSize)
    );
  }

  function isAlignmentSensitiveTextHighlightNode(node) {
    if (!node || !("textAlignHorizontal" in node)) {
      return false;
    }

    try {
      const alignment = String(node.textAlignHorizontal || "").trim().toUpperCase();
      return alignment === "CENTER" || alignment === "RIGHT" || alignment === "JUSTIFIED";
    } catch (error) {}

    return false;
  }

  function subtractTextHighlightPrefixBounds(endBoundsList, startBoundsList, fontSize, lineHeight) {
    const normalizedEndBoundsList = sortTextHighlightBoundsList(endBoundsList);
    const normalizedStartBoundsList = sortTextHighlightBoundsList(startBoundsList);
    if (!normalizedEndBoundsList.length) {
      return [];
    }
    if (!normalizedStartBoundsList.length) {
      return normalizedEndBoundsList;
    }

    const rowTolerance = getTextHighlightRowTolerance(fontSize, lineHeight);
    const minimumWidth = getTextHighlightMinimumWidth(fontSize);
    const nextBoundsList = [];

    for (let index = 0; index < normalizedEndBoundsList.length; index += 1) {
      const endBounds = normalizedEndBoundsList[index];
      const endRight = endBounds.x + endBounds.width;
      let matchedStartRight = findTextHighlightMatchedStartRight(
        endBounds,
        normalizedStartBoundsList,
        rowTolerance,
        fontSize,
        lineHeight
      );
      if (!Number.isFinite(matchedStartRight)) {
        matchedStartRight = findTextHighlightIndexMatchedStartRight(
          endBounds,
          normalizedStartBoundsList,
          index,
          fontSize,
          lineHeight
        );
      }
      if (!Number.isFinite(matchedStartRight)) {
        continue;
      }

      const nextX = Math.max(endBounds.x, Math.min(endRight, matchedStartRight));
      const nextWidth = endRight - nextX;
      if (nextWidth < minimumWidth) {
        continue;
      }

      nextBoundsList.push({
        x: roundTextHighlightMetric(nextX),
        y: endBounds.y,
        width: roundTextHighlightMetric(nextWidth),
        height: endBounds.height,
      });
    }

    return nextBoundsList;
  }

  function extractTrailingTextHighlightBounds(endBoundsList, startBoundsList, fontSize, lineHeight) {
    const normalizedEndBoundsList = sortTextHighlightBoundsList(endBoundsList);
    const normalizedStartBoundsList = sortTextHighlightBoundsList(startBoundsList);
    if (!normalizedEndBoundsList.length) {
      return [];
    }
    if (!normalizedStartBoundsList.length) {
      return normalizedEndBoundsList.slice(-1);
    }

    const rowTolerance = getTextHighlightRowTolerance(fontSize, lineHeight);
    const minimumWidth = getTextHighlightMinimumWidth(fontSize);

    for (let index = normalizedEndBoundsList.length - 1; index >= 0; index -= 1) {
      const endBounds = normalizedEndBoundsList[index];
      const endRight = endBounds.x + endBounds.width;
      let matchedStartRight = findTextHighlightMatchedStartRight(
        endBounds,
        normalizedStartBoundsList,
        rowTolerance,
        fontSize,
        lineHeight
      );
      if (!Number.isFinite(matchedStartRight)) {
        matchedStartRight = findTextHighlightIndexMatchedStartRight(
          endBounds,
          normalizedStartBoundsList,
          index,
          fontSize,
          lineHeight
        );
      }

      if (!Number.isFinite(matchedStartRight)) {
        continue;
      }

      const nextX = Math.max(endBounds.x, Math.min(endRight, matchedStartRight));
      const nextWidth = endRight - nextX;
      if (nextWidth >= minimumWidth) {
        return [
          {
            x: roundTextHighlightMetric(nextX),
            y: endBounds.y,
            width: roundTextHighlightMetric(nextWidth),
            height: endBounds.height,
          },
        ];
      }
    }

    return [];
  }

  function findTextHighlightMatchedStartRight(endBounds, startBoundsList, rowTolerance, fontSize, lineHeight) {
    const normalizedEndBounds = normalizeTextHighlightWorldBounds(endBounds);
    if (!normalizedEndBounds || !Array.isArray(startBoundsList) || !startBoundsList.length) {
      return -Infinity;
    }

    let matchedStartRight = -Infinity;
    for (const startBounds of startBoundsList) {
      if (!doTextHighlightBoundsShareRow(normalizedEndBounds, startBounds, rowTolerance)) {
        continue;
      }
      matchedStartRight = Math.max(matchedStartRight, startBounds.x + startBounds.width);
    }

    if (Number.isFinite(matchedStartRight)) {
      return matchedStartRight;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const relaxedTolerance = Math.max(rowTolerance * 2.5, Math.min(resolvedLineHeight * 0.55, size * 0.9));
    const endCenter = normalizedEndBounds.y + normalizedEndBounds.height / 2;
    const endBottom = normalizedEndBounds.y + normalizedEndBounds.height;
    let bestDistance = Infinity;
    let bestRight = -Infinity;

    for (const startBounds of startBoundsList) {
      const normalizedStartBounds = normalizeTextHighlightWorldBounds(startBounds);
      if (!normalizedStartBounds) {
        continue;
      }

      const startCenter = normalizedStartBounds.y + normalizedStartBounds.height / 2;
      const startBottom = normalizedStartBounds.y + normalizedStartBounds.height;
      const distance = Math.min(Math.abs(startCenter - endCenter), Math.abs(startBottom - endBottom));
      if (distance > relaxedTolerance || distance >= bestDistance) {
        continue;
      }

      bestDistance = distance;
      bestRight = normalizedStartBounds.x + normalizedStartBounds.width;
    }

    return bestRight;
  }

  function findTextHighlightIndexMatchedStartRight(endBounds, startBoundsList, index, fontSize, lineHeight) {
    const normalizedEndBounds = normalizeTextHighlightWorldBounds(endBounds);
    const normalizedStartBoundsList = sortTextHighlightBoundsList(startBoundsList);
    if (!normalizedEndBounds || !normalizedStartBoundsList.length) {
      return -Infinity;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const relaxedTolerance = Math.max(resolvedLineHeight * 0.65, size);
    const endRight = normalizedEndBounds.x + normalizedEndBounds.width;
    const endCenter = normalizedEndBounds.y + normalizedEndBounds.height / 2;
    const candidateIndexes = [index, index - 1, index + 1, normalizedStartBoundsList.length - 1];
    let bestRight = -Infinity;
    let bestScore = Infinity;

    for (const candidateIndex of candidateIndexes) {
      if (candidateIndex < 0 || candidateIndex >= normalizedStartBoundsList.length) {
        continue;
      }

      const candidate = normalizeTextHighlightWorldBounds(normalizedStartBoundsList[candidateIndex]);
      if (!candidate) {
        continue;
      }

      const candidateRight = candidate.x + candidate.width;
      const verticalDistance = Math.abs(candidate.y + candidate.height / 2 - endCenter);
      if (candidateRight <= normalizedEndBounds.x - size * 0.5 || candidateRight > endRight + size * 0.5) {
        continue;
      }
      if (verticalDistance > relaxedTolerance) {
        continue;
      }

      const score = verticalDistance + Math.abs(endRight - candidateRight) * 0.02;
      if (score < bestScore) {
        bestScore = score;
        bestRight = candidateRight;
      }
    }

    return bestRight;
  }

  function sortTextHighlightBoundsList(boundsList) {
    if (!Array.isArray(boundsList) || !boundsList.length) {
      return [];
    }

    return boundsList
      .map((bounds) => normalizeTextHighlightWorldBounds(bounds))
      .filter(Boolean)
      .sort((left, right) => {
        if (Math.abs(left.y - right.y) > 0.5) {
          return left.y - right.y;
        }
        if (Math.abs(left.x - right.x) > 0.5) {
          return left.x - right.x;
        }
        return left.width - right.width;
      });
  }

  function getTextHighlightRowTolerance(fontSize, lineHeight) {
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    return roundTextHighlightMetric(Math.max(2, Math.min(12, resolvedLineHeight * 0.18)));
  }

  function getTextHighlightMinimumWidth(fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    return roundTextHighlightMetric(Math.max(1, Math.min(6, size * 0.08)));
  }

  function doTextHighlightBoundsShareRow(leftBounds, rightBounds, rowTolerance) {
    const left = normalizeTextHighlightWorldBounds(leftBounds);
    const right = normalizeTextHighlightWorldBounds(rightBounds);
    if (!left || !right) {
      return false;
    }

    const leftBottom = left.y + left.height;
    const rightBottom = right.y + right.height;
    const overlap = Math.min(leftBottom, rightBottom) - Math.max(left.y, right.y);
    const minimumOverlap = Math.max(1, Math.min(left.height, right.height) * 0.25);
    if (overlap >= minimumOverlap) {
      return true;
    }

    const leftCenter = left.y + left.height / 2;
    const rightCenter = right.y + right.height / 2;
    return Math.abs(leftCenter - rightCenter) <= Math.max(1, Number(rowTolerance) || 0);
  }

  function buildConservativeTextHighlightBounds(node, start, end, fontSize, lineHeight, startBounds, endBounds) {
    const resolvedEndBounds = normalizeTextHighlightWorldBounds(endBounds);
    if (!resolvedEndBounds) {
      return [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const minimumWidth = getTextHighlightMinimumWidth(size);
    const approximateLineHeight = roundTextHighlightMetric(
      Math.max(1, Math.min(resolvedEndBounds.height, resolvedLineHeight))
    );
    const endBottom = resolvedEndBounds.y + resolvedEndBounds.height;
    const nextY = roundTextHighlightMetric(Math.max(resolvedEndBounds.y, endBottom - approximateLineHeight));
    const endRight = resolvedEndBounds.x + resolvedEndBounds.width;
    let nextX = resolvedEndBounds.x;

    const resolvedStartBounds = normalizeTextHighlightWorldBounds(startBounds);
    if (resolvedStartBounds) {
      const startBottom = resolvedStartBounds.y + resolvedStartBounds.height;
      if (Math.abs(startBottom - endBottom) <= Math.max(2, resolvedLineHeight * 0.75)) {
        nextX = Math.max(resolvedEndBounds.x, Math.min(endRight, resolvedStartBounds.x + resolvedStartBounds.width));
      }
    } else if ((Number(start) || 0) <= 0) {
      nextX = resolvedEndBounds.x;
    }

    let nextWidth = endRight - nextX;
    if (nextWidth < minimumWidth) {
      nextX = Math.max(resolvedEndBounds.x, endRight - Math.max(minimumWidth, resolvedEndBounds.width * 0.18));
      nextWidth = endRight - nextX;
    }

    if (nextWidth < minimumWidth) {
      return [];
    }

    return [
      {
        x: roundTextHighlightMetric(nextX),
        y: nextY,
        width: roundTextHighlightMetric(nextWidth),
        height: approximateLineHeight,
      },
    ];
  }

  function buildEmergencyTextHighlightBounds(node, start, end, fontSize, lineHeight) {
    const nodeBounds = getNodeRenderBounds(node);
    const resolvedNodeBounds = normalizeTextHighlightWorldBounds(nodeBounds);
    if (!resolvedNodeBounds) {
      return [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const approximateLineHeight = roundTextHighlightMetric(
      Math.max(1, Math.min(resolvedNodeBounds.height, resolvedLineHeight))
    );
    const selectionLength = Math.max(1, Math.floor(Number(end) || 0) - Math.floor(Number(start) || 0));
    const estimatedWidth = roundTextHighlightMetric(
      Math.max(
        getTextHighlightMinimumWidth(size),
        Math.min(resolvedNodeBounds.width, selectionLength * Math.max(4, size * 0.42))
      )
    );

    return [
      {
        x: resolvedNodeBounds.x,
        y: roundTextHighlightMetric(
          Math.max(resolvedNodeBounds.y, resolvedNodeBounds.y + resolvedNodeBounds.height - approximateLineHeight)
        ),
        width: estimatedWidth,
        height: approximateLineHeight,
      },
    ];
  }

  function truncateTextHighlightMeasurementClone(node, end) {
    if (!node || typeof node.characters !== "string") {
      return;
    }

    const characterCount = node.characters.length;
    const truncateAt = Math.max(0, Math.min(characterCount, Math.floor(Number(end) || 0)));
    if (truncateAt >= characterCount) {
      return;
    }

    if (typeof node.deleteCharacters === "function") {
      try {
        node.deleteCharacters(truncateAt, characterCount);
        return;
      } catch (error) {}
    }

    try {
      node.characters = node.characters.slice(0, truncateAt);
    } catch (error) {}
  }

  function moveTextHighlightMeasureClone(sourceNode, cloneNode, parentNode, offsetX, offsetY) {
    const targetParent = parentNode && !parentNode.removed ? parentNode : figma.currentPage;
    if (cloneNode.parent !== targetParent) {
      targetParent.appendChild(cloneNode);
    }

    if ("layoutPositioning" in cloneNode) {
      try {
        cloneNode.layoutPositioning = "ABSOLUTE";
      } catch (error) {}
    }

    const sourceTransform = getAbsoluteTransformMatrix(sourceNode);
    const xOffset = Number(offsetX) || 0;
    const yOffset = Number(offsetY) || 0;
    if ("relativeTransform" in cloneNode && Array.isArray(cloneNode.relativeTransform)) {
      cloneNode.relativeTransform = [
        [sourceTransform[0][0], sourceTransform[0][1], sourceTransform[0][2] - xOffset],
        [sourceTransform[1][0], sourceTransform[1][1], sourceTransform[1][2] - yOffset],
      ];
      return;
    }

    if ("x" in cloneNode && typeof cloneNode.x === "number" && "y" in cloneNode && typeof cloneNode.y === "number") {
      cloneNode.x = roundTextHighlightMetric(sourceTransform[0][2] - xOffset);
      cloneNode.y = roundTextHighlightMetric(sourceTransform[1][2] - yOffset);
    }
  }

  function getVisibleTextHighlightBounds(node) {
    if (!node) {
      return null;
    }

    const bounds = getNodeRenderBounds(node);
    if (bounds) {
      return bounds;
    }

    return null;
  }

  function getTextHighlightMeasurementBoundsList(measurement, fallbackNode) {
    const boundsList = [];
    const measurementSegments =
      measurement && Array.isArray(measurement.boundsList)
        ? measurement.boundsList
        : measurement && Array.isArray(measurement.segments)
          ? measurement.segments
          : null;
    if (measurementSegments) {
      for (const segment of measurementSegments) {
        const normalizedSegment = normalizeTextHighlightWorldBounds(segment);
        if (normalizedSegment) {
          boundsList.push(normalizedSegment);
        }
      }
    }

    if (!boundsList.length && measurement && measurement.bounds) {
      const normalizedBounds = normalizeTextHighlightWorldBounds(measurement.bounds);
      if (normalizedBounds) {
        boundsList.push(normalizedBounds);
      }
    }

    if (!boundsList.length && fallbackNode) {
      const fallbackBounds = getVisibleTextHighlightBounds(fallbackNode);
      if (fallbackBounds) {
        boundsList.push(fallbackBounds);
      }
    }

    return boundsList;
  }

  function normalizeTextHighlightWorldBounds(bounds) {
    if (!bounds || typeof bounds !== "object") {
      return null;
    }

    const x = Number(bounds.x);
    const y = Number(bounds.y);
    const width = Number(bounds.width);
    const height = Number(bounds.height);
    if (!Number.isFinite(x) || !Number.isFinite(y) || !Number.isFinite(width) || !Number.isFinite(height)) {
      return null;
    }

    if (width <= 0 || height <= 0) {
      return null;
    }

    return {
      x: roundTextHighlightMetric(x),
      y: roundTextHighlightMetric(y),
      width: roundTextHighlightMetric(width),
      height: roundTextHighlightMetric(height),
    };
  }

  function mergeTextHighlightBoundsList(boundsList) {
    if (!Array.isArray(boundsList) || !boundsList.length) {
      return null;
    }

    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;
    for (const bounds of boundsList) {
      const normalizedBounds = normalizeTextHighlightWorldBounds(bounds);
      if (!normalizedBounds) {
        continue;
      }

      minX = Math.min(minX, normalizedBounds.x);
      minY = Math.min(minY, normalizedBounds.y);
      maxX = Math.max(maxX, normalizedBounds.x + normalizedBounds.width);
      maxY = Math.max(maxY, normalizedBounds.y + normalizedBounds.height);
    }

    if (!Number.isFinite(minX) || !Number.isFinite(minY) || !Number.isFinite(maxX) || !Number.isFinite(maxY)) {
      return null;
    }

    return {
      x: roundTextHighlightMetric(minX),
      y: roundTextHighlightMetric(minY),
      width: roundTextHighlightMetric(Math.max(1, maxX - minX)),
      height: roundTextHighlightMetric(Math.max(1, maxY - minY)),
    };
  }

  function hasMergedTextHighlightBlockBounds(boundsList, fontSize) {
    if (!Array.isArray(boundsList) || boundsList.length !== 1) {
      return false;
    }

    const bounds = normalizeTextHighlightWorldBounds(boundsList[0]);
    if (!bounds) {
      return false;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    return bounds.height > Math.max(size * 1.9, 30);
  }

  function splitMergedTextHighlightBlockBounds(boundsList, fontSize, lineHeight) {
    const mergedBounds = mergeTextHighlightBoundsList(boundsList);
    const bounds = normalizeTextHighlightWorldBounds(mergedBounds);
    if (!bounds) {
      return Array.isArray(boundsList) ? boundsList : [];
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const lineCount = Math.max(2, Math.min(80, Math.ceil(Math.max(0, bounds.height - size) / resolvedLineHeight) + 1));
    if (lineCount <= 1) {
      return [bounds];
    }

    const chunkHeight = bounds.height / lineCount;
    const nextBoundsList = [];
    for (let index = 0; index < lineCount; index += 1) {
      const chunkY = bounds.y + chunkHeight * index;
      const nextY = index === lineCount - 1 ? bounds.y + bounds.height : bounds.y + chunkHeight * (index + 1);
      const nextHeight = Math.max(1, nextY - chunkY);
      nextBoundsList.push({
        x: bounds.x,
        y: roundTextHighlightMetric(chunkY),
        width: bounds.width,
        height: roundTextHighlightMetric(nextHeight),
      });
    }

    return nextBoundsList.length ? nextBoundsList : [bounds];
  }

  function normalizeTextHighlightBoundsListForApply(node, start, end, boundsList, fontSize, lineHeight) {
    let rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length) {
      rows = buildConservativeTextHighlightBounds(node, start, end, fontSize, lineHeight, null, null);
    }
    if (!rows.length) {
      rows = buildEmergencyTextHighlightBounds(node, start, end, fontSize, lineHeight);
    }
    if (!rows.length) {
      return [];
    }

    const nodeBounds = normalizeTextHighlightWorldBounds(getNodeRenderBounds(node));
    if (!nodeBounds) {
      return rows;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const padding = Math.max(120, size * 4, resolvedLineHeight * 3);
    const expandedNodeBounds = {
      x: nodeBounds.x - padding,
      y: nodeBounds.y - padding,
      width: nodeBounds.width + padding * 2,
      height: nodeBounds.height + padding * 2,
    };
    const maximumRowHeight = Math.max(resolvedLineHeight * 2.6, size * 3.2);
    const filteredRows = rows.filter((row) => {
      const bounds = normalizeTextHighlightWorldBounds(row);
      if (!bounds) {
        return false;
      }
      if (bounds.height > maximumRowHeight && bounds.height > nodeBounds.height * 0.65) {
        return false;
      }
      return doTextHighlightBoundsIntersect(bounds, expandedNodeBounds);
    });

    if (filteredRows.length) {
      return filteredRows;
    }

    const conservativeRows = buildConservativeTextHighlightBounds(node, start, end, fontSize, lineHeight, null, null);
    const safeConservativeRows = conservativeRows.filter((row) => {
      const bounds = normalizeTextHighlightWorldBounds(row);
      return !!bounds && doTextHighlightBoundsIntersect(bounds, expandedNodeBounds);
    });
    if (safeConservativeRows.length) {
      return safeConservativeRows;
    }

    return buildEmergencyTextHighlightBounds(node, start, end, fontSize, lineHeight).filter((row) => {
      const bounds = normalizeTextHighlightWorldBounds(row);
      return !!bounds && doTextHighlightBoundsIntersect(bounds, expandedNodeBounds);
    });
  }

  function stabilizeTextHighlightBoxBoundsToTypographyRows(node, boundsList, fontSize, lineHeight) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length || !node || node.removed) {
      return rows;
    }

    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      return rows;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const layoutLocalBounds = getTextHighlightTypographyLocalBounds(node, inverseNodeTransform);
    const layoutTop = layoutLocalBounds ? layoutLocalBounds.y : 0;
    const layoutHeight = layoutLocalBounds ? layoutLocalBounds.height : resolvedLineHeight;
    const metrics = buildTextHighlightBoxGlyphMetrics(size, resolvedLineHeight);
    const targetHeight = roundTextHighlightMetric(Math.max(1, Math.min(resolvedLineHeight, metrics.height)));
    const maximumLineIndex = getTextHighlightMaximumLocalLineIndex(node, layoutHeight, resolvedLineHeight);
    const maximumSnapShift = Math.max(size * 0.9, resolvedLineHeight * 0.55);
    const stableRows = [];

    for (const row of rows) {
      const bounds = normalizeTextHighlightWorldBounds(row);
      if (!bounds) {
        continue;
      }

      const localBounds = getTextHighlightLocalBounds(inverseNodeTransform, bounds);
      const localCenterY = localBounds.y + localBounds.height / 2;
      let lineIndex = Math.round((localCenterY - layoutTop - resolvedLineHeight / 2) / resolvedLineHeight);
      if (!Number.isFinite(lineIndex)) {
        lineIndex = 0;
      }
      lineIndex = Math.max(0, Math.min(maximumLineIndex, lineIndex));

      const lineBoxTop = layoutTop + lineIndex * resolvedLineHeight;
      const snappedY = lineBoxTop + Math.max(0, (resolvedLineHeight - targetHeight) / 2);
      const centeredY = localCenterY - targetHeight / 2;
      const nextY =
        Math.abs(snappedY - localBounds.y) <= maximumSnapShift
          ? snappedY
          : centeredY;
      const stableWorldBounds = getTextHighlightWorldBoundsFromLocalBounds(node, {
        x: localBounds.x,
        y: roundTextHighlightMetric(nextY),
        width: localBounds.width,
        height: targetHeight,
      });
      if (stableWorldBounds) {
        stableRows.push(stableWorldBounds);
      }
    }

    return stableRows.length ? sortTextHighlightBoundsList(stableRows) : rows;
  }

  function tightenTextHighlightBoxBoundsToVisualRows(node, boundsList, fontSize, lineHeight) {
    const rows = sortTextHighlightBoundsList(boundsList);
    if (!rows.length || !node || node.removed) {
      return rows;
    }

    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      return rows;
    }

    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedLineHeight = Math.max(size, Number(lineHeight) || size * 1.2);
    const targetHeight = buildTextHighlightBoxVisualRowHeight(size, resolvedLineHeight);
    const tightenedRows = [];

    for (const row of rows) {
      const bounds = normalizeTextHighlightWorldBounds(row);
      if (!bounds) {
        continue;
      }

      const localBounds = getTextHighlightLocalBounds(inverseNodeTransform, bounds);
      const localCenterY = localBounds.y + localBounds.height / 2;
      const tightenedWorldBounds = getTextHighlightWorldBoundsFromLocalBounds(node, {
        x: localBounds.x,
        y: roundTextHighlightMetric(localCenterY - targetHeight / 2),
        width: localBounds.width,
        height: targetHeight,
      });
      if (tightenedWorldBounds) {
        tightenedRows.push(tightenedWorldBounds);
      }
    }

    return tightenedRows.length ? sortTextHighlightBoundsList(tightenedRows) : rows;
  }

  function getTextHighlightTypographyLocalBounds(node, inverseNodeTransform) {
    if (!node || node.removed) {
      return null;
    }

    try {
      if ("width" in node && "height" in node && Number(node.width) > 0 && Number(node.height) > 0) {
        return {
          x: 0,
          y: 0,
          width: roundTextHighlightMetric(Number(node.width) || 0),
          height: roundTextHighlightMetric(Number(node.height) || 0),
        };
      }
    } catch (error) {}

    try {
      const absoluteBounds = "absoluteBoundingBox" in node ? node.absoluteBoundingBox : null;
      const bounds = normalizeTextHighlightWorldBounds(absoluteBounds);
      if (bounds && inverseNodeTransform) {
        return getTextHighlightLocalBounds(inverseNodeTransform, bounds);
      }
    } catch (error) {}

    return null;
  }

  function getTextHighlightMaximumLocalLineIndex(node, layoutHeight, lineHeight) {
    const resolvedLineHeight = Math.max(1, Number(lineHeight) || 1);
    const heightLineCount =
      Number.isFinite(Number(layoutHeight)) && Number(layoutHeight) > 0
        ? Math.max(1, Math.round(Number(layoutHeight) / resolvedLineHeight))
        : 1;
    const textLineCount =
      node && typeof node.characters === "string" ? Math.max(1, countTextLines(node.characters)) : 1;
    return Math.max(0, Math.max(heightLineCount, textLineCount) - 1);
  }

  function getTextHighlightSmallFontTightenBlend(fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    if (size <= AI_TEXT_HIGHLIGHT_SMALL_FONT_FULL_TIGHTEN_PX) {
      return 1;
    }
    if (size >= AI_TEXT_HIGHLIGHT_SMALL_FONT_NO_TIGHTEN_PX) {
      return 0;
    }
    return Math.max(
      0,
      Math.min(
        1,
        (AI_TEXT_HIGHLIGHT_SMALL_FONT_NO_TIGHTEN_PX - size) /
          (AI_TEXT_HIGHLIGHT_SMALL_FONT_NO_TIGHTEN_PX - AI_TEXT_HIGHLIGHT_SMALL_FONT_FULL_TIGHTEN_PX)
      )
    );
  }

  function getTextHighlightMicroFontTightenBlend(fontSize) {
    const size = Math.max(1, Number(fontSize) || 16);
    if (size <= AI_TEXT_HIGHLIGHT_MICRO_FONT_FULL_TIGHTEN_PX) {
      return 1;
    }
    if (size >= AI_TEXT_HIGHLIGHT_MICRO_FONT_NO_TIGHTEN_PX) {
      return 0;
    }
    return Math.max(
      0,
      Math.min(
        1,
        (AI_TEXT_HIGHLIGHT_MICRO_FONT_NO_TIGHTEN_PX - size) /
          (AI_TEXT_HIGHLIGHT_MICRO_FONT_NO_TIGHTEN_PX - AI_TEXT_HIGHLIGHT_MICRO_FONT_FULL_TIGHTEN_PX)
      )
    );
  }

  function doTextHighlightBoundsIntersect(left, right) {
    const leftBounds = normalizeTextHighlightWorldBounds(left);
    const rightBounds = normalizeTextHighlightWorldBounds(right);
    if (!leftBounds || !rightBounds) {
      return false;
    }

    return (
      leftBounds.x < rightBounds.x + rightBounds.width &&
      leftBounds.x + leftBounds.width > rightBounds.x &&
      leftBounds.y < rightBounds.y + rightBounds.height &&
      leftBounds.y + leftBounds.height > rightBounds.y
    );
  }

  function getTextRangeFontSize(node, start, end) {
    const segments = getTextHighlightStyledSegments(node, start, end, ["fontSize"]);
    let maxSize = 0;

    for (const segment of segments) {
      const fontSize = segment && typeof segment.fontSize === "number" ? segment.fontSize : 0;
      if (fontSize > maxSize) {
        maxSize = fontSize;
      }
    }

    if (maxSize > 0) {
      return maxSize;
    }

    if (node && typeof node.getRangeFontSize === "function") {
      try {
        const rangeFontSize = node.getRangeFontSize(start, end);
        if (typeof rangeFontSize === "number" && Number.isFinite(rangeFontSize) && rangeFontSize > 0) {
          return rangeFontSize;
        }
      } catch (error) {}
    }

    if (node && typeof node.fontSize === "number" && Number.isFinite(node.fontSize) && node.fontSize > 0) {
      return node.fontSize;
    }

    return 16;
  }

  function getTextRangeLineHeight(node, start, end, fontSize) {
    const fallback = getFallbackTextHighlightLineHeight(fontSize);
    const segments = getTextHighlightStyledSegments(node, start, end, ["fontSize", "lineHeight"]);
    let maxLineHeight = 0;

    for (const segment of segments) {
      const segmentFontSize =
        segment && typeof segment.fontSize === "number" && Number.isFinite(segment.fontSize)
          ? segment.fontSize
          : fontSize;
      const nextLineHeight = resolveTextHighlightLineHeight(segment && segment.lineHeight, segmentFontSize);
      if (nextLineHeight > maxLineHeight) {
        maxLineHeight = nextLineHeight;
      }
    }

    if (maxLineHeight > 0) {
      return roundTextHighlightMetric(maxLineHeight);
    }

    if (node && typeof node.getRangeLineHeight === "function") {
      try {
        const rangeLineHeight = resolveTextHighlightLineHeight(node.getRangeLineHeight(start, end), fontSize);
        if (rangeLineHeight > 0) {
          return roundTextHighlightMetric(rangeLineHeight);
        }
      } catch (error) {}
    }

    return fallback;
  }

  function resolveTextHighlightLineHeight(lineHeight, fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    if (!lineHeight || lineHeight === figma.mixed) {
      return 0;
    }

    if (typeof lineHeight === "number" && Number.isFinite(lineHeight) && lineHeight > 0) {
      return lineHeight;
    }

    if (typeof lineHeight !== "object") {
      return 0;
    }

    const unit = typeof lineHeight.unit === "string" ? lineHeight.unit.toUpperCase() : "";
    const value = Number(lineHeight.value);
    if (unit === "PIXELS" && Number.isFinite(value) && value > 0) {
      return value;
    }
    if (unit === "PERCENT" && Number.isFinite(value) && value > 0) {
      return size * (value / 100);
    }
    if (unit === "AUTO") {
      return getFallbackTextHighlightLineHeight(size);
    }

    return 0;
  }

  function getFallbackTextHighlightLineHeight(fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    return roundTextHighlightMetric(size * 1.2);
  }

  function getTextHighlightStyledSegments(node, start, end, fields) {
    if (!node || typeof node.getStyledTextSegments !== "function") {
      return [];
    }

    try {
      const segments = node.getStyledTextSegments(fields, start, end);
      return Array.isArray(segments) ? segments : [];
    } catch (error) {
      return [];
    }
  }

  function getTextHighlightPaints(node, start, end) {
    if (!node || typeof node.getRangeFills !== "function") {
      return null;
    }

    try {
      const fills = node.getRangeFills(start, end);
      return Array.isArray(fills) ? fills : null;
    } catch (error) {
      return null;
    }
  }

  async function applyTextHighlightColor(node, start, end, textColorHex) {
    if (!node || typeof node.setRangeFills !== "function") {
      throw new Error("선택한 텍스트 색상을 변경할 수 없습니다.");
    }

    try {
      await loadFontsForTextNode(node);
    } catch (error) {}

    node.setRangeFills(start, end, [createSolidPaint(textColorHex)]);
  }

  function createTextHighlightRect(node, parent, worldBounds, fontSize, colorHex, cornerRadius, boxPaddingPx, targetHeightPx) {
    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      throw new Error("텍스트 하이라이트 위치를 계산하지 못했습니다.");
    }

    const localBounds = getTextHighlightLocalBounds(inverseNodeTransform, worldBounds);
    const padding = buildTextHighlightBoxPixelPadding(fontSize, boxPaddingPx);
    const localX = localBounds.x - padding.left;
    const localWidth = Math.max(1, localBounds.width + padding.left + padding.right);
    const naturalLocalHeight = ceilTextHighlightMetric(localBounds.height + padding.top + padding.bottom);
    const targetLocalHeight = sanitizeTextHighlightBoxTargetHeight(targetHeightPx);
    const localHeight = targetLocalHeight > 0 ? Math.max(naturalLocalHeight, targetLocalHeight) : naturalLocalHeight;
    const extraTargetHeight = Math.max(0, localHeight - naturalLocalHeight);
    const localY = localBounds.y - padding.top - extraTargetHeight / 2;
    const clampedGeometry = clampTextHighlightLocalGeometryToParentBounds(node, parent, {
      x: localX,
      y: localY,
      width: localWidth,
      height: localHeight,
    });
    return createTextHighlightRectFromLocalGeometry(
      node,
      parent,
      clampedGeometry.x,
      clampedGeometry.y,
      clampedGeometry.width,
      clampedGeometry.height,
      colorHex,
      sanitizeTextHighlightRadius(cornerRadius, padding.radius)
    );
  }

  function createTextHighlightLineRect(node, parent, worldBounds, fontSize, thickness, colorHex, cornerRadius) {
    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      throw new Error("라인형 하이라이트 위치를 계산하지 못했습니다.");
    }

    const localBounds = getTextHighlightLocalBounds(inverseNodeTransform, worldBounds);
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedThickness = roundTextHighlightThickness(
      Math.max(2, Math.min(size * 1.35, Number(thickness) || size * 0.16))
    );
    const horizontalPadding = 0;
    const lineBottomAnchor = roundTextHighlightMetric(localBounds.y + localBounds.height + Math.max(0.5, size * 0.02));
    const localX = localBounds.x - horizontalPadding;
    const localY = lineBottomAnchor - resolvedThickness;
    const localWidth = Math.max(1, localBounds.width + horizontalPadding * 2);
    const localHeight = Math.max(1, resolvedThickness);
    const clampedGeometry = clampTextHighlightLocalGeometryToParentBounds(node, parent, {
      x: localX,
      y: localY,
      width: localWidth,
      height: localHeight,
    });

    return createTextHighlightRectFromLocalGeometry(
      node,
      parent,
      clampedGeometry.x,
      clampedGeometry.y,
      clampedGeometry.width,
      clampedGeometry.height,
      colorHex,
      sanitizeTextHighlightRadius(cornerRadius, AI_TEXT_HIGHLIGHT_DEFAULT_STRIKE_RADIUS)
    );
  }

  function createTextHighlightStrikeRect(node, parent, worldBounds, fontSize, thickness, colorHex, cornerRadius) {
    const nodeTransform = getAbsoluteTransformMatrix(node);
    const inverseNodeTransform = invertAffineTransform(nodeTransform);
    if (!inverseNodeTransform) {
      throw new Error("취소선형 하이라이트 위치를 계산하지 못했습니다.");
    }

    const localBounds = getTextHighlightLocalBounds(inverseNodeTransform, worldBounds);
    const size = Math.max(12, Number(fontSize) || 16);
    const resolvedThickness = roundTextHighlightThickness(
      Math.max(2, Math.min(size * 1.45, Number(thickness) || size * 0.22))
    );
    const horizontalPadding = 0;
    const centerY = localBounds.y + localBounds.height * 0.5;
    const localX = localBounds.x - horizontalPadding;
    const localY = centerY - resolvedThickness / 2;
    const localWidth = Math.max(1, localBounds.width + horizontalPadding * 2);
    const localHeight = Math.max(1, resolvedThickness);
    const clampedGeometry = clampTextHighlightLocalGeometryToParentBounds(node, parent, {
      x: localX,
      y: localY,
      width: localWidth,
      height: localHeight,
    });

    return createTextHighlightRectFromLocalGeometry(
      node,
      parent,
      clampedGeometry.x,
      clampedGeometry.y,
      clampedGeometry.width,
      clampedGeometry.height,
      colorHex,
      sanitizeTextHighlightRadius(cornerRadius, roundTextHighlightMetric(Math.max(0, Math.min(999, resolvedThickness / 2))))
    );
  }

  function clampTextHighlightLocalGeometryToParentBounds(node, parent, geometry) {
    return normalizeTextHighlightLocalGeometry(geometry);
  }

  function normalizeTextHighlightLocalGeometry(geometry) {
    return {
      x: roundTextHighlightMetric(Number(geometry && geometry.x) || 0),
      y: roundTextHighlightMetric(Number(geometry && geometry.y) || 0),
      width: roundTextHighlightMetric(Math.max(1, Number(geometry && geometry.width) || 1)),
      height: ceilTextHighlightMetric(Math.max(1, Number(geometry && geometry.height) || 1)),
    };
  }

  function createTextHighlightRectFromLocalGeometry(node, parent, localX, localY, localWidth, localHeight, colorHex, cornerRadius) {
    const nodeTransform = getAbsoluteTransformMatrix(node);
    const rect = figma.createRectangle();

    rect.resize(Math.max(1, localWidth), Math.max(1, localHeight));
    rect.cornerRadius = sanitizeTextHighlightRadius(cornerRadius, 0);
    rect.fills = [createSolidPaint(colorHex)];
    rect.strokes = [];

    if ("layoutPositioning" in rect && (isAutoLayoutParent(parent) || isReusableTextHighlightContainer(parent))) {
      setTextHighlightAbsoluteLayout(rect);
    }

    const absoluteOrigin = transformPointWithMatrix(nodeTransform, localX, localY);
    const desiredAbsoluteTransform = [
      [nodeTransform[0][0], nodeTransform[0][1], absoluteOrigin.x],
      [nodeTransform[1][0], nodeTransform[1][1], absoluteOrigin.y],
    ];

    if (rect.parent !== parent) {
      parent.appendChild(rect);
      if (isAutoLayoutParent(parent)) {
        setTextHighlightAbsoluteLayout(rect);
      }
    }

    applyTextHighlightRectTransform(rect, parent, desiredAbsoluteTransform);
    applyTextHighlightRectTransform(rect, parent, desiredAbsoluteTransform);

    return rect;
  }

  function applyTextHighlightRectTransform(rect, parent, desiredAbsoluteTransform) {
    const coordinateParent = getTextHighlightCoordinateParent(parent);
    const parentTransform = getAbsoluteTransformMatrix(coordinateParent);
    const inverseParentTransform = invertAffineTransform(parentTransform);
    const relativeTransform = inverseParentTransform
      ? multiplyAffineTransforms(inverseParentTransform, desiredAbsoluteTransform)
      : desiredAbsoluteTransform;

    if ("relativeTransform" in rect && Array.isArray(rect.relativeTransform)) {
      rect.relativeTransform = relativeTransform;
    } else if ("x" in rect && typeof rect.x === "number" && "y" in rect && typeof rect.y === "number") {
      rect.x = roundTextHighlightMetric(relativeTransform[0][2]);
      rect.y = roundTextHighlightMetric(relativeTransform[1][2]);
    }
  }

  function getTextHighlightCoordinateParent(parent) {
    let coordinateParent = parent;
    while (
      coordinateParent &&
      coordinateParent.type === "GROUP" &&
      coordinateParent.parent &&
      coordinateParent.parent.type !== "DOCUMENT"
    ) {
      coordinateParent = coordinateParent.parent;
    }

    return coordinateParent || parent;
  }

  function setTextHighlightAbsoluteLayout(node) {
    if (!node || !("layoutPositioning" in node)) {
      return;
    }

    try {
      node.layoutPositioning = "ABSOLUTE";
    } catch (error) {}
  }

  function prepareTextHighlightLayerContainer(parent, node) {
    const nodeIndex = findNodeChildIndex(parent, node && node.id);

    return {
      parent,
      nodeIndex,
      looseHighlights: true,
      anchorNodeId: node && node.id,
    };
  }

  function finalizeTextHighlightLayerContainer(container, node, options) {
    return;
  }

  function groupTextHighlightSelection(parent, node, rect, index, options) {
    const rects = Array.isArray(rect) ? rect.filter(Boolean) : rect ? [rect] : [];
    return groupLooseTextHighlightSelection(rects);
  }

  function groupLooseTextHighlightSelection(rects) {
    const nodesToGroup = Array.isArray(rects) ? rects.filter(Boolean) : [];
    if (!nodesToGroup.length) {
      throw new Error("Could not prepare text highlight layers.");
    }

    for (let nodeIndex = 0; nodeIndex < nodesToGroup.length; nodeIndex += 1) {
      const highlightNode = nodesToGroup[nodeIndex];
      try {
        highlightNode.name =
          nodesToGroup.length > 1
            ? `Text Highlight ${nodeIndex + 1}`
            : "Text Highlight";
      } catch (error) {}
    }

    return nodesToGroup[0];
  }

  function markTextHighlightGroup(group, node) {
    if (!group) {
      return;
    }

    try {
      group.name = AI_TEXT_HIGHLIGHT_GROUP_NAME;
    } catch (error) {}

    if (typeof group.setPluginData === "function") {
      try {
        group.setPluginData(AI_TEXT_HIGHLIGHT_GROUP_PLUGIN_KEY, "1");
        group.setPluginData(AI_TEXT_HIGHLIGHT_GROUP_TEXT_NODE_KEY, node && typeof node.id === "string" ? node.id : "");
      } catch (error) {}
    }
  }

  function isReusableTextHighlightGroup(group, node) {
    if (!group || group.removed || (group.type !== "GROUP" && group.type !== "FRAME")) {
      return false;
    }

    if (!node || node.removed || node.parent !== group) {
      return false;
    }

    if (safeName(group) === AI_TEXT_HIGHLIGHT_GROUP_NAME || looksLikeLegacyTextHighlightGroup(group, node)) {
      return true;
    }

    if (typeof group.getPluginData === "function") {
      try {
        return group.getPluginData(AI_TEXT_HIGHLIGHT_GROUP_PLUGIN_KEY) === "1";
      } catch (error) {}
    }

    return false;
  }

  function isReusableTextHighlightContainer(group) {
    if (!group || group.removed || (group.type !== "GROUP" && group.type !== "FRAME")) {
      return false;
    }

    if (safeName(group) === AI_TEXT_HIGHLIGHT_GROUP_NAME) {
      return true;
    }

    if (typeof group.getPluginData === "function") {
      try {
        return group.getPluginData(AI_TEXT_HIGHLIGHT_GROUP_PLUGIN_KEY) === "1";
      } catch (error) {}
    }

    return false;
  }

  function looksLikeLegacyTextHighlightGroup(group, node) {
    if (!group || group.type !== "GROUP" || !node || node.parent !== group) {
      return false;
    }
    if (!("children" in group) || !Array.isArray(group.children)) {
      return false;
    }

    let hasText = false;
    let hasHighlightShape = false;
    for (const child of group.children) {
      if (!child || child.removed) {
        continue;
      }
      if (child.id === node.id || child.type === "TEXT") {
        hasText = true;
      }
      const childName = safeName(child);
      if (
        /highlight/i.test(childName) &&
        (child.type === "RECTANGLE" || child.type === "VECTOR" || child.type === "LINE" || child.type === "BOOLEAN_OPERATION")
      ) {
        hasHighlightShape = true;
      }
    }

    return hasText && hasHighlightShape && /highlight/i.test(safeName(group));
  }

  function placeTextHighlightRectBehindNode(parent, rect, nodeId, index) {
    if (!parent || !rect || typeof parent.insertChild !== "function") {
      return;
    }

    const targetIndex =
      typeof index === "number" && index >= 0 ? index : findNodeChildIndex(parent, typeof nodeId === "string" ? nodeId : "");
    if (targetIndex < 0) {
      return;
    }

    try {
      parent.insertChild(targetIndex, rect);
      if (isAutoLayoutParent(parent)) {
        setTextHighlightAbsoluteLayout(rect);
      }
    } catch (error) {}
  }

  function findNodeChildIndex(parent, nodeId) {
    if (!parent || !("children" in parent) || !Array.isArray(parent.children)) {
      return -1;
    }

    for (let index = 0; index < parent.children.length; index += 1) {
      if (parent.children[index] && parent.children[index].id === nodeId) {
        return index;
      }
    }

    return -1;
  }

  function isAutoLayoutParent(node) {
    return !!node && "layoutMode" in node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function shouldPreserveAbsoluteLayoutPositioning(node) {
    return !!node && "layoutPositioning" in node && node.layoutPositioning === "ABSOLUTE";
  }

  function buildTextHighlightPadding(fontSize) {
    const size = Math.max(12, Number(fontSize) || 16);
    return {
      left: roundTextHighlightMetric(Math.max(6, size * 0.18)),
      right: roundTextHighlightMetric(Math.max(6, size * 0.18)),
      top: roundTextHighlightMetric(Math.max(3, size * 0.08)),
      bottom: roundTextHighlightMetric(Math.max(4, size * 0.12)),
      radius: roundTextHighlightMetric(Math.max(4, Math.min(14, size * 0.16))),
    };
  }

  function buildTextHighlightBoxPixelPadding(fontSize, boxPaddingPx) {
    const size = Math.max(12, Number(fontSize) || 16);
    const padding = sanitizeTextHighlightBoxPaddingPx(boxPaddingPx, AI_TEXT_HIGHLIGHT_DEFAULT_BOX_PADDING_PX);
    const baseHorizontalPadding = buildTextHighlightBoxBaseHorizontalPadding(size);
    const baseVerticalPadding = buildTextHighlightBoxBaseVerticalPadding(size);
    return {
      left: padding + baseHorizontalPadding,
      right: padding + baseHorizontalPadding,
      top: padding + baseVerticalPadding,
      bottom: padding + baseVerticalPadding,
      radius: roundTextHighlightMetric(Math.max(0, Math.min(14, size * 0.16))),
    };
  }

  function buildTextHighlightBoxTargetHeight(fontSize, lineHeight, boxPaddingPx, boundsList) {
    const rawSize = Math.max(1, Number(fontSize) || 16);
    const padding = buildTextHighlightBoxPixelPadding(fontSize, boxPaddingPx);
    const resolvedLineHeight = Math.max(rawSize, Number(lineHeight) || rawSize * 1.2);
    const visualRowHeight = buildTextHighlightBoxVisualRowHeight(rawSize, resolvedLineHeight);
    return ceilTextHighlightMetric(Math.max(1, visualRowHeight + padding.top + padding.bottom));
  }

  function sanitizeTextHighlightBoxTargetHeight(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric) || numeric <= 0) {
      return 0;
    }

    return ceilTextHighlightMetric(Math.max(1, Math.min(9999, numeric)));
  }

  function buildTextHighlightBoxBaseHorizontalPadding(fontSize) {
    const rawSize = Math.max(1, Number(fontSize) || 16);
    const size = Math.max(12, rawSize);
    const tightBlend = getTextHighlightSmallFontTightenBlend(size);
    const microBlend = getTextHighlightMicroFontTightenBlend(rawSize);
    const loosePadding = Math.max(
      AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_MIN_PX,
      Math.min(
        AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_MAX_PX,
        size * AI_TEXT_HIGHLIGHT_BASE_HORIZONTAL_PADDING_RATIO
      )
    );
    const tightPadding = Math.max(1.5, Math.min(5, size * 0.04));
    const smallPadding = loosePadding * (1 - tightBlend) + tightPadding * tightBlend;
    const microPadding = Math.max(0.75, Math.min(1.25, rawSize * 0.07));
    return roundTextHighlightMetric(smallPadding * (1 - microBlend) + microPadding * microBlend);
  }

  function buildTextHighlightBoxBaseVerticalPadding(fontSize) {
    const rawSize = Math.max(1, Number(fontSize) || 16);
    const size = Math.max(12, rawSize);
    const tightBlend = getTextHighlightSmallFontTightenBlend(size);
    const microBlend = getTextHighlightMicroFontTightenBlend(rawSize);
    const loosePadding = Math.max(
      AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_MIN_PX,
      Math.min(
        AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_MAX_PX,
        size * AI_TEXT_HIGHLIGHT_BASE_VERTICAL_PADDING_RATIO
      )
    );
    const tightPadding = Math.max(1, Math.min(3, size * 0.025));
    const smallPadding = loosePadding * (1 - tightBlend) + tightPadding * tightBlend;
    const microPadding = Math.max(0.25, Math.min(0.75, rawSize * 0.03));
    return roundTextHighlightMetric(smallPadding * (1 - microBlend) + microPadding * microBlend);
  }

  function getNodeRenderBounds(node) {
    if (!node || node.removed) {
      return null;
    }

    try {
      const renderBounds = "absoluteRenderBounds" in node ? node.absoluteRenderBounds : null;
      if (renderBounds && renderBounds.width > 0 && renderBounds.height > 0) {
        return {
          x: roundTextHighlightMetric(renderBounds.x),
          y: roundTextHighlightMetric(renderBounds.y),
          width: roundTextHighlightMetric(renderBounds.width),
          height: roundTextHighlightMetric(renderBounds.height),
        };
      }
    } catch (error) {}

    try {
      const absoluteBounds = "absoluteBoundingBox" in node ? node.absoluteBoundingBox : null;
      if (absoluteBounds && absoluteBounds.width > 0 && absoluteBounds.height > 0) {
        return {
          x: roundTextHighlightMetric(absoluteBounds.x),
          y: roundTextHighlightMetric(absoluteBounds.y),
          width: roundTextHighlightMetric(absoluteBounds.width),
          height: roundTextHighlightMetric(absoluteBounds.height),
        };
      }
    } catch (error) {}

    try {
      if ("width" in node && "height" in node && Number(node.width) > 0 && Number(node.height) > 0) {
        const matrix = getAbsoluteTransformMatrix(node);
        const isAxisAligned =
          Math.abs(Number(matrix[0][1]) || 0) <= 0.0001 &&
          Math.abs(Number(matrix[1][0]) || 0) <= 0.0001;
        if (isAxisAligned) {
          return {
            x: roundTextHighlightMetric(Number(matrix[0][2]) || 0),
            y: roundTextHighlightMetric(Number(matrix[1][2]) || 0),
            width: roundTextHighlightMetric(Number(node.width) || 0),
            height: roundTextHighlightMetric(Number(node.height) || 0),
          };
        }
      }
    } catch (error) {}

    return null;
  }

  function getAbsoluteTransformMatrix(node) {
    if (node && Array.isArray(node.absoluteTransform) && node.absoluteTransform.length >= 2) {
      const row0 = Array.isArray(node.absoluteTransform[0]) ? node.absoluteTransform[0] : [];
      const row1 = Array.isArray(node.absoluteTransform[1]) ? node.absoluteTransform[1] : [];
      const a = Number(row0[0]);
      const c = Number(row0[1]);
      const e = Number(row0[2]);
      const b = Number(row1[0]);
      const d = Number(row1[1]);
      const f = Number(row1[2]);
      return [
        [Number.isFinite(a) ? a : 1, Number.isFinite(c) ? c : 0, Number.isFinite(e) ? e : 0],
        [Number.isFinite(b) ? b : 0, Number.isFinite(d) ? d : 1, Number.isFinite(f) ? f : 0],
      ];
    }

    if (node && "x" in node && typeof node.x === "number" && "y" in node && typeof node.y === "number") {
      return [
        [1, 0, Number(node.x) || 0],
        [0, 1, Number(node.y) || 0],
      ];
    }

    return createIdentityTransform();
  }

  function createIdentityTransform() {
    return [
      [1, 0, 0],
      [0, 1, 0],
    ];
  }

  function invertAffineTransform(matrix) {
    if (!Array.isArray(matrix) || matrix.length < 2) {
      return null;
    }

    const a = Number(matrix[0][0]) || 0;
    const c = Number(matrix[0][1]) || 0;
    const e = Number(matrix[0][2]) || 0;
    const b = Number(matrix[1][0]) || 0;
    const d = Number(matrix[1][1]) || 0;
    const f = Number(matrix[1][2]) || 0;
    const determinant = a * d - b * c;

    if (!Number.isFinite(determinant) || Math.abs(determinant) < 1e-8) {
      return null;
    }

    return [
      [d / determinant, -c / determinant, (c * f - d * e) / determinant],
      [-b / determinant, a / determinant, (b * e - a * f) / determinant],
    ];
  }

  function multiplyAffineTransforms(left, right) {
    return [
      [
        left[0][0] * right[0][0] + left[0][1] * right[1][0],
        left[0][0] * right[0][1] + left[0][1] * right[1][1],
        left[0][0] * right[0][2] + left[0][1] * right[1][2] + left[0][2],
      ],
      [
        left[1][0] * right[0][0] + left[1][1] * right[1][0],
        left[1][0] * right[0][1] + left[1][1] * right[1][1],
        left[1][0] * right[0][2] + left[1][1] * right[1][2] + left[1][2],
      ],
    ];
  }

  function transformPointWithMatrix(matrix, x, y) {
    return {
      x: matrix[0][0] * x + matrix[0][1] * y + matrix[0][2],
      y: matrix[1][0] * x + matrix[1][1] * y + matrix[1][2],
    };
  }

  function getTextHighlightLocalBounds(inverseTransform, worldBounds) {
    const corners = [
      transformPointWithMatrix(inverseTransform, worldBounds.x, worldBounds.y),
      transformPointWithMatrix(inverseTransform, worldBounds.x + worldBounds.width, worldBounds.y),
      transformPointWithMatrix(inverseTransform, worldBounds.x, worldBounds.y + worldBounds.height),
      transformPointWithMatrix(inverseTransform, worldBounds.x + worldBounds.width, worldBounds.y + worldBounds.height),
    ];
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;

    for (const point of corners) {
      minX = Math.min(minX, point.x);
      minY = Math.min(minY, point.y);
      maxX = Math.max(maxX, point.x);
      maxY = Math.max(maxY, point.y);
    }

    return {
      x: roundTextHighlightMetric(minX),
      y: roundTextHighlightMetric(minY),
      width: roundTextHighlightMetric(Math.max(1, maxX - minX)),
      height: roundTextHighlightMetric(Math.max(1, maxY - minY)),
    };
  }

  function getTextHighlightWorldBoundsFromLocalBounds(node, localBounds) {
    const bounds = normalizeTextHighlightWorldBounds(localBounds);
    if (!bounds) {
      return null;
    }

    const matrix = getAbsoluteTransformMatrix(node);
    const corners = [
      transformPointWithMatrix(matrix, bounds.x, bounds.y),
      transformPointWithMatrix(matrix, bounds.x + bounds.width, bounds.y),
      transformPointWithMatrix(matrix, bounds.x, bounds.y + bounds.height),
      transformPointWithMatrix(matrix, bounds.x + bounds.width, bounds.y + bounds.height),
    ];
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY = -Infinity;

    for (const point of corners) {
      minX = Math.min(minX, point.x);
      minY = Math.min(minY, point.y);
      maxX = Math.max(maxX, point.x);
      maxY = Math.max(maxY, point.y);
    }

    if (!Number.isFinite(minX) || !Number.isFinite(minY) || maxX <= minX || maxY <= minY) {
      return null;
    }

    return {
      x: roundTextHighlightMetric(minX),
      y: roundTextHighlightMetric(minY),
      width: roundTextHighlightMetric(Math.max(1, maxX - minX)),
      height: roundTextHighlightMetric(Math.max(1, maxY - minY)),
    };
  }

  function roundTextHighlightMetric(value) {
    return Math.round((Number(value) || 0) * 1000) / 1000;
  }

  function ceilTextHighlightMetric(value) {
    return Math.max(1, Math.ceil(Number(value) || 0));
  }

  function roundTextHighlightThickness(value) {
    return Math.max(1, Math.round(Number(value) || 0));
  }

  function ceilTextHighlightThickness(value) {
    return Math.max(1, Math.ceil(Number(value) || 0));
  }

  function sanitizeHexColor(value, fallback) {
    const next = normalizeHexCandidate(value);
    if (next) {
      return next;
    }

    const fallbackValue = normalizeHexCandidate(fallback);
    return fallbackValue || AI_TEXT_HIGHLIGHT_DEFAULT_TEXT_COLOR;
  }

  function shouldApplyTextHighlightColor(message) {
    const mode = String((message && message.textColorMode) || "")
      .trim()
      .toLowerCase();

    if (mode === "default" || mode === "preserve") {
      return false;
    }

    if (mode === "custom") {
      return true;
    }

    return !!normalizeHexCandidate(message && message.textColorHex);
  }

  function sanitizeTextHighlightRadius(value, fallback) {
    const next = parseTextHighlightRadius(value);
    if (typeof next === "number") {
      return next;
    }

    const fallbackValue = parseTextHighlightRadius(fallback);
    return typeof fallbackValue === "number" ? fallbackValue : AI_TEXT_HIGHLIGHT_DEFAULT_RADIUS;
  }

  function sanitizeTextHighlightDecorationScale(value, fallback) {
    const next = parseTextHighlightDecorationScale(value);
    if (typeof next === "number") {
      return next;
    }

    const fallbackValue = parseTextHighlightDecorationScale(fallback);
    return typeof fallbackValue === "number" ? fallbackValue : AI_TEXT_HIGHLIGHT_DEFAULT_DECORATION_SCALE;
  }

  function sanitizeTextHighlightBoxPaddingPx(value, fallback) {
    const numeric = parseFloat(typeof value === "number" ? String(value) : String(value || "").trim());
    if (isFinite(numeric)) {
      return roundTextHighlightMetric(Math.max(-999, Math.min(999, Math.round(numeric))));
    }

    const fallbackValue = parseFloat(typeof fallback === "number" ? String(fallback) : String(fallback || "").trim());
    if (isFinite(fallbackValue)) {
      return roundTextHighlightMetric(Math.max(-999, Math.min(999, Math.round(fallbackValue))));
    }

    return AI_TEXT_HIGHLIGHT_DEFAULT_BOX_PADDING_PX;
  }

  function parseTextHighlightRadius(value) {
    const raw = typeof value === "number" ? String(value) : String(value || "").trim();
    if (!raw) {
      return null;
    }

    const numeric = parseFloat(raw);
    if (!isFinite(numeric)) {
      return null;
    }

    return roundTextHighlightMetric(Math.max(0, Math.min(999, Math.round(numeric))));
  }

  function parseTextHighlightDecorationScale(value) {
    const raw = typeof value === "number" ? String(value) : String(value || "").trim();
    if (!raw) {
      return null;
    }

    const numeric = parseFloat(raw);
    if (!isFinite(numeric)) {
      return null;
    }

    return roundTextHighlightMetric(Math.max(1, Math.min(10, Math.round(numeric * 10) / 10)));
  }

  function normalizeHexCandidate(value) {
    const raw = String(value || "")
      .trim()
      .replace(/^#+/, "")
      .toUpperCase();

    if (/^[0-9A-F]{6}$/.test(raw)) {
      return `#${raw}`;
    }

    if (/^[0-9A-F]{3}$/.test(raw)) {
      return `#${raw[0]}${raw[0]}${raw[1]}${raw[1]}${raw[2]}${raw[2]}`;
    }

    return "";
  }

  function hexToFigmaRgb(value) {
    const hex = sanitizeHexColor(value, AI_TEXT_HIGHLIGHT_DEFAULT_TEXT_COLOR);
    return {
      r: parseInt(hex.slice(1, 3), 16) / 255,
      g: parseInt(hex.slice(3, 5), 16) / 255,
      b: parseInt(hex.slice(5, 7), 16) / 255,
    };
  }

  function rgbToHex(color) {
    if (!color || typeof color !== "object") {
      return "";
    }

    const red = Math.max(0, Math.min(255, Math.round((Number(color.r) || 0) * 255)));
    const green = Math.max(0, Math.min(255, Math.round((Number(color.g) || 0) * 255)));
    const blue = Math.max(0, Math.min(255, Math.round((Number(color.b) || 0) * 255)));

    return `#${red.toString(16).padStart(2, "0")}${green.toString(16).padStart(2, "0")}${blue
      .toString(16)
      .padStart(2, "0")}`.toUpperCase();
  }

  function extractVisibleSolidPaintHex(paints) {
    if (!Array.isArray(paints)) {
      return "";
    }

    for (const paint of paints) {
      if (!paint || paint.type !== "SOLID" || paint.visible === false || !paint.color) {
        continue;
      }

      const opacity = typeof paint.opacity === "number" ? paint.opacity : 1;
      if (!Number.isFinite(opacity) || opacity <= 0) {
        continue;
      }

      const hex = rgbToHex(paint.color);
      if (hex) {
        return hex;
      }
    }

    return "";
  }

  function createSolidPaint(hexColor) {
    return {
      type: "SOLID",
      visible: true,
      opacity: 1,
      blendMode: "NORMAL",
      color: hexToFigmaRgb(hexColor),
    };
  }

  function createTransparentPaint() {
    return {
      type: "SOLID",
      visible: true,
      opacity: 0,
      blendMode: "NORMAL",
      color: { r: 0, g: 0, b: 0 },
    };
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }

  function getAiHelper() {
    const helper = globalScope.__PIGMA_AI_LLM__;
    return helper && typeof helper.requestJsonTask === "function" && typeof helper.hasConfiguredAiAsync === "function"
      ? helper
      : null;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_PIXEL_PERFECT_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const AI_DESIGN_READ_CACHE_KEY = "pigma:ai-design-read-cache:v1";
  const AI_PIXEL_PERFECT_CACHE_KEY = "pigma:ai-pixel-perfect-cache:v1";
  const AI_PIXEL_PERFECT_CLEAR_CACHE_KEY = "pigma:ai-pixel-perfect-clear-cache:v1";
  const PATCH_VERSION = 5;
  const RESULT_PREVIEW_LIMIT = 80;
  const VALUE_EPSILON = 0.0001;
  const EFFECT_RADIUS_KEYS = new Set(["radius", "spread", "startRadius", "endRadius"]);
  const OPACITY_PERCENT_SCALE = 100;
  const ANNOTATION_CATEGORY_LABEL = "Pigma Perfect pixel";
  const ANNOTATION_CATEGORY_COLOR = "red";
  const ANNOTATION_LABEL_PREFIX = "Pigma Perfect pixel";
  const ANNOTATION_CHANGE_PREVIEW_LIMIT = 4;
  let activePixelPerfectTask = "";

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isAiPixelPerfectMessage(message)) {
      if (message.type === "request-ai-pixel-perfect-cache") {
        await postCachedResult();
        return;
      }

      if (message.type === "request-ai-pixel-perfect-clear-cache") {
        await postCachedClearResult();
        return;
      }

      if (message.type === "run-ai-pixel-perfect-clear") {
        await withPixelPerfectTaskLock("clear", runPixelPerfectClear);
        return;
      }

      await withPixelPerfectTaskLock("apply", runPixelPerfect);
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_PIXEL_PERFECT_PATCH__ = true;

  function isAiPixelPerfectMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-pixel-perfect-cache" ||
        message.type === "run-ai-pixel-perfect" ||
        message.type === "request-ai-pixel-perfect-clear-cache" ||
        message.type === "run-ai-pixel-perfect-clear")
    );
  }

  async function withPixelPerfectTaskLock(task, runner) {
    if (activePixelPerfectTask) {
      if (activePixelPerfectTask === task) {
        postPixelPerfectTaskStatus(task, "running", getPixelPerfectTaskRunningMessage(task));
      } else {
        postPixelPerfectTaskError(
          task,
          "다른 정수 픽셀 정렬 교정 작업이 이미 진행 중입니다. 현재 작업이 끝난 뒤 다시 실행해 주세요."
        );
      }
      return false;
    }

    activePixelPerfectTask = task;
    try {
      await runner();
      return true;
    } finally {
      activePixelPerfectTask = "";
    }
  }

  function getPixelPerfectTaskRunningMessage(task) {
    if (task === "clear") {
      return "\uD604\uC7AC \uC120\uD0DD \uBC94\uC704\uC758 \uD53D\uC140 \uD37C\uD399\uD2B8 Dev Mode \uC8FC\uC11D\uC744 \uC815\uB9AC\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.";
    }
    return "소수점 보정 후보를 분석하고 정수 스냅 적용 중입니다.";
  }

  function postPixelPerfectTaskStatus(task, status, message) {
    if (task === "clear") {
      postClearStatus(status, message);
      return;
    }
    postStatus(status, message);
  }

  function postPixelPerfectTaskError(task, message) {
    if (task === "clear") {
      figma.ui.postMessage({
        type: "ai-pixel-perfect-clear-error",
        message,
      });
      return;
    }
    figma.ui.postMessage({
      type: "ai-pixel-perfect-error",
      message,
    });
  }

  async function runPixelPerfect() {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postStatus("running", "소수점 보정 후보를 분석하고 정수 스냅 적용 중입니다.");

    try {
      const result = await applyPixelPerfect();
      await writeCachedResult(result);

      figma.ui.postMessage({
        type: "ai-pixel-perfect-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      const summary = result.summary || {};
      const appliedCount = summary.appliedCount || 0;
      const excludedCount = summary.excludedCount || 0;
      const annotationCount = summary.annotationCount || 0;
      const annotationSuffix = annotationCount > 0 ? `, \uC8FC\uC11D ${annotationCount}\uAC74` : "";
      if (appliedCount > 0) {
        figma.notify(`\uD53D\uC140 \uAD50\uC815 \uC644\uB8CC (${appliedCount}\uAC74 \uC801\uC6A9, ${excludedCount}\uAC74 \uC81C\uC678${annotationSuffix})`, {
          timeout: 2200,
        });
      } else if ((summary.candidateCount || 0) === 0) {
        figma.notify("픽셀 퍼팩트 후보가 없습니다.", { timeout: 1800 });
      } else {
        figma.notify(`\uD53D\uC140 \uAD50\uC815 \uC644\uB8CC (0\uAC74 \uC801\uC6A9, ${excludedCount}\uAC74 \uC81C\uC678${annotationSuffix})`, {
          timeout: 2200,
        });
      }
    } catch (error) {
      const message = normalizeErrorMessage(error, "픽셀 퍼팩트 적용에 실패했습니다.");
      figma.ui.postMessage({
        type: "ai-pixel-perfect-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function runPixelPerfectClear() {
    const runSelectionSignature = getSelectionSignature(figma.currentPage.selection);
    postClearStatus("running", "\uD604\uC7AC \uC120\uD0DD \uBC94\uC704\uC758 \uD53D\uC140 \uD37C\uD399\uD2B8 Dev Mode \uC8FC\uC11D\uC744 \uC815\uB9AC\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = await clearPixelPerfectAnnotations();
      await writeCachedClearResult(result);

      figma.ui.postMessage({
        type: "ai-pixel-perfect-clear-result",
        result,
        matchesCurrentSelection: matchesSelectionSignature(result.selectionSignature || runSelectionSignature),
      });

      const summary = result.summary || {};
      const removedAnnotationCount = summary.removedAnnotationCount || 0;
      if (removedAnnotationCount > 0) {
        figma.notify(`\uD53D\uC140 \uD37C\uD399\uD2B8 \uC8FC\uC11D \uC815\uB9AC \uC644\uB8CC (${removedAnnotationCount}\uAC74 \uC0AD\uC81C)`, {
          timeout: 2200,
        });
      } else {
        figma.notify("\uD604\uC7AC \uC120\uD0DD \uBC94\uC704\uC5D0 \uC815\uB9AC\uD560 \uD53D\uC140 \uD37C\uD399\uD2B8 \uC8FC\uC11D\uC774 \uC5C6\uC2B5\uB2C8\uB2E4.", { timeout: 1800 });
      }
    } catch (error) {
      const message = normalizeErrorMessage(error, "\uD53D\uC140 \uD37C\uD399\uD2B8 \uC8FC\uC11D \uC815\uB9AC\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4.");
      figma.ui.postMessage({
        type: "ai-pixel-perfect-clear-error",
        message,
        matchesCurrentSelection: matchesSelectionSignature(runSelectionSignature),
      });
      figma.notify(message, { error: true, timeout: 2200 });
    }
  }

  async function postCachedResult() {
    const result = await readCachedResult();
    figma.ui.postMessage({
      type: "ai-pixel-perfect-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  async function postCachedClearResult() {
    const result = await readCachedClearResult();
    figma.ui.postMessage({
      type: "ai-pixel-perfect-clear-cache",
      result,
      matchesCurrentSelection: matchesCurrentSelection(result),
    });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-pixel-perfect-status",
      status,
      message,
    });
  }

  function postClearStatus(status, message) {
    figma.ui.postMessage({
      type: "ai-pixel-perfect-clear-status",
      status,
      message,
    });
  }

  async function applyPixelPerfect() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택하세요.");
    }

    const designReadResult = await readDesignReadCache();
    const context = buildRunContext(selection, designReadResult);
    const collection = collectPixelCandidates(selection);

    if (!collection.candidates.length) {
      return buildPixelPerfectResult({
        selection,
        context,
        source: "local-rules",
        aiSummary: buildLocalDecisionSummary([]),
        applied: [],
        annotations: [],
        excluded: collection.excluded,
        skipped: [],
        candidateCount: 0,
        annotationSummary: {
          applied: [],
          skipped: [],
          annotatedNodeCount: 0,
          annotationCount: 0,
          modeLabel: "Result only",
          categoryLabel: "",
        },
      });
    }

    const aiDecisionSummary = await requestAiDecisions(collection.candidates, context);
    const plans = buildDecisionPlans(collection.candidates, aiDecisionSummary);
    const applied = [];
    const appliedPlans = [];
    const skipped = [];

    for (const plan of plans) {
      try {
        await applyPlannedChange(plan);
        appliedPlans.push(plan);
        applied.push(buildAppliedEntry(plan));
      } catch (error) {
        skipped.push({
          label: `${plan.candidate.nodeName} / ${plan.candidate.label}`,
          reason: normalizeErrorMessage(error, "Failed to apply the snap target."),
        });
      }
    }

    const annotationApplied = buildResultOnlyAnnotation(appliedPlans, "");

    return buildPixelPerfectResult({
      selection,
      context,
      source: "local-rules",
      aiSummary: aiDecisionSummary,
      applied,
      annotations: annotationApplied.applied,
      excluded: collection.excluded,
      skipped: skipped.concat(annotationApplied.skipped),
      candidateCount: collection.candidates.length,
      annotationSummary: annotationApplied,
    });
  }

  async function clearPixelPerfectAnnotations() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택해 주세요.");
    }

    const designReadResult = await readDesignReadCache();
    const context = buildRunContext(selection, designReadResult);
    const annotationCollection = collectAnnotationNodes(selection);
    const category = await ensureAnnotationCategory(ANNOTATION_CATEGORY_COLOR, { createIfMissing: false });
    const cleared = removePixelPerfectAnnotations(annotationCollection.nodes, category);

    return buildPixelPerfectClearResult({
      selection,
      context,
      scannedNodeCount: annotationCollection.scannedNodeCount,
      category,
      cleared,
    });
  }

  function buildPixelPerfectClearResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const context = options.context && typeof options.context === "object" ? options.context : {};
    const cleared = options.cleared && typeof options.cleared === "object" ? options.cleared : {};
    const clearedItems = Array.isArray(cleared.cleared) ? cleared.cleared : [];
    const skipped = Array.isArray(cleared.skipped) ? cleared.skipped : [];
    const category = options.category && typeof options.category === "object" ? options.category : null;
    const scannedNodeCount =
      typeof options.scannedNodeCount === "number" && Number.isFinite(options.scannedNodeCount) ? options.scannedNodeCount : 0;

    return {
      version: PATCH_VERSION,
      source: "managed-annotation-clear",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel || formatSelectionLabel(selection),
        contextLabel: context.contextLabel || "General UI",
        rootCount: selection.length,
        scannedNodeCount,
        removedAnnotationCount: cleared.removedAnnotationCount || 0,
        clearedNodeCount: cleared.clearedNodeCount || 0,
        skippedCount: skipped.length,
        mode: "annotation-clear",
        modeLabel: "Dev Mode annotation clear",
        categoryLabel: category && category.label ? category.label : ANNOTATION_CATEGORY_LABEL,
      },
      cleared: clearedItems.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function buildPixelPerfectResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const context = options.context && typeof options.context === "object" ? options.context : {};
    const applied = Array.isArray(options.applied) ? options.applied : [];
    const annotations = Array.isArray(options.annotations) ? options.annotations : [];
    const excluded = Array.isArray(options.excluded) ? options.excluded : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];
    const aiSummary = options.aiSummary && typeof options.aiSummary === "object" ? options.aiSummary : {};
    const annotationSummary =
      options.annotationSummary && typeof options.annotationSummary === "object" ? options.annotationSummary : {};
    const candidateCount =
      typeof options.candidateCount === "number" && Number.isFinite(options.candidateCount) ? options.candidateCount : 0;

    return {
      version: PATCH_VERSION,
      source: options.source || "local-rules",
      selectionSignature: getSelectionSignature(selection),
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: context.selectionLabel || formatSelectionLabel(selection),
        contextLabel: context.contextLabel || "General UI",
        rootCount: selection.length,
        candidateCount,
        appliedCount: applied.length,
        excludedCount: excluded.length,
        skippedCount: skipped.length,
        annotationCount: annotationSummary.annotationCount || 0,
        annotatedNodeCount: annotationSummary.annotatedNodeCount || 0,
        annotationSkippedCount: Array.isArray(annotationSummary.skipped) ? annotationSummary.skipped.length : 0,
        aiDecisionCount: aiSummary.aiDecisionCount || 0,
        fallbackDecisionCount: aiSummary.fallbackDecisionCount || 0,
        aiStatusLabel: aiSummary.aiStatusLabel || "로컬 규칙",
        aiProviderLabel: aiSummary.aiProviderLabel || "",
        aiModelLabel: aiSummary.aiModelLabel || "",
        reviewStrategy: aiSummary.reviewStrategy || "0.5 stroke/blur 예외 유지 후 최근접 정수 스냅",
        modeLabel: annotationSummary.modeLabel || "Result only",
        categoryLabel: annotationSummary.categoryLabel || "",
      },
      applied: applied.slice(0, RESULT_PREVIEW_LIMIT),
      annotations: annotations.slice(0, RESULT_PREVIEW_LIMIT),
      excluded: excluded.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function buildRunContext(selection, designReadResult) {
    const signature = getSelectionSignature(selection);
    const validDesignRead =
      designReadResult &&
      typeof designReadResult === "object" &&
      designReadResult.selectionSignature === signature &&
      designReadResult.summary &&
      typeof designReadResult.summary === "object";

    return {
      selectionLabel: validDesignRead && typeof designReadResult.summary.selectionLabel === "string"
        ? designReadResult.summary.selectionLabel
        : formatSelectionLabel(selection),
      contextLabel: validDesignRead && typeof designReadResult.summary.contextLabel === "string"
        ? designReadResult.summary.contextLabel
        : "일반 UI 화면",
      languageLabel: validDesignRead && typeof designReadResult.summary.languageLabel === "string"
        ? designReadResult.summary.languageLabel
        : "",
      designReadPixelSummary:
        validDesignRead &&
        designReadResult.pixel &&
        typeof designReadResult.pixel === "object" &&
        typeof designReadResult.pixel.aiSummary === "string"
          ? designReadResult.pixel.aiSummary
          : "",
    };
  }

  function collectPixelCandidates(selection) {
    const candidates = [];
    const excluded = [];
    const stack = [];

    for (let index = selection.length - 1; index >= 0; index -= 1) {
      stack.push(selection[index]);
    }

    while (stack.length > 0) {
      const node = stack.pop();
      if (!node) {
        continue;
      }

      collectNodeCandidates(node, candidates, excluded);

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return { candidates, excluded };
  }

  function collectAnnotationNodes(selection) {
    const nodes = [];
    const stack = [];
    let scannedNodeCount = 0;

    for (let index = selection.length - 1; index >= 0; index -= 1) {
      stack.push(selection[index]);
    }

    while (stack.length > 0) {
      const node = stack.pop();
      if (!node) {
        continue;
      }

      scannedNodeCount += 1;

      if (supportsAnnotationsOnNode(node)) {
        nodes.push(node);
      }

      if (hasChildren(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return { nodes, scannedNodeCount };
  }

  function collectNodeCandidates(node, candidates, excluded) {
    const nodeName = safeName(node);
    const nodeType = String(node.type || "UNKNOWN");
    const resizable = canResizeNode(node);

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "rotation",
      label: "rotation",
      value: node.rotation,
      kind: "direct",
      category: "rotation",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "x",
      label: "x",
      value: getVisualPositionValue(node, "x"),
      kind: "position",
      category: "position",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "y",
      label: "y",
      value: getVisualPositionValue(node, "y"),
      kind: "position",
      category: "position",
    });
    if (resizable) {
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: "width",
        label: "width",
        value: node.width,
        kind: "size",
        category: "size",
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: "height",
        label: "height",
        value: node.height,
        kind: "size",
        category: "size",
      });
    }

    collectAutoLayoutCandidates(node, nodeName, nodeType, candidates, excluded);
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "strokeWeight",
      label: "stroke",
      value: node.strokeWeight,
      kind: "direct",
      category: "stroke",
    });

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "cornerRadius",
      label: "corner radius",
      value: node.cornerRadius,
      kind: "direct",
      category: "radius",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "topLeftRadius",
      label: "top-left radius",
      value: node.topLeftRadius,
      kind: "direct",
      category: "radius",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "topRightRadius",
      label: "top-right radius",
      value: node.topRightRadius,
      kind: "direct",
      category: "radius",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "bottomLeftRadius",
      label: "bottom-left radius",
      value: node.bottomLeftRadius,
      kind: "direct",
      category: "radius",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "bottomRightRadius",
      label: "bottom-right radius",
      value: node.bottomRightRadius,
      kind: "direct",
      category: "radius",
    });

    collectEffectCandidates(node, nodeName, nodeType, candidates, excluded);
    collectTextCandidates(node, nodeName, nodeType, candidates, excluded);
    collectPaintOpacityCandidates(node, nodeName, nodeType, "fills", "fill", candidates, excluded);
    collectPaintOpacityCandidates(node, nodeName, nodeType, "strokes", "stroke paint", candidates, excluded);
    collectLayerOpacityCandidate(node, nodeName, nodeType, candidates, excluded);
  }

  function collectAutoLayoutCandidates(node, nodeName, nodeType, candidates, excluded) {
    if (!hasAutoLayout(node)) {
      return;
    }

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "itemSpacing",
      label: "auto layout gap",
      value: typeof node.itemSpacing === "number" ? node.itemSpacing : null,
      kind: "direct",
      category: "auto-layout-gap",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "counterAxisSpacing",
      label: "auto layout wrap gap",
      value: typeof node.counterAxisSpacing === "number" ? node.counterAxisSpacing : null,
      kind: "direct",
      category: "auto-layout-gap",
    });

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "paddingTop",
      label: "padding top",
      value: typeof node.paddingTop === "number" ? node.paddingTop : null,
      kind: "direct",
      category: "auto-layout-padding",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "paddingRight",
      label: "padding right",
      value: typeof node.paddingRight === "number" ? node.paddingRight : null,
      kind: "direct",
      category: "auto-layout-padding",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "paddingBottom",
      label: "padding bottom",
      value: typeof node.paddingBottom === "number" ? node.paddingBottom : null,
      kind: "direct",
      category: "auto-layout-padding",
    });
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "paddingLeft",
      label: "padding left",
      value: typeof node.paddingLeft === "number" ? node.paddingLeft : null,
      kind: "direct",
      category: "auto-layout-padding",
    });
  }

  function collectEffectCandidates(node, nodeName, nodeType, candidates, excluded) {
    if (!node || !Array.isArray(node.effects) || node.effects.length === 0) {
      return;
    }

    for (let index = 0; index < node.effects.length; index += 1) {
      const effect = node.effects[index];
      if (!effect || effect.visible === false) {
        continue;
      }

      const effectLabel = formatEffectLabel(effect);
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.radius`,
        label: `${effectLabel} radius`,
        value: effect.radius,
        kind: "effect",
        category: "effect-blur",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["radius"],
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.spread`,
        label: `${effectLabel} spread`,
        value: effect.spread,
        kind: "effect",
        category: "effect-blur",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["spread"],
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.offset.x`,
        label: `${effectLabel} offset x`,
        value: effect.offset && effect.offset.x,
        kind: "effect",
        category: "effect-offset",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["offset", "x"],
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.offset.y`,
        label: `${effectLabel} offset y`,
        value: effect.offset && effect.offset.y,
        kind: "effect",
        category: "effect-offset",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["offset", "y"],
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.startRadius`,
        label: `${effectLabel} start radius`,
        value: effect.startRadius,
        kind: "effect",
        category: "effect-blur",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["startRadius"],
      });
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `effects.${index}.endRadius`,
        label: `${effectLabel} end radius`,
        value: effect.endRadius,
        kind: "effect",
        category: "effect-blur",
        effectIndex: index,
        effectType: String(effect.type || ""),
        effectPath: ["endRadius"],
      });
    }
  }

  function collectTextCandidates(node, nodeName, nodeType, candidates, excluded) {
    if (!node || node.type !== "TEXT") {
      return;
    }

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "fontSize",
      label: "font size",
      value: typeof node.fontSize === "number" ? node.fontSize : null,
      kind: "text-style",
      category: "text-size",
      meta: {
        textField: "fontSize",
      },
    });

    const lineHeight = getTextMetricDescriptor(node.lineHeight);
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "lineHeight",
      label: "line height",
      value: lineHeight ? lineHeight.value : null,
      kind: "text-style",
      category: "text-line-height",
      meta: {
        textField: "lineHeight",
        unit: lineHeight ? lineHeight.unit : "",
      },
    });

    const letterSpacing = getTextMetricDescriptor(node.letterSpacing);
    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "letterSpacing",
      label: "letter spacing",
      value: letterSpacing ? letterSpacing.value : null,
      kind: "text-style",
      category: "text-letter-spacing",
      meta: {
        textField: "letterSpacing",
        unit: letterSpacing ? letterSpacing.unit : "",
      },
    });

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "paragraphSpacing",
      label: "paragraph spacing",
      value: typeof node.paragraphSpacing === "number" ? node.paragraphSpacing : null,
      kind: "text-style",
      category: "text-spacing",
      meta: {
        textField: "paragraphSpacing",
      },
    });
  }

  function collectPaintOpacityCandidates(node, nodeName, nodeType, paintListKey, paintLabel, candidates, excluded) {
    const paints = node && Array.isArray(node[paintListKey]) ? node[paintListKey] : null;
    if (!paints || paints.length === 0) {
      return;
    }

    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (!paint || paint.visible === false) {
        continue;
      }

      const opacity = typeof paint.opacity === "number" ? paint.opacity : 1;
      maybeAddCandidate({
        candidates,
        excluded,
        node,
        nodeName,
        nodeType,
        fieldKey: `${paintListKey}.${index}.opacity`,
        label: paints.length === 1 ? `${paintLabel} opacity` : `${paintLabel} ${index + 1} opacity`,
        value: opacity * OPACITY_PERCENT_SCALE,
        kind: "paint-opacity",
        category: "paint-opacity",
        meta: {
          paintListKey,
          paintIndex: index,
          valueScale: OPACITY_PERCENT_SCALE,
        },
      });
    }
  }

  function collectLayerOpacityCandidate(node, nodeName, nodeType, candidates, excluded) {
    if (!node || typeof node.opacity !== "number") {
      return;
    }

    maybeAddCandidate({
      candidates,
      excluded,
      node,
      nodeName,
      nodeType,
      fieldKey: "opacity",
      label: "layer opacity",
      value: node.opacity * OPACITY_PERCENT_SCALE,
      kind: "scaled-direct",
      category: "opacity",
      meta: {
        valueScale: OPACITY_PERCENT_SCALE,
      },
    });
  }

  function maybeAddCandidate(entry) {
    const value = entry.value;
    if (typeof value !== "number" || !Number.isFinite(value)) {
      return;
    }

    if (isIntegerValue(value)) {
      return;
    }

    if (shouldPreserveHalfStep(entry.category, entry.fieldKey, value)) {
      entry.excluded.push({
        label: `${entry.nodeName} / ${entry.label} ${formatNumber(value)}`,
        reason: preserveReasonForCategory(entry.category),
      });
      return;
    }

    const floorValue = Math.floor(value);
    const ceilValue = Math.ceil(value);
    entry.candidates.push({
      id: `${entry.node.id}:${entry.fieldKey}`,
      node: entry.node,
      nodeId: entry.node.id,
      nodeName: entry.nodeName,
      nodeType: entry.nodeType,
      fieldKey: entry.fieldKey,
      label: entry.label,
      kind: entry.kind,
      category: entry.category,
      currentValue: value,
      floorValue,
      ceilValue,
      nearestValue: selectNearestInteger(value),
      effectIndex: typeof entry.effectIndex === "number" ? entry.effectIndex : null,
      effectType: entry.effectType || "",
      effectPath: Array.isArray(entry.effectPath) ? entry.effectPath.slice() : null,
      meta: entry.meta && typeof entry.meta === "object" ? clonePlainObject(entry.meta) : null,
    });
  }

  function buildLocalDecisionSummary(candidates) {
    const candidateCount = Array.isArray(candidates) ? candidates.length : 0;
    return {
      usedAi: false,
      aiStatusLabel: "로컬 규칙",
      aiProviderLabel: "",
      aiModelLabel: "",
      aiDecisionCount: 0,
      fallbackDecisionCount: candidateCount,
      reviewStrategy: candidateCount > 0 ? "0.5 stroke/blur 예외 유지 후 최근접 정수 스냅" : "보정 대상 없음",
      decisionMap: new Map(),
    };
  }

  async function requestAiDecisions(candidates) {
    return buildLocalDecisionSummary(candidates);
  }

  function buildDecisionPlans(candidates) {
    const plans = [];
    for (const candidate of candidates) {
      plans.push({
        candidate,
        source: "local",
        targetValue: candidate.nearestValue,
        reason: buildLocalDecisionReason(candidate),
      });
    }

    return plans;
  }

  function buildLocalDecisionReason(candidate) {
    if (!candidate || typeof candidate !== "object") {
      return "가장 가까운 정수로 스냅했습니다.";
    }

    switch (candidate.category) {
      case "position":
        return "가장 가까운 정수 좌표로 스냅했습니다.";
      case "rotation":
        return "Snap rotation to the nearest whole degree.";
      case "size":
        return "가장 가까운 정수 크기로 스냅했습니다.";
      case "auto-layout-gap":
        return "가장 가까운 정수 오토레이아웃 간격으로 스냅했습니다.";
      case "auto-layout-padding":
        return "가장 가까운 정수 오토레이아웃 패딩으로 스냅했습니다.";
      case "radius":
        return "가장 가까운 정수 반경 값으로 스냅했습니다.";
      case "effect-blur":
      case "effect-offset":
        return "가장 가까운 정수 효과 값으로 스냅했습니다.";
      case "text-size":
      case "text-line-height":
      case "text-letter-spacing":
      case "text-spacing":
        return "가장 가까운 정수 텍스트 값으로 스냅했습니다.";
      case "paint-opacity":
      case "opacity":
        return "가장 가까운 정수 퍼센트로 스냅했습니다.";
      default:
        return "가장 가까운 정수로 스냅했습니다.";
    }
  }

  async function applyPlannedChange(plan) {
    const candidate = plan.candidate;
    const targetValue = plan.targetValue;
    if (!Number.isInteger(targetValue)) {
      throw new Error("정수 목표값이 아닙니다.");
    }

    if (candidate.kind === "position") {
      applyPositionChange(candidate.node, candidate.fieldKey, targetValue);
      return;
    }

    if (candidate.kind === "size") {
      applySizeChange(candidate.node, candidate.fieldKey, targetValue);
      return;
    }

    if (candidate.kind === "effect") {
      applyEffectChange(candidate.node, candidate.effectIndex, candidate.effectPath, targetValue);
      return;
    }

    if (candidate.kind === "text-style") {
      await applyTextStyleChange(candidate, targetValue);
      return;
    }

    if (candidate.kind === "paint-opacity") {
      applyPaintOpacityChange(candidate.node, candidate.meta, targetValue);
      return;
    }

    if (candidate.kind === "scaled-direct") {
      applyScaledDirectChange(candidate.node, candidate.fieldKey, candidate.meta, targetValue);
      return;
    }

    applyDirectChange(candidate.node, candidate.fieldKey, targetValue);
  }

  function applyPositionChange(node, fieldKey, targetValue) {
    const position = getVisualPosition(node);
    const currentX = position && typeof position.x === "number" ? position.x : null;
    const currentY = position && typeof position.y === "number" ? position.y : null;
    if (!Number.isFinite(currentX) || !Number.isFinite(currentY)) {
      throw new Error("position 속성을 읽을 수 없습니다.");
    }

    const desiredX = fieldKey === "x" ? targetValue : currentX;
    const desiredY = fieldKey === "y" ? targetValue : currentY;
    const deltaX = desiredX - currentX;
    const deltaY = desiredY - currentY;

    if (Math.abs(deltaX) <= VALUE_EPSILON && Math.abs(deltaY) <= VALUE_EPSILON) {
      return;
    }

    if ("x" in node && "y" in node) {
      try {
        node.x += deltaX;
        node.y += deltaY;
      } catch (error) {}
    }

    if (isVisualPositionCloseEnough(node, desiredX, desiredY)) {
      return;
    }

    if (Array.isArray(node.relativeTransform) && node.relativeTransform.length === 2) {
      const transform = node.relativeTransform;
      node.relativeTransform = [
        [transform[0][0], transform[0][1], roundValue(transform[0][2] + deltaX)],
        [transform[1][0], transform[1][1], roundValue(transform[1][2] + deltaY)],
      ];
    }

    if (!isVisualPositionCloseEnough(node, desiredX, desiredY)) {
      throw new Error("position 적용에 실패했습니다.");
    }
  }

  function getVisualPositionValue(node, axis) {
    const position = getVisualPosition(node);
    return position && typeof position[axis] === "number" ? position[axis] : null;
  }

  function getVisualPosition(node) {
    if (!node) {
      return null;
    }

    const nodeBounds = getAbsoluteBounds(node);
    if (nodeBounds) {
      const parentBounds = getAbsoluteBounds(node.parent);
      return {
        x: parentBounds ? nodeBounds.x - parentBounds.x : nodeBounds.x,
        y: parentBounds ? nodeBounds.y - parentBounds.y : nodeBounds.y,
      };
    }

    if (typeof node.x === "number" && typeof node.y === "number") {
      return {
        x: node.x,
        y: node.y,
      };
    }

    return null;
  }

  function getAbsoluteBounds(node) {
    if (!node || !node.absoluteBoundingBox) {
      return null;
    }

    const bounds = node.absoluteBoundingBox;
    if (
      typeof bounds.x !== "number" ||
      typeof bounds.y !== "number" ||
      !Number.isFinite(bounds.x) ||
      !Number.isFinite(bounds.y)
    ) {
      return null;
    }

    return bounds;
  }

  function isVisualPositionCloseEnough(node, expectedX, expectedY) {
    const position = getVisualPosition(node);
    return !!position && isCloseEnough(position.x, expectedX) && isCloseEnough(position.y, expectedY);
  }

  function applySizeChange(node, fieldKey, targetValue) {
    if (!canResizeNode(node)) {
      throw new Error("resize 지원 노드가 아닙니다.");
    }

    const currentWidth = typeof node.width === "number" ? node.width : null;
    const currentHeight = typeof node.height === "number" ? node.height : null;
    if (!Number.isFinite(currentWidth) || !Number.isFinite(currentHeight)) {
      throw new Error("현재 크기를 읽을 수 없습니다.");
    }

    const nextWidth = fieldKey === "width" ? targetValue : currentWidth;
    const nextHeight = fieldKey === "height" ? targetValue : currentHeight;
    if (nextWidth < 1 || nextHeight < 1) {
      throw new Error("1px 미만 크기는 적용할 수 없습니다.");
    }

    if (typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(nextWidth, nextHeight);
      return;
    }

    if (typeof node.resize === "function") {
      node.resize(nextWidth, nextHeight);
      return;
    }

    throw new Error("resize API를 사용할 수 없습니다.");
  }

  function applyDirectChange(node, fieldKey, targetValue) {
    if (!node || !(fieldKey in node)) {
      throw new Error("속성 접근에 실패했습니다.");
    }

    node[fieldKey] = targetValue;
  }

  function applyScaledDirectChange(node, fieldKey, meta, targetValue) {
    const valueScale = meta && typeof meta.valueScale === "number" && meta.valueScale > 0 ? meta.valueScale : 1;
    applyDirectChange(node, fieldKey, targetValue / valueScale);
  }

  function applyEffectChange(node, effectIndex, effectPath, targetValue) {
    if (!node || !Array.isArray(node.effects)) {
      throw new Error("effect 속성이 없습니다.");
    }

    const effects = cloneEffects(node.effects);
    const effect = typeof effectIndex === "number" ? effects[effectIndex] : null;
    if (!effect) {
      throw new Error("effect 대상을 찾지 못했습니다.");
    }

    if (!setNestedNumericValue(effect, effectPath, targetValue)) {
      throw new Error("effect 속성 쓰기에 실패했습니다.");
    }

    node.effects = effects;
  }

  async function applyTextStyleChange(candidate, targetValue) {
    const node = candidate && candidate.node;
    const meta = candidate && candidate.meta && typeof candidate.meta === "object" ? candidate.meta : {};
    const textField = typeof meta.textField === "string" ? meta.textField : "";
    if (!node || node.type !== "TEXT" || !textField) {
      throw new Error("text 속성 적용에 실패했습니다.");
    }

    await loadFontsForTextNode(node);

    if (textField === "fontSize" || textField === "paragraphSpacing") {
      if (typeof node[textField] !== "number") {
        throw new Error(`${textField} 값을 읽을 수 없습니다.`);
      }
      node[textField] = targetValue;
      return;
    }

    if (textField === "lineHeight" || textField === "letterSpacing") {
      const currentValue = node[textField];
      const unit = typeof meta.unit === "string" && meta.unit ? meta.unit : currentValue && currentValue.unit;
      if (!unit || currentValue === figma.mixed || !currentValue || typeof currentValue !== "object") {
        throw new Error(`${textField} 값을 수정할 수 없습니다.`);
      }

      node[textField] = {
        unit,
        value: targetValue,
      };
      return;
    }

    throw new Error("지원하지 않는 text 속성입니다.");
  }

  function applyPaintOpacityChange(node, meta, targetValue) {
    const paintListKey = meta && typeof meta.paintListKey === "string" ? meta.paintListKey : "";
    const paintIndex = meta && typeof meta.paintIndex === "number" ? meta.paintIndex : -1;
    const valueScale = meta && typeof meta.valueScale === "number" && meta.valueScale > 0 ? meta.valueScale : 1;
    const paints = node && Array.isArray(node[paintListKey]) ? node[paintListKey] : null;
    if (!paints || paintIndex < 0 || paintIndex >= paints.length) {
      throw new Error("paint opacity 적용에 실패했습니다.");
    }

    const clonedPaints = paints.map((paint) => clonePlainObject(paint));
    clonedPaints[paintIndex].opacity = clampNumber(targetValue / valueScale, 0, 1);
    node[paintListKey] = clonedPaints;
  }

  function buildAppliedEntry(plan) {
    return {
      nodeName: plan.candidate.nodeName,
      nodeType: plan.candidate.nodeType,
      field: plan.candidate.label,
      from: formatNumber(plan.candidate.currentValue),
      to: String(plan.targetValue),
      source: "로컬 규칙",
      reason: plan.reason,
    };
  }

  function supportsAnnotationsForPlans(plans) {
    return Array.isArray(plans) && plans.some((plan) => supportsAnnotationsOnNode(plan && plan.candidate && plan.candidate.node));
  }

  function supportsAnnotationsOnNode(node) {
    return !!node && "annotations" in node;
  }

  async function ensureAnnotationCategory(requestedColor, options) {
    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return null;
    }

    try {
      const createIfMissing = !options || options.createIfMissing !== false;
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      const nextColor =
        typeof requestedColor === "string" && requestedColor.trim() ? requestedColor.trim().toLowerCase() : ANNOTATION_CATEGORY_COLOR;
      const existing = Array.isArray(categories)
        ? categories.find((category) => category && category.label === ANNOTATION_CATEGORY_LABEL)
        : null;
      if (existing) {
        if (typeof existing.setColor === "function" && existing.color !== nextColor) {
          existing.setColor(nextColor);
        }
        return existing;
      }

      if (!createIfMissing || typeof figma.annotations.addAnnotationCategoryAsync !== "function") {
        return null;
      }

      return await figma.annotations.addAnnotationCategoryAsync({
        label: ANNOTATION_CATEGORY_LABEL,
        color: nextColor,
      });
    } catch (error) {
      return null;
    }
  }

  function applyPixelPerfectAnnotations(plans, category) {
    const applied = [];
    const skipped = [];
    const plansByNode = new Map();
    const unsupportedNodeIds = new Set();

    for (const plan of plans) {
      const node = plan && plan.candidate ? plan.candidate.node : null;
      if (!node) {
        continue;
      }

      if (!supportsAnnotationsOnNode(node)) {
        if (!unsupportedNodeIds.has(node.id)) {
          unsupportedNodeIds.add(node.id);
          skipped.push({
            label: safeName(node),
            reason: "This node type does not support Dev Mode annotations.",
          });
        }
        continue;
      }

      const bucket = plansByNode.get(node.id) || { node, plans: [] };
      bucket.plans.push(plan);
      plansByNode.set(node.id, bucket);
    }

    let annotatedNodeCount = 0;
    let annotationCount = 0;

    for (const bucket of plansByNode.values()) {
      const node = bucket.node;
      const nodePlans = bucket.plans;
      if (!node || !nodePlans.length) {
        continue;
      }

      try {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        const preserved = existing.filter((annotation) => !isManagedPixelPerfectAnnotation(annotation, category));
        node.annotations = preserved.concat(buildPixelPerfectAnnotation(nodePlans, category));
        annotatedNodeCount += 1;
        annotationCount += 1;
        applied.push(buildPixelPerfectAnnotationResult(node, nodePlans, category));
      } catch (error) {
        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "\uD53D\uC140 \uD37C\uD399\uD2B8 \uC8FC\uC11D \uCD94\uAC00\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4."),
        });
      }
    }

    return {
      applied,
      skipped,
      annotatedNodeCount,
      annotationCount,
      modeLabel: annotationCount > 0 ? "Direct apply + Dev Mode annotation" : "Result only",
      categoryLabel: category && category.label ? category.label : ANNOTATION_CATEGORY_LABEL,
    };
  }

  function removePixelPerfectAnnotations(nodes, category) {
    const cleared = [];
    const skipped = [];
    let clearedNodeCount = 0;
    let removedAnnotationCount = 0;

    for (const node of nodes) {
      let existing = [];
      let managedAnnotations = [];

      try {
        existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        managedAnnotations = existing.filter((annotation) => isManagedPixelPerfectAnnotation(annotation, category));
        if (!managedAnnotations.length) {
          continue;
        }

        const preserved = existing.filter((annotation) => !isManagedPixelPerfectAnnotation(annotation, category));
        node.annotations = preserved;
        clearedNodeCount += 1;
        removedAnnotationCount += managedAnnotations.length;
        cleared.push({
          nodeId: node.id,
          nodeName: safeName(node),
          removedCount: managedAnnotations.length,
        });
      } catch (error) {
        if (!managedAnnotations.length) {
          continue;
        }

        skipped.push({
          label: safeName(node),
          reason: normalizeErrorMessage(error, "\uD53D\uC140 \uD37C\uD399\uD2B8 \uC8FC\uC11D \uC815\uB9AC\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4."),
        });
      }
    }

    return {
      cleared,
      skipped,
      clearedNodeCount,
      removedAnnotationCount,
    };
  }

  function buildResultOnlyAnnotation(plans, reason) {
    const skipped = [];
    if (Array.isArray(plans) && plans.length > 0 && reason) {
      skipped.push({
        label: "Annotations unavailable",
        reason,
      });
    }

    return {
      applied: [],
      skipped,
      annotatedNodeCount: 0,
      annotationCount: 0,
      modeLabel: "Result only",
      categoryLabel: "",
    };
  }

  function buildPixelPerfectAnnotation(plans, category) {
    const previewPlans = Array.isArray(plans) ? plans.slice(0, ANNOTATION_CHANGE_PREVIEW_LIMIT) : [];
    const lines = [ANNOTATION_LABEL_PREFIX];

    previewPlans.forEach((plan) => {
      lines.push(
        `- ${plan.candidate.label}: ${formatNumber(plan.candidate.currentValue)} -> ${plan.targetValue} (${plan.source === "ai" ? "AI" : "Local"})`
      );
    });

    if (Array.isArray(plans) && plans.length > previewPlans.length) {
      lines.push(`- ${plans.length - previewPlans.length} more change(s)`);
    }

    const annotation = {
      label: lines.join("\n"),
    };

    if (category && category.id) {
      annotation.categoryId = category.id;
    }

    return annotation;
  }

  function buildPixelPerfectAnnotationResult(node, plans, category) {
    const previewPlans = Array.isArray(plans) ? plans.slice(0, 2) : [];
    return {
      nodeId: node.id,
      nodeName: safeName(node),
      changeCount: Array.isArray(plans) ? plans.length : 0,
      categoryLabel: category && category.label ? category.label : ANNOTATION_CATEGORY_LABEL,
      preview: previewPlans
        .map((plan) => `${plan.candidate.label} ${formatNumber(plan.candidate.currentValue)} -> ${plan.targetValue}`)
        .join(" | "),
    };
  }

  function isManagedPixelPerfectAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }

    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }

    const label =
      typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return typeof label === "string" && (label.startsWith(ANNOTATION_LABEL_PREFIX) || label.startsWith(`[${ANNOTATION_LABEL_PREFIX}]`));
  }

  function shouldPreserveHalfStep(category, fieldKey, value) {
    if (!isHalfStepValue(value)) {
      return false;
    }

    if (category === "stroke") {
      return true;
    }

    if (category === "effect-blur") {
      return true;
    }

    if (typeof fieldKey === "string") {
      const normalized = fieldKey.toLowerCase();
      if (normalized.includes("strokeweight")) {
        return true;
      }
      for (const key of EFFECT_RADIUS_KEYS) {
        if (normalized.endsWith(`.${key.toLowerCase()}`)) {
          return true;
        }
      }
    }

    return false;
  }

  function preserveReasonForCategory(category) {
    if (category === "stroke") {
      return "0.5 단위 stroke 값은 의도값으로 보고 유지했습니다.";
    }

    if (category === "effect-blur") {
      return "0.5 단위 blur/spread 값은 의도값으로 보고 유지했습니다.";
    }

    return "0.5 단위 예외값으로 보고 유지했습니다.";
  }

  function canResizeNode(node) {
    if (!node) {
      return false;
    }

    if (node.type === "GROUP" || node.type === "TEXT" || node.type === "LINE") {
      return false;
    }

    return typeof node.resizeWithoutConstraints === "function" || typeof node.resize === "function";
  }

  function hasAutoLayout(node) {
    return !!node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function cloneEffects(effects) {
    return effects.map((effect) => clonePlainObject(effect));
  }

  function clonePlainObject(value) {
    if (!value || typeof value !== "object") {
      return value;
    }

    if (Array.isArray(value)) {
      return value.map((item) => clonePlainObject(item));
    }

    const clone = {};
    Object.keys(value).forEach((key) => {
      clone[key] = clonePlainObject(value[key]);
    });
    return clone;
  }

  function setNestedNumericValue(target, path, value) {
    if (!target || !Array.isArray(path) || path.length === 0) {
      return false;
    }

    let cursor = target;
    for (let index = 0; index < path.length - 1; index += 1) {
      const key = path[index];
      if (!cursor || typeof cursor !== "object" || !(key in cursor)) {
        return false;
      }
      cursor = cursor[key];
    }

    const lastKey = path[path.length - 1];
    if (!cursor || typeof cursor !== "object" || typeof cursor[lastKey] !== "number") {
      return false;
    }

    cursor[lastKey] = value;
    return true;
  }

  async function loadFontsForTextNode(node) {
    if (!node || node.type !== "TEXT") {
      return;
    }

    const fontNames = collectEditableFontNames(node);
    for (const fontName of fontNames) {
      await figma.loadFontAsync(fontName);
    }
  }

  function collectEditableFontNames(node) {
    const fontNames = [];
    const seen = new Set();
    const pushFont = (fontName) => {
      if (!fontName || typeof fontName !== "object" || typeof fontName.family !== "string" || typeof fontName.style !== "string") {
        return;
      }

      const key = `${fontName.family}::${fontName.style}`;
      if (seen.has(key)) {
        return;
      }

      seen.add(key);
      fontNames.push({
        family: fontName.family,
        style: fontName.style,
      });
    };

    if (typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string" && node.characters.length > 0) {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        for (const fontName of rangeFonts) {
          pushFont(fontName);
        }
      } catch (error) {}
    }

    if (fontNames.length === 0 && node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      pushFont(node.fontName);
    }

    return fontNames;
  }

  function getTextMetricDescriptor(value) {
    if (!value || value === figma.mixed || typeof value !== "object" || value.unit === "AUTO") {
      return null;
    }

    if (typeof value.value !== "number" || !Number.isFinite(value.value)) {
      return null;
    }

    return {
      unit: typeof value.unit === "string" ? value.unit : "",
      value: value.value,
    };
  }

  async function readDesignReadCache() {
    try {
      const value = await figma.clientStorage.getAsync(AI_DESIGN_READ_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readCachedResult() {
    try {
      const value = await figma.clientStorage.getAsync(AI_PIXEL_PERFECT_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function readCachedClearResult() {
    try {
      const value = await figma.clientStorage.getAsync(AI_PIXEL_PERFECT_CLEAR_CACHE_KEY);
      return value && typeof value === "object" ? value : null;
    } catch (error) {
      return null;
    }
  }

  async function writeCachedResult(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_PIXEL_PERFECT_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  async function writeCachedClearResult(result) {
    if (!result || typeof result !== "object") {
      return null;
    }

    try {
      await figma.clientStorage.setAsync(AI_PIXEL_PERFECT_CLEAR_CACHE_KEY, result);
    } catch (error) {}

    return result;
  }

  function matchesCurrentSelection(result) {
    return !!result && result.selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function matchesSelectionSignature(selectionSignature) {
    return typeof selectionSignature === "string" && selectionSignature === getSelectionSignature(figma.currentPage.selection);
  }

  function getSelectionSignature(selection) {
    const ids = Array.from(selection || [])
      .map((node) => String(node.id || ""))
      .sort();
    return `${figma.currentPage.id}:${ids.join(",")}`;
  }

  function formatSelectionLabel(selection) {
    const roots = Array.from(selection || []);
    if (!roots.length) {
      return "선택 없음";
    }

    if (roots.length === 1) {
      return safeName(roots[0]);
    }

    return `${safeName(roots[0])} 외 ${roots.length - 1}개`;
  }

  function formatEffectLabel(effect) {
    switch (String(effect && effect.type || "").toUpperCase()) {
      case "DROP_SHADOW":
        return "drop shadow";
      case "INNER_SHADOW":
        return "inner shadow";
      case "LAYER_BLUR":
        return "layer blur";
      case "BACKGROUND_BLUR":
        return "background blur";
      default:
        return "effect";
    }
  }

  function isHalfStepValue(value) {
    return Math.abs(value * 2 - Math.round(value * 2)) <= VALUE_EPSILON;
  }

  function isIntegerValue(value) {
    return Math.abs(value - Math.round(value)) <= VALUE_EPSILON;
  }

  function isCloseEnough(value, expected) {
    return typeof value === "number" && typeof expected === "number" && Math.abs(value - expected) <= VALUE_EPSILON;
  }

  function selectNearestInteger(value) {
    const rounded = Math.round(value);
    const floorValue = Math.floor(value);
    const ceilValue = Math.ceil(value);
    if (rounded === floorValue || rounded === ceilValue) {
      return rounded;
    }
    return Math.abs(value - floorValue) <= Math.abs(ceilValue - value) ? floorValue : ceilValue;
  }

  function roundValue(value) {
    return Math.round(value * 100) / 100;
  }

  function clampNumber(value, min, max) {
    return Math.min(max, Math.max(min, value));
  }

  function formatNumber(value) {
    const rounded = roundValue(value);
    return Number.isInteger(rounded) ? String(rounded) : String(rounded);
  }

  function safeName(node) {
    return node && typeof node.name === "string" && node.name.trim().length > 0
      ? node.name.trim()
      : String((node && node.type) || "Unnamed");
  }

  function hasChildren(node) {
    return !!node && Array.isArray(node.children) && node.children.length > 0;
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }

})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_SKEW_TRANSFORM_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const MAX_SKEW_DEGREES = 90;
  const MAX_TANGENT_SKEW_DEGREES = 89.9;
  const MAX_ROTATION_DEGREES = 180;
  const TRANSFORM_PRECISION = 1000000;
  const SKEW_PLUGIN_DATA_KEY = "__pigmaSkewTransform";
  const SKEW_EPSILON_DEGREES = 0.05;
  let activeSession = null;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isSkewTransformMessage(message)) {
      await handleSkewTransformMessage(message);
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_SKEW_TRANSFORM_PATCH__ = true;

  function isSkewTransformMessage(message) {
    return (
      !!message &&
      (message.type === "request-skew-transform-session" ||
        message.type === "preview-skew-transform" ||
        message.type === "apply-skew-transform" ||
        message.type === "cancel-skew-transform")
    );
  }

  async function handleSkewTransformMessage(message) {
    const clientRequestId = typeof message.clientRequestId === "string" ? message.clientRequestId : "";
    try {
      if (message.type === "request-skew-transform-session") {
        if (activeSession) {
          restoreSkewSession(activeSession);
          activeSession = null;
        }
        activeSession = createSkewSession();
        figma.ui.postMessage({
          type: "skew-transform-session-result",
          clientRequestId,
          session: serializeSkewSession(activeSession),
        });
        return;
      }

      const session = getActiveSkewSession(message.sessionId);
      if (message.type === "preview-skew-transform") {
        const result = applySkewToSession(
          session,
          message.horizontalDegrees,
          message.verticalDegrees,
          message.rotationDegrees,
          false
        );
        figma.ui.postMessage({
          type: "skew-transform-preview-result",
          clientRequestId,
          sessionId: session.sessionId,
          result,
        });
        return;
      }

      if (message.type === "apply-skew-transform") {
        const result = applySkewToSession(
          session,
          message.horizontalDegrees,
          message.verticalDegrees,
          message.rotationDegrees,
          true
        );
        activeSession = null;
        figma.ui.postMessage({
          type: "skew-transform-apply-result",
          clientRequestId,
          sessionId: session.sessionId,
          result,
        });
        figma.notify(buildSkewApplyToast(result), { timeout: 1800 });
        return;
      }

      if (message.type === "cancel-skew-transform") {
        const result = restoreSkewSession(session);
        activeSession = null;
        figma.ui.postMessage({
          type: "skew-transform-cancel-result",
          clientRequestId,
          sessionId: session.sessionId,
          result,
        });
      }
    } catch (error) {
      const messageText = normalizeSkewError(error, "기울기 보정을 처리하지 못했습니다.");
      figma.ui.postMessage({
        type: "skew-transform-error",
        clientRequestId,
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2400 });
    }
  }

  function createSkewSession() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("기울기를 적용할 프레임, 그룹, 레이어를 먼저 선택해주세요.");
    }

    const targets = [];
    const skipped = [];
    const seen = {};
    const selectedIds = buildSelectedSkewIdSet(selection);

    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (hasSelectedSkewAncestor(node, selectedIds)) {
        skipped.push(buildSkippedSkewNode(node, "상위 선택에 포함되어 별도 적용하지 않았습니다."));
        continue;
      }
      const result = collectSkewTargetsForSelectionNode(node, seen);
      appendSkewTargetResult(result, targets, skipped);
    }

    if (!targets.length) {
      const reason = skipped.length ? skipped[0].reason : "선택 항목에서 변형 가능한 레이어를 찾지 못했습니다.";
      throw new Error(reason);
    }

    const initialValues = estimateSessionSkewValues(targets);
    return {
      sessionId: "skew-transform-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8),
      selectionCount: selection.length,
      targets,
      skipped,
      horizontalDegrees: initialValues.horizontalDegrees,
      verticalDegrees: initialValues.verticalDegrees,
      rotationDegrees: initialValues.rotationDegrees,
      initialValues,
    };
  }

  function buildSelectedSkewIdSet(selection) {
    const result = {};
    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (node && typeof node.id === "string") {
        result[node.id] = true;
      }
    }
    return result;
  }

  function hasSelectedSkewAncestor(node, selectedIds) {
    let current = node && node.parent ? node.parent : null;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.id && selectedIds[current.id]) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function appendSkewTargetResult(result, targets, skipped) {
    if (!result) {
      return;
    }
    if (Array.isArray(result.targets) && result.targets.length) {
      for (let index = 0; index < result.targets.length; index += 1) {
        targets.push(result.targets[index]);
      }
    }
    if (Array.isArray(result.skipped) && result.skipped.length) {
      for (let index = 0; index < result.skipped.length; index += 1) {
        skipped.push(result.skipped[index]);
      }
    }
  }

  function collectSkewTargetsForSelectionNode(node, seen) {
    const direct = buildSkewTarget(node, seen);
    if (!direct.target) {
      return {
        targets: [],
        skipped: direct.skipped ? [direct.skipped] : [],
      };
    }

    if (hasMeaningfulSkewTarget(direct.target) || !isSkewContainerNode(node)) {
      return {
        targets: [direct.target],
        skipped: [],
      };
    }

    const nested = collectNestedSkewTargets(node, seen);
    if (nested.targets.length) {
      return nested;
    }

    return {
      targets: [direct.target],
      skipped: nested.skipped,
    };
  }

  function collectNestedSkewTargets(root, seen) {
    const targets = [];
    const skipped = [];
    const stack = [];
    if (isSkewContainerNode(root)) {
      for (let index = root.children.length - 1; index >= 0; index -= 1) {
        stack.push(root.children[index]);
      }
    }

    while (stack.length) {
      const node = stack.pop();
      if (!node) {
        continue;
      }

      const result = buildSkewTarget(node, seen);
      if (result.target && hasMeaningfulSkewTarget(result.target)) {
        targets.push(result.target);
        continue;
      }
      if (result.skipped) {
        skipped.push(result.skipped);
      }

      if (isSkewContainerNode(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return {
      targets,
      skipped,
    };
  }

  function isSkewContainerNode(node) {
    return !!(node && "children" in node && Array.isArray(node.children) && node.children.length);
  }

  function hasMeaningfulSkewTarget(target) {
    if (!target) {
      return false;
    }
    if (target.hasStoredSkewData) {
      return true;
    }
    return hasMeaningfulSkewValues(target.initialValues);
  }

  function hasMeaningfulSkewValues(values) {
    if (!values) {
      return false;
    }
    return (
      Math.abs(Number(values.horizontalDegrees) || 0) > SKEW_EPSILON_DEGREES ||
      Math.abs(Number(values.verticalDegrees) || 0) > SKEW_EPSILON_DEGREES
    );
  }

  function buildSkewTarget(node, seen) {
    if (!node || node.removed || !node.id || seen[node.id]) {
      return {};
    }
    seen[node.id] = true;

    const nodeName = safeSkewName(node);
    const nodeType = String(node.type || "UNKNOWN");
    if (node.type === "PAGE" || node.type === "DOCUMENT") {
      return {
        skipped: buildSkippedSkewNode(node, "페이지나 문서는 기울기를 적용할 수 없습니다."),
      };
    }
    if ("locked" in node && node.locked === true) {
      return {
        skipped: buildSkippedSkewNode(node, "잠긴 레이어입니다."),
      };
    }
    if (hasLockedSkewAncestor(node)) {
      return {
        skipped: buildSkippedSkewNode(node, "상위 레이어가 잠겨 있습니다."),
      };
    }
    if (!("relativeTransform" in node) || !Array.isArray(node.relativeTransform)) {
      return {
        skipped: buildSkippedSkewNode(node, "transform을 직접 수정할 수 없는 레이어입니다."),
      };
    }

    const width = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(width > 0) || !(height > 0)) {
      return {
        skipped: buildSkippedSkewNode(node, "레이어 크기가 비어 있습니다."),
      };
    }

    const originalTransform = copySkewTransform(node.relativeTransform);
    if (!originalTransform) {
      return {
        skipped: buildSkippedSkewNode(node, "기존 transform을 읽지 못했습니다."),
      };
    }

    const target = {
      node,
      nodeId: node.id,
      nodeName,
      nodeType,
      width,
      height,
      originalTransform,
      baseTransform: null,
      initialValues: {
        horizontalDegrees: 0,
        verticalDegrees: 0,
        rotationDegrees: 0,
      },
    };
    const storedState = readSkewPluginState(node);
    target.baseTransform =
      storedState && storedState.baseTransform ? copySkewTransform(storedState.baseTransform) : buildDeskewedTransform(target);
    target.initialValues = storedState ? normalizeStoredSkewValues(storedState) : estimateSkewValues(target);
    target.hasStoredSkewData = !!storedState;

    return {
      target,
    };
  }

  function hasLockedSkewAncestor(node) {
    let current = node && node.parent ? node.parent : null;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if ("locked" in current && current.locked === true) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function copySkewTransform(transform) {
    if (!Array.isArray(transform) || transform.length < 2) {
      return null;
    }
    const row0 = Array.isArray(transform[0]) ? transform[0] : null;
    const row1 = Array.isArray(transform[1]) ? transform[1] : null;
    if (!row0 || !row1 || row0.length < 3 || row1.length < 3) {
      return null;
    }
    return [
      [toFiniteSkewNumber(row0[0], 1), toFiniteSkewNumber(row0[1], 0), toFiniteSkewNumber(row0[2], 0)],
      [toFiniteSkewNumber(row1[0], 0), toFiniteSkewNumber(row1[1], 1), toFiniteSkewNumber(row1[2], 0)],
    ];
  }

  function getActiveSkewSession(sessionId) {
    if (!activeSession) {
      throw new Error("기울기 보정 세션이 만료되었습니다. 다시 실행해주세요.");
    }
    if (sessionId && sessionId !== activeSession.sessionId) {
      throw new Error("다른 기울기 보정 세션이 실행 중입니다.");
    }
    return activeSession;
  }

  function applySkewToSession(session, horizontalDegrees, verticalDegrees, rotationDegrees, persistState) {
    const horizontal = clampSkewDegrees(horizontalDegrees);
    const vertical = clampSkewDegrees(verticalDegrees);
    const rotation = clampRotationDegrees(rotationDegrees);
    const shouldPersistState = persistState === true;
    const applied = [];
    const skipped = session.skipped.slice(0);

    for (let index = 0; index < session.targets.length; index += 1) {
      const target = session.targets[index];
      if (!target.node || target.node.removed) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: "레이어가 더 이상 존재하지 않습니다.",
        });
        continue;
      }

      try {
        target.node.relativeTransform = buildSkewedTransform(target, horizontal, vertical, rotation);
        if (shouldPersistState) {
          writeSkewPluginState(target, horizontal, vertical, rotation);
        }
        applied.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
        });
      } catch (error) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: normalizeSkewError(error, "이 레이어에는 기울기를 적용하지 못했습니다."),
        });
      }
    }

    session.horizontalDegrees = horizontal;
    session.verticalDegrees = vertical;
    session.rotationDegrees = rotation;
    return buildSkewResult(session, applied, skipped);
  }

  function clearSkewInSession(session) {
    const cleaned = [];
    const skipped = session.skipped.slice(0);
    for (let index = 0; index < session.targets.length; index += 1) {
      const target = session.targets[index];
      if (!target.node || target.node.removed) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: "?덉씠?닿? ???댁긽 議댁옱?섏? ?딆뒿?덈떎.",
        });
        continue;
      }

      try {
        target.node.relativeTransform = buildDeskewedTransform(target);
        clearSkewPluginState(target.node);
        cleaned.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
        });
      } catch (error) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: normalizeSkewError(error, "?ㅽ걧 ?뺣━瑜??곸슜?섏? 紐삵뻽?듬땲??"),
        });
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionCount: session.selectionCount,
        targetCount: session.targets.length,
        cleanedCount: cleaned.length,
        skippedCount: skipped.length,
      },
      cleaned: cleaned.slice(0, 24),
      skipped: skipped.slice(0, 24),
    };
  }

  function restoreSkewSession(session) {
    const restored = [];
    const skipped = [];
    for (let index = 0; index < session.targets.length; index += 1) {
      const target = session.targets[index];
      if (!target.node || target.node.removed) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: "레이어가 더 이상 존재하지 않습니다.",
        });
        continue;
      }

      try {
        target.node.relativeTransform = copySkewTransform(target.originalTransform);
        restored.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
        });
      } catch (error) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: normalizeSkewError(error, "원래 transform으로 되돌리지 못했습니다."),
        });
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionCount: session.selectionCount,
        targetCount: session.targets.length,
        restoredCount: restored.length,
        skippedCount: skipped.length,
      },
      restored,
      skipped,
    };
  }

  function buildSkewedTransform(target, horizontalDegrees, verticalDegrees, rotationDegrees) {
    const original = target.baseTransform || target.originalTransform;
    const kx = Math.tan((clampSkewTangentDegrees(horizontalDegrees) * Math.PI) / 180);
    const ky = Math.tan((clampSkewTangentDegrees(verticalDegrees) * Math.PI) / 180);
    const rotationDelta = ((clampRotationDegrees(rotationDegrees) - getTransformRotationDegrees(original)) * Math.PI) / 180;
    const cos = Math.cos(rotationDelta);
    const sin = Math.sin(rotationDelta);
    const cx = target.width / 2;
    const cy = target.height / 2;

    const anchorX = original[0][0] * cx + original[0][1] * cy + original[0][2];
    const anchorY = original[1][0] * cx + original[1][1] * cy + original[1][2];
    const skew00 = 1 + kx * ky;
    const skew01 = kx;
    const skew10 = ky;
    const skew11 = 1;
    const local00 = cos * skew00 - sin * skew10;
    const local01 = cos * skew01 - sin * skew11;
    const local10 = sin * skew00 + cos * skew10;
    const local11 = sin * skew01 + cos * skew11;
    const local02 = cx - (local00 * cx + local01 * cy);
    const local12 = cy - (local10 * cx + local11 * cy);

    const a = original[0][0];
    const c = original[0][1];
    const tx = original[0][2];
    const b = original[1][0];
    const d = original[1][1];
    const ty = original[1][2];

    const next00 = a * local00 + c * local10;
    const next01 = a * local01 + c * local11;
    let next02 = a * local02 + c * local12 + tx;
    const next10 = b * local00 + d * local10;
    const next11 = b * local01 + d * local11;
    let next12 = b * local02 + d * local12 + ty;
    next02 += anchorX - (next00 * cx + next01 * cy + next02);
    next12 += anchorY - (next10 * cx + next11 * cy + next12);

    return [
      [roundSkewTransformValue(next00), roundSkewTransformValue(next01), roundSkewTransformValue(next02)],
      [roundSkewTransformValue(next10), roundSkewTransformValue(next11), roundSkewTransformValue(next12)],
    ];
  }

  function buildDeskewedTransform(target) {
    const original = target.originalTransform;
    const a = original[0][0];
    const c = original[0][1];
    const tx = original[0][2];
    const b = original[1][0];
    const d = original[1][1];
    const ty = original[1][2];
    const cx = target.width / 2;
    const cy = target.height / 2;
    const centerX = a * cx + c * cy + tx;
    const centerY = b * cx + d * cy + ty;
    const xScale = Math.hypot(a, b);
    const yScale = Math.hypot(c, d);
    const epsilon = 0.000001;
    if (xScale <= epsilon && yScale <= epsilon) {
      throw new Error("?ㅽ걧 ?뺣━???꾪븳 蹂??異뺤쓣 怨꾩궛?섏? 紐삵뻽?듬땲??");
    }

    const determinant = a * d - b * c;
    const orientation = determinant < 0 ? -1 : 1;
    let nextA = 1;
    let nextB = 0;
    let nextC = 0;
    let nextD = 1;

    if (xScale > epsilon) {
      const ux = a / xScale;
      const uy = b / xScale;
      nextA = ux * xScale;
      nextB = uy * xScale;
      nextC = -uy * yScale * orientation;
      nextD = ux * yScale * orientation;
    } else {
      const vx = c / yScale;
      const vy = d / yScale;
      nextA = vy * xScale * orientation;
      nextB = -vx * xScale * orientation;
      nextC = vx * yScale;
      nextD = vy * yScale;
    }

    return [
      [
        roundSkewTransformValue(nextA),
        roundSkewTransformValue(nextC),
        roundSkewTransformValue(centerX - nextA * cx - nextC * cy),
      ],
      [
        roundSkewTransformValue(nextB),
        roundSkewTransformValue(nextD),
        roundSkewTransformValue(centerY - nextB * cx - nextD * cy),
      ],
    ];
  }

  function estimateSessionSkewValues(targets) {
    if (!targets.length) {
      return {
        horizontalDegrees: 0,
        verticalDegrees: 0,
        rotationDegrees: 0,
      };
    }
    let horizontalTotal = 0;
    let verticalTotal = 0;
    let rotationTotal = 0;
    let count = 0;
    for (let index = 0; index < targets.length; index += 1) {
      const values = targets[index].initialValues;
      if (!values) {
        continue;
      }
      horizontalTotal += values.horizontalDegrees;
      verticalTotal += values.verticalDegrees;
      rotationTotal += values.rotationDegrees;
      count += 1;
    }
    if (!count) {
      return {
        horizontalDegrees: 0,
        verticalDegrees: 0,
        rotationDegrees: 0,
      };
    }
    return {
      horizontalDegrees: clampSkewDegrees(horizontalTotal / count),
      verticalDegrees: clampSkewDegrees(verticalTotal / count),
      rotationDegrees: clampRotationDegrees(rotationTotal / count),
    };
  }

  function estimateSkewValues(target) {
    const local = multiplySkewTransforms(invertSkewTransform(target.baseTransform), target.originalTransform);
    if (!local) {
      return {
        horizontalDegrees: 0,
        verticalDegrees: 0,
        rotationDegrees: getTransformRotationDegrees(target.baseTransform),
      };
    }
    const horizontal = Math.atan(toFiniteSkewNumber(local[0][1], 0)) * (180 / Math.PI);
    const vertical = Math.atan(toFiniteSkewNumber(local[1][0], 0)) * (180 / Math.PI);
    return {
      horizontalDegrees: clampSkewDegrees(horizontal),
      verticalDegrees: clampSkewDegrees(vertical),
      rotationDegrees: getTransformRotationDegrees(target.baseTransform),
    };
  }

  function readSkewPluginState(node) {
    if (!node || typeof node.getPluginData !== "function") {
      return null;
    }
    let raw = "";
    try {
      raw = node.getPluginData(SKEW_PLUGIN_DATA_KEY);
    } catch (error) {
      return null;
    }
    if (!raw || typeof raw !== "string") {
      return null;
    }
    try {
      const parsed = JSON.parse(raw);
      if (!parsed || typeof parsed !== "object") {
        return null;
      }
      const values = normalizeStoredSkewState(parsed);
      if (!hasMeaningfulSkewValues(values) && Math.abs(values.rotationDegrees) <= SKEW_EPSILON_DEGREES) {
        return null;
      }
      return values;
    } catch (error) {
      return null;
    }
  }

  function normalizeStoredSkewValues(state) {
    const source = state && typeof state === "object" ? state : {};
    return {
      horizontalDegrees: clampSkewDegrees(source.horizontalDegrees),
      verticalDegrees: clampSkewDegrees(source.verticalDegrees),
      rotationDegrees: clampRotationDegrees(source.rotationDegrees),
    };
  }

  function normalizeStoredSkewState(state) {
    const values = normalizeStoredSkewValues(state);
    const baseTransform = copySkewTransform(state && state.baseTransform);
    if (baseTransform) {
      values.baseTransform = baseTransform;
    }
    return values;
  }

  function writeSkewPluginState(target, horizontalDegrees, verticalDegrees, rotationDegrees) {
    const node = target && target.node ? target.node : null;
    if (!node || typeof node.setPluginData !== "function") {
      return;
    }
    const state = {
      version: 1,
      horizontalDegrees: clampSkewDegrees(horizontalDegrees),
      verticalDegrees: clampSkewDegrees(verticalDegrees),
      rotationDegrees: clampRotationDegrees(rotationDegrees),
      baseTransform: copySkewTransform(target.baseTransform),
      updatedAt: new Date().toISOString(),
    };

    if (!hasMeaningfulSkewValues(state) && Math.abs(state.rotationDegrees) <= SKEW_EPSILON_DEGREES) {
      clearSkewPluginState(node);
      return;
    }

    try {
      node.setPluginData(SKEW_PLUGIN_DATA_KEY, JSON.stringify(state));
    } catch (error) {
      // Metadata is only used to restore the UI values; the transform itself was already applied.
    }
  }

  function clearSkewPluginState(node) {
    if (!node || typeof node.setPluginData !== "function") {
      return;
    }
    try {
      node.setPluginData(SKEW_PLUGIN_DATA_KEY, "");
    } catch (error) {
      // Ignore metadata cleanup failures.
    }
  }

  function invertSkewTransform(transform) {
    if (!transform) {
      return null;
    }
    const a = transform[0][0];
    const c = transform[0][1];
    const tx = transform[0][2];
    const b = transform[1][0];
    const d = transform[1][1];
    const ty = transform[1][2];
    const determinant = a * d - b * c;
    if (Math.abs(determinant) <= 0.000001) {
      return null;
    }
    const nextA = d / determinant;
    const nextC = -c / determinant;
    const nextB = -b / determinant;
    const nextD = a / determinant;
    return [
      [nextA, nextC, -(nextA * tx + nextC * ty)],
      [nextB, nextD, -(nextB * tx + nextD * ty)],
    ];
  }

  function multiplySkewTransforms(left, right) {
    if (!left || !right) {
      return null;
    }
    const a0 = left[0][0];
    const c0 = left[0][1];
    const tx0 = left[0][2];
    const b0 = left[1][0];
    const d0 = left[1][1];
    const ty0 = left[1][2];
    const a1 = right[0][0];
    const c1 = right[0][1];
    const tx1 = right[0][2];
    const b1 = right[1][0];
    const d1 = right[1][1];
    const ty1 = right[1][2];
    return [
      [a0 * a1 + c0 * b1, a0 * c1 + c0 * d1, a0 * tx1 + c0 * ty1 + tx0],
      [b0 * a1 + d0 * b1, b0 * c1 + d0 * d1, b0 * tx1 + d0 * ty1 + ty0],
    ];
  }

  function buildSkewResult(session, applied, skipped) {
    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionCount: session.selectionCount,
        targetCount: session.targets.length,
        appliedCount: applied.length,
        skippedCount: skipped.length,
        horizontalDegrees: session.horizontalDegrees,
        verticalDegrees: session.verticalDegrees,
        rotationDegrees: session.rotationDegrees,
      },
      applied: applied.slice(0, 24),
      skipped: skipped.slice(0, 24),
    };
  }

  function serializeSkewSession(session) {
    return {
      sessionId: session.sessionId,
      summary: {
        selectionCount: session.selectionCount,
        targetCount: session.targets.length,
        skippedCount: session.skipped.length,
      },
      values: {
        horizontalDegrees: session.horizontalDegrees,
        verticalDegrees: session.verticalDegrees,
        rotationDegrees: session.rotationDegrees,
      },
      targets: session.targets.slice(0, 24).map(function (target) {
        return {
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
        };
      }),
      skipped: session.skipped.slice(0, 24),
    };
  }

  function buildSkippedSkewNode(node, reason) {
    return {
      nodeId: node && typeof node.id === "string" ? node.id : "",
      nodeName: safeSkewName(node),
      nodeType: String((node && node.type) || "UNKNOWN"),
      reason,
    };
  }

  function clampSkewDegrees(value) {
    const number = Number(value);
    if (!Number.isFinite(number)) {
      return 0;
    }
    return Math.max(-MAX_SKEW_DEGREES, Math.min(MAX_SKEW_DEGREES, Math.round(number * 10) / 10));
  }

  function clampRotationDegrees(value) {
    const number = Number(value);
    if (!Number.isFinite(number)) {
      return 0;
    }
    return Math.max(-MAX_ROTATION_DEGREES, Math.min(MAX_ROTATION_DEGREES, Math.round(number * 10) / 10));
  }

  function clampSkewTangentDegrees(value) {
    const number = clampSkewDegrees(value);
    return Math.max(-MAX_TANGENT_SKEW_DEGREES, Math.min(MAX_TANGENT_SKEW_DEGREES, number));
  }

  function getTransformRotationDegrees(transform) {
    if (!transform) {
      return 0;
    }
    return clampRotationDegrees((Math.atan2(toFiniteSkewNumber(transform[1][0], 0), toFiniteSkewNumber(transform[0][0], 1)) * 180) / Math.PI);
  }

  function toFiniteSkewNumber(value, fallback) {
    const number = Number(value);
    return Number.isFinite(number) ? number : fallback;
  }

  function roundSkewTransformValue(value) {
    return Math.round((Number(value) || 0) * TRANSFORM_PRECISION) / TRANSFORM_PRECISION;
  }

  function safeSkewName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }
    return node && node.type ? String(node.type) : "Layer";
  }

  function normalizeSkewError(error, fallback) {
    if (error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }

  function buildSkewApplyToast(result) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount = Number(summary.appliedCount) || 0;
    const skippedCount = Number(summary.skippedCount) || 0;
    if (appliedCount <= 0) {
      return "기울기를 적용할 수 있는 레이어가 없습니다.";
    }
    return skippedCount > 0
      ? "기울기 보정 완료 (" + appliedCount + "개 적용, " + skippedCount + "개 제외)"
      : "기울기 보정 완료 (" + appliedCount + "개 적용)";
  }
  function buildSkewClearToast(result) {
    const summary = result && result.summary ? result.summary : {};
    const cleanedCount = Number(summary.cleanedCount) || 0;
    const skippedCount = Number(summary.skippedCount) || 0;
    if (cleanedCount <= 0) {
      return "정리할 수 있는 기울기 보정 대상이 없습니다.";
    }
    return skippedCount > 0
      ? "기울기 정리 완료 (" + cleanedCount + "개 정리, " + skippedCount + "개 제외)"
      : "기울기 정리 완료 (" + cleanedCount + "개 정리)";
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_CORNER_RADIUS_ADJUST_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const LAST_VALUE_STORAGE_KEY = "pigma:corner-radius-adjust:last-value:v1";
  const FORMULA_PERCENT_RADIUS = 25;
  const MAX_PERCENT_RADIUS = 50;
  const AUTO_RADIUS_SOFT_CAP_START_SIDE = 96;
  const AUTO_RADIUS_SOFT_CAP_GROWTH = 1.3;
  const AUTO_RADIUS_ASPECT_DAMPING_POWER = 0.18;
  const AUTO_RADIUS_ASPECT_DAMPING_MIN = 0.72;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isCornerRadiusAdjustMessage(message)) {
      await handleCornerRadiusAdjustMessage(message);
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_CORNER_RADIUS_ADJUST_PATCH__ = true;

  function isCornerRadiusAdjustMessage(message) {
    return (
      !!message &&
      (message.type === "request-corner-radius-adjust-session" ||
        message.type === "apply-corner-radius-adjust")
    );
  }

  async function handleCornerRadiusAdjustMessage(message) {
    const clientRequestId = typeof message.clientRequestId === "string" ? message.clientRequestId : "";

    try {
      await ensureCornerRadiusSelectionAccess();

      if (message.type === "request-corner-radius-adjust-session") {
        const analysis = analyzeCornerRadiusSelection();
        const storedValue = await readLastCornerRadiusValue();
        const session = Object.assign({}, analysis, {
          lastValue: storedValue,
          defaultValue: analysis.defaultValue || storedValue || "auto",
        });
        figma.ui.postMessage({
          type: "corner-radius-adjust-session-result",
          clientRequestId,
          session,
        });
        return;
      }

      if (message.type === "apply-corner-radius-adjust") {
        const result = await applyCornerRadiusToSelection(message.radiusValue);
        figma.ui.postMessage({
          type: "corner-radius-adjust-result",
          clientRequestId,
          result,
        });
        figma.notify(buildCornerRadiusToast(result), { timeout: 1800 });
      }
    } catch (error) {
      const messageText = normalizeCornerRadiusError(error, "모서리 둥글기 조정에 실패했습니다.");
      figma.ui.postMessage({
        type: "corner-radius-adjust-error",
        clientRequestId,
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2400 });
    }
  }

  async function ensureCornerRadiusSelectionAccess() {
    if (typeof figma.loadAllPagesAsync !== "function") {
      return;
    }
    try {
      await figma.loadAllPagesAsync();
    } catch (error) {
      console.warn("[pigma] corner radius selection preload failed:", error);
    }
  }

  function analyzeCornerRadiusSelection() {
    const selection = getCornerRadiusSelection();
    const collection = collectCornerRadiusTargets(selection);
    const currentValues = collection.targets
      .map((target) => target.currentRadius)
      .filter((value) => typeof value === "number" && Number.isFinite(value));
    const firstValue = currentValues.length ? currentValues[0] : null;
    const allSame =
      firstValue !== null &&
      currentValues.length === collection.targets.length &&
      currentValues.every((value) => Math.abs(value - firstValue) <= 0.001);

    return {
      selectionCount: selection.length,
      targetCount: collection.targets.length,
      skippedCount: collection.skipped.length,
      defaultValue: allSame ? formatCornerRadiusNumber(firstValue) : "",
      targets: collection.targets.map(serializeCornerRadiusTarget).slice(0, 24),
      skipped: collection.skipped.slice(0, 24),
    };
  }

  async function applyCornerRadiusToSelection(rawRadiusValue) {
    const spec = parseCornerRadiusSpec(rawRadiusValue);
    const selection = getCornerRadiusSelection();
    if (!selection.length) {
      throw new Error("라운드 값을 적용할 사각형, 프레임, 그룹을 먼저 선택해 주세요.");
    }

    const collection = collectCornerRadiusTargets(selection);
    if (!collection.targets.length) {
      const reason = collection.skipped.length
        ? collection.skipped[0].reason
        : "선택 항목에서 라운드 값을 적용할 수 있는 사각형을 찾지 못했습니다.";
      throw new Error(reason);
    }

    const applied = [];
    const skipped = collection.skipped.slice(0);

    for (let index = 0; index < collection.targets.length; index += 1) {
      const target = collection.targets[index];
      if (!target.node || target.node.removed) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: "레이어가 더 이상 존재하지 않습니다.",
        });
        continue;
      }

      try {
        const radius = resolveCornerRadiusForTarget(spec, target);
        setCornerRadius(target.node, radius);
        applied.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          from: target.currentRadius === null ? "mixed" : formatCornerRadiusPx(target.currentRadius),
          to: formatCornerRadiusPx(radius),
        });
      } catch (error) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          nodeType: target.nodeType,
          reason: normalizeCornerRadiusError(error, "이 레이어의 라운드 값을 바꾸지 못했습니다."),
        });
      }
    }

    if (!applied.length) {
      const reason = skipped.length
        ? skipped[0].reason
        : "라운드 값을 적용할 수 있는 사각형을 찾지 못했습니다.";
      throw new Error(reason);
    }

    await writeLastCornerRadiusValue(formatCornerRadiusSpec(spec));

    return {
      processedAt: new Date().toISOString(),
      inputValue: formatCornerRadiusSpec(spec),
      summary: {
        selectionCount: selection.length,
        targetCount: collection.targets.length,
        appliedCount: applied.length,
        skippedCount: skipped.length,
      },
      applied: applied.slice(0, 24),
      skipped: skipped.slice(0, 24),
    };
  }

  function getCornerRadiusSelection() {
    return Array.from(figma.currentPage.selection || []);
  }

  function collectCornerRadiusTargets(selection) {
    const targets = [];
    const skipped = [];
    const seen = {};
    const selectedIds = buildSelectedCornerRadiusIdSet(selection);

    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (hasSelectedCornerRadiusAncestor(node, selectedIds)) {
        skipped.push(buildSkippedCornerRadiusNode(node, "상위 선택 항목에 포함되어 별도로 적용하지 않았습니다."));
        continue;
      }

      const result = collectCornerRadiusTargetsForSelectionNode(node, seen);
      appendCornerRadiusTargetResult(result, targets, skipped);
    }

    return { targets, skipped };
  }

  function collectCornerRadiusTargetsForSelectionNode(node, seen) {
    const direct = buildCornerRadiusTarget(node, seen);
    if (!isCornerRadiusContainerNode(node)) {
      return {
        targets: direct.target ? [direct.target] : [],
        skipped: direct.skipped ? [direct.skipped] : [],
      };
    }

    const nested = collectNestedCornerRadiusTargets(node, seen);
    const targets = [];
    const skipped = [];

    if (direct.target && shouldApplyCornerRadiusToDirectNode(node)) {
      targets.push(direct.target);
    }

    if (nested.targets.length) {
      targets.push(...nested.targets);
    }

    if (direct.target && !targets.length) {
      targets.push(direct.target);
    }

    if (direct.skipped) {
      skipped.push(direct.skipped);
    }
    if (nested.skipped.length) {
      skipped.push(...nested.skipped);
    }

    if (targets.length) {
      return {
        targets,
        skipped,
      };
    }

    return {
      targets: [],
      skipped,
    };
  }

  function collectNestedCornerRadiusTargets(root, seen) {
    const targets = [];
    const skipped = [];
    const stack = [];

    if (isCornerRadiusContainerNode(root)) {
      for (let index = root.children.length - 1; index >= 0; index -= 1) {
        stack.push(root.children[index]);
      }
    }

    while (stack.length) {
      const node = stack.pop();
      const result = buildCornerRadiusTarget(node, seen);
      if (result.target) {
        targets.push(result.target);
        continue;
      }
      if (result.skipped) {
        skipped.push(result.skipped);
      }
      if (isCornerRadiusContainerNode(node)) {
        for (let index = node.children.length - 1; index >= 0; index -= 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return { targets, skipped };
  }

  function buildCornerRadiusTarget(node, seen) {
    if (!node || node.removed || !node.id || seen[node.id]) {
      return {};
    }
    seen[node.id] = true;

    if (node.type === "PAGE" || node.type === "DOCUMENT") {
      return {
        skipped: buildSkippedCornerRadiusNode(node, "페이지나 문서에는 라운드 값을 적용할 수 없습니다."),
      };
    }
    if ("locked" in node && node.locked === true) {
      return {
        skipped: buildSkippedCornerRadiusNode(node, "잠긴 레이어입니다."),
      };
    }
    if (hasLockedCornerRadiusAncestor(node)) {
      return {
        skipped: buildSkippedCornerRadiusNode(node, "상위 레이어가 잠겨 있습니다."),
      };
    }
    if (!isCornerRadiusEditableNode(node)) {
      return {};
    }

    const width = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(width > 0) || !(height > 0)) {
      return {
        skipped: buildSkippedCornerRadiusNode(node, "레이어 크기가 비어 있습니다."),
      };
    }

    return {
      target: {
        node,
        nodeId: node.id,
        nodeName: safeCornerRadiusName(node),
        nodeType: String(node.type || "UNKNOWN"),
        width,
        height,
        currentRadius: readUniformCornerRadius(node),
      },
    };
  }

  function appendCornerRadiusTargetResult(result, targets, skipped) {
    if (!result) {
      return;
    }
    if (Array.isArray(result.targets)) {
      targets.push(...result.targets);
    }
    if (Array.isArray(result.skipped)) {
      skipped.push(...result.skipped);
    }
  }

  function buildSelectedCornerRadiusIdSet(selection) {
    const result = {};
    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (node && typeof node.id === "string") {
        result[node.id] = true;
      }
    }
    return result;
  }

  function hasSelectedCornerRadiusAncestor(node, selectedIds) {
    let current = node && node.parent ? node.parent : null;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (current.id && selectedIds[current.id]) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function hasLockedCornerRadiusAncestor(node) {
    let current = node && node.parent ? node.parent : null;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if ("locked" in current && current.locked === true) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function isCornerRadiusContainerNode(node) {
    return !!(node && "children" in node && Array.isArray(node.children) && node.children.length);
  }

  function isCornerRadiusEditableNode(node) {
    return !!(
      node &&
      typeof node === "object" &&
      "width" in node &&
      "height" in node &&
      ("cornerRadius" in node ||
        "topLeftRadius" in node ||
        "topRightRadius" in node ||
        "bottomRightRadius" in node ||
        "bottomLeftRadius" in node)
    );
  }

  function shouldApplyCornerRadiusToDirectNode(node) {
    if (!node) {
      return false;
    }
    if (node.type === "RECTANGLE") {
      return true;
    }
    if ("clipsContent" in node && node.clipsContent === true) {
      return true;
    }
    return hasVisibleCornerRadiusPaint(node);
  }

  function hasVisibleCornerRadiusPaint(node) {
    return hasVisiblePaintArray(node && node.fills) || hasVisiblePaintArray(node && node.strokes);
  }

  function hasVisiblePaintArray(paints) {
    if (!Array.isArray(paints)) {
      return false;
    }
    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (!paint || paint.visible === false) {
        continue;
      }
      const opacity = typeof paint.opacity === "number" && Number.isFinite(paint.opacity) ? paint.opacity : 1;
      if (opacity > 0) {
        return true;
      }
    }
    return false;
  }

  function parseCornerRadiusSpec(rawValue) {
    const raw = String(rawValue == null ? "" : rawValue).trim().toLowerCase();
    if (!raw) {
      throw new Error("라운드 값을 입력해 주세요. 예: 6, 12px, 25%, auto");
    }

    if (raw === "auto" || raw === "quarter" || raw === "formula") {
      return {
        mode: "auto",
        value: FORMULA_PERCENT_RADIUS,
      };
    }

    if (raw === "pill" || raw === "capsule" || raw === "max" || raw === "full") {
      return {
        mode: "percent",
        value: MAX_PERCENT_RADIUS,
      };
    }

    const percentMatch = raw.match(/^(\d+(?:\.\d+)?)\s*%$/);
    if (percentMatch) {
      const value = Number(percentMatch[1]);
      if (!Number.isFinite(value) || value < 0) {
        throw new Error("퍼센트 라운드 값은 0% 이상이어야 합니다.");
      }
      return {
        mode: "percent",
        value: Math.min(value, MAX_PERCENT_RADIUS),
      };
    }

    const pxMatch = raw.replace(",", ".").match(/^(\d+(?:\.\d+)?)\s*(?:px)?$/);
    if (pxMatch) {
      const value = Number(pxMatch[1]);
      if (!Number.isFinite(value) || value < 0) {
        throw new Error("px 라운드 값은 0 이상이어야 합니다.");
      }
      return {
        mode: "px",
        value,
      };
    }

    throw new Error("라운드 값 형식을 확인해 주세요. 예: 6, 12px, 25%, auto");
  }

  function resolveCornerRadiusForTarget(spec, target) {
    const minSide = Math.max(0, Math.min(Number(target.width) || 0, Number(target.height) || 0));
    const maxRadius = minSide > 0 ? minSide / 2 : Number.POSITIVE_INFINITY;
    const rawRadius =
      spec.mode === "auto"
        ? resolveAutoCornerRadiusForTarget(target)
        : spec.mode === "percent"
          ? minSide * (spec.value / 100)
          : spec.value;
    return roundCornerRadius(Math.max(0, Math.min(rawRadius, maxRadius)));
  }

  function resolveAutoCornerRadiusForTarget(target) {
    const width = Math.max(0, Number(target && target.width) || 0);
    const height = Math.max(0, Number(target && target.height) || 0);
    const shortSide = Math.min(width, height);
    const longSide = Math.max(width, height);
    if (!(shortSide > 0)) {
      return 0;
    }

    const baseRadius = shortSide * (FORMULA_PERCENT_RADIUS / 100);
    const softCappedRadius =
      shortSide <= AUTO_RADIUS_SOFT_CAP_START_SIDE
        ? baseRadius
        : AUTO_RADIUS_SOFT_CAP_START_SIDE * (FORMULA_PERCENT_RADIUS / 100) +
          Math.sqrt(shortSide - AUTO_RADIUS_SOFT_CAP_START_SIDE) * AUTO_RADIUS_SOFT_CAP_GROWTH;
    const aspectRatio = longSide / shortSide;
    const aspectDamping =
      shortSide <= AUTO_RADIUS_SOFT_CAP_START_SIDE || !(aspectRatio > 1)
        ? 1
        : Math.max(
            AUTO_RADIUS_ASPECT_DAMPING_MIN,
            1 / Math.pow(aspectRatio, AUTO_RADIUS_ASPECT_DAMPING_POWER)
          );

    return Math.min(baseRadius, softCappedRadius) * aspectDamping;
  }

  function setCornerRadius(node, radius) {
    let wrote = false;

    if ("cornerRadius" in node) {
      node.cornerRadius = radius;
      wrote = true;
    }
    if ("topLeftRadius" in node) {
      node.topLeftRadius = radius;
      wrote = true;
    }
    if ("topRightRadius" in node) {
      node.topRightRadius = radius;
      wrote = true;
    }
    if ("bottomRightRadius" in node) {
      node.bottomRightRadius = radius;
      wrote = true;
    }
    if ("bottomLeftRadius" in node) {
      node.bottomLeftRadius = radius;
      wrote = true;
    }

    if (!wrote) {
      throw new Error("이 레이어는 라운드 값을 직접 수정할 수 없습니다.");
    }
  }

  function readUniformCornerRadius(node) {
    if ("cornerRadius" in node && typeof node.cornerRadius === "number" && Number.isFinite(node.cornerRadius)) {
      return roundCornerRadius(node.cornerRadius);
    }

    const values = ["topLeftRadius", "topRightRadius", "bottomRightRadius", "bottomLeftRadius"]
      .filter((key) => key in node)
      .map((key) => node[key])
      .filter((value) => typeof value === "number" && Number.isFinite(value));

    if (!values.length || values.length < 4) {
      return null;
    }

    const first = values[0];
    return values.every((value) => Math.abs(value - first) <= 0.001) ? roundCornerRadius(first) : null;
  }

  function serializeCornerRadiusTarget(target) {
    return {
      nodeId: target.nodeId,
      nodeName: target.nodeName,
      nodeType: target.nodeType,
      width: roundCornerRadius(target.width),
      height: roundCornerRadius(target.height),
      currentRadius: target.currentRadius,
    };
  }

  function buildSkippedCornerRadiusNode(node, reason) {
    return {
      nodeId: node && node.id ? node.id : "",
      nodeName: safeCornerRadiusName(node),
      nodeType: node && node.type ? String(node.type) : "UNKNOWN",
      reason,
    };
  }

  async function readLastCornerRadiusValue() {
    try {
      const value = await figma.clientStorage.getAsync(LAST_VALUE_STORAGE_KEY);
      return typeof value === "string" && value.trim() ? value.trim() : "";
    } catch (error) {
      return "";
    }
  }

  async function writeLastCornerRadiusValue(value) {
    try {
      await figma.clientStorage.setAsync(LAST_VALUE_STORAGE_KEY, value);
    } catch (error) {}
  }

  function formatCornerRadiusSpec(spec) {
    if (spec.mode === "auto") {
      return "auto";
    }
    return spec.mode === "percent" ? formatCornerRadiusNumber(spec.value) + "%" : formatCornerRadiusNumber(spec.value);
  }

  function formatCornerRadiusPx(value) {
    return formatCornerRadiusNumber(value) + "px";
  }

  function formatCornerRadiusNumber(value) {
    const rounded = roundCornerRadius(value);
    return Math.abs(rounded % 1) < 0.001 ? String(Math.round(rounded)) : String(rounded);
  }

  function roundCornerRadius(value) {
    return Math.round(Number(value) || 0);
  }

  function safeCornerRadiusName(node) {
    const name = node && typeof node.name === "string" ? node.name.trim() : "";
    return name || "Layer";
  }

  function buildCornerRadiusToast(result) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount = Number(summary.appliedCount) || 0;
    const skippedCount = Number(summary.skippedCount) || 0;
    return skippedCount > 0
      ? `라운드 값 적용 ${appliedCount}개, 제외 ${skippedCount}개`
      : `라운드 값 적용 ${appliedCount}개`;
  }

  function normalizeCornerRadiusError(error, fallback) {
    if (error instanceof Error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_BUTTON_TEXT_AUTO_SIZE_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 24;
  const BUTTON_RULES = [
    { fontSize: 12, lineHeight: 16, paddingY: 6, height: 28 },
    { fontSize: 13, lineHeight: 18, paddingY: 7, height: 32 },
    { fontSize: 14, lineHeight: 20, paddingY: 10, height: 40 },
    { fontSize: 15, lineHeight: 22, paddingY: 11, height: 44 },
    { fontSize: 16, lineHeight: 24, paddingY: 12, height: 48 },
    { fontSize: 18, lineHeight: 28, paddingY: 14, height: 56 },
  ];
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isButtonTextAutoSizeMessage(message)) {
      await runButtonTextAutoSize();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_BUTTON_TEXT_AUTO_SIZE_PATCH__ = true;

  function isButtonTextAutoSizeMessage(message) {
    return !!message && message.type === "run-button-text-auto-size";
  }

  async function runButtonTextAutoSize() {
    if (isRunning) {
      postStatus("running", "\uBC84\uD2BC \uD14D\uC2A4\uD2B8 \uD06C\uAE30\uC5D0 \uB9DE\uCDB0 \uBC15\uC2A4\uB97C \uC870\uC808\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");
      return;
    }

    isRunning = true;
    postStatus("running", "\uBC84\uD2BC \uD14D\uC2A4\uD2B8 \uD06C\uAE30\uC5D0 \uB9DE\uB294 \uBC15\uC2A4 \uD06C\uAE30\uB97C \uACC4\uC0B0\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = await applyButtonTextAutoSize();
      figma.ui.postMessage({
        type: "button-text-auto-size-result",
        result,
      });
      figma.notify(buildResultToast(result), { timeout: 2200 });
    } catch (error) {
      const message = normalizeErrorMessage(
        error,
        "\uBC84\uD2BC \uD14D\uC2A4\uD2B8 \uC624\uD1A0 \uC0AC\uC774\uC988\uB97C \uCC98\uB9AC\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4."
      );
      figma.ui.postMessage({
        type: "button-text-auto-size-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2400 });
    } finally {
      isRunning = false;
    }
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "button-text-auto-size-status",
      status,
      message,
    });
  }

  async function applyButtonTextAutoSize() {
    await ensureSelectionAccess();

    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("\uD14D\uC2A4\uD2B8\uB098 \uD14D\uC2A4\uD2B8+\uBC15\uC2A4\uB97C \uBA3C\uC800 \uC120\uD0DD\uD574 \uC8FC\uC138\uC694.");
    }

    const textNodes = collectTextNodesFromSelection(selection);
    if (!textNodes.length) {
      throw new Error("\uBC84\uD2BC \uD06C\uAE30\uB97C \uB9DE\uCD9C \uD14D\uC2A4\uD2B8\uB97C \uD558\uB098 \uC774\uC0C1 \uC120\uD0DD\uD574 \uC8FC\uC138\uC694.");
    }

    const boxNodes = collectBoxNodesFromSelection(selection);
    const usedBoxIds = {};
    const resized = [];
    const created = [];
    const skipped = [];

    for (let index = 0; index < textNodes.length; index += 1) {
      const textNode = textNodes[index];
      try {
        const result = await fitSingleButtonText(textNode, boxNodes, usedBoxIds);
        if (result && result.mode === "created") {
          created.push(result.entry);
        } else if (result && result.mode === "resized") {
          resized.push(result.entry);
        }
      } catch (error) {
        skipped.push({
          nodeId: safeNodeId(textNode),
          nodeName: safeName(textNode),
          nodeType: safeNodeType(textNode),
          reason: normalizeErrorMessage(error, "\uC774 \uD14D\uC2A4\uD2B8\uB294 \uBC15\uC2A4 \uD06C\uAE30\uB97C \uB9DE\uCD9C \uC218 \uC5C6\uC2B5\uB2C8\uB2E4."),
        });
      }
    }

    if (!resized.length && !created.length) {
      const reason = skipped.length
        ? skipped[0].reason
        : "\uC801\uC6A9\uD560 \uD14D\uC2A4\uD2B8\uB098 \uBC15\uC2A4\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.";
      throw new Error(reason);
    }

    restoreSelection(textNodes, resized, created);

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionCount: selection.length,
        textCount: textNodes.length,
        resizedBoxCount: resized.length,
        createdBoxCount: created.length,
        skippedCount: skipped.length,
      },
      resized: resized.slice(0, RESULT_PREVIEW_LIMIT),
      created: created.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function collectTextNodesFromSelection(selection) {
    const textNodes = [];
    for (let index = 0; index < selection.length; index += 1) {
      collectDescendantNodes(selection[index], textNodes, (node) => node && node.type === "TEXT" && !node.removed);
    }
    return textNodes;
  }

  function collectBoxNodesFromSelection(selection) {
    const boxNodes = [];
    for (let index = 0; index < selection.length; index += 1) {
      collectDescendantNodes(selection[index], boxNodes, isResizableBoxNode);
    }
    return boxNodes;
  }

  function collectDescendantNodes(node, list, predicate) {
    if (!node || node.removed) {
      return;
    }
    if (predicate(node)) {
      appendUniqueNode(list, node);
    }
    const children = Array.isArray(node.children) ? node.children : [];
    for (let index = 0; index < children.length; index += 1) {
      collectDescendantNodes(children[index], list, predicate);
    }
  }

  async function fitSingleButtonText(textNode, boxNodes, usedBoxIds) {
    await loadFontsForTextNode(textNode);

    const fontSize = resolveUniformFontSize(textNode);
    if (!(fontSize > 0)) {
      throw new Error("\uD14D\uC2A4\uD2B8\uC758 \uD3F0\uD2B8 \uD06C\uAE30\uAC00 \uC11E\uC5EC \uC788\uC5B4 \uAE30\uC900 \uB192\uC774\uB97C \uACC4\uC0B0\uD560 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.");
    }

    const metrics = resolveButtonMetrics(fontSize);
    const beforeTextSize = {
      width: roundMetric(getNodeWidth(textNode)),
      height: roundMetric(getNodeHeight(textNode)),
    };
    const beforeLineHeight = describeLineHeight(textNode.lineHeight, fontSize);

    const textBounds = getAbsoluteBounds(textNode);
    if (!textBounds || textBounds.width <= 0 || textBounds.height <= 0) {
      throw new Error("\uD14D\uC2A4\uD2B8\uC758 \uBC14\uC6B4\uB4DC\uB97C \uC77D\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const targetRect = buildTargetButtonRect(textBounds, metrics);
    const pairedBox = findBestBoxForText(textNode, boxNodes, usedBoxIds);

    if (pairedBox) {
      const beforeBoxSize = {
        width: roundMetric(getNodeWidth(pairedBox)),
        height: roundMetric(getNodeHeight(pairedBox)),
      };

      applyRectToBox(pairedBox, textNode, targetRect);
      if (safeNodeId(pairedBox)) {
        usedBoxIds[safeNodeId(pairedBox)] = true;
      }

      return {
        mode: "resized",
        entry: {
          nodeId: safeNodeId(pairedBox),
          nodeName: safeName(pairedBox),
          textNodeId: safeNodeId(textNode),
          textNodeName: safeName(textNode),
          fontSize: roundMetric(fontSize),
          lineHeight: beforeLineHeight,
          paddingY: targetRect.paddingY,
          paddingX: targetRect.paddingX,
          from: formatSize(beforeBoxSize),
          to: formatSize({ width: targetRect.width, height: targetRect.height }),
          textFrom: formatSize(beforeTextSize) + " / " + beforeLineHeight,
          textTo: formatSize({ width: getNodeWidth(textNode), height: getNodeHeight(textNode) }) + " / " + beforeLineHeight,
        },
      };
    }

    const createdBox = createBoxBehindText(textNode, targetRect, metrics);
    centerTextInBox(textNode, createdBox, targetRect);
    return {
      mode: "created",
      entry: {
        nodeId: safeNodeId(createdBox),
        nodeName: safeName(createdBox),
        textNodeId: safeNodeId(textNode),
        textNodeName: safeName(textNode),
        fontSize: roundMetric(fontSize),
        lineHeight: beforeLineHeight,
        paddingY: targetRect.paddingY,
        paddingX: targetRect.paddingX,
        to: formatSize({ width: targetRect.width, height: targetRect.height }),
        textFrom: formatSize(beforeTextSize) + " / " + beforeLineHeight,
        textTo: formatSize({ width: getNodeWidth(textNode), height: getNodeHeight(textNode) }) + " / " + beforeLineHeight,
      },
    };
  }

  function resolveButtonMetrics(fontSize) {
    const roundedSize = Math.round(Number(fontSize) || 0);
    for (let index = 0; index < BUTTON_RULES.length; index += 1) {
      if (BUTTON_RULES[index].fontSize === roundedSize) {
        return buildMetrics(BUTTON_RULES[index]);
      }
    }

    const lineHeight = Math.max(12, roundToEven(roundedSize * 2 - 8));
    const paddingY = Math.max(4, Math.round(lineHeight / 2));
    return buildMetrics({
      fontSize: roundedSize,
      lineHeight,
      paddingY,
      height: lineHeight + paddingY * 2,
    });
  }

  function buildMetrics(rule) {
    const height = Math.max(1, Math.round(Number(rule.height) || Number(rule.lineHeight) + Number(rule.paddingY) * 2));
    return {
      fontSize: Number(rule.fontSize) || 0,
      lineHeight: Math.max(1, Math.round(Number(rule.lineHeight) || 0)),
      paddingY: Math.max(0, Math.round(Number(rule.paddingY) || 0)),
      paddingX: Math.max(12, Math.round(height / 2)),
      height,
    };
  }

  function buildTargetButtonRect(textBounds, metrics) {
    const paddingX = resolveTargetHorizontalPadding(metrics);
    const paddingY = resolveTargetVerticalPadding(metrics);
    const width = Math.max(1, Math.ceil(textBounds.width + paddingX * 2));
    const height = Math.max(1, Math.ceil(textBounds.height + paddingY * 2));
    return {
      x: roundMetric(textBounds.x - paddingX),
      y: roundMetric(textBounds.y - paddingY),
      width,
      height,
      paddingX,
      paddingY,
    };
  }

  function resolveTargetHorizontalPadding(metrics) {
    const fontSize = Number(metrics && metrics.fontSize) || 0;
    const paddingX = Math.max(0, Number(metrics && metrics.paddingX) || 0);
    if (fontSize > 0 && fontSize <= 18) {
      return paddingX + 4;
    }
    return Math.max(18, Math.min(44, Math.round(fontSize * 0.7)));
  }

  function resolveTargetVerticalPadding(metrics) {
    const fontSize = Number(metrics && metrics.fontSize) || 0;
    const paddingY = Math.max(0, Number(metrics && metrics.paddingY) || 0);
    if (fontSize > 0 && fontSize <= 18) {
      return paddingY + 3;
    }
    return Math.max(14, Math.min(34, Math.round(fontSize * 0.62)));
  }

  function findBestBoxForText(textNode, boxNodes, usedBoxIds) {
    const textBounds = getAbsoluteBounds(textNode);
    if (!textBounds) {
      return null;
    }

    let best = null;
    let bestScore = -Infinity;

    for (let index = 0; index < boxNodes.length; index += 1) {
      const box = boxNodes[index];
      if (!box || box.removed || box === textNode) {
        continue;
      }
      const boxId = safeNodeId(box);
      if (boxId && usedBoxIds[boxId]) {
        continue;
      }
      if (!canPairTextAndBox(textNode, box)) {
        continue;
      }

      const boxBounds = getAbsoluteBounds(box);
      if (!boxBounds) {
        continue;
      }

      const overlapRatio = getOverlapRatio(textBounds, boxBounds);
      const contains = containsBounds(boxBounds, textBounds);
      const centerDistance = getCenterDistance(textBounds, boxBounds);
      const sizePenalty = getBoxSizePenalty(textBounds, boxBounds);
      const singleBoxBoost = boxNodes.length === 1 ? 20000 : 0;
      const score = overlapRatio * 10000 + (contains ? 8000 : 0) + singleBoxBoost - centerDistance - sizePenalty;

      if (score > bestScore) {
        bestScore = score;
        best = box;
      }
    }

    if (!best) {
      return null;
    }

    if (boxNodes.length === 1) {
      return best;
    }

    return bestScore > -500 ? best : null;
  }

  function canPairTextAndBox(textNode, boxNode) {
    if (!textNode || !boxNode) {
      return false;
    }
    if (textNode.parent === boxNode) {
      return true;
    }
    if (textNode.parent && boxNode.parent && textNode.parent === boxNode.parent) {
      return true;
    }
    if (isAncestorNode(boxNode, textNode)) {
      return true;
    }
    const commonAncestor = findNearestCommonAncestor(textNode, boxNode);
    return !!commonAncestor && commonAncestor.type !== "PAGE" && commonAncestor.type !== "DOCUMENT";
  }

  function isAncestorNode(ancestor, node) {
    let parent = node && node.parent ? node.parent : null;
    while (parent) {
      if (parent === ancestor) {
        return true;
      }
      parent = parent.parent || null;
    }
    return false;
  }

  function findNearestCommonAncestor(firstNode, secondNode) {
    const ancestors = [];
    let parent = firstNode && firstNode.parent ? firstNode.parent : null;
    while (parent) {
      ancestors.push(parent);
      parent = parent.parent || null;
    }

    parent = secondNode && secondNode.parent ? secondNode.parent : null;
    while (parent) {
      for (let index = 0; index < ancestors.length; index += 1) {
        if (ancestors[index] === parent) {
          return parent;
        }
      }
      parent = parent.parent || null;
    }
    return null;
  }

  function applyRectToBox(boxNode, textNode, targetRect) {
    if (textNode.parent === boxNode) {
      if (isAutoLayoutParent(boxNode)) {
        applyAutoLayoutButtonPadding(boxNode, {
          paddingX: targetRect.paddingX,
          paddingY: targetRect.paddingY,
        });
        setEnumProperty(boxNode, "primaryAxisAlignItems", "CENTER");
        setEnumProperty(boxNode, "counterAxisAlignItems", "CENTER");
        setNodeAbsoluteRect(boxNode, targetRect);
        return;
      }

      setNodeAbsoluteRect(boxNode, targetRect);
      centerTextInBox(textNode, boxNode, targetRect);
      return;
    }

    setNodeAbsoluteRect(boxNode, targetRect);
    moveNodeBehindText(boxNode, textNode);
    centerTextInBox(textNode, boxNode, targetRect);
  }

  function createBoxBehindText(textNode, targetRect, metrics) {
    const parent = textNode && textNode.parent && "appendChild" in textNode.parent ? textNode.parent : figma.currentPage;
    const rect = figma.createRectangle();
    rect.name = "Button Text Auto Size Box";
    rect.fills = [buildDefaultBoxFill(textNode)];
    rect.strokes = [];
    rect.cornerRadius = Math.min(8, Math.max(4, Math.round(metrics.height / 4)));
    resizeNode(rect, targetRect.width, targetRect.height);

    if ("layoutPositioning" in rect && isAutoLayoutParent(parent)) {
      try {
        rect.layoutPositioning = "ABSOLUTE";
      } catch (error) {}
    }

    insertNodeBehindText(parent, rect, textNode);
    setNodeAbsoluteRect(rect, targetRect);
    return rect;
  }

  function insertNodeBehindText(parent, node, textNode) {
    if (!parent || !("appendChild" in parent)) {
      figma.currentPage.appendChild(node);
      return;
    }

    const children = Array.isArray(parent.children) ? parent.children : [];
    const textIndex = children.indexOf(textNode);
    if (textIndex >= 0 && typeof parent.insertChild === "function") {
      parent.insertChild(textIndex, node);
      return;
    }

    parent.appendChild(node);
  }

  function moveNodeBehindText(node, textNode) {
    const parent = node && node.parent ? node.parent : null;
    if (!parent || parent !== textNode.parent || typeof parent.insertChild !== "function") {
      return;
    }
    const children = Array.isArray(parent.children) ? parent.children : [];
    const textIndex = children.indexOf(textNode);
    if (textIndex >= 0) {
      parent.insertChild(textIndex, node);
    }
  }

  function setNodeAbsoluteRect(node, rect) {
    resizeNode(node, rect.width, rect.height);
    for (let attempt = 0; attempt < 4; attempt += 1) {
      setNodeAbsolutePosition(node, rect.x, rect.y);
      const bounds = getPlacementBounds(node) || getAbsoluteBounds(node);
      if (!bounds || (Math.abs(bounds.x - rect.x) < 0.01 && Math.abs(bounds.y - rect.y) < 0.01)) {
        return;
      }
    }
  }

  function centerTextInBox(textNode, boxNode, fallbackRect) {
    if (!textNode || textNode.removed) {
      return;
    }
    if (isAutoLayoutParent(textNode.parent) && !isAbsoluteLayoutChild(textNode)) {
      return;
    }

    for (let attempt = 0; attempt < 2; attempt += 1) {
      const boxRect = getAbsoluteBounds(boxNode) || fallbackRect;
      const textRenderBounds = getAbsoluteBounds(textNode);
      if (!boxRect || !textRenderBounds) {
        return;
      }
      const boxCenterX = boxRect.x + boxRect.width / 2;
      const boxCenterY = boxRect.y + boxRect.height / 2;
      const textCenterX = textRenderBounds.x + textRenderBounds.width / 2;
      const textCenterY = textRenderBounds.y + textRenderBounds.height / 2;
      const dx = boxCenterX - textCenterX;
      const dy = boxCenterY - textCenterY;
      if (Math.abs(dx) < 0.01 && Math.abs(dy) < 0.01) {
        return;
      }
      moveNodeByAbsoluteDelta(textNode, dx, dy);
    }
  }

  function getPlacementBounds(node) {
    if (!node) {
      return null;
    }
    const bounds = node.absoluteBoundingBox || node.absoluteRenderBounds || null;
    if (!bounds) {
      return null;
    }
    return {
      x: Number(bounds.x) || 0,
      y: Number(bounds.y) || 0,
      width: Math.max(0, Number(bounds.width) || 0),
      height: Math.max(0, Number(bounds.height) || 0),
    };
  }

  function isAbsoluteLayoutChild(node) {
    return !!node && "layoutPositioning" in node && node.layoutPositioning === "ABSOLUTE";
  }

  function setNodeAbsolutePosition(node, x, y) {
    const parent = node && node.parent ? node.parent : null;
    const localPoint = absolutePointToLocal(parent, x, y);
    if (node && "x" in node) {
      node.x = roundMetric(localPoint.x);
    }
    if (node && "y" in node) {
      node.y = roundMetric(localPoint.y);
    }
  }

  function moveNodeByAbsoluteDelta(node, dx, dy) {
    const parent = node && node.parent ? node.parent : null;
    const localDelta = absoluteDeltaToLocal(parent, dx, dy);
    if (node && "x" in node) {
      node.x = roundMetric((Number(node.x) || 0) + localDelta.x);
    }
    if (node && "y" in node) {
      node.y = roundMetric((Number(node.y) || 0) + localDelta.y);
    }
  }

  function absolutePointToLocal(parent, x, y) {
    if (!parent || !("absoluteTransform" in parent)) {
      return { x, y };
    }
    const inverse = invertTransform(parent.absoluteTransform);
    if (!inverse) {
      return { x, y };
    }
    return {
      x: inverse[0][0] * x + inverse[0][1] * y + inverse[0][2],
      y: inverse[1][0] * x + inverse[1][1] * y + inverse[1][2],
    };
  }

  function absoluteDeltaToLocal(parent, dx, dy) {
    if (!parent || !("absoluteTransform" in parent)) {
      return { x: dx, y: dy };
    }
    const inverse = invertTransform(parent.absoluteTransform);
    if (!inverse) {
      return { x: dx, y: dy };
    }
    return {
      x: inverse[0][0] * dx + inverse[0][1] * dy,
      y: inverse[1][0] * dx + inverse[1][1] * dy,
    };
  }

  function invertTransform(matrix) {
    if (!matrix || !matrix[0] || !matrix[1]) {
      return null;
    }
    const a = Number(matrix[0][0]) || 0;
    const b = Number(matrix[0][1]) || 0;
    const c = Number(matrix[1][0]) || 0;
    const d = Number(matrix[1][1]) || 0;
    const tx = Number(matrix[0][2]) || 0;
    const ty = Number(matrix[1][2]) || 0;
    const det = a * d - b * c;
    if (Math.abs(det) < 0.000001) {
      return null;
    }
    const invDet = 1 / det;
    return [
      [d * invDet, -b * invDet, (b * ty - d * tx) * invDet],
      [-c * invDet, a * invDet, (c * tx - a * ty) * invDet],
    ];
  }

  function buildDefaultBoxFill(textNode) {
    const textColor = getFirstSolidFill(textNode);
    if (textColor && getLuminance(textColor.color) > 0.72) {
      return {
        type: "SOLID",
        color: { r: 0.067, g: 0.094, b: 0.153 },
        opacity: 1,
      };
    }

    return {
      type: "SOLID",
      color: { r: 0.953, g: 0.961, b: 0.973 },
      opacity: 1,
    };
  }

  function getFirstSolidFill(node) {
    if (!node || !("fills" in node) || !Array.isArray(node.fills)) {
      return null;
    }
    for (let index = 0; index < node.fills.length; index += 1) {
      const fill = node.fills[index];
      if (fill && fill.type === "SOLID" && fill.visible !== false && fill.color) {
        return fill;
      }
    }
    return null;
  }

  function getLuminance(color) {
    const r = normalizeColorChannel(color && color.r);
    const g = normalizeColorChannel(color && color.g);
    const b = normalizeColorChannel(color && color.b);
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  }

  function normalizeColorChannel(value) {
    const channel = Math.max(0, Math.min(1, Number(value) || 0));
    return channel <= 0.03928 ? channel / 12.92 : Math.pow((channel + 0.055) / 1.055, 2.4);
  }

  function isResizableBoxNode(node) {
    return (
      !!node &&
      node.type !== "TEXT" &&
      node.type !== "PAGE" &&
      node.type !== "DOCUMENT" &&
      node.type !== "GROUP" &&
      !node.removed &&
      typeof node.resize === "function" &&
      typeof getNodeWidth(node) === "number" &&
      typeof getNodeHeight(node) === "number" &&
      "x" in node &&
      "y" in node
    );
  }

  function isAutoLayoutParent(node) {
    return !!node && "layoutMode" in node && node.layoutMode && node.layoutMode !== "NONE";
  }

  function applyAutoLayoutButtonPadding(node, metrics) {
    setNumericProperty(node, "paddingTop", metrics.paddingY);
    setNumericProperty(node, "paddingBottom", metrics.paddingY);
    setNumericProperty(node, "paddingLeft", metrics.paddingX);
    setNumericProperty(node, "paddingRight", metrics.paddingX);
    setEnumProperty(node, "primaryAxisAlignItems", "CENTER");
    setEnumProperty(node, "counterAxisAlignItems", "CENTER");
    setEnumProperty(node, "primaryAxisSizingMode", "FIXED");
    setEnumProperty(node, "counterAxisSizingMode", "FIXED");
  }

  function setNumericProperty(node, key, value) {
    if (!node || !(key in node)) {
      return;
    }
    try {
      node[key] = value;
    } catch (error) {}
  }

  function setEnumProperty(node, key, value) {
    if (!node || !(key in node)) {
      return;
    }
    try {
      node[key] = value;
    } catch (error) {}
  }

  function resizeNode(node, width, height) {
    const nextWidth = Math.max(1, roundMetric(width));
    const nextHeight = Math.max(1, roundMetric(height));
    if (typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(nextWidth, nextHeight);
      return;
    }
    node.resize(nextWidth, nextHeight);
  }

  function getAbsoluteBounds(node) {
    if (!node) {
      return null;
    }
    const bounds =
      node.type === "TEXT"
        ? node.absoluteRenderBounds || node.absoluteBoundingBox || null
        : node.absoluteBoundingBox || node.absoluteRenderBounds || null;
    if (!bounds) {
      return null;
    }
    return {
      x: Number(bounds.x) || 0,
      y: Number(bounds.y) || 0,
      width: Math.max(0, Number(bounds.width) || 0),
      height: Math.max(0, Number(bounds.height) || 0),
    };
  }

  function containsBounds(outer, inner) {
    return (
      outer.x <= inner.x + 0.5 &&
      outer.y <= inner.y + 0.5 &&
      outer.x + outer.width >= inner.x + inner.width - 0.5 &&
      outer.y + outer.height >= inner.y + inner.height - 0.5
    );
  }

  function getOverlapRatio(a, b) {
    const left = Math.max(a.x, b.x);
    const top = Math.max(a.y, b.y);
    const right = Math.min(a.x + a.width, b.x + b.width);
    const bottom = Math.min(a.y + a.height, b.y + b.height);
    const overlap = Math.max(0, right - left) * Math.max(0, bottom - top);
    return overlap / Math.max(1, a.width * a.height);
  }

  function getCenterDistance(a, b) {
    const ax = a.x + a.width / 2;
    const ay = a.y + a.height / 2;
    const bx = b.x + b.width / 2;
    const by = b.y + b.height / 2;
    const dx = ax - bx;
    const dy = ay - by;
    return Math.sqrt(dx * dx + dy * dy);
  }

  function getBoxSizePenalty(textBounds, boxBounds) {
    const textArea = Math.max(1, textBounds.width * textBounds.height);
    const boxArea = Math.max(1, boxBounds.width * boxBounds.height);
    const areaRatio = boxArea / textArea;
    return Math.min(6000, Math.max(0, areaRatio - 1) * 35);
  }

  async function loadFontsForTextNode(node) {
    const fonts = [];
    const characters = typeof node.characters === "string" ? node.characters : "";
    if (characters.length > 0 && typeof node.getRangeAllFontNames === "function") {
      const rangeFonts = node.getRangeAllFontNames(0, characters.length);
      for (let index = 0; index < rangeFonts.length; index += 1) {
        appendFontName(fonts, rangeFonts[index]);
      }
    } else {
      appendFontName(fonts, node.fontName);
    }

    for (let index = 0; index < fonts.length; index += 1) {
      await figma.loadFontAsync(fonts[index]);
    }
  }

  function appendFontName(fonts, fontName) {
    if (!fontName || fontName === figma.mixed || typeof fontName !== "object") {
      return;
    }
    if (typeof fontName.family !== "string" || typeof fontName.style !== "string") {
      return;
    }
    const key = fontName.family + "\u0000" + fontName.style;
    for (let index = 0; index < fonts.length; index += 1) {
      if (fonts[index].family + "\u0000" + fonts[index].style === key) {
        return;
      }
    }
    fonts.push(fontName);
  }

  function resolveUniformFontSize(node) {
    if (typeof node.fontSize === "number" && Number.isFinite(node.fontSize)) {
      return node.fontSize;
    }
    const characters = typeof node.characters === "string" ? node.characters : "";
    if (!characters.length || typeof node.getRangeFontSize !== "function") {
      return null;
    }
    const first = node.getRangeFontSize(0, 1);
    if (typeof first !== "number" || !Number.isFinite(first)) {
      return null;
    }
    for (let index = 1; index < characters.length; index += 1) {
      const next = node.getRangeFontSize(index, index + 1);
      if (typeof next !== "number" || Math.abs(next - first) > 0.001) {
        return null;
      }
    }
    return first;
  }

  async function ensureSelectionAccess() {
    if (typeof figma.loadAllPagesAsync !== "function") {
      return;
    }
    try {
      await figma.loadAllPagesAsync();
    } catch (error) {
      console.warn("[pigma] button text auto size selection preload failed:", error);
    }
  }

  function restoreSelection(textNodes, resized, created) {
    const nextSelection = [];
    appendUniqueNodeList(nextSelection, textNodes);
    appendResultNodes(nextSelection, resized);
    appendResultNodes(nextSelection, created);
    if (nextSelection.length) {
      figma.currentPage.selection = nextSelection;
    }
  }

  function appendResultNodes(list, entries) {
    if (typeof figma.getNodeById !== "function") {
      return;
    }
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (entry && entry.nodeId) {
        let node = null;
        try {
          node = figma.getNodeById(entry.nodeId);
        } catch (error) {
          node = null;
        }
        if (node && !node.removed) {
          appendUniqueNode(list, node);
        }
      }
    }
  }

  function appendUniqueNodeList(list, nodes) {
    for (let index = 0; index < nodes.length; index += 1) {
      appendUniqueNode(list, nodes[index]);
    }
  }

  function appendUniqueNode(list, node) {
    if (!node || node.removed) {
      return;
    }
    for (let index = 0; index < list.length; index += 1) {
      if (list[index] === node) {
        return;
      }
    }
    list.push(node);
  }

  function buildResultToast(result) {
    const summary = result && result.summary ? result.summary : {};
    const resizedCount = Number(summary.resizedBoxCount) || 0;
    const createdCount = Number(summary.createdBoxCount) || 0;
    const skippedCount = Number(summary.skippedCount) || 0;
    let message = "\uBC84\uD2BC \uD14D\uC2A4\uD2B8 \uC624\uD1A0 \uC0AC\uC774\uC988 \uC644\uB8CC";
    message += " (\uC870\uC808 " + resizedCount + "\uAC1C, \uC0DD\uC131 " + createdCount + "\uAC1C";
    if (skippedCount > 0) {
      message += ", \uC81C\uC678 " + skippedCount + "\uAC1C";
    }
    return message + ")";
  }

  function describeLineHeight(lineHeight, fontSize) {
    if (lineHeight && typeof lineHeight === "object") {
      if (lineHeight.unit === "PIXELS" && typeof lineHeight.value === "number") {
        return roundMetric(lineHeight.value) + "px";
      }
      if (lineHeight.unit === "PERCENT" && typeof lineHeight.value === "number") {
        return roundMetric((fontSize * lineHeight.value) / 100) + "px";
      }
      if (lineHeight.unit === "AUTO") {
        return "auto";
      }
    }
    return "mixed";
  }

  function formatSize(size) {
    return roundMetric(size.width) + " x " + roundMetric(size.height) + "px";
  }

  function getNodeWidth(node) {
    return typeof node.width === "number" ? node.width : 0;
  }

  function getNodeHeight(node) {
    return typeof node.height === "number" ? node.height : 0;
  }

  function roundMetric(value) {
    return Math.round((Number(value) || 0) * 100) / 100;
  }

  function roundToEven(value) {
    return Math.round((Number(value) || 0) / 2) * 2;
  }

  function safeNodeId(node) {
    try {
      return node && typeof node.id === "string" ? node.id : "";
    } catch (error) {
      return "";
    }
  }

  function safeName(node) {
    try {
      if (node && typeof node.name === "string" && node.name.trim()) {
        return node.name.trim();
      }
      return String((node && node.type) || "Layer");
    } catch (error) {
      return "Layer";
    }
  }

  function safeNodeType(node) {
    try {
      return String((node && node.type) || "UNKNOWN");
    } catch (error) {
      return "UNKNOWN";
    }
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_TEXT_LINE_HEIGHT_ADJUST_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 32;
  const HANGUL_SCRIPT = "ko";
  const CJK_SCRIPT = "cjk";
  const LATIN_SCRIPT = "latin";
  const EUROPEAN_SCRIPT = "european";
  const ARABIC_SCRIPT = "arabic";
  const HEBREW_SCRIPT = "hebrew";
  const THAI_SCRIPT = "thai";
  const INDIC_SCRIPT = "indic";
  // Line height uses script groups: CJK/Korean follows KRDS-like 130/140/150%,
  // Latin/European follows Material type-scale anchors, and complex scripts
  // keep at least readable paragraph spacing to avoid clipping marks.
  const MATERIAL_LATIN_LINE_HEIGHT_ANCHORS = [
    { size: 10, lineHeight: 16 },
    { size: 12, lineHeight: 16 },
    { size: 14, lineHeight: 20 },
    { size: 16, lineHeight: 24 },
    { size: 20, lineHeight: 32 },
    { size: 24, lineHeight: 32 },
    { size: 34, lineHeight: 40 },
    { size: 48, lineHeight: 56 },
    { size: 60, lineHeight: 72 },
    { size: 96, lineHeight: 112 },
  ];
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isTextLineHeightMessage(message)) {
      await runTextLineHeightAdjust();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_TEXT_LINE_HEIGHT_ADJUST_PATCH__ = true;

  function isTextLineHeightMessage(message) {
    return !!message && message.type === "run-text-line-height-adjust";
  }

  async function runTextLineHeightAdjust() {
    if (isRunning) {
      postStatus("running", "\uD14D\uC2A4\uD2B8 \uD589\uAC04\uC744 \uC870\uC815\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");
      return;
    }

    isRunning = true;
    postStatus("running", "\uC120\uD0DD\uD55C \uD14D\uC2A4\uD2B8\uC758 \uC5B8\uC5B4\uC640 \uD06C\uAE30\uB97C \uD655\uC778\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = await applyTextLineHeightAdjust();
      figma.ui.postMessage({
        type: "text-line-height-adjust-result",
        result,
      });
      figma.notify(buildResultToast(result), { timeout: 2400 });
    } catch (error) {
      const message = normalizeErrorMessage(
        error,
        "\uD14D\uC2A4\uD2B8 \uD589\uAC04\uC744 \uC870\uC815\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4."
      );
      figma.ui.postMessage({
        type: "text-line-height-adjust-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2600 });
    } finally {
      isRunning = false;
    }
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "text-line-height-adjust-status",
      status,
      message,
    });
  }

  async function applyTextLineHeightAdjust() {
    await ensureSelectionAccess();

    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("\uD589\uAC04\uC744 \uC870\uC815\uD560 \uD14D\uC2A4\uD2B8\uB098 \uD504\uB808\uC784\uC744 \uBA3C\uC800 \uC120\uD0DD\uD574 \uC8FC\uC138\uC694.");
    }

    const textNodes = collectTextNodes(selection);
    if (!textNodes.length) {
      throw new Error("\uC120\uD0DD\uC5D0\uC11C \uC870\uC815\uD560 \uD14D\uC2A4\uD2B8 \uB808\uC774\uC5B4\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const adjusted = [];
    const skipped = [];

    for (let index = 0; index < textNodes.length; index += 1) {
      const textNode = textNodes[index];
      try {
        adjusted.push(await adjustSingleTextNode(textNode));
      } catch (error) {
        skipped.push({
          nodeId: safeNodeId(textNode),
          nodeName: safeName(textNode),
          nodeType: safeNodeType(textNode),
          reason: normalizeErrorMessage(error, "\uC774 \uD14D\uC2A4\uD2B8\uB294 \uD589\uAC04\uC744 \uC870\uC815\uD560 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4."),
        });
      }
    }

    if (!adjusted.length) {
      const reason = skipped.length
        ? skipped[0].reason
        : "\uC801\uC6A9\uD560 \uD14D\uC2A4\uD2B8\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.";
      throw new Error(reason);
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionCount: selection.length,
        textCount: textNodes.length,
        adjustedCount: adjusted.length,
        skippedCount: skipped.length,
      },
      adjusted: adjusted.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  async function adjustSingleTextNode(textNode) {
    if (!textNode || textNode.removed || textNode.type !== "TEXT") {
      throw new Error("\uD14D\uC2A4\uD2B8 \uB808\uC774\uC5B4\uAC00 \uC544\uB2D9\uB2C8\uB2E4.");
    }

    const characters = typeof textNode.characters === "string" ? textNode.characters : "";
    if (!characters.length || !characters.replace(/\s+/g, "").length) {
      throw new Error("\uBE48 \uD14D\uC2A4\uD2B8\uB294 \uAC74\uB108\uB701\uB2C8\uB2E4.");
    }

    await loadFontsForTextNode(textNode);

    const dominantScript = detectDominantScript(characters);
    const runs = buildTypographyRuns(textNode, characters, dominantScript);
    if (!runs.length) {
      throw new Error("\uD589\uAC04 \uAE30\uC900\uC744 \uACC4\uC0B0\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const beforeLineHeight = describeLineHeight(textNode.lineHeight);
    for (let index = 0; index < runs.length; index += 1) {
      const run = runs[index];
      applyLineHeightRange(textNode, run.start, run.end, run.lineHeight);
    }

    return {
      nodeId: safeNodeId(textNode),
      nodeName: safeName(textNode),
      language: describeScriptLanguage(dominantScript),
      beforeLineHeight,
      lineHeightTargets: summarizeTargets(runs, "lineHeight", "px"),
      characterCount: characters.length,
    };
  }

  function buildTypographyRuns(textNode, characters, dominantScript) {
    const runs = [];
    let runStart = -1;
    let runEnd = -1;
    let runLineHeight = null;
    let lastScript = dominantScript;

    for (let index = 0; index < characters.length; index += 1) {
      const rawScript = classifyCharacterScript(characters[index]);
      const script = rawScript || lastScript || dominantScript;
      if (rawScript) {
        lastScript = rawScript;
      }

      const fontSize = resolveFontSizeAt(textNode, index);
      const lineHeight = resolveTargetLineHeightPx(script, fontSize);
      if (!Number.isFinite(lineHeight)) {
        continue;
      }

      if (runStart < 0) {
        runStart = index;
        runEnd = index + 1;
        runLineHeight = lineHeight;
        continue;
      }

      if (lineHeight === runLineHeight && index === runEnd) {
        runEnd = index + 1;
        continue;
      }

      runs.push({ start: runStart, end: runEnd, lineHeight: runLineHeight });
      runStart = index;
      runEnd = index + 1;
      runLineHeight = lineHeight;
    }

    if (runStart >= 0 && runEnd > runStart) {
      runs.push({ start: runStart, end: runEnd, lineHeight: runLineHeight });
    }

    return runs;
  }

  function resolveTargetLineHeightPx(script, fontSize) {
    const size = Math.max(1, Number(fontSize) || 16);
    if (script === HANGUL_SCRIPT || script === CJK_SCRIPT) {
      const ratio = size >= 40 ? 1.3 : size >= 24 ? 1.4 : 1.5;
      return Math.max(1, Math.round(size * ratio));
    }

    const materialLineHeight = interpolateLatinMaterialLineHeight(size);
    if (isComplexScript(script)) {
      return Math.max(materialLineHeight, Math.round(size * 1.5));
    }
    return materialLineHeight;
  }

  function interpolateLatinMaterialLineHeight(size) {
    if (size <= MATERIAL_LATIN_LINE_HEIGHT_ANCHORS[0].size) {
      return MATERIAL_LATIN_LINE_HEIGHT_ANCHORS[0].lineHeight;
    }

    const last = MATERIAL_LATIN_LINE_HEIGHT_ANCHORS[MATERIAL_LATIN_LINE_HEIGHT_ANCHORS.length - 1];
    if (size >= last.size) {
      return last.lineHeight;
    }

    for (let index = 1; index < MATERIAL_LATIN_LINE_HEIGHT_ANCHORS.length; index += 1) {
      const right = MATERIAL_LATIN_LINE_HEIGHT_ANCHORS[index];
      const left = MATERIAL_LATIN_LINE_HEIGHT_ANCHORS[index - 1];
      if (size > right.size) {
        continue;
      }
      const progress = (size - left.size) / Math.max(1, right.size - left.size);
      return Math.max(1, Math.round(left.lineHeight + (right.lineHeight - left.lineHeight) * progress));
    }

    return Math.max(1, Math.round(size * 1.5));
  }

  function isComplexScript(script) {
    return script === ARABIC_SCRIPT || script === HEBREW_SCRIPT || script === THAI_SCRIPT || script === INDIC_SCRIPT;
  }

  function applyLineHeightRange(textNode, start, end, lineHeight) {
    const value = {
      unit: "PIXELS",
      value: lineHeight,
    };

    if (typeof textNode.setRangeLineHeight === "function") {
      textNode.setRangeLineHeight(start, end, value);
      return;
    }

    textNode.lineHeight = value;
  }

  function collectTextNodes(nodes) {
    const result = [];
    const seen = {};
    const stack = Array.isArray(nodes) ? nodes.slice() : [];

    while (stack.length > 0) {
      const node = stack.shift();
      if (!node || node.removed) {
        continue;
      }

      if (node.type === "TEXT") {
        const id = safeNodeId(node);
        if (!id || !seen[id]) {
          if (id) {
            seen[id] = true;
          }
          result.push(node);
        }
        continue;
      }

      if (Array.isArray(node.children)) {
        for (let index = 0; index < node.children.length; index += 1) {
          stack.push(node.children[index]);
        }
      }
    }

    return result;
  }

  async function loadFontsForTextNode(node) {
    const fonts = [];
    const characters = typeof node.characters === "string" ? node.characters : "";
    if (characters.length > 0 && typeof node.getRangeAllFontNames === "function") {
      const rangeFonts = node.getRangeAllFontNames(0, characters.length);
      for (let index = 0; index < rangeFonts.length; index += 1) {
        appendFontName(fonts, rangeFonts[index]);
      }
    } else {
      appendFontName(fonts, node.fontName);
    }

    for (let index = 0; index < fonts.length; index += 1) {
      await figma.loadFontAsync(fonts[index]);
    }
  }

  function appendFontName(list, fontName) {
    if (!fontName || typeof fontName !== "object") {
      return;
    }
    if (typeof fontName.family !== "string" || typeof fontName.style !== "string") {
      return;
    }
    const key = fontName.family + "\u0000" + fontName.style;
    for (let index = 0; index < list.length; index += 1) {
      if (list[index].family + "\u0000" + list[index].style === key) {
        return;
      }
    }
    list.push({ family: fontName.family, style: fontName.style });
  }

  function resolveFontSizeAt(node, index) {
    if (typeof node.fontSize === "number" && Number.isFinite(node.fontSize)) {
      return node.fontSize;
    }
    if (typeof node.getRangeFontSize === "function") {
      const size = node.getRangeFontSize(index, index + 1);
      if (typeof size === "number" && Number.isFinite(size)) {
        return size;
      }
    }
    return 16;
  }

  function detectDominantScript(value) {
    const counts = {
      ko: 0,
      cjk: 0,
      latin: 0,
      european: 0,
      arabic: 0,
      hebrew: 0,
      thai: 0,
      indic: 0,
    };
    const text = String(value || "");
    for (let index = 0; index < text.length; index += 1) {
      const script = classifyCharacterScript(text[index]);
      if (script && Object.prototype.hasOwnProperty.call(counts, script)) {
        counts[script] += 1;
      }
    }

    let bestScript = LATIN_SCRIPT;
    let bestCount = 0;
    const scripts = [HANGUL_SCRIPT, CJK_SCRIPT, LATIN_SCRIPT, EUROPEAN_SCRIPT, ARABIC_SCRIPT, HEBREW_SCRIPT, THAI_SCRIPT, INDIC_SCRIPT];
    for (let index = 0; index < scripts.length; index += 1) {
      const script = scripts[index];
      const count = counts[script] || 0;
      if (count > bestCount) {
        bestScript = script;
        bestCount = count;
      }
    }
    return bestScript;
  }

  function classifyCharacterScript(character) {
    const code = character ? character.charCodeAt(0) : 0;
    if (
      (code >= 0xac00 && code <= 0xd7af) ||
      (code >= 0x1100 && code <= 0x11ff) ||
      (code >= 0x3130 && code <= 0x318f)
    ) {
      return HANGUL_SCRIPT;
    }
    if ((code >= 0x4e00 && code <= 0x9fff) || (code >= 0x3040 && code <= 0x30ff)) {
      return CJK_SCRIPT;
    }
    if (
      (code >= 0x0041 && code <= 0x005a) ||
      (code >= 0x0061 && code <= 0x007a) ||
      (code >= 0x0030 && code <= 0x0039) ||
      isLatinExtendedCode(code)
    ) {
      return LATIN_SCRIPT;
    }
    if (isEuropeanAlphabeticCode(code)) {
      return EUROPEAN_SCRIPT;
    }
    if (isArabicCode(code)) {
      return ARABIC_SCRIPT;
    }
    if (isHebrewCode(code)) {
      return HEBREW_SCRIPT;
    }
    if (isThaiCode(code)) {
      return THAI_SCRIPT;
    }
    if (isIndicCode(code)) {
      return INDIC_SCRIPT;
    }
    return "";
  }

  function isLatinExtendedCode(code) {
    return (
      (code >= 0x00c0 && code <= 0x024f) ||
      (code >= 0x1e00 && code <= 0x1eff) ||
      (code >= 0x2c60 && code <= 0x2c7f) ||
      (code >= 0xa720 && code <= 0xa7ff) ||
      (code >= 0xab30 && code <= 0xab6f)
    );
  }

  function isEuropeanAlphabeticCode(code) {
    return (
      (code >= 0x0370 && code <= 0x03ff) ||
      (code >= 0x0400 && code <= 0x052f) ||
      (code >= 0x1f00 && code <= 0x1fff)
    );
  }

  function isArabicCode(code) {
    return (
      (code >= 0x0600 && code <= 0x06ff) ||
      (code >= 0x0750 && code <= 0x077f) ||
      (code >= 0x08a0 && code <= 0x08ff) ||
      (code >= 0xfb50 && code <= 0xfdff) ||
      (code >= 0xfe70 && code <= 0xfeff)
    );
  }

  function isHebrewCode(code) {
    return code >= 0x0590 && code <= 0x05ff;
  }

  function isThaiCode(code) {
    return code >= 0x0e00 && code <= 0x0e7f;
  }

  function isIndicCode(code) {
    return (
      (code >= 0x0900 && code <= 0x097f) ||
      (code >= 0x0980 && code <= 0x09ff) ||
      (code >= 0x0a00 && code <= 0x0a7f) ||
      (code >= 0x0a80 && code <= 0x0aff) ||
      (code >= 0x0b00 && code <= 0x0b7f) ||
      (code >= 0x0b80 && code <= 0x0bff) ||
      (code >= 0x0c00 && code <= 0x0c7f) ||
      (code >= 0x0c80 && code <= 0x0cff) ||
      (code >= 0x0d00 && code <= 0x0d7f)
    );
  }

  function describeScriptLanguage(script) {
    if (script === HANGUL_SCRIPT) {
      return "ko";
    }
    if (script === CJK_SCRIPT) {
      return "cjk";
    }
    if (script === EUROPEAN_SCRIPT) {
      return "european";
    }
    if (script === ARABIC_SCRIPT) {
      return "arabic";
    }
    if (script === HEBREW_SCRIPT) {
      return "hebrew";
    }
    if (script === THAI_SCRIPT) {
      return "thai";
    }
    if (script === INDIC_SCRIPT) {
      return "indic";
    }
    return "latin";
  }

  function summarizeTargets(runs, key, suffix) {
    const values = [];
    const seen = {};
    for (let index = 0; index < runs.length; index += 1) {
      const value = runs[index] ? runs[index][key] : null;
      const seenKey = String(value);
      if (seen[seenKey]) {
        continue;
      }
      seen[seenKey] = true;
      values.push(value + suffix);
    }
    return values.join(", ");
  }

  function describeLineHeight(lineHeight) {
    if (lineHeight && typeof lineHeight === "object") {
      const unit = lineHeight.unit || "";
      const value = Number(lineHeight.value);
      if (unit === "AUTO") {
        return "auto";
      }
      if (Number.isFinite(value)) {
        return value + (unit === "PERCENT" ? "%" : "px");
      }
    }
    return "mixed";
  }

  function buildResultToast(result) {
    const summary = result && result.summary ? result.summary : {};
    const adjustedCount = Number(summary.adjustedCount) || 0;
    const skippedCount = Number(summary.skippedCount) || 0;
    if (skippedCount > 0) {
      return "\uD14D\uC2A4\uD2B8 \uD589\uAC04 " + adjustedCount + "\uAC1C \uC870\uC815, " + skippedCount + "\uAC1C \uAC74\uB108\uB700";
    }
    return "\uD14D\uC2A4\uD2B8 \uD589\uAC04 " + adjustedCount + "\uAC1C\uB97C \uC870\uC815\uD588\uC2B5\uB2C8\uB2E4.";
  }

  async function ensureSelectionAccess() {
    if (typeof figma.loadAllPagesAsync !== "function") {
      return;
    }
    try {
      await figma.loadAllPagesAsync();
    } catch (error) {
      console.warn("[pigma] text line height selection preload failed:", error);
    }
  }

  function safeNodeId(node) {
    return node && typeof node.id === "string" ? node.id : "";
  }

  function safeName(node) {
    return node && typeof node.name === "string" && node.name.trim() ? node.name.trim() : "\uC774\uB984 \uC5C6\uC74C";
  }

  function safeNodeType(node) {
    return node && typeof node.type === "string" ? node.type : "UNKNOWN";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_UNLOCK_LOCKED_LAYERS_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 24;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isUnlockLockedLayersMessage(message)) {
      if (isRunning) {
        postStatus("running", "Unlock Locked Layers is already running.");
        return;
      }

      await runUnlockLockedLayers();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_UNLOCK_LOCKED_LAYERS_PATCH__ = true;

  function isUnlockLockedLayersMessage(message) {
    return !!message && message.type === "run-unlock-locked-layers";
  }

  async function runUnlockLockedLayers() {
    isRunning = true;
    postStatus("running", "Finding locked layers in the current selection.");

    try {
      const result = unlockLockedLayersInSelection();
      figma.ui.postMessage({
        type: "unlock-locked-layers-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "Could not unlock locked layers.");
      figma.ui.postMessage({
        type: "unlock-locked-layers-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2400 });
    } finally {
      isRunning = false;
    }
  }

  function unlockLockedLayersInSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("Select a frame, group, or layer first.");
    }

    const candidates = collectLockedNodes(selection);
    if (!candidates.length) {
      return buildResult({
        selection,
        unlocked: [],
        skipped: [],
        candidateCount: 0,
      });
    }

    const unlocked = [];
    const skipped = [];

    for (const entry of candidates) {
      const target = entry.node;
      if (!target || target.removed) {
        continue;
      }

      try {
        target.locked = false;
        unlocked.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
        });
      } catch (error) {
        skipped.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
          reason: normalizeErrorMessage(error, "Could not unlock this layer."),
        });
      }
    }

    return buildResult({
      selection,
      unlocked,
      skipped,
      candidateCount: candidates.length,
    });
  }

  function collectLockedNodes(selection) {
    const results = [];
    const stack = [];

    for (let rootIndex = selection.length - 1; rootIndex >= 0; rootIndex -= 1) {
      const root = selection[rootIndex];
      stack.push({
        node: root,
        path: safeName(root),
      });
    }

    while (stack.length > 0) {
      const current = stack.pop();
      const node = current && current.node;
      if (!node || node.removed) {
        continue;
      }

      if ("locked" in node && node.locked === true) {
        results.push({
          node,
          nodeId: node.id,
          nodeName: safeName(node),
          nodeType: String(node.type || "UNKNOWN"),
          path: current.path,
        });
      }

      if (!hasChildren(node)) {
        continue;
      }

      for (let index = node.children.length - 1; index >= 0; index -= 1) {
        const child = node.children[index];
        stack.push({
          node: child,
          path: current.path + " / " + safeName(child),
        });
      }
    }

    return results;
  }

  function buildResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const unlocked = Array.isArray(options.unlocked) ? options.unlocked : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];
    const candidateCount =
      typeof options.candidateCount === "number" && Number.isFinite(options.candidateCount) ? options.candidateCount : 0;

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        rootCount: selection.length,
        candidateCount,
        unlockedCount: unlocked.length,
        skippedCount: skipped.length,
      },
      unlocked: unlocked.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const unlockedCount = summary.unlockedCount || 0;
    const skippedCount = summary.skippedCount || 0;

    if (unlockedCount === 0) {
      figma.notify("There are no locked layers to unlock.", { timeout: 1800 });
      return;
    }

    const baseMessage = "Unlocked locked layers (" + unlockedCount + ")";
    const message = skippedCount > 0 ? baseMessage + ", skipped " + skippedCount : baseMessage;
    figma.notify(message, { timeout: 2200 });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "unlock-locked-layers-status",
      status,
      message,
    });
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children) && node.children.length > 0;
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "No selection";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return safeName(selection[0]) + " +" + (selection.length - 1);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_DETACH_LINKED_COMPONENTS_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 24;
  const MAX_DETACH_PASSES = 50;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isDetachLinkedComponentsMessage(message)) {
      if (isRunning) {
        postStatus("running", "\uC5F0\uACB0\uB41C \uCEF4\uD3EC\uB10C\uD2B8 \uD574\uC81C\uAC00 \uC774\uBBF8 \uC9C4\uD589 \uC911\uC785\uB2C8\uB2E4.");
        return;
      }

      await runDetachLinkedComponents();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_DETACH_LINKED_COMPONENTS_PATCH__ = true;

  function isDetachLinkedComponentsMessage(message) {
    return !!message && message.type === "run-detach-linked-components";
  }

  async function runDetachLinkedComponents() {
    isRunning = true;
    postStatus("running", "\uC120\uD0DD \uBC94\uC704\uC758 \uC5F0\uACB0\uB41C \uCEF4\uD3EC\uB10C\uD2B8\uB97C \uCC3E\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = detachLinkedComponentsInSelection();
      figma.ui.postMessage({
        type: "detach-linked-components-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "\uC5F0\uACB0\uB41C \uCEF4\uD3EC\uB10C\uD2B8\uB97C \uD574\uC81C\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
      figma.ui.postMessage({
        type: "detach-linked-components-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2600 });
    } finally {
      isRunning = false;
    }
  }

  function detachLinkedComponentsInSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("\uD504\uB808\uC784, \uADF8\uB8F9, \uB808\uC774\uC5B4\uB97C \uBA3C\uC800 \uC120\uD0DD\uD558\uC138\uC694.");
    }

    let roots = selection.slice();
    const processed = [];
    const skipped = [];
    const skippedNodeIds = {};
    let candidateCount = 0;
    let passCount = 0;

    for (let pass = 0; pass < MAX_DETACH_PASSES; pass += 1) {
      const candidates = collectTopLevelLinkedComponentCandidates(roots, skippedNodeIds);
      if (!candidates.length) {
        break;
      }

      let changedCount = 0;
      candidateCount += candidates.length;
      passCount += 1;

      for (let index = 0; index < candidates.length; index += 1) {
        const entry = candidates[index];
        const target = entry && entry.node;
        if (!target || target.removed) {
          continue;
        }

        try {
          const replacement =
            entry.action === "detach-instance" ? detachInstanceNode(target) : convertComponentNodeToFrame(target);
          if (replacement && replacement !== target) {
            roots = replaceRootReference(roots, target, replacement);
          }
          processed.push(buildProcessedEntry(entry, replacement));
          changedCount += 1;
        } catch (error) {
          skippedNodeIds[entry.nodeId] = true;
          skipped.push({
            nodeId: entry.nodeId,
            nodeName: entry.nodeName,
            nodeType: entry.nodeType,
            action: entry.action,
            path: entry.path,
            reason: normalizeErrorMessage(error, "\uC774 \uCEF4\uD3EC\uB10C\uD2B8\uB97C \uD574\uC81C\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4."),
          });
        }
      }

      if (changedCount === 0) {
        break;
      }
    }

    const remaining = collectTopLevelLinkedComponentCandidates(roots, skippedNodeIds);
    const nextSelection = roots.filter((node) => node && !node.removed && isSelectableSceneNode(node));
    if (nextSelection.length > 0) {
      figma.currentPage.selection = nextSelection;
    }

    return buildResult({
      selection: nextSelection.length > 0 ? nextSelection : selection,
      processed,
      skipped,
      candidateCount,
      passCount,
      remainingCount: remaining.length,
    });
  }

  function collectTopLevelLinkedComponentCandidates(roots, skippedNodeIds) {
    const results = [];
    const stack = [];

    for (let rootIndex = roots.length - 1; rootIndex >= 0; rootIndex -= 1) {
      const root = roots[rootIndex];
      if (!root || root.removed) {
        continue;
      }
      stack.push({
        node: root,
        path: safeName(root),
        depth: 0,
      });
    }

    while (stack.length > 0) {
      const current = stack.pop();
      const node = current && current.node;
      if (!node || node.removed) {
        continue;
      }

      const action = getLinkedComponentAction(node);
      if (action && !skippedNodeIds[node.id]) {
        results.push({
          node,
          nodeId: node.id,
          nodeName: safeName(node),
          nodeType: String(node.type || "UNKNOWN"),
          action,
          path: current.path,
          depth: current.depth,
        });
        continue;
      }

      if (!hasChildren(node)) {
        continue;
      }

      for (let index = node.children.length - 1; index >= 0; index -= 1) {
        const child = node.children[index];
        stack.push({
          node: child,
          path: current.path + " / " + safeName(child),
          depth: current.depth + 1,
        });
      }
    }

    return results;
  }

  function getLinkedComponentAction(node) {
    const type = String((node && node.type) || "");
    if (type === "INSTANCE" && typeof node.detachInstance === "function") {
      return "detach-instance";
    }
    if (type === "COMPONENT" || type === "COMPONENT_SET") {
      return "convert-component";
    }
    return "";
  }

  function detachInstanceNode(node) {
    const wasLocked = readLocked(node);
    const unlocked = unlockNodeAndAncestors(node);
    let replacement = null;

    try {
      replacement = node.detachInstance();
      if (replacement && typeof replacement === "object") {
        setLocked(replacement, wasLocked);
      }
      return replacement || null;
    } finally {
      restoreUnlockedNodes(unlocked, replacement);
    }
  }

  function convertComponentNodeToFrame(node) {
    const parent = node.parent;
    if (!parent || typeof parent.insertChild !== "function" || !hasChildren(parent)) {
      throw new Error("\uCEF4\uD3EC\uB10C\uD2B8\uB97C \uBC14\uAFC0 \uC704\uCE58\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const index = getChildIndex(parent, node);
    if (index < 0) {
      throw new Error("\uCEF4\uD3EC\uB10C\uD2B8 \uC21C\uC11C\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const wasLocked = readLocked(node);
    const unlocked = unlockNodeAndAncestors(node);
    const frame = figma.createFrame();
    let movedChildCount = 0;

    try {
      frame.name = safeName(node);
      clearDefaultFrameAppearance(frame);
      resizeLike(node, frame);
      parent.insertChild(index, frame);
      copyFrameLikeProperties(node, frame);
      movedChildCount = moveChildren(node, frame);
      setLocked(frame, wasLocked);
      node.remove();
      return frame;
    } catch (error) {
      if (movedChildCount === 0 && frame && !frame.removed) {
        try {
          frame.remove();
        } catch (removeError) {}
      }
      throw error;
    } finally {
      restoreUnlockedNodes(unlocked, frame);
    }
  }

  function clearDefaultFrameAppearance(frame) {
    trySet(frame, "fills", []);
    trySet(frame, "strokes", []);
    trySet(frame, "effects", []);
  }

  function copyFrameLikeProperties(source, target) {
    const complexProperties = [
      "relativeTransform",
      "fills",
      "strokes",
      "effects",
      "exportSettings",
      "dashPattern",
      "constraints",
      "layoutGrids",
    ];
    const simpleProperties = [
      "x",
      "y",
      "rotation",
      "visible",
      "opacity",
      "blendMode",
      "isMask",
      "strokeWeight",
      "strokeAlign",
      "strokeCap",
      "strokeJoin",
      "strokeMiterLimit",
      "cornerRadius",
      "cornerSmoothing",
      "topLeftRadius",
      "topRightRadius",
      "bottomLeftRadius",
      "bottomRightRadius",
      "clipsContent",
      "layoutMode",
      "primaryAxisSizingMode",
      "counterAxisSizingMode",
      "primaryAxisAlignItems",
      "counterAxisAlignItems",
      "paddingLeft",
      "paddingRight",
      "paddingTop",
      "paddingBottom",
      "itemSpacing",
      "counterAxisSpacing",
      "layoutWrap",
      "strokesIncludedInLayout",
      "layoutAlign",
      "layoutGrow",
      "layoutPositioning",
      "minWidth",
      "maxWidth",
      "minHeight",
      "maxHeight",
      "itemReverseZIndex",
      "gridStyleId",
    ];

    for (let index = 0; index < simpleProperties.length; index += 1) {
      copyProperty(source, target, simpleProperties[index], false);
    }

    for (let index = 0; index < complexProperties.length; index += 1) {
      copyProperty(source, target, complexProperties[index], true);
    }
  }

  function copyProperty(source, target, property, cloneValue) {
    if (!source || !target || !(property in source) || !(property in target)) {
      return;
    }

    try {
      const value = source[property];
      target[property] = cloneValue ? clonePlainValue(value) : value;
    } catch (error) {}
  }

  function clonePlainValue(value) {
    if (!value || typeof value !== "object") {
      return value;
    }

    if (Array.isArray(value)) {
      return value.map((item) => clonePlainValue(item));
    }

    const clone = {};
    const keys = Object.keys(value);
    for (let index = 0; index < keys.length; index += 1) {
      const key = keys[index];
      clone[key] = clonePlainValue(value[key]);
    }
    return clone;
  }

  function resizeLike(source, target) {
    const width = typeof source.width === "number" && Number.isFinite(source.width) ? Math.max(0.01, source.width) : 1;
    const height = typeof source.height === "number" && Number.isFinite(source.height) ? Math.max(0.01, source.height) : 1;

    try {
      if (typeof target.resizeWithoutConstraints === "function") {
        target.resizeWithoutConstraints(width, height);
      } else if (typeof target.resize === "function") {
        target.resize(width, height);
      }
    } catch (error) {}
  }

  function moveChildren(source, target) {
    let moved = 0;
    while (hasChildren(source) && source.children.length > 0) {
      target.appendChild(source.children[0]);
      moved += 1;
    }
    return moved;
  }

  function replaceRootReference(roots, original, replacement) {
    return roots.map((root) => {
      if (root && original && root.id === original.id) {
        return replacement;
      }
      return root;
    });
  }

  function buildProcessedEntry(entry, replacement) {
    return {
      nodeId: entry.nodeId,
      nodeName: entry.nodeName,
      nodeType: entry.nodeType,
      action: entry.action,
      path: entry.path,
      replacementId: replacement && typeof replacement.id === "string" ? replacement.id : "",
      replacementType: replacement && typeof replacement.type === "string" ? replacement.type : "",
    };
  }

  function buildResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const processed = Array.isArray(options.processed) ? options.processed : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];
    const candidateCount =
      typeof options.candidateCount === "number" && Number.isFinite(options.candidateCount) ? options.candidateCount : 0;
    const passCount = typeof options.passCount === "number" && Number.isFinite(options.passCount) ? options.passCount : 0;
    const remainingCount =
      typeof options.remainingCount === "number" && Number.isFinite(options.remainingCount) ? options.remainingCount : 0;
    const detachedInstanceCount = countProcessedAction(processed, "detach-instance");
    const convertedComponentCount = processed.filter((entry) => entry.nodeType === "COMPONENT").length;
    const convertedComponentSetCount = processed.filter((entry) => entry.nodeType === "COMPONENT_SET").length;

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        rootCount: selection.length,
        candidateCount,
        detachedInstanceCount,
        convertedComponentCount,
        convertedComponentSetCount,
        processedCount: processed.length,
        skippedCount: skipped.length,
        passCount,
        remainingCount,
      },
      processed: processed.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function countProcessedAction(processed, action) {
    let count = 0;
    for (let index = 0; index < processed.length; index += 1) {
      if (processed[index] && processed[index].action === action) {
        count += 1;
      }
    }
    return count;
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const processedCount = summary.processedCount || 0;
    const skippedCount = summary.skippedCount || 0;
    const remainingCount = summary.remainingCount || 0;

    if (processedCount === 0) {
      figma.notify("\uD574\uC81C\uD560 \uC5F0\uACB0\uB41C \uCEF4\uD3EC\uB10C\uD2B8\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4.", { timeout: 1800 });
      return;
    }

    let message = "\uC5F0\uACB0\uB41C \uCEF4\uD3EC\uB10C\uD2B8 \uD574\uC81C \uC644\uB8CC (" + processedCount + "\uAC1C)";
    if (skippedCount > 0) {
      message += ", " + skippedCount + "\uAC1C \uAC74\uB108\uB700";
    }
    if (remainingCount > 0) {
      message += ", " + remainingCount + "\uAC1C \uB0A8\uC74C";
    }
    figma.notify(message, { timeout: 2400 });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "detach-linked-components-status",
      status,
      message,
    });
  }

  function unlockNodeAndAncestors(node) {
    const chain = [];
    let current = node;
    while (current && current.type !== "DOCUMENT") {
      chain.push(current);
      current = current.parent;
    }

    const unlocked = [];
    for (let index = chain.length - 1; index >= 0; index -= 1) {
      const candidate = chain[index];
      if (!candidate || !("locked" in candidate) || candidate.locked !== true) {
        continue;
      }

      try {
        candidate.locked = false;
        unlocked.push(candidate);
      } catch (error) {}
    }
    return unlocked;
  }

  function restoreUnlockedNodes(nodes, replacement) {
    if (!Array.isArray(nodes) || !nodes.length) {
      return;
    }

    const replacementId = replacement && typeof replacement.id === "string" ? replacement.id : "";
    for (let index = 0; index < nodes.length; index += 1) {
      const node = nodes[index];
      if (!node || node.removed) {
        continue;
      }
      if (replacementId && node.id === replacementId) {
        continue;
      }
      setLocked(node, true);
    }
  }

  function readLocked(node) {
    return !!(node && "locked" in node && node.locked === true);
  }

  function setLocked(node, locked) {
    if (!node || !("locked" in node) || node.removed) {
      return;
    }

    try {
      node.locked = locked === true;
    } catch (error) {}
  }

  function trySet(node, property, value) {
    if (!node || !(property in node)) {
      return;
    }

    try {
      node[property] = value;
    } catch (error) {}
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children);
  }

  function getChildIndex(parent, child) {
    if (!hasChildren(parent) || !child) {
      return -1;
    }

    for (let index = 0; index < parent.children.length; index += 1) {
      if (parent.children[index] && parent.children[index].id === child.id) {
        return index;
      }
    }
    return -1;
  }

  function isSelectableSceneNode(node) {
    return !!node && typeof node.id === "string" && node.type !== "PAGE" && node.type !== "DOCUMENT";
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "\uC120\uD0DD \uC5C6\uC74C";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return safeName(selection[0]) + " +" + (selection.length - 1);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_DELETE_HIDDEN_LAYERS_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 24;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isDeleteHiddenLayersMessage(message)) {
      if (isRunning) {
        postStatus("running", "\uC228\uACA8\uC9C4 \uB808\uC774\uC5B4 \uC0AD\uC81C\uAC00 \uC774\uBBF8 \uC9C4\uD589 \uC911\uC785\uB2C8\uB2E4.");
        return;
      }

      if (message.type === "request-delete-hidden-layers-preview") {
        await runDeleteHiddenLayersPreview();
      } else {
        await runDeleteHiddenLayers();
      }
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_DELETE_HIDDEN_LAYERS_PATCH__ = true;

  function isDeleteHiddenLayersMessage(message) {
    return (
      !!message &&
      (message.type === "request-delete-hidden-layers-preview" || message.type === "run-delete-hidden-layers")
    );
  }

  async function runDeleteHiddenLayersPreview() {
    isRunning = true;
    postStatus("running", "\uC0AD\uC81C\uD560 \uC228\uACA8\uC9C4 \uB808\uC774\uC5B4\uB97C \uD655\uC778\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = previewHiddenLayersInSelection();
      figma.ui.postMessage({
        type: "delete-hidden-layers-preview",
        result,
      });
    } catch (error) {
      const message = normalizeErrorMessage(
        error,
        "\uC228\uACA8\uC9C4 \uB808\uC774\uC5B4 \uD655\uC778\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4."
      );
      figma.ui.postMessage({
        type: "delete-hidden-layers-preview-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2200 });
    } finally {
      isRunning = false;
    }
  }

  async function runDeleteHiddenLayers() {
    isRunning = true;
    postStatus("running", "현재 선택 안의 숨겨진 레이어를 찾고 있습니다.");

    try {
      const result = deleteHiddenLayersInSelection();
      figma.ui.postMessage({
        type: "delete-hidden-layers-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "숨겨진 레이어 삭제에 실패했습니다.");
      figma.ui.postMessage({
        type: "delete-hidden-layers-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2400 });
    } finally {
      isRunning = false;
    }
  }

  function deleteHiddenLayersInSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("프레임, 그룹, 레이어를 먼저 선택하세요.");
    }

    const candidates = collectHiddenDescendants(selection);
    if (!candidates.length) {
      return buildResult({
        selection,
        deleted: [],
        skipped: [],
        candidateCount: 0,
        removedNodeCount: 0,
      });
    }

    const deleted = [];
    const skipped = [];
    let removedNodeCount = 0;

    for (const entry of candidates) {
      const target = entry.node;
      if (!target || target.removed) {
        continue;
      }

      try {
        target.remove();
        removedNodeCount += entry.subtreeSize;
        deleted.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
          removedLayerCount: entry.subtreeSize,
        });
      } catch (error) {
        skipped.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
          reason: normalizeErrorMessage(error, "해당 레이어를 삭제할 수 없습니다."),
        });
      }
    }

    return buildResult({
      selection,
      deleted,
      skipped,
      candidateCount: candidates.length,
      removedNodeCount,
    });
  }

  function previewHiddenLayersInSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("\uD504\uB808\uC784, \uADF8\uB8F9, \uB808\uC774\uC5B4\uB97C \uBA3C\uC800 \uC120\uD0DD\uD558\uC138\uC694.");
    }

    const candidates = collectHiddenDescendants(selection);

    return buildResult({
      selection,
      deleted: [],
      skipped: [],
      candidates,
      candidateCount: candidates.length,
      removedNodeCount: sumCandidateSubtreeSize(candidates),
    });
  }

  function collectHiddenDescendants(selection) {
    const results = [];
    const stack = [];

    for (let rootIndex = selection.length - 1; rootIndex >= 0; rootIndex -= 1) {
      const root = selection[rootIndex];
      if (!hasChildren(root)) {
        continue;
      }

      for (let childIndex = root.children.length - 1; childIndex >= 0; childIndex -= 1) {
        const child = root.children[childIndex];
        stack.push({
          node: child,
          path: `${safeName(root)} / ${safeName(child)}`,
          hiddenByAncestor: root.visible === false,
        });
      }
    }

    while (stack.length > 0) {
      const current = stack.pop();
      const node = current && current.node;
      if (!node || node.removed) {
        continue;
      }

      if (current.hiddenByAncestor || !node.visible) {
        results.push({
          node,
          nodeId: node.id,
          nodeName: safeName(node),
          nodeType: String(node.type || "UNKNOWN"),
          path: current.path,
          subtreeSize: countSubtreeNodes(node),
        });
        continue;
      }

      if (!hasChildren(node)) {
        continue;
      }

      for (let index = node.children.length - 1; index >= 0; index -= 1) {
        const child = node.children[index];
        stack.push({
          node: child,
          path: `${current.path} / ${safeName(child)}`,
          hiddenByAncestor: false,
        });
      }
    }

    return results;
  }

  function buildResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const deleted = Array.isArray(options.deleted) ? options.deleted : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];
    const candidates = Array.isArray(options.candidates) ? options.candidates : [];
    const candidateCount =
      typeof options.candidateCount === "number" && Number.isFinite(options.candidateCount) ? options.candidateCount : 0;
    const removedNodeCount =
      typeof options.removedNodeCount === "number" && Number.isFinite(options.removedNodeCount)
        ? options.removedNodeCount
        : 0;

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        rootCount: selection.length,
        candidateCount,
        deletedCount: deleted.length,
        skippedCount: skipped.length,
        removedNodeCount,
      },
      candidates: candidates.slice(0, RESULT_PREVIEW_LIMIT).map((entry) => ({
        nodeId: entry.nodeId,
        nodeName: entry.nodeName,
        nodeType: entry.nodeType,
        path: entry.path,
        removedLayerCount: entry.subtreeSize,
      })),
      deleted: deleted.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const deletedCount = summary.deletedCount || 0;
    const skippedCount = summary.skippedCount || 0;
    const removedNodeCount = summary.removedNodeCount || 0;

    if (deletedCount === 0) {
      figma.notify("삭제할 숨겨진 레이어가 없습니다.", { timeout: 1800 });
      return;
    }

    const baseMessage =
      removedNodeCount > deletedCount
        ? `숨겨진 레이어 정리 완료 (${removedNodeCount}개 레이어 제거)`
        : `숨겨진 레이어 정리 완료 (${deletedCount}개 삭제)`;

    const message = skippedCount > 0 ? `${baseMessage}, ${skippedCount}개 건너뜀` : baseMessage;
    figma.notify(message, { timeout: 2200 });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "delete-hidden-layers-status",
      status,
      message,
    });
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children) && node.children.length > 0;
  }

  function countSubtreeNodes(node) {
    let count = 0;
    const stack = [node];

    while (stack.length > 0) {
      const current = stack.pop();
      if (!current) {
        continue;
      }

      count += 1;
      if (!hasChildren(current)) {
        continue;
      }

      for (let index = current.children.length - 1; index >= 0; index -= 1) {
        stack.push(current.children[index]);
      }
    }

    return count;
  }

  function sumCandidateSubtreeSize(candidates) {
    if (!Array.isArray(candidates) || !candidates.length) {
      return 0;
    }

    return candidates.reduce((total, entry) => {
      const subtreeSize =
        entry && typeof entry.subtreeSize === "number" && Number.isFinite(entry.subtreeSize) ? entry.subtreeSize : 0;
      return total + subtreeSize;
    }, 0);
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "선택 없음";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return `${safeName(selection[0])} 외 ${selection.length - 1}개`;
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_CLEAR_FRAME_GUIDES_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 24;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isClearFrameGuidesMessage(message)) {
      if (isRunning) {
        postStatus("running", "\uD504\uB808\uC784 \uAC00\uC774\uB4DC \uC9C0\uC6B0\uAE30\uAC00 \uC774\uBBF8 \uC9C4\uD589 \uC911\uC785\uB2C8\uB2E4.");
        return;
      }

      await runClearFrameGuides();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_CLEAR_FRAME_GUIDES_PATCH__ = true;

  function isClearFrameGuidesMessage(message) {
    return !!message && message.type === "run-clear-frame-guides";
  }

  async function runClearFrameGuides() {
    isRunning = true;
    postStatus("running", "\uC120\uD0DD\uD55C \uD504\uB808\uC784\uC758 \uAC00\uC774\uB4DC\uB97C \uC9C0\uC6B0\uACE0 \uC788\uC2B5\uB2C8\uB2E4.");

    try {
      const result = clearFrameGuidesInSelection();
      figma.ui.postMessage({
        type: "clear-frame-guides-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "\uD504\uB808\uC784 \uAC00\uC774\uB4DC\uB97C \uC9C0\uC6B0\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
      figma.ui.postMessage({
        type: "clear-frame-guides-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2400 });
    } finally {
      isRunning = false;
    }
  }

  function clearFrameGuidesInSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("\uAC00\uC774\uB4DC\uB97C \uC9C0\uC6B8 \uD504\uB808\uC784\uC744 \uBA3C\uC800 \uC120\uD0DD\uD558\uC138\uC694.");
    }

    const frames = collectSelectedGuideFrames(selection);
    if (!frames.length) {
      throw new Error("\uC120\uD0DD\uD55C \uB300\uC0C1 \uC911 \uAC00\uC774\uB4DC\uB97C \uC9C0\uC6B8 \uD504\uB808\uC784\uC774 \uC5C6\uC2B5\uB2C8\uB2E4.");
    }

    const cleared = [];
    const skipped = [];
    let guideCount = 0;

    for (let index = 0; index < frames.length; index += 1) {
      const entry = frames[index];
      const frame = entry && entry.node;
      if (!frame || frame.removed) {
        continue;
      }

      const frameGuideCount = getFrameGuideCount(frame);
      if (frameGuideCount <= 0) {
        continue;
      }

      try {
        frame.guides = [];
        guideCount += frameGuideCount;
        cleared.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
          guideCount: frameGuideCount,
        });
      } catch (error) {
        skipped.push({
          nodeId: entry.nodeId,
          nodeName: entry.nodeName,
          nodeType: entry.nodeType,
          path: entry.path,
          guideCount: frameGuideCount,
          reason: normalizeErrorMessage(error, "\uC774 \uD504\uB808\uC784\uC758 \uAC00\uC774\uB4DC\uB97C \uC9C0\uC6B0\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4."),
        });
      }
    }

    return buildResult({
      selection,
      frameCount: frames.length,
      cleared,
      skipped,
      guideCount,
    });
  }

  function collectSelectedGuideFrames(selection) {
    const frames = [];

    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      if (!isGuideFrame(node)) {
        continue;
      }

      frames.push({
        node,
        nodeId: node.id,
        nodeName: safeName(node),
        nodeType: String(node.type || "UNKNOWN"),
        path: safeName(node),
      });
    }

    return frames;
  }

  function isGuideFrame(node) {
    if (!node || node.removed || String(node.type || "") !== "FRAME") {
      return false;
    }

    return "guides" in node && Array.isArray(node.guides);
  }

  function getFrameGuideCount(frame) {
    if (!frame || !("guides" in frame) || !Array.isArray(frame.guides)) {
      return 0;
    }

    return frame.guides.length;
  }

  function buildResult(options) {
    const selection = Array.isArray(options.selection) ? options.selection : [];
    const cleared = Array.isArray(options.cleared) ? options.cleared : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];
    const frameCount =
      typeof options.frameCount === "number" && Number.isFinite(options.frameCount) ? options.frameCount : 0;
    const guideCount =
      typeof options.guideCount === "number" && Number.isFinite(options.guideCount) ? options.guideCount : 0;

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        rootCount: selection.length,
        frameCount,
        clearedFrameCount: cleared.length,
        skippedCount: skipped.length,
        guideCount,
      },
      cleared: cleared.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const guideCount = summary.guideCount || 0;
    const skippedCount = summary.skippedCount || 0;

    if (guideCount === 0) {
      figma.notify("\uC120\uD0DD\uD55C \uD504\uB808\uC784\uC5D0 \uC9C0\uC6B8 \uAC00\uC774\uB4DC\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4.", {
        timeout: 1800,
      });
      return;
    }

    const baseMessage = "\uD504\uB808\uC784 \uAC00\uC774\uB4DC " + guideCount + "\uAC1C\uB97C \uC9C0\uC6E0\uC2B5\uB2C8\uB2E4.";
    const message = skippedCount > 0 ? baseMessage + " " + skippedCount + "\uAC1C \uAC74\uB108\uB700." : baseMessage;
    figma.notify(message, { timeout: 2200 });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "clear-frame-guides-status",
      status,
      message,
    });
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "\uC120\uD0DD \uC5C6\uC74C";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return safeName(selection[0]) + " +" + (selection.length - 1);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_SPLIT_LONG_FRAME_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const MAX_SEGMENT_AXIS = 3500;
  const HARD_MAX_SEGMENT_AXIS = 4500;
  const MIN_SECTION_AXIS = 120;
  const MIN_SEGMENT_AXIS = 48;
  const MIN_OUTPUT_SEGMENT_AXIS = 3500;
  const CUT_SNAP_TOLERANCE = 20;
  const SEGMENT_WHITESPACE_CAPTURE_MIN = 8;
  const SEGMENT_WHITESPACE_CAPTURE_MAX = 160;
  const SEGMENT_WHITESPACE_EDGE_TOLERANCE = 4;
  const SECTION_OVERLAP_TOLERANCE = 1;
  const OUTPUT_OFFSET = 96;
  const OUTPUT_GAP = 48;
  const RESULT_PREVIEW_LIMIT = 24;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isSplitLongFrameMessage(message)) {
      if (isRunning) {
        postStatus("running", "\uD504\uB808\uC784 \uB098\uB204\uAE30\uAC00 \uC774\uBBF8 \uC9C4\uD589 \uC911\uC785\uB2C8\uB2E4.");
        return;
      }

      await runSplitLongFrame(message);
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_SPLIT_LONG_FRAME_PATCH__ = true;
  globalScope.__PIGMA_SPLIT_LONG_FRAME_API__ = {
    createFramesForNode: createSplitLongFrames,
  };

  function isSplitLongFrameMessage(message) {
    return !!message && message.type === "run-split-long-frame";
  }

  async function runSplitLongFrame(message) {
    const splitMode = normalizeSplitMode(message && message.splitMode);
    isRunning = true;
    postStatus(
      "running",
      splitMode === "user"
        ? "\uC0AC\uC6A9\uC790\uAC00 \uB9CC\uB4E0 \uADF8\uB8F9 \uAE30\uC900\uC73C\uB85C \uD504\uB808\uC784\uC744 \uB098\uB204\uACE0 \uC788\uC2B5\uB2C8\uB2E4."
        : "\uAE34 \uD504\uB808\uC784\uC744 \uC139\uC158\uBCC4\uB85C \uB098\uB204\uACE0 \uC788\uC2B5\uB2C8\uB2E4."
    );

    try {
      const result = splitSelectedLongFrame({
        splitMode,
      });
      figma.ui.postMessage({
        type: "split-long-frame-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "\uD504\uB808\uC784 \uB098\uB204\uAE30\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4.");
      figma.ui.postMessage({
        type: "split-long-frame-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2600 });
    } finally {
      isRunning = false;
    }
  }

  function splitSelectedLongFrame(options) {
    const selection = Array.from(figma.currentPage.selection || []);
    if (selection.length !== 1) {
      throw new Error("\uB098\uB20C \uD504\uB808\uC784 \uD558\uB098\uB9CC \uC120\uD0DD\uD558\uC138\uC694.");
    }

    const normalizedOptions = options && typeof options === "object" ? options : {};
    const output = createSplitLongFrames(selection[0], {
      selectCreatedFrames: true,
      scrollIntoView: true,
      splitMode: normalizedOptions.splitMode,
    });
    return output.result;
  }

  function createSplitLongFrames(root, options) {
    const normalizedOptions = options && typeof options === "object" ? options : {};
    if (!hasChildren(root) || !hasUsableSize(root)) {
      throw new Error("\uC790\uC2DD \uB808\uC774\uC5B4\uAC00 \uC788\uB294 \uD504\uB808\uC784, \uCEF4\uD3EC\uB10C\uD2B8, \uC139\uC158\uC744 \uC120\uD0DD\uD558\uC138\uC694.");
    }

    const direction = normalizedOptions.direction === "horizontal" || normalizedOptions.direction === "vertical" ? normalizedOptions.direction : inferDirection(root);
    const rootAxis = getMainSize(root, direction);
    const rootCross = getCrossSize(root, direction);
    const maxSegmentAxis = getMaxSegmentAxis(normalizedOptions);
    const outputOffset = getNumberOption(normalizedOptions, "outputOffset", OUTPUT_OFFSET);
    const outputGap = getNumberOption(normalizedOptions, "outputGap", OUTPUT_GAP);
    const splitMode = normalizeSplitMode(normalizedOptions.splitMode);
    const entries = collectDirectChildEntries(root, direction);
    if (!entries.length) {
      throw new Error("\uB098\uB204\uAE30\uC5D0 \uC0AC\uC6A9\uD560 \uC790\uC2DD \uB808\uC774\uC5B4\uB97C \uCC3E\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.");
    }

    const boundaryEntries = collectNestedAnchorEntries(root, direction);
    let anchors = [];
    let segments = [];
    let userGroupCount = 0;

    if (splitMode === "user") {
      anchors = buildUserGroupAnchors(entries, rootAxis, rootCross);
      userGroupCount = anchors.length;
      if (anchors.length < 2) {
        throw new Error("\uC0AC\uC6A9\uC790 \uB098\uB204\uAE30\uB294 \uC120\uD0DD\uD55C \uD504\uB808\uC784 \uC548\uC5D0 \uC9C1\uC811 \uB9CC\uB4E0 \uADF8\uB8F9, \uD504\uB808\uC784, \uC139\uC158\uC774 2\uAC1C \uC774\uC0C1 \uC788\uC5B4\uC57C \uD569\uB2C8\uB2E4.");
      }
      segments = buildUserGroupSegments(anchors, rootAxis);
      segments = expandLargeSegments(segments, maxSegmentAxis, boundaryEntries);
    } else {
      anchors = buildSectionAnchors(entries, rootAxis, rootCross);
      if (anchors.length < 2) {
        anchors = buildSectionAnchors(boundaryEntries, rootAxis, rootCross);
      }
      segments =
        anchors.length >= 2 ? buildSectionSegments(anchors, rootAxis) : buildChunkSegments(rootAxis, maxSegmentAxis, boundaryEntries);
      if (anchors.length >= 2 && segmentsHaveUnsafeCuts(segments, boundaryEntries)) {
        segments = buildChunkSegments(rootAxis, maxSegmentAxis, boundaryEntries);
      }
      segments = expandLargeSegments(segments, maxSegmentAxis, boundaryEntries);
      segments = mergeSmallSegments(segments, maxSegmentAxis);
      segments = includeAdjacentWhitespaceAroundSegments(segments, boundaryEntries, rootAxis, maxSegmentAxis);
    }

    if (segments.length < 2) {
      throw new Error(
        splitMode === "user"
          ? "\uADF8\uB8F9 \uAE30\uC900\uC73C\uB85C \uB098\uB20C \uC139\uC158\uC774 \uBD80\uC871\uD569\uB2C8\uB2E4. \uD504\uB808\uC784 \uC548\uC758 \uC9C1\uACC4 \uADF8\uB8F9\uC744 2\uAC1C \uC774\uC0C1 \uB9CC\uB4E4\uC5B4 \uC8FC\uC138\uC694."
          : "\uB098\uB20C \uC139\uC158\uC774 \uBD80\uC871\uD558\uAC70\uB098 \uC774\uBBF8 PSD\uC6A9\uC73C\uB85C \uCDA9\uBD84\uD788 \uC9E7\uC2B5\uB2C8\uB2E4."
      );
    }

    const rootPageBox = getAbsoluteBox(root);
    const rootPageX = rootPageBox ? rootPageBox.x : getNumberProperty(root, "x", 0);
    const rootPageY = rootPageBox ? rootPageBox.y : getNumberProperty(root, "y", 0);
    const createdFrames = [];
    const skipped = [];
    const warnings = [];
    let cursor = 0;

    try {
      for (let index = 0; index < segments.length; index += 1) {
        const segment = segments[index];
        const frame = figma.createFrame();
        frame.name = buildFrameName(root, segment, index, segments.length);
        setFrameSize(frame, direction === "vertical" ? root.width : segment.length, direction === "vertical" ? segment.length : root.height);
        copyFrameAppearance(root, frame);
        trySet(frame, "clipsContent", true);

        if (direction === "vertical") {
          frame.x = rootPageX + root.width + outputOffset;
          frame.y = rootPageY + cursor;
        } else {
          frame.x = rootPageX + cursor;
          frame.y = rootPageY + root.height + outputOffset;
        }

        figma.currentPage.appendChild(frame);

        const cloneResult = cloneSegmentChildren(root, frame, entries, segment, direction);
        appendAll(skipped, cloneResult.skipped);
        appendAll(warnings, cloneResult.warnings);

        createdFrames.push({
          node: frame,
          name: frame.name,
          width: roundNumber(frame.width),
          height: roundNumber(frame.height),
          clonedLayerCount: cloneResult.clonedLayerCount,
        });

        cursor += segment.length + outputGap;
      }
    } catch (error) {
      cleanupCreatedFrames(createdFrames);
      throw error;
    }

    const selectionTargets = [];
    for (let index = 0; index < createdFrames.length; index += 1) {
      selectionTargets.push(createdFrames[index].node);
    }
    if (normalizedOptions.selectCreatedFrames === true) {
      figma.currentPage.selection = selectionTargets;
    }
    if (normalizedOptions.scrollIntoView === true && selectionTargets.length) {
      figma.viewport.scrollAndZoomIntoView(selectionTargets);
    }

    return {
      frames: selectionTargets,
      result: buildResult(root, direction, segments, createdFrames, skipped, warnings, anchors.length, maxSegmentAxis, splitMode, userGroupCount),
    };
  }

  function collectDirectChildEntries(root, direction) {
    const entries = [];
    for (let index = 0; index < root.children.length; index += 1) {
      const child = root.children[index];
      if (!child || child.removed) {
        continue;
      }

      const bounds = getLocalBounds(root, child);
      if (!bounds || bounds.width <= 0 || bounds.height <= 0) {
        continue;
      }

      const mainStart = direction === "vertical" ? bounds.y : bounds.x;
      const mainLength = direction === "vertical" ? bounds.height : bounds.width;
      const crossStart = direction === "vertical" ? bounds.x : bounds.y;
      const crossLength = direction === "vertical" ? bounds.width : bounds.height;

      entries.push({
        node: child,
        order: index,
        name: safeName(child),
        localX: getNumberProperty(child, "x", bounds.x),
        localY: getNumberProperty(child, "y", bounds.y),
        width: getNumberProperty(child, "width", bounds.width),
        height: getNumberProperty(child, "height", bounds.height),
        mainStart,
        mainEnd: mainStart + mainLength,
        mainLength,
        crossStart,
        crossEnd: crossStart + crossLength,
        crossLength,
      });
    }

    entries.sort((a, b) => a.order - b.order);
    return entries;
  }

  function collectNestedAnchorEntries(root, direction) {
    const entries = [];
    const stack = [];
    let order = 0;

    for (let index = root.children.length - 1; index >= 0; index -= 1) {
      stack.push({
        node: root.children[index],
        depth: 1,
      });
    }

    while (stack.length > 0) {
      const current = stack.pop();
      const node = current && current.node;
      if (!node || node.removed) {
        continue;
      }

      const bounds = getLocalBounds(root, node);
      if (bounds && bounds.width > 0 && bounds.height > 0) {
        const mainStart = direction === "vertical" ? bounds.y : bounds.x;
        const mainLength = direction === "vertical" ? bounds.height : bounds.width;
        const crossStart = direction === "vertical" ? bounds.x : bounds.y;
        const crossLength = direction === "vertical" ? bounds.width : bounds.height;
        entries.push({
          node,
          order,
          name: safeName(node),
          localX: bounds.x,
          localY: bounds.y,
          width: bounds.width,
          height: bounds.height,
          mainStart,
          mainEnd: mainStart + mainLength,
          mainLength,
          crossStart,
          crossEnd: crossStart + crossLength,
          crossLength,
        });
        order += 1;
      }

      if (current.depth >= 3 || !hasChildren(node)) {
        continue;
      }

      for (let index = node.children.length - 1; index >= 0; index -= 1) {
        stack.push({
          node: node.children[index],
          depth: current.depth + 1,
        });
      }
    }

    entries.sort((a, b) => a.mainStart - b.mainStart || a.order - b.order);
    return entries;
  }

  function buildSectionAnchors(entries, rootAxis, rootCross) {
    const candidates = [];
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!isAnchorCandidate(entry, rootAxis, rootCross)) {
        continue;
      }

      candidates.push({
        start: clamp(entry.mainStart, 0, rootAxis),
        end: clamp(entry.mainEnd, 0, rootAxis),
        label: entry.name,
        nodeId: entry.node.id,
      });
    }

    candidates.sort((a, b) => a.start - b.start || a.end - b.end);

    const anchors = [];
    for (let index = 0; index < candidates.length; index += 1) {
      const candidate = candidates[index];
      if (candidate.end - candidate.start < MIN_SECTION_AXIS) {
        continue;
      }

      const previous = anchors.length > 0 ? anchors[anchors.length - 1] : null;
      if (previous && shouldMergeSectionAnchor(previous, candidate)) {
        previous.end = Math.max(previous.end, candidate.end);
        if (!previous.label && candidate.label) {
          previous.label = candidate.label;
        }
        continue;
      }

      anchors.push(candidate);
    }

    return anchors;
  }

  function shouldMergeSectionAnchor(previous, candidate) {
    if (!previous || !candidate) {
      return false;
    }

    const overlap = previous.end - candidate.start;
    if (overlap <= SECTION_OVERLAP_TOLERANCE) {
      return false;
    }

    const previousLength = Math.max(1, previous.end - previous.start);
    const candidateLength = Math.max(1, candidate.end - candidate.start);
    const smallerLength = Math.min(previousLength, candidateLength);
    if (overlap >= smallerLength * 0.65) {
      return true;
    }

    return candidate.end <= previous.end + SECTION_OVERLAP_TOLERANCE;
  }

  function isAnchorCandidate(entry, rootAxis, rootCross) {
    if (!entry || entry.mainLength < MIN_SECTION_AXIS) {
      return false;
    }

    if (entry.node.visible === false) {
      return false;
    }

    if (entry.mainLength >= rootAxis * 0.85) {
      return false;
    }

    if (entry.crossLength < Math.max(32, rootCross * 0.08)) {
      return false;
    }

    return true;
  }

  function buildSectionSegments(anchors, rootAxis) {
    const segments = [];
    for (let index = 0; index < anchors.length; index += 1) {
      const anchor = anchors[index];
      const next = index < anchors.length - 1 ? anchors[index + 1] : null;
      const start = index === 0 ? 0 : anchor.start;
      const end = next ? next.start : rootAxis;
      appendSegment(segments, {
        start,
        end,
        label: anchor.label,
        mode: "section",
      }, rootAxis);
    }

    return segments;
  }

  function buildUserGroupAnchors(entries, rootAxis, rootCross) {
    const anchors = [];
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!isUserGroupAnchorCandidate(entry, rootAxis, rootCross)) {
        continue;
      }

      anchors.push({
        start: clamp(entry.mainStart, 0, rootAxis),
        end: clamp(entry.mainEnd, 0, rootAxis),
        label: entry.name,
        nodeId: entry.node.id,
      });
    }

    anchors.sort((a, b) => a.start - b.start || a.end - b.end);
    return removeDuplicateUserGroupAnchors(anchors);
  }

  function isUserGroupAnchorCandidate(entry, rootAxis, rootCross) {
    if (!entry || !entry.node || entry.node.visible === false) {
      return false;
    }

    const type = String(entry.node.type || "");
    if (type !== "GROUP" && type !== "FRAME" && type !== "SECTION") {
      return false;
    }

    if (entry.mainLength < MIN_SEGMENT_AXIS) {
      return false;
    }

    if (entry.mainLength >= rootAxis - 2) {
      return false;
    }

    return entry.crossLength >= Math.max(24, rootCross * 0.05);
  }

  function removeDuplicateUserGroupAnchors(anchors) {
    const unique = [];
    for (let index = 0; index < anchors.length; index += 1) {
      const anchor = anchors[index];
      const previous = unique.length > 0 ? unique[unique.length - 1] : null;
      if (previous && Math.abs(previous.start - anchor.start) <= CUT_SNAP_TOLERANCE) {
        if (anchor.end - anchor.start > previous.end - previous.start) {
          previous.end = anchor.end;
          previous.label = anchor.label || previous.label;
          previous.nodeId = anchor.nodeId || previous.nodeId;
        }
        continue;
      }
      unique.push(anchor);
    }
    return unique;
  }

  function buildUserGroupSegments(anchors, rootAxis) {
    const segments = [];
    for (let index = 0; index < anchors.length; index += 1) {
      const anchor = anchors[index];
      const next = index < anchors.length - 1 ? anchors[index + 1] : null;
      const start = index === 0 ? 0 : anchor.start;
      const end = next ? next.start : rootAxis;
      appendSegment(segments, {
        start,
        end,
        label: anchor.label,
        mode: "user-group",
      }, rootAxis);
    }
    return segments;
  }

  function segmentsHaveUnsafeCuts(segments, entries) {
    for (let index = 0; index < segments.length - 1; index += 1) {
      if (!isSafeCut(segments[index].end, entries)) {
        return true;
      }
    }
    return false;
  }

  function buildChunkSegments(rootAxis, maxSegmentAxis, entries) {
    const segments = [];
    const hardMaxSegmentAxis = getHardMaxSegmentAxis(maxSegmentAxis);
    let start = 0;
    while (start < rootAxis - 0.5) {
      const targetEnd = Math.min(rootAxis, start + maxSegmentAxis);
      const hardEnd = Math.min(rootAxis, start + hardMaxSegmentAxis);
      const end = chooseSegmentEnd(start, targetEnd, hardEnd, rootAxis, entries);
      appendSegment(segments, {
        start,
        end,
        label: "\uBD84\uD560",
        mode: "chunk",
      }, rootAxis);
      start = end;
    }
    return segments;
  }

  function expandLargeSegments(segments, maxSegmentAxis, entries) {
    const expanded = [];
    const hardMaxSegmentAxis = getHardMaxSegmentAxis(maxSegmentAxis);
    for (let index = 0; index < segments.length; index += 1) {
      const segment = segments[index];
      if (segment.length <= hardMaxSegmentAxis) {
        expanded.push(segment);
        continue;
      }

      let partStart = segment.start;
      let partIndex = 1;
      while (partStart < segment.end - 0.5) {
        const targetEnd = Math.min(segment.end, partStart + maxSegmentAxis);
        const hardEnd = Math.min(segment.end, partStart + hardMaxSegmentAxis);
        const partEnd = chooseSegmentEnd(partStart, targetEnd, hardEnd, segment.end, entries);
        appendSegment(expanded, {
          start: partStart,
          end: partEnd,
          label: segment.label + " " + partIndex,
          mode: segment.mode,
        }, segment.end);
        partStart = partEnd;
        partIndex += 1;
      }
    }

    return expanded;
  }

  function mergeSmallSegments(segments, maxSegmentAxis) {
    const minOutputAxis = getMinOutputSegmentAxis(maxSegmentAxis);
    const hardMaxSegmentAxis = getHardMaxSegmentAxis(maxSegmentAxis);
    const merged = segments.slice();
    let changed = true;

    while (changed) {
      changed = false;
      for (let index = 0; index < merged.length; index += 1) {
        const segment = merged[index];
        if (!segment || segment.length >= minOutputAxis || merged.length < 2) {
          continue;
        }

        const previous = index > 0 ? merged[index - 1] : null;
        const next = index < merged.length - 1 ? merged[index + 1] : null;
        const canMergePrevious = canMergeSegments(previous, segment, hardMaxSegmentAxis);
        const canMergeNext = canMergeSegments(segment, next, hardMaxSegmentAxis);
        if (!canMergePrevious && !canMergeNext) {
          continue;
        }

        if (canMergePrevious && (!canMergeNext || index === merged.length - 1 || previous.length <= next.length)) {
          merged.splice(index - 1, 2, mergeAdjacentSegments(previous, segment));
        } else {
          merged.splice(index, 2, mergeAdjacentSegments(segment, next));
        }
        changed = true;
        break;
      }
    }

    return merged;
  }

  function canMergeSegments(first, second, hardMaxSegmentAxis) {
    if (!first || !second) {
      return false;
    }
    return second.end - first.start <= hardMaxSegmentAxis + 0.5;
  }

  function mergeAdjacentSegments(first, second) {
    const start = first.start;
    const end = second.end;
    return {
      start,
      end,
      length: end - start,
      label: first.label || second.label || "\uC139\uC158",
      mode: first.mode === second.mode ? first.mode : "merged",
    };
  }

  function includeAdjacentWhitespaceAroundSegments(segments, entries, rootAxis, maxSegmentAxis) {
    const intervals = getWhitespaceContentIntervals(entries);
    if (!intervals.length) {
      return segments;
    }

    const maxAllowedLength = getHardMaxSegmentAxis(maxSegmentAxis) + SEGMENT_WHITESPACE_CAPTURE_MAX;
    const adjusted = segments.map(segment => includeAdjacentWhitespaceAroundSegment(segment, intervals, rootAxis, maxAllowedLength));
    return removeAdjacentWhitespaceOverlap(adjusted);
  }

  function includeAdjacentWhitespaceAroundSegment(segment, intervals, rootAxis, maxAllowedLength) {
    const contentRange = getSegmentContentRange(segment, intervals);
    if (!contentRange) {
      return segment;
    }

    let nextStart = segment.start;
    let nextEnd = segment.end;

    if (Math.abs(contentRange.start - segment.start) <= SEGMENT_WHITESPACE_EDGE_TOLERANCE) {
      const previousEnd = findPreviousContentEnd(intervals, contentRange.start);
      if (previousEnd !== null) {
        const gap = contentRange.start - previousEnd;
        if (gap >= SEGMENT_WHITESPACE_CAPTURE_MIN && gap <= SEGMENT_WHITESPACE_CAPTURE_MAX) {
          nextStart = previousEnd;
        }
      } else {
        const gap = contentRange.start;
        if (gap >= SEGMENT_WHITESPACE_CAPTURE_MIN && gap <= SEGMENT_WHITESPACE_CAPTURE_MAX) {
          nextStart = 0;
        }
      }
    }

    if (Math.abs(contentRange.end - segment.end) <= SEGMENT_WHITESPACE_EDGE_TOLERANCE) {
      const nextContentStart = findNextContentStart(intervals, contentRange.end);
      if (nextContentStart !== null) {
        const gap = nextContentStart - contentRange.end;
        if (gap >= SEGMENT_WHITESPACE_CAPTURE_MIN && gap <= SEGMENT_WHITESPACE_CAPTURE_MAX) {
          nextEnd = nextContentStart;
        }
      } else {
        const gap = rootAxis - contentRange.end;
        if (gap >= SEGMENT_WHITESPACE_CAPTURE_MIN && gap <= SEGMENT_WHITESPACE_CAPTURE_MAX) {
          nextEnd = rootAxis;
        }
      }
    }

    if (nextEnd - nextStart > maxAllowedLength) {
      return segment;
    }

    return {
      start: nextStart,
      end: nextEnd,
      length: nextEnd - nextStart,
      label: segment.label,
      mode: segment.mode,
    };
  }

  function getSegmentContentRange(segment, intervals) {
    let start = Infinity;
    let end = -Infinity;
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.end <= segment.start + 0.5 || interval.start >= segment.end - 0.5) {
        continue;
      }

      start = Math.min(start, interval.start);
      end = Math.max(end, interval.end);
    }

    if (!isFiniteNumber(start) || !isFiniteNumber(end) || end <= start) {
      return null;
    }

    return { start, end };
  }

  function getWhitespaceContentIntervals(entries) {
    const intervals = [];
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!isWhitespaceContentEntry(entry)) {
        continue;
      }

      intervals.push({
        start: Math.min(entry.mainStart, entry.mainEnd),
        end: Math.max(entry.mainStart, entry.mainEnd),
      });
    }

    intervals.sort((a, b) => a.start - b.start || a.end - b.end);
    return mergeContentIntervals(intervals);
  }

  function isWhitespaceContentEntry(entry) {
    if (!entry || entry.node.visible === false || entry.mainLength <= 0.5) {
      return false;
    }
    if (hasChildren(entry.node) && isStructuralContainerNode(entry.node)) {
      return false;
    }
    return entry.mainLength <= HARD_MAX_SEGMENT_AXIS + SEGMENT_WHITESPACE_CAPTURE_MAX;
  }

  function mergeContentIntervals(intervals) {
    const merged = [];
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      const previous = merged.length > 0 ? merged[merged.length - 1] : null;
      if (previous && interval.start <= previous.end + 0.5) {
        previous.end = Math.max(previous.end, interval.end);
      } else {
        merged.push({
          start: interval.start,
          end: interval.end,
        });
      }
    }
    return merged;
  }

  function findPreviousContentEnd(intervals, contentStart) {
    let previousEnd = null;
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.end >= contentStart - 0.5) {
        break;
      }
      previousEnd = interval.end;
    }
    return previousEnd;
  }

  function findNextContentStart(intervals, contentEnd) {
    for (let index = 0; index < intervals.length; index += 1) {
      const interval = intervals[index];
      if (interval.start > contentEnd + 0.5) {
        return interval.start;
      }
    }
    return null;
  }

  function removeAdjacentWhitespaceOverlap(segments) {
    const resolved = segments.map(segment => ({
      start: segment.start,
      end: segment.end,
      length: segment.length,
      label: segment.label,
      mode: segment.mode,
    }));

    for (let index = 1; index < resolved.length; index += 1) {
      const previous = resolved[index - 1];
      const current = resolved[index];
      if (!previous || !current || previous.end <= current.start + 0.5) {
        continue;
      }

      let boundary = current.start;
      const minPreviousEnd = previous.start + MIN_SEGMENT_AXIS;
      if (boundary < minPreviousEnd) {
        boundary = minPreviousEnd;
        current.start = boundary;
        current.length = current.end - current.start;
      }

      previous.end = boundary;
      previous.length = previous.end - previous.start;
    }

    return resolved;
  }

  function getHardMaxSegmentAxis(maxSegmentAxis) {
    return Math.max(maxSegmentAxis, HARD_MAX_SEGMENT_AXIS);
  }

  function getMinOutputSegmentAxis(maxSegmentAxis) {
    const preferredMin = Math.min(MIN_OUTPUT_SEGMENT_AXIS, Math.max(maxSegmentAxis, MIN_SEGMENT_AXIS));
    return Math.min(preferredMin, getHardMaxSegmentAxis(maxSegmentAxis));
  }

  function normalizeSplitMode(value) {
    return value === "user" ? "user" : "system";
  }

  function chooseSegmentEnd(partStart, targetEnd, hardEnd, segmentEnd, entries) {
    if (targetEnd >= segmentEnd - 0.5) {
      return segmentEnd;
    }

    const snappedForward = findForwardSafeCut(targetEnd, hardEnd, segmentEnd, entries);
    if (snappedForward > targetEnd + 0.5) {
      return snappedForward;
    }

    const snappedBackward = findBackwardSafeCut(partStart, targetEnd, entries);
    if (snappedBackward > partStart + MIN_SEGMENT_AXIS) {
      return snappedBackward;
    }

    return targetEnd;
  }

  function findForwardSafeCut(targetEnd, hardEnd, segmentEnd, entries) {
    let best = 0;
    const limit = Math.min(hardEnd, segmentEnd);
    const boundaries = getSafeCutBoundaries(entries);
    for (let index = 0; index < boundaries.length; index += 1) {
      const boundary = boundaries[index];
      if (boundary <= targetEnd + CUT_SNAP_TOLERANCE || boundary > limit + CUT_SNAP_TOLERANCE) {
        continue;
      }
      if (isSafeCut(boundary, entries)) {
        best = boundary;
        break;
      }
    }
    return best;
  }

  function findBackwardSafeCut(partStart, targetEnd, entries) {
    let best = 0;
    const boundaries = getSafeCutBoundaries(entries);
    for (let index = 0; index < boundaries.length; index += 1) {
      const boundary = boundaries[index];
      if (boundary <= partStart + MIN_SEGMENT_AXIS || boundary >= targetEnd - CUT_SNAP_TOLERANCE) {
        continue;
      }
      if (isSafeCut(boundary, entries)) {
        best = boundary;
      }
    }
    return best;
  }

  function getSafeCutBoundaries(entries) {
    const values = [];
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!isCutSensitiveEntry(entry)) {
        continue;
      }
      values.push(entry.mainStart);
      values.push(entry.mainEnd);
    }
    values.sort((a, b) => a - b);
    const unique = [];
    for (let index = 0; index < values.length; index += 1) {
      const value = values[index];
      if (!isFiniteNumber(value)) {
        continue;
      }
      if (!unique.length || Math.abs(unique[unique.length - 1] - value) > CUT_SNAP_TOLERANCE) {
        unique.push(value);
      }
    }
    return unique;
  }

  function isSafeCut(cut, entries) {
    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!isCutSensitiveEntry(entry)) {
        continue;
      }
      if (entry.mainStart < cut - CUT_SNAP_TOLERANCE && entry.mainEnd > cut + CUT_SNAP_TOLERANCE) {
        return false;
      }
    }
    return true;
  }

  function isCutSensitiveEntry(entry) {
    if (!entry || entry.node.visible === false || entry.mainLength < MIN_SECTION_AXIS) {
      return false;
    }
    return entry.mainLength <= HARD_MAX_SEGMENT_AXIS + CUT_SNAP_TOLERANCE;
  }

  function getMaxSegmentAxis(options) {
    const value = options && typeof options.maxSegmentAxis === "number" ? options.maxSegmentAxis : MAX_SEGMENT_AXIS;
    if (!isFiniteNumber(value)) {
      return MAX_SEGMENT_AXIS;
    }
    return Math.max(MIN_SEGMENT_AXIS, Math.round(value));
  }

  function getNumberOption(options, key, fallback) {
    const value = options && typeof options[key] === "number" ? options[key] : fallback;
    if (!isFiniteNumber(value) || value < 0) {
      return fallback;
    }
    return value;
  }

  function appendSegment(segments, source, axisLimit) {
    const start = clamp(source.start, 0, axisLimit);
    const end = clamp(source.end, 0, axisLimit);
    const length = end - start;
    if (length < MIN_SEGMENT_AXIS) {
      return;
    }

    segments.push({
      start,
      end,
      length,
      label: source.label || "\uC139\uC158",
      mode: source.mode || "section",
    });
  }

  function cloneSegmentChildren(root, frame, entries, segment, direction) {
    const skipped = [];
    const warnings = [];
    let clonedLayerCount = 0;

    for (let index = 0; index < entries.length; index += 1) {
      const entry = entries[index];
      if (!overlapsSegment(entry, segment)) {
        continue;
      }

      if (shouldInlineContainerForSegment(entry, segment)) {
        const inlineEntries = collectInlineSegmentEntries(root, entry.node, segment, direction);
        for (let inlineIndex = 0; inlineIndex < inlineEntries.length; inlineIndex += 1) {
          clonedLayerCount += cloneSegmentEntry(frame, inlineEntries[inlineIndex], segment, direction, skipped, warnings);
        }
        continue;
      }

      clonedLayerCount += cloneSegmentEntry(frame, entry, segment, direction, skipped, warnings);
    }

    return {
      clonedLayerCount,
      skipped,
      warnings,
    };
  }

  function cloneSegmentEntry(frame, entry, segment, direction, skipped, warnings) {
      let clone = null;
      try {
        clone = entry.node.clone();
        frame.appendChild(clone);
        positionClone(clone, entry, segment, direction);
        const trimResult = trimCloneToSegmentIfNeeded(clone, entry, segment, direction);
        if (trimResult.warning) {
          warnings.push({
            nodeName: entry.name,
            reason: trimResult.warning,
          });
        }
        const pruneResult = pruneCloneOutsideFrame(clone, frame);
        if (pruneResult.warning) {
          warnings.push({
            nodeName: entry.name,
            reason: pruneResult.warning,
          });
        }
        return 1;
      } catch (error) {
        removeNodeQuietly(clone);
        skipped.push({
          nodeId: entry.node.id,
          nodeName: entry.name,
          nodeType: String(entry.node.type || "UNKNOWN"),
          reason: normalizeErrorMessage(error, "\uD574\uB2F9 \uB808\uC774\uC5B4\uB97C \uBCF5\uC81C\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4."),
        });
        return 0;
      }
  }

  function shouldInlineContainerForSegment(entry, segment) {
    if (!entry || !hasChildren(entry.node) || !overlapsSegment(entry, segment)) {
      return false;
    }
    if (!isStructuralContainerNode(entry.node)) {
      return false;
    }
    return entry.mainStart < segment.start - 0.5 || entry.mainEnd > segment.end + 0.5;
  }

  function collectInlineSegmentEntries(root, container, segment, direction) {
    const inlineEntries = [];
    appendInlineSegmentEntries(root, container, segment, direction, inlineEntries);
    return inlineEntries;
  }

  function appendInlineSegmentEntries(root, container, segment, direction, inlineEntries) {
    if (!hasChildren(container)) {
      return;
    }

    for (let index = 0; index < container.children.length; index += 1) {
      const child = container.children[index];
      const entry = createEntryFromNode(root, child, direction, inlineEntries.length);
      if (!entry || !overlapsSegment(entry, segment)) {
        continue;
      }

      if (shouldInlineContainerForSegment(entry, segment)) {
        appendInlineSegmentEntries(root, child, segment, direction, inlineEntries);
      } else {
        inlineEntries.push(entry);
      }
    }
  }

  function removeNodeQuietly(node) {
    try {
      if (node && !node.removed) {
        node.remove();
      }
    } catch (error) {
      /* empty */
    }
  }

  function overlapsSegment(entry, segment) {
    return entry.mainEnd > segment.start + 0.5 && entry.mainStart < segment.end - 0.5;
  }

  function createEntryFromNode(root, node, direction, order) {
    if (!node || node.removed) {
      return null;
    }

    const bounds = getLocalBounds(root, node);
    if (!bounds || bounds.width <= 0 || bounds.height <= 0) {
      return null;
    }

    const mainStart = direction === "vertical" ? bounds.y : bounds.x;
    const mainLength = direction === "vertical" ? bounds.height : bounds.width;
    const crossStart = direction === "vertical" ? bounds.x : bounds.y;
    const crossLength = direction === "vertical" ? bounds.width : bounds.height;
    return {
      node,
      order,
      name: safeName(node),
      localX: bounds.x,
      localY: bounds.y,
      width: bounds.width,
      height: bounds.height,
      mainStart,
      mainEnd: mainStart + mainLength,
      mainLength,
      crossStart,
      crossEnd: crossStart + crossLength,
      crossLength,
    };
  }

  function positionClone(clone, entry, segment, direction) {
    const wasLocked = tryUnlock(clone);
    const mainOffset = direction === "vertical" ? segment.start : 0;
    const crossOffset = direction === "horizontal" ? segment.start : 0;

    if (direction === "vertical") {
      trySet(clone, "x", entry.localX);
      trySet(clone, "y", entry.localY - mainOffset);
    } else {
      trySet(clone, "x", entry.localX - crossOffset);
      trySet(clone, "y", entry.localY);
    }

    restoreLocked(clone, wasLocked);
  }

  function trimCloneToSegmentIfNeeded(clone, entry, segment, direction) {
    const protrudesBefore = entry.mainStart < segment.start - 0.5;
    const protrudesAfter = entry.mainEnd > segment.end + 0.5;
    if (!protrudesBefore && !protrudesAfter) {
      return {};
    }

    if (!canResize(clone)) {
      return {
        warning: "\uC6D0\uBCF8\uBCF4\uB2E4 \uAE34 \uB808\uC774\uC5B4\uB77C \uD074\uB9AC\uD551\uB9CC \uC801\uC6A9\uD588\uC2B5\uB2C8\uB2E4.",
      };
    }

    if (String(clone.type || "") === "INSTANCE" && hasChildren(clone)) {
      return {
        warning: "\uC778\uC2A4\uD134\uC2A4 \uB0B4\uBD80\uB294 \uC790\uC2DD \uC704\uCE58\uB97C \uC870\uC815\uD560 \uC218 \uC5C6\uC5B4 \uD074\uB9AC\uD551\uB9CC \uC801\uC6A9\uD588\uC2B5\uB2C8\uB2E4.",
      };
    }

    const overlapStart = Math.max(entry.mainStart, segment.start);
    const overlapEnd = Math.min(entry.mainEnd, segment.end);
    const overlapLength = overlapEnd - overlapStart;
    if (overlapLength < MIN_SEGMENT_AXIS) {
      return {};
    }

    const cropOffset = overlapStart - entry.mainStart;
    const outputMain = overlapStart - segment.start;
    const wasLocked = tryUnlock(clone);
    const childShift = shiftCloneChildren(clone, cropOffset, direction);

    trySet(clone, "clipsContent", true);
    setNodeAxisPosition(clone, direction, outputMain, getCrossLocalPosition(entry, direction));
    resizeNodeByAxis(clone, direction, overlapLength, getCrossSizeFromEntry(entry, direction));
    restoreLocked(clone, wasLocked);

    if (hasChildren(clone) && childShift.failedCount > 0) {
      return {
        warning: "\uC77C\uBD80 \uC790\uC2DD \uB808\uC774\uC5B4\uB294 \uC139\uC158 \uC704\uCE58\uC5D0 \uB9DE\uAC8C \uC774\uB3D9\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.",
      };
    }

    return {};
  }

  function pruneCloneOutsideFrame(clone, frame) {
    const frameBox = getAbsoluteBox(frame);
    if (!frameBox || !hasChildren(clone)) {
      return {};
    }

    const result = pruneChildrenOutsideBox(clone, frameBox);
    if (result.failedCount > 0) {
      return {
        warning: "\uD504\uB808\uC784 \uBC16 \uD558\uC704 \uB808\uC774\uC5B4 \uC77C\uBD80\uB97C \uC815\uB9AC\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4.",
      };
    }
    return {};
  }

  function pruneChildrenOutsideBox(parent, frameBox) {
    if (!hasChildren(parent)) {
      return {
        removedCount: 0,
        failedCount: 0,
      };
    }

    let removedCount = 0;
    let failedCount = 0;
    const children = Array.from(parent.children);
    for (let index = children.length - 1; index >= 0; index -= 1) {
      const child = children[index];
      if (!child || child.removed) {
        continue;
      }

      const childBox = getAbsoluteBox(child);
      if (childBox && !boxesOverlap(childBox, frameBox)) {
        if (removeChildForPrune(child)) {
          removedCount += 1;
        } else {
          failedCount += 1;
        }
        continue;
      }

      if (hasChildren(child)) {
        const childResult = pruneChildrenOutsideBox(child, frameBox);
        removedCount += childResult.removedCount;
        failedCount += childResult.failedCount;
        if (hasChildren(child) && child.children.length === 0 && isEmptyStructuralContainerNode(child)) {
          if (removeChildForPrune(child)) {
            removedCount += 1;
          } else {
            failedCount += 1;
          }
        }
      }
    }

    return {
      removedCount,
      failedCount,
    };
  }

  function removeChildForPrune(node) {
    try {
      if (node && !node.removed) {
        node.remove();
        return true;
      }
    } catch (error) {
      return false;
    }
    return false;
  }

  function boxesOverlap(first, second) {
    return first.x + first.width > second.x + 0.5 &&
      first.x < second.x + second.width - 0.5 &&
      first.y + first.height > second.y + 0.5 &&
      first.y < second.y + second.height - 0.5;
  }

  function shiftCloneChildren(clone, cropOffset, direction) {
    if (!hasChildren(clone) || Math.abs(cropOffset) < 0.5) {
      return {
        movedCount: 0,
        failedCount: 0,
      };
    }

    trySet(clone, "layoutMode", "NONE");

    let movedCount = 0;
    let failedCount = 0;
    for (let index = 0; index < clone.children.length; index += 1) {
      const child = clone.children[index];
      const wasLocked = tryUnlock(child);
      try {
        if (direction === "vertical") {
          child.y = child.y - cropOffset;
        } else {
          child.x = child.x - cropOffset;
        }
        movedCount += 1;
      } catch (error) {
        failedCount += 1;
      }
      restoreLocked(child, wasLocked);
    }

    return {
      movedCount,
      failedCount,
    };
  }

  function inferDirection(root) {
    if (root.height >= root.width * 1.25) {
      return "vertical";
    }

    if (root.width >= root.height * 1.25) {
      return "horizontal";
    }

    return root.height >= root.width ? "vertical" : "horizontal";
  }

  function getLocalBounds(root, child) {
    const rootBox = getAbsoluteBox(root);
    const childBox = getAbsoluteBox(child);
    if (rootBox && childBox) {
      return {
        x: childBox.x - rootBox.x,
        y: childBox.y - rootBox.y,
        width: childBox.width,
        height: childBox.height,
      };
    }

    const width = getNumberProperty(child, "width", 0);
    const height = getNumberProperty(child, "height", 0);
    if (width <= 0 || height <= 0) {
      return null;
    }

    return {
      x: getNumberProperty(child, "x", 0),
      y: getNumberProperty(child, "y", 0),
      width,
      height,
    };
  }

  function getAbsoluteBox(node) {
    try {
      const box = node.absoluteBoundingBox;
      if (box && isFiniteNumber(box.x) && isFiniteNumber(box.y) && isFiniteNumber(box.width) && isFiniteNumber(box.height)) {
        return box;
      }
    } catch (error) {
      return null;
    }
    return null;
  }

  function hasChildren(node) {
    return !!node && Array.isArray(node.children);
  }

  function isStructuralContainerNode(node) {
    if (!hasChildren(node) || !isEmptyStructuralContainerNode(node)) {
      return false;
    }
    const type = String(node.type || "");
    return type === "GROUP" || type === "FRAME" || type === "SECTION";
  }

  function isEmptyStructuralContainerNode(node) {
    if (!node || readProperty(node, "isMask") === true || readProperty(node, "clipsContent") === true) {
      return false;
    }
    const type = String(node.type || "");
    if (type === "INSTANCE" || type === "COMPONENT" || type === "COMPONENT_SET" || type === "BOOLEAN_OPERATION") {
      return false;
    }
    if (hasVisibleArrayProperty(node, "fills") || hasVisibleArrayProperty(node, "strokes") || hasVisibleArrayProperty(node, "effects")) {
      return false;
    }
    const opacity = readProperty(node, "opacity");
    if (typeof opacity === "number" && opacity < 0.999) {
      return false;
    }
    const blendMode = String(readProperty(node, "blendMode") || "NORMAL");
    return blendMode === "NORMAL" || blendMode === "PASS_THROUGH";
  }

  function hasVisibleArrayProperty(node, key) {
    const value = readProperty(node, key);
    if (!Array.isArray(value)) {
      return false;
    }
    return value.some((item) => item && item.visible !== false);
  }

  function hasUsableSize(node) {
    return getNumberProperty(node, "width", 0) > 1 && getNumberProperty(node, "height", 0) > 1;
  }

  function getMainSize(node, direction) {
    return direction === "vertical" ? node.height : node.width;
  }

  function getCrossSize(node, direction) {
    return direction === "vertical" ? node.width : node.height;
  }

  function getCrossSizeFromEntry(entry, direction) {
    return direction === "vertical" ? entry.width : entry.height;
  }

  function getCrossLocalPosition(entry, direction) {
    return direction === "vertical" ? entry.localX : entry.localY;
  }

  function setNodeAxisPosition(node, direction, main, cross) {
    if (direction === "vertical") {
      trySet(node, "x", cross);
      trySet(node, "y", main);
    } else {
      trySet(node, "x", main);
      trySet(node, "y", cross);
    }
  }

  function resizeNodeByAxis(node, direction, mainLength, crossLength) {
    const width = direction === "vertical" ? crossLength : mainLength;
    const height = direction === "vertical" ? mainLength : crossLength;
    resizeNode(node, width, height);
  }

  function setFrameSize(frame, width, height) {
    resizeNode(frame, width, height);
  }

  function resizeNode(node, width, height) {
    const safeWidth = Math.max(1, width);
    const safeHeight = Math.max(1, height);
    if (typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(safeWidth, safeHeight);
      return;
    }

    if (typeof node.resize === "function") {
      node.resize(safeWidth, safeHeight);
    }
  }

  function canResize(node) {
    return !!node && (typeof node.resizeWithoutConstraints === "function" || typeof node.resize === "function");
  }

  function copyFrameAppearance(source, frame) {
    assignArrayProperty(frame, "fills", readProperty(source, "fills"), []);
    assignArrayProperty(frame, "strokes", readProperty(source, "strokes"), []);
    assignArrayProperty(frame, "effects", readProperty(source, "effects"), null);
    assignArrayProperty(frame, "exportSettings", readProperty(source, "exportSettings"), null);
    assignScalarProperty(frame, "strokeWeight", readProperty(source, "strokeWeight"));
    assignScalarProperty(frame, "cornerRadius", readProperty(source, "cornerRadius"));
  }

  function assignArrayProperty(target, key, value, fallback) {
    try {
      if (Array.isArray(value)) {
        target[key] = JSON.parse(JSON.stringify(value));
      } else if (Array.isArray(fallback)) {
        target[key] = JSON.parse(JSON.stringify(fallback));
      }
    } catch (error) {
      try {
        if (Array.isArray(fallback)) {
          target[key] = fallback;
        }
      } catch (innerError) {
        /* empty */
      }
    }
  }

  function assignScalarProperty(target, key, value) {
    if (value === undefined || value === null || typeof value === "symbol") {
      return;
    }

    trySet(target, key, value);
  }

  function readProperty(node, key) {
    try {
      return node[key];
    } catch (error) {
      return undefined;
    }
  }

  function trySet(node, key, value) {
    try {
      if (node && key in node) {
        node[key] = value;
        return true;
      }
    } catch (error) {
      return false;
    }
    return false;
  }

  function tryUnlock(node) {
    try {
      if (node && node.locked === true) {
        node.locked = false;
        return true;
      }
    } catch (error) {
      return false;
    }
    return false;
  }

  function restoreLocked(node, wasLocked) {
    if (!wasLocked) {
      return;
    }

    try {
      node.locked = true;
    } catch (error) {
      /* empty */
    }
  }

  function buildFrameName(root, segment, index, total) {
    const number = String(index + 1).padStart(3, "0");
    return number + "_" + sanitizeGeneratedFrameBaseName(safeName(root));
  }

  function sanitizeGeneratedFrameBaseName(name) {
    const cleaned = String(name || "")
      .replace(/[\\/:*?"<>|]+/g, " ")
      .replace(/\s+/g, " ")
      .trim();
    if (!cleaned) {
      return "\uC774\uB984 \uC5C6\uC74C";
    }
    return cleaned.length > 120 ? cleaned.slice(0, 120).trim() : cleaned;
  }

  function buildResult(root, direction, segments, createdFrames, skipped, warnings, anchorCount, maxSegmentAxis, splitMode, userGroupCount) {
    const frames = [];
    for (let index = 0; index < createdFrames.length && index < RESULT_PREVIEW_LIMIT; index += 1) {
      frames.push({
        nodeId: createdFrames[index].node.id,
        name: createdFrames[index].name,
        width: createdFrames[index].width,
        height: createdFrames[index].height,
        clonedLayerCount: createdFrames[index].clonedLayerCount,
      });
    }

    return {
      summary: {
        rootName: safeName(root),
        direction,
        directionLabel: direction === "vertical" ? "\uC138\uB85C" : "\uAC00\uB85C",
        sourceChildCount: root.children.length,
        sectionAnchorCount: anchorCount,
        userGroupCount: userGroupCount || 0,
        splitMode: normalizeSplitMode(splitMode),
        splitModeLabel: normalizeSplitMode(splitMode) === "user" ? "\uC0AC\uC6A9\uC790 \uB098\uB204\uAE30" : "\uC2DC\uC2A4\uD15C \uB098\uB204\uAE30",
        segmentCount: segments.length,
        createdFrameCount: createdFrames.length,
        skippedLayerCount: skipped.length,
        warningCount: warnings.length,
        maxSegmentAxis: maxSegmentAxis,
        originalWidth: roundNumber(root.width),
        originalHeight: roundNumber(root.height),
      },
      frames,
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
      warnings: warnings.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const count = summary.createdFrameCount || 0;
    const skippedCount = summary.skippedLayerCount || 0;
    let message = "\uD504\uB808\uC784 " + count + "\uAC1C\uB85C \uB098\uB220\uC2B5\uB2C8\uB2E4.";
    if (skippedCount > 0) {
      message += " \uBCF5\uC81C \uC2E4\uD328 " + skippedCount + "\uAC1C\uAC00 \uC788\uC2B5\uB2C8\uB2E4.";
    }
    figma.notify(message, { timeout: 3000 });
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "split-long-frame-status",
      status,
      message,
    });
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    return fallback;
  }

  function appendAll(target, source) {
    for (let index = 0; index < source.length; index += 1) {
      target.push(source[index]);
    }
  }

  function cleanupCreatedFrames(createdFrames) {
    for (let index = 0; index < createdFrames.length; index += 1) {
      const frame = createdFrames[index] && createdFrames[index].node ? createdFrames[index].node : null;
      if (frame && !frame.removed) {
        try {
          frame.remove();
        } catch (error) {
        }
      }
    }
  }

  function safeName(node) {
    try {
      const name = node && typeof node.name === "string" ? node.name.trim() : "";
      return name || "\uC774\uB984 \uC5C6\uC74C";
    } catch (error) {
      return "\uC774\uB984 \uC5C6\uC74C";
    }
  }

  function getNumberProperty(node, key, fallback) {
    try {
      const value = node[key];
      return isFiniteNumber(value) ? value : fallback;
    } catch (error) {
      return fallback;
    }
  }

  function isFiniteNumber(value) {
    return typeof value === "number" && isFinite(value);
  }

  function clamp(value, min, max) {
    if (value < min) {
      return min;
    }
    if (value > max) {
      return max;
    }
    return value;
  }

  function roundNumber(value) {
    return Math.round(value * 100) / 100;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_FIGMA_URL_SHORTENER_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const AI_SETTINGS_KEY = "pigma:ai-settings:v1";
  const DUB_LINKS_API_URL = "https://api.dub.co/links";
  const DUB_SHORTENER_PROXY_URL = "";
  const BUNDLED_DUB_API_KEY = "dub_r1y6JZGJtEAGD5vqJ17gIOqU";
  const shortUrlCache = {};
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isShortenRequestMessage(message)) {
      if (isRunning) {
        postStatus("running", "Shorten Link is already running.");
        return;
      }

      await runShortenFigmaUrl();
      return;
    }

    if (isCopyPrototypeLinkMessage(message)) {
      if (isRunning) {
        postPrototypeStatus("running", "Preparing the prototype link.");
        return;
      }

      await runCopyPrototypeLink();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_FIGMA_URL_SHORTENER_PATCH__ = true;

  function isShortenRequestMessage(message) {
    return !!message && message.type === "run-shorten-figma-url";
  }

  function isCopyPrototypeLinkMessage(message) {
    return !!message && message.type === "run-copy-prototype-link";
  }

  async function runShortenFigmaUrl() {
    isRunning = true;
    postStatus("running", "Shortening the prototype link for the selected frame or section.");

    try {
      await ensureCurrentPageLoaded();
      const target = collectTargetNode();
      const longUrl = buildPrototypeLink(target);
      const shortUrl = await getShortUrl(longUrl);
      figma.ui.postMessage({
        type: "shorten-figma-url-result",
        result: {
          selectionId: target.node.id,
          shareNodeId: target.shareNode.id,
          selectionLabel: safeName(target.node),
          selectionType: safeNodeType(target.node),
          longUrl: longUrl,
          shortUrl: shortUrl,
        },
      });
      figma.notify("Short link is ready.", { timeout: 1800 });
    } catch (error) {
      const message = normalizeErrorMessage(error, "Failed to shorten the link.");
      figma.ui.postMessage({
        type: "shorten-figma-url-error",
        message: message,
      });
      figma.notify(message, { error: true, timeout: 2600 });
    } finally {
      isRunning = false;
    }
  }

  async function runCopyPrototypeLink() {
    isRunning = true;
    postPrototypeStatus("running", "Preparing the prototype link.");

    try {
      await ensureCurrentPageLoaded();
      const target = collectTargetNode();
      const prototypeUrl = buildPrototypeLink(target);
      figma.ui.postMessage({
        type: "copy-prototype-link-result",
        result: {
          selectionId: target.node.id,
          shareNodeId: target.shareNode.id,
          selectionLabel: safeName(target.node),
          selectionType: safeNodeType(target.node),
          prototypeUrl: prototypeUrl,
          longUrl: prototypeUrl,
        },
      });
      figma.notify("Prototype link is ready.", { timeout: 1800 });
    } catch (error) {
      const message = normalizeErrorMessage(error, "Failed to get the prototype link.");
      figma.ui.postMessage({
        type: "copy-prototype-link-error",
        message: message,
      });
      figma.notify(message, { error: true, timeout: 2600 });
    } finally {
      isRunning = false;
    }
  }

  function postStatus(status, message) {
    figma.ui.postMessage({
      type: "shorten-figma-url-status",
      status: status,
      message: message,
    });
  }

  function postPrototypeStatus(status, message) {
    figma.ui.postMessage({
      type: "copy-prototype-link-status",
      status: status,
      message: message,
    });
  }

  function collectTargetNode() {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error("Select one frame or section first.");
    }

    if (selection.length !== 1) {
      throw new Error("Select only one frame or section.");
    }

    const node = selection[0];
    if (!node || node.removed) {
      throw new Error("Could not find the selected item again. Select it again and retry.");
    }

    if (node.type !== "FRAME" && node.type !== "SECTION") {
      throw new Error("This only works when one frame or section is selected.");
    }

    const shareNode = resolvePrototypeShareNode(node);
    const page = getPageNode(shareNode);

    return {
      node: node,
      shareNode: shareNode,
      page: page,
    };
  }

  async function getShortUrl(longUrl) {
    const normalizedLongUrl = String(longUrl || "").trim();
    if (!normalizedLongUrl) {
      throw new Error("Could not build a link to shorten.");
    }

    const dubApiKey = (await readDubApiKey()) || sanitizeApiKey(BUNDLED_DUB_API_KEY);
    const cacheKey = (dubApiKey ? "dub:" : "public:") + normalizedLongUrl;
    if (shortUrlCache[cacheKey]) {
      return shortUrlCache[cacheKey];
    }

    let lastError = "";

    if (DUB_SHORTENER_PROXY_URL) {
      const proxyCacheKey = "proxy:" + normalizedLongUrl;
      if (shortUrlCache[proxyCacheKey]) {
        return shortUrlCache[proxyCacheKey];
      }

      try {
        const proxyShortUrl = await requestProxyShortUrl(normalizedLongUrl);
        shortUrlCache[proxyCacheKey] = proxyShortUrl;
        return proxyShortUrl;
      } catch (error) {
        lastError = normalizeErrorMessage(error, "Could not shorten the link with the shortener server.");
      }
    }

    if (dubApiKey) {
      try {
        const dubShortUrl = await requestDubShortUrl(normalizedLongUrl, dubApiKey);
        shortUrlCache[cacheKey] = dubShortUrl;
        return dubShortUrl;
      } catch (error) {
        lastError = normalizeErrorMessage(error, "Could not shorten the link with Dub.");
      }
    }

    try {
      const isGdShortUrl = await requestIsGdShortUrl(normalizedLongUrl);
      shortUrlCache[cacheKey] = isGdShortUrl;
      return isGdShortUrl;
    } catch (error) {
      const isGdError = normalizeErrorMessage(error, "Could not shorten the link with is.gd.");
      lastError = lastError ? lastError + " / " + isGdError : isGdError;
    }

    try {
      const tinyUrlShortUrl = await requestTinyUrlShortUrl(normalizedLongUrl);
      shortUrlCache[cacheKey] = tinyUrlShortUrl;
      return tinyUrlShortUrl;
    } catch (error) {
      const tinyUrlError = normalizeErrorMessage(error, "Could not shorten the link with TinyURL.");
      throw new Error(lastError ? lastError + " / " + tinyUrlError : tinyUrlError);
    }
  }

  async function readDubApiKey() {
    try {
      const settings = await figma.clientStorage.getAsync(AI_SETTINGS_KEY);
      if (!settings || typeof settings !== "object") {
        return "";
      }

      return sanitizeApiKey(settings.dubApiKey);
    } catch (error) {
      return "";
    }
  }

  async function requestDubShortUrl(longUrl, apiKey) {
    let response;
    let text = "";
    try {
      response = await fetch(DUB_LINKS_API_URL, {
        method: "POST",
        headers: {
          Authorization: "Bearer " + apiKey,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          url: longUrl,
        }),
      });
      text = String(await response.text()).trim();
    } catch (error) {
      throw new Error("Could not connect to Dub.");
    }

    let payload = null;
    if (text) {
      try {
        payload = JSON.parse(text);
      } catch (error) {
        payload = null;
      }
    }

    if (!response.ok) {
      throw new Error(normalizeDubError(payload, text, response.status));
    }

    const shortLink =
      payload && typeof payload.shortLink === "string" && payload.shortLink.trim()
        ? payload.shortLink.trim()
        : payload && typeof payload.shortUrl === "string" && payload.shortUrl.trim()
          ? payload.shortUrl.trim()
          : "";

    if (!shortLink) {
      throw new Error("Dub did not return a short link.");
    }

    if (!/^https?:\/\//i.test(shortLink)) {
      throw new Error("Could not read the Dub response format.");
    }

    return shortLink;
  }

  async function requestProxyShortUrl(longUrl) {
    let response;
    let text = "";
    try {
      response = await fetch(DUB_SHORTENER_PROXY_URL, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          url: longUrl,
        }),
      });
      text = String(await response.text()).trim();
    } catch (error) {
      throw new Error("Could not connect to the shortener server.");
    }

    let payload = null;
    if (text) {
      try {
        payload = JSON.parse(text);
      } catch (error) {
        payload = null;
      }
    }

    if (!response.ok) {
      throw new Error(normalizeProxyError(payload, text, response.status));
    }

    const shortUrl =
      payload && typeof payload.shortUrl === "string" && payload.shortUrl.trim()
        ? payload.shortUrl.trim()
        : payload && typeof payload.shortLink === "string" && payload.shortLink.trim()
          ? payload.shortLink.trim()
          : "";

    if (!shortUrl) {
      throw new Error("The shortener server did not return a short link.");
    }

    if (!/^https?:\/\//i.test(shortUrl)) {
      throw new Error("Could not read the shortener server response format.");
    }

    return shortUrl;
  }

  function normalizeDubError(payload, text, statusCode) {
    const data = payload && typeof payload === "object" ? payload : {};
    const message =
      typeof data.message === "string" && data.message.trim()
        ? data.message.trim()
        : typeof data.error === "string" && data.error.trim()
          ? data.error.trim()
          : String(text || "").replace(/\s+/g, " ").trim();

    if (message) {
      return message;
    }

    if (statusCode === 401 || statusCode === 403) {
      return "Check the Dub API key permissions.";
    }

    if (statusCode === 429) {
      return "Dub usage or request limit was exceeded.";
    }

    return "Could not shorten the link with Dub.";
  }

  function normalizeProxyError(payload, text, statusCode) {
    const data = payload && typeof payload === "object" ? payload : {};
    const message =
      typeof data.message === "string" && data.message.trim()
        ? data.message.trim()
        : typeof data.error === "string" && data.error.trim()
          ? data.error.trim()
          : String(text || "").replace(/\s+/g, " ").trim();

    if (message) {
      return message;
    }

    if (statusCode === 429) {
      return "The shortener server usage or request limit was exceeded.";
    }

    return "Could not shorten the link with the shortener server.";
  }

  function sanitizeApiKey(value) {
    return String(value || "")
      .replace(/[\u0000-\u001F\u007F-\u009F]/g, "")
      .replace(/[\u200B-\u200D\uFEFF]/g, "")
      .replace(/\s+/g, "")
      .replace(/^['"`]+|['"`]+$/g, "")
      .replace(/[^\x21-\x7E]/g, "")
      .trim();
  }

  async function requestIsGdShortUrl(longUrl) {
    let response;
    try {
      response = await fetch("https://is.gd/create.php?format=simple&url=" + encodeURIComponent(longUrl), {
        method: "GET",
      });
    } catch (error) {
      throw new Error("Could not connect to is.gd.");
    }

    const text = String(await response.text()).trim();
    if (!response.ok) {
      throw new Error(normalizeShortenerError("is.gd", text, response.status));
    }

    if (!text) {
      throw new Error("is.gd did not return a short link.");
    }

    if (/^Error:\s*/i.test(text)) {
      throw new Error(text.replace(/^Error:\s*/i, "").trim() || "is.gd could not process the short link request.");
    }

    if (!/^https?:\/\//i.test(text)) {
      throw new Error("Could not read the is.gd response format.");
    }

    return text;
  }

  async function requestTinyUrlShortUrl(longUrl) {
    let response;
    try {
      response = await fetch("https://tinyurl.com/api-create.php?url=" + encodeURIComponent(longUrl), {
        method: "GET",
      });
    } catch (error) {
      throw new Error("Could not connect to TinyURL.");
    }

    const text = String(await response.text()).trim();
    if (!response.ok) {
      throw new Error(normalizeShortenerError("TinyURL", text, response.status));
    }

    if (!text) {
      throw new Error("TinyURL did not return a short link.");
    }

    if (/^Error:\s*/i.test(text)) {
      throw new Error(text.replace(/^Error:\s*/i, "").trim() || "TinyURL could not process the short link request.");
    }

    if (!/^https?:\/\//i.test(text)) {
      throw new Error("Could not read the TinyURL response format.");
    }

    return text;
  }

  function normalizeShortenerError(providerLabel, text, statusCode) {
    const normalized = String(text || "").replace(/^Error:\s*/i, "").replace(/\s+/g, " ").trim();
    if (normalized) {
      return normalized;
    }

    if (statusCode === 502) {
      return providerLabel + " request limit was exceeded. Try again later.";
    }

    if (statusCode === 503) {
      return providerLabel + " service is unavailable right now. Try again later.";
    }

    return providerLabel + " could not shorten the link.";
  }

  async function ensureCurrentPageLoaded() {
    if (figma.currentPage && typeof figma.currentPage.loadAsync === "function") {
      await figma.currentPage.loadAsync();
    }
  }

  function buildPrototypeLink(target) {
    const fileKey = getFileKey();
    const fileNameSegment = buildFileNameSegment();
    const shareNode = target && target.shareNode ? target.shareNode : null;
    const page = target && target.page ? target.page : getPageNode(shareNode);
    const nodeId = normalizeNodeIdForUrl(shareNode && shareNode.id);
    const pageId = normalizePageIdForUrl(page && page.id);
    return (
      "https://www.figma.com/proto/" +
      encodeURIComponent(fileKey) +
      "/" +
      fileNameSegment +
      "?node-id=" +
      nodeId +
      "&page-id=" +
      pageId +
      "&scaling=min-zoom&content-scaling=fixed"
    );
  }

  function getFileKey() {
    const fileKey = typeof figma.fileKey === "string" ? figma.fileKey.trim() : "";
    if (fileKey) {
      return fileKey;
    }

    throw new Error("This file cannot create a share link yet. Save the file, then try again.");
  }

  function buildFileNameSegment() {
    const rootName =
      figma.root && typeof figma.root.name === "string" && figma.root.name.trim()
        ? figma.root.name.trim()
        : "shared-frame";
    return encodeURIComponent(rootName).replace(/%20/g, "-");
  }

  function normalizeNodeIdForUrl(value) {
    const raw = String(value || "").trim();
    if (!raw) {
      throw new Error("Could not read the selected item's node id.");
    }

    return encodeURIComponent(raw).replace(/%3A/gi, "-");
  }

  function normalizePageIdForUrl(value) {
    const raw = String(value || "").trim();
    if (!raw) {
      throw new Error("Could not read the selected page id.");
    }

    return encodeURIComponent(raw);
  }

  function resolvePrototypeShareNode(node) {
    if (isPrototypePresentableNode(node)) {
      return node;
    }

    if (!node || node.type !== "SECTION") {
      throw new Error("Could not find a frame to share as a prototype.");
    }

    const candidates = collectSectionPrototypeCandidates(node);
    if (!candidates.length) {
      throw new Error("Could not find a prototype-ready frame inside the selected section.");
    }

    const candidateById = new Map();
    for (const candidate of candidates) {
      candidateById.set(candidate.id, candidate);
    }

    const page = getPageNode(node);
    if (page) {
      const flowStartingPoints = Array.isArray(page.flowStartingPoints) ? page.flowStartingPoints : [];
      for (const flow of flowStartingPoints) {
        const flowNodeId = flow && typeof flow.nodeId === "string" ? flow.nodeId : "";
        if (flowNodeId && candidateById.has(flowNodeId)) {
          return candidateById.get(flowNodeId);
        }
      }

      if (
        page.prototypeStartNode &&
        !page.prototypeStartNode.removed &&
        candidateById.has(page.prototypeStartNode.id)
      ) {
        return candidateById.get(page.prototypeStartNode.id);
      }
    }

    const reactionCandidate = candidates.find((candidate) => hasReactions(candidate));
    if (reactionCandidate) {
      return reactionCandidate;
    }

    return candidates[0];
  }

  function collectSectionPrototypeCandidates(section) {
    const visibleMatches = [];
    const hiddenMatches = [];
    const stack = Array.isArray(section && section.children) ? Array.from(section.children) : [];

    while (stack.length) {
      const current = stack.shift();
      if (!current || current.removed) {
        continue;
      }

      if (isPrototypePresentableNode(current)) {
        if (current.visible === false) {
          hiddenMatches.push(current);
        } else {
          visibleMatches.push(current);
        }
      }

      if (Array.isArray(current.children) && current.children.length) {
        for (const child of current.children) {
          stack.push(child);
        }
      }
    }

    return visibleMatches.length ? visibleMatches : hiddenMatches;
  }

  function isPrototypePresentableNode(node) {
    if (!node || node.removed || typeof node.type !== "string") {
      return false;
    }

    return node.type === "FRAME" || node.type === "GROUP" || node.type === "COMPONENT" || node.type === "INSTANCE";
  }

  function hasReactions(node) {
    return !!node && Array.isArray(node.reactions) && node.reactions.length > 0;
  }

  function getPageNode(node) {
    let current = node || null;
    while (current) {
      if (current.type === "PAGE") {
        return current;
      }
      current = current.parent || null;
    }

    throw new Error("Could not find the selected item's page.");
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    return safeNodeType(node);
  }

  function safeNodeType(node) {
    return node && typeof node.type === "string" && node.type ? node.type : "NODE";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_COLOR_EXTRACT_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const ADOBE_TREND_DATA_URL = "https://d2ulm998byv1ft.cloudfront.net/curaredData.json";
  const ADOBE_BEHANCE_GALLERIES_URL = "https://cc-api-behance.adobe.io/v2/galleries";
  const ADOBE_STOCK_SEARCH_URL = "https://stock.adobe.io/Rest/Media/1/Search/Files";
  const ADOBE_COLOR_WEB_KEY = "ColorWeb";
  const COLOR_HUNT_FEED_URL = "https://colorhunt.co/php/feed.php";
  const ADOBE_STOCK_TOPIC_QUERIES = [
    { key: "travel", label: "Travel", query: "travel", tags: ["travel", "journey", "beach", "sky"] },
    { key: "wild", label: "Wild", query: "wildlife", tags: ["wild", "nature", "animal", "forest"] },
    { key: "flavor", label: "Flavor", query: "food", tags: ["food", "flavor", "drink", "dessert"] },
  ];
  const COLOR_HUNT_TOPIC_QUERIES = [
    { key: "popular", label: "Popular", tag: "", aliases: ["popular", "trend", "general"] },
    { key: "neon", label: "Neon", tag: "neon", aliases: ["neon", "pop", "bold", "energetic"] },
    { key: "kids", label: "Kids", tag: "kids", aliases: ["kids", "cute", "playful", "happy"] },
    { key: "food", label: "Food", tag: "food", aliases: ["food", "flavor", "warm", "appetizing"] },
    { key: "sky", label: "Sky", tag: "sky", aliases: ["sky", "travel", "airy", "bright"] },
    { key: "sea", label: "Sea", tag: "sea", aliases: ["sea", "travel", "fresh", "cool"] },
    { key: "nature", label: "Nature", tag: "nature", aliases: ["nature", "wild", "fresh", "earth"] },
    { key: "earth", label: "Earth", tag: "earth", aliases: ["earth", "natural", "organic", "warm"] },
    { key: "pastel", label: "Pastel", tag: "pastel", aliases: ["pastel", "soft", "light", "gentle"] },
    { key: "gradient", label: "Gradient", tag: "gradient", aliases: ["gradient", "ui", "digital", "modern"] },
    { key: "space", label: "Space", tag: "space", aliases: ["space", "future", "night", "cyber"] },
    { key: "sunset", label: "Sunset", tag: "sunset", aliases: ["sunset", "warm", "glow", "travel"] },
    { key: "warm", label: "Warm", tag: "warm", aliases: ["warm", "cozy", "food", "lively"] },
    { key: "cold", label: "Cold", tag: "cold", aliases: ["cold", "cool", "tech", "clean"] },
    { key: "dark", label: "Dark", tag: "dark", aliases: ["dark", "premium", "night", "dramatic"] },
    { key: "light", label: "Light", tag: "light", aliases: ["light", "soft", "minimal", "clean"] },
    { key: "retro", label: "Retro", tag: "retro", aliases: ["retro", "playful", "graphic", "vintage"] },
    { key: "vintage", label: "Vintage", tag: "vintage", aliases: ["vintage", "editorial", "muted", "classic"] },
  ];
  let isPreparing = false;
  let isApplying = false;
  let pendingSession = null;
  let adobeTrendCache = null;
  let adobeTrendCacheExpiresAt = 0;
  let colorHuntCache = null;
  let colorHuntCacheExpiresAt = 0;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isColorExtractMessage(message)) {
      if (message.type === "request-ai-color-extract-source") {
        await prepareColorExtractSource(message);
        return;
      }

      if (message.type === "apply-ai-color-extract-palette") {
        await applyColorExtractPalette(message);
        return;
      }

      if (message.type === "ai-color-extract-report-error") {
        pendingSession = null;
        notifyUiReportedError(message);
        return;
      }
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_COLOR_EXTRACT_PATCH__ = true;

  function isColorExtractMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-color-extract-source" ||
        message.type === "apply-ai-color-extract-palette" ||
        message.type === "ai-color-extract-report-error")
    );
  }

  async function prepareColorExtractSource(message) {
    if (isPreparing || isApplying) {
      postPrepareError("Extract Colors is already running.", sanitizeClientRequestId(message && message.clientRequestId));
      return;
    }

    isPreparing = true;
    pendingSession = null;

    try {
      const target = collectColorExtractTargetFromSelection();
      const bounds = getNodeBounds(target.node);
      const summary = buildSelectionSummary(target.node, bounds);
      const [adobeTrends, colorHuntPalettes] = await Promise.all([loadAdobeTrendPaletteDataset(), loadColorHuntDataset()]);
      const constraint = buildColorExtractConstraint(bounds);
      const exportOptions = {
        format: "PNG",
        useAbsoluteBounds: false,
      };

      if (constraint) {
        exportOptions.constraint = constraint;
      }

      const bytes = await target.node.exportAsync(exportOptions);
      if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
        throw new Error("Could not prepare the selected screen as an image.");
      }

      const sessionId = buildSessionId();
      pendingSession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        targetNodeId: target.node.id,
        targetNodeName: safeName(target.node),
        selectionLabel: summary.selectionLabel,
        bounds: bounds,
        summary: summary,
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
      };

      figma.ui.postMessage({
        type: "ai-color-extract-source-ready",
        sessionId: sessionId,
        clientRequestId: pendingSession.clientRequestId,
        image: {
          bytes: bytes,
          mimeType: "image/png",
          fileName: sanitizeFileName(target.node.name || "color-source") + ".png",
        },
        summary: summary,
        adobeTrends: adobeTrends,
        colorHuntPalettes: colorHuntPalettes,
      });
    } catch (error) {
      pendingSession = null;
      postPrepareError(
        normalizeErrorMessage(error, "Could not prepare the selected image for Extract Colors."),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isPreparing = false;
    }
  }

  async function applyColorExtractPalette(message) {
    if (isApplying) {
      postApplyError("The Extract Colors palette is already being created.", sanitizeClientRequestId(message && message.clientRequestId));
      return;
    }

    isApplying = true;

    try {
      if (!pendingSession || !message || message.sessionId !== pendingSession.id) {
        throw new Error("The Extract Colors session expired. Please run it again.");
      }

      const palette = normalizePalette(message && message.palette);
      const analysis = normalizeAnalysis(message && message.analysis);
      const targetNode = await getFreshTargetNode(pendingSession.targetNodeId);
      const anchorBounds = targetNode ? getNodeBounds(targetNode) : pendingSession.bounds;
      const origin = resolvePaletteOrigin(anchorBounds);
      const created = createPaletteNodes(palette, origin, pendingSession.selectionLabel, analysis);
      const result = {
        summary: {
          selectionLabel: pendingSession.selectionLabel,
          paletteName: created.group.name,
          swatchCount: created.swatches.length,
          columnCount: 3,
          rowCount: 7,
          domain: analysis.domain,
          brand: analysis.brand,
          intent: analysis.intent,
          confidence: analysis.confidence,
        },
        colors: palette,
        analysis: analysis,
        groupId: created.group.id,
      };

      figma.currentPage.selection = [created.group];
      figma.viewport.scrollAndZoomIntoView([created.group]);

      figma.ui.postMessage({
        type: "ai-color-extract-apply-result",
        clientRequestId: pendingSession.clientRequestId,
        result: result,
      });
      notifyApplyResult(result, pendingSession.operationLabel);
      pendingSession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "Could not create the color palette.");
      figma.ui.postMessage({
        type: "ai-color-extract-apply-error",
        clientRequestId:
          pendingSession && pendingSession.clientRequestId
            ? pendingSession.clientRequestId
            : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isApplying = false;
    }
  }

  function collectColorExtractTargetFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error("Select one frame, group, image, or text layer before extracting colors.");
    }

    if (selection.length !== 1) {
      throw new Error("Extract Colors creates a palette from one selected item. Select only one target.");
    }

    const node = selection[0];
    if (node.removed || typeof node.exportAsync !== "function") {
      throw new Error("The selected layer cannot create a preview for Extract Colors.");
    }

    return {
      node: node,
    };
  }

  function buildSelectionSummary(node, bounds) {
    const textInfo = collectTextInfo(node);
    return {
      selectionLabel: safeName(node),
      targetNodeName: safeName(node),
      width: Math.round(bounds && typeof bounds.width === "number" ? bounds.width : 0),
      height: Math.round(bounds && typeof bounds.height === "number" ? bounds.height : 0),
      nodeType: safeNodeType(node),
      sourceType: resolveSourceType(node, textInfo),
      textHint: textInfo.text,
      textColors: textInfo.colors,
      textNodeCount: textInfo.count,
      childCount: countChildren(node),
    };
  }

  function resolveSourceType(node, textInfo) {
    if (node && node.type === "TEXT") {
      return "text";
    }
    if (textInfo && textInfo.text) {
      return "mixed";
    }
    return "image";
  }

  function collectTextInfo(rootNode) {
    const snippets = [];
    const colorWeights = {};
    const queue = rootNode ? [rootNode] : [];
    let textCount = 0;
    let visited = 0;
    let totalChars = 0;

    while (queue.length && visited < 160 && totalChars < 320) {
      const current = queue.shift();
      visited += 1;
      if (!current || current.removed) {
        continue;
      }

      if (current.type === "TEXT" && typeof current.characters === "string") {
        const normalized = compactText(current.characters);
        if (normalized) {
          textCount += 1;
          mergeWeightedHexes(colorWeights, extractTextFillHexes(current), Math.max(1, Math.min(normalized.length, 140)));
          const remaining = Math.max(0, 320 - totalChars);
          if (!remaining) {
            break;
          }
          const snippet = normalized.slice(0, remaining);
          snippets.push(snippet);
          totalChars += snippet.length;
        }
      }

      if ("children" in current && Array.isArray(current.children) && current.children.length) {
        for (let index = 0; index < current.children.length; index += 1) {
          queue.push(current.children[index]);
          if (queue.length > 240) {
            break;
          }
        }
      }
    }

    return {
      text: snippets.join(" | "),
      count: textCount,
      colors: listWeightedHexes(colorWeights, 6),
    };
  }

  function extractTextFillHexes(node) {
    if (!node || !Array.isArray(node.fills)) {
      return [];
    }

    const hexes = [];
    const seen = {};
    for (let index = 0; index < node.fills.length; index += 1) {
      const paint = node.fills[index];
      if (!paint || paint.visible === false || paint.type !== "SOLID" || !paint.color) {
        continue;
      }
      const hex = rgbUnitToHex(paint.color);
      if (!hex || seen[hex]) {
        continue;
      }
      seen[hex] = true;
      hexes.push(hex);
      if (hexes.length >= 4) {
        break;
      }
    }

    return hexes;
  }

  function mergeWeightedHexes(store, hexes, weight) {
    if (!store || typeof store !== "object" || !Array.isArray(hexes) || !hexes.length) {
      return;
    }

    const safeWeight = Number.isFinite(Number(weight)) ? Number(weight) : 1;
    for (let index = 0; index < hexes.length; index += 1) {
      const hex = normalizeHexColor(hexes[index]);
      if (!hex) {
        continue;
      }
      store[hex] = (store[hex] || 0) + safeWeight;
    }
  }

  function listWeightedHexes(store, limit) {
    if (!store || typeof store !== "object") {
      return [];
    }

    return Object.keys(store)
      .sort((first, second) => {
        const weightGap = (store[second] || 0) - (store[first] || 0);
        if (weightGap !== 0) {
          return weightGap;
        }
        return first.localeCompare(second);
      })
      .slice(0, Math.max(0, Number(limit) || 0));
  }

  function countChildren(node) {
    if (node && "children" in node && Array.isArray(node.children)) {
      return node.children.length;
    }
    return 0;
  }

  function safeNodeType(node) {
    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }
    return "UNKNOWN";
  }

  function buildColorExtractConstraint(bounds) {
    const width = bounds && typeof bounds.width === "number" && Number.isFinite(bounds.width) ? Math.max(1, bounds.width) : 0;
    const height =
      bounds && typeof bounds.height === "number" && Number.isFinite(bounds.height) ? Math.max(1, bounds.height) : 0;
    const longest = Math.max(width, height);
    if (!(longest > 1600)) {
      return null;
    }

    return width >= height
      ? {
          type: "WIDTH",
          value: 1600,
        }
      : {
          type: "HEIGHT",
          value: 1600,
        };
  }

  async function getFreshTargetNode(nodeId) {
    if (!(typeof nodeId === "string" && nodeId)) {
      return null;
    }

    if (typeof figma.getNodeByIdAsync === "function") {
      try {
        return await figma.getNodeByIdAsync(nodeId);
      } catch (error) {
        return null;
      }
    }

    try {
      return figma.getNodeById(nodeId);
    } catch (error) {
      return null;
    }
  }

  function getNodeBounds(node) {
    if (node && "absoluteRenderBounds" in node && node.absoluteRenderBounds) {
      return cloneBounds(node.absoluteRenderBounds);
    }

    if (node && "absoluteBoundingBox" in node && node.absoluteBoundingBox) {
      return cloneBounds(node.absoluteBoundingBox);
    }

    if (
      node &&
      typeof node.x === "number" &&
      typeof node.y === "number" &&
      typeof node.width === "number" &&
      typeof node.height === "number"
    ) {
      return {
        x: node.x,
        y: node.y,
        width: Math.max(1, node.width),
        height: Math.max(1, node.height),
      };
    }

    return {
      x: figma.viewport.center.x,
      y: figma.viewport.center.y,
      width: 1,
      height: 1,
    };
  }

  function cloneBounds(bounds) {
    return {
      x: typeof bounds.x === "number" ? bounds.x : 0,
      y: typeof bounds.y === "number" ? bounds.y : 0,
      width: typeof bounds.width === "number" ? Math.max(1, bounds.width) : 1,
      height: typeof bounds.height === "number" ? Math.max(1, bounds.height) : 1,
    };
  }

  function resolvePaletteOrigin(bounds) {
    const baseX = bounds && typeof bounds.x === "number" ? bounds.x : figma.viewport.center.x;
    const baseY = bounds && typeof bounds.y === "number" ? bounds.y : figma.viewport.center.y;
    const width = bounds && typeof bounds.width === "number" ? bounds.width : 0;

    return {
      x: Math.round(baseX + width + 80),
      y: Math.round(baseY),
    };
  }

  function createPaletteNodes(palette, origin, selectionLabel, analysis) {
    const cellSize = 92;
    const gap = 12;
    const rowDefinitions = buildPaletteRowDefinitions();
    const swatches = [];

    for (let rowIndex = 0; rowIndex < rowDefinitions.length; rowIndex += 1) {
      const rowDefinition = rowDefinitions[rowIndex];
      const row = rowDefinition && palette && Array.isArray(palette[rowDefinition.key]) ? palette[rowDefinition.key] : [];
      for (let columnIndex = 0; columnIndex < row.length; columnIndex += 1) {
        const hex = row[columnIndex];
        const rectangle = figma.createRectangle();
        rectangle.x = origin.x + columnIndex * (cellSize + gap);
        rectangle.y = origin.y + rowIndex * (cellSize + gap);
        rectangle.resize(cellSize, cellSize);
        rectangle.cornerRadius = 14;
        const swatchLabel = getPaletteSwatchLabel(rowDefinition, columnIndex);
        const swatchValue = getPaletteSwatchValue(rowDefinition, columnIndex, palette);
        rectangle.name = swatchLabel + " \u00B7 " + swatchValue;
        rectangle.fills =
          rowDefinition && rowDefinition.gradient ? [buildGradientSwatchPaint(palette, columnIndex)] : [solidPaintFromHex(hex, 1)];
        rectangle.strokes = [solidPaintFromHex("#111827", 0.14)];
        rectangle.strokeWeight = 1;
        swatches.push(rectangle);
      }
    }

    const group = figma.group(swatches, figma.currentPage);
    group.name = buildPaletteName(selectionLabel);

    if (analysis && typeof group.setPluginData === "function") {
      try {
        group.setPluginData(
          "ai-color-extract-analysis",
          JSON.stringify({
            sourceType: analysis.sourceType,
            domain: analysis.domain,
            brand: analysis.brand,
            intent: analysis.intent,
            tone: analysis.tone,
            confidence: analysis.confidence,
            reason: analysis.reason,
          })
        );
      } catch (error) {}
    }

    return {
      group: group,
      swatches: swatches,
    };
  }

  function buildPaletteRowDefinitions() {
    return [
      {
        key: "representative",
        label: "\uB300\uD45C\uC0C9",
      },
      {
        key: "adobe",
        label: "\uC5B4\uB3C4\uBE44 \uCD94\uCC9C\uC0C9",
        columnLabels: ["\uC5B4\uB3C4\uBE44 \uCD94\uCC9C 1", "\uC5B4\uB3C4\uBE44 \uCD94\uCC9C 2", "\uC5B4\uB3C4\uBE44 \uCD94\uCC9C 3"],
      },
      {
        key: "trendy",
        label: "\uD2B8\uB80C\uB514 \uCEEC\uB7EC",
        columnLabels: ["\uD2B8\uB80C\uB514 \uCEEC\uB7EC 1", "\uD2B8\uB80C\uB514 \uCEEC\uB7EC 2", "\uD2B8\uB80C\uB514 \uCEEC\uB7EC 3"],
      },
      {
        key: "companions",
        label: "\uC5B4\uC6B8\uB9AC\uB294 \uC0C9",
      },
      {
        key: "accents",
        label: "\uD3EC\uC778\uD2B8 \uC0C9",
        columnLabels: ["\uD3EC\uC778\uD2B8 \uC0C9 1", "\uD3EC\uC778\uD2B8 \uC0C9 2", "\uD3EC\uC778\uD2B8 \uC0C9 3"],
      },
      {
        key: "text",
        label: "\uD14D\uC2A4\uD2B8 \uC5ED\uD560\uC0C9",
        columnLabels: ["\uD14D\uC2A4\uD2B8 \uC8FC\uC870\uC0C9", "\uD14D\uC2A4\uD2B8 \uD558\uC774\uB77C\uC774\uD2B8", "\uD14D\uC2A4\uD2B8 \uC11C\uBE0C\uC0C9"],
      },
      {
        key: "surfaces",
        label: "\uADF8\uB77C\uB370\uC774\uC158 \uC0C9",
        columnLabels: [
          "\uADF8\uB77C\uB370\uC774\uC158 \uC138\uD2B8 1",
          "\uADF8\uB77C\uB370\uC774\uC158 \uC138\uD2B8 2",
          "\uADF8\uB77C\uB370\uC774\uC158 \uC138\uD2B8 3",
        ],
        gradient: true,
      },
    ];
  }

  function getPaletteSwatchLabel(rowDefinition, columnIndex) {
    const rowLabel = rowDefinition && typeof rowDefinition.label === "string" ? rowDefinition.label : "\uC0C9";
    const columnLabels = rowDefinition && Array.isArray(rowDefinition.columnLabels) ? rowDefinition.columnLabels : [];
    return columnLabels[columnIndex] || rowLabel + " " + (columnIndex + 1);
  }

  function getPaletteSwatchValue(rowDefinition, columnIndex, palette) {
    if (rowDefinition && rowDefinition.gradient) {
      return buildGradientSwatchColorLabel(palette, columnIndex);
    }

    const key = rowDefinition && typeof rowDefinition.key === "string" ? rowDefinition.key : "";
    const row = key && palette && Array.isArray(palette[key]) ? palette[key] : [];
    return row[columnIndex] || "#000000";
  }

  function buildPaletteName(selectionLabel) {
    const base = typeof selectionLabel === "string" && selectionLabel.trim() ? selectionLabel.trim() : "Selection";
    return base + " Extract Colors palette";
  }

  function solidPaintFromHex(hex, opacity) {
    const rgb = hexToRgb(hex);
    return {
      type: "SOLID",
      color: {
        r: rgb.r / 255,
        g: rgb.g / 255,
        b: rgb.b / 255,
      },
      opacity: typeof opacity === "number" ? Math.max(0, Math.min(1, opacity)) : 1,
    };
  }

  function buildGradientSwatchPaint(palette, index) {
    const colors = resolveGradientSwatchColors(palette, index);
    return {
      type: "GRADIENT_LINEAR",
      gradientTransform: [
        [1, 0, 0],
        [0, 1, 0],
      ],
      gradientStops: colors.map((hex, stopIndex) => ({
        position: stopIndex / (colors.length - 1),
        color: rgbaFromHex(hex, 1),
      })),
      opacity: 1,
    };
  }

  function buildGradientSwatchColorLabel(palette, index) {
    return resolveGradientSwatchColors(palette, index).join(" -> ");
  }

  function resolveGradientSwatchColors(palette, index) {
    const representative = palette && Array.isArray(palette.representative) ? palette.representative[index] : null;
    const companion = palette && Array.isArray(palette.companions) ? palette.companions[index] : null;
    const accent = palette && Array.isArray(palette.accents) ? palette.accents[index] : null;
    const surface = palette && Array.isArray(palette.surfaces) ? palette.surfaces[index] : null;
    const baseHex = normalizeHexColor(representative) || normalizeHexColor(companion) || "#2563EB";
    const supportHex = normalizeHexColor(companion) || mixHex(baseHex, "#FFFFFF", 0.32);
    const accentHex = normalizeHexColor(accent) || mixHex(supportHex, "#FFFFFF", 0.16);
    const softHex = normalizeHexColor(surface) || mixHex(baseHex, "#FFFFFF", 0.72);
    return [softHex, mixHex(baseHex, supportHex, 0.42), accentHex];
  }

  function rgbaFromHex(hex, opacity) {
    const rgb = hexToRgb(hex);
    return {
      r: rgb.r / 255,
      g: rgb.g / 255,
      b: rgb.b / 255,
      a: typeof opacity === "number" ? Math.max(0, Math.min(1, opacity)) : 1,
    };
  }

  function normalizePalette(value) {
    const source = value && typeof value === "object" ? value : {};
    const representative = normalizePaletteRow(source.representative, ["#2563EB", "#7C3AED", "#F97316"]);
    const companions = normalizePaletteRow(source.companions, ["#0EA5E9", "#EC4899", "#F59E0B"]);
    const accents = hasCompletePaletteRow(source.accents)
      ? normalizePaletteRow(source.accents, ["#EC4899", "#22C55E", "#FACC15"])
      : buildDerivedAccentRow(representative, companions);
    const adobe = hasCompletePaletteRow(source.adobe)
      ? normalizePaletteRow(source.adobe, buildDerivedAdobeRow(representative, companions, accents))
      : buildDerivedAdobeRow(representative, companions, accents);
    const trendy = hasCompletePaletteRow(source.trendy)
      ? normalizePaletteRow(source.trendy, buildDerivedTrendyRow(representative, companions))
      : buildDerivedTrendyRow(representative, companions);
    const text = normalizePaletteRow(source.text, ["#0F172A", "#2563EB", "#64748B"]);
    const surfaces = hasCompletePaletteRow(source.surfaces)
      ? normalizePaletteRow(source.surfaces, ["#E0F2FE", "#F5D0FE", "#FEF3C7"])
      : buildDerivedSurfaceRow(representative, companions, accents);
    return {
      representative: representative,
      adobe: adobe,
      trendy: trendy,
      companions: companions,
      accents: accents,
      text: text,
      surfaces: surfaces,
    };
  }

  function hasCompletePaletteRow(value) {
    return Array.isArray(value) && value.length >= 3 && value.slice(0, 3).every((item) => !!normalizeHexColor(item));
  }

  function normalizePaletteRow(value, fallback) {
    const list = Array.isArray(value) ? value : [];
    const normalized = [];

    for (let index = 0; index < 3; index += 1) {
      const hex = normalizeHexColor(list[index]);
      normalized.push(hex || fallback[index]);
    }

    return normalized;
  }

  function buildDerivedAccentRow(representative, companions) {
    const hueShifts = [138, -124, 72];
    const row = [];
    for (let index = 0; index < 3; index += 1) {
      const baseHex = companions[index] || representative[index] || "#4F46E5";
      const base = rgbBytesToHsl(hexToRgb(baseHex));
      row.push(
        hslByteToHex(
          normalizeHue(base.h + hueShifts[index % hueShifts.length]),
          clampNumber(base.s < 0.22 ? 0.62 : base.s * 0.82 + 0.12, 0.48, 0.88),
          clampNumber(base.l < 0.25 ? 0.52 : base.l > 0.72 ? 0.58 : base.l, 0.38, 0.68)
        )
      );
    }
    return row;
  }

  function buildDerivedAdobeRow(representative, companions, accents) {
    const hueShifts = [24, -18, 34];
    const row = [];
    for (let index = 0; index < 3; index += 1) {
      const baseHex = accents[index] || companions[index] || representative[index] || "#EC4899";
      const base = rgbBytesToHsl(hexToRgb(baseHex));
      row.push(
        hslByteToHex(
          normalizeHue(base.h + hueShifts[index % hueShifts.length]),
          clampNumber(base.s < 0.28 ? 0.68 : base.s * 0.82 + 0.1, 0.54, 0.92),
          clampNumber(base.l < 0.24 ? 0.56 : base.l > 0.72 ? 0.58 : base.l, 0.42, 0.72)
        )
      );
    }
    return row;
  }

  function buildDerivedTrendyRow(representative, companions) {
    const hueShifts = [22, -34, 48];
    const row = [];
    for (let index = 0; index < 3; index += 1) {
      const baseHex = companions[index] || representative[index] || "#0EA5E9";
      const base = rgbBytesToHsl(hexToRgb(baseHex));
      row.push(
        hslByteToHex(
          normalizeHue(base.h + hueShifts[index % hueShifts.length]),
          clampNumber(base.s < 0.26 ? 0.62 : base.s * 0.88 + 0.08, 0.48, 0.9),
          clampNumber(base.l < 0.22 ? 0.56 : base.l > 0.76 ? 0.62 : base.l + 0.04, 0.42, 0.74)
        )
      );
    }
    return row;
  }

  function buildDerivedSurfaceRow(representative, companions, accents) {
    const row = [];
    for (let index = 0; index < 3; index += 1) {
      const first = companions[index] || representative[index] || "#DCEBFF";
      const second = accents[index] || representative[index] || "#FFFFFF";
      row.push(mixHex(mixHex(first, "#FFFFFF", 0.78), second, 0.1));
    }
    return row;
  }

  function mixHex(firstHex, secondHex, amount) {
    const first = hexToRgb(firstHex);
    const second = hexToRgb(secondHex);
    const ratio = typeof amount === "number" ? Math.max(0, Math.min(1, amount)) : 0.5;
    return rgbByteToHex(
      first.r + (second.r - first.r) * ratio,
      first.g + (second.g - first.g) * ratio,
      first.b + (second.b - first.b) * ratio
    );
  }

  function clampNumber(value, min, max) {
    const number = Number(value);
    if (!Number.isFinite(number)) {
      return min;
    }
    return Math.max(min, Math.min(max, number));
  }

  function normalizeHue(value) {
    const number = Number(value) || 0;
    return ((number % 360) + 360) % 360;
  }

  function normalizeAnalysis(value) {
    const source = value && typeof value === "object" ? value : {};
    return {
      sourceType: normalizeAnalysisField(source.sourceType, "image", 32),
      domain: normalizeAnalysisField(source.domain, "general", 48),
      brand: normalizeAnalysisField(source.brand, "none", 48),
      intent: normalizeAnalysisField(source.intent, "ui", 32),
      tone: normalizeToneList(source.tone),
      confidence: normalizeConfidence(source.confidence),
      reason: normalizeAnalysisField(source.reason, "", 220),
    };
  }

  function normalizeAnalysisField(value, fallback, maxLength) {
    const text = compactText(value);
    if (!text) {
      return fallback;
    }
    return text.slice(0, Math.max(1, maxLength || 48));
  }

  function normalizeToneList(value) {
    const source = Array.isArray(value) ? value : typeof value === "string" ? value.split(/[|,]/) : [];
    const seen = {};
    const normalized = [];

    for (let index = 0; index < source.length; index += 1) {
      const entry = compactText(source[index]);
      if (!entry) {
        continue;
      }
      const key = entry.toLowerCase();
      if (seen[key]) {
        continue;
      }
      seen[key] = true;
      normalized.push(entry.slice(0, 32));
      if (normalized.length >= 3) {
        break;
      }
    }

    return normalized;
  }

  function normalizeConfidence(value) {
    const text = compactText(value).toLowerCase();
    if (text === "high" || text === "medium" || text === "low") {
      return text;
    }
    if (/높/.test(text)) {
      return "high";
    }
    if (/낮/.test(text)) {
      return "low";
    }
    return "medium";
  }

  function normalizeHexColor(value) {
    const text = typeof value === "string" ? value.replace(/\s+/g, "").trim().toUpperCase() : "";
    if (/^#[0-9A-F]{6}$/.test(text)) {
      return text;
    }
    return "";
  }

  async function loadAdobeTrendPaletteDataset() {
    if (Array.isArray(adobeTrendCache) && adobeTrendCache.length && adobeTrendCacheExpiresAt > Date.now()) {
      return adobeTrendCache;
    }

    try {
      const [curated, live] = await Promise.all([loadAdobeCuratedTrendDataset(), loadAdobeLiveTrendDataset()]);
      const normalized = mergeAdobeTrendRecords([].concat(curated, live));
      if (normalized.length) {
        adobeTrendCache = normalized;
        adobeTrendCacheExpiresAt = Date.now() + 1000 * 60 * 60 * 6;
      }
      return normalized;
    } catch (error) {
      console.warn("[pigma] adobe trend palette feed unavailable:", error);
      return Array.isArray(adobeTrendCache) ? adobeTrendCache : [];
    }
  }

  async function loadAdobeCuratedTrendDataset() {
    const response = await fetch(ADOBE_TREND_DATA_URL);
    if (!response || !response.ok) {
      throw new Error("Adobe trend palette feed was unavailable.");
    }
    const data = await response.json();
    return normalizeAdobeTrendPaletteDataset(data);
  }

  async function loadAdobeLiveTrendDataset() {
    const [behance, stock] = await Promise.all([
      loadAdobeBehanceTrendDataset().catch((error) => {
        console.warn("[pigma] adobe behance trend feed unavailable:", error);
        return [];
      }),
      loadAdobeStockTrendDataset().catch((error) => {
        console.warn("[pigma] adobe stock trend feed unavailable:", error);
        return [];
      }),
    ]);

    return mergeAdobeTrendRecords([].concat(behance, stock));
  }

  async function loadColorHuntDataset() {
    if (Array.isArray(colorHuntCache) && colorHuntCache.length && colorHuntCacheExpiresAt > Date.now()) {
      return colorHuntCache;
    }

    try {
      const batches = await Promise.all(
        COLOR_HUNT_TOPIC_QUERIES.map((topic) =>
          fetchColorHuntTopic(topic).catch((error) => {
            console.warn("[pigma] colorhunt topic unavailable:", topic && topic.key, error);
            return [];
          })
        )
      );
      const normalized = mergeAdobeTrendRecords(batches.flat());
      if (normalized.length) {
        colorHuntCache = normalized;
        colorHuntCacheExpiresAt = Date.now() + 1000 * 60 * 60 * 6;
      }
      return normalized;
    } catch (error) {
      console.warn("[pigma] colorhunt palette feed unavailable:", error);
      return Array.isArray(colorHuntCache) ? colorHuntCache : [];
    }
  }

  async function fetchColorHuntTopic(topic) {
    const body = new URLSearchParams();
    body.set("step", "0");
    body.set("sort", "popular");
    body.set("tags", topic && topic.tag ? topic.tag : "");
    body.set("timeframe", "365");

    const response = await fetch(COLOR_HUNT_FEED_URL, {
      method: "POST",
      headers: {
        "content-type": "application/x-www-form-urlencoded; charset=UTF-8",
      },
      body: body.toString(),
    });
    if (!response || !response.ok) {
      throw new Error(`ColorHunt topic ${topic && topic.key ? topic.key : "popular"} was unavailable.`);
    }

    const data = await response.json();
    return normalizeColorHuntFeedEntries(data, topic);
  }

  function normalizeColorHuntFeedEntries(entries, topic) {
    const source = Array.isArray(entries) ? entries : [];
    const normalized = [];

    for (let index = 0; index < Math.min(source.length, 18); index += 1) {
      const entry = source[index] && typeof source[index] === "object" ? source[index] : {};
      const code = compactText(entry.code).toUpperCase();
      const swatches = splitColorHuntCode(code);
      if (swatches.length < 4) {
        continue;
      }

      normalized.push({
        id: clipText(`${topic && topic.key ? topic.key : "popular"}-${code}`, 32),
        name: clipText(`${topic && topic.label ? topic.label : "Popular"} Palette ${index + 1}`, 96),
        source: "COLORHUNT",
        href: clipText(`https://colorhunt.co/palette/${code.toLowerCase()}`, 180),
        tags: []
          .concat(topic && Array.isArray(topic.aliases) ? topic.aliases : [])
          .concat([topic && topic.label, topic && topic.tag, entry.date])
          .map((tag) => clipText(tag, 48))
          .filter(Boolean)
          .slice(0, 18),
        swatches: swatches,
        categoryKey: clipText(topic && topic.key, 48).toLowerCase(),
        thumbnailUrl: "",
        likes: Number(entry.likes) || 0,
      });
    }

    return normalized;
  }

  function splitColorHuntCode(code) {
    const text = typeof code === "string" ? code.replace(/[^0-9A-F]/gi, "").toUpperCase() : "";
    const swatches = [];

    for (let index = 0; index + 6 <= text.length; index += 6) {
      const hex = normalizeHexColor("#" + text.slice(index, index + 6));
      if (!hex) {
        continue;
      }
      swatches.push(hex);
    }

    return swatches.slice(0, 4);
  }

  function normalizeAdobeTrendPaletteDataset(value) {
    const files = value && Array.isArray(value.files) ? value.files : [];
    const normalized = [];

    for (let index = 0; index < files.length; index += 1) {
      const entry = normalizeAdobeTrendPaletteEntry(files[index]);
      if (entry) {
        normalized.push(entry);
      }
    }

    return normalized.slice(0, 180);
  }

  function normalizeAdobeTrendPaletteEntry(value) {
    const source = value && typeof value === "object" ? value : {};
    const swatches = Array.isArray(source.swatches)
      ? source.swatches
          .map((swatch) => {
            const hex = normalizeHexColor("#" + compactText(swatch && swatch.hex));
            return hex || "";
          })
          .filter(Boolean)
          .slice(0, 5)
      : [];
    if (swatches.length < 3) {
      return null;
    }

    const tags = Array.isArray(source.tags)
      ? source.tags
          .map((tag) => clipText(tag && typeof tag === "object" ? tag.value : tag, 48))
          .filter(Boolean)
          .slice(0, 18)
      : [];

    return {
      id: clipText(source.id, 32),
      name: clipText(source.name, 96),
      source: clipText(source.source, 24).toUpperCase(),
      href: clipText(source.href, 180),
      tags: tags,
      swatches: swatches,
      categoryKey: "",
      thumbnailUrl: "",
    };
  }

  async function loadAdobeBehanceTrendDataset() {
    const response = await fetch(ADOBE_BEHANCE_GALLERIES_URL, {
      headers: {
        "x-api-key": ADOBE_COLOR_WEB_KEY,
      },
    });
    if (!response || !response.ok) {
      throw new Error("Adobe Behance galleries were unavailable.");
    }

    const data = await response.json();
    const categories = data && Array.isArray(data.categories) ? data.categories : [];
    const normalized = [];

    for (let index = 0; index < categories.length; index += 1) {
      const category = categories[index];
      const records = normalizeAdobeBehanceCategory(category);
      for (let recordIndex = 0; recordIndex < records.length; recordIndex += 1) {
        normalized.push(records[recordIndex]);
      }
    }

    return normalized.slice(0, 72);
  }

  function normalizeAdobeBehanceCategory(value) {
    const source = value && typeof value === "object" ? value : {};
    const projects = Array.isArray(source.latest_projects) ? source.latest_projects : [];
    const records = [];
    const categoryKey = clipText(source.slug, 48).toLowerCase();
    const categoryLabel = clipText(source.label || source.label_en, 48);

    for (let index = 0; index < Math.min(projects.length, 4); index += 1) {
      const project = projects[index] && typeof projects[index] === "object" ? projects[index] : {};
      const tags = [];
      appendAdobeTrendTag(tags, categoryLabel);
      appendAdobeTrendTag(tags, categoryKey);
      const fieldLinks = Array.isArray(project.field_links) ? project.field_links : [];
      for (let fieldIndex = 0; fieldIndex < fieldLinks.length; fieldIndex += 1) {
        appendAdobeTrendTag(tags, fieldLinks[fieldIndex] && fieldLinks[fieldIndex].name);
      }
      const fields = Array.isArray(project.fields) ? project.fields : [];
      for (let fieldIndex = 0; fieldIndex < fields.length; fieldIndex += 1) {
        appendAdobeTrendTag(tags, fields[fieldIndex] && fields[fieldIndex].name);
      }

      const colorHexes = Array.isArray(project.colors)
        ? project.colors
            .map((color) => rgbUnitToHex({
              r: Number(color && color.r) / 255,
              g: Number(color && color.g) / 255,
              b: Number(color && color.b) / 255,
            }))
            .filter(Boolean)
            .slice(0, 3)
        : [];

      records.push({
        id: clipText(project.id || `${categoryKey}-${index}`, 32),
        name: clipText(project.name, 96),
        source: "BEHANCE",
        href: clipText(project.url || source.url, 180),
        tags: tags.slice(0, 18),
        swatches: colorHexes,
        categoryKey: categoryKey,
        thumbnailUrl: clipText(resolveBehanceProjectThumbnailUrl(project), 240),
      });
    }

    return records.filter((record) => record.thumbnailUrl || record.swatches.length >= 1);
  }

  function resolveBehanceProjectThumbnailUrl(project) {
    const covers = project && typeof project === "object" && project.covers && typeof project.covers === "object" ? project.covers : {};
    return (
      covers["404_webp"] ||
      covers["404"] ||
      covers["202_webp"] ||
      covers["202"] ||
      covers["230_webp"] ||
      covers["230"] ||
      covers["original_webp"] ||
      covers["original"] ||
      ""
    );
  }

  async function loadAdobeStockTrendDataset() {
    const batches = await Promise.all(
      ADOBE_STOCK_TOPIC_QUERIES.map((topic) =>
        fetchAdobeStockTopic(topic).catch((error) => {
          console.warn("[pigma] adobe stock topic unavailable:", topic && topic.key, error);
          return [];
        })
      )
    );
    return mergeAdobeTrendRecords(batches.flat());
  }

  async function fetchAdobeStockTopic(topic) {
    const search = new URLSearchParams();
    search.append("search_parameters[words]", topic.query);
    search.append("search_parameters[limit]", "10");
    search.append("search_parameters[order]", "relevance");
    const response = await fetch(`${ADOBE_STOCK_SEARCH_URL}?${search.toString()}`, {
      headers: {
        "x-api-key": ADOBE_COLOR_WEB_KEY,
        "X-Product": "Color",
        "X-Product-Location": "Color Website",
      },
    });
    if (!response || !response.ok) {
      throw new Error(`Adobe Stock topic ${topic.key} was unavailable.`);
    }

    const data = await response.json();
    const files = data && Array.isArray(data.files) ? data.files : [];
    const normalized = [];

    for (let index = 0; index < Math.min(files.length, 8); index += 1) {
      const file = files[index] && typeof files[index] === "object" ? files[index] : {};
      const tags = []
        .concat(Array.isArray(topic.tags) ? topic.tags : [])
        .concat([
          topic.label,
          file.category && file.category.name,
          file.creator_name,
        ]);

      normalized.push({
        id: clipText(file.id || `${topic.key}-${index}`, 32),
        name: clipText(file.title, 96),
        source: "STOCK",
        href: "",
        tags: tags.map((tag) => clipText(tag, 48)).filter(Boolean).slice(0, 18),
        swatches: [],
        categoryKey: topic.key,
        thumbnailUrl: clipText(file.thumbnail_url, 240),
      });
    }

    return normalized.filter((record) => record.thumbnailUrl);
  }

  function mergeAdobeTrendRecords(records) {
    const source = Array.isArray(records) ? records : [];
    const merged = [];
    const seen = {};

    for (let index = 0; index < source.length; index += 1) {
      const record = normalizeAdobeTrendLiveEntry(source[index]);
      if (!record) {
        continue;
      }
      const key = [record.source, record.id, record.href, record.thumbnailUrl].filter(Boolean).join("::");
      if (!key || seen[key]) {
        continue;
      }
      seen[key] = true;
      merged.push(record);
    }

    return merged.slice(0, 220);
  }

  function normalizeAdobeTrendLiveEntry(value) {
    const source = value && typeof value === "object" ? value : {};
    const swatches = Array.isArray(source.swatches)
      ? source.swatches.map((swatch) => normalizeHexColor(swatch)).filter(Boolean).slice(0, 7)
      : [];
    const thumbnailUrl = clipText(source.thumbnailUrl, 240);
    if (swatches.length < 3 && !thumbnailUrl) {
      return null;
    }

    const tags = Array.isArray(source.tags)
      ? source.tags.map((tag) => clipText(tag, 48)).filter(Boolean).slice(0, 18)
      : [];

    return {
      id: clipText(source.id, 32),
      name: clipText(source.name, 96),
      source: clipText(source.source, 24).toUpperCase(),
      href: clipText(source.href, 180),
      tags: tags,
      swatches: swatches,
      categoryKey: clipText(source.categoryKey, 48).toLowerCase(),
      thumbnailUrl: thumbnailUrl,
    };
  }

  function appendAdobeTrendTag(tags, value) {
    const text = clipText(value, 48);
    if (!text) {
      return;
    }
    const list = Array.isArray(tags) ? tags : [];
    const key = text.toLowerCase();
    for (let index = 0; index < list.length; index += 1) {
      if (clipText(list[index], 48).toLowerCase() === key) {
        return;
      }
    }
    list.push(text);
  }

  function rgbUnitToHex(color) {
    if (!color || typeof color !== "object") {
      return "";
    }
    return rgbToHex(color.r, color.g, color.b);
  }

  function rgbToHex(red, green, blue) {
    const toHex = (value) => {
      const channel = Math.max(0, Math.min(255, Math.round(Number(value) * 255)));
      return channel.toString(16).padStart(2, "0").toUpperCase();
    };
    return "#" + toHex(red) + toHex(green) + toHex(blue);
  }

  function rgbByteToHex(red, green, blue) {
    const toHex = (value) => Math.max(0, Math.min(255, Math.round(Number(value) || 0))).toString(16).padStart(2, "0").toUpperCase();
    return "#" + toHex(red) + toHex(green) + toHex(blue);
  }

  function rgbBytesToHsl(rgb) {
    const red = clampNumber(rgb && rgb.r, 0, 255) / 255;
    const green = clampNumber(rgb && rgb.g, 0, 255) / 255;
    const blue = clampNumber(rgb && rgb.b, 0, 255) / 255;
    const max = Math.max(red, green, blue);
    const min = Math.min(red, green, blue);
    const lightness = (max + min) / 2;
    const delta = max - min;
    let hue = 0;
    let saturation = 0;

    if (delta > 0) {
      saturation = delta / (1 - Math.abs(2 * lightness - 1));
      switch (max) {
        case red:
          hue = ((green - blue) / delta) % 6;
          break;
        case green:
          hue = (blue - red) / delta + 2;
          break;
        default:
          hue = (red - green) / delta + 4;
          break;
      }
      hue *= 60;
    }

    return {
      h: normalizeHue(hue),
      s: clampNumber(saturation, 0, 1),
      l: clampNumber(lightness, 0, 1),
    };
  }

  function hslByteToHex(hue, saturation, lightness) {
    const normalizedHue = normalizeHue(hue);
    const normalizedSaturation = clampNumber(saturation, 0, 1);
    const normalizedLightness = clampNumber(lightness, 0, 1);
    const chroma = (1 - Math.abs(2 * normalizedLightness - 1)) * normalizedSaturation;
    const x = chroma * (1 - Math.abs(((normalizedHue / 60) % 2) - 1));
    const match = normalizedLightness - chroma / 2;
    let red = 0;
    let green = 0;
    let blue = 0;

    if (normalizedHue < 60) {
      red = chroma;
      green = x;
    } else if (normalizedHue < 120) {
      red = x;
      green = chroma;
    } else if (normalizedHue < 180) {
      green = chroma;
      blue = x;
    } else if (normalizedHue < 240) {
      green = x;
      blue = chroma;
    } else if (normalizedHue < 300) {
      red = x;
      blue = chroma;
    } else {
      red = chroma;
      blue = x;
    }

    return rgbByteToHex((red + match) * 255, (green + match) * 255, (blue + match) * 255);
  }

  function hexToRgb(hex) {
    const normalized = normalizeHexColor(hex) || "#000000";
    return {
      r: parseInt(normalized.slice(1, 3), 16),
      g: parseInt(normalized.slice(3, 5), 16),
      b: parseInt(normalized.slice(5, 7), 16),
    };
  }

  function notifyApplyResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const swatchCount =
      typeof summary.swatchCount === "number" && Number.isFinite(summary.swatchCount) ? summary.swatchCount : 0;

    if (!swatchCount) {
      figma.notify("Extract Colors returned an empty result.", { timeout: 2200 });
      return;
    }

    const note = buildAnalysisNote(summary);
    figma.notify((operationLabel || "Extract Colors") + " complete (" + swatchCount + " swatches created" + note + ")", { timeout: 2600 });
  }

  function buildAnalysisNote(summary) {
    const parts = [];

    if (summary && typeof summary.domain === "string" && summary.domain && summary.domain !== "general") {
      parts.push(summary.domain);
    }
    if (summary && typeof summary.brand === "string" && summary.brand && summary.brand !== "none" && summary.brand !== "unknown") {
      parts.push(summary.brand);
    }
    if (summary && typeof summary.intent === "string" && summary.intent && summary.intent !== "ui") {
      parts.push(summary.intent);
    }

    if (!parts.length) {
      return "";
    }

    return ", detected: " + parts.slice(0, 3).join(" / ");
  }

  function postPrepareError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "ai-color-extract-source-error",
      clientRequestId: clientRequestId,
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2400 });
  }

  function postApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "ai-color-extract-apply-error",
      clientRequestId: clientRequestId,
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2400 });
  }

  function notifyUiReportedError(message) {
    const text = normalizeErrorMessage(
      message && message.message ? message.message : "",
      "A browser analysis step failed during Extract Colors."
    );
    figma.notify(text, { error: true, timeout: 2400 });
  }

  function sanitizeClientRequestId(value) {
    const text = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    return text || "";
  }

  function sanitizeOperationLabel(value) {
    const label = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    return label || "Extract Colors";
  }

  function sanitizeFileName(value) {
    const source = typeof value === "string" && value ? value : "color-source";
    const trimmed = source.replace(/[<>:"/\\|?*\u0000-\u001f]/g, " ").replace(/\s+/g, " ").trim();
    return trimmed ? trimmed.slice(0, 80) : "color-source";
  }

  function buildSessionId() {
    return "ai-color-extract-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function compactText(value) {
    return String(value || "").replace(/\s+/g, " ").trim();
  }

  function clipText(value, maxLength) {
    const text = compactText(value);
    if (!text) {
      return "";
    }
    const limit = Number.isFinite(Number(maxLength)) ? Number(maxLength) : 0;
    if (!(limit > 0) || text.length <= limit) {
      return text;
    }
    return text.slice(0, Math.max(1, limit)).trim();
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_IMAGE_UPSCALE_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  let isPreparing = false;
  let isApplying = false;
  let pendingSession = null;
  let isImageExtendPreparing = false;
  let isImageExtendApplying = false;
  let pendingImageExtendSession = null;
  let isCompositePreparing = false;
  let isCompositeApplying = false;
  let pendingCompositeSession = null;
  let isBoundsFitPreparing = false;
  let isBoundsFitApplying = false;
  let pendingBoundsFitSession = null;
  let isReferencePreparing = false;
  let isPromptDraftPreparing = false;
  let isTextOverlayPreparing = false;
  let isTextOverlayApplying = false;
  let pendingTextOverlaySession = null;
  let availableFontsPromise = null;
  const IMAGE_TASK_ALREADY_RUNNING_MESSAGE = "Another image task is already running.";
  const IMAGE_TASK_APPLY_IN_PROGRESS_MESSAGE = "Another image task is already applying a result.";
  const UPSCALE_SOURCE_PREPARE_ERROR_MESSAGE = "Failed to prepare the source image for the AI image task.";
  const UPSCALE_SESSION_EXPIRED_MESSAGE = "The AI image session expired. Please run it again.";
  const UPSCALE_RESULT_BYTES_MISSING_MESSAGE = "No generated image bytes were provided.";
  const UPSCALE_IMAGE_CREATE_ERROR_MESSAGE = "Failed to create an image hash from the generated result.";
  const UPSCALE_APPLY_ERROR_MESSAGE = "Failed to apply the generated AI image result.";
  const BOUNDS_FIT_SOURCE_PREPARE_ERROR_MESSAGE = "Failed to prepare the bounds-fit source.";
  const REFERENCE_SEARCH_SOURCE_PREPARE_ERROR_MESSAGE = "Failed to prepare the reference search source.";
  const REFERENCE_SEARCH_QUERY_REQUIRED_MESSAGE = "Enter a search query before opening reference pages.";
  const REFERENCE_SEARCH_OPENED_MESSAGE_PREFIX = "Opened reference search: ";
  const REFERENCE_SEARCH_OPEN_ERROR_MESSAGE = "Failed to open the reference search pages.";
  const BOUNDS_FIT_APPLY_IN_PROGRESS_MESSAGE = "Bounds fit is already applying a result.";
  const BOUNDS_FIT_SESSION_EXPIRED_MESSAGE = "The bounds-fit session expired. Please run it again.";
  const BOUNDS_FIT_APPLY_ERROR_MESSAGE = "Failed to apply the bounds-fit result.";
  const ORIGINAL_SIZE_FIT_APPLY_ERROR_MESSAGE = "Could not apply Fit Original Size.";
  const UI_REPORTED_IMAGE_ERROR_FALLBACK = "The image task failed before a usable result was returned.";
  const IMAGE_TASK_NO_SELECTION_MESSAGE = "Select at least one node before running this image task.";
  const IMAGE_FILL_NOT_FOUND_MESSAGE = "Could not find the selected IMAGE fill.";
  const IMAGE_FILL_BYTES_MISSING_MESSAGE = "Could not read bytes from the selected IMAGE fill.";
  const IMAGE_FILL_SELECTION_REQUIRED_MESSAGE = "Could not find an IMAGE fill in the current selection.";
  const SHAPE_EDIT_SINGLE_SELECTION_MESSAGE = "Select exactly one editable shape layer to use this mode.";
  const SHAPE_OR_IMAGE_SELECTION_MESSAGE = "Select one editable shape layer or a layer that uses an IMAGE fill.";
  const SHAPE_EXPORT_SOURCE_ERROR_MESSAGE = "Failed to export the selected shape as an image source.";
  const SHAPE_EXPORT_EMPTY_MESSAGE = "The exported shape image was empty.";
  const IMAGE_NODE_UNAVAILABLE_REASON = "The original node is no longer available.";
  const IMAGE_FILL_LIST_EMPTY_REASON = "The node does not have any fills.";
  const IMAGE_FILL_HASH_MISMATCH_REASON = "Could not find a matching IMAGE fill with the original hash.";
  const IMAGE_FILL_UPDATE_ERROR_MESSAGE = "Failed to update the IMAGE fill.";
  const SHAPE_TARGET_UNAVAILABLE_REASON = "Could not find an editable shape layer to apply the generated image.";
  const SHAPE_FILL_APPLY_ERROR_MESSAGE = "Failed to apply the generated image to the shape fill.";
  const REFERENCE_SOURCE_NOT_FOUND_MESSAGE = "Could not find usable text or image content in the selection.";
  const PROMPT_DRAFT_SOURCE_PREPARE_ERROR_MESSAGE = "Failed to prepare the prompt draft source.";
  const PROMPT_DRAFT_SOURCE_NOT_FOUND_MESSAGE = "Select one image, frame, shape, or text layer to generate a prompt.";
  const PROMPT_SMART_SELECTION_MESSAGE = "Select at most one visual layer plus optional text layers for prompt edit/generation.";
  const PROMPT_MULTI_IMAGE_CONTAINER_MESSAGE =
    "Prompt edit/generation supports a frame only when it contains one main image plus optional text. Select a single image layer instead.";
  const PROMPT_TARGET_EXPORT_ERROR_MESSAGE = "Failed to export the selected layer for prompt edit/generation.";
  const PROMPT_TARGET_EXPORT_EMPTY_MESSAGE = "The selected layer export for prompt edit/generation was empty.";
  const PROMPT_PLACEMENT_APPLY_ERROR_MESSAGE = "Failed to place the generated prompt image.";
  const PROMPT_TEXT_ANCHOR_GAP = 24;
  const IMAGE_MERGE_FAST_EXPORT_TIMEOUT_MS = 6000;
  const IMAGE_MERGE_EXPORT_TIMEOUT_MS = 30000;
  const IMAGE_MERGE_PREFERRED_EXPORT_SCALE = 2;
  const IMAGE_MERGE_MAX_EXPORT_PIXEL_COUNT = 16000000;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isAiImageUpscaleMessage(message)) {
      if (message.type === "request-ai-image-upscale-source") {
        await prepareUpscaleSource(message);
        return;
      }

      if (message.type === "request-ai-image-prompt-draft-source") {
        await preparePromptDraftSource(message);
        return;
      }

      if (message.type === "request-ai-image-prompt-size-summary") {
        await postPromptSizeSummary(message);
        return;
      }

      if (message.type === "apply-ai-image-upscaled-image") {
        await applyUpscaledImage(message);
        return;
      }

      if (message.type === "ai-image-upscale-report-error") {
        pendingSession = null;
        notifyUiReportedError(message);
        return;
      }

      if (message.type === "request-image-extend-source") {
        await prepareImageExtendSource(message);
        return;
      }

      if (message.type === "apply-image-extend-underlay") {
        await applyImageExtendUnderlay(message);
        return;
      }

      if (message.type === "image-extend-report-error") {
        pendingImageExtendSession = null;
        notifyUiReportedError(message);
        return;
      }

      if (message.type === "request-image-composite-source") {
        await prepareImageCompositeSource(message);
        return;
      }

      if (message.type === "apply-image-composite-image") {
        await applyImageCompositeImage(message);
        return;
      }

      if (message.type === "image-composite-report-error") {
        pendingCompositeSession = null;
        notifyUiReportedError(message);
        return;
      }

      if (message.type === "run-image-merge") {
        await runImageMerge(message);
        return;
      }

      if (message.type === "run-image-original-size-fit") {
        await runImageOriginalSizeFit(message);
        return;
      }

      if (message.type === "request-image-bounds-fit-source") {
        await prepareImageBoundsFitSource(message);
        return;
      }

      if (message.type === "apply-image-bounds-fit-image") {
        await applyImageBoundsFit(message);
        return;
      }

      if (message.type === "image-bounds-fit-report-error") {
        pendingBoundsFitSession = null;
        notifyUiReportedError(message);
        return;
      }

      if (message.type === "request-image-reference-search-source") {
        await prepareReferenceSearchSource(message);
        return;
      }

      if (message.type === "request-image-text-overlay-source") {
        await prepareImageTextOverlaySource(message);
        return;
      }

      if (message.type === "apply-image-text-overlay") {
        await applyImageTextOverlay(message);
        return;
      }

      if (message.type === "open-image-reference-search") {
        await openImageReferenceSearch(message);
        return;
      }

      if (message.type === "image-text-overlay-report-error") {
        notifyUiReportedError(message);
        return;
      }

      if (message.type === "image-reference-report-error") {
        notifyUiReportedError(message);
        return;
      }
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_AI_IMAGE_UPSCALE_PATCH__ = true;

  function isAiImageUpscaleMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-image-upscale-source" ||
        message.type === "request-ai-image-prompt-draft-source" ||
        message.type === "request-ai-image-prompt-size-summary" ||
        message.type === "apply-ai-image-upscaled-image" ||
        message.type === "ai-image-upscale-report-error" ||
        message.type === "request-image-extend-source" ||
        message.type === "apply-image-extend-underlay" ||
        message.type === "image-extend-report-error" ||
        message.type === "request-image-composite-source" ||
        message.type === "apply-image-composite-image" ||
        message.type === "image-composite-report-error" ||
        message.type === "run-image-merge" ||
        message.type === "run-image-original-size-fit" ||
        message.type === "request-image-bounds-fit-source" ||
        message.type === "apply-image-bounds-fit-image" ||
        message.type === "image-bounds-fit-report-error" ||
        message.type === "request-image-reference-search-source" ||
        message.type === "request-image-text-overlay-source" ||
        message.type === "apply-image-text-overlay" ||
        message.type === "image-text-overlay-report-error" ||
        message.type === "open-image-reference-search" ||
        message.type === "image-reference-report-error")
    );
  }

  async function prepareUpscaleSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postPrepareError(IMAGE_TASK_ALREADY_RUNNING_MESSAGE, sanitizeClientRequestId(message && message.clientRequestId));
      return;
    }

    isPreparing = true;
    pendingSession = null;

    try {
      const target = collectAiImageSourceTargetFromSelection(
        sanitizeSourceMode(message && message.sourceMode),
        message
      );
      const source = await buildAiImageSource(target, {
        preferOriginalImageBytes: shouldPreferOriginalUpscaleSource(message),
      });
      const sessionId = buildSessionId();

      pendingSession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        targetKind: target.kind,
        captureMode: source && source.summary && typeof source.summary.captureMode === "string" ? source.summary.captureMode : "",
        targetNodeId: target.entry && target.entry.nodeId ? target.entry.nodeId : "",
        originalHash: target.kind === "image-fill" ? target.entry.imageHash : "",
        usages: target.kind === "image-fill" ? target.usages : [],
        selectionLabel: formatSelectionLabel(target.selection),
        targetNodeName: target.entry && target.entry.nodeName ? target.entry.nodeName : "",
        targetWidth: target.targetWidth || 0,
        targetHeight: target.targetHeight || 0,
        targetParentId: target.targetParentId || "",
        targetX: target.targetX || 0,
        targetY: target.targetY || 0,
        placementMode: target.placementMode || "",
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        preparedAt: new Date().toISOString(),
      };

      figma.ui.postMessage(buildAiImageSourceReadyPayload(sessionId, pendingSession, source));
    } catch (error) {
      pendingSession = null;
      postPrepareError(
        normalizeErrorMessage(error, UPSCALE_SOURCE_PREPARE_ERROR_MESSAGE, {
          operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        }),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isPreparing = false;
    }
  }

  async function applyUpscaledImage(message) {
    if (
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postApplyError(IMAGE_TASK_APPLY_IN_PROGRESS_MESSAGE, sanitizeClientRequestId(message && message.clientRequestId));
      return;
    }

    isApplying = true;

    try {
      if (!pendingSession || !message || message.sessionId !== pendingSession.id) {
        throw new Error(UPSCALE_SESSION_EXPIRED_MESSAGE);
      }

      const bytes = normalizeBytes(message.bytes);
      if (!bytes.length) {
        throw new Error(UPSCALE_RESULT_BYTES_MISSING_MESSAGE);
      }

      const createdImage = figma.createImage(bytes);
      if (!createdImage || typeof createdImage.hash !== "string" || !createdImage.hash) {
        throw new Error(UPSCALE_IMAGE_CREATE_ERROR_MESSAGE);
      }

      let result = null;
      if (pendingSession.targetKind === "shape-export") {
        result = await applyGeneratedImageToShapeNode(pendingSession, createdImage.hash, bytes.length);
      } else if (pendingSession.targetKind === "container-placement" || pendingSession.targetKind === "new-image-placement") {
        result = await applyGeneratedPromptImagePlacement(pendingSession, createdImage.hash, bytes.length);
      } else {
        result = await replaceSelectionImageFills(pendingSession, createdImage.hash, bytes.length);
      }
      figma.ui.postMessage({
        type: "ai-image-upscale-apply-result",
        clientRequestId: pendingSession.clientRequestId,
        result: result,
      });
      notifyApplyResult(result, sanitizeOperationLabel(message && message.operationLabel) || pendingSession.operationLabel);
      pendingSession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, UPSCALE_APPLY_ERROR_MESSAGE, {
        operationLabel:
          sanitizeOperationLabel(message && message.operationLabel) ||
          (pendingSession && pendingSession.operationLabel) ||
          "",
      });
      figma.ui.postMessage({
        type: "ai-image-upscale-apply-error",
        clientRequestId: pendingSession && pendingSession.clientRequestId ? pendingSession.clientRequestId : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isApplying = false;
    }
  }

  async function prepareImageExtendSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isReferencePreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postImageExtendSourceError(
        "Another image task is already running.",
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isImageExtendPreparing = true;
    pendingImageExtendSession = null;

    try {
      const target = await collectImageExtendTargetFromSelection();
      const requestedExpand = sanitizeImageExtendPadding(message && message.expand);
      const expand = resolveImageExtendEffectivePadding(requestedExpand, target.boundsPadding);
      const sessionId = buildImageExtendSessionId();

      pendingImageExtendSession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        selectionLabel: formatSelectionLabel(target.selection),
        targetNodeId: target.entry.nodeId,
        targetNodeName: target.entry.nodeName,
        originalHash: target.entry.imageHash,
        expand: expand,
        outputMode: sanitizeImageExtendOutputMode(message && message.outputMode),
        imageBounds: target.imageBounds,
        placementMode: target.placementMode || "underlay",
        preparedAt: new Date().toISOString(),
      };

      figma.ui.postMessage(buildImageExtendSourceReadyPayload(sessionId, pendingImageExtendSession, target, expand));
    } catch (error) {
      pendingImageExtendSession = null;
      postImageExtendSourceError(
        normalizeErrorMessage(error, "Failed to prepare the source image for image extend."),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isImageExtendPreparing = false;
    }
  }

  async function applyImageExtendUnderlay(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postImageExtendApplyError(
        "Image extend is already running.",
        pendingImageExtendSession && pendingImageExtendSession.clientRequestId
          ? pendingImageExtendSession.clientRequestId
          : sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isImageExtendApplying = true;

    try {
      if (!pendingImageExtendSession || !message || message.sessionId !== pendingImageExtendSession.id) {
        throw new Error("The image extend session expired. Please run it again.");
      }

      const bytes = normalizeBytes(message.bytes);
      if (!bytes.length) {
        throw new Error("The generated image extend result is empty.");
      }

      const createdImage = figma.createImage(bytes);
      if (!createdImage || typeof createdImage.hash !== "string" || !createdImage.hash) {
        throw new Error("Could not create an image hash for the generated background.");
      }

      const result = await applyImageExtendUnderlayToSelection(pendingImageExtendSession, createdImage.hash, bytes.length);
      figma.ui.postMessage({
        type: "image-extend-apply-result",
        clientRequestId: pendingImageExtendSession.clientRequestId,
        result: result,
      });
      notifyImageExtendResult(result, pendingImageExtendSession.operationLabel);
      pendingImageExtendSession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "Failed to apply the generated image extend result.");
      figma.ui.postMessage({
        type: "image-extend-apply-error",
        clientRequestId:
          pendingImageExtendSession && pendingImageExtendSession.clientRequestId
            ? pendingImageExtendSession.clientRequestId
            : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isImageExtendApplying = false;
    }
  }

  async function prepareImageCompositeSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isReferencePreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postImageCompositeSourceError(
        "Another image task is already running.",
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isCompositePreparing = true;
    pendingCompositeSession = null;

    try {
      const collection = await collectImageCompositeTargetsFromSelection();
      if (collection.layers.length < 2) {
        throw new Error(buildImageCompositeEmptySelectionMessage(collection.layers, collection.skipped));
      }

      const sessionId = buildImageCompositeSessionId();
      pendingCompositeSession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        selectionLabel: formatSelectionLabel(collection.selection),
        unionRect: collection.unionRect,
        selectedRootNodeId: collection.selectedRoot && collection.selectedRoot.id ? collection.selectedRoot.id : "",
        selectedRootNodeName: collection.selectedRoot ? safeName(collection.selectedRoot) : "",
        layerCount: collection.layers.length,
        preparedAt: new Date().toISOString(),
      };

      figma.ui.postMessage(buildImageCompositeSourceReadyPayload(sessionId, pendingCompositeSession, collection));
    } catch (error) {
      pendingCompositeSession = null;
      postImageCompositeSourceError(
        normalizeErrorMessage(error, "Failed to prepare the composite image source."),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isCompositePreparing = false;
    }
  }

  async function applyImageCompositeImage(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postImageCompositeApplyError(
        "Image composite is already running.",
        pendingCompositeSession && pendingCompositeSession.clientRequestId
          ? pendingCompositeSession.clientRequestId
          : sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isCompositeApplying = true;

    try {
      if (!pendingCompositeSession || !message || message.sessionId !== pendingCompositeSession.id) {
        throw new Error("The image composite session expired. Please run it again.");
      }

      const bytes = normalizeBytes(message.bytes);
      if (!bytes.length) {
        throw new Error("The generated composite image is empty.");
      }

      const createdImage = figma.createImage(bytes);
      if (!createdImage || typeof createdImage.hash !== "string" || !createdImage.hash) {
        throw new Error("Could not create an image hash for the composite result.");
      }

      const result = await applyImageCompositeToSelection(pendingCompositeSession, createdImage.hash, bytes.length);
      figma.ui.postMessage({
        type: "image-composite-apply-result",
        clientRequestId: pendingCompositeSession.clientRequestId,
        result: result,
      });
      notifyImageCompositeResult(result, pendingCompositeSession.operationLabel);
      pendingCompositeSession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "Failed to apply the generated composite image.");
      figma.ui.postMessage({
        type: "image-composite-apply-error",
        clientRequestId:
          pendingCompositeSession && pendingCompositeSession.clientRequestId
            ? pendingCompositeSession.clientRequestId
            : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isCompositeApplying = false;
    }
  }

  async function prepareImageBoundsFitSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postBoundsFitSourceError(
        IMAGE_TASK_ALREADY_RUNNING_MESSAGE,
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isBoundsFitPreparing = true;
    pendingBoundsFitSession = null;

    try {
      const collection = await collectBoundsFitTargetsFromSelection();
      if (!collection.targets.length) {
        throw new Error(buildBoundsFitEmptySelectionMessage(collection.skipped));
      }

      const sessionId = buildBoundsFitSessionId();
      pendingBoundsFitSession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        selectionLabel: formatSelectionLabel(collection.selection),
        targets: collection.targets,
        containers: collection.containers,
        skipped: collection.skipped,
        requestedCount: collection.selection.length,
        preparedAt: new Date().toISOString(),
      };

      figma.ui.postMessage({
        type: "image-bounds-fit-source-ready",
        sessionId: sessionId,
        clientRequestId: pendingBoundsFitSession.clientRequestId,
        items: collection.targets
          .filter(function (target) {
            return !target || target.skipRasterAnalysis !== true;
          })
          .map(function (target) {
            return {
              nodeId: target.nodeId,
              nodeName: target.nodeName,
              imageHash: target.originalHash,
              fileName: target.fileName,
              mimeType: target.mimeType,
              bytes: target.bytes,
              sourceWidth: target.sourceWidth,
              sourceHeight: target.sourceHeight,
              analysisOffsetX: target.analysisOffsetX,
              analysisOffsetY: target.analysisOffsetY,
              analysisWidth: target.analysisWidth,
              analysisHeight: target.analysisHeight,
              rasterAnalysisOffsetX: Number.isFinite(Number(target.rasterAnalysisOffsetX))
                ? Number(target.rasterAnalysisOffsetX)
                : Number(target.analysisOffsetX) || 0,
              rasterAnalysisOffsetY: Number.isFinite(Number(target.rasterAnalysisOffsetY))
                ? Number(target.rasterAnalysisOffsetY)
                : Number(target.analysisOffsetY) || 0,
              rasterAnalysisWidth: Number(target.rasterAnalysisWidth) > 0 ? Number(target.rasterAnalysisWidth) : Number(target.analysisWidth) || 0,
              rasterAnalysisHeight:
                Number(target.rasterAnalysisHeight) > 0 ? Number(target.rasterAnalysisHeight) : Number(target.analysisHeight) || 0,
            };
          }),
        summary: {
          selectionLabel: pendingBoundsFitSession.selectionLabel,
          eligibleCount: collection.targets.length + collection.containers.length,
          skippedCount: collection.skipped.length,
        },
      });
    } catch (error) {
      pendingBoundsFitSession = null;
      postBoundsFitSourceError(
        normalizeErrorMessage(error, BOUNDS_FIT_SOURCE_PREPARE_ERROR_MESSAGE),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isBoundsFitPreparing = false;
    }
  }

  async function prepareReferenceSearchSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isReferencePreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postReferenceSearchSourceError(
        IMAGE_TASK_ALREADY_RUNNING_MESSAGE,
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isReferencePreparing = true;

    try {
      const source = await collectReferenceSearchSourceFromSelection();
      figma.ui.postMessage({
        type: "image-reference-search-source-ready",
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        sourceType: source.sourceType,
        text: source.text || "",
        image: source.image || null,
        summary: source.summary,
      });
    } catch (error) {
      postReferenceSearchSourceError(
        normalizeErrorMessage(error, REFERENCE_SEARCH_SOURCE_PREPARE_ERROR_MESSAGE),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isReferencePreparing = false;
    }
  }

  async function preparePromptDraftSource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isReferencePreparing ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postPromptDraftSourceError(
        IMAGE_TASK_ALREADY_RUNNING_MESSAGE,
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isPromptDraftPreparing = true;

    try {
      const source = await collectPromptDraftSourceFromSelection();
      figma.ui.postMessage(buildPromptDraftSourcePayload(source, message));
    } catch (error) {
      postPromptDraftSourceError(
        normalizeErrorMessage(error, PROMPT_DRAFT_SOURCE_PREPARE_ERROR_MESSAGE),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isPromptDraftPreparing = false;
    }
  }

  function buildPromptDraftSourcePayload(source, message) {
    return {
      type: "ai-image-prompt-draft-source-ready",
      clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
      sourceType:
        source && typeof source.sourceType === "string" && source.sourceType ? source.sourceType : "image",
      text: source && typeof source.text === "string" ? source.text : "",
      image: sanitizePromptDraftImagePayload(source && source.image),
      summary: sanitizePromptDraftSummaryPayload(source && source.summary),
    };
  }

  async function postPromptSizeSummary(message) {
    let summary = null;
    try {
      summary = await collectPromptSizeSummaryFromSelection();
    } catch (error) {
      summary = {
        sourceKind: "none",
        selectionCount: 0,
        currentWidth: 0,
        currentHeight: 0,
        sourceWidth: 0,
        sourceHeight: 0,
        message: normalizeErrorMessage(error, ""),
      };
    }

    figma.ui.postMessage({
      type: "ai-image-prompt-size-summary-ready",
      clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
      summary: sanitizePromptSizeSummaryPayload(summary),
    });
  }

  async function collectPromptSizeSummaryFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    const visualNodes = selection.filter(function (node) {
      return !!node && !node.removed && node.type !== "TEXT";
    });
    const rects = [];

    for (let index = 0; index < selection.length; index += 1) {
      const rect = getBoundsFitNodeRect(selection[index]);
      if (rect && Number(rect.width) > 0 && Number(rect.height) > 0) {
        rects.push(rect);
      }
    }

    const unionRect = rects.length ? unionAbsoluteRects(rects) : null;
    const currentWidth = unionRect && Number(unionRect.width) > 0 ? roundBoundsFitMetric(unionRect.width) : 0;
    const currentHeight = unionRect && Number(unionRect.height) > 0 ? roundBoundsFitMetric(unionRect.height) : 0;
    let sourceWidth = currentWidth;
    let sourceHeight = currentHeight;
    let sourceKind = selection.length ? "selection" : "none";

    if (visualNodes.length === 1) {
      sourceKind = "visual-node";
      const fillSize = await readPrimaryImageFillSourceSize(visualNodes[0]);
      if (fillSize && fillSize.width > 0 && fillSize.height > 0) {
        sourceKind = "image-fill";
        sourceWidth = fillSize.width;
        sourceHeight = fillSize.height;
      }
    } else if (visualNodes.length > 1) {
      sourceKind = "multi-selection";
    } else if (selection.length) {
      sourceKind = "text-selection";
    }

    return {
      sourceKind: sourceKind,
      selectionCount: selection.length,
      currentWidth: currentWidth,
      currentHeight: currentHeight,
      sourceWidth: sourceWidth,
      sourceHeight: sourceHeight,
    };
  }

  async function readPrimaryImageFillSourceSize(node) {
    if (!node || node.removed || !hasSimpleVisibleImagePaint(node)) {
      return null;
    }
    const fills = getNodeFills(node);
    const fillIndex = getPrimaryVisibleImageFillIndex(fills);
    if (fillIndex < 0 || !fills[fillIndex] || !fills[fillIndex].imageHash) {
      return null;
    }
    const image = figma.getImageByHash(fills[fillIndex].imageHash);
    if (!image || typeof image.getSizeAsync !== "function") {
      return null;
    }

    try {
      const size = await image.getSizeAsync();
      const width = size && typeof size.width === "number" && Number.isFinite(size.width) ? Math.max(0, Math.round(size.width)) : 0;
      const height =
        size && typeof size.height === "number" && Number.isFinite(size.height) ? Math.max(0, Math.round(size.height)) : 0;
      return width > 0 && height > 0 ? { width: width, height: height } : null;
    } catch (error) {
      return null;
    }
  }

  function sanitizePromptSizeSummaryPayload(summary) {
    const source = summary && typeof summary === "object" ? summary : {};
    const text = typeof source.message === "string" ? source.message : "";
    return {
      sourceKind: typeof source.sourceKind === "string" && source.sourceKind ? source.sourceKind : "none",
      selectionCount: sanitizePromptSizeNumber(source.selectionCount),
      currentWidth: sanitizePromptSizeNumber(source.currentWidth),
      currentHeight: sanitizePromptSizeNumber(source.currentHeight),
      sourceWidth: sanitizePromptSizeNumber(source.sourceWidth),
      sourceHeight: sanitizePromptSizeNumber(source.sourceHeight),
      message: text.slice(0, 160),
    };
  }

  function sanitizePromptSizeNumber(value) {
    const numberValue = Number(value);
    if (!Number.isFinite(numberValue) || numberValue <= 0) {
      return 0;
    }
    return Math.round(numberValue);
  }

  function buildAiImageSourceReadyPayload(sessionId, session, source) {
    return {
      type: "ai-image-upscale-source-ready",
      sessionId: typeof sessionId === "string" ? sessionId : "",
      clientRequestId: session && session.clientRequestId ? session.clientRequestId : "",
      image: sanitizeImageMessagePayload(source && source.image),
      summary: sanitizeImageMessageSummaryPayload(source && source.summary),
      composite: sanitizeCompositeMessagePayload(source && source.composite),
    };
  }

  function buildImageExtendSourceReadyPayload(sessionId, session, target, expand) {
    const sourceImage = target && target.image ? target.image : null;
    const currentWidth = Number(target && target.currentWidth) || 0;
    const currentHeight = Number(target && target.currentHeight) || 0;
    const safeExpand = sanitizeImageExtendExpandPayload(expand);
    return {
      type: "image-extend-source-ready",
      sessionId: typeof sessionId === "string" ? sessionId : "",
      clientRequestId: session && session.clientRequestId ? session.clientRequestId : "",
      image: sanitizeImageMessagePayload(sourceImage),
      summary: sanitizeImageMessageSummaryPayload({
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        targetNodeName: target && target.entry && target.entry.nodeName ? target.entry.nodeName : "",
        captureMode: "image-bounds",
        currentWidth: currentWidth,
        currentHeight: currentHeight,
        targetWidth: currentWidth + safeExpand.left + safeExpand.right,
        targetHeight: currentHeight + safeExpand.top + safeExpand.bottom,
        placementMode: target && target.placementMode === "outside-reference" ? "outside-reference" : "underlay",
        outputMode: session ? sanitizeImageExtendOutputMode(session.outputMode) : "use_ai_only",
        defaultOutputMode: "use_ai_only",
        preserveMode: "soft",
        expandTop: safeExpand.top,
        expandRight: safeExpand.right,
        expandBottom: safeExpand.bottom,
        expandLeft: safeExpand.left,
        expansionSource: safeExpand.source,
        byteLength: sourceImage && sourceImage.bytes ? sourceImage.bytes.length : 0,
      }),
    };
  }

  function buildImageCompositeSourceReadyPayload(sessionId, session, collection) {
    const layers = collection && Array.isArray(collection.layers) ? collection.layers : [];
    const skipped = collection && Array.isArray(collection.skipped) ? collection.skipped : [];
    const unionRect = collection && collection.unionRect ? collection.unionRect : {};
    return {
      type: "image-composite-source-ready",
      sessionId: typeof sessionId === "string" ? sessionId : "",
      clientRequestId: session && session.clientRequestId ? session.clientRequestId : "",
      layers: layers.map(sanitizeCompositeLayerMessagePayload).filter(Boolean),
      summary: sanitizeImageMessageSummaryPayload({
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        layerCount: layers.length,
        skippedCount: skipped.length,
        backgroundNodeName:
          session && session.selectedRootNodeName
            ? session.selectedRootNodeName
            : layers[0] && layers[0].nodeName
              ? layers[0].nodeName
              : "",
        foregroundCount: Math.max(0, layers.length - 1),
        canvasWidth: unionRect.width,
        canvasHeight: unionRect.height,
      }),
    };
  }

  function sanitizePromptDraftImagePayload(image) {
    return sanitizeImageMessagePayload(image);
  }

  function sanitizeImageMessagePayload(image) {
    if (!image || typeof image !== "object") {
      return null;
    }

    const bytes = normalizeBytes(image.bytes);
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      return null;
    }

    return {
      bytes: bytes,
      mimeType: typeof image.mimeType === "string" && image.mimeType ? image.mimeType : "image/png",
      fileName: typeof image.fileName === "string" && image.fileName ? image.fileName : "figma-image.png",
    };
  }

  function sanitizePromptDraftSummaryPayload(summary) {
    return sanitizeImageMessageSummaryPayload(summary);
  }

  function sanitizeImageMessageSummaryPayload(summary) {
    if (!summary || typeof summary !== "object") {
      return {};
    }

    const payload = {};
    const textKeys = [
      "selectionLabel",
      "targetNodeName",
      "captureMode",
      "requestMode",
      "pipelineMode",
      "placementMode",
      "textMode",
      "imageHash",
      "expansionSource",
      "backgroundNodeName",
    ];
    const numberKeys = [
      "targetFillCount",
      "targetWidth",
      "targetHeight",
      "sourceWidth",
      "sourceHeight",
      "currentWidth",
      "currentHeight",
      "byteLength",
      "textLength",
      "expandTop",
      "expandRight",
      "expandBottom",
      "expandLeft",
      "layerCount",
      "skippedCount",
      "foregroundCount",
      "canvasWidth",
      "canvasHeight",
    ];

    for (let index = 0; index < textKeys.length; index += 1) {
      const key = textKeys[index];
      if (typeof summary[key] === "string") {
        payload[key] = summary[key];
      }
    }

    for (let index = 0; index < numberKeys.length; index += 1) {
      const key = numberKeys[index];
      const value = Number(summary[key]);
      if (Number.isFinite(value)) {
        payload[key] = value;
      }
    }

    return payload;
  }

  function sanitizeCompositeMessagePayload(composite) {
    if (!composite || typeof composite !== "object") {
      return null;
    }

    const layers = Array.isArray(composite.layers)
      ? composite.layers.map(sanitizeCompositeLayerMessagePayload).filter(Boolean)
      : [];
    if (!layers.length) {
      return null;
    }

    return {
      layers: layers,
      summary: sanitizeImageMessageSummaryPayload(composite.summary),
    };
  }

  function sanitizeCompositeLayerMessagePayload(layer) {
    if (!layer || typeof layer !== "object") {
      return null;
    }

    const bytes = normalizeBytes(layer.bytes);
    if (!bytes.length) {
      return null;
    }

    return {
      nodeId: typeof layer.nodeId === "string" ? layer.nodeId : "",
      nodeName: typeof layer.nodeName === "string" ? layer.nodeName : "",
      role: typeof layer.role === "string" ? layer.role : "",
      orderIndex: Number.isFinite(Number(layer.orderIndex)) ? Number(layer.orderIndex) : 0,
      fileName: typeof layer.fileName === "string" && layer.fileName ? layer.fileName : "figma-image.png",
      mimeType: typeof layer.mimeType === "string" && layer.mimeType ? layer.mimeType : "image/png",
      bytes: bytes,
      layerKind: typeof layer.layerKind === "string" && layer.layerKind ? layer.layerKind : "image",
      nodeType: typeof layer.nodeType === "string" ? layer.nodeType : "",
      textContent: typeof layer.textContent === "string" ? layer.textContent : "",
      offsetX: Number.isFinite(Number(layer.offsetX)) ? Number(layer.offsetX) : 0,
      offsetY: Number.isFinite(Number(layer.offsetY)) ? Number(layer.offsetY) : 0,
      width: Number.isFinite(Number(layer.width)) ? Number(layer.width) : 0,
      height: Number.isFinite(Number(layer.height)) ? Number(layer.height) : 0,
    };
  }

  function sanitizeImageExtendExpandPayload(expand) {
    return {
      top: sanitizeImageExtendPaddingValue(expand && expand.top),
      right: sanitizeImageExtendPaddingValue(expand && expand.right),
      bottom: sanitizeImageExtendPaddingValue(expand && expand.bottom),
      left: sanitizeImageExtendPaddingValue(expand && expand.left),
      source: typeof (expand && expand.source) === "string" && expand.source ? expand.source : "prompt",
    };
  }

  async function prepareImageTextOverlaySource(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isReferencePreparing ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postTextOverlaySourceError(
        "Another image task is already running.",
        sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isTextOverlayPreparing = true;
    pendingTextOverlaySession = null;

    try {
      const prepared = await collectImageTextOverlaySourceFromSelection(message);
      pendingTextOverlaySession = prepared.overlaySession;
      figma.ui.postMessage({
        type: "image-text-overlay-source-ready",
        sessionId: prepared.overlaySession ? prepared.overlaySession.id : "",
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        sourceType: prepared.source.sourceType,
        text: prepared.source.text || "",
        image: prepared.source.image || null,
        summary: prepared.source.summary || {},
      });
    } catch (error) {
      pendingTextOverlaySession = null;
      postTextOverlaySourceError(
        normalizeErrorMessage(error, "Failed to prepare the text overlay source."),
        sanitizeClientRequestId(message && message.clientRequestId)
      );
    } finally {
      isTextOverlayPreparing = false;
    }
  }

  async function applyImageTextOverlay(message) {
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isReferencePreparing ||
      isPromptDraftPreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postTextOverlayApplyError(
        "Text overlay is already running.",
        pendingTextOverlaySession && pendingTextOverlaySession.clientRequestId
          ? pendingTextOverlaySession.clientRequestId
          : sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isTextOverlayApplying = true;

    try {
      if (!pendingTextOverlaySession || !message || message.sessionId !== pendingTextOverlaySession.id) {
        throw new Error("The text overlay session expired. Please run it again.");
      }

      const lines = sanitizeImageTextOverlayLines(message && message.lines);
      if (!lines.length) {
        throw new Error("No detected text lines were provided.");
      }

      const result = await applyImageTextOverlayToSelection(pendingTextOverlaySession, lines);
      figma.ui.postMessage({
        type: "image-text-overlay-apply-result",
        clientRequestId: pendingTextOverlaySession.clientRequestId,
        result: result,
      });
      notifyImageTextOverlayResult(
        result,
        sanitizeOperationLabel(message && message.operationLabel) || pendingTextOverlaySession.operationLabel
      );
      pendingTextOverlaySession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "Failed to create the text overlay.");
      figma.ui.postMessage({
        type: "image-text-overlay-apply-error",
        clientRequestId:
          pendingTextOverlaySession && pendingTextOverlaySession.clientRequestId
            ? pendingTextOverlaySession.clientRequestId
            : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isTextOverlayApplying = false;
    }
  }

  async function openImageReferenceSearch(message) {
    const clientRequestId = sanitizeClientRequestId(message && message.clientRequestId);
    try {
      const query = sanitizeReferenceSearchQuery(message && message.query);
      if (!query) {
        throw new Error(REFERENCE_SEARCH_QUERY_REQUIRED_MESSAGE);
      }

      const encodedQuery = encodeURIComponent(query);
      const dribbbleUrl = "https://dribbble.com/search/" + encodedQuery;
      const behanceUrl = "https://www.behance.net/search/projects/" + encodedQuery;
      const pinterestUrl = "https://www.pinterest.com/search/pins/?q=" + encodedQuery;
      figma.openExternal(dribbbleUrl);
      figma.openExternal(behanceUrl);
      figma.openExternal(pinterestUrl);
      figma.ui.postMessage({
        type: "image-reference-search-opened",
        clientRequestId: clientRequestId,
        query: query,
        urls: [dribbbleUrl, behanceUrl, pinterestUrl],
      });
      figma.notify(REFERENCE_SEARCH_OPENED_MESSAGE_PREFIX + summarizeReferenceSearchQuery(query), { timeout: 2400 });
    } catch (error) {
      postReferenceSearchOpenError(
        normalizeErrorMessage(error, REFERENCE_SEARCH_OPEN_ERROR_MESSAGE),
        clientRequestId
      );
    }
  }

  async function runImageOriginalSizeFit(message) {
    const clientRequestId = sanitizeClientRequestId(message && message.clientRequestId);
    const operationLabel = normalizeOriginalSizeFitOperationLabel(
      sanitizeOperationLabel(message && message.operationLabel)
    );
    if (
      isPreparing ||
      isApplying ||
      isImageExtendPreparing ||
      isImageExtendApplying ||
      isCompositePreparing ||
      isCompositeApplying ||
      isBoundsFitPreparing ||
      isBoundsFitApplying ||
      isPromptDraftPreparing ||
      isReferencePreparing ||
      isTextOverlayPreparing ||
      isTextOverlayApplying
    ) {
      postOriginalSizeFitError(IMAGE_TASK_ALREADY_RUNNING_MESSAGE, clientRequestId);
      return;
    }

    isBoundsFitApplying = true;

    try {
      const result = await applyOriginalSizeFitToSelection(message);
      figma.ui.postMessage({
        type: "image-original-size-fit-result",
        clientRequestId: clientRequestId,
        result: result,
      });
      notifyOriginalSizeFitResult(result, operationLabel);
    } catch (error) {
      const messageText = normalizeErrorMessage(error, ORIGINAL_SIZE_FIT_APPLY_ERROR_MESSAGE, {
        operationLabel: "Fit Original Size",
        operationKind: "original-size-fit",
      });
      postOriginalSizeFitError(messageText, clientRequestId);
    } finally {
      isBoundsFitApplying = false;
    }
  }

  async function applyImageBoundsFit(message) {
    if (isPreparing || isApplying || isImageExtendPreparing || isImageExtendApplying || isBoundsFitApplying) {
      postBoundsFitApplyError(
        BOUNDS_FIT_APPLY_IN_PROGRESS_MESSAGE,
        pendingBoundsFitSession && pendingBoundsFitSession.clientRequestId
          ? pendingBoundsFitSession.clientRequestId
          : sanitizeClientRequestId(message && message.clientRequestId)
      );
      return;
    }

    isBoundsFitApplying = true;

    try {
      if (!pendingBoundsFitSession || !message || message.sessionId !== pendingBoundsFitSession.id) {
        throw new Error(BOUNDS_FIT_SESSION_EXPIRED_MESSAGE);
      }

      const result = await applyBoundsFitResultsToSelection(pendingBoundsFitSession, message.results);
      figma.ui.postMessage({
        type: "image-bounds-fit-apply-result",
        clientRequestId: pendingBoundsFitSession.clientRequestId,
        result: result,
      });
      notifyBoundsFitResult(result, pendingBoundsFitSession.operationLabel);
      pendingBoundsFitSession = null;
    } catch (error) {
      const messageText = normalizeErrorMessage(error, BOUNDS_FIT_APPLY_ERROR_MESSAGE);
      figma.ui.postMessage({
        type: "image-bounds-fit-apply-error",
        clientRequestId:
          pendingBoundsFitSession && pendingBoundsFitSession.clientRequestId
            ? pendingBoundsFitSession.clientRequestId
            : sanitizeClientRequestId(message && message.clientRequestId),
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    } finally {
      isBoundsFitApplying = false;
    }
  }

  function notifyUiReportedError(message) {
    const text = toKoreanImageErrorMessage(
      message && typeof message.message === "string" && message.message.trim()
        ? message.message.trim()
        : UI_REPORTED_IMAGE_ERROR_FALLBACK,
      UI_REPORTED_IMAGE_ERROR_FALLBACK,
      {
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        operationKind: message && typeof message.operationKind === "string" ? message.operationKind : "",
      }
    );
    figma.notify(text, { error: true, timeout: 2600 });
  }

  function postPrepareError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "ai-image-upscale-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postPromptDraftSourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "ai-image-prompt-draft-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "ai-image-upscale-apply-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postImageExtendSourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-extend-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postImageExtendApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-extend-apply-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postImageCompositeSourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-composite-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postImageCompositeApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-composite-apply-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postBoundsFitSourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-bounds-fit-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postBoundsFitApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-bounds-fit-apply-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postOriginalSizeFitError(message, clientRequestId) {
    const messageText = toKoreanImageErrorMessage(
      message,
      ORIGINAL_SIZE_FIT_APPLY_ERROR_MESSAGE,
      {
        operationLabel: "Fit Original Size",
        operationKind: "original-size-fit",
      }
    );
    figma.ui.postMessage({
      type: "image-original-size-fit-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: messageText,
    });
    figma.notify(messageText, { error: true, timeout: 2600 });
  }

  function postReferenceSearchSourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-reference-search-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postReferenceSearchOpenError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-reference-search-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postTextOverlaySourceError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-text-overlay-source-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function postTextOverlayApplyError(message, clientRequestId) {
    figma.ui.postMessage({
      type: "image-text-overlay-apply-error",
      clientRequestId: sanitizeClientRequestId(clientRequestId),
      message: message,
    });
    figma.notify(message, { error: true, timeout: 2600 });
  }

  function collectAiImageSourceTargetFromSelection(sourceMode, message) {
    if (sanitizeSourceMode(sourceMode) === "prompt-smart") {
      return collectPromptSmartTargetFromSelection(message);
    }

    try {
      const imageTarget = collectSingleUpscaleTargetFromSelection();
      imageTarget.kind = "image-fill";
      return imageTarget;
    } catch (error) {
      if (sanitizeSourceMode(sourceMode) !== "shape-or-image") {
        throw error;
      }
      return collectSingleShapeEditTargetFromSelection();
    }
  }

  function collectPromptSmartTargetFromSelection(message) {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    const outputSize = sanitizePromptOutputSize(message && message.requestedOutputSize);
    const visualNodes = selection.filter(function (node) {
      return !!node && !node.removed && node.type !== "TEXT";
    });

    if (!visualNodes.length) {
      return buildPromptPlacementTarget(selection, outputSize);
    }

    if (visualNodes.length > 1) {
      throw new Error(PROMPT_SMART_SELECTION_MESSAGE);
    }

    const node = visualNodes[0];
    if ("locked" in node && node.locked) {
      throw new Error("Locked layers are not supported for prompt edit/generation.");
    }

    const directImageTarget = collectDirectImageFillTarget(node, selection);
    if (directImageTarget) {
      return directImageTarget;
    }

    if (isPromptEditableShapeNode(node)) {
      const localBounds = getImageExtendLocalBounds(node);
      return {
        kind: "shape-export",
        selection: selection,
        entry: {
          nodeId: node.id,
          nodeName: safeName(node),
          path: safeName(node),
        },
        targetWidth: localBounds ? localBounds.width : roundBoundsFitMetric(node.width),
        targetHeight: localBounds ? localBounds.height : roundBoundsFitMetric(node.height),
      };
    }

    if (!isPromptSmartContainerTarget(node)) {
      throw new Error(PROMPT_SMART_SELECTION_MESSAGE);
    }

    return {
      kind: "container-placement",
      selection: selection,
      entry: {
        nodeId: node.id,
        nodeName: safeName(node),
        path: safeName(node),
      },
      targetWidth: roundBoundsFitMetric(node.width),
      targetHeight: roundBoundsFitMetric(node.height),
      placementMode: hasChildren(node) ? "outside-reference" : "inside-target",
    };
  }

  function buildPromptPlacementTarget(selection, outputSize) {
    const dimensions = buildPromptPlacementDimensions(outputSize);
    const target = {
      kind: "new-image-placement",
      selection: Array.isArray(selection) ? selection.slice() : [],
      entry: {
        nodeId: "",
        nodeName: Array.isArray(selection) && selection.length ? safeName(selection[0]) : "Viewport center",
        path: "",
      },
      targetWidth: dimensions.width,
      targetHeight: dimensions.height,
    };

    if (Array.isArray(selection) && selection.length === 1 && selection[0] && selection[0].type === "TEXT") {
      const textNode = selection[0];
      const textRect = getBoundsFitNodeRect(textNode);
      target.entry.nodeId = textNode.id;
      target.entry.nodeName = safeName(textNode);
      target.entry.path = safeName(textNode);
      target.targetParentId = textNode.parent && typeof textNode.parent.id === "string" ? textNode.parent.id : "";
      target.targetX = textRect ? textRect.x : 0;
      target.targetY = textRect ? textRect.y : 0;
      target.placementMode = "below-selected-text";
    }

    return target;
  }

  function collectDirectImageFillTarget(node, selection) {
    if (!node || node.removed || !hasSimpleVisibleImagePaint(node)) {
      return null;
    }

    const fills = getNodeFills(node);
    const fillIndex = getPrimaryVisibleImageFillIndex(fills);
    if (fillIndex < 0) {
      return null;
    }

    const fill = fills[fillIndex];
    if (!fill || !fill.imageHash) {
      return null;
    }

    return {
      kind: "image-fill",
      selection: Array.isArray(selection) ? selection.slice() : [node],
      entry: {
        imageHash: fill.imageHash,
        nodeId: node.id,
        nodeName: safeName(node),
        path: safeName(node),
      },
      usages: [
        {
          nodeId: node.id,
          nodeName: safeName(node),
          fillIndex: fillIndex,
          imageHash: fill.imageHash,
          path: safeName(node),
        },
      ],
      targetWidth: roundBoundsFitMetric(node.width),
      targetHeight: roundBoundsFitMetric(node.height),
    };
  }

  function isPromptSmartContainerTarget(node) {
    return (
      !!node &&
      !node.removed &&
      node.type !== "TEXT" &&
      (!("locked" in node) || !node.locked) &&
      typeof node.exportAsync === "function" &&
      typeof node.width === "number" &&
      typeof node.height === "number" &&
      node.width > 0 &&
      node.height > 0
    );
  }

  async function buildAiImageSource(target, options) {
    if (target && target.kind === "shape-export") {
      return await buildShapeEditSource(target);
    }
    if (target && target.kind === "container-placement") {
      return await buildContainerPromptSource(target);
    }
    if (target && target.kind === "new-image-placement") {
      return buildPromptPlacementSource(target);
    }
    return await buildUpscaleImageSource(target, options);
  }

  function buildPromptPlacementSource(target) {
    return {
      image: null,
      summary: {
        selectionLabel: formatSelectionLabel(target.selection),
        targetNodeName: target.entry && target.entry.nodeName ? target.entry.nodeName : "Viewport center",
        targetWidth: target.targetWidth || 0,
        targetHeight: target.targetHeight || 0,
        placementMode: "viewport-center",
        requestMode: "prompt-only",
        byteLength: 0,
      },
    };
  }

  async function buildContainerPromptSource(target) {
    const node = await figma.getNodeByIdAsync(target.entry.nodeId);
    if (!isPromptSmartContainerTarget(node)) {
      throw new Error(PROMPT_TARGET_EXPORT_ERROR_MESSAGE);
    }

    const bytes = await node.exportAsync({
      format: "PNG",
      useAbsoluteBounds: false,
    });
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error(PROMPT_TARGET_EXPORT_EMPTY_MESSAGE);
    }

    return {
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: node.id,
          },
          "png"
        ),
      },
      summary: {
        selectionLabel: formatSelectionLabel(target.selection),
        targetNodeName: safeName(node),
        captureMode: "rendered-node",
        targetWidth: roundBoundsFitMetric(node.width),
        targetHeight: roundBoundsFitMetric(node.height),
        requestMode: "image-edit",
        byteLength: bytes.length,
        pipelineMode: target.placementMode === "outside-reference" ? "reference-placement" : "upscale",
        placementMode: target.placementMode || "",
      },
    };
  }

  async function buildUpscaleImageSource(target, options) {
    const targetNode = await figma.getNodeByIdAsync(target.entry.nodeId);
    const preferOriginalImageBytes = !!(options && options.preferOriginalImageBytes);
    const sourceFill = resolveUpscaleSourceFill(targetNode, target);
    const requiresRenderedSource = sourceFill ? await isRenderedUpscaleSourceRequired(targetNode, sourceFill) : false;
    const localBounds = getImageExtendLocalBounds(targetNode);
    let sourceWidth = 0;
    let sourceHeight = 0;
    if (target && target.entry && target.entry.imageHash) {
      const sourceImage = figma.getImageByHash(target.entry.imageHash);
      if (sourceImage && typeof sourceImage.getSizeAsync === "function") {
        try {
          const size = await sourceImage.getSizeAsync();
          sourceWidth = size && typeof size.width === "number" && Number.isFinite(size.width) ? Math.max(0, Math.round(size.width)) : 0;
          sourceHeight =
            size && typeof size.height === "number" && Number.isFinite(size.height) ? Math.max(0, Math.round(size.height)) : 0;
        } catch (error) {}
      }
    }
    const summary = {
      selectionLabel: formatSelectionLabel(target.selection),
      targetNodeName: target.entry.nodeName,
      targetFillCount: target.usages.length,
      imageHash: target.entry.imageHash,
      captureMode: "rendered-node",
      targetWidth: localBounds ? localBounds.width : 0,
      targetHeight: localBounds ? localBounds.height : 0,
      sourceWidth: sourceWidth,
      sourceHeight: sourceHeight,
      byteLength: 0,
    };
    let bytes = new Uint8Array(0);
    let mimeType = "image/png";
    let fileName = buildFileName(target.entry, "png");

    if (!preferOriginalImageBytes && targetNode && !targetNode.removed && typeof targetNode.exportAsync === "function") {
      try {
        bytes = await targetNode.exportAsync({
          format: "PNG",
          useAbsoluteBounds: false,
        });
      } catch (error) {
        bytes = new Uint8Array(0);
      }
    }

    if (!bytes.length) {
      if (requiresRenderedSource && !preferOriginalImageBytes) {
        throw new Error("Could not export the currently visible image bytes for upscale.");
      }
      const image = figma.getImageByHash(target.entry.imageHash);
      if (!image) {
        throw new Error(IMAGE_FILL_NOT_FOUND_MESSAGE);
      }
      bytes = await image.getBytesAsync();
      if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
        throw new Error(IMAGE_FILL_BYTES_MISSING_MESSAGE);
      }
      const extension = detectImageExtension(bytes);
      mimeType = detectImageMimeType(bytes);
      fileName = buildFileName(target.entry, extension === "bin" ? "png" : extension);
      summary.captureMode = "source-image";
    }

    summary.byteLength = bytes.length;

    return {
      image: {
        bytes: bytes,
        mimeType: mimeType,
        fileName: fileName,
      },
      summary: summary,
    };
  }

  function resolveUpscaleSourceFill(node, target) {
    if (!node || node.removed) {
      return null;
    }
    const fills = getNodeFills(node);
    if (!fills.length) {
      return null;
    }
    const preferredUsage = target && Array.isArray(target.usages) && target.usages.length ? target.usages[0] : null;
    if (
      preferredUsage &&
      Number.isInteger(preferredUsage.fillIndex) &&
      preferredUsage.fillIndex >= 0 &&
      preferredUsage.fillIndex < fills.length
    ) {
      const directFill = fills[preferredUsage.fillIndex];
      if (isImagePaint(directFill) && directFill.imageHash === target.entry.imageHash) {
        return directFill;
      }
    }
    for (let index = 0; index < fills.length; index += 1) {
      const fill = fills[index];
      if (isImagePaint(fill) && fill.imageHash === target.entry.imageHash) {
        return fill;
      }
    }
    return null;
  }

  async function isRenderedUpscaleSourceRequired(node, fill) {
    if (!node || !fill || !fill.imageHash) {
      return false;
    }
    const nodeWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const nodeHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    const image = figma.getImageByHash(fill.imageHash);
    if (!image) {
      return false;
    }
    const size = await image.getSizeAsync();
    const sourceWidth = size && typeof size.width === "number" ? size.width : 0;
    const sourceHeight = size && typeof size.height === "number" ? size.height : 0;
    if (!(nodeWidth > 0) || !(nodeHeight > 0) || !(sourceWidth > 0) || !(sourceHeight > 0)) {
      return false;
    }
    const scaleMode = typeof fill.scaleMode === "string" ? fill.scaleMode : "FILL";
    const fillRotation = typeof fill.rotation === "number" && Number.isFinite(fill.rotation) ? fill.rotation : 0;
    return (
      scaleMode === "CROP" ||
      scaleMode === "TILE" ||
      !!fill.imageTransform ||
      Math.abs(fillRotation) > 0.01 ||
      !hasMatchingAspectRatio(nodeWidth, nodeHeight, sourceWidth, sourceHeight)
    );
  }

  function collectSingleUpscaleTargetFromSelection(selectionInput) {
    const selection = Array.isArray(selectionInput) ? selectionInput.filter(Boolean) : Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error(IMAGE_TASK_NO_SELECTION_MESSAGE);
    }

    let selectedEntry = null;
    const usages = [];

    for (let rootIndex = 0; rootIndex < selection.length; rootIndex += 1) {
      const root = selection[rootIndex];
      const stack = [
        {
          node: root,
          path: safeName(root),
        },
      ];

      while (stack.length > 0) {
        const current = stack.pop();
        const node = current && current.node;
        if (!node || node.removed) {
          continue;
        }

        const fills = getNodeFills(node);
        const fillIndices = getFillIndicesInUiOrder(fills);
        for (let orderedFillIndex = 0; orderedFillIndex < fillIndices.length; orderedFillIndex += 1) {
          const fillIndex = fillIndices[orderedFillIndex];
          const fill = fills[fillIndex];
          if (!isImagePaint(fill)) {
            continue;
          }

          if (!fill.imageHash || typeof fill.imageHash !== "string") {
            continue;
          }

          if (!selectedEntry) {
            selectedEntry = {
              imageHash: fill.imageHash,
              nodeId: node.id,
              nodeName: safeName(node),
              path: current.path,
            };
          }

          if (fill.imageHash === selectedEntry.imageHash) {
            usages.push({
              nodeId: node.id,
              nodeName: safeName(node),
              fillIndex: fillIndex,
              imageHash: fill.imageHash,
              path: current.path,
            });
          }
        }

        if (!hasChildren(node)) {
          continue;
        }

        for (let childIndex = node.children.length - 1; childIndex >= 0; childIndex -= 1) {
          const child = node.children[childIndex];
          stack.push({
            node: child,
            path: current.path + " / " + safeName(child),
          });
        }
      }
    }

    if (!selectedEntry || !usages.length) {
      throw new Error(IMAGE_FILL_SELECTION_REQUIRED_MESSAGE);
    }

    if (false) {
      throw new Error(IMAGE_FILL_SELECTION_REQUIRED_MESSAGE);
    }

    return {
      selection: selection,
      entry: selectedEntry,
      usages: usages,
    };
  }

  function collectSingleShapeEditTargetFromSelection(selectionInput) {
    const selection = Array.isArray(selectionInput) ? selectionInput.filter(Boolean) : Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error(IMAGE_TASK_NO_SELECTION_MESSAGE);
    }

    if (selection.length !== 1) {
      throw new Error(SHAPE_EDIT_SINGLE_SELECTION_MESSAGE);
    }

    const node = selection[0];
    if (!isPromptEditableShapeNode(node)) {
      throw new Error(SHAPE_OR_IMAGE_SELECTION_MESSAGE);
    }

    return {
      kind: "shape-export",
      selection: selection,
      entry: {
        nodeId: node.id,
        nodeName: safeName(node),
        path: safeName(node),
      },
    };
  }

  async function buildShapeEditSource(target) {
    const node = await figma.getNodeByIdAsync(target.entry.nodeId);
    if (!isPromptEditableShapeNode(node) || typeof node.exportAsync !== "function") {
      throw new Error(SHAPE_EXPORT_SOURCE_ERROR_MESSAGE);
    }
    const localBounds = getImageExtendLocalBounds(node);

    let bytes = new Uint8Array(0);
    if (shapeEditNeedsPlaceholderSource(node)) {
      bytes = await exportShapeEditPlaceholderPng(node);
    } else {
      bytes = await node.exportAsync({
        format: "PNG",
        useAbsoluteBounds: false,
      });
    }
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error(SHAPE_EXPORT_EMPTY_MESSAGE);
    }

    return {
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: target.entry.nodeName,
            imageHash: target.entry.nodeId,
          },
          "png"
        ),
      },
      summary: {
        selectionLabel: formatSelectionLabel(target.selection),
        targetNodeName: target.entry.nodeName,
        targetFillCount: 1,
        imageHash: "",
        captureMode: "rendered-node",
        targetWidth: localBounds ? localBounds.width : 0,
        targetHeight: localBounds ? localBounds.height : 0,
        byteLength: bytes.length,
      },
    };
  }

  async function collectImageExtendTargetFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("Select a frame, group, or layer first.");
    }

    if (selection.length !== 1) {
      throw new Error("Image extend requires exactly one selected image layer.");
    }

    const node = selection[0];
    if (!node || node.removed) {
      throw new Error("The selected layer could not be read.");
    }

    if ("locked" in node && node.locked) {
      throw new Error("Locked layers are not supported for image extend.");
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      throw new Error("Rotated image layers are not supported yet.");
    }

    if (!hasSimpleVisibleImagePaint(node)) {
      if (isImageExtendReferencePlacementNode(node)) {
        return await collectImageExtendReferencePlacementTarget(node, selection);
      }
      throw new Error("Image extend requires one image layer, text layer, or composite frame.");
    }

    if (!node.parent || !canUseImageExtendParent(node.parent)) {
      throw new Error("The current parent container does not support inserting the extended background.");
    }

    if (
      "layoutMode" in node.parent &&
      typeof node.parent.layoutMode === "string" &&
      node.parent.layoutMode !== "NONE"
    ) {
      throw new Error("Image extend does not support auto-layout parents yet.");
    }

    const fills = getNodeFills(node);
    const fillIndex = getPrimaryVisibleImageFillIndex(fills);
    if (fillIndex < 0) {
      throw new Error("Could not find an IMAGE fill on the selected layer.");
    }

    const fill = fills[fillIndex];
    const width = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(width > 0) || !(height > 0)) {
      throw new Error("Could not determine the current layer size.");
    }

    const imageBounds = await resolveImageExtendImageLocalBounds(node, fill, width, height);
    const boundsPadding = buildImageExtendBoundsPadding(imageBounds, width, height);
    const bytes = await exportImageExtendSourcePng(node, imageBounds);
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error("Could not export the selected image layer as PNG.");
    }

    return {
      selection: selection,
      entry: {
        nodeId: node.id,
        nodeName: safeName(node),
        imageHash: fill.imageHash || "",
      },
      currentWidth: imageBounds.width,
      currentHeight: imageBounds.height,
      imageBounds: imageBounds,
      boundsPadding: boundsPadding,
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: fill.imageHash || node.id,
          },
          "png"
        ),
      },
    };
  }

  function isImageExtendReferencePlacementNode(node) {
    return (
      !!node &&
      !node.removed &&
      (node.type === "TEXT" || hasChildren(node)) &&
      typeof node.exportAsync === "function" &&
      typeof node.width === "number" &&
      typeof node.height === "number" &&
      node.width > 0 &&
      node.height > 0
    );
  }

  async function collectImageExtendReferencePlacementTarget(node, selection) {
    const width = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(width > 0) || !(height > 0)) {
      throw new Error("Could not determine the current layer size.");
    }

    const imageBounds = buildImageExtendFullLocalBounds(width, height);
    const bytes = await node.exportAsync({
      format: "PNG",
      useAbsoluteBounds: false,
    });
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error("Could not export the selected layer as PNG.");
    }

    return {
      selection: selection,
      placementMode: "outside-reference",
      entry: {
        nodeId: node.id,
        nodeName: safeName(node),
        imageHash: "",
      },
      currentWidth: imageBounds.width,
      currentHeight: imageBounds.height,
      imageBounds: imageBounds,
      boundsPadding: { top: 0, right: 0, bottom: 0, left: 0 },
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: node.id,
          },
          "png"
        ),
      },
    };
  }

  async function resolveImageExtendImageLocalBounds(node, fill, fallbackWidth, fallbackHeight) {
    const nodeWidth = Math.max(1, Number(fallbackWidth) || 1);
    const nodeHeight = Math.max(1, Number(fallbackHeight) || 1);
    const fullBounds = buildImageExtendFullLocalBounds(nodeWidth, nodeHeight);
    if (!fill || !isImagePaint(fill)) {
      return fullBounds;
    }

    const fillRotation = typeof fill.rotation === "number" && Number.isFinite(fill.rotation) ? fill.rotation : 0;
    if (Math.abs(fillRotation) > 0.01 || !!fill.imageTransform) {
      return fullBounds;
    }

    const scaleMode = typeof fill.scaleMode === "string" ? fill.scaleMode : "FILL";
    if (scaleMode !== "FIT") {
      return fullBounds;
    }

    const sourceSize = await getImageExtendSourceSize(fill);
    if (!sourceSize) {
      return fullBounds;
    }

    const scale = Math.min(nodeWidth / sourceSize.width, nodeHeight / sourceSize.height);
    if (!(scale > 0)) {
      return fullBounds;
    }

    const imageWidth = Math.max(1, roundBoundsFitMetric(sourceSize.width * scale));
    const imageHeight = Math.max(1, roundBoundsFitMetric(sourceSize.height * scale));
    return normalizeImageExtendLocalBounds(
      {
        x: roundBoundsFitMetric((nodeWidth - imageWidth) / 2),
        y: roundBoundsFitMetric((nodeHeight - imageHeight) / 2),
        width: imageWidth,
        height: imageHeight,
      },
      nodeWidth,
      nodeHeight
    );
  }

  async function getImageExtendSourceSize(fill) {
    if (!fill || !fill.imageHash) {
      return null;
    }

    const image = figma.getImageByHash(fill.imageHash);
    if (!image || typeof image.getSizeAsync !== "function") {
      return null;
    }

    try {
      const size = await image.getSizeAsync();
      const width = size && typeof size.width === "number" && Number.isFinite(size.width) ? size.width : 0;
      const height = size && typeof size.height === "number" && Number.isFinite(size.height) ? size.height : 0;
      if (!(width > 0) || !(height > 0)) {
        return null;
      }
      return {
        width: width,
        height: height,
      };
    } catch (error) {
      return null;
    }
  }

  function buildImageExtendFullLocalBounds(width, height) {
    return {
      x: 0,
      y: 0,
      width: Math.max(1, roundBoundsFitMetric(width)),
      height: Math.max(1, roundBoundsFitMetric(height)),
    };
  }

  function normalizeImageExtendLocalBounds(value, fallbackWidth, fallbackHeight) {
    const fallback = buildImageExtendFullLocalBounds(fallbackWidth, fallbackHeight);
    if (!value || typeof value !== "object") {
      return fallback;
    }

    const x = Number(value.x);
    const y = Number(value.y);
    const width = Number(value.width);
    const height = Number(value.height);
    if (!Number.isFinite(x) || !Number.isFinite(y) || !(width > 0) || !(height > 0)) {
      return fallback;
    }

    return {
      x: roundBoundsFitMetric(x),
      y: roundBoundsFitMetric(y),
      width: Math.max(1, roundBoundsFitMetric(width)),
      height: Math.max(1, roundBoundsFitMetric(height)),
    };
  }

  function areImageExtendLocalBoundsFullNode(bounds, width, height) {
    const normalized = normalizeImageExtendLocalBounds(bounds, width, height);
    return (
      Math.abs(normalized.x) <= 0.01 &&
      Math.abs(normalized.y) <= 0.01 &&
      Math.abs(normalized.width - Math.max(1, Number(width) || 1)) <= 0.01 &&
      Math.abs(normalized.height - Math.max(1, Number(height) || 1)) <= 0.01
    );
  }

  async function exportImageExtendSourcePng(node, imageBounds) {
    if (!node || typeof node.exportAsync !== "function") {
      throw new Error("Could not export the selected image layer for image extend.");
    }

    const nodeWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const nodeHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    const bounds = normalizeImageExtendLocalBounds(imageBounds, nodeWidth, nodeHeight);
    if (areImageExtendLocalBoundsFullNode(bounds, nodeWidth, nodeHeight)) {
      return await node.exportAsync({
        format: "PNG",
        useAbsoluteBounds: false,
      });
    }

    const preview = figma.createFrame();
    let clone = null;
    try {
      preview.name = "__pigma-image-extend-source-bounds__";
      preview.resize(bounds.width, bounds.height);
      preview.clipsContent = true;
      preview.fills = [];
      preview.strokes = [];
      figma.currentPage.appendChild(preview);

      clone = node.clone();
      preview.appendChild(clone);
      setImageExtendNodePosition(clone, -bounds.x, -bounds.y);

      return await preview.exportAsync({
        format: "PNG",
        useAbsoluteBounds: false,
      });
    } finally {
      if (clone && !clone.removed) {
        clone.remove();
      }
      if (preview && !preview.removed) {
        preview.remove();
      }
    }
  }

  function sanitizeImageExtendPadding(value) {
    const source = value && typeof value === "object" ? value : {};
    const next = {
      top: sanitizeImageExtendPaddingValue(source.top),
      right: sanitizeImageExtendPaddingValue(source.right),
      bottom: sanitizeImageExtendPaddingValue(source.bottom),
      left: sanitizeImageExtendPaddingValue(source.left),
    };

    if (!(next.top > 0 || next.right > 0 || next.bottom > 0 || next.left > 0)) {
      throw new Error("Enter at least one positive expand value.");
    }

    return next;
  }

  function buildImageExtendBoundsPadding(imageBounds, nodeWidth, nodeHeight) {
    const bounds = normalizeImageExtendLocalBounds(imageBounds, nodeWidth, nodeHeight);
    const width = Math.max(1, Number(nodeWidth) || 1);
    const height = Math.max(1, Number(nodeHeight) || 1);
    return {
      top: sanitizeImageExtendPaddingValue(bounds.y),
      right: sanitizeImageExtendPaddingValue(width - (bounds.x + bounds.width)),
      bottom: sanitizeImageExtendPaddingValue(height - (bounds.y + bounds.height)),
      left: sanitizeImageExtendPaddingValue(bounds.x),
    };
  }

  function hasPositiveImageExtendPadding(padding) {
    return !!padding && (padding.top > 0 || padding.right > 0 || padding.bottom > 0 || padding.left > 0);
  }

  function sanitizeImageExtendOutputMode(value) {
    return "use_ai_only";
  }

  function resolveImageExtendEffectivePadding(requestedPadding, boundsPadding) {
    const requested = requestedPadding || { top: 0, right: 0, bottom: 0, left: 0 };
    const bounds = boundsPadding || { top: 0, right: 0, bottom: 0, left: 0 };
    if (hasPositiveImageExtendPadding(bounds)) {
      return {
        top: sanitizeImageExtendPaddingValue(bounds.top),
        right: sanitizeImageExtendPaddingValue(bounds.right),
        bottom: sanitizeImageExtendPaddingValue(bounds.bottom),
        left: sanitizeImageExtendPaddingValue(bounds.left),
        source: "image-bounds",
      };
    }
    return {
      top: sanitizeImageExtendPaddingValue(requested.top),
      right: sanitizeImageExtendPaddingValue(requested.right),
      bottom: sanitizeImageExtendPaddingValue(requested.bottom),
      left: sanitizeImageExtendPaddingValue(requested.left),
      source: "prompt",
    };
  }

  function sanitizeImageExtendPaddingValue(value) {
    const numeric = Math.floor(Number(value) || 0);
    if (!(numeric > 0)) {
      return 0;
    }
    return Math.min(4096, numeric);
  }

  function canUseImageExtendParent(parent) {
    if (!parent || parent.removed) {
      return false;
    }

    if (!("children" in parent) || !Array.isArray(parent.children) || typeof parent.insertChild !== "function") {
      return false;
    }

    if (parent.type === "INSTANCE" || parent.type === "COMPONENT" || parent.type === "COMPONENT_SET") {
      return false;
    }

    return true;
  }

  async function resolveImageExtendCurrentImageBounds(node, session, localBounds) {
    const width = localBounds && localBounds.width > 0 ? localBounds.width : typeof node.width === "number" ? node.width : 0;
    const height = localBounds && localBounds.height > 0 ? localBounds.height : typeof node.height === "number" ? node.height : 0;
    const fallback = normalizeImageExtendLocalBounds(session && session.imageBounds, width, height);
    const fills = getNodeFills(node);
    let fill = null;
    const originalHash = session && typeof session.originalHash === "string" ? session.originalHash : "";

    if (originalHash) {
      const fillIndex = findVisibleImageFillIndexByHash(fills, originalHash);
      if (fillIndex >= 0) {
        fill = fills[fillIndex];
      }
    }

    if (!fill) {
      const primaryFillIndex = getPrimaryVisibleImageFillIndex(fills);
      if (primaryFillIndex >= 0) {
        fill = fills[primaryFillIndex];
      }
    }

    if (!fill) {
      return fallback;
    }

    return await resolveImageExtendImageLocalBounds(node, fill, width, height);
  }

  async function applyImageExtendUnderlayToSelection(session, newImageHash, byteLength) {
    const skipped = [];
    const node = await figma.getNodeByIdAsync(session.targetNodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: session.targetNodeId,
        nodeName: session.targetNodeName,
        reason: "Could not find the layer again.",
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Rotated image layers are not supported yet.",
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }

    if (session && session.placementMode === "outside-reference") {
      return applyImageExtendOutsideReference(session, node, newImageHash, byteLength, skipped);
    }

    if (!node.parent || !canUseImageExtendParent(node.parent)) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "The current parent container does not support inserting the extended background.",
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }

    if (
      "layoutMode" in node.parent &&
      typeof node.parent.layoutMode === "string" &&
      node.parent.layoutMode !== "NONE"
    ) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Image extend does not support auto-layout parents yet.",
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }

    const localBounds = getImageExtendLocalBounds(node);
    if (!localBounds) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Could not calculate the layer position.",
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }

    const parent = node.parent;
    const expand = session.expand || { top: 0, right: 0, bottom: 0, left: 0 };
    const imageBounds = await resolveImageExtendCurrentImageBounds(node, session, localBounds);
    const nextX = localBounds.x + imageBounds.x - expand.left;
    const nextY = localBounds.y + imageBounds.y - expand.top;
    const nextWidth = Math.max(1, roundBoundsFitMetric(imageBounds.width + expand.left + expand.right));
    const nextHeight = Math.max(1, roundBoundsFitMetric(imageBounds.height + expand.top + expand.bottom));
    const outputMode = sanitizeImageExtendOutputMode(session && session.outputMode);
    let aiExpandedLayer = null;
    let maskLayer = null;
    let group = null;
    const originalVisible = "visible" in node ? node.visible : true;

    try {
      aiExpandedLayer = createImageExtendResultRectangle("AI Expanded Layer", newImageHash, nextWidth, nextHeight);
      setImageExtendNodePosition(aiExpandedLayer, nextX, nextY);

      const nodeIndex = findNodeChildIndex(parent, node.id);
      if (nodeIndex < 0) {
        throw new Error("Could not find the layer inside its current parent.");
      }

      if (outputMode === "use_ai_only") {
        parent.insertChild(nodeIndex + 1, aiExpandedLayer);
        if ("visible" in node) {
          node.visible = false;
        }
        figma.currentPage.selection = [aiExpandedLayer];
        return buildImageExtendApplyResult(session, aiExpandedLayer, byteLength, skipped);
      }

      maskLayer = createImageExtendExpansionMaskLayer(imageBounds, expand, nextWidth, nextHeight);
      if (maskLayer) {
        setImageExtendNodePosition(maskLayer, nextX, nextY);
      }

      parent.insertChild(nodeIndex, aiExpandedLayer);
      if (maskLayer) {
        parent.insertChild(nodeIndex + 1, maskLayer);
      }
      group = figma.group(maskLayer ? [aiExpandedLayer, maskLayer, node] : [aiExpandedLayer, node], parent);
      group.name = safeName(node) + " / image extension layers";
      figma.currentPage.selection = [group];

      return buildImageExtendApplyResult(session, group, byteLength, skipped);
    } catch (error) {
      if (maskLayer && !maskLayer.removed && maskLayer.parent) {
        maskLayer.remove();
      }
      if (aiExpandedLayer && !aiExpandedLayer.removed && aiExpandedLayer.parent) {
        aiExpandedLayer.remove();
      }
      if ("visible" in node) {
        node.visible = originalVisible;
      }
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, "Failed to apply the generated image extend result."),
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }
  }

  function createImageExtendResultRectangle(name, imageHash, width, height) {
    const node = figma.createRectangle();
    node.name = name || "AI Expanded Layer";
    node.resize(Math.max(1, roundBoundsFitMetric(width)), Math.max(1, roundBoundsFitMetric(height)));
    node.fills = [buildVisibleImageFill(imageHash)];
    node.strokes = [];
    if ("cornerRadius" in node) {
      node.cornerRadius = 0;
    }
    return node;
  }

  function createImageExtendMaskBand(name, x, y, width, height) {
    const safeWidth = Math.max(0, roundBoundsFitMetric(width));
    const safeHeight = Math.max(0, roundBoundsFitMetric(height));
    if (!(safeWidth > 0) || !(safeHeight > 0)) {
      return null;
    }

    const band = figma.createRectangle();
    band.name = name;
    band.resize(Math.max(1, safeWidth), Math.max(1, safeHeight));
    band.fills = [
      {
        type: "SOLID",
        color: { r: 1, g: 1, b: 1 },
        visible: true,
      },
    ];
    band.strokes = [];
    setImageExtendNodePosition(band, roundBoundsFitMetric(x), roundBoundsFitMetric(y));
    return band;
  }

  function createImageExtendExpansionMaskLayer(imageBounds, expand, width, height) {
    const safeBounds = normalizeImageExtendLocalBounds(imageBounds, width, height);
    const safeExpand = expand || { top: 0, right: 0, bottom: 0, left: 0 };
    const safeWidth = Math.max(1, roundBoundsFitMetric(width));
    const safeHeight = Math.max(1, roundBoundsFitMetric(height));
    const maskFrame = figma.createFrame();
    maskFrame.name = "Expansion Mask Layer";
    maskFrame.resize(safeWidth, safeHeight);
    maskFrame.fills = [];
    maskFrame.strokes = [];
    maskFrame.clipsContent = true;
    if ("visible" in maskFrame) {
      maskFrame.visible = false;
    }

    const bands = [
      createImageExtendMaskBand("Mask / Top", 0, 0, safeWidth, safeExpand.top),
      createImageExtendMaskBand(
        "Mask / Bottom",
        0,
        safeExpand.top + safeBounds.height,
        safeWidth,
        safeExpand.bottom
      ),
      createImageExtendMaskBand("Mask / Left", 0, safeExpand.top, safeExpand.left, safeBounds.height),
      createImageExtendMaskBand(
        "Mask / Right",
        safeExpand.left + safeBounds.width,
        safeExpand.top,
        safeExpand.right,
        safeBounds.height
      ),
    ].filter(Boolean);

    if (!bands.length) {
      if (!maskFrame.removed) {
        maskFrame.remove();
      }
      return null;
    }

    bands.forEach((band) => {
      maskFrame.appendChild(band);
    });
    return maskFrame;
  }

  function applyImageExtendOutsideReference(session, targetNode, newImageHash, byteLength, skipped) {
    let resultNode = null;
    try {
      const localBounds = getImageExtendLocalBounds(targetNode);
      const absoluteRect = getBoundsFitNodeRect(targetNode);
      const expand = session.expand || { top: 0, right: 0, bottom: 0, left: 0 };
      const imageBounds = normalizeImageExtendLocalBounds(
        session.imageBounds,
        localBounds && localBounds.width > 0 ? localBounds.width : targetNode.width,
        localBounds && localBounds.height > 0 ? localBounds.height : targetNode.height
      );
      const nextWidth = Math.max(1, roundBoundsFitMetric(imageBounds.width + expand.left + expand.right));
      const nextHeight = Math.max(1, roundBoundsFitMetric(imageBounds.height + expand.top + expand.bottom));

      resultNode = figma.createRectangle();
      resultNode.name = safeName(targetNode) + " / extended";
      resultNode.resize(nextWidth, nextHeight);
      resultNode.fills = [buildVisibleImageFill(newImageHash)];
      resultNode.strokes = [];
      if ("cornerRadius" in resultNode) {
        resultNode.cornerRadius = 0;
      }

      if (
        targetNode.parent &&
        canUseImageExtendParent(targetNode.parent) &&
        !isAutoLayoutNode(targetNode.parent) &&
        localBounds
      ) {
        const parent = targetNode.parent;
        const insertIndex = findNodeChildIndex(parent, targetNode.id);
        if (insertIndex >= 0) {
          parent.insertChild(insertIndex + 1, resultNode);
        } else {
          parent.insertChild(parent.children.length, resultNode);
        }
        setImageExtendNodePosition(resultNode, localBounds.x + localBounds.width + PROMPT_TEXT_ANCHOR_GAP, localBounds.y);
      } else if (absoluteRect) {
        figma.currentPage.appendChild(resultNode);
        resultNode.x = roundBoundsFitMetric(absoluteRect.x + absoluteRect.width + PROMPT_TEXT_ANCHOR_GAP);
        resultNode.y = roundBoundsFitMetric(absoluteRect.y);
      } else {
        const viewportCenter = figma.viewport && figma.viewport.center ? figma.viewport.center : { x: 0, y: 0 };
        figma.currentPage.appendChild(resultNode);
        resultNode.x = roundBoundsFitMetric(viewportCenter.x - resultNode.width / 2);
        resultNode.y = roundBoundsFitMetric(viewportCenter.y - resultNode.height / 2);
      }

      figma.currentPage.selection = [resultNode];
      if (typeof figma.viewport.scrollAndZoomIntoView === "function") {
        figma.viewport.scrollAndZoomIntoView([resultNode]);
      }
      return buildImageExtendApplyResult(session, resultNode, byteLength, skipped);
    } catch (error) {
      if (resultNode && !resultNode.removed && resultNode.parent) {
        resultNode.remove();
      }
      skipped.push({
        nodeId: targetNode && targetNode.id ? targetNode.id : session.targetNodeId,
        nodeName: targetNode ? safeName(targetNode) : session.targetNodeName,
        reason: normalizeErrorMessage(error, "Failed to place the generated image extend result."),
      });
      return buildImageExtendApplyResult(session, null, byteLength, skipped);
    }
  }

  function buildImageExtendApplyResult(session, group, byteLength, skipped) {
    const appliedCount = group ? 1 : 0;
    const groupName = group && typeof group.name === "string" ? group.name : "";
    const groupId = group && typeof group.id === "string" ? group.id : "";
    const expand = session && session.expand ? session.expand : { top: 0, right: 0, bottom: 0, left: 0 };
    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        targetNodeName: session && session.targetNodeName ? session.targetNodeName : "",
        appliedCount: appliedCount,
        skippedCount: Array.isArray(skipped) ? skipped.length : 0,
        groupId: groupId,
        groupName: groupName,
        expandTop: expand.top,
        expandRight: expand.right,
        expandBottom: expand.bottom,
        expandLeft: expand.left,
        outputMode: session ? sanitizeImageExtendOutputMode(session.outputMode) : "use_ai_only",
        resultByteLength: byteLength,
      },
      skipped: Array.isArray(skipped) ? skipped.slice(0, 24) : [],
    };
  }

  function buildImageCompositeApplyResult(session, resultNode, byteLength, skipped, placementMode) {
    const appliedCount = resultNode ? 1 : 0;
    const nodeName = resultNode && typeof resultNode.name === "string" ? resultNode.name : "";
    const nodeId = resultNode && typeof resultNode.id === "string" ? resultNode.id : "";
    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        appliedCount: appliedCount,
        skippedCount: Array.isArray(skipped) ? skipped.length : 0,
        resultNodeId: nodeId,
        resultNodeName: nodeName,
        placementMode: typeof placementMode === "string" ? placementMode : "",
        resultByteLength: byteLength,
        exportScale: session && Number(session.exportScale) > 0 ? Number(session.exportScale) : 1,
      },
      skipped: Array.isArray(skipped) ? skipped.slice(0, 24) : [],
    };
  }

  async function runImageMerge(message) {
    const clientRequestId = message && typeof message.clientRequestId === "string" ? message.clientRequestId : "";
    try {
      const source = await exportImageMergeSelection();
      const createdImage = figma.createImage(source.bytes);
      if (!createdImage || !createdImage.hash) {
        throw new Error("이미지 합치기 결과를 Figma 이미지로 만들지 못했습니다.");
      }

      const result = await applyImageCompositeToSelection(
        {
          selectionLabel: source.selectionLabel,
          selectedRootNodeId: source.selectedRootNodeId,
          selectedRootNodeName: source.selectedRootNodeName,
          unionRect: source.unionRect,
          exportScale: source.exportScale,
          preserveSelectedRootTransform: source.preserveSelectedRootTransform,
          selectedRootWidth: source.selectedRootWidth,
          selectedRootHeight: source.selectedRootHeight,
          placementAnchorNodeId: source.placementAnchorNodeId,
          placementAnchorNodeName: source.placementAnchorNodeName,
        },
        createdImage.hash,
        source.bytes.length
      );

      const appliedCount = result && result.summary && Number(result.summary.appliedCount) > 0 ? 1 : 0;
      if (!appliedCount) {
        const failureMessage = buildImageMergeFailureMessage(result);
        figma.ui.postMessage({
          type: "image-merge-error",
          clientRequestId: clientRequestId,
          message: failureMessage,
          result: result,
        });
        figma.notify(failureMessage, { error: true, timeout: 3200 });
        return;
      }

      figma.ui.postMessage({
        type: "image-merge-result",
        clientRequestId: clientRequestId,
        result: result,
      });

      figma.notify("이미지 합치기 레이어를 만들었습니다.", {
        timeout: 2200,
      });
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "이미지 합치기에 실패했습니다.");
      figma.ui.postMessage({
        type: "image-merge-error",
        clientRequestId: clientRequestId,
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2600 });
    }
  }

  function buildImageMergeFailureMessage(result) {
    const skipped = result && Array.isArray(result.skipped) ? result.skipped : [];
    for (let index = 0; index < skipped.length; index += 1) {
      const reason = skipped[index] && typeof skipped[index].reason === "string" ? skipped[index].reason.trim() : "";
      if (reason) {
        return "이미지 합치기 레이어를 만들지 못했습니다: " + reason;
      }
    }

    const summary = result && result.summary ? result.summary : {};
    const byteLength = Number(summary.resultByteLength) || 0;
    if (byteLength > 0) {
      return "이미지 합치기 PNG는 생성됐지만 새 레이어로 배치하지 못했습니다.";
    }

    return "이미지 합치기 레이어를 만들지 못했습니다.";
  }

  function withImageMergeTimeout(promise, timeoutMs, label) {
    const ms = Number(timeoutMs) || 0;
    if (ms <= 0 || typeof setTimeout !== "function" || typeof clearTimeout !== "function") {
      return Promise.resolve(promise);
    }

    let settled = false;
    let timerId = null;
    const taskLabel = typeof label === "string" && label.trim() ? label.trim() : "이미지 합치기";
    return new Promise(function (resolve, reject) {
      timerId = setTimeout(function () {
        if (settled) {
          return;
        }
        settled = true;
        reject(new Error(taskLabel + " 시간이 초과되었습니다. 선택 범위를 줄이거나 복잡한 마스크 그룹은 프레임 단위로 다시 시도해주세요."));
      }, ms);

      Promise.resolve(promise).then(
        function (value) {
          if (settled) {
            return;
          }
          settled = true;
          clearTimeout(timerId);
          resolve(value);
        },
        function (error) {
          if (settled) {
            return;
          }
          settled = true;
          clearTimeout(timerId);
          reject(error);
        }
      );
    });
  }

  async function exportImageMergeSelection() {
    const selection = Array.from(figma.currentPage.selection || []).filter(function (node) {
      return isImageMergeExportableNode(node);
    });
    if (!selection.length) {
      throw new Error("이미지로 병합할 프레임, 그룹, 레이어를 먼저 선택해주세요.");
    }

    const exportSelection = resolveImageMergeExportSelection(selection);

    const rects = exportSelection
      .map(function (node) {
        return getImageMergeNodeRect(node);
      })
      .filter(Boolean);
    const unionRect = unionAbsoluteRects(rects);
    if (!unionRect || !(unionRect.width > 0) || !(unionRect.height > 0)) {
      throw new Error("선택한 항목의 병합 영역을 계산하지 못했습니다.");
    }

    const exportScale = resolveImageMergeExportScale(unionRect);
    const exportSettings = buildImageMergeExportSettings(exportScale);

    const bytes =
      (await tryExportImageMergeDirectSelection(exportSelection, exportSettings)) ||
      (await exportImageMergeFrameSelection(exportSelection, unionRect, exportSettings));

    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error("이미지 합치기 PNG가 비어 있습니다.");
    }

    return {
      selectionLabel: formatSelectionLabel(selection),
      placementAnchorNodeId:
        selection.length === 1 && selection[0] && typeof selection[0].id === "string" ? selection[0].id : "",
      placementAnchorNodeName: selection.length === 1 ? safeName(selection[0]) : "",
      selectedRootNodeId:
        exportSelection.length === 1 && exportSelection[0] && typeof exportSelection[0].id === "string"
          ? exportSelection[0].id
          : "",
      selectedRootNodeName: exportSelection.length === 1 ? safeName(exportSelection[0]) : "",
      preserveSelectedRootTransform: exportSelection.length === 1,
      selectedRootWidth:
        exportSelection.length === 1 &&
        typeof exportSelection[0].width === "number" &&
        Number.isFinite(exportSelection[0].width)
          ? exportSelection[0].width
          : 0,
      selectedRootHeight:
        exportSelection.length === 1 &&
        typeof exportSelection[0].height === "number" &&
        Number.isFinite(exportSelection[0].height)
          ? exportSelection[0].height
          : 0,
      unionRect: unionRect,
      exportScale: exportScale,
      bytes: bytes,
    };
  }

  function buildImageMergeExportSettings(exportScale) {
    const settings = {
      format: "PNG",
      useAbsoluteBounds: true,
    };
    const scale = Number(exportScale) || 1;
    if (Math.abs(scale - 1) > 0.01) {
      settings.constraint = {
        type: "SCALE",
        value: scale,
      };
    }
    return settings;
  }

  function resolveImageMergeExportScale(unionRect) {
    const width = unionRect && Number(unionRect.width) > 0 ? Number(unionRect.width) : 0;
    const height = unionRect && Number(unionRect.height) > 0 ? Number(unionRect.height) : 0;
    const area = width * height;
    if (!(area > 0) || !Number.isFinite(area)) {
      return 1;
    }

    const maxScale = Math.sqrt(IMAGE_MERGE_MAX_EXPORT_PIXEL_COUNT / area);
    const scale = Math.min(IMAGE_MERGE_PREFERRED_EXPORT_SCALE, maxScale);
    if (scale >= 1.75) {
      return 2;
    }
    if (scale >= 1.35) {
      return 1.5;
    }
    return 1;
  }

  function resolveImageMergeExportSelection(selection) {
    if (!Array.isArray(selection) || !selection.length) {
      return [];
    }

    const result = [];
    const seen = {};
    for (let index = 0; index < selection.length; index += 1) {
      const node = selection[index];
      const exportNode = findImageMergeMaskContainer(node) || node;
      if (!isImageMergeExportableNode(exportNode) || seen[exportNode.id]) {
        continue;
      }
      seen[exportNode.id] = true;
      result.push(exportNode);
    }

    return result.length ? result : selection;
  }

  function isImageMergeExportableNode(node) {
    return (
      !!node &&
      !node.removed &&
      node.type !== "PAGE" &&
      node.type !== "DOCUMENT" &&
      typeof node.exportAsync === "function"
    );
  }

  async function tryExportImageMergeDirectSelection(selection, exportSettings) {
    if (!Array.isArray(selection) || selection.length !== 1 || !isImageMergeExportableNode(selection[0])) {
      return null;
    }

    try {
      const bytes = await withImageMergeTimeout(
        selection[0].exportAsync(exportSettings),
        IMAGE_MERGE_FAST_EXPORT_TIMEOUT_MS,
        "이미지 합치기 빠른 export"
      );
      return bytes && typeof bytes.length === "number" && bytes.length > 0 ? bytes : null;
    } catch (error) {
      console.warn("[pigma] image merge direct export fallback:", normalizeErrorMessage(error, "direct export failed"));
      return null;
    }
  }

  function hasImageMergeChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children) && node.children.length > 0;
  }

  function findImageMergeMaskContainer(node) {
    let current = node;
    while (current && current.parent && current.parent.type !== "DOCUMENT") {
      if (isImageMergeExportableNode(current) && hasImageMergeDirectMaskChild(current)) {
        return current;
      }

      const parent = current.parent;
      if (!parent || parent.type === "PAGE" || parent.type === "DOCUMENT") {
        break;
      }

      if (isImageMergeExportableNode(parent) && hasImageMergeDirectMaskChild(parent)) {
        return parent;
      }

      current = parent;
    }

    return null;
  }

  function hasImageMergeDirectMaskChild(node) {
    if (!node || !("children" in node) || !Array.isArray(node.children)) {
      return false;
    }

    for (let index = 0; index < node.children.length; index += 1) {
      const child = node.children[index];
      if (child && child.isMask === true) {
        return true;
      }
    }

    return false;
  }

  function hasImageMergeMaskDescendant(node, depth) {
    if (!node || depth > 8 || !hasImageMergeChildren(node)) {
      return false;
    }

    for (let index = 0; index < node.children.length; index += 1) {
      const child = node.children[index];
      if (!child || child.removed) {
        continue;
      }
      if (child.isMask === true || hasImageMergeMaskDescendant(child, depth + 1)) {
        return true;
      }
    }

    return false;
  }

  function getImageMergeNodeRect(node) {
    return getBoundsFitNodeRect(node) || getImageMergeChildrenUnionRect(node, 0);
  }

  function getImageMergeChildrenUnionRect(node, depth) {
    if (!node || depth > 8 || !("children" in node) || !Array.isArray(node.children)) {
      return null;
    }

    const rects = [];
    for (let index = 0; index < node.children.length; index += 1) {
      const child = node.children[index];
      if (!child || child.removed || child.visible === false) {
        continue;
      }

      const rect = getBoundsFitNodeRect(child) || getImageMergeChildrenUnionRect(child, depth + 1);
      if (rect) {
        rects.push(rect);
      }
    }

    return unionAbsoluteRects(rects);
  }

  function positionImageMergePreviewClone(sourceNode, clonedNode, unionRect, fallbackRect) {
    if (
      "relativeTransform" in clonedNode &&
      Array.isArray(clonedNode.relativeTransform) &&
      Array.isArray(sourceNode.absoluteTransform) &&
      sourceNode.absoluteTransform.length >= 2
    ) {
      const row0 = Array.isArray(sourceNode.absoluteTransform[0]) ? sourceNode.absoluteTransform[0] : null;
      const row1 = Array.isArray(sourceNode.absoluteTransform[1]) ? sourceNode.absoluteTransform[1] : null;
      if (row0 && row1 && row0.length >= 3 && row1.length >= 3) {
        clonedNode.relativeTransform = [
          [
            Number(row0[0]) || 0,
            Number(row0[1]) || 0,
            roundBoundsFitMetric((Number(row0[2]) || 0) - unionRect.x),
          ],
          [
            Number(row1[0]) || 0,
            Number(row1[1]) || 0,
            roundBoundsFitMetric((Number(row1[2]) || 0) - unionRect.y),
          ],
        ];
        return;
      }
    }

    if ("x" in clonedNode && typeof clonedNode.x === "number" && "y" in clonedNode && typeof clonedNode.y === "number") {
      clonedNode.x = roundBoundsFitMetric(fallbackRect.x - unionRect.x);
      clonedNode.y = roundBoundsFitMetric(fallbackRect.y - unionRect.y);
      return;
    }

    throw new Error("Could not position the preview clone for image merge.");
  }

  async function exportImageMergeFrameSelection(selection, unionRect, exportSettings) {
    const preview = figma.createFrame();
    const clones = [];
    try {
      preview.name = "__pigma-image-merge-preview__";
      preview.resize(unionRect.width, unionRect.height);
      preview.clipsContent = true;
      preview.fills = [];
      preview.strokes = [];
      preview.x = unionRect.x;
      preview.y = unionRect.y;
      figma.currentPage.appendChild(preview);

      const orderedSelection = selection.slice().sort(compareSceneNodeCanvasOrder);
      for (let index = 0; index < orderedSelection.length; index += 1) {
        const node = orderedSelection[index];
        if (!isImageMergeExportableNode(node)) {
          continue;
        }
        const rect = getImageMergeNodeRect(node);
        if (!rect) {
          continue;
        }
        const clone = node.clone();
        preview.appendChild(clone);
        clones.push(clone);
        positionImageMergePreviewClone(node, clone, unionRect, rect);
      }

      if (!clones.length) {
        throw new Error("병합 가능한 선택 레이어를 찾지 못했습니다.");
      }

      return await withImageMergeTimeout(
        preview.exportAsync(exportSettings),
        IMAGE_MERGE_EXPORT_TIMEOUT_MS,
        "이미지 합치기 preview export"
      );
    } finally {
      for (let index = 0; index < clones.length; index += 1) {
        if (clones[index] && !clones[index].removed) {
          clones[index].remove();
        }
      }
      if (preview && !preview.removed) {
        preview.remove();
      }
    }
  }

  async function applyImageCompositeToSelection(session, newImageHash, byteLength) {
    const skipped = [];
    const unionRect = session && session.unionRect ? session.unionRect : null;
    if (!unionRect || !(unionRect.width > 0) || !(unionRect.height > 0)) {
      skipped.push({
        nodeId: "",
        nodeName: "",
        reason: "Could not calculate the composite placement bounds.",
      });
      return buildImageCompositeApplyResult(session, null, byteLength, skipped, "");
    }
    let resultNode = null;
    let focusNode = null;
    let placementMode = "";

    try {
      resultNode = figma.createRectangle();
      resultNode.name = buildImageCompositeResultName(session && session.selectionLabel ? session.selectionLabel : "");
      const resultWidth =
        session && session.preserveSelectedRootTransform && Number(session.selectedRootWidth) > 0
          ? Number(session.selectedRootWidth)
          : unionRect.width;
      const resultHeight =
        session && session.preserveSelectedRootTransform && Number(session.selectedRootHeight) > 0
          ? Number(session.selectedRootHeight)
          : unionRect.height;
      resultNode.resize(resultWidth, resultHeight);
      resultNode.fills = [buildImageCompositeFill(newImageHash)];
      resultNode.strokes = [];
      if ("cornerRadius" in resultNode) {
        resultNode.cornerRadius = 0;
      }

      let placed = false;
      if (session && (session.placementAnchorNodeId || session.selectedRootNodeId)) {
        const placementAnchorNode = await figma.getNodeByIdAsync(session.placementAnchorNodeId || session.selectedRootNodeId);
        if (
          placementAnchorNode &&
          !placementAnchorNode.removed
        ) {
          const directPlacement = placeImageCompositeResultNextToAnchor(resultNode, placementAnchorNode, unionRect, {
            preserveTransform: !!(
              session &&
              session.preserveSelectedRootTransform &&
              placementAnchorNode.id === session.selectedRootNodeId
            ),
            modePrefix: placementAnchorNode.id === session.selectedRootNodeId ? "selected-root-sibling" : "placement-anchor-sibling",
          });
          if (directPlacement) {
            placementMode = directPlacement.placementMode;
            focusNode = directPlacement.focusNode || resultNode;
            placed = true;
          } else {
            const outerAnchorNode = findImageCompositeEditableOuterAnchor(placementAnchorNode);
            if (outerAnchorNode && outerAnchorNode.id !== placementAnchorNode.id) {
              const outerPlacement = placeImageCompositeResultNextToAnchor(resultNode, outerAnchorNode, unionRect, {
                preserveTransform: false,
                modePrefix: "outer-anchor-sibling",
              });
              if (outerPlacement) {
                placementMode = outerPlacement.placementMode;
                focusNode = outerPlacement.focusNode || resultNode;
                placed = true;
              }
            }
          }

          if (!placed) {
            const selectedRootHasMaskStack = hasImageMergeMaskDescendant(placementAnchorNode, 0);
            if (
              !selectedRootHasMaskStack &&
              (!("locked" in placementAnchorNode) || !placementAnchorNode.locked) &&
              canUsePromptPlacementContainer(placementAnchorNode)
            ) {
              placementAnchorNode.insertChild(placementAnchorNode.children.length, resultNode);
              setImageExtendNodePosition(resultNode, 0, 0);
              placementMode = "inside-selected-root";
              focusNode = resultNode;
              placed = true;
            }
          }
        }
      }

      if (!placed) {
        figma.currentPage.appendChild(resultNode);
        resultNode.x = unionRect.x;
        resultNode.y = unionRect.y;
        placementMode = "page-overlay";
        focusNode = resultNode;
      }

      const resultFocusNode = focusNode || resultNode;
      try {
        figma.currentPage.selection = [resultFocusNode];
      } catch (error) {
      }
      try {
        if (typeof figma.viewport.scrollAndZoomIntoView === "function") {
          figma.viewport.scrollAndZoomIntoView([resultFocusNode]);
        }
      } catch (error) {
      }
      return buildImageCompositeApplyResult(session, resultFocusNode, byteLength, skipped, placementMode);
    } catch (error) {
      if (resultNode && !resultNode.removed && resultNode.parent) {
        resultNode.remove();
      }
      skipped.push({
        nodeId:
          session && session.placementAnchorNodeId
            ? session.placementAnchorNodeId
            : session && session.selectedRootNodeId
              ? session.selectedRootNodeId
              : "",
        nodeName:
          session && session.placementAnchorNodeName
            ? session.placementAnchorNodeName
            : session && session.selectedRootNodeName
              ? session.selectedRootNodeName
              : "",
        reason: normalizeErrorMessage(error, "Failed to place the generated composite image."),
      });
      return buildImageCompositeApplyResult(session, null, byteLength, skipped, placementMode);
    }
  }

  function placeImageCompositeResultNextToAnchor(resultNode, anchorNode, unionRect, options) {
    if (!resultNode || !anchorNode || anchorNode.removed || !anchorNode.parent) {
      return null;
    }

    const parent = anchorNode.parent;
    if (!canUsePromptPlacementContainer(parent)) {
      return null;
    }

    const insertIndex = findNodeChildIndex(parent, anchorNode.id);
    if (insertIndex < 0) {
      return null;
    }

    try {
      parent.insertChild(insertIndex + 1, resultNode);
      let placementMode = "";
      const modePrefix = options && options.modePrefix ? options.modePrefix : "anchor-sibling";
      if (options && options.preserveTransform === true && copyNodeRelativeTransform(resultNode, anchorNode)) {
        placementMode = modePrefix + "-transform";
      } else if (setImageCompositeNodePositionInParent(resultNode, parent, unionRect)) {
        placementMode = modePrefix + "-absolute";
      } else {
        const localBounds = getImageExtendLocalBounds(anchorNode);
        if (!localBounds) {
          return null;
        }
        setImageExtendNodePosition(resultNode, localBounds.x, localBounds.y);
        placementMode = modePrefix + "-local";
      }

      return {
        focusNode: resultNode,
        placementMode: placementMode,
      };
    } catch (error) {
      return null;
    }
  }

  function findImageCompositeEditableOuterAnchor(node) {
    let current = node;
    let depth = 0;
    while (current && current.parent && current.parent.type !== "DOCUMENT" && depth < 12) {
      if (canUsePromptPlacementContainer(current.parent)) {
        return current;
      }
      current = current.parent;
      depth += 1;
    }

    return null;
  }

  function setImageCompositeNodePositionInParent(node, parent, absoluteRect) {
    if (!node || !parent || !absoluteRect) {
      return false;
    }

    const localPoint = convertImageCompositeAbsolutePointToParentLocal(parent, absoluteRect.x, absoluteRect.y);
    if (!localPoint) {
      return false;
    }

    setImageExtendNodePosition(node, localPoint.x, localPoint.y);
    return true;
  }

  function convertImageCompositeAbsolutePointToParentLocal(parent, x, y) {
    if (!parent || parent.type === "PAGE") {
      return {
        x: roundBoundsFitMetric(Number(x) || 0),
        y: roundBoundsFitMetric(Number(y) || 0),
      };
    }

    const transform = Array.isArray(parent.absoluteTransform) ? parent.absoluteTransform : null;
    if (!transform || transform.length < 2) {
      return null;
    }

    const row0 = Array.isArray(transform[0]) ? transform[0] : null;
    const row1 = Array.isArray(transform[1]) ? transform[1] : null;
    if (!row0 || !row1 || row0.length < 3 || row1.length < 3) {
      return null;
    }

    const a = Number(row0[0]) || 0;
    const c = Number(row0[1]) || 0;
    const tx = Number(row0[2]) || 0;
    const b = Number(row1[0]) || 0;
    const d = Number(row1[1]) || 0;
    const ty = Number(row1[2]) || 0;
    const det = a * d - b * c;
    if (!Number.isFinite(det) || Math.abs(det) < 0.000001) {
      return null;
    }

    const dx = (Number(x) || 0) - tx;
    const dy = (Number(y) || 0) - ty;
    return {
      x: roundBoundsFitMetric((d * dx - c * dy) / det),
      y: roundBoundsFitMetric((-b * dx + a * dy) / det),
    };
  }

  function getImageExtendLocalBounds(node) {
    if (!node || node.removed) {
      return null;
    }

    const width = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(width > 0) || !(height > 0)) {
      return null;
    }

    if ("x" in node && typeof node.x === "number" && "y" in node && typeof node.y === "number") {
      return {
        x: roundBoundsFitMetric(node.x),
        y: roundBoundsFitMetric(node.y),
        width: roundBoundsFitMetric(width),
        height: roundBoundsFitMetric(height),
      };
    }

    if ("relativeTransform" in node && Array.isArray(node.relativeTransform) && node.relativeTransform.length >= 2) {
      const row0 = Array.isArray(node.relativeTransform[0]) ? node.relativeTransform[0] : null;
      const row1 = Array.isArray(node.relativeTransform[1]) ? node.relativeTransform[1] : null;
      if (row0 && row1 && Number.isFinite(Number(row0[2])) && Number.isFinite(Number(row1[2]))) {
        return {
          x: roundBoundsFitMetric(Number(row0[2])),
          y: roundBoundsFitMetric(Number(row1[2])),
          width: roundBoundsFitMetric(width),
          height: roundBoundsFitMetric(height),
        };
      }
    }

    return null;
  }

  function setImageExtendNodePosition(node, x, y) {
    const nextX = roundBoundsFitMetric(x);
    const nextY = roundBoundsFitMetric(y);

    if ("x" in node && typeof node.x === "number" && "y" in node && typeof node.y === "number") {
      node.x = nextX;
      node.y = nextY;
      return;
    }

    if ("relativeTransform" in node && Array.isArray(node.relativeTransform)) {
      node.relativeTransform = [
        [node.relativeTransform[0][0], node.relativeTransform[0][1], nextX],
        [node.relativeTransform[1][0], node.relativeTransform[1][1], nextY],
      ];
      return;
    }

    throw new Error("Could not update the layer position.");
  }

  function copyNodeRelativeTransform(targetNode, sourceNode) {
    if (
      !targetNode ||
      !sourceNode ||
      !("relativeTransform" in targetNode) ||
      !("relativeTransform" in sourceNode) ||
      !Array.isArray(sourceNode.relativeTransform) ||
      sourceNode.relativeTransform.length < 2
    ) {
      return false;
    }

    const row0 = Array.isArray(sourceNode.relativeTransform[0]) ? sourceNode.relativeTransform[0] : null;
    const row1 = Array.isArray(sourceNode.relativeTransform[1]) ? sourceNode.relativeTransform[1] : null;
    if (!row0 || !row1 || row0.length < 3 || row1.length < 3) {
      return false;
    }

    targetNode.relativeTransform = [
      [Number(row0[0]) || 0, Number(row0[1]) || 0, roundBoundsFitMetric(Number(row0[2]) || 0)],
      [Number(row1[0]) || 0, Number(row1[1]) || 0, roundBoundsFitMetric(Number(row1[2]) || 0)],
    ];
    return true;
  }

  function findNodeChildIndex(parent, nodeId) {
    if (!parent || !("children" in parent) || !Array.isArray(parent.children)) {
      return -1;
    }

    for (let index = 0; index < parent.children.length; index += 1) {
      const child = parent.children[index];
      if (child && child.id === nodeId) {
        return index;
      }
    }

    return -1;
  }

  function buildSceneNodeOrderPath(node) {
    if (!node || node.removed) {
      return null;
    }

    const path = [];
    let current = node;
    while (current && current.parent && current.parent.type !== "DOCUMENT") {
      const parent = current.parent;
      const childIndex = findNodeChildIndex(parent, current.id);
      if (childIndex < 0) {
        return null;
      }
      path.unshift(childIndex);
      current = parent;
      if (parent.type === "PAGE") {
        break;
      }
    }

    return path;
  }

  function compareSceneNodeCanvasOrder(leftNode, rightNode) {
    const leftPath = buildSceneNodeOrderPath(leftNode);
    const rightPath = buildSceneNodeOrderPath(rightNode);
    if (!leftPath || !rightPath) {
      return 0;
    }

    const sharedLength = Math.min(leftPath.length, rightPath.length);
    for (let index = 0; index < sharedLength; index += 1) {
      if (leftPath[index] !== rightPath[index]) {
        return leftPath[index] - rightPath[index];
      }
    }

    return leftPath.length - rightPath.length;
  }

  async function applyImageTextOverlayToSelection(session, lines) {
    const skipped = [];
    const node = await figma.getNodeByIdAsync(session.targetNodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: session.targetNodeId,
        nodeName: session.targetNodeName,
        reason: "Could not find the selected layer again.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    if ("locked" in node && node.locked) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Locked layers are not supported for text overlay.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Rotated layers are not supported for text overlay yet.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    if (!node.parent || !canUseImageExtendParent(node.parent)) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "The current parent container does not support grouping the text overlay result.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    if (
      "layoutMode" in node.parent &&
      typeof node.parent.layoutMode === "string" &&
      node.parent.layoutMode !== "NONE"
    ) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Auto-layout parents are not supported for text overlay yet.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    const localBounds = getImageExtendLocalBounds(node);
    if (!localBounds) {
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: "Could not determine the selected layer position.",
      });
      return buildImageTextOverlayApplyResult(session, null, 0, skipped);
    }

    const parent = node.parent;
    const overlay = createImageTextOverlayContainer(node, localBounds);
    const sortedLines = lines.slice().sort(function (leftLine, rightLine) {
      if (Math.abs(leftLine.topRatio - rightLine.topRatio) > 0.0005) {
        return leftLine.topRatio - rightLine.topRatio;
      }
      return leftLine.leftRatio - rightLine.leftRatio;
    });
    const availableFonts = await getImageTextOverlayAvailableFontsSafely();
    let createdCount = 0;
    let group = null;

    try {
      for (let index = 0; index < sortedLines.length; index += 1) {
        const line = sortedLines[index];
        const textNode = await createImageTextOverlayTextNode(line, localBounds.width, localBounds.height, availableFonts);
        if (!textNode) {
          skipped.push({
            nodeId: node.id,
            nodeName: safeName(node),
            reason: "Skipped a text line because its font could not be loaded.",
          });
          continue;
        }

        overlay.appendChild(textNode);
        textNode.x = roundBoundsFitMetric(localBounds.width * line.leftRatio);
        textNode.y = roundBoundsFitMetric(localBounds.height * line.topRatio);
        createdCount += 1;
      }

      if (!createdCount) {
        overlay.remove();
        skipped.push({
          nodeId: node.id,
          nodeName: safeName(node),
          reason: "No editable text layers could be created from the detected text.",
        });
        return buildImageTextOverlayApplyResult(session, null, createdCount, skipped);
      }

      const nodeIndex = findNodeChildIndex(parent, node.id);
      parent.insertChild(nodeIndex >= 0 ? nodeIndex + 1 : parent.children.length, overlay);
      group = figma.group([node, overlay], parent);
      group.name = safeName(node) + " / text overlay";
      tagImageTextOverlayNode(group, "group");
      tagImageTextOverlayNode(overlay, "overlay");
      figma.currentPage.selection = [group];
      figma.viewport.scrollAndZoomIntoView([group]);
      return buildImageTextOverlayApplyResult(session, group, createdCount, skipped);
    } catch (error) {
      if (overlay && !overlay.removed) {
        overlay.remove();
      }
      skipped.push({
        nodeId: node.id,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, "Failed to create the text overlay group."),
      });
      return buildImageTextOverlayApplyResult(session, null, createdCount, skipped);
    }
  }

  function createImageTextOverlayContainer(node, localBounds) {
    const overlay = figma.createFrame();
    overlay.name = safeName(node) + " / editable text";
    overlay.resize(
      Math.max(1, roundBoundsFitMetric(localBounds.width)),
      Math.max(1, roundBoundsFitMetric(localBounds.height))
    );
    overlay.clipsContent = false;
    overlay.fills = [];
    overlay.strokes = [];
    setImageExtendNodePosition(overlay, localBounds.x, localBounds.y);
    return overlay;
  }

  async function createImageTextOverlayTextNode(line, targetWidth, targetHeight, availableFonts) {
    const characters = sanitizeImageTextOverlayCharacters(line && line.text);
    if (!characters) {
      return null;
    }

    const fontName = await loadImageTextOverlayFont(line, availableFonts);
    if (!fontName) {
      return null;
    }

    const textNode = figma.createText();
    const width = Math.max(1, roundBoundsFitMetric(targetWidth * line.widthRatio));
    const height = Math.max(1, roundBoundsFitMetric(targetHeight * line.heightRatio));
    const fontSize = resolveImageTextOverlayFontSize(line, targetHeight, height);
    textNode.fontName = fontName;
    textNode.fontSize = fontSize;
    textNode.characters = characters;
    textNode.name = buildImageTextOverlayLayerName(characters);
    applyImageTextOverlayTextAlign(textNode, line.align);
    applyImageTextOverlayTextSizing(textNode, width, height);

    const fillPaint = createImageTextOverlayPaint(line);
    textNode.fills = [fillPaint || createSolidPaintFromColor("000000", 1)];

    if ("rotation" in textNode) {
      const rotation = sanitizeImageTextOverlayRotation(line.rotation);
      if (rotation !== 0) {
        textNode.rotation = rotation;
      }
    }

    return textNode;
  }

  function applyImageTextOverlayTextAlign(textNode, align) {
    if (!textNode || !("textAlignHorizontal" in textNode)) {
      return;
    }

    if (align === "center") {
      textNode.textAlignHorizontal = "CENTER";
      return;
    }

    if (align === "right") {
      textNode.textAlignHorizontal = "RIGHT";
      return;
    }

    textNode.textAlignHorizontal = "LEFT";
  }

  function applyImageTextOverlayTextSizing(textNode, width, height) {
    if (!textNode) {
      return;
    }

    try {
      if ("textAutoResize" in textNode) {
        textNode.textAutoResize = "NONE";
      }
      if (typeof textNode.resize === "function") {
        textNode.resize(Math.max(1, roundBoundsFitMetric(width)), Math.max(1, roundBoundsFitMetric(height)));
      }
    } catch (error) {
      if ("textAutoResize" in textNode) {
        textNode.textAutoResize = "WIDTH_AND_HEIGHT";
      }
    }
  }

  function resolveImageTextOverlayFontSize(line, targetHeight, fallbackHeight) {
    const ratio = Number(line && line.fontSizeRatio);
    if (Number.isFinite(ratio) && ratio > 0) {
      return Math.max(1, roundBoundsFitMetric(targetHeight * ratio));
    }

    if (Number.isFinite(fallbackHeight) && fallbackHeight > 0) {
      return Math.max(1, roundBoundsFitMetric(fallbackHeight * 0.82));
    }

    return 12;
  }

  function buildImageTextOverlayApplyResult(session, group, createdCount, skipped) {
    const appliedCount = group ? 1 : 0;
    const groupName = group && typeof group.name === "string" ? group.name : "";
    const groupId = group && typeof group.id === "string" ? group.id : "";
    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        targetNodeName: session && session.targetNodeName ? session.targetNodeName : "",
        appliedCount: appliedCount,
        createdTextCount: Math.max(0, Math.floor(Number(createdCount) || 0)),
        skippedCount: Array.isArray(skipped) ? skipped.length : 0,
        groupId: groupId,
        groupName: groupName,
      },
      skipped: Array.isArray(skipped) ? skipped.slice(0, 24) : [],
    };
  }

  function notifyImageTextOverlayResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount =
      typeof summary.appliedCount === "number" && Number.isFinite(summary.appliedCount) ? summary.appliedCount : 0;
    const createdTextCount =
      typeof summary.createdTextCount === "number" && Number.isFinite(summary.createdTextCount)
        ? summary.createdTextCount
        : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;

    if (!appliedCount) {
      figma.notify("No editable text overlay could be created.", { timeout: 2400 });
      return;
    }

    let message = (operationLabel || "Text overlay") + " complete (" + createdTextCount + " text layer";
    if (createdTextCount !== 1) {
      message += "s";
    }
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    figma.notify(message + ")", { timeout: 2600 });
  }

  function sanitizeImageTextOverlayLines(input) {
    const list = Array.isArray(input) ? input : [];
    const normalized = [];

    for (let index = 0; index < list.length; index += 1) {
      const raw = list[index];
      if (!raw || typeof raw !== "object") {
        continue;
      }

      const text = sanitizeImageTextOverlayCharacters(raw.text);
      if (!text) {
        continue;
      }

      const leftRatio = sanitizeImageTextOverlayRatio(raw.leftRatio, 0);
      const topRatio = sanitizeImageTextOverlayRatio(raw.topRatio, 0);
      const widthRatio = sanitizeImageTextOverlayRatio(raw.widthRatio, 0);
      const heightRatio = sanitizeImageTextOverlayRatio(raw.heightRatio, 0);
      if (!(widthRatio > 0.002) || !(heightRatio > 0.002) || leftRatio >= 1 || topRatio >= 1) {
        continue;
      }

      normalized.push({
        text: text,
        leftRatio: leftRatio,
        topRatio: topRatio,
        widthRatio: Math.min(1 - leftRatio, widthRatio),
        heightRatio: Math.min(1 - topRatio, heightRatio),
        fontSizeRatio: sanitizeImageTextOverlayRatio(raw.fontSizeRatio, 0),
        fontFamily: sanitizeImageTextOverlayFontFamily(raw.fontFamily),
        fontStyle: sanitizeImageTextOverlayFontStyle(raw.fontStyle),
        fontWeight: sanitizeImageTextOverlayFontWeight(raw.fontWeight),
        fillColor: typeof raw.fillColor === "string" ? raw.fillColor.trim() : "",
        fillOpacity: sanitizeImageTextOverlayOpacity(raw.fillOpacity),
        align: sanitizeImageTextOverlayAlign(raw.align),
        rotation: sanitizeImageTextOverlayRotation(raw.rotation),
      });
    }

    return normalized.slice(0, 120);
  }

  function sanitizeImageTextOverlayCharacters(value) {
    if (typeof value !== "string") {
      return "";
    }

    const normalized = value.replace(/\u0000/g, "").replace(/\r\n?/g, "\n").trim();
    return normalized ? normalized.slice(0, 500) : "";
  }

  function sanitizeImageTextOverlayRatio(value, fallbackValue) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) {
      return Number.isFinite(fallbackValue) ? fallbackValue : 0;
    }
    return Math.max(0, Math.min(1, Math.round(numeric * 1000000) / 1000000));
  }

  function sanitizeImageTextOverlayOpacity(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) {
      return 1;
    }
    return Math.max(0, Math.min(1, Math.round(numeric * 1000) / 1000));
  }

  function sanitizeImageTextOverlayAlign(value) {
    const normalized = typeof value === "string" ? value.replace(/\s+/g, " ").trim().toLowerCase() : "";
    if (normalized === "center" || normalized === "centre") {
      return "center";
    }
    if (normalized === "right") {
      return "right";
    }
    return "left";
  }

  function sanitizeImageTextOverlayRotation(value) {
    const numeric = Number(value);
    if (!Number.isFinite(numeric)) {
      return 0;
    }
    const rounded = Math.round(numeric * 1000) / 1000;
    if (rounded > 180) {
      return 180;
    }
    if (rounded < -180) {
      return -180;
    }
    return rounded;
  }

  function sanitizeImageTextOverlayFontFamily(value) {
    return typeof value === "string" ? value.trim().slice(0, 120) : "";
  }

  function sanitizeImageTextOverlayFontStyle(value) {
    return typeof value === "string" ? value.trim().slice(0, 80) : "";
  }

  function sanitizeImageTextOverlayFontWeight(value) {
    const numeric = Math.round(Number(value) || 0);
    if (!(numeric > 0)) {
      return 400;
    }
    return Math.max(100, Math.min(900, numeric));
  }

  function buildImageTextOverlayLayerName(characters) {
    const normalized = String(characters || "").replace(/\s+/g, " ").trim();
    if (!normalized) {
      return "Editable Text";
    }
    return normalized.length > 48 ? normalized.slice(0, 48) : normalized;
  }

  function createImageTextOverlayPaint(line) {
    if (!line || !line.fillColor) {
      return null;
    }
    return createSolidPaintFromColor(line.fillColor, line.fillOpacity);
  }

  function createSolidPaintFromColor(value, opacityValue) {
    const normalized = typeof value === "string" ? value.trim() : "";
    if (!normalized) {
      return null;
    }

    const hexMatch = normalized.match(/^#?([0-9a-f]{3}|[0-9a-f]{6})$/i);
    let fullHex = "";
    if (hexMatch) {
      const compactHex = hexMatch[1];
      fullHex =
        compactHex.length === 3
          ? compactHex[0] + compactHex[0] + compactHex[1] + compactHex[1] + compactHex[2] + compactHex[2]
          : compactHex;
    } else {
      const rgbMatch = normalized.match(
        /^rgba?\(\s*([0-9.]+)\s*,\s*([0-9.]+)\s*,\s*([0-9.]+)(?:\s*,\s*[0-9.]+\s*)?\)$/i
      );
      if (!rgbMatch) {
        return null;
      }

      fullHex = rgbMatch
        .slice(1, 4)
        .map(function (channel) {
          return Math.max(0, Math.min(255, Math.round(Number(channel) || 0))).toString(16).padStart(2, "0");
        })
        .join("");
    }

    const red = Number.parseInt(fullHex.slice(0, 2), 16);
    const green = Number.parseInt(fullHex.slice(2, 4), 16);
    const blue = Number.parseInt(fullHex.slice(4, 6), 16);
    if (!Number.isFinite(red) || !Number.isFinite(green) || !Number.isFinite(blue)) {
      return null;
    }

    const paint = {
      type: "SOLID",
      color: {
        r: red / 255,
        g: green / 255,
        b: blue / 255,
      },
    };
    if (Number.isFinite(opacityValue) && opacityValue >= 0 && opacityValue < 1) {
      paint.opacity = Math.max(0, Math.min(1, Number(opacityValue)));
    }
    return paint;
  }

  async function loadImageTextOverlayFont(line, availableFonts) {
    const candidates = getImageTextOverlayFontCandidates(line, availableFonts);
    for (let index = 0; index < candidates.length; index += 1) {
      const fontName = candidates[index];
      try {
        await figma.loadFontAsync(fontName);
        return fontName;
      } catch (error) {}
    }
    return null;
  }

  function getImageTextOverlayFontCandidates(line, availableFonts) {
    const candidates = [];
    const requestedStyle = getRequestedImageTextOverlayFontStyle(line);
    const requestedFamily = getRequestedImageTextOverlayFontFamily(line);
    const splitRequest = splitRequestedImageTextOverlayFontFamily(requestedFamily);

    pushDirectImageTextOverlayFontCandidate(candidates, requestedFamily, requestedStyle);
    pushImageTextOverlayFontNameCandidate(
      candidates,
      findExactImageTextOverlayFontMatch(
        {
          fontFamily: requestedFamily,
          fontStyle: requestedStyle,
        },
        availableFonts
      )
    );

    if (splitRequest.family && splitRequest.family !== requestedFamily) {
      pushDirectImageTextOverlayFontCandidate(candidates, splitRequest.family, requestedStyle);
      pushImageTextOverlayFontNameCandidate(
        candidates,
        findExactImageTextOverlayFontMatch(
          {
            fontFamily: splitRequest.family,
            fontStyle: requestedStyle,
          },
          availableFonts
        )
      );
      if (splitRequest.style) {
        pushDirectImageTextOverlayFontCandidate(candidates, splitRequest.family, splitRequest.style);
        pushImageTextOverlayFontNameCandidate(
          candidates,
          findExactImageTextOverlayFontMatch(
            {
              fontFamily: splitRequest.family,
              fontStyle: splitRequest.style,
            },
            availableFonts
          )
        );
      }
    }

    pushImageTextOverlayFontNameCandidate(candidates, findImageTextOverlayFamilyFontMatch(requestedFamily, line, availableFonts));
    if (splitRequest.family && splitRequest.family !== requestedFamily) {
      pushImageTextOverlayFontNameCandidate(
        candidates,
        findImageTextOverlayFamilyFontMatch(splitRequest.family, line, availableFonts)
      );
    }

    pushKnownImageTextOverlayFontFallbacks(candidates, line);
    pushImageTextOverlayFontNameCandidate(candidates, findImageTextOverlayFallbackFontName(availableFonts));
    return candidates;
  }

  function pushDirectImageTextOverlayFontCandidate(candidates, family, style) {
    const normalizedFamily = cleanupImageTextOverlayFontFamily(family);
    if (!normalizedFamily) {
      return;
    }

    pushImageTextOverlayFontNameCandidate(candidates, {
      family: normalizedFamily,
      style: normalizeImageTextOverlayFontStyle(style) || "Regular",
    });
  }

  function pushKnownImageTextOverlayFontFallbacks(candidates, line) {
    const styleCandidates = buildImageTextOverlayFontStyleCandidates(line);
    const families = [
      "LGEIText",
      "Malgun Gothic",
      "Apple SD Gothic Neo",
      "Noto Sans KR",
      "Noto Sans CJK KR",
      "Arial",
      "Inter",
      "Roboto",
    ];

    for (let familyIndex = 0; familyIndex < families.length; familyIndex += 1) {
      const family = families[familyIndex];
      for (let styleIndex = 0; styleIndex < styleCandidates.length; styleIndex += 1) {
        pushDirectImageTextOverlayFontCandidate(candidates, family, styleCandidates[styleIndex]);
      }
    }
  }

  function getRequestedImageTextOverlayFontFamily(line) {
    const preferred = cleanupImageTextOverlayFontFamily(line && line.fontFamily);
    return preferred || "Arial";
  }

  function getRequestedImageTextOverlayFontStyle(line) {
    const preferred = normalizeImageTextOverlayFontStyle(line && line.fontStyle);
    return preferred || "Regular";
  }

  function cleanupImageTextOverlayFontFamily(value) {
    if (typeof value !== "string") {
      return "";
    }

    return value
      .trim()
      .replace(/^["']+|["']+$/g, "")
      .replace(/(?:PS)?MT$/i, "")
      .replace(/\s+/g, " ")
      .trim();
  }

  function normalizeImageTextOverlayFontStyle(value) {
    const normalized = typeof value === "string" ? value.replace(/[_-]+/g, " ").trim().toLowerCase() : "";
    if (!normalized) {
      return "";
    }

    if (
      (normalized.indexOf("bold") !== -1 && normalized.indexOf("italic") !== -1) ||
      (normalized.indexOf("bold") !== -1 && normalized.indexOf("oblique") !== -1)
    ) {
      return "Bold Italic";
    }
    if (
      (normalized.indexOf("semi") !== -1 || normalized.indexOf("demi") !== -1) &&
      normalized.indexOf("bold") !== -1
    ) {
      return "Semi Bold";
    }
    if (
      (normalized.indexOf("extra") !== -1 || normalized.indexOf("ultra") !== -1) &&
      normalized.indexOf("bold") !== -1
    ) {
      return "Extra Bold";
    }
    if (normalized.indexOf("bold") !== -1) {
      return "Bold";
    }
    if (normalized.indexOf("medium") !== -1) {
      return "Medium";
    }
    if (normalized.indexOf("light") !== -1) {
      return "Light";
    }
    if (normalized.indexOf("thin") !== -1) {
      return "Thin";
    }
    if (normalized.indexOf("black") !== -1 || normalized.indexOf("heavy") !== -1) {
      return "Black";
    }
    if (normalized.indexOf("italic") !== -1 || normalized.indexOf("oblique") !== -1) {
      return "Italic";
    }
    if (normalized.indexOf("book") !== -1) {
      return "Book";
    }
    if (normalized.indexOf("roman") !== -1 || normalized.indexOf("regular") !== -1) {
      return "Regular";
    }

    return "";
  }

  function splitRequestedImageTextOverlayFontFamily(value) {
    const normalized = cleanupImageTextOverlayFontFamily(value);
    if (!normalized) {
      return { family: "", style: "" };
    }

    const styleMatch = normalized.match(
      /(?:^|[-\s])(bold[\s-]*italic|italic[\s-]*bold|semi[\s-]*bold|demi[\s-]*bold|extra[\s-]*bold|ultra[\s-]*bold|bold|medium|light|thin|black|heavy|italic|oblique|regular|roman|book)$/i
    );
    const rawStyle = styleMatch ? styleMatch[1] : "";
    const family = cleanupImageTextOverlayFontFamily(
      styleMatch ? normalized.slice(0, styleMatch.index).replace(/[-\s]+$/, "") : normalized
    );
    return {
      family: family || normalized,
      style: normalizeImageTextOverlayFontStyle(rawStyle),
    };
  }

  function buildImageTextOverlayFontStyleCandidates(line) {
    const candidates = [];
    const requestedStyle = getRequestedImageTextOverlayFontStyle(line);
    const weight = Number(line && line.fontWeight);
    const normalizedWeight = Number.isFinite(weight) ? weight : 400;

    pushImageTextOverlayFontStyleCandidate(candidates, requestedStyle);
    if (requestedStyle === "Bold Italic") {
      pushImageTextOverlayFontStyleCandidate(candidates, "Italic");
      pushImageTextOverlayFontStyleCandidate(candidates, "Bold");
    } else if (requestedStyle === "Italic") {
      pushImageTextOverlayFontStyleCandidate(candidates, "Regular");
    }

    if (normalizedWeight >= 800) {
      pushImageTextOverlayFontStyleCandidate(candidates, "Black");
      pushImageTextOverlayFontStyleCandidate(candidates, "Extra Bold");
    }
    if (normalizedWeight >= 700) {
      pushImageTextOverlayFontStyleCandidate(candidates, "Bold");
    } else if (normalizedWeight >= 600) {
      pushImageTextOverlayFontStyleCandidate(candidates, "Semi Bold");
    } else if (normalizedWeight >= 500) {
      pushImageTextOverlayFontStyleCandidate(candidates, "Medium");
    } else if (normalizedWeight <= 300) {
      pushImageTextOverlayFontStyleCandidate(candidates, "Light");
    }

    pushImageTextOverlayFontStyleCandidate(candidates, "Regular");
    pushImageTextOverlayFontStyleCandidate(candidates, "Book");
    pushImageTextOverlayFontStyleCandidate(candidates, "Roman");
    return candidates;
  }

  function pushImageTextOverlayFontStyleCandidate(candidates, value) {
    const normalized = normalizeImageTextOverlayFontStyle(value);
    if (!normalized) {
      return;
    }

    for (let index = 0; index < candidates.length; index += 1) {
      if (normalizeImageTextOverlayFontToken(candidates[index]) === normalizeImageTextOverlayFontToken(normalized)) {
        return;
      }
    }

    candidates.push(normalized);
  }

  function getImageTextOverlayFontFamilyTokens(value) {
    const tokens = [];
    const cleaned = cleanupImageTextOverlayFontFamily(value);
    const split = splitRequestedImageTextOverlayFontFamily(cleaned);
    pushImageTextOverlayFontFamilyToken(tokens, cleaned);
    pushImageTextOverlayFontFamilyToken(tokens, split.family);
    return tokens;
  }

  function pushImageTextOverlayFontFamilyToken(tokens, value) {
    const normalized = normalizeImageTextOverlayFontToken(cleanupImageTextOverlayFontFamily(value));
    if (!normalized || tokens.indexOf(normalized) !== -1) {
      return;
    }

    tokens.push(normalized);
  }

  function findImageTextOverlayFamilyFontMatch(family, line, availableFonts) {
    if (!Array.isArray(availableFonts) || availableFonts.length === 0) {
      return null;
    }

    const familyTokens = getImageTextOverlayFontFamilyTokens(family);
    if (familyTokens.length === 0) {
      return null;
    }

    const exactMatches = [];
    const partialMatches = [];
    for (let index = 0; index < availableFonts.length; index += 1) {
      const entry = availableFonts[index];
      if (!entry || !entry.fontName) {
        continue;
      }

      const entryFamily = normalizeImageTextOverlayFontToken(cleanupImageTextOverlayFontFamily(entry.fontName.family));
      if (!entryFamily) {
        continue;
      }

      if (familyTokens.indexOf(entryFamily) !== -1) {
        exactMatches.push(entry);
        continue;
      }

      if (
        familyTokens.some(function (token) {
          return token.indexOf(entryFamily) !== -1 || entryFamily.indexOf(token) !== -1;
        })
      ) {
        partialMatches.push(entry);
      }
    }

    return selectImageTextOverlayFontEntryByStyle(exactMatches.length > 0 ? exactMatches : partialMatches, line);
  }

  function selectImageTextOverlayFontEntryByStyle(entries, line) {
    if (!Array.isArray(entries) || entries.length === 0) {
      return null;
    }

    const styleCandidates = buildImageTextOverlayFontStyleCandidates(line);
    for (let styleIndex = 0; styleIndex < styleCandidates.length; styleIndex += 1) {
      const normalizedStyle = normalizeImageTextOverlayFontToken(styleCandidates[styleIndex]);
      for (let entryIndex = 0; entryIndex < entries.length; entryIndex += 1) {
        const entry = entries[entryIndex];
        if (!entry || !entry.fontName) {
          continue;
        }

        if (normalizeImageTextOverlayFontToken(entry.fontName.style) === normalizedStyle) {
          return entry.fontName;
        }
      }
    }

    for (let fallbackIndex = 0; fallbackIndex < 3; fallbackIndex += 1) {
      const fallbackStyle = ["Regular", "Book", "Roman"][fallbackIndex];
      const normalizedFallbackStyle = normalizeImageTextOverlayFontToken(fallbackStyle);
      for (let entryIndex = 0; entryIndex < entries.length; entryIndex += 1) {
        const entry = entries[entryIndex];
        if (!entry || !entry.fontName) {
          continue;
        }

        if (normalizeImageTextOverlayFontToken(entry.fontName.style) === normalizedFallbackStyle) {
          return entry.fontName;
        }
      }
    }

    return entries[0] && entries[0].fontName ? entries[0].fontName : null;
  }

  function findImageTextOverlayFallbackFontName(availableFonts) {
    if (!Array.isArray(availableFonts) || availableFonts.length === 0) {
      return null;
    }

    let fallback = findImageTextOverlayFamilyFontMatch("Arial", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    fallback = findImageTextOverlayFamilyFontMatch("Inter", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    fallback = findImageTextOverlayFamilyFontMatch("Roboto", { fontStyle: "Regular", fontWeight: 400 }, availableFonts);
    if (fallback) {
      return fallback;
    }

    return selectImageTextOverlayFontEntryByStyle(availableFonts, { fontStyle: "Regular", fontWeight: 400 });
  }

  function pushImageTextOverlayFontNameCandidate(candidates, fontName) {
    if (!fontName || typeof fontName !== "object") {
      return;
    }

    const candidateKey =
      normalizeImageTextOverlayFontToken(fontName.family) + ":" + normalizeImageTextOverlayFontToken(fontName.style);
    for (let index = 0; index < candidates.length; index += 1) {
      const existing = candidates[index];
      const existingKey =
        normalizeImageTextOverlayFontToken(existing.family) + ":" + normalizeImageTextOverlayFontToken(existing.style);
      if (existingKey === candidateKey) {
        return;
      }
    }

    candidates.push(fontName);
  }

  function findExactImageTextOverlayFontMatch(style, availableFonts) {
    if (!style || !Array.isArray(availableFonts)) {
      return null;
    }

    const targetFamily = normalizeImageTextOverlayFontToken(style.fontFamily);
    const targetStyle = normalizeImageTextOverlayFontToken(style.fontStyle);
    if (targetFamily.length === 0 || targetStyle.length === 0) {
      return null;
    }

    for (let index = 0; index < availableFonts.length; index += 1) {
      const entry = availableFonts[index];
      if (!entry || !entry.fontName) {
        continue;
      }

      const family = normalizeImageTextOverlayFontToken(entry.fontName.family);
      const fontStyle = normalizeImageTextOverlayFontToken(entry.fontName.style);
      if (family === targetFamily && fontStyle === targetStyle) {
        return entry.fontName;
      }
    }

    return null;
  }

  async function getImageTextOverlayAvailableFonts() {
    if (!availableFontsPromise) {
      availableFontsPromise = figma.listAvailableFontsAsync().catch(function (error) {
        availableFontsPromise = null;
        throw error;
      });
    }

    return availableFontsPromise;
  }

  async function getImageTextOverlayAvailableFontsSafely() {
    try {
      return await getImageTextOverlayAvailableFonts();
    } catch (error) {
      return [];
    }
  }

  function normalizeImageTextOverlayFontToken(value) {
    return typeof value === "string" ? value.trim().toLowerCase().replace(/[^a-z0-9]/g, "") : "";
  }

  function tagImageTextOverlayNode(node, role) {
    if (!node || typeof node.setPluginData !== "function") {
      return;
    }

    try {
      node.setPluginData("__pigmaTextOverlay", "1");
      node.setPluginData("__pigmaTextOverlayRole", String(role || ""));
    } catch (error) {}
  }

  async function replaceSelectionImageFills(session, newImageHash, byteLength) {
    const skipped = [];
    const appliedNodeIds = {};
    let appliedFillCount = 0;

    for (let index = 0; index < session.usages.length; index += 1) {
      const usage = session.usages[index];
      const node = await figma.getNodeByIdAsync(usage.nodeId);
      if (!node || node.removed) {
        skipped.push({
          nodeId: usage.nodeId,
          nodeName: usage.nodeName,
          reason: IMAGE_NODE_UNAVAILABLE_REASON,
        });
        continue;
      }

      const fills = getNodeFills(node);
      if (!fills.length) {
        skipped.push({
          nodeId: usage.nodeId,
          nodeName: safeName(node),
          reason: IMAGE_FILL_LIST_EMPTY_REASON,
        });
        continue;
      }

      let targetIndex = -1;
      if (usage.fillIndex >= 0 && usage.fillIndex < fills.length) {
        const directFill = fills[usage.fillIndex];
        if (isImagePaint(directFill) && directFill.imageHash === session.originalHash) {
          targetIndex = usage.fillIndex;
        }
      }

      if (targetIndex < 0) {
        const fillIndices = getFillIndicesInUiOrder(fills);
        for (let orderedFillIndex = 0; orderedFillIndex < fillIndices.length; orderedFillIndex += 1) {
          const fillIndex = fillIndices[orderedFillIndex];
          const fill = fills[fillIndex];
          if (isImagePaint(fill) && fill.imageHash === session.originalHash) {
            targetIndex = fillIndex;
            break;
          }
        }
      }

      if (targetIndex < 0) {
        skipped.push({
          nodeId: usage.nodeId,
          nodeName: safeName(node),
          reason: IMAGE_FILL_HASH_MISMATCH_REASON,
        });
        continue;
      }

      const nextFills = fills.slice();
      const originalFill = nextFills[targetIndex];
      const hiddenOriginalFill = cloneImagePaintWithHashAndVisibility(originalFill, session.originalHash, false);
      const newVisibleFill =
        session && session.captureMode === "source-image"
          ? cloneImagePaintWithHash(originalFill, newImageHash)
          : cloneRenderedUpscaleImagePaint(originalFill, newImageHash);
      nextFills.splice(targetIndex, 1, hiddenOriginalFill, newVisibleFill);

      try {
        node.fills = nextFills;
        appliedFillCount += 1;
        appliedNodeIds[node.id] = safeName(node);
      } catch (error) {
        skipped.push({
          nodeId: usage.nodeId,
          nodeName: safeName(node),
          reason: normalizeErrorMessage(error, IMAGE_FILL_UPDATE_ERROR_MESSAGE),
        });
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session.selectionLabel,
        targetNodeName: session.targetNodeName,
        originalImageHash: session.originalHash,
        newImageHash: newImageHash,
        appliedFillCount: appliedFillCount,
        appliedNodeCount: Object.keys(appliedNodeIds).length,
        skippedCount: skipped.length,
        resultByteLength: byteLength,
      },
      skipped: skipped.slice(0, 24),
    };
  }

  async function applyGeneratedImageToShapeNode(session, newImageHash, byteLength) {
    const skipped = [];
    let appliedFillCount = 0;
    const node = await figma.getNodeByIdAsync(session.targetNodeId);
    if (!isPromptEditableShapeNode(node)) {
      skipped.push({
        nodeId: session.targetNodeId,
        nodeName: session.targetNodeName,
        reason: SHAPE_TARGET_UNAVAILABLE_REASON,
      });
    } else {
      const fills = getNodeFills(node);
      const nextFills = fills.map(function (fill) {
        return clonePaintWithVisibility(fill, false);
      });
      nextFills.push(buildVisibleImageFill(newImageHash));

      try {
        node.fills = nextFills;
        appliedFillCount = 1;
      } catch (error) {
        skipped.push({
          nodeId: session.targetNodeId,
          nodeName: safeName(node),
          reason: normalizeErrorMessage(error, SHAPE_FILL_APPLY_ERROR_MESSAGE),
        });
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session.selectionLabel,
        targetNodeName: session.targetNodeName,
        newImageHash: newImageHash,
        appliedFillCount: appliedFillCount,
        appliedNodeCount: appliedFillCount ? 1 : 0,
        skippedCount: skipped.length,
        resultByteLength: byteLength,
      },
      skipped: skipped.slice(0, 24),
    };
  }

  async function applyGeneratedPromptImagePlacement(session, newImageHash, byteLength) {
    const skipped = [];
    let resultNode = null;
    let placementMode = "";

    try {
      if (session.targetKind === "new-image-placement") {
        resultNode = createPromptPlacementResultNode(session, newImageHash);
        placementMode = await placePromptGeneratedNodeFromAnchor(session, resultNode);
      } else {
        const targetNode = await figma.getNodeByIdAsync(session.targetNodeId);
        if (!targetNode || targetNode.removed) {
          throw new Error("Could not find the selected target layer again.");
        }
        if ("locked" in targetNode && targetNode.locked) {
          throw new Error("Locked layers are not supported for prompt edit/generation.");
        }
        if ("rotation" in targetNode && typeof targetNode.rotation === "number" && Math.abs(targetNode.rotation) > 0.01) {
          throw new Error("Rotated layers are not supported for prompt edit/generation yet.");
        }

        resultNode = createPromptPlacementResultNode(session, newImageHash);
        if (session.placementMode === "outside-reference") {
          placementMode = placePromptGeneratedNodeOutsideReference(targetNode, resultNode);
        } else if (canUsePromptPlacementContainer(targetNode)) {
          targetNode.insertChild(targetNode.children.length, resultNode);
          setImageExtendNodePosition(resultNode, 0, 0);
          placementMode = "inside-target";
        } else if (
          targetNode.parent &&
          canUsePromptPlacementContainer(targetNode.parent) &&
          !isAutoLayoutNode(targetNode.parent)
        ) {
          const localBounds = getImageExtendLocalBounds(targetNode);
          if (!localBounds) {
            throw new Error("Could not determine the selected layer position.");
          }
          const parent = targetNode.parent;
          const insertIndex = findNodeChildIndex(parent, targetNode.id);
          if (insertIndex >= 0) {
            parent.insertChild(insertIndex + 1, resultNode);
          } else {
            parent.insertChild(parent.children.length, resultNode);
          }
          setImageExtendNodePosition(resultNode, localBounds.x, localBounds.y);
          placementMode = "sibling-overlay";
        } else {
          const absoluteRect = getBoundsFitNodeRect(targetNode);
          if (!absoluteRect) {
            throw new Error("Could not determine the selected layer bounds.");
          }
          figma.currentPage.appendChild(resultNode);
          resultNode.x = absoluteRect.x;
          resultNode.y = absoluteRect.y;
          placementMode = "page-overlay";
        }
      }

      if (resultNode) {
        figma.currentPage.selection = [resultNode];
        if (typeof figma.viewport.scrollAndZoomIntoView === "function") {
          figma.viewport.scrollAndZoomIntoView([resultNode]);
        }
      }
    } catch (error) {
      if (resultNode && !resultNode.removed && resultNode.parent) {
        resultNode.remove();
      }
      skipped.push({
        nodeId: session.targetNodeId,
        nodeName: session.targetNodeName,
        reason: normalizeErrorMessage(error, PROMPT_PLACEMENT_APPLY_ERROR_MESSAGE),
      });
      resultNode = null;
    }

    return buildPromptPlacementApplyResult(session, resultNode, byteLength, skipped, placementMode);
  }

  function placePromptGeneratedNodeOutsideReference(targetNode, resultNode) {
    const gap = PROMPT_TEXT_ANCHOR_GAP;
    if (
      targetNode &&
      targetNode.parent &&
      canUsePromptPlacementContainer(targetNode.parent) &&
      !isAutoLayoutNode(targetNode.parent)
    ) {
      const localBounds = getImageExtendLocalBounds(targetNode);
      if (localBounds) {
        const parent = targetNode.parent;
        const insertIndex = findNodeChildIndex(parent, targetNode.id);
        if (insertIndex >= 0) {
          parent.insertChild(insertIndex + 1, resultNode);
        } else {
          parent.insertChild(parent.children.length, resultNode);
        }
        setImageExtendNodePosition(resultNode, localBounds.x + localBounds.width + gap, localBounds.y);
        return "sibling-right";
      }
    }

    const absoluteRect = getBoundsFitNodeRect(targetNode);
    if (absoluteRect) {
      figma.currentPage.appendChild(resultNode);
      resultNode.x = roundBoundsFitMetric(absoluteRect.x + absoluteRect.width + gap);
      resultNode.y = roundBoundsFitMetric(absoluteRect.y);
      return "page-right";
    }

    const viewportCenter = figma.viewport && figma.viewport.center ? figma.viewport.center : { x: 0, y: 0 };
    figma.currentPage.appendChild(resultNode);
    resultNode.x = roundBoundsFitMetric(viewportCenter.x - resultNode.width / 2);
    resultNode.y = roundBoundsFitMetric(viewportCenter.y - resultNode.height / 2);
    return "viewport-center";
  }

  async function placePromptGeneratedNodeFromAnchor(session, resultNode) {
    if (
      session &&
      session.targetNodeId &&
      typeof session.targetNodeId === "string" &&
      session.targetNodeId &&
      session.placementMode === "below-selected-text"
    ) {
      const anchorNode = await figma.getNodeByIdAsync(session.targetNodeId);
      if (
        anchorNode &&
        !anchorNode.removed &&
        (!("locked" in anchorNode) || !anchorNode.locked) &&
        (!("rotation" in anchorNode) || typeof anchorNode.rotation !== "number" || Math.abs(anchorNode.rotation) <= 0.01)
      ) {
        const localBounds = getImageExtendLocalBounds(anchorNode);
        if (
          localBounds &&
          anchorNode.parent &&
          canUsePromptPlacementContainer(anchorNode.parent) &&
          !isAutoLayoutNode(anchorNode.parent)
        ) {
          const parent = anchorNode.parent;
          const insertIndex = findNodeChildIndex(parent, anchorNode.id);
          if (insertIndex >= 0) {
            parent.insertChild(insertIndex + 1, resultNode);
          } else {
            parent.insertChild(parent.children.length, resultNode);
          }
          setImageExtendNodePosition(resultNode, localBounds.x, localBounds.y + localBounds.height + PROMPT_TEXT_ANCHOR_GAP);
          return "below-selected-text";
        }

        const absoluteRect = getBoundsFitNodeRect(anchorNode);
        if (absoluteRect) {
          figma.currentPage.appendChild(resultNode);
          resultNode.x = absoluteRect.x;
          resultNode.y = absoluteRect.y + absoluteRect.height + PROMPT_TEXT_ANCHOR_GAP;
          return "below-selected-text-page";
        }
      }
    }

    const viewportCenter = figma.viewport && figma.viewport.center ? figma.viewport.center : { x: 0, y: 0 };
    figma.currentPage.appendChild(resultNode);
    resultNode.x = roundBoundsFitMetric(viewportCenter.x - resultNode.width / 2);
    resultNode.y = roundBoundsFitMetric(viewportCenter.y - resultNode.height / 2);
    return "viewport-center";
  }

  function createPromptPlacementResultNode(session, newImageHash) {
    const width = Math.max(1, roundBoundsFitPixel(session && session.targetWidth ? session.targetWidth : 1024));
    const height = Math.max(1, roundBoundsFitPixel(session && session.targetHeight ? session.targetHeight : 1024));
    const node = figma.createRectangle();
    node.name = buildPromptPlacementResultName(session);
    node.resize(width, height);
    node.fills = [buildVisibleImageFill(newImageHash)];
    node.strokes = [];
    if ("cornerRadius" in node) {
      node.cornerRadius = 0;
    }
    return node;
  }

  function buildPromptPlacementResultName(session) {
    const baseName =
      session && typeof session.targetNodeName === "string" && session.targetNodeName.trim()
        ? session.targetNodeName.trim()
        : "AI generated image";
    return baseName + " / generated";
  }

  function buildPromptPlacementApplyResult(session, resultNode, byteLength, skipped, placementMode) {
    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session && session.selectionLabel ? session.selectionLabel : "",
        targetNodeName: session && session.targetNodeName ? session.targetNodeName : "",
        appliedNodeCount: resultNode ? 1 : 0,
        skippedCount: Array.isArray(skipped) ? skipped.length : 0,
        resultNodeId: resultNode && resultNode.id ? resultNode.id : "",
        resultNodeName: resultNode && resultNode.name ? resultNode.name : "",
        placementMode: placementMode || "",
        resultByteLength: byteLength,
      },
      skipped: Array.isArray(skipped) ? skipped.slice(0, 24) : [],
    };
  }

  function isAutoLayoutNode(node) {
    return !!node && "layoutMode" in node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function canUsePromptPlacementContainer(node) {
    return canUseImageExtendParent(node) && !isAutoLayoutNode(node);
  }

  function notifyApplyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const appliedFillCount =
      typeof summary.appliedFillCount === "number" && Number.isFinite(summary.appliedFillCount)
        ? summary.appliedFillCount
        : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;

    if (!appliedFillCount) {
      figma.notify("Could not find an IMAGE fill to replace.", { timeout: 2200 });
      return;
    }

    let message = "Image upscale applied (" + appliedFillCount + " fill replaced";
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    figma.notify(message + ")", { timeout: 2600 });
  }

  function clonePaintWithVisibility(fill, visible) {
    const cloned = JSON.parse(JSON.stringify(fill));
    cloned.visible = visible !== false;
    return cloned;
  }

  function cloneImagePaintWithHashAndVisibility(fill, imageHash, visible) {
    const cloned = clonePaintWithVisibility(fill, visible);
    cloned.imageHash = imageHash;
    return cloned;
  }

  function cloneImagePaintWithHash(fill, imageHash) {
    return cloneImagePaintWithHashAndVisibility(fill, imageHash, true);
  }

  function buildVisibleImageFill(imageHash) {
    return {
      type: "IMAGE",
      imageHash: imageHash,
      scaleMode: "FILL",
      visible: true,
    };
  }

  function buildImageCompositeFill(imageHash) {
    return {
      type: "IMAGE",
      imageHash: imageHash,
      scaleMode: "FILL",
      visible: true,
    };
  }

  function resolveRenderedUpscaleScaleMode(fill) {
    const scaleMode = typeof (fill && fill.scaleMode) === "string" ? fill.scaleMode : "FILL";
    if (scaleMode === "STRETCH") {
      return "FILL";
    }
    if (scaleMode === "FIT" || scaleMode === "FILL") {
      return scaleMode;
    }
    return "FIT";
  }

  function cloneRenderedUpscaleImagePaint(fill, imageHash) {
    const cloned = cloneImagePaintWithHash(fill, imageHash);
    const resolvedScaleMode = resolveRenderedUpscaleScaleMode(fill);
    cloned.scaleMode = resolvedScaleMode;
    if ("imageTransform" in cloned) {
      delete cloned.imageTransform;
    }
    if ("scalingFactor" in cloned) {
      delete cloned.scalingFactor;
    }
    if ("rotation" in cloned) {
      delete cloned.rotation;
    }
    return cloned;
  }

  function cloneBoundsFitImagePaint(fill, imageHash) {
    const cloned = cloneImagePaintWithHash(fill, imageHash);
    cloned.scaleMode = "FILL";
    if ("imageTransform" in cloned) {
      delete cloned.imageTransform;
    }
    if ("scalingFactor" in cloned) {
      delete cloned.scalingFactor;
    }
    if ("rotation" in cloned) {
      delete cloned.rotation;
    }
    return cloned;
  }

  function cloneOriginalSizeFitImagePaint(fill) {
    if (!fill || !isImagePaint(fill) || !fill.imageHash) {
      return null;
    }

    const cloned = cloneImagePaintWithHash(fill, fill.imageHash);
    cloned.scaleMode = "FILL";
    if ("imageTransform" in cloned) {
      delete cloned.imageTransform;
    }
    if ("scalingFactor" in cloned) {
      delete cloned.scalingFactor;
    }
    if ("rotation" in cloned) {
      delete cloned.rotation;
    }
    return cloned;
  }

  function doesOriginalSizeFitNeedPaintReset(fill) {
    if (!fill || !isImagePaint(fill)) {
      return false;
    }
    if (fill.scaleMode !== "FILL") {
      return true;
    }
    if ("imageTransform" in fill && !!fill.imageTransform) {
      return true;
    }
    if ("scalingFactor" in fill && Number(fill.scalingFactor) !== 1) {
      return true;
    }
    if ("rotation" in fill && Math.abs(Number(fill.rotation) || 0) > 0.01) {
      return true;
    }
    return false;
  }

  function cloneBoundsFitPreservedImagePaint(fill, target, processed) {
    if (!fill || !isImagePaint(fill) || !fill.imageHash || !target || !processed) {
      return null;
    }

    const useRenderedAnalysis = target.analysisMode === "rendered-node";
    const sourceWidth =
      useRenderedAnalysis && Number(processed.sourceWidth) > 0
        ? Number(processed.sourceWidth)
        : Number(target.sourceWidth) > 0
          ? Number(target.sourceWidth)
          : 0;
    const sourceHeight =
      useRenderedAnalysis && Number(processed.sourceHeight) > 0
        ? Number(processed.sourceHeight)
        : Number(target.sourceHeight) > 0
          ? Number(target.sourceHeight)
          : 0;
    const rasterOffsetX =
      useRenderedAnalysis || !Number.isFinite(Number(target.rasterAnalysisOffsetX))
        ? 0
        : Number(target.rasterAnalysisOffsetX);
    const rasterOffsetY =
      useRenderedAnalysis || !Number.isFinite(Number(target.rasterAnalysisOffsetY))
        ? 0
        : Number(target.rasterAnalysisOffsetY);
    const cropX = rasterOffsetX + (Number(processed.cropX) || 0);
    const cropY = rasterOffsetY + (Number(processed.cropY) || 0);
    const cropWidth = Number(processed.cropWidth) > 0 ? Number(processed.cropWidth) : 0;
    const cropHeight = Number(processed.cropHeight) > 0 ? Number(processed.cropHeight) : 0;

    if (!(sourceWidth > 0) || !(sourceHeight > 0) || !(cropWidth > 0) || !(cropHeight > 0)) {
      return null;
    }

    const cloned = cloneImagePaintWithHash(fill, fill.imageHash);
    cloned.scaleMode = "CROP";
    cloned.imageTransform = [
      [cropWidth / sourceWidth, 0, cropX / sourceWidth],
      [0, cropHeight / sourceHeight, cropY / sourceHeight],
    ];
    if ("scalingFactor" in cloned) {
      delete cloned.scalingFactor;
    }
    if ("rotation" in cloned) {
      delete cloned.rotation;
    }
    return cloned;
  }

  function notifyApplyResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedFillCount =
      typeof summary.appliedFillCount === "number" && Number.isFinite(summary.appliedFillCount)
        ? summary.appliedFillCount
        : 0;
    const appliedNodeCount =
      typeof summary.appliedNodeCount === "number" && Number.isFinite(summary.appliedNodeCount)
        ? summary.appliedNodeCount
        : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;
    const skipped = result && Array.isArray(result.skipped) ? result.skipped : [];
    const firstSkippedReason =
      skipped.length && skipped[0] && typeof skipped[0].reason === "string" ? skipped[0].reason.trim() : "";

    if (!appliedFillCount && !appliedNodeCount) {
      const message = firstSkippedReason
        ? "AI image result could not be applied: " + firstSkippedReason
        : "AI image result could not be applied.";
      figma.notify(message, { timeout: 3200, error: true });
      return;
    }

    let message = operationLabel || "AI Image";
    if (appliedFillCount > 0) {
      message += " complete (" + appliedFillCount + " fill applied";
    } else {
      message += " complete (" + appliedNodeCount + " layer created";
    }
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    figma.notify(message + ")", { timeout: 2600 });
  }

  function sanitizeOperationLabel(value) {
    const label = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    return label || "AI Image";
  }

  function isBoundsFitOperation(options) {
    const operationKind =
      options && typeof options.operationKind === "string" ? options.operationKind.replace(/\s+/g, " ").trim().toLowerCase() : "";
    if (operationKind === "bounds-fit") {
      return true;
    }
    const operationLabel = sanitizeOperationLabel(options && options.operationLabel);
    return /bounds\s*fit/i.test(operationLabel);
  }

  function isImageExtendOperation(options) {
    const operationKind =
      options && typeof options.operationKind === "string" ? options.operationKind.replace(/\s+/g, " ").trim().toLowerCase() : "";
    if (operationKind === "image-extend") {
      return true;
    }
    const operationLabel = sanitizeOperationLabel(options && options.operationLabel);
    return /extend|확장/i.test(operationLabel);
  }

  function isOriginalSizeFitOperation(options) {
    const operationKind =
      options && typeof options.operationKind === "string" ? options.operationKind.replace(/\s+/g, " ").trim().toLowerCase() : "";
    if (operationKind === "original-size-fit") {
      return true;
    }
    const operationLabel = sanitizeOperationLabel(options && options.operationLabel);
    return /원본\s*크기|original\s*size/i.test(operationLabel);
  }

  function sanitizeSourceMode(value) {
    const mode = typeof value === "string" ? value.replace(/\s+/g, " ").trim().toLowerCase() : "";
    return mode === "shape-or-image" || mode === "prompt-smart" ? mode : "image-fill-only";
  }

  function shouldPreferOriginalUpscaleSource(message) {
    if (message && message.preferOriginalImageBytes === true) {
      return true;
    }
    return isSharpenOperationLabel(message && message.operationLabel) || isUpscaleOperationLabel(message && message.operationLabel);
  }

  function sanitizePromptOutputSize(value) {
    const normalized = typeof value === "string" ? value.replace(/\s+/g, "").trim().toUpperCase() : "";
    return normalized === "2K" || normalized === "4K" ? normalized : "1K";
  }

  function isSharpenOperationLabel(value) {
    const label = sanitizeOperationLabel(value);
    return label === "샤프닝" || /sharpen/i.test(label);
  }

  function isUpscaleOperationLabel(value) {
    const label = sanitizeOperationLabel(value);
    return /업스케일|upscale/i.test(label);
  }

  function shouldPreferOriginalUpscaleSource(message) {
    if (message && message.preferOriginalImageBytes === true) {
      return true;
    }
    return isSharpenOperationLabel(message && message.operationLabel) || isUpscaleOperationLabel(message && message.operationLabel);
  }

  function buildPromptPlacementDimensions(outputSize) {
    const normalizedSize = sanitizePromptOutputSize(outputSize);
    if (normalizedSize === "4K") {
      return { width: 4096, height: 4096 };
    }
    if (normalizedSize === "2K") {
      return { width: 2048, height: 2048 };
    }
    return { width: 1024, height: 1024 };
  }

  async function collectReferenceSearchSourceFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error(IMAGE_TASK_NO_SELECTION_MESSAGE);
    }

    const selectedRangeText = getSelectedTextRangeSnapshot();
    if (selectedRangeText) {
      return {
        sourceType: "text",
        text: selectedRangeText.text,
        summary: {
          selectionLabel: selectedRangeText.selectionLabel,
          targetNodeName: selectedRangeText.nodeName,
          textMode: "selected-range",
          textLength: selectedRangeText.text.length,
        },
      };
    }

    if (selection.every(function (node) { return node && node.type === "TEXT"; })) {
      const directText = collectReferenceTextFromNodes(selection, {
        maxNodes: 6,
        maxLength: 480,
        includeDescendants: false,
      });
      if (directText) {
        return {
          sourceType: "text",
          text: directText,
          summary: {
            selectionLabel: formatSelectionLabel(selection),
            targetNodeName: safeName(selection[0]),
            textMode: "text-selection",
            textLength: directText.length,
          },
        };
      }
    }

    let imageTarget = null;
    try {
      imageTarget = collectSingleUpscaleTargetFromSelection();
    } catch (error) {
      imageTarget = null;
    }

    if (imageTarget) {
      return await buildReferenceImageSource(imageTarget);
    }

    const fallbackText = collectReferenceTextFromNodes(selection, {
      maxNodes: 8,
      maxLength: 560,
      includeDescendants: true,
    });
    if (fallbackText) {
      return {
        sourceType: "text",
        text: fallbackText,
        summary: {
          selectionLabel: formatSelectionLabel(selection),
          targetNodeName: safeName(selection[0]),
          textMode: "selection-descendants",
          textLength: fallbackText.length,
        },
      };
    }

    throw new Error(REFERENCE_SOURCE_NOT_FOUND_MESSAGE);
  }

  function getSelectedTextRangeSnapshot() {
    try {
      const page = figma.currentPage;
      const selectedTextRange = page && "selectedTextRange" in page ? page.selectedTextRange : null;
      if (!selectedTextRange || !selectedTextRange.node || selectedTextRange.node.removed || selectedTextRange.node.type !== "TEXT") {
        return null;
      }

      const characters = typeof selectedTextRange.node.characters === "string" ? selectedTextRange.node.characters : "";
      const start = Math.max(0, Math.floor(Number(selectedTextRange.start) || 0));
      const end = Math.max(start, Math.floor(Number(selectedTextRange.end) || 0));
      const text = normalizeReferenceText(characters.slice(start, end), 480);
      if (!text) {
        return null;
      }

      return {
        text: text,
        nodeName: safeName(selectedTextRange.node),
        selectionLabel: safeName(selectedTextRange.node),
      };
    } catch (error) {
      return null;
    }
  }

  function collectReferenceTextFromNodes(nodes, options) {
    const list = Array.isArray(nodes) ? nodes : [];
    const maxNodes =
      options && Number(options.maxNodes) > 0 ? Math.max(1, Math.floor(Number(options.maxNodes))) : 6;
    const maxLength =
      options && Number(options.maxLength) > 0 ? Math.max(80, Math.floor(Number(options.maxLength))) : 480;
    const includeDescendants = !(options && options.includeDescendants === false);
    const parts = [];
    const seen = {};

    function visit(node) {
      if (!node || node.removed || parts.length >= maxNodes) {
        return;
      }
      if ("visible" in node && node.visible === false) {
        return;
      }

      if (node.type === "TEXT" && typeof node.characters === "string") {
        const text = normalizeReferenceText(node.characters, Math.max(40, Math.min(200, maxLength)));
        if (text && !seen[text]) {
          seen[text] = true;
          parts.push(text);
        }
      }

      if (includeDescendants && hasChildren(node)) {
        for (let index = 0; index < node.children.length; index += 1) {
          visit(node.children[index]);
          if (parts.length >= maxNodes) {
            break;
          }
        }
      }
    }

    for (let index = 0; index < list.length; index += 1) {
      visit(list[index]);
      if (parts.length >= maxNodes) {
        break;
      }
    }

    return normalizeReferenceText(parts.join(" / "), maxLength);
  }

  function normalizeReferenceText(value, maxLength) {
    const normalized = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    if (!normalized) {
      return "";
    }
    const limit = Number(maxLength) > 0 ? Math.floor(Number(maxLength)) : 0;
    if (!limit || normalized.length <= limit) {
      return normalized;
    }
    return normalized.slice(0, Math.max(0, limit - 1)).trim() + "...";
  }

  async function buildReferenceImageSource(target) {
    const summary = {
      selectionLabel: formatSelectionLabel(target.selection),
      targetNodeName: target.entry.nodeName,
      imageHash: target.entry.imageHash,
      captureMode: "rendered-node",
      byteLength: 0,
    };
    let bytes = new Uint8Array(0);
    let mimeType = "image/png";
    let fileName = buildFileName(target.entry, "png");

    const node = await figma.getNodeByIdAsync(target.entry.nodeId);
    if (node && !node.removed && typeof node.exportAsync === "function") {
      try {
        bytes = await node.exportAsync({
          format: "PNG",
          useAbsoluteBounds: false,
        });
      } catch (error) {
        bytes = new Uint8Array(0);
      }
    }

    if (!bytes.length) {
      const image = figma.getImageByHash(target.entry.imageHash);
      if (!image) {
        throw new Error("Could not find the selected IMAGE fill source.");
      }
      bytes = await image.getBytesAsync();
      if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
        throw new Error("Could not read the selected IMAGE fill bytes.");
      }
      const extension = detectImageExtension(bytes);
      mimeType = detectImageMimeType(bytes);
      fileName = buildFileName(target.entry, extension === "bin" ? "png" : extension);
      summary.captureMode = "source-image";
    }

    summary.byteLength = bytes.length;

    return {
      sourceType: "image",
      image: {
        bytes: bytes,
        mimeType: mimeType,
        fileName: fileName,
      },
      summary: summary,
    };
  }

  async function collectPromptDraftSourceFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error(PROMPT_DRAFT_SOURCE_NOT_FOUND_MESSAGE);
    }

    const visualNodes = selection.filter(function (node) {
      return !!node && !node.removed && node.type !== "TEXT";
    });
    const selectedRangeText = visualNodes.length ? null : getSelectedTextRangeSnapshot();
    const textPayload = collectPromptDraftTextPayload(selection, selectedRangeText);

    if (visualNodes.length > 1) {
      throw new Error(PROMPT_SMART_SELECTION_MESSAGE);
    }

    if (visualNodes.length === 1) {
      const source = await buildPromptDraftVisualSource(visualNodes[0], selection);
      if (textPayload && textPayload.text) {
        source.sourceType = "mixed";
        source.text = textPayload.text;
        source.summary = source.summary || {};
        source.summary.textLength = textPayload.text.length;
        source.summary.textMode = textPayload.textMode;
      }
      return source;
    }

    if (textPayload && textPayload.text) {
      return {
        sourceType: "text",
        text: textPayload.text,
        summary: {
          selectionLabel: formatSelectionLabel(selection),
          targetNodeName: textPayload.nodeName || safeName(selection[0]),
          textMode: textPayload.textMode,
          textLength: textPayload.text.length,
        },
      };
    }

    throw new Error(PROMPT_DRAFT_SOURCE_NOT_FOUND_MESSAGE);
  }

  function collectPromptDraftTextPayload(selection, selectedRangeText) {
    if (selectedRangeText && selectedRangeText.text) {
      return {
        text: selectedRangeText.text,
        textMode: "selected-range",
        nodeName: selectedRangeText.nodeName,
      };
    }

    if (selection.every(function (node) { return node && node.type === "TEXT"; })) {
      const directText = collectReferenceTextFromNodes(selection, {
        maxNodes: 8,
        maxLength: 600,
        includeDescendants: false,
      });
      if (directText) {
        return {
          text: directText,
          textMode: "text-selection",
          nodeName: safeName(selection[0]),
        };
      }
    }

    const fallbackText = collectReferenceTextFromNodes(selection, {
      maxNodes: 10,
      maxLength: 720,
      includeDescendants: true,
    });
    if (fallbackText) {
      return {
        text: fallbackText,
        textMode: "selection-descendants",
        nodeName: safeName(selection[0]),
      };
    }

    return null;
  }

  async function buildPromptDraftVisualSource(node, selection) {
    if (!node || node.removed || typeof node.exportAsync !== "function") {
      throw new Error(PROMPT_TARGET_EXPORT_ERROR_MESSAGE);
    }

    const bytes = await node.exportAsync({
      format: "PNG",
      useAbsoluteBounds: false,
    });
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error(PROMPT_TARGET_EXPORT_EMPTY_MESSAGE);
    }

    const bounds = getBoundsFitNodeRect(node);
    return {
      sourceType: "image",
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: node.id,
          },
          "png"
        ),
      },
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        targetNodeName: safeName(node),
        captureMode: "rendered-node",
        targetWidth: bounds ? bounds.width : roundBoundsFitMetric(node.width),
        targetHeight: bounds ? bounds.height : roundBoundsFitMetric(node.height),
        byteLength: bytes.length,
      },
    };
  }

  async function collectImageTextOverlaySourceFromSelection(message) {
    let overlayReason = "";

    try {
      const target = await collectTextOverlayApplyTargetFromSelection();
      const source = await buildImageTextOverlaySourceFromTarget(target);
      const sessionId = buildTextOverlaySessionId();
      const overlaySession = {
        id: sessionId,
        clientRequestId: sanitizeClientRequestId(message && message.clientRequestId),
        operationLabel: sanitizeOperationLabel(message && message.operationLabel),
        selectionLabel: target.selectionLabel,
        targetNodeId: target.nodeId,
        targetNodeName: target.nodeName,
        preparedAt: new Date().toISOString(),
      };

      source.summary = source.summary || {};
      source.summary.canCreateOverlay = true;
      source.summary.overlayReason = "";
      source.summary.overlayMode = "group-selection";
      return {
        source: source,
        overlaySession: overlaySession,
      };
    } catch (error) {
      overlayReason = normalizeErrorMessage(error, "");
    }

    const fallbackSource = await collectReferenceSearchSourceFromSelection();
    if (!fallbackSource.summary || typeof fallbackSource.summary !== "object") {
      fallbackSource.summary = {};
    }
    fallbackSource.summary.canCreateOverlay = false;
    fallbackSource.summary.overlayReason = overlayReason;
    fallbackSource.summary.overlayMode = "";
    return {
      source: fallbackSource,
      overlaySession: null,
    };
  }

  async function collectTextOverlayApplyTargetFromSelection(selectionInput) {
    const selection = Array.isArray(selectionInput) ? selectionInput.filter(Boolean) : Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("Select a frame, group, or image layer first.");
    }

    if (selection.length !== 1) {
      throw new Error("Text overlay currently supports exactly one selected layer.");
    }

    const node = selection[0];
    if (!node || node.removed) {
      throw new Error("Could not read the selected layer.");
    }

    if ("locked" in node && node.locked) {
      throw new Error("Locked layers are not supported for text overlay.");
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      throw new Error("Rotated layers are not supported for text overlay yet.");
    }

    if (!node.parent || !canUseImageExtendParent(node.parent)) {
      throw new Error("The current parent container does not support grouping the text overlay result.");
    }

    if (
      "layoutMode" in node.parent &&
      typeof node.parent.layoutMode === "string" &&
      node.parent.layoutMode !== "NONE"
    ) {
      throw new Error("Auto-layout parents are not supported for text overlay yet.");
    }

    if (typeof node.exportAsync !== "function") {
      throw new Error("The selected layer cannot be exported for text overlay.");
    }

    const localBounds = getImageExtendLocalBounds(node);
    if (!localBounds) {
      throw new Error("Could not determine the selected layer bounds.");
    }

    return {
      selection: selection,
      selectionLabel: formatSelectionLabel(selection),
      nodeId: node.id,
      nodeName: safeName(node),
      localBounds: localBounds,
    };
  }

  async function buildImageTextOverlaySourceFromTarget(target) {
    const node = await figma.getNodeByIdAsync(target.nodeId);
    if (!node || node.removed || typeof node.exportAsync !== "function") {
      throw new Error("Could not export the selected layer for text overlay.");
    }

    const bytes = await node.exportAsync({
      format: "PNG",
      useAbsoluteBounds: false,
    });
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      throw new Error("The selected layer export is empty.");
    }

    return {
      sourceType: "image",
      image: {
        bytes: bytes,
        mimeType: "image/png",
        fileName: buildFileName(
          {
            nodeName: target.nodeName,
            imageHash: target.nodeId,
          },
          "png"
        ),
      },
      summary: {
        selectionLabel: target.selectionLabel,
        targetNodeName: target.nodeName,
        captureMode: "rendered-node",
        targetWidth: target.localBounds.width,
        targetHeight: target.localBounds.height,
        byteLength: bytes.length,
      },
    };
  }

  function shapeEditNeedsPlaceholderSource(node) {
    return !hasRenderablePaints(node && node.fills) && !hasRenderablePaints(node && node.strokes);
  }

  function hasRenderablePaints(paints) {
    return Array.isArray(paints) && paints.some(isRenderablePaint);
  }

  function isRenderablePaint(paint) {
    if (!paint || paint.visible === false) {
      return false;
    }

    if (paint.type === "IMAGE") {
      return typeof paint.imageHash === "string" && paint.imageHash.length > 0;
    }

    const opacity = typeof paint.opacity === "number" && Number.isFinite(paint.opacity) ? Number(paint.opacity) : 1;
    if (!(opacity > 0.001)) {
      return false;
    }

    return (
      paint.type === "SOLID" ||
      paint.type === "GRADIENT_LINEAR" ||
      paint.type === "GRADIENT_RADIAL" ||
      paint.type === "GRADIENT_ANGULAR" ||
      paint.type === "GRADIENT_DIAMOND"
    );
  }

  async function exportShapeEditPlaceholderPng(node) {
    const width = typeof node.width === "number" && Number.isFinite(node.width) ? Math.max(1, Math.round(node.width)) : 0;
    const height = typeof node.height === "number" && Number.isFinite(node.height) ? Math.max(1, Math.round(node.height)) : 0;
    if (!(width > 0) || !(height > 0)) {
      throw new Error(SHAPE_EXPORT_SOURCE_ERROR_MESSAGE);
    }

    const placeholder = figma.createRectangle();
    try {
      placeholder.name = "__pigma-shape-source-placeholder__";
      placeholder.resize(width, height);
      placeholder.fills = [createSolidPaintFromColor("#f4f4f5", 1)];
      placeholder.strokes = [];
      placeholder.x = -100000;
      placeholder.y = -100000;
      figma.currentPage.appendChild(placeholder);
      return await placeholder.exportAsync({
        format: "PNG",
        useAbsoluteBounds: false,
      });
    } finally {
      if (placeholder && !placeholder.removed) {
        placeholder.remove();
      }
    }
  }

  function sanitizeReferenceSearchQuery(value) {
    const normalized = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    return normalized ? normalized.slice(0, 120) : "";
  }

  function summarizeReferenceSearchQuery(query) {
    const text = sanitizeReferenceSearchQuery(query);
    if (text.length <= 44) {
      return text;
    }
    return text.slice(0, 43) + "...";
  }

  async function collectBoundsFitTargetsFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error("Select a frame, group, text, or image layer first.");
    }

    const state = {
      targets: [],
      containers: [],
      skipped: [],
      targetNodeIds: {},
      containerNodeIds: {},
      skippedNodeIds: {},
    };

    for (let index = 0; index < selection.length; index += 1) {
      await collectBoundsFitPlansFromNode(selection[index], state, true);
    }

    return {
      selection: selection,
      targets: state.targets,
      containers: state.containers,
      skipped: state.skipped,
    };
  }

  async function applyOriginalSizeFitToSelection(message) {
    const selection = Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error("Select an image layer to fit to original size first.");
    }

    const state = {
      targets: [],
      skipped: [],
      targetNodeIds: {},
      skippedNodeIds: {},
    };

    for (let index = 0; index < selection.length; index += 1) {
      await collectOriginalSizeFitTargetsFromNode(selection[index], state, true);
    }

    if (!state.targets.length) {
      throw new Error(buildOriginalSizeFitEmptySelectionMessage(state.skipped));
    }

    const skipped = state.skipped.slice();
    let appliedCount = 0;
    let unchangedCount = 0;

    for (let index = 0; index < state.targets.length; index += 1) {
      const status = await applyOriginalSizeFitTarget(state.targets[index], skipped);
      if (status === "applied") {
        appliedCount += 1;
      } else if (status === "unchanged") {
        unchangedCount += 1;
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        requestedCount: selection.length,
        eligibleCount: state.targets.length,
        appliedCount: appliedCount,
        unchangedCount: unchangedCount,
        skippedCount: skipped.length,
      },
      skipped: skipped.slice(0, 24),
    };
  }

  async function collectOriginalSizeFitTargetsFromNode(node, state, isRootSelection) {
    if (!node || node.removed) {
      if (isRootSelection) {
        appendOriginalSizeFitSkipped(state, node, "Could not read the selected layer.");
      }
      return false;
    }

    if (!isBoundsFitNodeVisible(node)) {
      if (isRootSelection) {
        appendOriginalSizeFitSkipped(state, node, "Hidden layers cannot be fit to original size.");
      }
      return false;
    }

    if ("locked" in node && node.locked) {
      if (isRootSelection) {
        appendOriginalSizeFitSkipped(state, node, "Locked layers cannot be fit to original size.");
      }
      return false;
    }

    if (hasChildren(node)) {
      let hasEligibleDescendant = false;
      for (let index = 0; index < node.children.length; index += 1) {
        if (await collectOriginalSizeFitTargetsFromNode(node.children[index], state, false)) {
          hasEligibleDescendant = true;
        }
      }

      if (isRootSelection && !hasEligibleDescendant) {
        appendOriginalSizeFitSkipped(
          state,
          node,
          "Could not find an image fill layer to fit to original size in the selection."
        );
      }
      return hasEligibleDescendant;
    }

    const target = await analyzeOriginalSizeFitNode(node, isRootSelection);
    if (target && target.target) {
      appendOriginalSizeFitTarget(target.target, state);
      return true;
    }

    if (target && target.skipped && isRootSelection) {
      appendOriginalSizeFitSkipped(state, node, target.skipped.reason);
    }
    return false;
  }

  async function analyzeOriginalSizeFitNode(node, isRootSelection) {
    if (!node || node.removed) {
      return buildOriginalSizeFitSkipped(node, "Could not read the selected layer.");
    }

    if (!canResizeBoundsFitNode(node)) {
      return buildOriginalSizeFitSkipped(node, "This layer cannot be resized.");
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      return buildOriginalSizeFitSkipped(node, "Rotated image layers are not supported yet.");
    }

    const fills = getNodeFills(node);
    const fillIndex = getPrimaryVisibleImageFillIndex(fills);
    if (fillIndex < 0) {
      return isRootSelection ? buildOriginalSizeFitSkipped(node, "Could not find an image fill.") : null;
    }

    const fill = fills[fillIndex];
    const image = fill && fill.imageHash ? figma.getImageByHash(fill.imageHash) : null;
    if (!image || typeof image.getSizeAsync !== "function") {
      return buildOriginalSizeFitSkipped(node, "Could not find the original image object.");
    }

    const size = await image.getSizeAsync();
    const sourceWidth = size && typeof size.width === "number" ? size.width : 0;
    const sourceHeight = size && typeof size.height === "number" ? size.height : 0;
    if (!(sourceWidth > 0) || !(sourceHeight > 0)) {
      return buildOriginalSizeFitSkipped(node, "Could not read the original image size.");
    }

    return {
      target: {
        nodeId: node.id,
        nodeName: safeName(node),
        fillIndex: fillIndex,
        originalHash: fill.imageHash,
        sourceWidth: sourceWidth,
        sourceHeight: sourceHeight,
      },
    };
  }

  function appendOriginalSizeFitTarget(target, state) {
    if (!target || !target.nodeId || !state || !state.targetNodeIds) {
      return;
    }
    if (state.targetNodeIds[target.nodeId]) {
      return;
    }
    state.targetNodeIds[target.nodeId] = true;
    state.targets.push(target);
  }

  function appendOriginalSizeFitSkipped(state, node, reason) {
    if (!state || !Array.isArray(state.skipped)) {
      return;
    }
    const skipped = buildOriginalSizeFitSkipped(node, reason).skipped;
    const key = skipped.nodeId || skipped.nodeName || skipped.reason;
    if (key && state.skippedNodeIds && state.skippedNodeIds[key]) {
      return;
    }
    if (key && state.skippedNodeIds) {
      state.skippedNodeIds[key] = true;
    }
    state.skipped.push(skipped);
  }

  async function collectBoundsFitPlansFromNode(node, state, isRootSelection) {
    if (!node || node.removed) {
      if (isRootSelection) {
        appendBoundsFitSkipped(buildBoundsFitSkipped(node, "The selected layer could not be read."), state);
      }
      return false;
    }

    if (!isBoundsFitNodeVisible(node)) {
      if (isRootSelection) {
        appendBoundsFitSkipped(buildBoundsFitSkipped(node, "Hidden layers are not supported for bounds-fit."), state);
      }
      return false;
    }

    if ("locked" in node && node.locked) {
      if (isRootSelection) {
        appendBoundsFitSkipped(buildBoundsFitSkipped(node, "Locked layers are not supported for bounds-fit."), state);
      }
      return false;
    }

    if (hasChildren(node)) {
      let hasEligibleDescendant = false;
      for (let index = 0; index < node.children.length; index += 1) {
        if (await collectBoundsFitPlansFromNode(node.children[index], state, false)) {
          hasEligibleDescendant = true;
        }
      }

      if (hasEligibleDescendant && canApplyBoundsFitContainer(node)) {
        appendBoundsFitContainerPlan(node, state, {
          selectedRoot: isRootSelection,
        });
      } else if (isRootSelection && !hasEligibleDescendant) {
        appendBoundsFitSkipped(
          buildBoundsFitSkipped(
            node,
            isBoundsFitContainerTreeNode(node)
              ? "Could not find visible text or image layers inside the selected container."
              : buildBoundsFitUnsupportedRootReason(node)
          ),
          state
        );
      }

      return hasEligibleDescendant;
    }

    let analysis = null;
    if (isBoundsFitTextSelectionNodeCandidate(node)) {
      analysis = await analyzeBoundsFitTextSelectionNode(node);
    } else if (isBoundsFitImageSelectionNodeCandidate(node)) {
      analysis = await analyzeBoundsFitImageSelectionNode(node);
    } else {
      if (isRootSelection) {
        appendBoundsFitSkipped(buildBoundsFitSkipped(node, buildBoundsFitUnsupportedRootReason(node)), state);
      }
      return false;
    }

    if (analysis && analysis.target) {
      appendBoundsFitTarget(analysis.target, state);
      return true;
    }

    if (analysis && analysis.skipped && isRootSelection) {
      appendBoundsFitSkipped(analysis, state);
    }

    return false;
  }

  function appendBoundsFitTarget(target, state) {
    if (!target || !target.nodeId || !state || !state.targetNodeIds) {
      return;
    }

    if (state.targetNodeIds[target.nodeId]) {
      return;
    }

    state.targetNodeIds[target.nodeId] = true;
    state.targets.push(target);
  }

  function appendBoundsFitContainerPlan(node, state, options) {
    if (!node || !node.id || !state || !state.containerNodeIds) {
      return;
    }

    const existingIndex =
      typeof state.containerNodeIds[node.id] === "number" && state.containerNodeIds[node.id] >= 0
        ? state.containerNodeIds[node.id]
        : -1;
    if (existingIndex >= 0) {
      if (options && options.selectedRoot === true && state.containers[existingIndex]) {
        state.containers[existingIndex].selectedRoot = true;
      }
      return;
    }

    state.containerNodeIds[node.id] = state.containers.length;
    state.containers.push({
      nodeId: node.id,
      nodeName: safeName(node),
      nodeType: typeof node.type === "string" ? node.type : "",
      depth: getBoundsFitNodeDepth(node),
      selectedRoot: !!(options && options.selectedRoot === true),
    });
  }

  function appendBoundsFitSkipped(analysis, state) {
    const skipped = analysis && analysis.skipped ? analysis.skipped : analysis;
    if (!skipped || !state || !Array.isArray(state.skipped)) {
      return;
    }

    const key = skipped.nodeId || skipped.nodeName || skipped.reason;
    if (key && state.skippedNodeIds && state.skippedNodeIds[key]) {
      return;
    }

    if (key && state.skippedNodeIds) {
      state.skippedNodeIds[key] = true;
    }

    state.skipped.push(skipped);
  }

  function isBoundsFitNodeVisible(node) {
    return !!node && (!("visible" in node) || node.visible !== false);
  }

  function isBoundsFitContainerTreeNode(node) {
    if (!node || !hasChildren(node)) {
      return false;
    }

    return node.type === "FRAME" || node.type === "GROUP" || node.type === "SECTION" || node.type === "INSTANCE";
  }

  function isBoundsFitAutoLayoutNode(node) {
    return !!node && "layoutMode" in node && typeof node.layoutMode === "string" && node.layoutMode !== "NONE";
  }

  function isBoundsFitAutoLayoutChildNode(node) {
    return !!node && !!node.parent && isBoundsFitAutoLayoutNode(node.parent);
  }

  function canMoveBoundsFitNode(node) {
    if (!node) {
      return false;
    }

    if (!isBoundsFitAutoLayoutChildNode(node)) {
      return true;
    }

    return "layoutPositioning" in node && node.layoutPositioning === "ABSOLUTE";
  }

  function shouldPreserveBoundsFitTextWidth(node) {
    if (!node || node.type !== "TEXT") {
      return false;
    }

    if (isBoundsFitAutoLayoutChildNode(node)) {
      return true;
    }

    if ("textAutoResize" in node && typeof node.textAutoResize === "string" && node.textAutoResize === "NONE") {
      return true;
    }

    return false;
  }

  function shouldResizeBoundsFitContainer(node) {
    if (!node || node.type === "GROUP") {
      return false;
    }

    if (isBoundsFitAutoLayoutChildNode(node) && (!("layoutPositioning" in node) || node.layoutPositioning !== "ABSOLUTE")) {
      return false;
    }

    return true;
  }

  function canApplyBoundsFitContainer(node) {
    if (!isBoundsFitContainerTreeNode(node)) {
      return false;
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      return false;
    }

    if (node.type !== "GROUP" && !canResizeBoundsFitNode(node)) {
      return false;
    }

    return true;
  }

  function buildBoundsFitUnsupportedRootReason(node) {
    if (!node) {
      return "Could not find an eligible layer for bounds-fit.";
    }

    if (hasChildren(node)) {
      return buildBoundsFitContainerUnsupportedReason(node);
    }

    return "Bounds-fit currently supports text layers, image layers, and groups/frames that contain them.";
  }

  function buildBoundsFitContainerUnsupportedReason(node) {
    if (!node) {
      return "Could not find an eligible container for bounds-fit.";
    }

    if (!isBoundsFitContainerTreeNode(node)) {
      return "Bounds-fit container mode currently supports groups, frames, sections, and instances that contain text or image layers.";
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      return "Rotated containers are not supported yet.";
    }

    if (node.type !== "GROUP" && !canResizeBoundsFitNode(node)) {
      return "Bounds-fit could not resize the selected container.";
    }

    return "Bounds-fit could not find visible text or image layers inside the selected container.";
  }

  function isBoundsFitTextSelectionNodeCandidate(node) {
    return !!node && node.type === "TEXT";
  }

  function isBoundsFitImageSelectionNodeCandidate(node) {
    return !!node && !hasChildren(node) && hasSimpleVisibleImagePaint(node);
  }

  function isBoundsFitPreferredContentNode(node) {
    return isBoundsFitTextSelectionNodeCandidate(node) || isBoundsFitImageSelectionNodeCandidate(node);
  }

  function canResizeBoundsFitNode(node) {
    return !!node && (typeof node.resizeWithoutConstraints === "function" || typeof node.resize === "function");
  }

  function getBoundsFitNodeDepth(node) {
    let depth = 0;
    let current = node && node.parent ? node.parent : null;
    while (current) {
      depth += 1;
      current = current.parent;
    }
    return depth;
  }

  const IMAGE_COMPOSITE_MAX_LAYER_COUNT = 8;

  async function collectImageCompositeTargetsFromSelection(selectionInput) {
    const selection = Array.isArray(selectionInput)
      ? selectionInput.filter(Boolean)
      : Array.from(figma.currentPage.selection || []).filter(Boolean);
    if (!selection.length) {
      throw new Error("Select a frame or 2 to 8 visible visual layers first.");
    }
    const rootSelection = resolveImageCompositeSelectionRoot(selection);
    const rootRect = rootSelection ? getBoundsFitNodeRect(rootSelection) : null;
    const candidateNodes = collectImageCompositeCandidateNodes(selection);

    const layers = [];
    const skipped = [];

    for (let index = 0; index < candidateNodes.length; index += 1) {
      const analysis = await analyzeImageCompositeSelectionNode(candidateNodes[index], {
        rootRect: rootRect,
      });
      if (analysis && analysis.target) {
        layers.push(analysis.target);
      } else if (analysis && analysis.skipped) {
        skipped.push(analysis.skipped);
      }
    }

    if (!layers.length) {
      throw new Error(buildImageCompositeEmptySelectionMessage(layers, skipped));
    }

    layers.sort(function (left, right) {
      return compareSceneNodeCanvasOrder(left.node, right.node);
    });

    const trimmedLayers = trimImageCompositeLayersForPayload(layers, skipped, IMAGE_COMPOSITE_MAX_LAYER_COUNT);

    const unionRect =
      rootRect ||
      unionAbsoluteRects(
        trimmedLayers.map(function (layer) {
          return layer.visibleRect;
        })
      );
    if (!unionRect) {
      throw new Error(buildImageCompositeEmptySelectionMessage(trimmedLayers, skipped));
    }

    for (let index = 0; index < trimmedLayers.length; index += 1) {
      const layer = trimmedLayers[index];
      layer.role = index === 0 ? "background" : "foreground";
      layer.orderIndex = index;
      layer.offsetX = roundBoundsFitMetric(layer.visibleRect.x - unionRect.x);
      layer.offsetY = roundBoundsFitMetric(layer.visibleRect.y - unionRect.y);
    }

    return {
      selection: selection,
      selectedRoot: rootSelection,
      layers: trimmedLayers,
      skipped: skipped,
      unionRect: unionRect,
    };
  }

  function resolveImageCompositeSelectionRoot(selection) {
    if (!Array.isArray(selection) || selection.length !== 1) {
      return null;
    }

    const node = selection[0];
    if (!node || node.removed || !hasChildren(node)) {
      return null;
    }

    return getBoundsFitNodeRect(node) ? node : null;
  }

  function getImageCompositeLayerArea(layer) {
    const rect = layer && layer.visibleRect ? layer.visibleRect : null;
    const width = rect && Number(rect.width) > 0 ? Number(rect.width) : 0;
    const height = rect && Number(rect.height) > 0 ? Number(rect.height) : 0;
    return width * height;
  }

  function getImageCompositeLayerWeight(layer) {
    if (!layer || layer.layerKind === "image") {
      return 3;
    }
    if (layer.layerKind === "text") {
      return 2.4;
    }
    return 1.8;
  }

  function trimImageCompositeLayersForPayload(layers, skipped, maxCount) {
    if (!Array.isArray(layers) || layers.length <= maxCount) {
      return Array.isArray(layers) ? layers.slice() : [];
    }

    const ranked = layers
      .slice()
      .sort(function (left, right) {
        const scoreDelta =
          getImageCompositeLayerArea(right) * getImageCompositeLayerWeight(right) -
          getImageCompositeLayerArea(left) * getImageCompositeLayerWeight(left);
        if (Math.abs(scoreDelta) > 0.01) {
          return scoreDelta > 0 ? 1 : -1;
        }
        return compareSceneNodeCanvasOrder(left.node, right.node);
      })
      .slice(0, maxCount);
    const kept = {};
    for (let index = 0; index < ranked.length; index += 1) {
      kept[ranked[index].nodeId] = true;
    }

    const result = [];
    for (let index = 0; index < layers.length; index += 1) {
      const layer = layers[index];
      if (layer && kept[layer.nodeId]) {
        result.push(layer);
        continue;
      }
      skipped.push({
        nodeId: layer && typeof layer.nodeId === "string" ? layer.nodeId : "",
        nodeName: layer && typeof layer.nodeName === "string" ? layer.nodeName : "",
        reason: "Skipped because image composite currently keeps up to 8 of the most prominent visible layers.",
      });
    }

    return result;
  }

  function collectImageCompositeCandidateNodes(selection) {
    const result = [];
    const seen = {};
    for (let index = 0; index < selection.length; index += 1) {
      appendImageCompositeCandidateNode(selection[index], result, seen);
    }
    return result;
  }

  function appendImageCompositeCandidateNode(node, result, seen) {
    if (!node || node.removed || !Array.isArray(result) || !seen) {
      return;
    }

    if (hasChildren(node)) {
      for (let index = 0; index < node.children.length; index += 1) {
        appendImageCompositeCandidateNode(node.children[index], result, seen);
      }
      return;
    }

    if (seen[node.id]) {
      return;
    }

    seen[node.id] = true;
    result.push(node);
  }

  function roundBoundsFitMetric(value) {
    return Math.round((Number(value) || 0) * 1000) / 1000;
  }

  function roundBoundsFitPixel(value) {
    return Math.round(Number(value) || 0);
  }

  function unionAbsoluteRects(rects) {
    if (!Array.isArray(rects) || !rects.length) {
      return null;
    }

    let left = Infinity;
    let top = Infinity;
    let right = -Infinity;
    let bottom = -Infinity;

    for (let index = 0; index < rects.length; index += 1) {
      const rect = rects[index];
      if (!rect) {
        continue;
      }
      left = Math.min(left, rect.x);
      top = Math.min(top, rect.y);
      right = Math.max(right, rect.x + rect.width);
      bottom = Math.max(bottom, rect.y + rect.height);
    }

    const width = right - left;
    const height = bottom - top;
    if (!(width > 0) || !(height > 0) || !Number.isFinite(left) || !Number.isFinite(top)) {
      return null;
    }

    return {
      x: roundBoundsFitMetric(left),
      y: roundBoundsFitMetric(top),
      width: roundBoundsFitMetric(width),
      height: roundBoundsFitMetric(height),
    };
  }

  function readBoundsFitRectCandidate(getter) {
    if (typeof getter !== "function") {
      return null;
    }
    try {
      const rect = getter();
      if (!rect) {
        return null;
      }
      const width = Number(rect.width) || 0;
      const height = Number(rect.height) || 0;
      if (!(width > 0) || !(height > 0)) {
        return null;
      }
      return {
        x: roundBoundsFitMetric(rect.x),
        y: roundBoundsFitMetric(rect.y),
        width: roundBoundsFitMetric(width),
        height: roundBoundsFitMetric(height),
      };
    } catch (error) {
      return null;
    }
  }

  function getBoundsFitTransformRect(node) {
    if (!node) {
      return null;
    }

    try {
      const width = Number(node.width) || 0;
      const height = Number(node.height) || 0;
      if (!(width > 0) || !(height > 0)) {
        return null;
      }

      const transform = Array.isArray(node.absoluteTransform) ? node.absoluteTransform : null;
      if (!transform || transform.length < 2) {
        return null;
      }

      const row0 = Array.isArray(transform[0]) ? transform[0] : null;
      const row1 = Array.isArray(transform[1]) ? transform[1] : null;
      if (!row0 || !row1 || row0.length < 3 || row1.length < 3) {
        return null;
      }

      const a = Number(row0[0]) || 0;
      const c = Number(row0[1]) || 0;
      const tx = Number(row0[2]) || 0;
      const b = Number(row1[0]) || 0;
      const d = Number(row1[1]) || 0;
      const ty = Number(row1[2]) || 0;
      const points = [
        { x: tx, y: ty },
        { x: a * width + tx, y: b * width + ty },
        { x: c * height + tx, y: d * height + ty },
        { x: a * width + c * height + tx, y: b * width + d * height + ty },
      ];
      let left = Infinity;
      let top = Infinity;
      let right = -Infinity;
      let bottom = -Infinity;
      for (let index = 0; index < points.length; index += 1) {
        const point = points[index];
        left = Math.min(left, point.x);
        top = Math.min(top, point.y);
        right = Math.max(right, point.x);
        bottom = Math.max(bottom, point.y);
      }
      if (!Number.isFinite(left) || !Number.isFinite(top) || !(right > left) || !(bottom > top)) {
        return null;
      }

      return {
        x: roundBoundsFitMetric(left),
        y: roundBoundsFitMetric(top),
        width: roundBoundsFitMetric(right - left),
        height: roundBoundsFitMetric(bottom - top),
      };
    } catch (error) {
      return null;
    }
  }

  function getBoundsFitNodeRect(node) {
    const isTextNode = !!node && node.type === "TEXT";
    const transformRect = getBoundsFitTransformRect(node);

    const preferredRect = isTextNode
      ? readBoundsFitRectCandidate(function () {
          return node.absoluteRenderBounds;
        }) ||
        readBoundsFitRectCandidate(function () {
          return node.absoluteBoundingBox;
        }) ||
        transformRect
      : transformRect ||
        readBoundsFitRectCandidate(function () {
          return node.absoluteBoundingBox;
        }) ||
        readBoundsFitRectCandidate(function () {
          return node.absoluteRenderBounds;
        });

    return preferredRect || null;
  }

  function getBoundsFitNodeLayoutRect(node) {
    return (
      readBoundsFitRectCandidate(function () {
        return node.absoluteBoundingBox;
      }) ||
      getBoundsFitTransformRect(node) ||
      getBoundsFitNodeRect(node)
    );
  }

  function getBoundsFitNodeRenderRect(node) {
    return readBoundsFitRectCandidate(function () {
      return node.absoluteRenderBounds;
    });
  }

  function intersectBoundsFitRects(baseRect, clipRect) {
    if (!baseRect || !clipRect) {
      return null;
    }

    const left = Math.max(baseRect.x, clipRect.x);
    const top = Math.max(baseRect.y, clipRect.y);
    const right = Math.min(baseRect.x + baseRect.width, clipRect.x + clipRect.width);
    const bottom = Math.min(baseRect.y + baseRect.height, clipRect.y + clipRect.height);
    const width = right - left;
    const height = bottom - top;

    if (!(width > 0) || !(height > 0)) {
      return null;
    }

    return {
      x: roundBoundsFitMetric(left),
      y: roundBoundsFitMetric(top),
      width: roundBoundsFitMetric(width),
      height: roundBoundsFitMetric(height),
    };
  }

  function areBoundsFitRectsEqual(leftRect, rightRect) {
    if (!leftRect || !rightRect) {
      return false;
    }

    return (
      Math.abs(leftRect.x - rightRect.x) <= 0.01 &&
      Math.abs(leftRect.y - rightRect.y) <= 0.01 &&
      Math.abs(leftRect.width - rightRect.width) <= 0.01 &&
      Math.abs(leftRect.height - rightRect.height) <= 0.01
    );
  }

  function getBoundsFitAnalysisContext(node) {
    const nodeRect = getBoundsFitNodeRect(node);
    if (!nodeRect) {
      return null;
    }

    let visibleRect = {
      x: nodeRect.x,
      y: nodeRect.y,
      width: nodeRect.width,
      height: nodeRect.height,
    };
    let clipped = false;
    let current = node.parent;

    while (current) {
      if ("clipsContent" in current && current.clipsContent === true) {
        const clipRect = getBoundsFitNodeRect(current);
        if (clipRect) {
          const nextVisibleRect = intersectBoundsFitRects(visibleRect, clipRect);
          if (!nextVisibleRect) {
            return null;
          }
          if (!areBoundsFitRectsEqual(nextVisibleRect, visibleRect)) {
            clipped = true;
          }
          visibleRect = nextVisibleRect;
        }
      }
      current = current.parent;
    }

    return {
      nodeRect: nodeRect,
      visibleRect: visibleRect,
      clipped: clipped,
      offsetX: roundBoundsFitMetric(visibleRect.x - nodeRect.x),
      offsetY: roundBoundsFitMetric(visibleRect.y - nodeRect.y),
    };
  }

  function positionBoundsFitPreviewClone(sourceNode, clonedNode, visibleRect) {
    if (
      "relativeTransform" in clonedNode &&
      Array.isArray(clonedNode.relativeTransform) &&
      Array.isArray(sourceNode.absoluteTransform) &&
      sourceNode.absoluteTransform.length >= 2
    ) {
      clonedNode.relativeTransform = [
        [
          sourceNode.absoluteTransform[0][0],
          sourceNode.absoluteTransform[0][1],
          roundBoundsFitMetric(sourceNode.absoluteTransform[0][2] - visibleRect.x),
        ],
        [
          sourceNode.absoluteTransform[1][0],
          sourceNode.absoluteTransform[1][1],
          roundBoundsFitMetric(sourceNode.absoluteTransform[1][2] - visibleRect.y),
        ],
      ];
      return;
    }

    if ("x" in clonedNode && typeof clonedNode.x === "number" && "y" in clonedNode && typeof clonedNode.y === "number") {
      clonedNode.x = roundBoundsFitMetric(-visibleRect.x);
      clonedNode.y = roundBoundsFitMetric(-visibleRect.y);
      return;
    }

    throw new Error("Could not position the preview clone for bounds-fit analysis.");
  }

  function resolveBoundsFitRenderedExportScale(nodeWidth, nodeHeight, sourceWidth, sourceHeight) {
    // Bounds-fit only needs the currently visible pixels to shrink the layer.
    // Exporting at the on-canvas size is the most stable path for edited Figma images.
    return 1;
  }

  function resolveBoundsFitSourceImageAnalysis(fill, nodeWidth, nodeHeight, sourceWidth, sourceHeight, analysisContext) {
    if (!fill || !(nodeWidth > 0) || !(nodeHeight > 0) || !(sourceWidth > 0) || !(sourceHeight > 0) || !analysisContext) {
      return null;
    }

    const scaleMode = typeof fill.scaleMode === "string" ? fill.scaleMode : "FILL";
    const fillRotation = typeof fill.rotation === "number" && Number.isFinite(fill.rotation) ? fill.rotation : 0;
    if (!!fill.imageTransform || Math.abs(fillRotation) > 0.01 || scaleMode === "CROP" || scaleMode === "TILE") {
      return null;
    }

    const localVisibleRect = {
      x: roundBoundsFitMetric(analysisContext.offsetX),
      y: roundBoundsFitMetric(analysisContext.offsetY),
      width: roundBoundsFitMetric(analysisContext.visibleRect.width),
      height: roundBoundsFitMetric(analysisContext.visibleRect.height),
    };

    if (scaleMode === "STRETCH") {
      const scaleX = sourceWidth / nodeWidth;
      const scaleY = sourceHeight / nodeHeight;
      return {
        analysisOffsetX: localVisibleRect.x,
        analysisOffsetY: localVisibleRect.y,
        analysisWidth: localVisibleRect.width,
        analysisHeight: localVisibleRect.height,
        rasterAnalysisOffsetX: roundBoundsFitMetric(localVisibleRect.x * scaleX),
        rasterAnalysisOffsetY: roundBoundsFitMetric(localVisibleRect.y * scaleY),
        rasterAnalysisWidth: roundBoundsFitMetric(localVisibleRect.width * scaleX),
        rasterAnalysisHeight: roundBoundsFitMetric(localVisibleRect.height * scaleY),
      };
    }

    const uniformScale =
      scaleMode === "FIT" ? Math.min(nodeWidth / sourceWidth, nodeHeight / sourceHeight) : Math.max(nodeWidth / sourceWidth, nodeHeight / sourceHeight);
    if (!(uniformScale > 0)) {
      return null;
    }

    const displayRect = {
      x: roundBoundsFitMetric((nodeWidth - sourceWidth * uniformScale) / 2),
      y: roundBoundsFitMetric((nodeHeight - sourceHeight * uniformScale) / 2),
      width: roundBoundsFitMetric(sourceWidth * uniformScale),
      height: roundBoundsFitMetric(sourceHeight * uniformScale),
    };
    const visibleImageRect = intersectBoundsFitRects(localVisibleRect, displayRect);
    if (!visibleImageRect) {
      return null;
    }

    return {
      analysisOffsetX: visibleImageRect.x,
      analysisOffsetY: visibleImageRect.y,
      analysisWidth: visibleImageRect.width,
      analysisHeight: visibleImageRect.height,
      rasterAnalysisOffsetX: roundBoundsFitMetric((visibleImageRect.x - displayRect.x) / uniformScale),
      rasterAnalysisOffsetY: roundBoundsFitMetric((visibleImageRect.y - displayRect.y) / uniformScale),
      rasterAnalysisWidth: roundBoundsFitMetric(visibleImageRect.width / uniformScale),
      rasterAnalysisHeight: roundBoundsFitMetric(visibleImageRect.height / uniformScale),
    };
  }

  async function exportBoundsFitRenderedAnalysisBytes(node, analysisContext, exportScale) {
    const visibleRect = analysisContext && analysisContext.visibleRect ? analysisContext.visibleRect : null;
    if (!visibleRect) {
      return new Uint8Array(0);
    }

    const resolvedExportScale =
      Number.isFinite(Number(exportScale)) && Number(exportScale) > 0 ? Number(exportScale) : 1;
    const exportSettings = {
      format: "PNG",
      // Bounds-fit needs the full node/frame area so the UI can crop visible pixels itself.
      // Using absolute bounds also avoids Figma's internal render-bounds crop path, which
      // has been unstable for some edited images such as background-removed assets.
      useAbsoluteBounds: true,
    };
    if (Math.abs(resolvedExportScale - 1) > 0.01) {
      exportSettings.constraint = {
        type: "SCALE",
        value: resolvedExportScale,
      };
    }

    if (!analysisContext.clipped) {
      return await node.exportAsync(exportSettings);
    }

    const preview = figma.createFrame();
    let clonedNode = null;

    try {
      preview.name = "__pigma-bounds-fit-preview__";
      preview.resize(visibleRect.width, visibleRect.height);
      preview.clipsContent = true;
      preview.fills = [];
      preview.strokes = [];
      preview.x = visibleRect.x;
      preview.y = visibleRect.y;
      figma.currentPage.appendChild(preview);

      clonedNode = node.clone();
      preview.appendChild(clonedNode);
      positionBoundsFitPreviewClone(node, clonedNode, visibleRect);

      return await preview.exportAsync(exportSettings);
    } finally {
      if (clonedNode && !clonedNode.removed) {
        clonedNode.remove();
      }
      if (preview && !preview.removed) {
        preview.remove();
      }
    }
  }

  async function analyzeBoundsFitImageSelectionNode(node) {
    if (!node || node.removed) {
      return buildBoundsFitSkipped(node, "The selected image layer could not be read.");
    }

    if (hasChildren(node)) {
      return buildBoundsFitSkipped(node, "Bounds-fit does not support frames, groups, or layers with children yet.");
    }

    if ("locked" in node && node.locked) {
      return buildBoundsFitSkipped(node, "Locked image layers are not supported for bounds-fit.");
    }

    if (!("resize" in node) || typeof node.resize !== "function") {
      return buildBoundsFitSkipped(node, "Bounds-fit only supports image layers that can be resized directly.");
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      return buildBoundsFitSkipped(node, "Rotated image layers are not supported yet.");
    }

    if (!hasSimpleVisibleImagePaint(node)) {
      return buildBoundsFitSkipped(node, "Bounds-fit requires a layer with exactly one visible IMAGE fill.");
    }

    if (hasVisiblePaints(node.strokes)) {
      return buildBoundsFitSkipped(node, "Image layers with visible strokes are not supported yet.");
    }

    if (hasVisibleEffects(node.effects)) {
      return buildBoundsFitSkipped(node, "Image layers with visible effects are not supported yet.");
    }

    const fills = getNodeFills(node);
    const fillIndex = getPrimaryVisibleImageFillIndex(fills);
    if (fillIndex < 0) {
      return buildBoundsFitSkipped(node, "Could not find an IMAGE fill.");
    }

    const fill = fills[fillIndex];

    const nodeWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const nodeHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(nodeWidth > 0) || !(nodeHeight > 0)) {
      return buildBoundsFitSkipped(node, "Could not calculate the current layer size.");
    }

    const image = fill.imageHash ? figma.getImageByHash(fill.imageHash) : null;
    if (!image) {
      return buildBoundsFitSkipped(node, "Could not find the source image data.");
    }

    const size = await image.getSizeAsync();
    const sourceWidth = size && typeof size.width === "number" ? size.width : 0;
    const sourceHeight = size && typeof size.height === "number" ? size.height : 0;
    if (!(sourceWidth > 0) || !(sourceHeight > 0)) {
      return buildBoundsFitSkipped(node, "Could not read the source image size.");
    }

    const renderedExportScale = resolveBoundsFitRenderedExportScale(nodeWidth, nodeHeight, sourceWidth, sourceHeight);
    // Bounds-fit must follow the pixels currently visible on canvas. Raw getImageByHash() bytes can lag
    // behind in-editor image edits such as background removal, so we always analyze the rendered node.
    const analysisContext = getBoundsFitAnalysisContext(node);
    if (!analysisContext) {
      return buildBoundsFitSkipped(node, "Could not calculate the visible bounds for analysis.");
    }

    const directSourceAnalysis = resolveBoundsFitSourceImageAnalysis(
      fill,
      nodeWidth,
      nodeHeight,
      sourceWidth,
      sourceHeight,
      analysisContext
    );
    if (directSourceAnalysis) {
      const directBytes = await image.getBytesAsync();
      if (!directBytes || typeof directBytes.length !== "number" || directBytes.length <= 0) {
        return buildBoundsFitSkipped(node, "Could not read the source image bytes.");
      }

      return {
        target: {
          nodeId: node.id,
          nodeName: safeName(node),
          targetKind: "image-fill",
          analysisMode: "source-image",
          fillIndex: fillIndex,
          originalHash: fill.imageHash,
          sourceWidth: sourceWidth,
          sourceHeight: sourceHeight,
          analysisOffsetX: directSourceAnalysis.analysisOffsetX,
          analysisOffsetY: directSourceAnalysis.analysisOffsetY,
          analysisWidth: directSourceAnalysis.analysisWidth,
          analysisHeight: directSourceAnalysis.analysisHeight,
          rasterAnalysisOffsetX: directSourceAnalysis.rasterAnalysisOffsetX,
          rasterAnalysisOffsetY: directSourceAnalysis.rasterAnalysisOffsetY,
          rasterAnalysisWidth: directSourceAnalysis.rasterAnalysisWidth,
          rasterAnalysisHeight: directSourceAnalysis.rasterAnalysisHeight,
          fileName: buildFileName(
            {
              nodeName: safeName(node),
              imageHash: fill.imageHash,
            },
            detectImageExtension(directBytes) === "bin" ? "png" : detectImageExtension(directBytes)
          ),
          mimeType: detectImageMimeType(directBytes),
          bytes: directBytes,
        },
      };
    }

    const exportSettings = {
      format: "PNG",
      useAbsoluteBounds: true,
    };
    if (Math.abs(renderedExportScale - 1) > 0.01) {
      exportSettings.constraint = {
        type: "SCALE",
        value: renderedExportScale,
      };
    }

    let bytes = new Uint8Array(0);
    let analysisMode = "rendered-node";
    let mimeType = "image/png";
    let rasterAnalysisOffsetX = analysisContext.offsetX;
    let rasterAnalysisOffsetY = analysisContext.offsetY;
    let rasterAnalysisWidth = analysisContext.visibleRect.width;
    let rasterAnalysisHeight = analysisContext.visibleRect.height;

    try {
      bytes = await node.exportAsync(exportSettings);
    } catch (error) {
      let previewExportError = null;
      try {
        // Some edited fills (notably background-removed assets in crop mode) can fail
        // direct exportAsync(). Exporting a temporary preview clone still captures the
        // currently visible pixels and keeps bounds-fit usable for those layers.
        bytes = await exportBoundsFitRenderedAnalysisBytes(
          node,
          {
            visibleRect: analysisContext.visibleRect,
            clipped: true,
          },
          renderedExportScale
        );
        if (bytes && typeof bytes.length === "number" && bytes.length > 0) {
          rasterAnalysisOffsetX = 0;
          rasterAnalysisOffsetY = 0;
          rasterAnalysisWidth = analysisContext.visibleRect.width;
          rasterAnalysisHeight = analysisContext.visibleRect.height;
        }
      } catch (previewError) {
        previewExportError = previewError;
      }

      if (bytes && typeof bytes.length === "number" && bytes.length > 0) {
        // Recovered with the preview export path.
      } else {
        const scaleMode = typeof fill.scaleMode === "string" ? fill.scaleMode : "FILL";
        const fillRotation = typeof fill.rotation === "number" && Number.isFinite(fill.rotation) ? fill.rotation : 0;
        const canFallbackToSourceImage =
          scaleMode !== "CROP" &&
          scaleMode !== "TILE" &&
          !fill.imageTransform &&
          Math.abs(fillRotation) <= 0.01 &&
          hasMatchingAspectRatio(nodeWidth, nodeHeight, sourceWidth, sourceHeight);
        if (!canFallbackToSourceImage) {
          return buildBoundsFitSkipped(
            node,
            normalizeErrorMessage(
              previewExportError,
              normalizeErrorMessage(error, "Could not export the currently visible image bytes.")
            )
          );
        }

        bytes = await image.getBytesAsync();
        if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
          return buildBoundsFitSkipped(node, "Could not read the source image bytes.");
        }

        analysisMode = "source-image";
        mimeType = detectImageMimeType(bytes);
        const scaleX = sourceWidth / nodeWidth;
        const scaleY = sourceHeight / nodeHeight;
        rasterAnalysisOffsetX = roundBoundsFitMetric(analysisContext.offsetX * scaleX);
        rasterAnalysisOffsetY = roundBoundsFitMetric(analysisContext.offsetY * scaleY);
        rasterAnalysisWidth = roundBoundsFitMetric(analysisContext.visibleRect.width * scaleX);
        rasterAnalysisHeight = roundBoundsFitMetric(analysisContext.visibleRect.height * scaleY);
      }
    }
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      return buildBoundsFitSkipped(node, "Could not export the currently visible image bytes.");
    }

    return {
      target: {
        nodeId: node.id,
        nodeName: safeName(node),
        targetKind: "image-fill",
        analysisMode: analysisMode,
        fillIndex: fillIndex,
        originalHash: fill.imageHash,
        sourceWidth: sourceWidth,
        sourceHeight: sourceHeight,
        analysisOffsetX: analysisContext.offsetX,
        analysisOffsetY: analysisContext.offsetY,
        analysisWidth: analysisContext.visibleRect.width,
        analysisHeight: analysisContext.visibleRect.height,
        rasterAnalysisOffsetX: rasterAnalysisOffsetX,
        rasterAnalysisOffsetY: rasterAnalysisOffsetY,
        rasterAnalysisWidth: rasterAnalysisWidth,
        rasterAnalysisHeight: rasterAnalysisHeight,
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: fill.imageHash,
          },
          "png"
        ),
        mimeType: mimeType,
        bytes: bytes,
      },
    };
  }

  async function applyBoundsFitResultsToSelection(session, rawResults) {
    const normalizedResults = normalizeBoundsFitResults(rawResults);
    const resultByNodeId = {};
    const skipped = Array.isArray(session.skipped) ? session.skipped.slice() : [];
    let appliedCount = 0;
    let unchangedCount = 0;

    for (let index = 0; index < normalizedResults.length; index += 1) {
      const entry = normalizedResults[index];
      if (entry && entry.nodeId) {
        resultByNodeId[entry.nodeId] = entry;
      }
    }

    for (let index = 0; index < session.targets.length; index += 1) {
      const target = session.targets[index];
      const processed = resultByNodeId[target.nodeId];
      if (!processed && !(target && target.skipRasterAnalysis === true)) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          reason: "Did not receive a UI analysis result.",
        });
        continue;
      }

      if (!processed && target && target.skipRasterAnalysis === true) {
        const directStatus = await applyBoundsFitTargetResult(target, null, skipped);
        if (directStatus === "applied") {
          appliedCount += 1;
        } else if (directStatus === "unchanged") {
          unchangedCount += 1;
        }
        continue;
      }

      if (processed.status === "unchanged") {
        unchangedCount += 1;
        continue;
      }

      if (processed.status === "skipped" || processed.status !== "trimmed") {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: target.nodeName,
          reason: processed.reason || "Could not find visible content bounds.",
        });
        continue;
      }

      const applyStatus = await applyBoundsFitTargetResult(target, processed, skipped);
      if (applyStatus === "applied") {
        appliedCount += 1;
      } else if (applyStatus === "unchanged") {
        unchangedCount += 1;
      }
    }

    const containerPlans = sortBoundsFitContainerPlans(session && Array.isArray(session.containers) ? session.containers : []);
    for (let index = 0; index < containerPlans.length; index += 1) {
      const containerStatus = await applyBoundsFitContainerPlan(containerPlans[index], skipped);
      if (containerStatus === "applied") {
        appliedCount += 1;
      } else if (containerStatus === "unchanged") {
        unchangedCount += 1;
      }
    }

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: session.selectionLabel,
        requestedCount: session.requestedCount,
        eligibleCount: session.targets.length + (session && Array.isArray(session.containers) ? session.containers.length : 0),
        appliedCount: appliedCount,
        unchangedCount: unchangedCount,
        skippedCount: skipped.length,
      },
      skipped: skipped.slice(0, 24),
    };
  }

  async function applyBoundsFitTargetResult(target, processed, skipped) {
    if (!target) {
      return "skipped";
    }

    if (!processed && !(target.targetKind === "text" && target.skipRasterAnalysis === true)) {
      return "skipped";
    }

    if (target.targetKind === "text") {
      return await applyBoundsFitTextTargetResult(target, processed, skipped);
    }

    return await applyBoundsFitImageTargetResult(target, processed, skipped);
  }

  async function applyOriginalSizeFitTarget(target, skipped) {
    const node = await figma.getNodeByIdAsync(target.nodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: target.nodeName,
        reason: "Could not find the layer again.",
      });
      return "skipped";
    }

    if (!canResizeBoundsFitNode(node)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "This layer cannot be resized.",
      });
      return "skipped";
    }

    const fills = getNodeFills(node);
    let targetIndex = -1;
    if (target.fillIndex >= 0 && target.fillIndex < fills.length) {
      const directFill = fills[target.fillIndex];
      if (isImagePaint(directFill) && directFill.imageHash === target.originalHash) {
        targetIndex = target.fillIndex;
      }
    }
    if (targetIndex < 0) {
      targetIndex = findVisibleImageFillIndexByHash(fills, target.originalHash);
    }
    if (targetIndex < 0) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not find the original image fill again.",
      });
      return "skipped";
    }

    const sourceWidth = Number(target.sourceWidth) > 0 ? Number(target.sourceWidth) : 0;
    const sourceHeight = Number(target.sourceHeight) > 0 ? Number(target.sourceHeight) : 0;
    if (!(sourceWidth > 0) || !(sourceHeight > 0)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not read the original image size.",
      });
      return "skipped";
    }

    const currentWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const currentHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    const resetPaint = cloneOriginalSizeFitImagePaint(fills[targetIndex]);
    if (!resetPaint) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not reset the image fill settings.",
      });
      return "skipped";
    }

    const needsResize = Math.abs(currentWidth - sourceWidth) > 0.01 || Math.abs(currentHeight - sourceHeight) > 0.01;
    const needsFillReset = doesOriginalSizeFitNeedPaintReset(fills[targetIndex]);
    if (!needsResize && !needsFillReset) {
      return "unchanged";
    }

    try {
      const nextFills = fills.slice();
      nextFills[targetIndex] = resetPaint;
      node.fills = nextFills;
      if (needsResize) {
        resizeBoundsFitNode(node, sourceWidth, sourceHeight, true);
      }
      return "applied";
    } catch (error) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, ORIGINAL_SIZE_FIT_APPLY_ERROR_MESSAGE, {
          operationLabel: "Fit Original Size",
          operationKind: "original-size-fit",
        }),
      });
      return "skipped";
    }
  }

  async function applyBoundsFitImageTargetResult(target, processed, skipped) {
    const node = await figma.getNodeByIdAsync(target.nodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: target.nodeName,
        reason: "Could not find the layer again.",
      });
      return "skipped";
    }

    if (!canResizeBoundsFitNode(node)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "This layer cannot be resized.",
      });
      return "skipped";
    }

    const fills = getNodeFills(node);
    if (!fills.length) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "The IMAGE fill is no longer available.",
      });
      return "skipped";
    }

    let targetIndex = -1;
    if (target.fillIndex >= 0 && target.fillIndex < fills.length) {
      const directFill = fills[target.fillIndex];
      if (isImagePaint(directFill) && directFill.imageHash === target.originalHash) {
        targetIndex = target.fillIndex;
      }
    }

    if (targetIndex < 0) {
      targetIndex = findVisibleImageFillIndexByHash(fills, target.originalHash);
    }

    if (targetIndex < 0) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not find the IMAGE fill with the original image hash.",
      });
      return "skipped";
    }

    const currentWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const currentHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(currentWidth > 0) || !(currentHeight > 0)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not calculate the current layer size.",
      });
      return "skipped";
    }

    const geometry = resolveBoundsFitProcessedGeometry(target, processed, currentWidth, currentHeight);
    if (!geometry) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not calculate the visible bounds size.",
      });
      return "skipped";
    }

    if (!hasMatchingAspectRatio(geometry.analysisWidth, geometry.analysisHeight, processed.sourceWidth, processed.sourceHeight)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "The analyzed crop aspect ratio no longer matches the source image.",
      });
      return "skipped";
    }

    try {
      const nextFills = fills.slice();
      const cropPaint = cloneBoundsFitPreservedImagePaint(nextFills[targetIndex], target, processed);
      if (!cropPaint) {
        skipped.push({
          nodeId: target.nodeId,
          nodeName: safeName(node),
          reason: "Could not preserve the original IMAGE fill while fitting visible bounds.",
        });
        return "skipped";
      }
      nextFills[targetIndex] = cropPaint;
      node.fills = nextFills;
      applyBoundsFitGeometry(node, geometry.nextWidth, geometry.nextHeight, geometry.shiftX, geometry.shiftY, null);
      return "applied";
    } catch (error) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, "Failed to apply bounds-fit."),
      });
      return "skipped";
    }
  }

  async function loadBoundsFitTextFonts(node) {
    if (!node || node.type !== "TEXT") {
      return;
    }

    const fontNames = collectBoundsFitTextFontNames(node);
    for (let index = 0; index < fontNames.length; index += 1) {
      await figma.loadFontAsync(fontNames[index]);
    }
  }

  function collectBoundsFitTextFontNames(node) {
    const fontNames = [];
    const seen = {};

    function pushFont(fontName) {
      if (!fontName || typeof fontName !== "object") {
        return;
      }

      if (typeof fontName.family !== "string" || typeof fontName.style !== "string") {
        return;
      }

      const key = fontName.family + "::" + fontName.style;
      if (seen[key]) {
        return;
      }

      seen[key] = true;
      fontNames.push({
        family: fontName.family,
        style: fontName.style,
      });
    }

    if (typeof node.getRangeAllFontNames === "function" && typeof node.characters === "string" && node.characters.length > 0) {
      try {
        const rangeFonts = node.getRangeAllFontNames(0, node.characters.length);
        for (let index = 0; index < rangeFonts.length; index += 1) {
          pushFont(rangeFonts[index]);
        }
      } catch (error) {}
    }

    if (!fontNames.length && node.fontName && node.fontName !== figma.mixed && typeof node.fontName === "object") {
      pushFont(node.fontName);
    }

    return fontNames;
  }

  async function resolveBoundsFitTextMeasuredRect(node) {
    const layoutRect = getBoundsFitNodeLayoutRect(node);
    if (!layoutRect) {
      return null;
    }

    const renderRect = getBoundsFitNodeRenderRect(node);
    if (renderRect) {
      if (!areBoundsFitRectsEqual(renderRect, layoutRect)) {
        return renderRect;
      }
    }

    await loadBoundsFitTextFonts(node);

    const probe = figma.createFrame();
    let clonedNode = null;
    let flattenedNode = null;

    try {
      probe.name = "__pigma-bounds-fit-text-probe__";
      probe.resize(layoutRect.width, layoutRect.height);
      probe.clipsContent = false;
      probe.fills = [];
      probe.strokes = [];
      probe.x = layoutRect.x;
      probe.y = layoutRect.y;
      figma.currentPage.appendChild(probe);

      clonedNode = node.clone();
      probe.appendChild(clonedNode);
      positionBoundsFitPreviewClone(node, clonedNode, layoutRect);

      flattenedNode = figma.flatten([clonedNode], probe);
      const flattenedRect = getBoundsFitNodeRect(flattenedNode);
      if (!flattenedRect) {
        return renderRect || layoutRect;
      }

      return flattenedRect;
    } catch (error) {
      return renderRect || layoutRect;
    } finally {
      if (clonedNode && !clonedNode.removed) {
        clonedNode.remove();
      }
      if (flattenedNode && !flattenedNode.removed) {
        flattenedNode.remove();
      }
      if (probe && !probe.removed) {
        probe.remove();
      }
    }
  }

  async function applyBoundsFitTextTargetResult(target, processed, skipped) {
    const node = await figma.getNodeByIdAsync(target.nodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: target.nodeName,
        reason: "Could not find the layer again.",
      });
      return "skipped";
    }

    if (!canResizeBoundsFitNode(node)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "This text layer cannot be resized.",
      });
      return "skipped";
    }

    const currentWidth = typeof node.width === "number" && Number.isFinite(node.width) ? node.width : 0;
    const currentHeight = typeof node.height === "number" && Number.isFinite(node.height) ? node.height : 0;
    if (!(currentWidth > 0) || !(currentHeight > 0)) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "Could not calculate the current text layer size.",
      });
      return "skipped";
    }

    const geometry =
      target.skipRasterAnalysis === true
        ? resolveBoundsFitDirectGeometry(target, currentWidth, currentHeight)
        : resolveBoundsFitProcessedGeometry(target, processed, currentWidth, currentHeight);
    if (!geometry) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: "The visible text bounds are invalid.",
      });
      return "skipped";
    }

    if (shouldPreserveBoundsFitTextWidth(node)) {
      geometry.nextWidth = currentWidth;
      geometry.shiftX = 0;
      geometry.nextHeight = Math.max(currentHeight, geometry.nextHeight);
    }

    if (isBoundsFitAutoLayoutChildNode(node)) {
      geometry.shiftX = 0;
      geometry.shiftY = 0;
      geometry.nextHeight = Math.max(currentHeight, geometry.nextHeight);
    }

    if (
      Math.abs(geometry.shiftX) <= 0.01 &&
      Math.abs(geometry.shiftY) <= 0.01 &&
      Math.abs(geometry.nextWidth - currentWidth) <= 0.01 &&
      Math.abs(geometry.nextHeight - currentHeight) <= 0.01
    ) {
      return "unchanged";
    }

    try {
      applyBoundsFitGeometry(node, geometry.nextWidth, geometry.nextHeight, geometry.shiftX, geometry.shiftY, {
        textAutoResizeNone: true,
        preferWithoutConstraints: true,
      });
      return "applied";
    } catch (error) {
      skipped.push({
        nodeId: target.nodeId,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, "Failed to apply bounds-fit to the selected text layer."),
      });
      return "skipped";
    }
  }

  function resolveBoundsFitProcessedGeometry(target, processed, fallbackWidth, fallbackHeight) {
    const analysisWidth =
      Number(target.analysisWidth) > 0 ? Number(target.analysisWidth) : Number(fallbackWidth) > 0 ? Number(fallbackWidth) : 0;
    const analysisHeight =
      Number(target.analysisHeight) > 0 ? Number(target.analysisHeight) : Number(fallbackHeight) > 0 ? Number(fallbackHeight) : 0;
    const analysisOffsetX = Number.isFinite(Number(target.analysisOffsetX)) ? Number(target.analysisOffsetX) : 0;
    const analysisOffsetY = Number.isFinite(Number(target.analysisOffsetY)) ? Number(target.analysisOffsetY) : 0;
    const cropWidth = Number(processed.cropWidth) || 0;
    const cropHeight = Number(processed.cropHeight) || 0;
    const cropX = Number(processed.cropX) || 0;
    const cropY = Number(processed.cropY) || 0;

    if (!(analysisWidth > 0) || !(analysisHeight > 0)) {
      return null;
    }

    if (!(processed.sourceWidth > 0) || !(processed.sourceHeight > 0)) {
      return null;
    }

    if (!(cropWidth > 0) || !(cropHeight > 0)) {
      return null;
    }

    const scaleX = analysisWidth / processed.sourceWidth;
    const scaleY = analysisHeight / processed.sourceHeight;
    return {
      analysisWidth: analysisWidth,
      analysisHeight: analysisHeight,
      nextWidth: Math.max(1, roundBoundsFitMetric(cropWidth * scaleX)),
      nextHeight: Math.max(1, roundBoundsFitMetric(cropHeight * scaleY)),
      shiftX: roundBoundsFitMetric(analysisOffsetX + cropX * scaleX),
      shiftY: roundBoundsFitMetric(analysisOffsetY + cropY * scaleY),
    };
  }

  function resolveBoundsFitDirectGeometry(target, fallbackWidth, fallbackHeight) {
    const analysisWidth = Number(fallbackWidth) > 0 ? Number(fallbackWidth) : 0;
    const analysisHeight = Number(fallbackHeight) > 0 ? Number(fallbackHeight) : 0;
    const cropWidth = Number(target.directCropWidth) || 0;
    const cropHeight = Number(target.directCropHeight) || 0;
    const cropX = Number(target.directCropX) || 0;
    const cropY = Number(target.directCropY) || 0;

    if (!(analysisWidth > 0) || !(analysisHeight > 0)) {
      return null;
    }

    if (!(cropWidth > 0) || !(cropHeight > 0)) {
      return null;
    }

    return {
      analysisWidth: analysisWidth,
      analysisHeight: analysisHeight,
      nextWidth: Math.max(1, roundBoundsFitMetric(cropWidth)),
      nextHeight: Math.max(1, roundBoundsFitMetric(cropHeight)),
      shiftX: roundBoundsFitMetric(cropX),
      shiftY: roundBoundsFitMetric(cropY),
    };
  }

  function sortBoundsFitContainerPlans(plans) {
    if (!Array.isArray(plans) || !plans.length) {
      return [];
    }

    return plans.slice().sort(function (left, right) {
      const leftDepth = left && typeof left.depth === "number" ? left.depth : 0;
      const rightDepth = right && typeof right.depth === "number" ? right.depth : 0;
      return rightDepth - leftDepth;
    });
  }

  async function applyBoundsFitContainerPlan(plan, skipped) {
    if (!plan || !plan.nodeId) {
      return "skipped";
    }

    const node = await figma.getNodeByIdAsync(plan.nodeId);
    if (!node || node.removed) {
      skipped.push({
        nodeId: plan.nodeId,
        nodeName: plan.nodeName || "Unnamed",
        reason: "Could not find the selected container again.",
      });
      return "skipped";
    }

    if (!canApplyBoundsFitContainer(node)) {
      skipped.push({
        nodeId: plan.nodeId,
        nodeName: safeName(node),
        reason: buildBoundsFitContainerUnsupportedReason(node),
      });
      return "skipped";
    }

    const containerRect = getBoundsFitNodeRect(node);
    if (!containerRect) {
      skipped.push({
        nodeId: plan.nodeId,
        nodeName: safeName(node),
        reason: "Could not calculate the current container bounds.",
      });
      return "skipped";
    }

    const childUnion = getBoundsFitContainerChildrenUnionRect(node, containerRect);
    if (!childUnion) {
      skipped.push({
        nodeId: plan.nodeId,
        nodeName: safeName(node),
        reason: "Could not calculate the visible child bounds inside the selected container.",
      });
      return "skipped";
    }

    const shiftX = roundBoundsFitMetric(childUnion.x - containerRect.x);
    const shiftY = roundBoundsFitMetric(childUnion.y - containerRect.y);
    const needsMove = Math.abs(shiftX) > 0.01 || Math.abs(shiftY) > 0.01;
    const needsResize =
      shouldResizeBoundsFitContainer(node) &&
      (Math.abs(childUnion.width - containerRect.width) > 0.01 || Math.abs(childUnion.height - containerRect.height) > 0.01);

    if (!needsMove && !needsResize) {
      return "unchanged";
    }

    try {
      if (isBoundsFitAutoLayoutNode(node)) {
        applyBoundsFitAutoLayoutContainerGeometry(node, containerRect, childUnion, {
          preserveLeadingInsets: !!(plan && plan.selectedRoot === true),
        });
      } else {
        if (plan && plan.selectedRoot === true) {
          applyBoundsFitSelectedRootContainerGeometry(node, containerRect, childUnion);
        } else {
          if (canMoveBoundsFitNode(node)) {
            moveBoundsFitNode(node, shiftX, shiftY);
          }
          moveBoundsFitContainerChildren(node, -shiftX, -shiftY);
          if (shouldResizeBoundsFitContainer(node)) {
            resizeBoundsFitNode(node, childUnion.width, childUnion.height, false);
          }
        }
      }
      return "applied";
    } catch (error) {
      skipped.push({
        nodeId: plan.nodeId,
        nodeName: safeName(node),
        reason: normalizeErrorMessage(error, "Failed to apply bounds-fit to the selected container."),
      });
      return "skipped";
    }
  }

  function getBoundsFitContainerChildrenUnionRect(node, containerRect) {
    if (!node || !hasChildren(node)) {
      return null;
    }

    const preferredRects = [];
    const fallbackRects = [];
    const clipRect = containerRect || getBoundsFitNodeRect(node);
    const nextClipRect =
      "clipsContent" in node && node.clipsContent === true && clipRect ? clipRect : null;
    collectBoundsFitDescendantRects(node, nextClipRect, preferredRects, fallbackRects);

    return unionAbsoluteRects(preferredRects.length ? preferredRects : fallbackRects);
  }

  function collectBoundsFitDescendantRects(node, clipRect, preferredRects, fallbackRects) {
    if (!node || !hasChildren(node) || !Array.isArray(preferredRects) || !Array.isArray(fallbackRects)) {
      return;
    }

    for (let index = 0; index < node.children.length; index += 1) {
      const child = node.children[index];
      if (!child || child.removed || !isBoundsFitNodeVisible(child)) {
        continue;
      }

      let childRect = getBoundsFitNodeRect(child);
      if (!childRect) {
        continue;
      }

      if (clipRect) {
        childRect = intersectBoundsFitRects(childRect, clipRect);
        if (!childRect) {
          continue;
        }
      }

      if (hasChildren(child)) {
        const descendantClipRect =
          "clipsContent" in child && child.clipsContent === true ? childRect : clipRect;
        collectBoundsFitDescendantRects(child, descendantClipRect, preferredRects, fallbackRects);
        continue;
      }

      fallbackRects.push(childRect);
      if (isBoundsFitPreferredContentNode(child)) {
        preferredRects.push(childRect);
      }
    }
  }

  function moveBoundsFitContainerChildren(node, shiftX, shiftY) {
    if (!node || !hasChildren(node)) {
      return;
    }

    for (let index = 0; index < node.children.length; index += 1) {
      moveBoundsFitNode(node.children[index], shiftX, shiftY);
    }
  }

  function applyBoundsFitSelectedRootContainerGeometry(node, containerRect, childUnion) {
    const nextLeft = Math.min(containerRect.x, childUnion.x);
    const nextTop = Math.min(containerRect.y, childUnion.y);
    const nextRight = Math.max(containerRect.x, childUnion.x + childUnion.width);
    const nextBottom = Math.max(containerRect.y, childUnion.y + childUnion.height);
    const shiftX = roundBoundsFitMetric(nextLeft - containerRect.x);
    const shiftY = roundBoundsFitMetric(nextTop - containerRect.y);
    const nextWidth = Math.max(1, roundBoundsFitMetric(nextRight - nextLeft));
    const nextHeight = Math.max(1, roundBoundsFitMetric(nextBottom - nextTop));

    if (canMoveBoundsFitNode(node) && (Math.abs(shiftX) > 0.01 || Math.abs(shiftY) > 0.01)) {
      moveBoundsFitNode(node, shiftX, shiftY);
    }

    if (shouldResizeBoundsFitContainer(node)) {
      resizeBoundsFitNode(node, nextWidth, nextHeight, false);
    }
  }

  function applyBoundsFitAutoLayoutContainerGeometry(node, containerRect, childUnion, options) {
    const preserveLeadingInsets = !!(options && options.preserveLeadingInsets === true);
    const leftInset = roundBoundsFitMetric(childUnion.x - containerRect.x);
    const topInset = roundBoundsFitMetric(childUnion.y - containerRect.y);
    const rightInset = roundBoundsFitMetric(containerRect.x + containerRect.width - (childUnion.x + childUnion.width));
    const bottomInset = roundBoundsFitMetric(containerRect.y + containerRect.height - (childUnion.y + childUnion.height));

    if (!preserveLeadingInsets && canMoveBoundsFitNode(node)) {
      moveBoundsFitNode(node, leftInset, topInset);
    }

    if ("paddingLeft" in node && typeof node.paddingLeft === "number") {
      node.paddingLeft = preserveLeadingInsets && leftInset >= 0 ? roundBoundsFitPixel(node.paddingLeft) : Math.max(0, roundBoundsFitPixel(node.paddingLeft - leftInset));
    }
    if ("paddingTop" in node && typeof node.paddingTop === "number") {
      node.paddingTop = preserveLeadingInsets && topInset >= 0 ? roundBoundsFitPixel(node.paddingTop) : Math.max(0, roundBoundsFitPixel(node.paddingTop - topInset));
    }
    if ("paddingRight" in node && typeof node.paddingRight === "number") {
      node.paddingRight = Math.max(0, roundBoundsFitPixel(node.paddingRight - rightInset));
    }
    if ("paddingBottom" in node && typeof node.paddingBottom === "number") {
      node.paddingBottom = Math.max(0, roundBoundsFitPixel(node.paddingBottom - bottomInset));
    }

    if (shouldResizeBoundsFitContainer(node)) {
      const nextWidth = preserveLeadingInsets && leftInset >= 0 ? childUnion.width + leftInset : childUnion.width;
      const nextHeight = preserveLeadingInsets && topInset >= 0 ? childUnion.height + topInset : childUnion.height;
      resizeBoundsFitNode(node, nextWidth, nextHeight, false);
    }
  }

  function applyBoundsFitGeometry(node, width, height, shiftX, shiftY, options) {
    if (canMoveBoundsFitNode(node)) {
      moveBoundsFitNode(node, shiftX, shiftY);
    }

    if (options && options.textAutoResizeNone === true && node && node.type === "TEXT" && "textAutoResize" in node) {
      try {
        node.textAutoResize = "NONE";
      } catch (error) {
        // Ignore text nodes that do not allow changing auto-resize in the current runtime.
      }
    }

    resizeBoundsFitNode(node, width, height, !!(options && options.preferWithoutConstraints === true));
  }

  function moveBoundsFitNode(node, shiftX, shiftY) {
    const nextShiftX = roundBoundsFitPixel(shiftX);
    const nextShiftY = roundBoundsFitPixel(shiftY);
    if ("x" in node && typeof node.x === "number" && "y" in node && typeof node.y === "number") {
      node.x = roundBoundsFitPixel(node.x + nextShiftX);
      node.y = roundBoundsFitPixel(node.y + nextShiftY);
    } else if ("relativeTransform" in node && Array.isArray(node.relativeTransform)) {
      node.relativeTransform = [
        [node.relativeTransform[0][0], node.relativeTransform[0][1], roundBoundsFitPixel(node.relativeTransform[0][2] + nextShiftX)],
        [node.relativeTransform[1][0], node.relativeTransform[1][1], roundBoundsFitPixel(node.relativeTransform[1][2] + nextShiftY)],
      ];
    } else if (nextShiftX !== 0 || nextShiftY !== 0) {
      throw new Error("Could not move the layer to the analyzed position.");
    }
  }

  function resizeBoundsFitNode(node, width, height, preferWithoutConstraints) {
    const nextWidth = Math.max(1, roundBoundsFitPixel(width));
    const nextHeight = Math.max(1, roundBoundsFitPixel(height));

    if (preferWithoutConstraints && typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(nextWidth, nextHeight);
      return;
    }

    if (typeof node.resize === "function") {
      node.resize(nextWidth, nextHeight);
      return;
    }

    if (typeof node.resizeWithoutConstraints === "function") {
      node.resizeWithoutConstraints(nextWidth, nextHeight);
      return;
    }

    throw new Error("Could not resize the selected layer.");
  }

  function normalizeBoundsFitResults(value) {
    if (!Array.isArray(value)) {
      return [];
    }

    return value.map(function (entry) {
      return {
        nodeId: entry && typeof entry.nodeId === "string" ? entry.nodeId : "",
        status: entry && typeof entry.status === "string" ? entry.status : "",
        reason: entry && typeof entry.reason === "string" ? entry.reason : "",
        sourceWidth: entry && Number(entry.sourceWidth) > 0 ? Number(entry.sourceWidth) : 0,
        sourceHeight: entry && Number(entry.sourceHeight) > 0 ? Number(entry.sourceHeight) : 0,
        analysisOffsetX: entry && Number.isFinite(Number(entry.analysisOffsetX)) ? Number(entry.analysisOffsetX) : 0,
        analysisOffsetY: entry && Number.isFinite(Number(entry.analysisOffsetY)) ? Number(entry.analysisOffsetY) : 0,
        analysisWidth: entry && Number(entry.analysisWidth) > 0 ? Number(entry.analysisWidth) : 0,
        analysisHeight: entry && Number(entry.analysisHeight) > 0 ? Number(entry.analysisHeight) : 0,
        cropX: entry && Number.isFinite(Number(entry.cropX)) ? Number(entry.cropX) : 0,
        cropY: entry && Number.isFinite(Number(entry.cropY)) ? Number(entry.cropY) : 0,
        cropWidth: entry && Number(entry.cropWidth) > 0 ? Number(entry.cropWidth) : 0,
        cropHeight: entry && Number(entry.cropHeight) > 0 ? Number(entry.cropHeight) : 0,
        bytes: normalizeBytes(entry && entry.bytes),
      };
    });
  }

  function buildBoundsFitSkipped(node, reason) {
    return {
      skipped: {
        nodeId: node && typeof node.id === "string" ? node.id : "",
        nodeName: safeName(node),
        reason: reason,
      },
    };
  }

  function buildOriginalSizeFitSkipped(node, reason) {
    return {
      skipped: {
        nodeId: node && typeof node.id === "string" ? node.id : "",
        nodeName: safeName(node),
        reason: reason,
      },
    };
  }

  async function analyzeBoundsFitTextSelectionNode(node) {
    if (!node || node.removed) {
      return buildBoundsFitSkipped(node, "The selected text layer could not be read.");
    }

    if (node.type !== "TEXT") {
      return buildBoundsFitSkipped(node, "Bounds-fit text mode requires a text layer.");
    }

    if ("locked" in node && node.locked) {
      return buildBoundsFitSkipped(node, "Locked text layers are not supported for bounds-fit.");
    }

    if (!canResizeBoundsFitNode(node)) {
      return buildBoundsFitSkipped(node, "Bounds-fit could not resize the selected text layer.");
    }

    if ("rotation" in node && typeof node.rotation === "number" && Math.abs(node.rotation) > 0.01) {
      return buildBoundsFitSkipped(node, "Rotated text layers are not supported yet.");
    }

    const layoutRect = getBoundsFitNodeLayoutRect(node);
    if (!layoutRect) {
      return buildBoundsFitSkipped(node, "Could not calculate the selected text layer bounds.");
    }

    const measuredRect = await resolveBoundsFitTextMeasuredRect(node);
    if (!measuredRect) {
      return buildBoundsFitSkipped(node, "Could not calculate the visible bounds for the selected text layer.");
    }

    const measuredWidth = Number(measuredRect.width) || 0;
    const measuredHeight = Number(measuredRect.height) || 0;
    if (!(measuredWidth > 0) || !(measuredHeight > 0)) {
      return buildBoundsFitSkipped(node, "Could not calculate the visible text bounds inside the selected layer.");
    }

    return {
      target: {
        nodeId: node.id,
        nodeName: safeName(node),
        targetKind: "text",
        analysisMode: "direct-text-bounds",
        skipRasterAnalysis: true,
        fillIndex: -1,
        originalHash: "",
        sourceWidth: layoutRect.width,
        sourceHeight: layoutRect.height,
        analysisOffsetX: 0,
        analysisOffsetY: 0,
        analysisWidth: layoutRect.width,
        analysisHeight: layoutRect.height,
        directCropX: roundBoundsFitMetric(measuredRect.x - layoutRect.x),
        directCropY: roundBoundsFitMetric(measuredRect.y - layoutRect.y),
        directCropWidth: measuredRect.width,
        directCropHeight: measuredRect.height,
        fileName: "",
        mimeType: "",
        bytes: new Uint8Array(0),
      },
    };
  }

  function buildImageCompositeSkipped(node, reason) {
    return {
      skipped: {
        nodeId: node && typeof node.id === "string" ? node.id : "",
        nodeName: safeName(node),
        reason: reason,
      },
    };
  }

  function buildBoundsFitEmptySelectionMessage(skipped) {
    if (Array.isArray(skipped) && skipped.length > 0) {
      const first = skipped[0];
      if (first && typeof first.reason === "string" && first.reason.trim()) {
        return first.reason.trim();
      }
    }
    return "Could not find an eligible text, image, or supported container for bounds-fit.";
  }

  function buildOriginalSizeFitEmptySelectionMessage(skipped) {
    if (Array.isArray(skipped) && skipped.length > 0) {
      const first = skipped[0];
      if (first && typeof first.reason === "string" && first.reason.trim()) {
        return first.reason.trim();
      }
    }
    return "Could not find an image fill layer to fit to original size in the selection.";
  }

  function buildImageCompositeEmptySelectionMessage(layers, skipped) {
    if (Array.isArray(layers) && layers.length === 1) {
      return "Image composite requires at least 2 eligible visible layers.";
    }

    if (Array.isArray(skipped) && skipped.length > 0 && (!Array.isArray(layers) || !layers.length)) {
      const first = skipped[0];
      if (first && typeof first.reason === "string" && first.reason.trim()) {
        return first.reason.trim();
      }
    }

    return "Could not find 2 to 8 eligible visible layers for image composite.";
  }

  function resolveImageCompositeLayerKind(node) {
    if (node && node.type === "TEXT") {
      return "text";
    }
    if (hasSimpleVisibleImagePaint(node)) {
      return "image";
    }
    return "shape";
  }

  function getImageCompositeTextContent(node) {
    if (!node || node.type !== "TEXT" || typeof node.characters !== "string") {
      return "";
    }

    return node.characters.replace(/\s+/g, " ").trim().slice(0, 240);
  }

  function hasRenderableImageCompositeAppearance(node) {
    if (!node) {
      return false;
    }

    if (node.type === "TEXT") {
      return hasVisiblePaints(node.fills) || hasVisibleEffects(node.effects);
    }

    if (hasSimpleVisibleImagePaint(node)) {
      return true;
    }

    return hasVisiblePaints(getNodeFills(node)) || hasVisiblePaints(node.strokes) || hasVisibleEffects(node.effects);
  }

  async function analyzeImageCompositeSelectionNode(node, options) {
    if (!node || node.removed) {
      return buildImageCompositeSkipped(node, "The selected layer could not be read.");
    }

    if (!isBoundsFitNodeVisible(node)) {
      return buildImageCompositeSkipped(node, "The selected layer is hidden.");
    }

    if (hasChildren(node)) {
      return buildImageCompositeSkipped(node, "Image composite supports visible leaf layers only inside the selected frame.");
    }

    if ("locked" in node && node.locked) {
      return buildImageCompositeSkipped(node, "Locked layers are not supported for image composite.");
    }

    if (typeof node.exportAsync !== "function") {
      return buildImageCompositeSkipped(node, "The selected layer cannot be exported for image composite.");
    }

    if (!hasRenderableImageCompositeAppearance(node)) {
      return buildImageCompositeSkipped(node, "The selected layer does not have visible pixels to composite.");
    }

    const analysisContext = getBoundsFitAnalysisContext(node);
    if (!analysisContext || !analysisContext.visibleRect) {
      return buildImageCompositeSkipped(node, "Could not calculate the visible bounds for the selected layer.");
    }

    let visibleRect = analysisContext.visibleRect;
    const rootRect = options && options.rootRect ? options.rootRect : null;
    if (rootRect) {
      visibleRect = intersectBoundsFitRects(visibleRect, rootRect);
      if (!visibleRect) {
        return buildImageCompositeSkipped(node, "The selected layer is outside the chosen frame bounds.");
      }
    }

    const exportContext =
      rootRect && !areBoundsFitRectsEqual(visibleRect, analysisContext.visibleRect)
        ? {
            nodeRect: analysisContext.nodeRect,
            visibleRect: visibleRect,
            clipped: true,
            offsetX: roundBoundsFitMetric(visibleRect.x - analysisContext.nodeRect.x),
            offsetY: roundBoundsFitMetric(visibleRect.y - analysisContext.nodeRect.y),
          }
        : analysisContext;

    const bytes = await exportBoundsFitRenderedAnalysisBytes(node, exportContext);
    if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
      return buildImageCompositeSkipped(node, "Could not export the current layer as PNG.");
    }

    const fills = getNodeFills(node);
    const fillIndex = hasSimpleVisibleImagePaint(node) ? getPrimaryVisibleImageFillIndex(fills) : -1;
    const fill = fillIndex >= 0 ? fills[fillIndex] : null;
    return {
      target: {
        node: node,
        nodeId: node.id,
        nodeName: safeName(node),
        layerKind: resolveImageCompositeLayerKind(node),
        nodeType: node.type,
        textContent: getImageCompositeTextContent(node),
        fillIndex: fillIndex,
        originalHash: fill && typeof fill.imageHash === "string" ? fill.imageHash : "",
        visibleRect: visibleRect,
        fileName: buildFileName(
          {
            nodeName: safeName(node),
            imageHash: fill && fill.imageHash ? fill.imageHash : node.id,
          },
          "png"
        ),
        mimeType: "image/png",
        bytes: bytes,
      },
    };
  }

  function hasSimpleVisibleImagePaint(node) {
    const fills = getNodeFills(node);
    let visibleImageCount = 0;
    let visibleOtherCount = 0;

    for (let index = 0; index < fills.length; index += 1) {
      const fill = fills[index];
      if (!fill || fill.visible === false) {
        continue;
      }
      if (fill.type === "IMAGE") {
        visibleImageCount += 1;
      } else {
        visibleOtherCount += 1;
      }
    }

    return visibleImageCount === 1 && visibleOtherCount === 0;
  }

  function isPromptEditableShapeNode(node) {
    return (
      !!node &&
      !node.removed &&
      node.type !== "TEXT" &&
      (!("locked" in node) || !node.locked) &&
      !hasChildren(node) &&
      "fills" in node &&
      Array.isArray(node.fills) &&
      typeof node.exportAsync === "function"
    );
  }

  function getPrimaryVisibleImageFillIndex(fills) {
    const fillIndices = getFillIndicesInUiOrder(fills);
    for (let index = 0; index < fillIndices.length; index += 1) {
      const fillIndex = fillIndices[index];
      const fill = fills[fillIndex];
      if (isImagePaint(fill) && fill.imageHash) {
        return fillIndex;
      }
    }
    return -1;
  }

  function findVisibleImageFillIndexByHash(fills, imageHash) {
    const fillIndices = getFillIndicesInUiOrder(fills);
    for (let index = 0; index < fillIndices.length; index += 1) {
      const fillIndex = fillIndices[index];
      const fill = fills[fillIndex];
      if (isImagePaint(fill) && fill.imageHash === imageHash) {
        return fillIndex;
      }
    }
    return -1;
  }

  function hasVisiblePaints(paints) {
    if (!Array.isArray(paints) || !paints.length) {
      return false;
    }

    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (paint && paint.visible !== false) {
        return true;
      }
    }

    return false;
  }

  function hasVisibleEffects(effects) {
    if (!Array.isArray(effects) || !effects.length) {
      return false;
    }

    for (let index = 0; index < effects.length; index += 1) {
      const effect = effects[index];
      if (effect && effect.visible !== false) {
        return true;
      }
    }

    return false;
  }

  function hasMatchingAspectRatio(nodeWidth, nodeHeight, sourceWidth, sourceHeight) {
    if (!(nodeWidth > 0) || !(nodeHeight > 0) || !(sourceWidth > 0) || !(sourceHeight > 0)) {
      return false;
    }

    const nodeAspect = nodeWidth / nodeHeight;
    const sourceAspect = sourceWidth / sourceHeight;
    return Math.abs(nodeAspect - sourceAspect) <= 0.02;
  }

  function buildBoundsFitSessionId() {
    return "image-bounds-fit-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function buildImageExtendSessionId() {
    return "image-extend-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function buildImageCompositeSessionId() {
    return "image-composite-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function buildTextOverlaySessionId() {
    return "image-text-overlay-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function notifyOriginalSizeFitResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount =
      typeof summary.appliedCount === "number" && Number.isFinite(summary.appliedCount) ? summary.appliedCount : 0;
    const unchangedCount =
      typeof summary.unchangedCount === "number" && Number.isFinite(summary.unchangedCount) ? summary.unchangedCount : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;
    const label = normalizeOriginalSizeFitOperationLabel(operationLabel);

    if (!appliedCount && unchangedCount > 0 && skippedCount === 0) {
      figma.notify("The selected image is already at original size.", { timeout: 2200 });
      return;
    }

    if (!appliedCount && skippedCount > 0) {
      figma.notify(label + " could not find an eligible image layer to apply.", { timeout: 2200 });
      return;
    }

    let message = label + " complete (" + appliedCount + " applied";
    if (unchangedCount > 0) {
      message += ", " + unchangedCount + " unchanged";
    }
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    message += ")";
    figma.notify(message, { timeout: 2600 });
  }

  function normalizeOriginalSizeFitOperationLabel(value) {
    const label = sanitizeOperationLabel(value);
    if (!label || label === "원본 크기 맞춤") {
      return "Fit Original Size";
    }
    return label;
  }

  function notifyBoundsFitResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount =
      typeof summary.appliedCount === "number" && Number.isFinite(summary.appliedCount) ? summary.appliedCount : 0;
    const unchangedCount =
      typeof summary.unchangedCount === "number" && Number.isFinite(summary.unchangedCount) ? summary.unchangedCount : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;
    const label = operationLabel || "Bounds Fit";

    if (!appliedCount && unchangedCount > 0 && skippedCount === 0) {
      figma.notify("The selected layer is already tightly fit to its visible bounds.", { timeout: 2200 });
      return;
    }

    if (!appliedCount && skippedCount > 0) {
      figma.notify(label + " could not find an eligible layer or container to apply.", { timeout: 2200 });
      return;
    }

    let message = label + " complete (" + appliedCount + " applied";
    if (unchangedCount > 0) {
      message += ", " + unchangedCount + " unchanged";
    }
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    message += ")";
    figma.notify(message, { timeout: 2600 });
  }

  function notifyImageExtendResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount =
      typeof summary.appliedCount === "number" && Number.isFinite(summary.appliedCount) ? summary.appliedCount : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;
    const label = operationLabel || "Image Extend";

    if (!appliedCount) {
      figma.notify(label + " could not find an eligible image layer to apply.", { timeout: 2200 });
      return;
    }

    let message = label + " complete (" + appliedCount + " group created";
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    message += ")";
    figma.notify(message, { timeout: 2600 });
  }

  function notifyImageCompositeResult(result, operationLabel) {
    const summary = result && result.summary ? result.summary : {};
    const appliedCount =
      typeof summary.appliedCount === "number" && Number.isFinite(summary.appliedCount) ? summary.appliedCount : 0;
    const skippedCount =
      typeof summary.skippedCount === "number" && Number.isFinite(summary.skippedCount) ? summary.skippedCount : 0;
    const label = operationLabel || "AI Composite";

    if (!appliedCount) {
      figma.notify(label + " could not create a composite layer.", { timeout: 2200 });
      return;
    }

    let message = label + " complete (" + appliedCount + " layer created";
    if (skippedCount > 0) {
      message += ", " + skippedCount + " skipped";
    }
    message += ")";
    figma.notify(message, { timeout: 2600 });
  }

  function buildImageCompositeResultName(selectionLabel) {
    const base = typeof selectionLabel === "string" && selectionLabel.trim() ? selectionLabel.trim() : "Selection";
    return base + " / AI composite";
  }

  function sanitizeClientRequestId(value) {
    const requestId = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    return requestId || "";
  }

  function looksLikeCorruptedImageText(value) {
    const text = typeof value === "string" ? value.trim() : "";
    if (!text) {
      return false;
    }

    if (/\?{2,}|\uFFFD/.test(text)) {
      return true;
    }

    const questionCount = (text.match(/\?/g) || []).length;
    const nonAsciiCount = (text.match(/[^\x00-\x7F]/g) || []).length;
    return questionCount >= 2 && nonAsciiCount >= 4;
  }

  function buildSharpenSpecificImageError(text, provider) {
    const providerLabel = provider && provider !== "AI" ? provider + " " : "";

    if (
      /(select at least one node|could not find the selected image fill|could not find an image fill in the current selection|select one editable shape layer|shape layer or a layer that uses an image fill|select exactly one editable shape layer)/i.test(
        text
      )
    ) {
      return "샤프닝할 이미지를 찾지 못했습니다. 이미지 레이어를 다시 선택해주세요.";
    }

    if (
      /(could not read bytes|bytes from the selected image fill|image fill bytes missing|input image bytes|source image .*empty|exported shape image was empty|empty image bytes|no generated image bytes)/i.test(
        text
      )
    ) {
      return "샤프닝 원본 이미지 바이트를 읽지 못했습니다. 원본 이미지가 비었거나 손상되었을 수 있습니다.";
    }

    if (/(application\/octet-stream|unsupported|mime|format|image\/gif|image\/bmp|image\/tiff|image\/heic|image\/heif)/i.test(text)) {
      return "샤프닝 입력 이미지 형식을 지원하지 않습니다. PNG, JPG, WEBP처럼 일반적인 이미지로 다시 시도해주세요.";
    }

    if (/(payload|request size|20 ?mb|inline[_ ]data|too large|exceeds?.*limit|max(?:imum)? size)/i.test(text)) {
      return "샤프닝 입력 이미지가 너무 큽니다. 더 작은 이미지로 다시 시도해주세요.";
    }

    if (/(aspect[_ -]?ratio|unsupported image setting|image dimensions|width|height)/i.test(text)) {
      return "샤프닝 입력 이미지 비율이나 크기가 현재 처리 범위를 벗어났습니다. 일반적인 비율이나 크기로 다시 시도해주세요.";
    }

    if (/(400|invalid_argument|invalid image|unsupported image|image|size)/i.test(text)) {
      return providerLabel + "샤프닝 입력 이미지를 현재 요청 형식으로 처리하지 못했습니다. 이미지 소스나 설정을 다시 확인해주세요.";
    }

    return "";
  }

  function toKoreanImageErrorMessage(value, fallback, options) {
    const text = typeof value === "string" ? value.replace(/\s+/g, " ").trim() : "";
    if (!text) {
      return fallback;
    }

    if (looksLikeCorruptedImageText(text)) {
      return fallback;
    }

    if (isBoundsFitOperation(options)) {
      return text;
    }

    if (isImageExtendOperation(options)) {
      return text;
    }

    if (isOriginalSizeFitOperation(options)) {
      return text;
    }

    if (
      /(bounds-fit|auto-layout|text overlay|selected text layer|selected container|visible bounds|cannot be resized|could not resize)/i.test(
        text
      )
    ) {
      return text;
    }

    const provider = /openai/i.test(text) ? "OpenAI" : /gemini/i.test(text) ? "Gemini" : "AI";
    const operationLabel = sanitizeOperationLabel(options && options.operationLabel);

    if (/(401|403|permission|forbidden|unauthori|auth|api key|credential)/i.test(text)) {
      const detail = text.length > 260 ? text.slice(0, 260).trim() + "..." : text;
      return (
        provider +
        " 이미지 요청 권한이 거절되었습니다. API 키, 프로젝트, 결제 tier, 모델 접근 권한을 확인해 주세요." +
        (detail ? " 원문: " + detail : "")
      );
    }

    if (/(429|resource_exhausted|rate limit|quota|too many requests)/i.test(text)) {
      return provider + " image request hit a rate limit or quota. Please try again later.";
    }

    if (/(503|unavailable|high demand|overloaded|temporar|try again later|busy)/i.test(text)) {
      return provider + " image request is temporarily unavailable. Please try again later.";
    }

    if (isSharpenOperationLabel(operationLabel)) {
      const sharpenSpecificMessage = buildSharpenSpecificImageError(text, provider);
      if (sharpenSpecificMessage) {
        return sharpenSpecificMessage;
      }
    }

    if (/(payload|request size|20 ?mb|inline[_ ]data|aspect[_ -]?ratio|too large)/i.test(text)) {
      return provider + " image request is too large or uses an unsupported image setting. Try again with fewer or smaller layers.";
    }

    if (/(400|invalid_argument|unsupported|mime|format|image|size)/i.test(text)) {
      return provider + " image request format or input image is invalid. Check the request and try again.";
    }

    if (/(500|502|504|internal|backend|server)/i.test(text)) {
      return provider + " server error occurred while processing the image request. Please try again later.";
    }

    return text;
  }

  function normalizeBytes(value) {
    if (value instanceof Uint8Array) {
      return value;
    }

    if (value instanceof ArrayBuffer) {
      return new Uint8Array(value);
    }

    if (typeof ArrayBuffer !== "undefined" && typeof ArrayBuffer.isView === "function" && ArrayBuffer.isView(value)) {
      return new Uint8Array(value.buffer);
    }

    if (Array.isArray(value)) {
      return new Uint8Array(value);
    }

    return new Uint8Array(0);
  }

  function isImagePaint(fill) {
    return !!fill && fill.visible !== false && fill.type === "IMAGE";
  }

  function getFillIndicesInUiOrder(fills) {
    const indices = [];
    if (!Array.isArray(fills) || !fills.length) {
      return indices;
    }

    // Reverse paint order so the Fill panel's top-most visible image wins first.
    for (let fillIndex = fills.length - 1; fillIndex >= 0; fillIndex -= 1) {
      indices.push(fillIndex);
    }

    return indices;
  }

  function getNodeFills(node) {
    if (!node || !("fills" in node) || !Array.isArray(node.fills)) {
      return [];
    }

    return node.fills;
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children) && node.children.length > 0;
  }

  function detectImageExtension(bytes) {
    if (!bytes || typeof bytes.length !== "number" || bytes.length < 4) {
      return "bin";
    }

    if (
      bytes.length >= 8 &&
      bytes[0] === 137 &&
      bytes[1] === 80 &&
      bytes[2] === 78 &&
      bytes[3] === 71 &&
      bytes[4] === 13 &&
      bytes[5] === 10 &&
      bytes[6] === 26 &&
      bytes[7] === 10
    ) {
      return "png";
    }

    if (bytes[0] === 255 && bytes[1] === 216 && bytes[2] === 255) {
      return "jpg";
    }

    if (bytes[0] === 71 && bytes[1] === 73 && bytes[2] === 70) {
      return "gif";
    }

    if (
      bytes.length >= 12 &&
      bytes[0] === 82 &&
      bytes[1] === 73 &&
      bytes[2] === 70 &&
      bytes[3] === 70 &&
      bytes[8] === 87 &&
      bytes[9] === 69 &&
      bytes[10] === 66 &&
      bytes[11] === 80
    ) {
      return "webp";
    }

    if (bytes[0] === 66 && bytes[1] === 77) {
      return "bmp";
    }

    return "bin";
  }

  function detectImageMimeType(bytes) {
    const extension = detectImageExtension(bytes);
    if (extension === "png") {
      return "image/png";
    }
    if (extension === "jpg") {
      return "image/jpeg";
    }
    if (extension === "webp") {
      return "image/webp";
    }
    if (extension === "gif") {
      return "image/gif";
    }
    if (extension === "bmp") {
      return "image/bmp";
    }
    return "application/octet-stream";
  }

  function buildFileName(entry, extension) {
    const baseName = sanitizeFileName(entry && entry.nodeName ? entry.nodeName : "figma-image");
    const hash = entry && typeof entry.imageHash === "string" ? entry.imageHash.slice(0, 8) : "image";
    return baseName + "-" + hash + "." + extension;
  }

  function sanitizeFileName(value) {
    const source = typeof value === "string" && value ? value : "figma-image";
    const trimmed = source.replace(/[<>:"/\\|?*\u0000-\u001f]/g, " ").replace(/\s+/g, " ").trim();
    return trimmed ? trimmed.slice(0, 80) : "figma-image";
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "No selection";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return safeName(selection[0]) + " +" + (selection.length - 1) + " more";
  }

  function buildSessionId() {
    return "ai-image-upscale-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback, options) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return toKoreanImageErrorMessage(error.message, fallback, options);
    }

    if (typeof error === "string" && error.trim()) {
      return toKoreanImageErrorMessage(error, fallback, options);
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_ORIGINAL_IMAGE_DOWNLOAD_PATCH__) {
    return;
  }

  const originalOnMessage = figma.ui.onmessage;
  const RESULT_PREVIEW_LIMIT = 80;
  const USAGE_PREVIEW_LIMIT = 12;
  let isRunning = false;

  if (typeof originalOnMessage !== "function") {
    return;
  }

  figma.ui.onmessage = async (message) => {
    if (isOriginalImageDownloadMessage(message)) {
      if (isRunning) {
        postStatus({
          status: "running",
          currentCount: 0,
          totalCount: 0,
          message: "Original image search is already running.",
        });
        return;
      }

      await runOriginalImageDownload();
      return;
    }

    return originalOnMessage(message);
  };

  globalScope.__PIGMA_ORIGINAL_IMAGE_DOWNLOAD_PATCH__ = true;

  function isOriginalImageDownloadMessage(message) {
    return !!message && message.type === "run-original-image-download";
  }

  async function runOriginalImageDownload() {
    isRunning = true;

    try {
      const collection = collectOriginalImagesFromSelection();
      const totalCount = collection.uniqueEntries.length;

      if (!totalCount) {
        const emptyResult = buildResult({
          collection,
          downloadedFiles: [],
          skipped: collection.skipped,
        });

        figma.ui.postMessage({
          type: "original-image-download-result",
          result: emptyResult,
        });
        figma.notify("No original images are available to download in the selection.", { timeout: 2000 });
        return;
      }

      figma.ui.postMessage({
        type: "original-image-download-start",
        totalCount,
        totalImagePaintCount: collection.totalImagePaintCount,
      });

      const downloadedFiles = [];
      const skipped = collection.skipped.slice();

      for (let index = 0; index < collection.uniqueEntries.length; index += 1) {
        const entry = collection.uniqueEntries[index];
        postStatus({
          status: "prepare",
          currentCount: index + 1,
          totalCount,
          message: `Preparing original image for "${entry.displayName}".`,
        });

        try {
          const image = figma.getImageByHash(entry.imageHash);
          if (!image) {
            throw new Error("Could not find the original image object.");
          }

          const bytes = await image.getBytesAsync();
          const extension = detectImageExtension(bytes);
          const mimeType = detectImageMimeType(extension);
          const fileName = buildFileName(entry, index, extension);
          const fileRecord = buildFileRecord(entry, {
            index: index + 1,
            fileName,
            mimeType,
            byteLength: bytes.length,
          });

          downloadedFiles.push(fileRecord);

          figma.ui.postMessage({
            type: "original-image-download-file",
            file: Object.assign({}, fileRecord, {
              totalCount,
              bytes,
            }),
          });
        } catch (error) {
          skipped.push({
            nodeId: entry.nodeId,
            nodeName: entry.nodeName,
            imageHash: entry.imageHash,
            path: entry.path,
            reason: normalizeErrorMessage(error, "Could not read the original image bytes."),
          });
        }
      }

      const result = buildResult({
        collection,
        downloadedFiles,
        skipped,
      });

      figma.ui.postMessage({
        type: "original-image-download-result",
        result,
      });
      notifyResult(result);
    } catch (error) {
      const message = normalizeErrorMessage(error, "Could not prepare original image saving.");
      figma.ui.postMessage({
        type: "original-image-download-error",
        message,
      });
      figma.notify(message, { error: true, timeout: 2400 });
    } finally {
      isRunning = false;
    }
  }

  function collectOriginalImagesFromSelection() {
    const selection = Array.from(figma.currentPage.selection || []);
    if (!selection.length) {
      throw new Error("Select a frame, group, or image layer first.");
    }

    const uniqueEntries = [];
    const entriesByHash = {};
    const skipped = [];
    let totalFillCount = 0;
    let totalStrokeCount = 0;
    let totalImagePaintCount = 0;
    let duplicateImagePaintCount = 0;

    for (let rootIndex = 0; rootIndex < selection.length; rootIndex += 1) {
      const root = selection[rootIndex];
      const stack = [
        {
          node: root,
          path: safeName(root),
        },
      ];

      while (stack.length > 0) {
        const current = stack.pop();
        const node = current && current.node;
        if (!node || node.removed) {
          continue;
        }

        const imagePaints = collectNodeImagePaintEntries(node);
        for (let paintIndex = 0; paintIndex < imagePaints.length; paintIndex += 1) {
          const paintEntry = imagePaints[paintIndex];
          totalImagePaintCount += 1;
          if (paintEntry.paintKind === "fill") {
            totalFillCount += 1;
          } else if (paintEntry.paintKind === "stroke") {
            totalStrokeCount += 1;
          }

          const usage = {
            nodeId: node.id,
            nodeName: safeName(node),
            path: current.path,
            paintKind: paintEntry.paintKind,
            paintIndex: paintEntry.paintIndex,
          };

          if (!paintEntry.imageHash || typeof paintEntry.imageHash !== "string") {
            skipped.push({
              nodeId: node.id,
              nodeName: safeName(node),
              imageHash: "",
              path: current.path,
              paintKind: paintEntry.paintKind,
              paintIndex: paintEntry.paintIndex,
              reason: `${paintEntry.paintKind} IMAGE paint has no imageHash, so it was skipped.`,
            });
            continue;
          }

          const existing = entriesByHash[paintEntry.imageHash];
          if (existing) {
            existing.usageCount += 1;
            existing.usages.push(usage);
            existing.layerNames = collectUniqueStrings(existing.layerNames.concat([usage.nodeName]));
            existing.paintKinds = collectUniqueStrings(existing.paintKinds.concat([usage.paintKind]));
            duplicateImagePaintCount += 1;
            continue;
          }

          const entry = {
            imageHash: paintEntry.imageHash,
            nodeId: node.id,
            nodeName: safeName(node),
            displayName: safeName(node),
            path: current.path,
            usageCount: 1,
            usages: [usage],
            layerNames: [usage.nodeName],
            paintKinds: [usage.paintKind],
          };

          entriesByHash[paintEntry.imageHash] = entry;
          uniqueEntries.push(entry);
        }

        if (!hasChildren(node)) {
          continue;
        }

        for (let childIndex = node.children.length - 1; childIndex >= 0; childIndex -= 1) {
          const child = node.children[childIndex];
          stack.push({
            node: child,
            path: current.path + " / " + safeName(child),
          });
        }
      }
    }

    return {
      selection,
      totalFillCount,
      totalStrokeCount,
      totalImagePaintCount,
      duplicateFillCount: duplicateImagePaintCount,
      duplicateImagePaintCount,
      uniqueEntries,
      skipped,
    };
  }

  function buildFileRecord(entry, options) {
    const usages = Array.isArray(entry.usages) ? entry.usages : [];
    return {
      index: options.index,
      fileName: options.fileName,
      mimeType: options.mimeType,
      byteLength: options.byteLength,
      imageHash: entry.imageHash,
      nodeId: entry.nodeId,
      nodeName: entry.nodeName,
      path: entry.path,
      usageCount: entry.usageCount,
      usages: usages.slice(0, USAGE_PREVIEW_LIMIT),
      layerNames: collectUniqueStrings(entry.layerNames).slice(0, USAGE_PREVIEW_LIMIT),
      paintKinds: collectUniqueStrings(entry.paintKinds),
    };
  }

  function buildResult(options) {
    const collection = options && options.collection ? options.collection : {};
    const selection = Array.isArray(collection.selection) ? collection.selection : [];
    const downloadedFiles = Array.isArray(options.downloadedFiles) ? options.downloadedFiles : [];
    const skipped = Array.isArray(options.skipped) ? options.skipped : [];

    return {
      processedAt: new Date().toISOString(),
      summary: {
        selectionLabel: formatSelectionLabel(selection),
        rootCount: selection.length,
        totalFillCount: normalizeCount(collection.totalFillCount),
        totalStrokeCount: normalizeCount(collection.totalStrokeCount),
        totalImagePaintCount: normalizeCount(collection.totalImagePaintCount),
        uniqueCandidateCount: normalizeCount(collection.uniqueEntries && collection.uniqueEntries.length),
        duplicateFillCount: normalizeCount(collection.duplicateImagePaintCount),
        duplicateImagePaintCount: normalizeCount(collection.duplicateImagePaintCount),
        downloadedCount: downloadedFiles.length,
        skippedCount: skipped.length,
      },
      files: downloadedFiles.slice(0, RESULT_PREVIEW_LIMIT),
      skipped: skipped.slice(0, RESULT_PREVIEW_LIMIT),
    };
  }

  function notifyResult(result) {
    const summary = result && result.summary ? result.summary : {};
    const downloadedCount = summary.downloadedCount || 0;
    const skippedCount = summary.skippedCount || 0;
    const duplicateCount = summary.duplicateImagePaintCount || summary.duplicateFillCount || 0;

    if (!downloadedCount) {
      figma.notify("No original images are available to download.", { timeout: 2000 });
      return;
    }

    let message = `Found ${downloadedCount} original image${downloadedCount === 1 ? "" : "s"}. You can download from the list.`;
    if (duplicateCount > 0) {
      message += ` ${duplicateCount} duplicate use${duplicateCount === 1 ? "" : "s"} were prepared only once.`;
    }
    if (skippedCount > 0) {
      message += ` ${skippedCount} item${skippedCount === 1 ? "" : "s"} skipped.`;
    }
    figma.notify(message, { timeout: 2600 });
  }

  function postStatus(status) {
    figma.ui.postMessage({
      type: "original-image-download-status",
      status: status && status.status ? status.status : "running",
      currentCount:
        status && typeof status.currentCount === "number" && Number.isFinite(status.currentCount) ? status.currentCount : 0,
      totalCount:
        status && typeof status.totalCount === "number" && Number.isFinite(status.totalCount) ? status.totalCount : 0,
      message: status && typeof status.message === "string" ? status.message : "",
    });
  }

  function collectNodeImagePaintEntries(node) {
    const entries = [];
    appendImagePaintEntries(entries, getNodePaints(node, "fills"), "fill");
    appendImagePaintEntries(entries, getNodePaints(node, "strokes"), "stroke");
    return entries;
  }

  function appendImagePaintEntries(entries, paints, paintKind) {
    for (let index = 0; index < paints.length; index += 1) {
      const paint = paints[index];
      if (!paint || paint.type !== "IMAGE") {
        continue;
      }

      entries.push({
        paintKind,
        paintIndex: index,
        imageHash: paint.imageHash,
      });
    }
  }

  function getNodePaints(node, propertyName) {
    if (!node || !(propertyName in node) || !Array.isArray(node[propertyName])) {
      return [];
    }

    return node[propertyName];
  }

  function hasChildren(node) {
    return !!node && "children" in node && Array.isArray(node.children) && node.children.length > 0;
  }

  function detectImageExtension(bytes) {
    if (!bytes || typeof bytes.length !== "number" || bytes.length < 4) {
      return "bin";
    }

    if (
      bytes.length >= 8 &&
      bytes[0] === 137 &&
      bytes[1] === 80 &&
      bytes[2] === 78 &&
      bytes[3] === 71 &&
      bytes[4] === 13 &&
      bytes[5] === 10 &&
      bytes[6] === 26 &&
      bytes[7] === 10
    ) {
      return "png";
    }

    if (bytes[0] === 255 && bytes[1] === 216 && bytes[2] === 255) {
      return "jpg";
    }

    if (bytes[0] === 71 && bytes[1] === 73 && bytes[2] === 70) {
      return "gif";
    }

    if (
      bytes.length >= 12 &&
      bytes[0] === 82 &&
      bytes[1] === 73 &&
      bytes[2] === 70 &&
      bytes[3] === 70 &&
      bytes[8] === 87 &&
      bytes[9] === 69 &&
      bytes[10] === 66 &&
      bytes[11] === 80
    ) {
      return "webp";
    }

    if (bytes[0] === 66 && bytes[1] === 77) {
      return "bmp";
    }

    return "bin";
  }

  function detectImageMimeType(extension) {
    if (extension === "png") {
      return "image/png";
    }

    if (extension === "jpg") {
      return "image/jpeg";
    }

    if (extension === "gif") {
      return "image/gif";
    }

    if (extension === "webp") {
      return "image/webp";
    }

    if (extension === "bmp") {
      return "image/bmp";
    }

    return "application/octet-stream";
  }

  function buildFileName(entry, index, extension) {
    const baseName = sanitizeFileName(entry && entry.displayName ? entry.displayName : `figma-image-${index + 1}`);
    const hash = entry && typeof entry.imageHash === "string" ? entry.imageHash.slice(0, 8) : "image";
    return `${baseName}-${hash}.${extension}`;
  }

  function sanitizeFileName(value) {
    const source = typeof value === "string" && value ? value : "figma-image";
    const trimmed = source.replace(/[<>:"/\\|?*\u0000-\u001f]/g, " ").replace(/\s+/g, " ").trim();
    return trimmed ? trimmed.slice(0, 80) : "figma-image";
  }

  function formatSelectionLabel(selection) {
    if (!selection.length) {
      return "No selection";
    }

    if (selection.length === 1) {
      return safeName(selection[0]);
    }

    return `${safeName(selection[0])} + ${selection.length - 1} more`;
  }

  function collectUniqueStrings(values) {
    const result = [];
    const seen = {};
    const source = Array.isArray(values) ? values : [];
    for (let index = 0; index < source.length; index += 1) {
      const value = typeof source[index] === "string" ? source[index].trim() : "";
      if (!value || seen[value]) {
        continue;
      }
      seen[value] = true;
      result.push(value);
    }
    return result;
  }

  function normalizeCount(value) {
    return typeof value === "number" && Number.isFinite(value) && value > 0 ? Math.floor(value) : 0;
  }

  function safeName(node) {
    if (node && typeof node.name === "string" && node.name.trim()) {
      return node.name.trim();
    }

    if (node && typeof node.type === "string" && node.type.trim()) {
      return node.type.trim();
    }

    return "Unnamed";
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }

    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }

    return fallback;
  }
})();

;(() => {
  const globalScope = typeof globalThis !== "undefined" ? globalThis : {};
  if (globalScope.__PIGMA_AI_DESIGN_CHAT_PATCH__) {
    return;
  }

  const ANNOTATION_CATEGORY_LABEL = "Pigma Design Chat";
  const ANNOTATION_CATEGORY_COLOR = "yellow";
  const ANNOTATION_LABEL_PREFIX = "[Design Chat]";
  const TEXT_PREVIEW_LIMIT = 320;
  const TEXT_PREVIEW_EDGE_LENGTH = 10;
  const TEXT_CONTENT_LIMIT = 2400;
  const SOURCE_EXPORT_EDGE = 1536;
  const originalOnMessage = typeof figma.ui.onmessage === "function" ? figma.ui.onmessage : null;

  const designChatOnMessage = async (message) => {
    if (isDesignChatMessage(message)) {
      if (message.type === "request-ai-design-chat-selection") {
        postSelectionState();
        return;
      }

      if (message.type === "request-ai-design-chat-source") {
        await handleSourceRequest(message);
        return;
      }

      if (message.type === "apply-ai-design-chat-annotation") {
        await handleAnnotationRequest(message);
        return;
      }
    }

    if (typeof originalOnMessage === "function") {
      return originalOnMessage(message);
    }
  };

  globalScope.__PIGMA_AI_DESIGN_CHAT_PATCH__ = true;

  figma.on("selectionchange", () => {
    postSelectionState();
  });
  figma.on("currentpagechange", () => {
    postSelectionState();
  });

  figma.ui.onmessage = designChatOnMessage;

  postSelectionState();

  function isDesignChatMessage(message) {
    return (
      !!message &&
      (message.type === "request-ai-design-chat-selection" ||
        message.type === "request-ai-design-chat-source" ||
        message.type === "apply-ai-design-chat-annotation")
    );
  }

  function postSelectionState() {
    let selection = null;
    try {
      selection = buildSelectionState();
    } catch (error) {
      selection = buildSelectionFallbackState();
    }
    figma.ui.postMessage({
      type: "ai-design-chat-selection",
      selection: selection,
    });
    figma.ui.postMessage({
      type: "selection-state",
      state: buildPsdSelectionStateFromDesignChat(selection),
    });
  }

  function buildPsdSelectionStateFromDesignChat(selection) {
    const count = Math.max(0, Number(selection && selection.selectionCount) || 0);
    const width = Math.max(0, Number(selection && selection.width) || 0);
    const height = Math.max(0, Number(selection && selection.height) || 0);
    const label = sanitizeText(selection && selection.selectionLabel);
    const typeLabel = sanitizeText(selection && selection.selectionTypeLabel);
    const ready = !!(selection && selection.ready && count > 0);
    return {
      ready,
      selectionId: count === 1 ? sanitizeText(selection && selection.selectionSignature) : null,
      selectionCount: count,
      selectionName: ready ? label || "Selection" : "",
      selectionType: ready ? typeLabel || "Selection" : null,
      summary: ready ? `"${label || "Selection"}" is ready to export.` : "Select one or more frames, groups, or layers to export.",
      detail: ready
        ? "AI selection bridge loaded the current Figma selection for PSD export."
        : "The exporter is waiting for a Figma selection.",
      documentWidth: ready && width > 0 ? width : null,
      documentHeight: ready && height > 0 ? height : null,
      exportNodeCount: ready ? count : 0,
      editableTextCount: 0,
      preservedGroupCount: 0,
      warnings: [],
      analysisPending: ready,
    };
  }

  async function handleSourceRequest(message) {
    const clientRequestId = sanitizeText(message && message.clientRequestId);
    try {
      const roots = getSelectedRoots();
      const exportRoots = roots.filter((node) => canExportNode(node));
      const selection = buildSelectionStateFromRoots(roots);
      if (!selection.ready) {
        throw new Error("프레임, 이미지, 텍스트를 먼저 선택해 주세요.");
      }

      if (!exportRoots.length) {
        throw new Error("Could not export the current selection.");
      }

      const exportSelection = buildSelectionStateFromRoots(exportRoots);
      const bytes =
        exportRoots.length === 1
          ? await exportSingleRoot(exportRoots[0], exportSelection)
          : exportSelection.bounds
            ? await exportCombinedRoots(
                exportRoots,
                exportSelection.bounds,
                exportSelection.width || exportSelection.bounds.width,
                exportSelection.height || exportSelection.bounds.height
              )
            : await exportSingleRoot(exportRoots[0], exportSelection);
      if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
        throw new Error("선택 영역 캡처 결과가 비어 있습니다.");
      }

      figma.ui.postMessage({
        type: "ai-design-chat-source-ready",
        clientRequestId,
        selection,
        image: {
          bytes,
          mimeType: "image/png",
          fileName: buildFileName(selection.selectionLabel || "design-chat") + ".png",
        },
      });
    } catch (error) {
      figma.ui.postMessage({
        type: "ai-design-chat-source-error",
        clientRequestId,
        message: normalizeErrorMessage(error, "선택 캡처를 준비하지 못했습니다."),
      });
    }
  }

  async function handleAnnotationRequest(message) {
    const clientRequestId = sanitizeText(message && message.clientRequestId);
    const messageId = sanitizeText(message && message.messageId);
    try {
      const annotationText = clipText(message && message.annotationText, 120);
      if (!annotationText) {
        throw new Error("주석으로 남길 텍스트가 없습니다.");
      }

      const roots = getSelectedRoots();
      const selection = buildSelectionStateFromRoots(roots);
      if (!selection.ready) {
        throw new Error("주석을 남길 선택이 없습니다.");
      }

      const requestedSignature = sanitizeText(message && message.selectionSignature);
      if (requestedSignature && selection.selectionSignature !== requestedSignature) {
        throw new Error("선택이 바뀌어서 주석을 남길 수 없습니다. 다시 질문해 주세요.");
      }

      const annotationNodes = roots.filter((node) => supportsAnnotationsOnNode(node));
      if (!annotationNodes.length) {
        throw new Error("현재 선택에는 주석을 남길 수 있는 노드가 없습니다.");
      }

      const category = await ensureAnnotationCategory(ANNOTATION_CATEGORY_COLOR);
      let annotationCount = 0;

      for (const node of annotationNodes) {
        const existing = Array.isArray(node.annotations) ? Array.from(node.annotations) : [];
        const preserved = existing.filter((annotation) => !isManagedAnnotation(annotation, category));
        node.annotations = preserved.concat(buildAnnotation(annotationText, category));
        annotationCount += 1;
      }

      figma.ui.postMessage({
        type: "ai-design-chat-annotation-result",
        clientRequestId,
        messageId,
        selectionSignature: selection.selectionSignature,
        annotationCount,
        annotatedNodeCount: annotationNodes.length,
      });
      figma.notify(`디자인 채팅 주석 ${annotationNodes.length}개를 남겼습니다.`, { timeout: 1800 });
    } catch (error) {
      const messageText = normalizeErrorMessage(error, "주석을 남기지 못했습니다.");
      figma.ui.postMessage({
        type: "ai-design-chat-annotation-error",
        clientRequestId,
        messageId,
        message: messageText,
      });
      figma.notify(messageText, { error: true, timeout: 2200 });
    }
  }

  function buildSelectionState() {
    return buildSelectionStateFromRoots(getSelectedRoots());
  }

  function buildSelectionStateFromRoots(roots) {
    const selectionRoots = Array.isArray(roots) ? roots.filter(Boolean) : [];
    const bounds = combineBounds(selectionRoots.map((node) => getNodeBoundsSafe(node)).filter(Boolean));
    const typeLabels = uniqueStrings(selectionRoots.map((node) => formatNodeType(getNodeTypeSafe(node))), 4);
    const sourceType = resolveSourceType(selectionRoots);
    const captureMode = selectionRoots.length > 1 ? "combined" : sourceType === "text" ? "text" : selectionRoots.length === 1 ? "single" : "idle";

    if (!selectionRoots.length) {
      return {
        ready: false,
        selectionSignature: getSelectionSignature([], ""),
        selectionLabel: "",
        selectionCount: 0,
        selectionTypeLabel: "",
        captureMode: "idle",
        captureModeLabel: "대기",
        width: 0,
        height: 0,
        textPreview: "",
        textContent: "",
        hint: "프레임, 이미지, 텍스트를 선택하면 대화를 시작할 수 있습니다.",
        roots: [],
        annotationTargetCount: 0,
        sourceType: "visual",
        bounds: null,
      };
    }

    const textContent = collectTextContent(selectionRoots, sourceType === "text");

    return {
      ready: true,
      selectionSignature: getSelectionSignature(selectionRoots, textContent),
      selectionLabel: formatSelectionLabel(selectionRoots, textContent),
      selectionCount: selectionRoots.length,
      selectionTypeLabel: typeLabels.join(" · "),
      captureMode,
      captureModeLabel:
        captureMode === "combined" ? "통합 화면" : captureMode === "text" ? "텍스트 단일" : captureMode === "single" ? "단일 선택" : "대기",
      width: bounds ? roundPixel(bounds.width) : 0,
      height: bounds ? roundPixel(bounds.height) : 0,
      textPreview: abbreviateMiddleText(textContent, TEXT_PREVIEW_EDGE_LENGTH),
      textContent,
      hint:
        captureMode === "combined"
          ? "다중 선택을 한 화면으로 묶어 캡처합니다."
          : sourceType === "text"
            ? "선택한 텍스트만 캡처해서 질문합니다."
            : "현재 선택을 그대로 캡처해서 질문합니다.",
      roots: selectionRoots.map((node) => ({
        id: node.id,
        name: safeName(node),
        type: getNodeTypeSafe(node),
      })),
      annotationTargetCount: selectionRoots.filter((node) => supportsAnnotationsOnNode(node)).length,
      sourceType,
      bounds,
    };
  }

  function getSelectedRoots() {
    const selection = Array.from(figma.currentPage.selection || []).filter((node) => isSelectableNode(node));
    if (!selection.length) {
      return [];
    }

    const selectedIds = new Set(selection.map((node) => node.id));
    const roots = selection.filter((node) => !hasSelectedAncestor(node, selectedIds));
    try {
      return roots.sort(compareNodeOrder);
    } catch (error) {
      return roots;
    }
  }

  function isSelectableNode(node) {
    return !!node && node.removed !== true && typeof node.id === "string" && typeof node.type === "string";
  }

  function canExportNode(node) {
    return isSelectableNode(node) && typeof node.exportAsync === "function";
  }

  function hasSelectedAncestor(node, selectedIds) {
    let current = node ? node.parent : null;
    while (current && current.type !== "PAGE" && current.type !== "DOCUMENT") {
      if (selectedIds.has(current.id)) {
        return true;
      }
      current = current.parent;
    }
    return false;
  }

  function compareNodeOrder(left, right) {
    const leftPath = getNodeIndexPath(left);
    const rightPath = getNodeIndexPath(right);
    const length = Math.max(leftPath.length, rightPath.length);
    for (let index = 0; index < length; index += 1) {
      const leftValue = index < leftPath.length ? leftPath[index] : -1;
      const rightValue = index < rightPath.length ? rightPath[index] : -1;
      if (leftValue !== rightValue) {
        return leftValue - rightValue;
      }
    }
    return safeName(left).localeCompare(safeName(right));
  }

  function getNodeIndexPath(node) {
    const path = [];
    let current = node;
    while (current && current.parent && Array.isArray(current.parent.children)) {
      path.unshift(current.parent.children.indexOf(current));
      current = current.parent;
      if (current.type === "PAGE") {
        break;
      }
    }
    return path;
  }

  function resolveSourceType(roots) {
    const items = Array.isArray(roots) ? roots : [];
    if (!items.length) {
      return "visual";
    }
    const allText = items.every((node) => getNodeTypeSafe(node) === "TEXT");
    if (allText) {
      return "text";
    }
    const hasText = items.some((node) => getNodeTypeSafe(node) === "TEXT");
    return hasText ? "mixed" : "visual";
  }

  function collectTextPreview(roots, directOnly) {
    return abbreviateMiddleText(collectTextContent(roots, directOnly), TEXT_PREVIEW_EDGE_LENGTH);
  }

  function collectTextContent(roots, directOnly) {
    const snippets = [];
    let totalLength = 0;
    const queue = Array.isArray(roots) ? roots.slice() : [];

    while (queue.length > 0 && totalLength < TEXT_CONTENT_LIMIT) {
      const node = queue.shift();
      if (!node || node.visible === false) {
        continue;
      }

      if (getNodeTypeSafe(node) === "TEXT" && typeof node.characters === "string") {
        const snippet = compactText(node.characters);
        if (snippet) {
          snippets.push(snippet);
          totalLength += snippet.length;
        }
      }

      const children = getNodeChildrenSafe(node);
      if (!directOnly && children.length) {
        for (const child of children) {
          queue.push(child);
        }
      }
    }

    return clipText(snippets.join(" · "), TEXT_CONTENT_LIMIT);
  }

  async function exportSingleRoot(root, selection) {
    const exportOptions = {
      format: "PNG",
      useAbsoluteBounds: true,
    };
    const constraint = buildExportConstraint(selection.width, selection.height);
    if (constraint) {
      exportOptions.constraint = constraint;
    }
    return await root.exportAsync(exportOptions);
  }

  async function exportCombinedRoots(roots, bounds, width, height) {
    const tempFrame = figma.createFrame();
    tempFrame.name = "__Pigma Design Chat Capture__";
    tempFrame.x = roundPixel(bounds.x + width + 4000);
    tempFrame.y = roundPixel(bounds.y);
    tempFrame.resize(Math.max(1, roundPixel(width)), Math.max(1, roundPixel(height)));
    tempFrame.fills = [];
    tempFrame.strokes = [];
    tempFrame.clipsContent = true;
    figma.currentPage.appendChild(tempFrame);

    let placedCount = 0;

    try {
      for (const node of roots) {
        const nodeBounds = getNodeBounds(node);
        if (!nodeBounds) {
          continue;
        }
        const bytes = await node.exportAsync({
          format: "PNG",
          useAbsoluteBounds: true,
        });
        if (!bytes || typeof bytes.length !== "number" || bytes.length <= 0) {
          continue;
        }
        const image = figma.createImage(bytes);
        const rect = figma.createRectangle();
        rect.name = safeName(node);
        rect.x = nodeBounds.x - bounds.x;
        rect.y = nodeBounds.y - bounds.y;
        rect.resize(Math.max(1, nodeBounds.width), Math.max(1, nodeBounds.height));
        rect.strokes = [];
        rect.fills = [
          {
            type: "IMAGE",
            scaleMode: "FILL",
            imageHash: image.hash,
          },
        ];
        tempFrame.appendChild(rect);
        placedCount += 1;
      }

      if (!placedCount) {
        throw new Error("통합 캡처에 사용할 선택 레이어를 찾지 못했습니다.");
      }

      const exportOptions = {
        format: "PNG",
        useAbsoluteBounds: false,
      };
      const constraint = buildExportConstraint(width, height);
      if (constraint) {
        exportOptions.constraint = constraint;
      }
      return await tempFrame.exportAsync(exportOptions);
    } finally {
      tempFrame.remove();
    }
  }

  function buildExportConstraint(width, height) {
    const maxEdge = Math.max(Number(width) || 0, Number(height) || 0);
    if (!(maxEdge > SOURCE_EXPORT_EDGE)) {
      return null;
    }
    if ((Number(width) || 0) >= (Number(height) || 0)) {
      return {
        type: "WIDTH",
        value: SOURCE_EXPORT_EDGE,
      };
    }
    return {
      type: "HEIGHT",
      value: SOURCE_EXPORT_EDGE,
    };
  }

  function getSelectionSignature(selection, textContent) {
    const items = Array.from(selection || [])
      .map((node) => {
        const bounds = getNodeBoundsSafe(node);
        const boundsKey = bounds
          ? [
              roundSignatureNumber(bounds.x),
              roundSignatureNumber(bounds.y),
              roundSignatureNumber(bounds.width),
              roundSignatureNumber(bounds.height),
            ].join(":")
          : "";
        return [node.id, getNodeTypeSafe(node), hashSignatureText(safeName(node)), boundsKey, getNodeChildrenSafe(node).length].join("@");
      })
      .sort();
    const text = sanitizeText(textContent);
    return `${figma.currentPage.id}:${hashSignatureText(items.join("|"))}:${text.length}:${hashSignatureText(text)}`;
  }

  function roundSignatureNumber(value) {
    return Math.round((Number(value) || 0) * 100) / 100;
  }

  function hashSignatureText(value) {
    const text = String(value || "");
    let hash = 2166136261;
    for (let index = 0; index < text.length; index += 1) {
      hash ^= text.charCodeAt(index);
      hash = Math.imul(hash, 16777619);
    }
    return (hash >>> 0).toString(36);
  }

  function getNodeBounds(node) {
    if (node && node.absoluteRenderBounds) {
      return cloneBounds(node.absoluteRenderBounds);
    }
    if (node && node.absoluteBoundingBox) {
      return cloneBounds(node.absoluteBoundingBox);
    }
    if (
      node &&
      typeof node.x === "number" &&
      typeof node.y === "number" &&
      typeof node.width === "number" &&
      typeof node.height === "number"
    ) {
      return {
        x: node.x,
        y: node.y,
        width: Math.max(1, node.width),
        height: Math.max(1, node.height),
      };
    }
    return null;
  }

  function getNodeBoundsSafe(node) {
    try {
      return getNodeBounds(node);
    } catch (error) {
      return null;
    }
  }

  function cloneBounds(bounds) {
    return {
      x: Number(bounds.x) || 0,
      y: Number(bounds.y) || 0,
      width: Math.max(1, Number(bounds.width) || 1),
      height: Math.max(1, Number(bounds.height) || 1),
    };
  }

  function combineBounds(boundsList) {
    if (!Array.isArray(boundsList) || !boundsList.length) {
      return null;
    }
    let left = boundsList[0].x;
    let top = boundsList[0].y;
    let right = boundsList[0].x + boundsList[0].width;
    let bottom = boundsList[0].y + boundsList[0].height;

    for (let index = 1; index < boundsList.length; index += 1) {
      const bounds = boundsList[index];
      left = Math.min(left, bounds.x);
      top = Math.min(top, bounds.y);
      right = Math.max(right, bounds.x + bounds.width);
      bottom = Math.max(bottom, bounds.y + bounds.height);
    }

    return {
      x: left,
      y: top,
      width: Math.max(1, right - left),
      height: Math.max(1, bottom - top),
    };
  }

  async function ensureAnnotationCategory(requestedColor) {
    if (!figma.annotations || typeof figma.annotations.getAnnotationCategoriesAsync !== "function") {
      return null;
    }

    try {
      const categories = await figma.annotations.getAnnotationCategoriesAsync();
      const existing = Array.isArray(categories)
        ? categories.find((category) => category && category.label === ANNOTATION_CATEGORY_LABEL)
        : null;
      const color = typeof requestedColor === "string" && requestedColor.trim() ? requestedColor.trim().toLowerCase() : ANNOTATION_CATEGORY_COLOR;

      if (existing) {
        if (typeof existing.setColor === "function" && existing.color !== color) {
          existing.setColor(color);
        }
        return existing;
      }

      if (typeof figma.annotations.addAnnotationCategoryAsync !== "function") {
        return null;
      }

      return await figma.annotations.addAnnotationCategoryAsync({
        label: ANNOTATION_CATEGORY_LABEL,
        color,
      });
    } catch (error) {
      return null;
    }
  }

  function buildAnnotation(text, category) {
    const annotation = {
      label: `${ANNOTATION_LABEL_PREFIX} ${clipText(text, 120)}`,
    };
    if (category && category.id) {
      annotation.categoryId = category.id;
    }
    return annotation;
  }

  function isManagedAnnotation(annotation, category) {
    if (!annotation || typeof annotation !== "object") {
      return false;
    }
    if (category && category.id && annotation.categoryId === category.id) {
      return true;
    }
    const label =
      typeof annotation.label === "string" ? annotation.label : typeof annotation.labelMarkdown === "string" ? annotation.labelMarkdown : "";
    return typeof label === "string" && label.startsWith(ANNOTATION_LABEL_PREFIX);
  }

  function supportsAnnotationsOnNode(node) {
    try {
      return !!node && Array.isArray(node.annotations);
    } catch (error) {
      return false;
    }
  }

  function formatSelectionLabel(roots, textContent) {
    if (!Array.isArray(roots) || !roots.length) {
      return "";
    }
    const textPreview = abbreviateMiddleText(textContent, TEXT_PREVIEW_EDGE_LENGTH);
    if (textPreview && roots.every((node) => getNodeTypeSafe(node) === "TEXT")) {
      return textPreview;
    }
    if (roots.length === 1) {
      return safeName(roots[0]);
    }
    return `${safeName(roots[0])} 외 ${roots.length - 1}개`;
  }

  function formatNodeType(type) {
    switch (String(type || "").toUpperCase()) {
      case "FRAME":
        return "프레임";
      case "GROUP":
        return "그룹";
      case "TEXT":
        return "텍스트";
      case "SECTION":
        return "섹션";
      case "COMPONENT":
        return "컴포넌트";
      case "COMPONENT_SET":
        return "컴포넌트 세트";
      case "INSTANCE":
        return "인스턴스";
      case "VECTOR":
        return "벡터";
      case "RECTANGLE":
        return "사각형";
      case "ELLIPSE":
        return "원형";
      default:
        return String(type || "레이어");
    }
  }

  function uniqueStrings(values, limit) {
    const seen = new Set();
    const result = [];
    for (const value of Array.isArray(values) ? values : []) {
      const text = typeof value === "string" ? value.trim() : "";
      if (!text || seen.has(text)) {
        continue;
      }
      seen.add(text);
      result.push(text);
      if (limit && result.length >= limit) {
        break;
      }
    }
    return result;
  }

  function safeName(node) {
    try {
      if (node && typeof node.name === "string" && node.name.trim()) {
        return node.name.trim();
      }
    } catch (error) {}
    try {
      return String((node && node.type) || "Layer");
    } catch (error) {
      return "Layer";
    }
  }

  function getNodeTypeSafe(node) {
    try {
      return String((node && node.type) || "UNKNOWN");
    } catch (error) {
      return "UNKNOWN";
    }
  }

  function getNodeChildrenSafe(node) {
    try {
      return Array.isArray(node && node.children) ? node.children : [];
    } catch (error) {
      return [];
    }
  }

  function buildSelectionFallbackState() {
    const roots = Array.from(figma.currentPage.selection || []).filter((node) => isSelectableNode(node));
    return buildSelectionStateFromRoots(roots);
  }

  function buildFileName(value) {
    const source = sanitizeText(value) || "design-chat";
    return source.replace(/[<>:"/\\|?*\u0000-\u001f]/g, " ").replace(/\s+/g, " ").trim().slice(0, 80) || "design-chat";
  }

  function clipText(value, maxLength) {
    const text = sanitizeText(value);
    if (!text) {
      return "";
    }
    const limit = Math.max(1, Number(maxLength) || 0);
    if (!limit || text.length <= limit) {
      return text;
    }
    return `${text.slice(0, Math.max(1, limit - 1)).trim()}...`;
  }

  function abbreviateMiddleText(value, edgeLength) {
    const text = sanitizeText(value);
    if (!text) {
      return "";
    }
    const edge = Math.max(1, Number(edgeLength) || TEXT_PREVIEW_EDGE_LENGTH);
    if (text.length <= edge * 2 + 4) {
      return text;
    }
    return `${text.slice(0, edge).trim()}....${text.slice(-edge).trim()}`;
  }

  function sanitizeText(value) {
    return String(value || "").replace(/\s+/g, " ").trim();
  }

  function compactText(value) {
    return String(value || "").replace(/\s+/g, " ").trim();
  }

  function roundPixel(value) {
    return Math.max(1, Math.round(Number(value) || 0));
  }

  function normalizeErrorMessage(error, fallback) {
    if (error && typeof error === "object" && typeof error.message === "string" && error.message.trim()) {
      return error.message.trim();
    }
    if (typeof error === "string" && error.trim()) {
      return error.trim();
    }
    return fallback;
  }
})();
